---
phase: quick-260723-j56
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/components/intake/NextStepBanner.tsx
  - frontend/src/components/intake/HandoffBlock.tsx
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/fr/admin.json
  - frontend/src/locales/en/admin.json
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/en/intake.json
autonomous: true
requirements: [QUICK-J56]

must_haves:
  truths:
    - "The persistent 'end of platform scope' note no longer renders on decomposed/in_research/delivered intakes"
    - "No 'research happens outside this system' / 'stops at decomposed' user-visible string remains in the admin UI"
    - "The dead HandoffBlock component and its handoff.* i18n namespace are gone"
    - "The dead onStartManualResearch handler + prop plumbing are removed"
    - "The status-unavailable toast reads cleanly without the '(stops at decomposed)' parenthetical"
    - "The History trigger sits in the page header next to the Intake-info button and no longer occupies its own workflow-card row"
    - "npx tsc --noEmit and npm run build both pass"
  artifacts:
    - path: "frontend/src/routes/admin.pulse.intakes.$id.tsx"
      provides: "Header History button, no scopeNote block, no manual-research plumbing"
    - path: "frontend/src/locales/en/admin.json"
      provides: "Reworded statusUnavailable, no scopeNote/researchOutOfScope keys"
  key_links:
    - from: "admin.pulse.intakes.$id.tsx header actions row"
      to: "toggleHistory + history Sheet"
      via: "onClick={toggleHistory}"
      pattern: "onClick=\\{toggleHistory\\}"
---

<objective>
Purge outdated "research is out of scope / happens outside this system / stops at decomposed"
messaging from the admin frontend — it is factually wrong since Phases 13–16 brought Tribunal
deep-research in-platform (triggerResearch → research_runs → ResearchRunProgress). Also relocate the
History trigger from its own workflow-card row into the page header, immediately beside the
Intake-info button, restyled to the header button grammar.

Purpose: Stop the UI from lying to the operator about where research runs and where scope ends.
Output: Cleaned locale files (3× admin.json, 3× intake.json), a slimmed route file, a slimmed
NextStepBanner prop contract, and a deleted dead HandoffBlock component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Verified against current code 2026-07-23. Line numbers are approximate — re-confirm before editing. -->

Route: frontend/src/routes/admin.pulse.intakes.$id.tsx
- ~L548:  toast.error(t("intakeDetail.toast.statusUnavailable"))  — KEEP call, only reword the string
- ~L784–786: `const onStartManualResearch = async () => { toast.message(t("intakeDetail.toast.researchOutOfScope")); };`  — DELETE handler
- ~L1034–1041: page-header actions row `<div className="flex flex-wrap items-center gap-2">` containing the Intake-info `<button onClick={() => setInfoModalOpen(true)} className="border border-ink bg-paper px-3 py-1.5 font-mono text-xs uppercase tracking-wider text-ink hover:border-2">{t("intakeDetail.info.openButton")}</button>`  — INSERT History button as sibling here
- ~L1160: `onStartManualResearch={onStartManualResearch}` prop pass to NextStepBanner  — DELETE
- ~L1174–1187: workflow-card History row `<div className="border-t border-ink/10 px-6 py-4"><button onClick={toggleHistory} …><Clock …/>{t("intakeDetail.history.title")}{skillRuns && skillRuns.length > 0 && (<span>({skillRuns.length})</span>)}</button></div>`  — DELETE this row (move contents to header)
- ~L1237–1250: `{["decomposed","in_research","delivered"].includes(...) && (<div … scopeNote …/>)}` S6 block  — DELETE entire block
- `Clock` icon already imported (used by the workflow-card row) — keep the import (still used after the move)

NextStepBanner: frontend/src/components/intake/NextStepBanner.tsx
- ~L36: `onStartManualResearch: () => void;` in the props type  — DELETE (prop is never rendered as a button inside NextStepBanner; dead plumbing)

HandoffBlock: frontend/src/components/intake/HandoffBlock.tsx
- Not imported anywhere (grep-confirmed). Comment self-declares dead UI.  — DELETE file

Locales (all three languages parallel):
- admin.json intakeDetail.toast.statusUnavailable  — REWORD (drop "(stops at decomposed)")
- admin.json intakeDetail.toast.autoResearchOutOfScope  — DELETE key
- admin.json intakeDetail.toast.researchOutOfScope  — DELETE key
- admin.json intakeDetail.scopeNote { label, body }  — DELETE namespace
- intake.json handoff { … full namespace, ~L370/374 onward }  — DELETE namespace

KEEP (do NOT touch — these are NOT research-location lies):
- intake.json results.*.outOfScope ("OUT OF SCOPE" / "HORS PÉRIMÈTRE") — NestorBriefingPDF form-section label
- Code comments containing "out of scope this milestone" in ResearchArtifacts.tsx / ResearchResultsPanel.tsx / AdminResearchResultsPanel.tsx — internal notes, not user-visible; leave as-is
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead HandoffBlock component + handoff i18n namespace</name>
  <files>frontend/src/components/intake/HandoffBlock.tsx, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/intake.json</files>
  <action>
    Delete the file frontend/src/components/intake/HandoffBlock.tsx entirely (grep-confirmed it is
    imported nowhere; its own comment declares it dead UI embodying the retired external-handoff flow).
    In each of the three intake.json locale files, delete the entire top-level "handoff": { … } object
    (nl ~L374, fr ~L370, en ~L370) including all its child keys (packUnavailable, researchQuestionsHeader,
    questionsSection, kindDecision, kindExploration, extraSection, whyRelevant, questionsCopied,
    copyFailed, pdfFailed, statusBackend, title, intro, synthesizing, generateContextPack, and any
    others in that block). Fix trailing-comma JSON validity after removal. Do NOT touch the unrelated
    results.*.outOfScope label. All three locales must remain key-consistent (identical remaining key
    sets).
  </action>
  <verify>
    <automated>cd frontend && node -e "['nl','fr','en'].forEach(l=>{const j=require('./src/locales/'+l+'/intake.json'); if(j.handoff) throw new Error(l+' still has handoff'); })" && npx tsc --noEmit</automated>
  </verify>
  <done>HandoffBlock.tsx does not exist; no intake.json contains a handoff namespace; all three intake.json parse as valid JSON; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove scope/research-out-of-scope messaging (route block, toasts, dead handler + prop, keys)</name>
  <files>frontend/src/routes/admin.pulse.intakes.$id.tsx, frontend/src/components/intake/NextStepBanner.tsx, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <action>
    In admin.pulse.intakes.$id.tsx: (a) delete the S6 "end-of-platform-scope note" block (~L1237–1250)
    — the `{["decomposed","in_research","delivered"].includes(intake.status ?? "") && (<div … scopeNote
    label/body …/>)}` JSX and its leading comment. If this leaves `Trans` unused, remove its import;
    if `Trans` is still used elsewhere, keep it. (b) delete the `onStartManualResearch` handler
    (~L784–786) and its prop pass `onStartManualResearch={onStartManualResearch}` (~L1160).
    In NextStepBanner.tsx: delete the `onStartManualResearch: () => void;` line from the props type
    (~L36) — the prop is never rendered as a button there.
    In all three admin.json locales: delete the `autoResearchOutOfScope` and `researchOutOfScope` toast
    keys and the entire `scopeNote` namespace ({label, body}). Reword the still-used `statusUnavailable`
    toast to drop the "(stops at decomposed)" / "(s'arrête à decomposed)" parenthetical:
    nl → "Statuswijziging niet beschikbaar via de API.",
    en → "Status change not available via the API.",
    fr → "Changement de statut non disponible via l'API.".
    Fix trailing-comma JSON validity. Keep all three admin.json locales key-consistent.
  </action>
  <verify>
    <automated>cd frontend && node -e "['nl','fr','en'].forEach(l=>{const j=require('./src/locales/'+l+'/admin.json').intakeDetail; if(j.scopeNote||j.toast.researchOutOfScope||j.toast.autoResearchOutOfScope) throw new Error(l+' stale key'); if(/decomposed/.test(j.toast.statusUnavailable)) throw new Error(l+' statusUnavailable not reworded'); })" && npx tsc --noEmit</automated>
  </verify>
  <done>Route no longer renders the scopeNote block or wires onStartManualResearch; NextStepBanner prop type drops onStartManualResearch; no admin.json has scopeNote/researchOutOfScope/autoResearchOutOfScope; statusUnavailable reworded in all three; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Move History trigger into the page header + final sweep and build</name>
  <files>frontend/src/routes/admin.pulse.intakes.$id.tsx</files>
  <action>
    In admin.pulse.intakes.$id.tsx: delete the old workflow-card History row
    (~L1174–1187: `<div className="border-t border-ink/10 px-6 py-4"> … toggleHistory … Clock … history.title …</div>`).
    Add a new History trigger button in the page-header actions row (the `<div className="flex flex-wrap
    items-center gap-2">` at ~L1034), placed immediately after the Intake-info button (the one with
    onClick={() => setInfoModalOpen(true)}). Style it to match the header button grammar:
    className="inline-flex items-center gap-1.5 border border-ink bg-paper px-3 py-1.5 font-mono text-xs
    uppercase tracking-wider text-ink hover:border-2", type="button", onClick={toggleHistory}. Keep the
    Clock icon (<Clock className="h-3.5 w-3.5" />), the label {t("intakeDetail.history.title")}, and the
    count badge {skillRuns && skillRuns.length > 0 && (<span className="tabular-nums">({skillRuns.length})</span>)}.
    Do NOT change toggleHistory's lazy-load behavior or the history Sheet markup.
    Then run the final sweep: grep the frontend src for any remaining referenced-but-removed keys
    (t("intakeDetail.scopeNote, t("intakeDetail.toast.researchOutOfScope, t("intakeDetail.toast.autoResearchOutOfScope,
    handoff.) and confirm none remain. Confirm the production build compiles.
  </action>
  <verify>
    <automated>cd frontend && node -e "const s=require('fs').readFileSync('src/routes/admin.pulse.intakes.$id.tsx','utf8'); if(/scopeNote|researchOutOfScope|onStartManualResearch/.test(s)) throw new Error('stale ref in route'); if(!/onClick=\{toggleHistory\}/.test(s)) throw new Error('history trigger missing'); " && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>History button renders in the header next to Intake-info and the old workflow-card row is gone; no removed i18n key is still referenced in src; npx tsc --noEmit and npm run build both pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes (no type errors from removed props/keys).
- `cd frontend && npm run build` passes.
- Grep proves no orphaned references: `t("intakeDetail.scopeNote`, `researchOutOfScope`, `autoResearchOutOfScope`, `handoff.`, `onStartManualResearch` all return zero hits in `frontend/src/**`.
- All three admin.json and all three intake.json parse as valid JSON and share identical remaining key sets per file family.
</verification>

<success_criteria>
- No user-visible admin string claims research runs "outside this system" or that scope "stops at decomposed".
- The persistent scope note no longer renders for decomposed/in_research/delivered intakes.
- Dead HandoffBlock component, handoff i18n namespace, onStartManualResearch handler+prop, and two out-of-scope toast keys are removed.
- statusUnavailable toast reads cleanly in all three languages.
- History trigger lives in the page header beside the Intake-info button; old workflow-card row removed; lazy-load + Sheet behavior unchanged.
- Frontend typechecks and builds.
</success_criteria>

<output>
Create `.planning/quick/260723-j56-sweep-outdated-end-of-scope-research-mes/260723-j56-SUMMARY.md` when done.
</output>
