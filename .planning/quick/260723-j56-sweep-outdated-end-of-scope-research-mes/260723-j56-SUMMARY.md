---
phase: quick-260723-j56
plan: 01
subsystem: frontend-admin-ui
tags: [i18n, cleanup, dead-code, admin-ui]
requires: []
provides:
  - "Header History button (page-header actions row)"
  - "Admin UI free of out-of-scope research messaging"
affects:
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/components/intake/NextStepBanner.tsx
tech-stack:
  added: []
  patterns: ["header button grammar for the run-history trigger"]
key-files:
  created: []
  modified:
    - frontend/src/routes/admin.pulse.intakes.$id.tsx
    - frontend/src/components/intake/NextStepBanner.tsx
    - frontend/src/locales/nl/admin.json
    - frontend/src/locales/fr/admin.json
    - frontend/src/locales/en/admin.json
    - frontend/src/locales/nl/intake.json
    - frontend/src/locales/fr/intake.json
    - frontend/src/locales/en/intake.json
  deleted:
    - frontend/src/components/intake/HandoffBlock.tsx
decisions:
  - "Left pre-existing nl-only research-start toast keys untouched (out of scope; logged to deferred-items.md)"
metrics:
  duration: ~7 min
  completed: 2026-07-23
  tasks: 3
  files: 9
requirements: [QUICK-J56]
---

# Phase quick-260723-j56 Plan 01: Sweep Outdated End-of-Scope Research Messaging Summary

Purged the factually-wrong "research is out of scope / happens outside this system / stops at
decomposed" messaging from the admin frontend (Tribunal deep-research has been in-platform since
Phases 13-16), deleted the dead HandoffBlock component and its i18n namespace, and relocated the
run-history trigger from its own workflow-card row into the page header beside the Intake-info button.

## What Was Done

### Task 1 — Delete dead HandoffBlock component + handoff i18n namespace (`dadf51f`)
- Removed `frontend/src/components/intake/HandoffBlock.tsx` via `git rm` (grep-confirmed imported
  nowhere; its own comments declared it gated-off dead UI from the retired external-handoff flow).
- Deleted the entire top-level `handoff` namespace (28 keys) from all three `intake.json` locales.
- All three `intake.json` files remain valid JSON and key-consistent.

### Task 2 — Remove scope/out-of-scope messaging (route block, toasts, dead handler + prop, keys) (`881fb02`)
- Deleted the S6 "end-of-platform-scope note" JSX block (rendered for decomposed/in_research/delivered).
- Removed the now-unused `Trans` import (it was only used by that block).
- Deleted the dead `onStartManualResearch` handler and its `NextStepBanner` prop pass.
- Removed `onStartManualResearch: () => void;` from the `NextStepBanner` props type (never rendered).
- Dropped `autoResearchOutOfScope`, `researchOutOfScope` toast keys and the `scopeNote` namespace
  from all three `admin.json` locales.
- Reworded `statusUnavailable` in all three languages to drop the "(stops at decomposed)" parenthetical.

### Task 3 — Move History trigger into the page header + final sweep and build (`9b65438`)
- Added a History button in the page-header actions row, immediately after the Intake-info button,
  restyled to the header button grammar (`border border-ink bg-paper px-3 py-1.5 …`), preserving
  the Clock icon, label, and run-count badge.
- Removed the old workflow-card History row.
- `toggleHistory` lazy-load and the history Sheet markup are unchanged.

## Verification

- `npx tsc --noEmit` — PASS (after each task).
- `npm run build` — PASS (`built in 17.41s`, `.output` generated).
- Grep sweep over `frontend/src`: zero hits for `scopeNote`, `researchOutOfScope`,
  `autoResearchOutOfScope`, `onStartManualResearch`, `handoff.`.
- `results.*.outOfScope` (NestorBriefingPDF form-section label) preserved — KEEP list honored.
- All three `intake.json` and `admin.json` parse as valid JSON.

## Deviations from Plan

### Out-of-Scope Discovery (logged, not fixed)

**1. [Scope boundary] Pre-existing nl-only admin.json toast keys**
- **Found during:** Task 3 locale key-consistency check.
- **Issue:** `intakeDetail.toast.researchStarted` and `intakeDetail.toast.researchStartFailed`
  exist only in the `nl` `admin.json`, absent from `fr`/`en`.
- **Determination:** Confirmed present-only-in-nl at parent commit `7c2d258`, before this task's
  changes. Unrelated to the removed out-of-scope messaging. Left untouched per the executor scope
  boundary and logged to `deferred-items.md` for a future i18n-parity sweep.

No other deviations — the three planned tasks executed as written.

## Note on routeTree.gen.ts

`frontend/src/routeTree.gen.ts` carried a pre-existing uncommitted modification at the start of
this session (unrelated to this task). No route paths changed here, so the build did not alter it
further; it was not staged or committed by this plan.

## Self-Check: PASSED

- `frontend/src/components/intake/HandoffBlock.tsx` — DELETED (confirmed absent).
- Commits `dadf51f`, `881fb02`, `9b65438` — all FOUND in git log.
- `results.*.outOfScope` KEEP label — still present in `en/intake.json`.
