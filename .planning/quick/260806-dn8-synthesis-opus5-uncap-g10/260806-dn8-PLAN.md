---
phase: quick-260806-dn8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py
  - tribunal/nestor_pulse_sdk/audit/cost_prices.json
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
  - tribunal/nestor_pulse_sdk/tests/test_synthesize_report.py
  - tribunal/nestor_pulse_sdk/tests/test_citation_anchors.py
  - tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py
  - tribunal/nestor_pulse_sdk/tests/test_synthesis_opus5.py
autonomous: true
requirements: [QUICK-260806-dn8, G-10, G-7-CLASS]

must_haves:
  truths:
    - "The three report-synthesis calls (`final_synthesis_audited`, `_one_section`, the wrap) run on `audited.anthropic_messages` against `claude-opus-5`. The distiller, conflict detector and scrubber stay on Gemini and are not touched."
    - "No `temperature`, `top_p`, `top_k`, `thinking` or `budget_tokens` key reaches `messages.create` from the synthesis path — those are HTTP 400 on Opus 5. The only keys passed are `max_tokens`, `system`, `messages`."
    - "Text is read from Anthropic content blocks where `type == \"text\"`, joining ALL such blocks — never `content[0]`, because thinking blocks precede text blocks when thinking is on."
    - "`stop_reason == \"refusal\"` is tested BEFORE content is read, and routes into the SAME degraded placeholder the existing empty-text guard produces, with its own distinct log line."
    - "All three existing degraded paths keep their exact user-visible strings: `*(Section generation failed: {exc})*`, `*(Section generation returned no content.)*`, and `final_synthesis_audited` returning `\"\"`."
    - "`_SECTION_MAX_TOKENS` and `_WRAP_MAX_TOKENS` are 20000 — above today's 8192, and provably below the anthropic 0.104.1 non-streaming ceiling of 21333 which is asserted in code as a named constant."
    - "`cost_table.compute(provider='anthropic', model='claude-opus-5', ...)` returns a Decimal, not None — proved by a test that computes an exact figure, so the G-7 NULL-cost defect cannot recur on the three synthesis rows."
    - "A focus-area section heading renders the client's FULL question when the mission brief carries it, degrading to the 120-char label when it does not — and the test proving it is proved RED against the pre-change code."
    - "The report's Verification-appendix `claims_per_facet` keys render through the SAME resolver as the headings; there is exactly one implementation of the label -> full-text rule in this change."
    - "No new hardcoded 120 is introduced anywhere. The match is a prefix test, per pipeline.py:1928."
    - "`_LABEL_MAX_CHARS`, `_GATE_DECISION_CONTEXT_CHARS`, `_QUESTION_MAX_CHARS` and `_DECISION_MAX_CHARS` are byte-identical after this change."
    - "`import nestor_pulse_sdk.pipeline.synthesis.steps` still succeeds in a bare venv — no new module-scope import pulls a provider SDK into this module's import graph."
    - "Nothing is built, pushed or deployed. No gcloud command runs."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py"
      provides: "Opus 5 synthesis calls, raised caps, the block-text reader, and the label -> full-question resolver"
      contains: "claude-opus-5"
    - path: "tribunal/nestor_pulse_sdk/audit/cost_prices.json"
      provides: "the anthropic/claude-opus-5 price row"
      contains: "anthropic/claude-opus-5"
    - path: "tribunal/nestor_pulse_sdk/tests/test_synthesis_opus5.py"
      provides: "the Anthropic-shape, cap-bound, price-row and G-10 heading proofs"
      contains: "claude-opus-5"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py:_one_section"
      to: "tribunal/nestor_pulse_sdk/audit/audited_llm_client.py:anthropic_messages"
      via: "audited call with max_tokens/system/messages only"
      pattern: "audited\\.anthropic_messages"
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py:_write_final_report"
      to: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py:relabel_facets"
      via: "claims_per_facet keys resolved before _verification_appendix renders them"
      pattern: "relabel_facets\\("
    - from: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py:focus_area_questions"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py:build_mission_brief_from_winners"
      via: "mission_brief.focus_areas[*].research_prompt — the CR-08 field that already carries the full text"
      pattern: "research_prompt"
---

<objective>
Move Tribunal report synthesis from `gemini-2.5-pro` to `claude-opus-5`, raise the two synthesis
output caps that truncate sections, add the Opus 5 price row so the run total stays a real total, and
close **G-10** (per-focus-area headings cut mid-word) using the CR-08 full-text pattern.

Purpose: the delivered report of run `368ff3a0` carries a heading that ends
`...hoe wordt dit operat` — the 120-char join key rendered as a title — and the same truncated string
is reused as a key in the report's own Verification section. Separately, `_SECTION_MAX_TOKENS = 8192`
was already tight for Gemini text alone; on Opus 5 the cap covers thinking AND text together, so it
would truncate every section.

Output: four coupled changes in `steps.py` / `cost_prices.json` / one line of `pipeline.py`, three
adapted test fakes, and one new test module carrying a RED-proved G-10 test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Primary file: `tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py`
Audited client: `tribunal/nestor_pulse_sdk/audit/audited_llm_client.py`
CR-08 precedent: `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py:709-871`
CR-08 caller:   `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py:1914-1974`
</context>

<facts_established_during_planning>
These were read out of the codebase and the anthropic SDK source during planning. Do NOT re-derive
them; DO verify any you are about to depend on that looks stale.

**F1 — the three synthesis call sites, and only those, move.**
`steps.py` calls `audited.gemini_generate` at lines 252, 1053 and 1127. Those are the whole report
writer. `_DISTILLER_MODEL` (:1191), `_CONFLICT_MODEL` (:3059) and `_SCRUB_MODEL` (:3170) are also
`gemini-*` and are NOT in scope — after the change `steps.py` still contains `gemini_generate`, so
the module docstring's grep gate at :20-22 still holds.

**F2 — `_make_synthesis_config` has exactly three callers**, all three of them the call sites in F1
(`grep -rn _make_synthesis_config` returns :125, :250, :1050, :1123 and nothing else). No test
references it. `_make_distiller_config` (:1259) is a separate function and stays.

**F3 — the anthropic client is constructed with default options.**
`audited_llm_client.py:2043` is `AsyncAnthropic()` — no `timeout=`, no `max_retries=`.

**F4 — THE CAP CEILING IS THE SDK, NOT THE MODEL.** In `anthropic==0.104.1`
(`requirements.txt:16`), `resources/messages/messages.py` runs

    if not stream and not is_given(timeout) and self._client.timeout == DEFAULT_TIMEOUT:
        timeout = self._client._calculate_nonstreaming_timeout(
            max_tokens, MODEL_NONSTREAMING_TOKENS.get(model, None))

and `_base_client._calculate_nonstreaming_timeout` is

    maximum_time = 60 * 60
    default_time = 60 * 10
    expected_time = maximum_time * max_tokens / 128_000
    if expected_time > default_time or (max_nonstreaming_tokens and max_tokens > max_nonstreaming_tokens):
        raise ValueError("Streaming is required for operations that may take longer than 10 minutes. ...")
    return Timeout(default_time, connect=5.0)

Per F3 all three guard conditions are true on our calls, and `MODEL_NONSTREAMING_TOKENS.get(
"claude-opus-5")` is `None` for an unknown key, so the binding rule is
`3600 * max_tokens / 128_000 > 600`, i.e. **`max_tokens > 21_333` raises `ValueError` client-side,
before any HTTP request**. The model's 128K output ceiling never comes into play.
The same code sets the request timeout to **600 s**.

**F5 — the full question is ALREADY inside `mission_brief`; no new plumbing is needed.**
`build_mission_brief_from_winners` (research_division.py:845-861) writes
`focus_areas[i]["research_prompt"] = _compose_parent_assignment(full_question, deep_research_prompt)`,
and `_compose_parent_assignment` (:744-751) returns `f"{q}\n\n{b}"` after collapsing whitespace in
each half with `" ".join(str(x).split())`. So the FIRST PARAGRAPH of `research_prompt` is exactly the
client's full, untruncated, whitespace-collapsed question — and neither half can contain a blank line.
`synthesize_report` already receives `mission_brief`. Do NOT add a `parent_prompts` kwarg.

**F6 — degenerate briefs exist and the prefix test is what makes this safe.**
`_compose_parent_assignment` returns `q or b` when either half is empty, so on a question-less brief
the first paragraph is the BRIEF, not a question. And `intake.py`-built briefs carry a
`research_prompt` that is a multi-line assignment with a different shape entirely. Both are handled
by one rule: accept the extracted text only when it starts with the label. That is
pipeline.py:1928's rule verbatim — "a label is a PREFIX of its text by construction" — and it is why
this change introduces no second copy of the 120.

**F7 — the Verification-appendix key.** `pipeline.py:4216` passes
`claims_per_facet=v.get("claims_per_facet") or {}` into `_verification_appendix` (:4943), which
renders `f"{label}: {count}"` at :4971 and a zero-list at :4974. Both are display-only; nothing
downstream joins on those keys. `mission_brief` is already in scope at :4216 (used at :4184).

**F8 — the fakes that actually drive the synthesis path.** Only three:
`tests/test_synthesize_report.py` (`FakeAudited`, `FlakyAudited`, `SingleCallAudited`),
`tests/test_citation_anchors.py` (`CapturingAudited`, :404), and
`tests/test_engine_e2e_stubbed.py` (`_ScriptedProvidersAudited` — routes `_M_SECTION` / `_M_WRAP`
inside `gemini_generate` at :604-609, and already has an `anthropic_messages` surface at :724 with a
`_anthropic_prompt_text(kwargs)` helper at :1156 and a `_FakeTextBlock(dict)` at :445).
Every other file that stubs `gemini_generate` drives the distiller / gates / workshop and is
unaffected — confirmed by cross-referencing `grep -l gemini_generate` against
`grep -l 'synthesize_report\|final_synthesis_audited'`.

**F9 — the repo already has the block reader.** `pipeline/tribunal/skeptic.py:152` is
`_block_get(obj, key)`, which reads a content block that may be an object OR a dict, and is already
imported by `group_skeptic.py` and `own_researcher.py`. Reuse it; do not write a second one.
Import it FUNCTION-LOCALLY (see Task 1) — `steps.py:49-52` documents that this module's module-scope
import graph is deliberately kept light.
</facts_established_during_planning>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the three synthesis calls to claude-opus-5</name>
  <files>
    tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py
    tribunal/nestor_pulse_sdk/tests/test_synthesize_report.py
    tribunal/nestor_pulse_sdk/tests/test_citation_anchors.py
    tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py
  </files>
  <behavior>
    - A section call reaches `anthropic_messages` with EXACTLY the keys `model`, `run_id`,
      `tenant_id`, `max_tokens`, `system`, `messages` — asserted as a set equality, not a
      containment check, so a stray `temperature` or `config` fails the test.
    - `system` is `_SYNTHESIS_SYSTEM` and `messages` is `[{"role": "user", "content": <prompt>}]`.
    - A response whose `content` is `[thinking_block, text_block_a, text_block_b]` yields
      `text_a + text_b` — proving both that thinking blocks are skipped and that all text blocks
      are joined rather than `content[0]` being taken.
    - A response with `stop_reason == "refusal"` and NO content yields the existing
      `*(Section generation returned no content.)*` placeholder, not a crash and not an empty
      section, and does not raise even though `.content` is empty.
    - A response with `stop_reason == "refusal"` and PARTIAL content takes the same degraded path
      (the refusal is checked before the content is read).
    - The wrap call's failure path still leaves `exec_part`/`tail_part` empty and still produces a
      report from the sections alone.
  </behavior>
  <action>
Port the report writer, and nothing else, to Anthropic.

**1a. `FINAL_SYNTHESIS_MODEL`** (:61) becomes `"claude-opus-5"`.

**1b. Replace `_make_synthesis_config` (:125-140) with `_synthesis_kwargs(prompt, max_tokens) -> dict`.**
REPLACE, do not adapt, and say why in its docstring: the old function returns a `google.genai`
`GenerateContentConfig` and returns `None` from a bare `except`, and the call sites turn that `None`
into "send no config at all". Gemini tolerates that; Anthropic does not — `max_tokens` and `messages`
are REQUIRED, so an optional-config shape cannot survive the port and keeping the name with new
semantics would be a trap for the next reader. The new function returns
`{"max_tokens": max_tokens, "system": _SYNTHESIS_SYSTEM, "messages": [{"role": "user", "content": prompt}]}`
and nothing else. `temperature=0.2` is DROPPED (per F4's sibling rule: with thinking on by default,
`temperature`/`top_p`/`top_k` are HTTP 400 on Opus 5) — record that in the docstring so it is not
"restored" later as a lost setting. Do NOT add `thinking` or `budget_tokens`: thinking is on by
default and `budget_tokens` is a 400.

**1c. Add `_synthesis_text(response) -> tuple[str, bool]`** returning `(text, refused)`. Rules:
  - `refused = getattr(response, "stop_reason", None) == "refusal"`, read FIRST. Opus 5's safety
    classifier returns HTTP 200 with empty (pre-output) or partial (mid-stream) content, so a
    content-only check would silently ship a half section.
  - text = the concatenation of every content block whose type is `"text"`, in order. Read both the
    type and the text through `_block_get`, imported function-locally
    (`from nestor_pulse_sdk.pipeline.tribunal.skeptic import _block_get`) — see F9, and give the
    one-line reason (this module's import graph is kept light on purpose, `steps.py:49-52`).
  - NEVER raises: wrap in `try/except Exception` returning `("", False)`, matching `_response_text`
    (:2335) which is this module's existing "an unreadable response is an empty one" precedent.

**1d. Port the three call sites** (:250-268, :1050-1076, :1123-1141). At each one:
  - `kwargs = _synthesis_kwargs(prompt, <the site's cap>)`, then
    `await audited.anthropic_messages(run_id=..., tenant_id=..., model=FINAL_SYNTHESIS_MODEL, **kwargs)`.
    `anthropic_messages` forwards `**kwargs` verbatim to `messages.create`, so an unknown key is a
    400 — pass nothing else.
  - Replace the two-step `.text` / `.candidates[0].content.parts[0].text` extraction with
    `text, refused = _synthesis_text(response)`.
  - **Preserve the guard semantics exactly.** `final_synthesis_audited` still returns `text or ""`.
    `_one_section` still returns `f"## {fa}\n\n*(Section generation failed: {exc})*"` from its
    `except` and `f"## {fa}\n\n*(Section generation returned no content.)*"` on empty. The wrap still
    leaves `exec_part`/`tail_part` empty on failure. A refusal takes the empty-text branch — same
    visible string — but logs its own line, e.g.
    `log.error("synthesize_report: section %r was REFUSED by the model (stop_reason=refusal) — the section is empty", fa)`.
  - Add one more diagnostic, and only this one: when `stop_reason == "max_tokens"`, log a WARNING
    naming the section and the cap. That is the exact failure this plan's Task 2 exists to prevent,
    and it must not be silent a second time.

**1e. Sweep the now-false prose.** The module docstring (:9-31, including the "Anti-pattern to
preserve from the legacy: gemini-2.5-pro does NOT support thinking_budget=0" note, which after this
change describes only the distiller/scrub/conflict paths — re-point it, do not delete it),
`final_synthesis_audited`'s "One audited Gemini call" docstring (:192), the `# google-genai response
shape` comment (:260), and the block comment at :271-281. A comment that lies about which provider
writes the report is the failure class already booked in this repo's gate-integrity notes.

**1f. Adapt the three fakes (F8).** Each keeps its existing routing logic and answers; only the
surface and the response shape change.
  - `test_synthesize_report.py`: `_FakeResponse` becomes an Anthropic-shaped response
    (`.content = [ {"type": "text", "text": ...} ]`, `.stop_reason = "end_turn"`). `FakeAudited`,
    `FlakyAudited` and `SingleCallAudited` swap `gemini_generate(..., contents, **kwargs)` for
    `anthropic_messages(*, run_id, tenant_id, model, **kwargs)` and read the prompt out of
    `kwargs["messages"][0]["content"]` — the shape already used at `test_blind_critique.py:122`.
  - `test_citation_anchors.py`: same swap on `CapturingAudited`. Its `section_prompts` /
    `wrap_prompt` properties and every assertion downstream of them stay untouched.
  - `test_engine_e2e_stubbed.py`: move the `_M_SECTION` / `_M_WRAP` branches from
    `gemini_generate` (:604-609) into `anthropic_messages` (:724), keeping the `_book(
    "synthesis_section")` / `_book("synthesis_wrap")` counter names EXACTLY — they are asserted at
    :1962. Return `_FakeMessage([_FakeTextBlock(...)], stop_reason="end_turn")`; `_FakeTextBlock` is
    a dict subclass, which is precisely the case `_block_get` exists for. The gemini branch for
    those two markers is removed, so a synthesis prompt arriving on the Gemini surface now falls
    through to `_book("gemini_unrouted")` + `_unexpected(...)` — that is a FEATURE: it makes a
    half-done port loud.

Write the `<behavior>` assertions as a new file `tests/test_synthesis_opus5.py` (Task 2 adds to it).
  </action>
  <verify>
    <automated>cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_synthesis_opus5.py nestor_pulse_sdk/tests/test_synthesize_report.py nestor_pulse_sdk/tests/test_citation_anchors.py -x -q</automated>
    <automated>cd tribunal && python -c "import nestor_pulse_sdk.pipeline.synthesis.steps as s; assert s.FINAL_SYNTHESIS_MODEL == 'claude-opus-5'; assert not hasattr(s, '_make_synthesis_config'); k = s._synthesis_kwargs('p', 123); assert set(k) == {'max_tokens','system','messages'}, k; assert k['messages'] == [{'role':'user','content':'p'}]; print('OK')"</automated>
    <automated>cd tribunal/nestor_pulse_sdk && grep -n "gemini_generate" pipeline/synthesis/steps.py | grep -v '^ *[0-9]*: *#' | wc -l   # expect 3 (distiller, conflict, scrub) — NOT 0, the module's own grep gate needs them</automated>
  </verify>
  <done>The three report-writing calls run on `anthropic_messages`/`claude-opus-5` with only
  `max_tokens`/`system`/`messages`; text comes from `type == "text"` blocks joined in order;
  refusals and truncation are logged distinctly; every existing degraded string is byte-identical;
  the three adapted fakes and the full local suite are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raise the caps to 20000 and add the Opus 5 price row</name>
  <files>
    tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py
    tribunal/nestor_pulse_sdk/audit/cost_prices.json
    tribunal/nestor_pulse_sdk/tests/test_synthesis_opus5.py
  </files>
  <behavior>
    - The `max_tokens` value CAPTURED FROM THE ACTUAL CALL (not read off the constant) is 20000 on
      both a section call and the wrap call.
    - `_SECTION_MAX_TOKENS <= _ANTHROPIC_NONSTREAMING_MAX_TOKENS` and
      `_WRAP_MAX_TOKENS <= _ANTHROPIC_NONSTREAMING_MAX_TOKENS`, and
      `_ANTHROPIC_NONSTREAMING_MAX_TOKENS == 21333`, which is `int(600 * 128_000 / 3600)` — spell
      the arithmetic in the test, not just the literal, so the bound is checkable rather than copied.
    - `cost_table.compute(provider="anthropic", model="claude-opus-5", prompt_tokens=1_000_000,
      completion_tokens=1_000_000, cached_tokens=0)` is NOT None and equals `Decimal("30.0")`
      ($5 in + $25 out). The `is not None` assertion is the G-7 regression guard and must be its own
      assert with its own message.
    - The new entry carries all four rate fields, so `_rate()`'s "missing rate -> 0 with a warning"
      degradation never fires for it. Assert the key set equals the key set of
      `anthropic/claude-opus-4-5`.
  </behavior>
  <action>
**2a. `steps.py`** — add, immediately above the two constants at :283-284:

    #: The anthropic SDK's NON-STREAMING ceiling, and the real bound on the two caps below —
    #: it binds long before the model's 128K output ceiling does.
    #: `messages.create` is called without `stream` and without `timeout`, and the client is
    #: built as a bare `AsyncAnthropic()` (audited_llm_client.py:2043), so anthropic 0.104.1's
    #: `_base_client._calculate_nonstreaming_timeout` applies: it raises ValueError when
    #: `3600 * max_tokens / 128_000 > 600`, i.e. above int(600 * 128_000 / 3600) tokens, and
    #: otherwise sets this call's timeout to 600 s.
    _ANTHROPIC_NONSTREAMING_MAX_TOKENS = 21_333

Then `_SECTION_MAX_TOKENS = 20_000` and `_WRAP_MAX_TOKENS = 20_000`, with a comment carrying the
reasoning: 8192 was already tight for Gemini's TEXT alone, and on Opus 5 the cap covers thinking AND
text together, so it would truncate every section; 20,000 is a round value inside the SDK bound with
headroom (~15K thinking + ~5K text ≈ 3,700 words per section); going higher needs either an explicit
`timeout=` kwarg — which disables the SDK's own guard against a 10-minute blocking call, times N
sections — or `messages.stream`, which `anthropic_messages` does not implement and whose usage
extraction is a rework. Record both escape hatches; implement neither.

Leave `final_synthesis_audited`'s `max_tokens: int = 16384` default ALONE — it is already inside the
bound, and it only runs on the zero-focus-area fallback.

**2b. `cost_prices.json`** — add after the `anthropic/claude-opus-4-5` block (:12-17), matching its
shape exactly, and precede it with a `_claude_opus_5_source` comment key in the style of
`_gpt_5_6_sol_source`. The comment must state, plainly: `prompt` 5.00 and `completion` 25.00 are the
operator-supplied published rates (2026-08-06); `cache_read` and `cache_creation_5m` are NOT
independent readings — they are Anthropic's published 0.1x / 1.25x multipliers applied to `prompt`,
which is the identical derivation every other `anthropic/*` row in this file uses (opus-4-5:
15 / 1.5 / 18.75; sonnet-4-6: 3 / 0.30 / 3.75). Also record that both cache fields are INERT today —
nothing on the synthesis path sends `cache_control` — and that they exist so a later caching change
prices correctly instead of silently.

      "anthropic/claude-opus-5": { "prompt": 5.0, "completion": 25.0,
                                   "cache_read": 0.50, "cache_creation_5m": 6.25 }

⛔ This row is load-bearing, not cosmetic: `compute()` returns `None` for an unknown model, the
caller writes NULL `cost_usd`, and `SUM(cost_usd)` silently skips it — the booked G-7 defect. The
`is not None` assertion in `<behavior>` is what makes that unrepeatable.

**2c.** Extend `tests/test_synthesis_opus5.py` with the `<behavior>` assertions. The price test must
set `COST_PRICES_PATH` only if the suite's existing convention does; otherwise read the shipped file,
as `test_cost_cache_write.py:206-243` does for `gemini-2.5-pro` — mirror that test's exact-figure
style rather than inventing a new one.
  </action>
  <verify>
    <automated>cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_synthesis_opus5.py -x -q</automated>
    <automated>cd tribunal && python -c "import json,pathlib; p=json.loads(pathlib.Path('nestor_pulse_sdk/audit/cost_prices.json').read_text(encoding='utf-8')); a=p['anthropic/claude-opus-5']; b=p['anthropic/claude-opus-4-5']; assert set(a)==set(b), (set(a), set(b)); assert (a['prompt'], a['completion'])==(5.0,25.0), a; print('OK', a)"</automated>
  </verify>
  <done>Both caps are 20000 and provably under a named, arithmetic-checked SDK ceiling; the Opus 5
  price row exists with all four fields and a source comment; `compute()` returns an exact Decimal
  for it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: G-10 — render the full client question, not the join key</name>
  <files>
    tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py
    tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
    tribunal/nestor_pulse_sdk/tests/test_synthesis_opus5.py
  </files>
  <behavior>
    - **RED FIRST.** With a mission brief whose focus-area label is the 120-char prefix of a ~180-char
      question and whose `research_prompt` is `f"{full_question}\n\nBrief context."`, the rendered
      report contains a heading line `## {full_question}` — a heading longer than 120 characters —
      and does NOT contain a heading line that is the bare truncated label. This test MUST be run
      against the pre-change `steps.py` and MUST fail there; paste that failure into the SUMMARY.
    - The section PROMPT quotes the full question too (assert on the captured prompt).
    - Degradation, all four cases, each its own assertion: a focus area with no `research_prompt`
      key; one whose `research_prompt` does not start with the label (the `intake.py` brief shape);
      one whose `research_prompt` is the brief alone because the question half was empty (F6); and a
      brief with no `focus_areas` at all. Every one of them renders the label and nothing raises.
    - `relabel_facets({label: 3}, brief)` returns `{full_question: 3}`, and returns its input
      unchanged when the brief carries no mapping — key order preserved.
    - `extract_focus_areas` returns the LABELS, unchanged, for the same brief — the facet/join key is
      untouched and only the display strings move.
  </behavior>
  <action>
**3a. `steps.py` — add ONE resolver, spelled once** (the `_compose_parent_assignment` discipline):

    def focus_area_questions(mission_brief: Optional[dict]) -> dict[str, str]:
        """focus-area LABEL -> the client's FULL question, when the brief carries it. PURE, NEVER RAISES."""

Rules, and each one has a reason that belongs in the docstring:
  - Read `mission_brief["focus_areas"]`; for each dict entry take `focus_area` (the label) and
    `research_prompt`.
  - `full = research_prompt.split("\n\n", 1)[0].strip()`. Correct because `_compose_parent_assignment`
    joins the two halves with exactly one blank line AND collapses whitespace inside each half, so
    neither half can contain a blank line (F5). Cite that function by name.
  - Accept `full` only when `full.startswith(" ".join(label.split()))` and `full != label`. The
    prefix test IS the matching rule (pipeline.py:1928, "a label is a PREFIX of its text by
    construction") — it is what keeps this change free of a second hardcoded 120, and what makes
    every degenerate brief in F6 fall back safely. Collapse the label's whitespace before the
    comparison because the stored full text is collapsed and the label may not be.
  - Skip anything else. Never raise (`try/except Exception` returning `{}` around the whole body).

Add the thin second resolver next to it, so `pipeline.py` needs no logic of its own:

    def relabel_facets(counts: dict, mission_brief: Optional[dict]) -> dict:
        """Re-key a facet-count map onto the full questions for DISPLAY. PURE, NEVER RAISES."""

Preserve insertion order; leave unmapped keys exactly as they are; on any error return `counts`.

**3b. `synthesize_report` (:1015-1076)** — call `focus_area_questions(mission_brief)` ONCE before
`_one_section` is defined, then inside it `title = questions.get(fa, fa)` and use `title` in the four
display sites: the `Section contract (heading first: ## {fa})` line (:1026), the
`if not text.lstrip().startswith("#")` fallback (:1075), and BOTH degraded returns (:1062, :1073).

Also use `title` in the assignment quote at :1024 (`f"  \"{fa}\"\n"`). **This is a judgment call and
is flagged for the operator:** the scope names the heading and the Verification key, and this is a
third site. It is included because it is the same string in the same prompt — telling the model to
head its section with the full question while handing it the truncated one to answer is an
internally inconsistent prompt, and "a label is not something a research provider should be asked to
answer" is the CR-08 sentence itself. It is one line to revert if the operator disagrees.

`fa` STAYS the key everywhere it is a key: `render_fact_ledger(anchor_ledger, facet=fa)` (:1018),
the `included_focus_areas` filter (:970-975), and the `focus_areas` list itself. Do not touch them.

**3c. `pipeline.py:4216`** — one line:
`claims_per_facet=relabel_facets(v.get("claims_per_facet") or {}, mission_brief),`
plus a short comment naming G-10 and pointing at `focus_area_questions`. `mission_brief` is already
in scope (F7). Import `relabel_facets` alongside the existing `synthesize_report` import at :150.
Nothing else in `pipeline.py` changes.

**3d.** Extend `tests/test_synthesis_opus5.py` with the `<behavior>` cases. The RED proof is the
deliverable here, not the green run: capture the pre-change failure output verbatim.
  </action>
  <verify>
    <automated>cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_synthesis_opus5.py -x -q</automated>
    <automated>cd tribunal && python -c "
import nestor_pulse_sdk.pipeline.synthesis.steps as s
full = 'Welke fuel retailers in Europa passen vandaag dynamic pricing toe op brandstof en/of shopproducten, hoe wordt dit operationeel ingericht en wat levert het op?'
label = full[:120]
b = {'focus_areas': [{'focus_area': label, 'research_prompt': full + chr(10)*2 + 'Brief.'}]}
assert s.focus_area_questions(b) == {label: full}
assert s.relabel_facets({label: 3}, b) == {full: 3}
assert s.extract_focus_areas(b) == [label], 'the join key must NOT move'
assert s.focus_area_questions({'focus_areas': [{'focus_area': label}]}) == {}
assert s.focus_area_questions({'focus_areas': [{'focus_area': label, 'research_prompt': 'Unrelated brief text.'}]}) == {}
assert s.focus_area_questions(None) == {} and s.relabel_facets({label: 1}, None) == {label: 1}
print('OK')"</automated>
    <automated>cd tribunal/nestor_pulse_sdk && grep -rn "120" pipeline/synthesis/steps.py | grep -v '^ *[0-9]*: *#' | grep -v "^[0-9]*:#" ; echo "^^ expect NO new 120 literal in code (comments referencing the 120-char label are fine)"</automated>
  </verify>
  <done>Headings, the section prompt's assignment quote and the Verification appendix's facet keys
  all render the full client question when the brief carries it, through ONE resolver, degrading to
  the label in all four degenerate cases; `extract_focus_areas` and every facet/join key are
  unchanged; the G-10 test is proved RED against the pre-change code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine -> Anthropic API | engine-authored prompts leave; model-authored report text returns |
| research providers -> synthesis prompt | third-party report prose is pasted into every section prompt (pre-existing) |
| client brief -> report heading | NEW: client-authored question text now reaches a rendered `##` heading and the Verification section |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-dn8-01 | Tampering | `focus_area_questions` heading text | mitigate | The text is non-model input (the client's own question) and is whitespace-COLLAPSED upstream by `_compose_parent_assignment`, so it cannot forge block structure or a second `##` heading. The prefix test means a value that is not the label's own expansion is never used. Same register as CR-08's own argument. |
| T-dn8-02 | Denial of Service | `_SECTION_MAX_TOKENS` | mitigate | Bounded by `_ANTHROPIC_NONSTREAMING_MAX_TOKENS` and asserted in test; the SDK's 600 s timeout still applies per call, and a breach raises client-side into `_one_section`'s `except`, degrading visibly rather than hanging. |
| T-dn8-03 | Information disclosure | audit body in GCS | accept | Unchanged: `anthropic_messages` already uploads request+response for the ~113 skeptic calls through the same path. This adds three rows of the same class. |
| T-dn8-04 | Repudiation | `cost_usd` NULL on synthesis rows | mitigate | The price row plus the `is not None` assertion; this is the G-7 defect made unrepeatable. |
| T-dn8-05 | Tampering | model refusal shipped as a partial section | mitigate | `stop_reason == "refusal"` checked before content is read; routed to the degraded placeholder with its own log line. |
| T-dn8-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this change installs NOTHING. `anthropic==0.104.1` is already pinned and already on the critical path. No package-legitimacy gate is triggered. |
</threat_model>

<verification>
Run the FULL engine suite, not just the touched files — the port moves a call surface that three
fake clients script and one route-counter asserts:

    cd tribunal && python -m pytest nestor_pulse_sdk/tests -q

A scratchpad venv built from gcloud's bundled python runs this in ~50 s. If `test_engine_e2e_stubbed.py`
or `test_tribunal_pipeline.py` cannot be collected locally because `pipeline.py` needs `httpx`, say so
explicitly in the SUMMARY and name those files as CI-only — do NOT report a partial run as a full one,
and do NOT reach for the `ast`-lift harness, which manufactures missing names.

Then confirm the three untouched caps are byte-identical:

    cd tribunal && git diff -- nestor_pulse_sdk/pipeline/tribunal/workshop.py nestor_pulse_sdk/pipeline/tribunal/brief_input.py   # expect EMPTY
    cd tribunal && git diff -- nestor_pulse_sdk/pipeline/tribunal/pipeline.py | grep -c "GATE_DECISION_CONTEXT"                    # expect 0
</verification>

<success_criteria>
- Report synthesis runs on `claude-opus-5` via `anthropic_messages` with only `max_tokens`/`system`/
  `messages`; distiller, conflict detector and scrubber untouched on Gemini.
- Refusal and `max_tokens` stop reasons are logged distinctly; every degraded string is unchanged.
- Both synthesis caps are 20000, under a named and arithmetic-checked SDK ceiling of 21333.
- `anthropic/claude-opus-5` prices to an exact Decimal — no NULL `cost_usd` on synthesis rows.
- Headings, the section assignment quote and the Verification facet keys carry the full client
  question, through one resolver, degrading to the label; the G-10 test proved RED first.
- Full local suite green, with any CI-only files named explicitly.
- Nothing deployed.
</success_criteria>

<operator_notes>
Flagged for the operator; none of these are tasks in this plan.

**1. This change is NOT live until a rebuild, and a rebuild voids the baseline.** Shipping it needs
new Tribunal images, which invalidates the digest baseline verified 2026-08-06 and makes 15.8-14's
deploy record stale. **All five 15.8 pre-flight gates must be re-run before any subsequent measured
run.** No `gcloud`, `builds submit` or deploy step appears anywhere in this plan, deliberately.

**2. Synthesis cost rises roughly 4-7x**, from ~$1.04 to ~$4-7 per run: three section calls plus a
wrap, each pushing on the order of 180K input tokens at $5/M in and $25/M out, against
gemini-2.5-pro's $1.25/$10. Opus 5's thinking tokens bill as OUTPUT, so the raised cap is a cost
lever as well as a quality one.

**3. Prompt caching is the obvious lever and is NOT implemented here.** Every section call repeats
the identical `reports_concatenated` block; a `cache_control` breakpoint on it would cut input cost
substantially (Opus 5's cacheable minimum is 512 tokens, and the block is far above it). It did not
fall out for free — it needs a cache-breakpoint design plus a check that the parallel `asyncio.gather`
does not race the cache write — so it is recorded, not built. The price row's `cache_read` /
`cache_creation_5m` fields are already correct for when it lands.

**4. The 600 s per-call timeout is now a live failure mode.** The SDK sets it (F4). Three parallel
section calls at 20K max_tokens on a ~180K-token input should land well inside it, but if a section
ever times out it will surface as `*(Section generation failed: ...)*` in the delivered report. That
is visible, not silent — which is the intended behaviour, not a defect to suppress.

**5. Task 3 changes a third prompt site** beyond the two the scope names (the section prompt's
assignment quote at `steps.py:1024`). The reasoning and the one-line revert are stated in Task 3a.
</operator_notes>

<output>
Create `.planning/quick/260806-dn8-synthesis-opus5-uncap-g10/260806-dn8-SUMMARY.md` when done.
Include the verbatim RED failure output from the G-10 test run against the pre-change code, and an
explicit list of any test file that could not be collected locally.
</output>
