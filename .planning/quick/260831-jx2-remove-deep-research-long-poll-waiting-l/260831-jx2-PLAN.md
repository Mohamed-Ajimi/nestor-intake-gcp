---
quick_id: 260831-jx2
type: execute
base: 49f2eb5
autonomous: true
files_modified:
  - tribunal/nestor_pulse_sdk/audit/audited_llm_client.py
  - tribunal/nestor_pulse_sdk/tests/test_own_researcher.py

must_haves:
  truths:
    - "The live run feed emits no 'Waiting on …' dispatch line and no 'Still waiting … NOT A STALL' heartbeat during a deep-research long poll."
    - "A deep-research poll loop still completes normally across 31 polls — removing the narration did not touch the loop."
    - "Real provider failures (job failed / cancelled / incomplete / gave up) still reach the feed as agent_fail events."
    - "The file no longer claims, in its docstring or its comments, that the removed events exist."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/audit/audited_llm_client.py"
      provides: "Deep-research poll loops with the four waiting emissions and the dead stride constant removed; agent_fail emissions intact"
    - path: "tribunal/nestor_pulse_sdk/tests/test_own_researcher.py"
      provides: "test_a_long_poll_emits_no_waiting_chatter — the inverted guard for the new silence contract"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/tests/test_own_researcher.py"
      to: "audited_llm_client.gemini_deep_research_raw"
      via: "31-poll fake harness asserting absence of waiting chatter"
      pattern: "test_a_long_poll_emits_no_waiting_chatter"
---

<objective>
Remove the four deep-research long-poll "waiting" emissions from the live run feed, on operator
request (2026-08-31, verbatim: "remove these logs from deep research").

Purpose: the operator watches the live feed and considers these lines noise. They accept that this
restores full feed silence for up to 35 minutes during a deep-research call.

Output: one source file with four `kind="thinking"` emission blocks and one now-dead constant
removed, its two stale narrative claims corrected; one test rewritten from pinning the old
behaviour to guarding the new silence.

**No deploy.** This ships with the next `tribunal-worker` / `tribunal-api` build. A research run that
has already finished is unaffected — these emissions only ever fired inside a live poll loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md

Base commit: `49f2eb5`. Work directly on the master working tree — **no worktree**.

**Scope ceiling, absolute:** two files only. Nothing else in `tribunal/`, nothing in `backend/`,
nothing in `frontend/`. No `gcloud`, no build, no deploy.
</context>

<planning_findings>
Six things were verified against the code at `49f2eb5` during planning. Four of them change how the
work must be done; read all six before touching anything.

**F1 — The 3.12 floor does NOT apply here. The suite is probably runnable on this box.**
The task brief warned the >=3.12 requirement might block local testing. That floor belongs to the
`backend/` package. `tribunal/pyproject.toml:9` says `requires-python = ">=3.11"`, and this machine's
gcloud interpreter is **3.11.9** — the floor is met. Stronger evidence:
`tribunal/nestor_pulse_sdk/audit/__pycache__/audited_llm_client.cpython-311.pyc` already exists, so
this exact module has been byte-compiled under 3.11 on this box before. Treat "the suite runs" as the
expected outcome and the fallback path as the surprise, not the reverse. Still prove it; do not
assume it.

**F2 — A repo-wide grep will read RED on correct code. Two poison sources.**
  - `.claude/worktrees/agent-af281d695d9b34c35/tribunal/nestor_pulse_sdk/audit/audited_llm_client.py`
    is a stale worktree copy that still contains every string being removed. A `grep -rn` from the
    repo root will match it forever.
  - `__pycache__/audited_llm_client.cpython-311.pyc` matches `_POLL_EVENT_STRIDE` as a binary hit.
  Every absence check in this plan is therefore scoped to a **named file path**, or uses
  `-I --exclude-dir=__pycache__ --exclude-dir=.claude`. Do not widen them.

**F3 — There is a SECOND stale narrative the brief did not name.**
The brief flagged the comment at `:100-105`. But the **module docstring** at `:34-40` also asserts the
removed behaviour: *"the two deep-research poll loops narrate themselves onto the run feed -- dispatch,
a strided progress line stating elapsed minutes and attempt number IN WORDS, reconnects, timeouts and
provider-reported failures."* After this change, "dispatch" and "a strided progress line" are false
while "reconnects, timeouts and provider-reported failures" stay true. Both sites must be corrected or
the file ships a second lie.

**F4 — `import os` STAYS. The question the brief left open is settled.**
`os.environ.get` is used at nine other sites in this file (`:93, :155, :187, :213, :239, :243, :1372,
:1373, :1676`). Deleting the stride's env read leaves `os` fully in use. Do not touch the imports.

**F5 — The removed strings live in exactly two files, repo-wide.**
A text-only grep across `.py/.md/.ts/.tsx` returns 8 hits in `audited_llm_client.py` and 2 in
`test_own_researcher.py`, and nothing anywhere else. There is **no frontend copy and no i18n key** to
update — the feed renders whatever text the backend emitted. Absence criteria are therefore clean.

**F6 — `EXPECTED_FILES` stays 45. Do not touch any cloudbuild config.**
Both test files are already named in `cloudbuild.test-engine.yaml` (`:498`, `:502`) under
`EXPECTED_FILES=45` (`:535`). This change renames one test function and adds no file, so both the file
count and the test count are unchanged. That config's own comments forbid "helpfully" adjusting it —
obey them.

**Also confirmed:** `NESTOR_RUN_EVENT_POLL_STRIDE` is set by no deploy script anywhere in the repo
(only `.planning/` prose mentions it). Removing the constant strands nothing.
</planning_findings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stand up the harness, invert the pinning test, and prove it RED</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_own_researcher.py</files>

  <behavior>
    The rewritten test, run against the STILL-UNEDITED source, must FAIL — because the source still
    emits the chatter it now forbids. That red is the only thing that makes the green in Task 3 mean
    anything.
  </behavior>

  <action>
**Step A — determine whether the suite can execute here (F1 says it probably can).**

Build a venv in the scratchpad from the gcloud interpreter:

  PY="/c/Users/ajimimo/AppData/Local/Google/Cloud SDK/google-cloud-sdk/platform/bundledpython/python.exe"
  SCRATCH="C:/Users/ajimimo/AppData/Local/Temp/claude/C--Users-ajimimo-Desktop-MOELD-nestor-intake-gcp/e0330556-a606-41a2-8dc9-b324db25ce5a/scratchpad"

Create `$SCRATCH/venv-jx2`, then try `pip install -e tribunal/` first. If that resolves too slowly or
fails, fall back to installing only the import chain the two test files actually need — at minimum
`pytest`, `pytest-asyncio`, `httpx` — and add whatever else the collection error names, one at a time.
`test_provider_resume.py` gates itself on httpx via `importorskip`, so a missing httpx presents as a
SKIP, not a failure: **a skipped file is not a pass.** Read the pytest summary line, never an exit code.

Run pytest with `tribunal/` as the working directory so `pyproject.toml`'s `asyncio_mode = "auto"`
applies — these are bare `async def` tests with no decorator and they will ERROR without it.

Record verbatim: the interpreter version, the install command that worked, and the collection summary.

**Step B — capture the baseline.** Run
`python -m pytest nestor_pulse_sdk/tests/test_own_researcher.py nestor_pulse_sdk/tests/test_provider_resume.py -q`
and write down the REAL passed/failed/skipped numbers before any edit.

**Step C — rewrite the test.** At `test_own_researcher.py:1282`, replace
`test_a_long_poll_says_in_words_that_it_is_a_wait` with `test_a_long_poll_emits_no_waiting_chatter`.

Keep the harness byte-identical: the `_pr._FakeHTTP(get_script=[{"status": "running"}] * 30 +
[{"status": "completed"}])` script, `_pr._install_gemini`, `monkeypatch.setattr(asyncio, "sleep",
_no_sleep)`, `_install_recorder`, the `start_call` with `_RUN_ID`/`_TENANT_ID`, and the
`gemini_deep_research_raw("q", poll_interval=30, max_attempts=70)` call.

New assertions:
  - `result["status"] == "success"` — KEEP. The poll loop must still survive 31 polls; this is the
    guard that the deletion removed narration and not machinery.
  - No emitted thinking text starts with `"Waiting on"`.
  - No emitted thinking text contains `"Still waiting"`.
  - No emitted thinking text contains `"NOT A STALL"`.
Delete the old count bound (`2 <= len(thinking) <= 8`), the `"5 min elapsed"` assertion and the
`"poll 10 of 70"` assertion — all three pin the removed lines.

Do NOT assert `recorder.texts("thinking") == []` for this Gemini path. The rejoin line is emitted on
the resume branch and other thinking emissions may exist on paths this fixture does not exercise;
assert the ABSENCE OF THE THREE FORBIDDEN SHAPES, which is exactly the operator's request, rather than
total emptiness, which is a stronger claim than was asked for and would be brittle.

Rewrite the docstring to say: the wait lines were removed on operator request 2026-08-31 because they
were noise in the live feed; the silence during a long poll is now DELIBERATE. Keep a pointer to the
2026-07-27 incident (a mid-poll run misread as hung) so the next reader knows the history was
considered and overruled, not forgotten.

**Do not touch `test_a_poll_with_no_run_context_emits_nothing` (`:1332`) or
`test_a_rejoined_job_says_so_and_a_refused_id_is_never_quoted` (`:1349`).**

**Step D — prove RED.** Run the single new test against the unedited source. It MUST fail on the
"Waiting on" / "Still waiting" / "NOT A STALL" assertions. Paste the failure text into the summary.
If Step A determined the suite cannot run at all, write that plainly here — what was attempted, what
error appeared — and state that RED was NOT proven. Never claim a run that did not happen.
  </action>

  <verify>
    <automated>cd tribunal &amp;&amp; "$SCRATCH/venv-jx2/Scripts/python.exe" -m pytest nestor_pulse_sdk/tests/test_own_researcher.py::test_a_long_poll_emits_no_waiting_chatter -q  # MUST FAIL at this point; a pass here means the assertions are vacuous</automated>
  </verify>

  <done>
    Harness feasibility is settled with evidence either way. The renamed test exists with the three
    absence assertions plus the `status == "success"` guard, and its failure against the unedited
    source is recorded verbatim (or its unrunnability is stated plainly).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the four emission blocks, the dead constant, and correct both stale narratives</name>
  <files>tribunal/nestor_pulse_sdk/audit/audited_llm_client.py</files>

  <action>
Line numbers below were verified at `49f2eb5` but are a **hint, not authority**. Locate each block by
its literal text and confirm before cutting. **Work bottom-up** (block 4 → 3 → 2 → 1 → the constant)
so earlier deletions do not shift the targets that follow.

**Block 4 — OpenAI heartbeat (~:1938-1962).** The comment `# The heartbeat. Same wording contract as
Gemini's…` through the close of the `if _run is not None and (_attempt + 1) % _POLL_EVENT_STRIDE == 0:`
block emitting `"Still waiting on OpenAI … THIS IS A WAIT, NOT A STALL."`. It sits between the
`elif response.status == "incomplete":` arm and `timeout_msg = (…)`.

**Block 3 — OpenAI announcement (~:1864-1883).** The comment `# Announced BEFORE the wait, for the same
reason as Gemini's.` through the close of the `if _run is not None:` block emitting `"Waiting on OpenAI
{model} — background research dispatched…"`. It sits between the `on_job_started` callback block and
the `# ---- Polling loop ----` comment.

**Block 2 — Google heartbeat (~:1584-1615).** The comment `# THE HEARTBEAT, and the whole point of this
task.…` through the close of the `if _run is not None and (_attempt + 1) % _POLL_EVENT_STRIDE == 0:`
block emitting `"Still waiting on Google … THIS IS A WAIT, NOT A STALL."`. It sits between the
`if status in ("failed", "error", "cancelled"):` arm and `timeout_msg = (…)`.

**Block 1 — Google announcement (~:1492-1512).** The comment `# THE WAIT IS ANNOUNCED BEFORE IT STARTS.…`
through the close of the `if _run is not None:` block emitting `"Waiting on Google {agent} — background
research dispatched…"`. It sits between the `on_job_started` callback block and
`for _attempt in range(max_attempts):`.

**The dead constant (~:107-115).** After the four deletions, re-grep with
`grep -rnI --exclude-dir=__pycache__ "_POLL_EVENT_STRIDE" tribunal/`. If zero references remain,
delete the `_POLL_EVENT_STRIDE = max(1, int(os.environ.get(…)))` definition at `:113-115` **and** the
`# _POLL_EVENT_STRIDE: one progress line every N poll attempts…` explanatory paragraph at `:107-111`.
If ANY reference survives, KEEP the constant and report the surviving site rather than breaking the
module. Note the `.pyc` trap from F2 — a binary hit is not a reference.

**Narrative site A (`:96-112` comment block).** KEEP the 2026-07-27 incident history at `:99-103` — it
is true and worth keeping. The trailing claim at `:104-105` ("The events below are the fix, and they
only work if they say, in a sentence, that the wait is expected.") becomes false; replace it with a
short note that the feed lines were **removed on operator request 2026-08-31** because they were noise
in the live feed, so a long poll is now silent by design. Keep the incident as the reason the silence
is worth flagging to a future reader.

**Narrative site B (module docstring `:34-40`, per F3).** The clause "dispatch, a strided progress line
stating elapsed minutes and attempt number IN WORDS" is now false. Trim it so the sentence describes
only what remains — reconnects, timeouts and provider-reported failures — and note the 2026-08-31
removal.

**Do not touch, in this file or any other:**
  - Any `kind="agent_fail"` emission: Google job failed (~:1568-1578), Google gave up (~:1621+),
    OpenAI failed/cancelled (~:1910-1921), OpenAI incomplete (~:1924-1935), OpenAI gave up (~:1968+).
  - The rejoin/resume events, the `safe_job_id` guard blocks, `RESUME_REDISPATCH`.
  - The `_CURRENT_RUN` ContextVar or the `run_events` import — the surviving `agent_fail` emissions
    need both.
  - Any `log.info` / `log.warning` call.
  - `import os` (F4) or any other import.
  - Any cloudbuild YAML (F6).
  </action>

  <verify>
    <automated>cd tribunal &amp;&amp; grep -nI -e "Waiting on Google" -e "Waiting on OpenAI" -e "Still waiting" -e "NOT A STALL" -e "normal shape of this" nestor_pulse_sdk/audit/audited_llm_client.py; test $? -eq 1 &amp;&amp; grep -nI -e "Google research job reported" -e "Google research gave up after" -e "OpenAI research response reported" -e "OpenAI research ended incomplete" -e "OpenAI research gave up after" nestor_pulse_sdk/audit/audited_llm_client.py &amp;&amp; echo "ABSENCE+PRESENCE OK"</automated>
  </verify>

  <done>
    Zero occurrences of the five removed phrases in `audited_llm_client.py`, and all five `agent_fail`
    sentinel phrases still present. `_POLL_EVENT_STRIDE` either fully removed with zero surviving
    source references, or kept with the surviving reference named in the summary. Both narrative sites
    corrected; the incident history retained.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove GREEN, run the absence gates, and report real numbers</name>
  <files>tribunal/nestor_pulse_sdk/audit/audited_llm_client.py, tribunal/nestor_pulse_sdk/tests/test_own_researcher.py</files>

  <action>
**If Task 1 got the harness running:**
Run `python -m pytest nestor_pulse_sdk/tests/test_own_researcher.py
nestor_pulse_sdk/tests/test_provider_resume.py -v -rs` from `tribunal/`. Report the REAL
passed / failed / skipped numbers and reconcile them against Task 1's Step B baseline: the total
should be unchanged (one test renamed, none added, none removed). Any drift must be EXPLAINED, not
merely noted. `-rs` is there so a SKIP cannot masquerade as a pass. Confirm
`test_a_long_poll_emits_no_waiting_chatter` now PASSES where Task 1 proved it failed, and that
`test_a_poll_with_no_run_context_emits_nothing` and the rejoin test still pass untouched.

**If Task 1 determined the harness cannot run:**
Say so plainly and state exactly what was attempted and what error appeared. **Do not assert a test
pass that did not happen.** Then fall back to, and report the output of:
  - `python -m py_compile` on both changed files (or `ast.parse` if py_compile is unavailable),
  - the absence and presence greps from Task 2's verify line,
  - the `_POLL_EVENT_STRIDE` zero-reference grep.
Label the summary honestly: "static checks only, tests NOT executed."

**Either way**, run the two scoped gates below and paste their output. Note per F2 that these are
scoped to named paths precisely because a repo-root recursive grep matches the stale
`.claude/worktrees/` copy and the `.pyc`, and would read RED on correct code.

Note also: the removed literals DO still appear in `test_own_researcher.py` — the new test asserts
their absence, so it must name them. The absence gate applies to the SOURCE file only. Do not write a
gate that requires them gone from the test file.

Commit both files together with a message naming the operator request and the date. Then write the
quick-task summary. State in it, explicitly: **no deploy was performed**; this ships with the next
`tribunal-worker` / `tribunal-api` build; an already-finished research run is unaffected; and the
operator-visible consequence is up to 35 minutes of deliberate feed silence during deep research.
  </action>

  <verify>
    <automated>cd tribunal &amp;&amp; grep -nI -e "Waiting on Google" -e "Waiting on OpenAI" -e "Still waiting" -e "NOT A STALL" nestor_pulse_sdk/audit/audited_llm_client.py; test $? -eq 1 &amp;&amp; { grep -rnI --exclude-dir=__pycache__ "_POLL_EVENT_STRIDE" nestor_pulse_sdk/; test $? -eq 1; } &amp;&amp; echo "SOURCE CLEAN + STRIDE FULLY REMOVED"</automated>
  </verify>

  <done>
    Either real pytest numbers are reported with the new test proven green against Task 1's red, or
    the inability to run is stated plainly with the static-check fallback reported instead. Both
    scoped gates pass. Both files committed. Summary records the no-deploy status and the operator-
    visible silence.
  </done>
</task>

</tasks>

<verification>
- `audited_llm_client.py` contains zero occurrences of "Waiting on Google", "Waiting on OpenAI",
  "Still waiting", "NOT A STALL", "normal shape of this".
- All five `agent_fail` sentinel phrases still present in that file.
- `_POLL_EVENT_STRIDE` has zero source references under `nestor_pulse_sdk/` (excluding `__pycache__`),
  or the surviving reference is named and the constant kept.
- `test_a_long_poll_emits_no_waiting_chatter` exists, was RED before Task 2 and GREEN after — or its
  unrunnability is stated plainly.
- `test_a_poll_with_no_run_context_emits_nothing` unchanged.
- Exactly two files changed: `git diff --name-only 49f2eb5..HEAD` returns those two paths and nothing else.
- No cloudbuild YAML touched; `EXPECTED_FILES` still 45.
</verification>

<success_criteria>
A deep-research long poll emits no dispatch announcement and no heartbeat to the run feed, while
provider failures, rejoins and timeouts still do. The poll loop itself still completes across 31
polls. The file no longer claims the removed events exist. The change is committed and not deployed.
</success_criteria>

<output>
Create `.planning/quick/260831-jx2-remove-deep-research-long-poll-waiting-l/260831-jx2-SUMMARY.md` when done.
</output>

