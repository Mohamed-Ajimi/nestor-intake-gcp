---
phase: quick
plan: 260716-e59
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/intake.index.tsx
  - frontend/src/routes/admin.pulse.tsx
  - frontend/src/routes/admin.sales.tsx
  - frontend/src/components/admin/adminNav.ts
  - frontend/src/components/admin/ProductShell.tsx
  - frontend/src/routes/admin.pulse.intakes.index.tsx
  - frontend/src/routes/admin.pulse.clients.tsx
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/fr/admin.json
  - frontend/src/locales/en/admin.json
autonomous: true
requirements: [UAT-1205-D1-user-lang-switcher, UAT-1205-D2-i18n-nav-labels, UAT-1205-D3-decomposed-filter, UAT-1205-D4-space-switch-refetch]

must_haves:
  truths:
    - "A logged-in user on /intake can switch NL/FR/EN from the list page header and the choice persists"
    - "Sidebar nav labels (Pulse, Sales, and Beheer management links) render in the active UI language, not hardcoded Dutch"
    - "The admin intakes list status filter offers 'decomposed' between 'validated_by_client' and 'in_research', labeled in all 3 languages"
    - "When a superadmin changes the active space, the intakes list AND clients list refetch and show only that space's data"
  artifacts:
    - path: "frontend/src/components/admin/adminNav.ts"
      provides: "ADMIN_NAV items carrying i18n labelKey instead of hardcoded Dutch label"
      contains: "labelKey"
    - path: "frontend/src/components/admin/ProductShell.tsx"
      provides: "labelKey → t() resolution for all sidebar nav items"
    - path: "frontend/src/locales/nl/admin.json"
      provides: "nav.* keys + intakesList.filter.decomposed"
      contains: "decomposed"
  key_links:
    - from: "frontend/src/routes/intake.index.tsx"
      to: "frontend/src/components/LanguageSwitcher.tsx"
      via: "LanguageSwitcher persist mount in header chrome"
      pattern: "LanguageSwitcher persist"
    - from: "frontend/src/components/admin/ProductShell.tsx"
      to: "frontend/src/locales/*/admin.json"
      via: "t(item.labelKey) with useTranslation(\"admin\")"
      pattern: "t\\(item\\.labelKey\\)"
    - from: "frontend/src/routes/admin.pulse.clients.tsx"
      to: "frontend/src/lib/active-space"
      via: "useActiveSpace().activeSpaceId in load-effect deps"
      pattern: "useActiveSpace"
---

<objective>
Fix 4 frontend defects found in parity UAT 12-05: (1) user-role intake list has no language
switcher; (2) sidebar nav labels hardcoded in Dutch; (3) "decomposed" missing from the intakes
status filter; (4) intakes/clients lists do not refetch when the superadmin switches active space.

Purpose: Close user-visible parity gaps before final cutover UAT sign-off.
Output: Frontend-only changes across 7 source files + 3 locale catalogs. No backend changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@frontend/src/routes/intake.index.tsx
@frontend/src/routes/admin.pulse.tsx
@frontend/src/routes/admin.sales.tsx
@frontend/src/components/admin/adminNav.ts
@frontend/src/components/admin/ProductShell.tsx
@frontend/src/routes/admin.pulse.intakes.index.tsx
@frontend/src/routes/admin.pulse.clients.tsx
@frontend/src/components/LanguageSwitcher.tsx
@frontend/src/locales/nl/admin.json
@frontend/src/locales/fr/admin.json
@frontend/src/locales/en/admin.json

<interfaces>
<!-- Verified against current source. Executor should use these directly. -->

From frontend/src/components/LanguageSwitcher.tsx:
```typescript
export function LanguageSwitcher({ persist = true }: { persist?: boolean }): JSX.Element;
// Trigger is w-full (border border-ink, mono/uppercase) — constrain width when mounting
// inside a flex header row. Uses the "common" i18n namespace internally.
```

From frontend/src/lib/active-space (already imported by admin.pulse.intakes.index.tsx):
```typescript
export function useActiveSpace(): { activeSpaceId: string | null; /* ... */ };
// Provided by ActiveSpaceProvider, which ProductShell mounts — both routes render inside it.
```

From frontend/src/components/admin/adminNav.ts (CURRENT — to be changed):
```typescript
export type AdminNavItem = { to: string; label: string; exact: boolean };
export const ADMIN_NAV: AdminNavItem[] = [
  { to: "/admin/users", label: "gebruikers", exact: false },
  { to: "/admin/spaces", label: "spaces", exact: false },
  { to: "/admin/templates", label: "templates", exact: false },
];
```

ProductShell nav rendering (CURRENT): renders `{item.label}` in two places — the primary
`items.map(...)` nav AND the superadmin `ADMIN_NAV.map(...)` manage section. It already has
`const { t } = useTranslation("admin")` in scope. The guard `items !== ADMIN_NAV` relies on
REFERENCE equality of the exported constant — preserve it.

ProductShell `items` callers (complete list, verified by grep — no others):
- admin.pulse.tsx      (inline items: "Nieuwe intake", "Intakes", "Klanten", "AI-zoek")
- admin.sales.tsx      (inline items: "Nieuw project", "Projecten")
- admin.users.tsx, admin.spaces.tsx, admin.templates.tsx (all pass the ADMIN_NAV constant)

Existing status label translations for "decomposed" (reuse for the filter label):
nl "Gedecomposeerd" / en "Decomposed" / fr "Décomposé" (from intakeDetail.status.decomposed).

admin.json filter block (all 3 catalogs, same shape): `intakesList.filter.{all,draft,submitted,
reviewed,validated_by_client,in_research,delivered,archived}` — decomposed is the missing key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n the sidebar nav labels via labelKey + t() in ProductShell</name>
  <files>frontend/src/components/admin/adminNav.ts, frontend/src/components/admin/ProductShell.tsx, frontend/src/routes/admin.pulse.tsx, frontend/src/routes/admin.sales.tsx, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <action>
    1. In adminNav.ts: change `AdminNavItem` to `{ to: string; labelKey: string; exact: boolean }`
       and update ADMIN_NAV entries to labelKeys: `nav.manageUsers`, `nav.manageSpaces`,
       `nav.manageTemplates`. Keep it a single exported const (reference equality with the
       ProductShell `items !== ADMIN_NAV` guard and the three Beheer routes must survive).
       Update the file's header comment accordingly.
    2. In ProductShell.tsx: change the local `Item` type to `labelKey`, and render
       `t(item.labelKey)` instead of `{item.label}` in BOTH nav render sites (primary items map
       and the superadmin ADMIN_NAV manage section). `t` from `useTranslation("admin")` is
       already in scope — no new hooks.
    3. In admin.pulse.tsx: replace the four inline `label:` entries with `labelKey:`
       `nav.pulseNewIntake`, `nav.pulseIntakes`, `nav.pulseClients`, `nav.pulseSearch`.
    4. In admin.sales.tsx: same pattern — `nav.salesNewProject`, `nav.salesProjects`.
    5. Add a top-level `"nav"` object to ALL THREE catalogs (nl, fr, en) in
       frontend/src/locales/{nl,fr,en}/admin.json with the 9 keys. Values:
       - nl: pulseNewIntake "Nieuwe intake", pulseIntakes "Intakes", pulseClients "Klanten",
         pulseSearch "AI-zoek", salesNewProject "Nieuw project", salesProjects "Projecten",
         manageUsers "gebruikers", manageSpaces "spaces", manageTemplates "templates"
       - fr: "Nouvelle intake", "Intakes", "Clients", "Recherche IA", "Nouveau projet",
         "Projets", "utilisateurs", "spaces", "templates"
       - en: "New intake", "Intakes", "Clients", "AI search", "New project", "Projects",
         "users", "spaces", "templates"
       (Lowercase manage labels are intentional — the sidebar styles them uppercase via CSS;
       current NL labels are lowercase.)
    6. Do NOT touch admin.users.tsx / admin.spaces.tsx / admin.templates.tsx — they pass the
       ADMIN_NAV constant and need no edits (verified: no other ProductShell callers exist).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>No remaining hardcoded Dutch nav strings in admin.pulse.tsx / admin.sales.tsx / adminNav.ts (grep for "Nieuwe intake"\|"AI-zoek"\|"gebruikers" in those files returns nothing); ProductShell renders t(item.labelKey) at both nav sites; all 3 catalogs contain the identical 9-key nav block; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: LanguageSwitcher on user intake list + decomposed status filter</name>
  <files>frontend/src/routes/intake.index.tsx, frontend/src/routes/admin.pulse.intakes.index.tsx, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <action>
    1. intake.index.tsx: import `LanguageSwitcher` from `@/components/LanguageSwitcher` and mount
       `<LanguageSwitcher persist />` in the header chrome row (the `mb-10 flex items-center
       justify-between` div), grouped with the email/logout cluster on the right. The switcher's
       trigger is `w-full` — wrap it in a fixed-width container (e.g. `<div className="w-28">`)
       so it does not stretch the flex row. Keep the existing comment accurate: this page still
       does NOT mount ProductShell or the space switcher; only the language switcher is added
       (a user needs to change language post-login — UAT defect 1).
    2. admin.pulse.intakes.index.tsx: add `"decomposed"` to STATUS_FILTER_VALUES between
       `"validated_by_client"` and `"in_research"` (mirrors the real status machine order).
    3. Add `"decomposed"` to the `intakesList.filter` object in ALL THREE catalogs, positioned
       between validated_by_client and in_research: nl "Gedecomposeerd", en "Decomposed",
       fr "Décomposé" (reuse the existing intakeDetail.status.decomposed values verbatim).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -c '"decomposed"' src/locales/nl/admin.json src/locales/fr/admin.json src/locales/en/admin.json</automated>
  </verify>
  <done>LanguageSwitcher renders in the /intake list header without breaking the layout row; STATUS_FILTER_VALUES contains decomposed in machine order; each catalog now has 3 occurrences of "decomposed" (status + statusBanner + filter); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Refetch intakes + clients lists on active-space switch, then full build check</name>
  <files>frontend/src/routes/admin.pulse.intakes.index.tsx, frontend/src/routes/admin.pulse.clients.tsx</files>
  <action>
    1. admin.pulse.intakes.index.tsx: the route already reads
       `const { activeSpaceId } = useActiveSpace();`. Change the load useEffect dependency array
       from `[]` to `[activeSpaceId]` so the list refetches when the superadmin switches space
       (listIntakes threads the active space via withActiveSpace at call time — re-running the
       effect is sufficient; the `cancelled` flag already handles stale responses). Update the
       effect's comment if it claims one-shot loading.
    2. admin.pulse.clients.tsx: import `useActiveSpace` from `@/lib/active-space`, read
       `const { activeSpaceId } = useActiveSpace();` in ClientsPage (route renders inside
       ProductShell's ActiveSpaceProvider), and change the load useEffect deps from `[]` to
       `[activeSpaceId]`. Add a one-line comment noting the deps exist to refetch on space
       switch (TENANT-04 view-filter; backend stays authoritative).
    3. Do NOT add activeSpaceId as a request parameter anywhere — listIntakes/listSpaces already
       thread it internally; this task is deps-only.
    4. Full verification gate for the whole plan: run `npx tsc --noEmit` and `npm run build:dev`
       in frontend/ — both must succeed.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build:dev</automated>
  </verify>
  <done>Both load effects list [activeSpaceId] as deps; clients.tsx imports and reads useActiveSpace; no API call signatures changed; tsc clean and build:dev succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Presentation/data-loading changes only; no new inputs cross a trust boundary. Tenant authority remains server-side (activeSpaceId is a UX view-filter; backend re-derives tenancy from the verified token). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e59-01 | Information Disclosure | space-switch refetch (intakes/clients lists) | accept | Client-side filter only; listIntakes/listSpaces are authenticated seam calls and the backend enforces space scoping per TENANT-04 — no new exposure. |
| T-e59-SC | Tampering | package installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — clean
- `cd frontend && npm run build:dev` — succeeds
- Grep gates: no "Nieuwe intake"/"AI-zoek"/"Klanten" string literals left in admin.pulse.tsx,
  no "Nieuw project" in admin.sales.tsx, no `label:` entries in adminNav.ts
- All three locale catalogs contain the identical `nav` key set and `intakesList.filter.decomposed`
</verification>

<success_criteria>
- User intake list (/intake) header mounts LanguageSwitcher with persist
- All ProductShell sidebar labels (pulse nav, sales nav, Beheer manage nav) resolve via t() in nl/fr/en
- Status filter includes decomposed with translated label in all 3 languages
- Intakes list and clients list refetch when activeSpaceId changes
- No backend files touched; no frontend/src/components/ui/ files touched
</success_criteria>

<output>
Create `.planning/quick/260716-e59-fix-4-uat-found-frontend-defects-user-la/260716-e59-SUMMARY.md` when done
</output>
