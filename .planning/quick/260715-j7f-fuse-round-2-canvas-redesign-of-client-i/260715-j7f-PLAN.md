---
phase: quick
plan: 260715-j7f
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/intake/IntakeForm.tsx
  - frontend/src/components/intake/FieldRenderer.tsx
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/en/intake.json
autonomous: true
requirements: [QUICK-260715-j7f]

must_haves:
  truths:
    - "Client intake sidebar shows a 'Voortgang' header with an 'n / total' tabular counter and a thin progress bar that fills as the user advances sections"
    - "Each sidebar section row shows a zero-padded number (01..NN) with three visual states: current (bg-paper2, green mark), completed (ink text, ink mark), future (dimmed text, plain mark)"
    - "Sidebar nav scrolls independently when sections exceed viewport height; sticky positioning and mobile hiding are unchanged"
    - "Header has a bottom hairline separator; the form column never overflows the grid"
    - "Last step's standard submit button reads '{schema label} →' (arrow appended); all save/submit/validation behavior is byte-identical"
    - "All longtext textareas are vertically resizable"
  artifacts:
    - path: "frontend/src/components/intake/IntakeForm.tsx"
      provides: "Redesigned sidebar progress chrome mapped onto existing currentStep/completedSections/dirtyFields state"
      contains: "max-h-[calc(100vh-12rem)]"
    - path: "frontend/src/locales/nl/intake.json"
      provides: "form.progress key ('Voortgang')"
      contains: "\"progress\""
    - path: "frontend/src/components/intake/FieldRenderer.tsx"
      provides: "resize-y on the longtext textarea"
      contains: "resize-y"
  key_links:
    - from: "frontend/src/components/intake/IntakeForm.tsx"
      to: "frontend/src/locales/*/intake.json"
      via: "t(\"form.progress\") in the sidebar progress header"
      pattern: "form\\.progress"
    - from: "sidebar progress bar width"
      to: "existing currentStep state"
      via: "inline style width = ((currentStep+1)/sections.length)*100%"
      pattern: "currentStep \\+ 1"
---

<objective>
Fuse the round-2 Claude Design canvas redesign of the client intake form: upgrade the stepper
sidebar chrome (progress header + bar, numbered section rows with three visual states, scrollable
nav), tighten the layout grid, add a header hairline, append the arrow to the final submit label,
and make longtext textareas vertically resizable.

Purpose: visual parity with the approved canvas before Phase 12 parity UAT. Presentation-only —
zero behavior change to save/submit/validation/navigation logic.
Output: updated IntakeForm.tsx, FieldRenderer.tsx, and one new i18n key in the three intake.json
locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260715-j7f-fuse-round-2-canvas-redesign-of-client-i/DIFF-NOTES-2.md  (AUTHORITATIVE worklist)
@frontend/src/components/intake/IntakeForm.tsx
@frontend/src/components/intake/FieldRenderer.tsx
@frontend/src/locales/nl/intake.json

Facts established during planning (verified against current source — do not re-derive):
- IntakeForm header block is `<header className="mb-10">` (~line 360).
- Grid is `grid gap-8 md:grid-cols-[320px_1fr]` (~line 392); the form column is a bare `<div>` (~line 429).
- Sidebar is `<aside className="hidden md:block">` wrapping `<nav className="sticky top-8 space-y-1">`.
- Section buttons already compute `active`, `done = completedSections[idx]` (currently UNUSED in
  classes), `sectionDirty`, and `changed`. nav-mark today: `sectionDirty ? "" : active ? "nav-mark-green" : "nav-mark-ink"`
  — i.e. ALL non-active non-dirty rows get nav-mark-ink; round-2 restricts nav-mark-ink to done rows.
- Last-step button renders `submitting ? t("form.submitting") : isValidation ? t("validationPhase.approveSubmit") : schema.submit.label`.
  `schema.submit.label` is served per-locale by the backend canonical template — the label TEXT must
  stay schema-sourced; only the trailing arrow is a frontend presentation suffix.
- i18n namespace "intake" lives in frontend/src/locales/{nl,fr,en}/intake.json; the `"form"` object
  (nl lines 16-30) is where `"progress"` belongs.
- FieldRenderer has exactly ONE textarea: the `longtext` case (~line 84), using shared `inputCls`.
  Add `resize-y` on that element only — do NOT change `inputCls` (it is shared with inputs/selects).
</context>

<tasks>

<task type="auto">
  <name>Task 1: IntakeForm sidebar/header/grid redesign + form.progress i18n key</name>
  <files>frontend/src/components/intake/IntakeForm.tsx, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/intake.json</files>
  <action>
    All edits are presentation-only: no changes to goToSection/saveCurrentSection/handleSubmit/
    validateCurrent, state shape, or data fetching. Verify current classes before each edit — the
    canvas snapshot was hand-authored; apply intent, not blind string replace.

    1. Header: change `<header className="mb-10">` to `<header className="mb-12 border-b border-ink/15 pb-10">`.

    2. Grid: change `md:grid-cols-[320px_1fr]` to `md:grid-cols-[300px_minmax(0,1fr)]` (keep `grid gap-8`).
       Give the form-column `<div>` (the one containing the "Stap X / Y" row) `className="min-w-0"`.
       KEEP the aside's `hidden md:block` exactly as is.

    3. Sidebar structure: move `sticky top-8` from the nav to a new wrapper div inside the aside:
       `<div className="sticky top-8">` containing, in order:
       a. Progress header row: `<div className="flex items-baseline justify-between gap-3 px-3">` with
          - label: `<span className="font-mono text-[11px] uppercase tracking-wider text-ink/60">{t("form.progress")}</span>`
          - counter: `<span className="font-mono text-[11px] tabular-nums text-ink/60">{currentStep + 1} / {sections.length}</span>`
            (numeric counter rendered directly — no i18n interpolation needed; the translated
            "Stap X / Y" line in the form column is unchanged).
       b. Progress bar: track `<div className="mx-3 mt-2 h-1 bg-ink/10">` with fill
          `<div className="h-1 bg-ink" style={{ width: `${((currentStep + 1) / sections.length) * 100}%` }} />`.
       c. The existing nav, reclassed to `<nav className="mt-5 space-y-1 overflow-y-auto max-h-[calc(100vh-12rem)]">`
          (sticky removed here — it lives on the wrapper now).

    4. Section buttons — extend the EXISTING state logic (active / done / sectionDirty), putting the
       currently-unused `done` flag to work:
       - Insert a number column between the nav-mark span and the title span:
         `<span className={"w-6 shrink-0 tabular-nums " + (active || done ? "text-ink" : "text-ink/40")}>{String(idx + 1).padStart(2, "0")}</span>`
       - Button text/bg classes: active → `bg-paper2 text-ink` (unchanged); `done && !active` →
         `text-ink hover:bg-ink/5`; otherwise (future) → `text-ink/60 hover:bg-ink/5 hover:text-ink`.
       - nav-mark classes: keep the `sectionDirty → plain "nav-mark"` override first; then active →
         `nav-mark nav-mark-green` (unchanged); then `done` → `nav-mark nav-mark-ink`; else future →
         plain `nav-mark`. (This narrows today's "nav-mark-ink for every non-active row" to done rows only.)
       - Keep the `changed` badge, key, onClick, and the base
         `flex w-full items-start gap-2 px-3 py-2 text-left font-mono text-xs uppercase tracking-wider leading-[1.4] transition-colors` string untouched.

    5. Last-step submit button: keep the existing ternary and text sources; append the arrow only to
       the standard intake label: render `schema.submit.label + " →"` (i.e. `{schema.submit.label} →`)
       in the non-submitting, non-validation branch. Leave `t("form.submitting")` and
       `t("validationPhase.approveSubmit")` untouched (validation phase has its own approved wording).
       Do not hardcode "Verstuur intake" — the text stays schema-sourced per locale.

    6. i18n: add `"progress"` inside the `"form"` object of each locale file:
       nl → `"Voortgang"`, fr → `"Progression"`, en → `"Progress"`. Match existing JSON formatting
       (2-space indent, key order: place after `"step"`).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    Header has border-b hairline; grid is 300px/minmax with min-w-0 form column; sticky wrapper holds
    Voortgang label + n/total counter + h-1 progress bar + scrollable nav; rows show zero-padded
    numbers with current/completed/future states driven by existing active/done state; standard
    submit label ends with " →"; form.progress exists in all three intake.json files; tsc clean.
    `git diff` shows no changes to any handler/effect/state logic in IntakeForm.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 2: FieldRenderer textarea resize-y + build gate</name>
  <files>frontend/src/components/intake/FieldRenderer.tsx</files>
  <action>
    In the `longtext` case of FieldControl, change the textarea's className from `{inputCls}` to
    `{inputCls + " resize-y"}`. Do NOT modify the shared `inputCls` constant (it also styles inputs
    and selects) and do NOT touch any other control.

    Then run both gates from frontend/: `npx tsc --noEmit` and `npm run build:dev`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build:dev</automated>
  </verify>
  <done>
    The longtext textarea carries resize-y; no other element changed in FieldRenderer.tsx;
    tsc and build:dev both pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none changed | Presentation-only class/label edits; no new inputs, endpoints, packages, or data paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | IntakeForm.tsx behavior drift | mitigate | Done-criteria require git diff to show zero handler/state-logic changes; gates: tsc + build:dev |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes.
- `cd frontend && npm run build:dev` passes.
- `git diff frontend/src/components/intake/IntakeForm.tsx` contains only JSX/className/label-suffix
  changes — no edits inside handleChange, saveCurrentSection, goToSection, validateCurrent,
  handleNext, handleSubmit, doSubmit, or any useEffect.
- All three intake.json files parse (build gate covers this) and each contains form.progress.
</verification>

<success_criteria>
- Canvas round-2 chrome fused: header hairline, 300px/minmax grid + min-w-0, sidebar progress
  header + bar, numbered three-state section rows, scrollable nav, submit arrow, resize-y textareas.
- Mobile behavior (sidebar hidden below md) and all form behavior unchanged.
- "Voortgang" goes through i18n (form.progress) in nl/fr/en; submit label text stays schema-sourced.
- Both gates green.
</success_criteria>

<output>
Create `.planning/quick/260715-j7f-fuse-round-2-canvas-redesign-of-client-i/260715-j7f-SUMMARY.md` when done.
</output>
