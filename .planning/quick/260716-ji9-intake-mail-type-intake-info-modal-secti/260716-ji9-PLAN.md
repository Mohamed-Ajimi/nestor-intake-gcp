---
phase: quick-260716-ji9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/mail/templates/nl/intake.html.j2
  - backend/app/mail/templates/fr/intake.html.j2
  - backend/app/mail/templates/en/intake.html.j2
  - backend/app/mail/render.py
  - backend/app/api/intake_routes.py
  - frontend/src/lib/api/intakes.ts
  - frontend/src/components/intake/NextStepBanner.tsx
  - frontend/src/components/intake/RecipientPicker.tsx
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/en/intake.json
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/fr/admin.json
  - frontend/src/locales/en/admin.json
autonomous: true
requirements: [QUICK-260716-JI9]

must_haves:
  truths:
    - "In the draft phase (awaiting_client_submission), the banner's PRIMARY action opens the RecipientPicker for mail type 'intake'; 'Kopieer intake-link' remains as SECONDARY"
    - "POST /intakes/{id}/mail/intake sends a localized intake-invite mail to selected active members and writes a mail.sent audit row; it 409s when intake.status != 'draft'"
    - "The Intake-info dl no longer renders as the first page section; a mono-outline 'Intake-info' button in the sticky header opens a house-style overlay modal with the identical dl content"
    - "Intake section h2 titles render as authored (no lowercase transform); page h1 and archive dialog unchanged"
    - "All new user-facing strings exist in nl, fr, and en catalogs (frontend) and locale template dirs (backend)"
  artifacts:
    - path: "backend/app/mail/templates/nl/intake.html.j2"
      provides: "NL intake-invite mail body (CTA to {app_base_url}/intake/{intake_id})"
    - path: "backend/app/mail/templates/fr/intake.html.j2"
      provides: "FR intake-invite mail body"
    - path: "backend/app/mail/templates/en/intake.html.j2"
      provides: "EN intake-invite mail body"
    - path: "backend/app/mail/render.py"
      provides: "render_intake() mirroring render_validation()"
      contains: "def render_intake"
    - path: "backend/app/api/intake_routes.py"
      provides: "POST /{intake_id}/mail/intake + draft-only 409 gate + 'intake' rows in _SUBJECTS"
      contains: "mail/intake"
  key_links:
    - from: "frontend/src/routes/admin.pulse.intakes.$id.tsx"
      to: "POST /intakes/{id}/mail/intake"
      via: "setMailPickerType('intake') -> RecipientPicker -> handleSendMail -> sendIntakeMail"
      pattern: "setMailPickerType\\(\"intake\"\\)"
    - from: "backend/app/api/intake_routes.py"
      to: "backend/app/mail/render.py"
      via: "_run_intake_send calling mail_render.render_intake"
      pattern: "render_intake"
---

<objective>
Four intake-detail UX changes from UAT feedback: (1) a NEW "intake" mail type so the client
receives the intake fill-in link by email (draft status only), (2) the Intake-info block moves
from first page section into a header-button-triggered house-style modal, (3) intake section h2s
drop the `lowercase` Tailwind transform so schema titles render as authored, (4) all new strings
in nl/fr/en on both frontend catalogs and backend mail templates.

Purpose: The operator currently can only copy the intake link in draft; UAT wants a first-class
"mail the client the intake link" action, plus de-cluttering of the detail page.
Output: 3 new Jinja2 templates, render_intake(), a new gated send endpoint, and the reworked
intake-detail frontend, all typechecking and building.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@backend/app/api/intake_routes.py (lines 630-1000: _SUBJECTS, _subject_for, send endpoints, _run_intake_send; lines 1140-1160: the 409 status-gate idiom)
@backend/app/mail/render.py
@backend/app/mail/templates/nl/validation.html.j2 (pattern to mirror)
@frontend/src/lib/api/intakes.ts
@frontend/src/components/intake/NextStepBanner.tsx
@frontend/src/components/intake/RecipientPicker.tsx
@frontend/src/routes/admin.pulse.intakes.$id.tsx (lines 556-652 mail wiring; 889-1126 header/banner/picker/archive dialog; 1196-1279 Intake-info section; 1389 section h2)

<interfaces>
Existing contracts the executor builds against (already read — do not re-explore):

From backend/app/api/intake_routes.py:
- `_SUBJECTS: dict[str, dict[str, str]]` keyed locale -> mail_type -> subject template with `{client}` placeholder (nl/fr/en rows; nl is fallback via `_subject_for`).
- `_run_intake_send(intake_id, body, repo, identity, *, is_reminder, is_results) -> dict` — the shared send body: repo.get 404-gate, `_resolve_recipient_locales`, APP_BASE_URL refusal, per-locale render+send loop, then (2xx only) optional timestamp stamp + `audit.log(..., event_type="mail.sent", metadata={"type": mail_type, "recipient_count": total_recipients})`.
- IMPORTANT REALITY CHECK: the three existing send endpoints do NOT gate on intake.status server-side (results does not require delivered). The codebase's status-gating idiom is the transition guard: `raise HTTPException(status.HTTP_409_CONFLICT, f"Cannot submit an intake in status {current!r}")` (see `_submit_target`/`_review_target`, ~lines 1142-1159). Use THAT idiom for the new intake type only.

From backend/app/mail/render.py:
- `_localized_template(name, locale)` — loads `{locale}/{name}.html.j2` with nl fallback.
- `render_validation(*, first_name, project_title, cta_url, is_reminder, app_base_url=None, locale="nl") -> str` — the signature pattern to mirror (minus `is_reminder`).

From frontend/src/lib/api/intakes.ts:
- `export type IntakeMailType = "validation" | "reminder" | "results";`
- `sendIntakeMail(intakeId, type, recipients)` POSTs `/intakes/{id}/mail/{type}` — generic over the type union; needs NO body change, only the union extension.

From frontend/src/components/intake/NextStepBanner.tsx:
- `export type BusyKey = "runSkill" | "sendValidation" | ...` and `Props` with one `on*` callback per CTA; `case "awaiting_client_submission":` currently renders only `SecondaryBtn onCopyIntakeLink`.
- `PrimaryBtn`/`SecondaryBtn` local components; `secondaryCls` = the mono-outline button style (`border border-ink bg-transparent px-4 py-2 font-mono text-xs uppercase tracking-wider text-ink hover:border-2`).

From frontend/src/components/intake/RecipientPicker.tsx:
- `TYPE_COPY: Record<IntakeMailType, { title; confirm }>` keyed by mail type — extending the union forces adding an `intake` entry (tsc enforces exhaustiveness).

From frontend/src/routes/admin.pulse.intakes.$id.tsx:
- `mailPickerType: IntakeMailType | null` state; `MAIL_BUSY_KEY: Record<IntakeMailType, BusyKey>`; `handleSendMail` (generic — no per-type change needed); `onSendValidationMail = () => setMailPickerType("validation")`.
- Archive dialog convention (lines 1092-1126): `fixed inset-0 z-50 flex items-center justify-center bg-ink/40 p-4` overlay with backdrop-click close, inner `w-full max-w-md border border-ink bg-paper p-6 shadow-lg` panel with `stopPropagation`.
- Intake-info section (lines 1196-1279): `<section>` with h2 `intakeDetail.info.title` + `<dl>` of `Meta` rows (client link, product, StatusPill, createdAt, lastEdited, conditional DeliveredAtEditor, initialIntakeLink LinkRow, validationLink LinkRow, validation state, ResultsLinkRow). `Meta`, `LinkRow`, `ResultsLinkRow`, `StatusPill`, `DeliveredAtEditor` are local helpers at file bottom — the dl moves verbatim, helpers stay.

i18n namespaces: `nextStep.*` and `recipients.*` live in `frontend/src/locales/{nl,fr,en}/intake.json`; `intakeDetail.*` in `frontend/src/locales/{nl,fr,en}/admin.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend "intake" mail type — templates, render_intake, gated endpoint</name>
  <files>backend/app/mail/templates/nl/intake.html.j2, backend/app/mail/templates/fr/intake.html.j2, backend/app/mail/templates/en/intake.html.j2, backend/app/mail/render.py, backend/app/api/intake_routes.py</files>
  <action>
    Author-by-construction (no local Python runtime — deploy + Cloud Build suite run happen post-task; keep every change a strict pattern mirror).

    1. Templates: create `intake.html.j2` in ALL THREE locale dirs, each `{% extends "_base.html.j2" %}` with a `{% block content %}` mirroring `validation.html.j2`'s structure (h1 greeting `Hi {{ first_name or "team" }}`, intro paragraph, CTA anchor `class="btn"` on `{{ cta_url }}`, sign-off `— Team Agenic`, `.footer` div referencing `{{ project_title }}`). Prose: an invitation to fill in the intake form (nl: "Open intake-formulier →" CTA, ~"We hebben een intake voor jullie klaargezet. Vul het formulier in wanneer het jullie past — het vormt de basis voor ons onderzoek."; fr/en: faithful translations preserving the same shape, matching the register of the existing fr/en validation.html.j2 variants — read those two files for tone before authoring). NO `is_reminder` branch, NO `| safe` anywhere (autoescape stays the XSS guard, T-10-01). Include the same explanatory Jinja comment header convention as the siblings.

    2. `backend/app/mail/render.py`: add `render_intake(*, first_name: str, project_title: str, cta_url: str, app_base_url: str | None = None, locale: str = "nl") -> str` directly after `render_validation`, calling `_localized_template("intake", locale).render(...)` — an exact mirror of `render_validation` minus `is_reminder`, docstring noting cta_url is the token-free `{app_base_url}/intake/{intake_id}` app route (NOTIF-01) and nl fallback (D-07).

    3. `backend/app/api/intake_routes.py`:
       - `_SUBJECTS`: add an `"intake"` key to each of the nl/fr/en rows (nl: `"Jullie intake staat klaar — {client}"`, fr: `"Votre intake est prêt — {client}"`, en: `"Your intake is ready — {client}"`). Do not touch the existing keys or `_subject_for`.
       - `_run_intake_send`: add keyword-only `is_intake: bool = False`. When true: FIRST gate status using the transition-guard idiom — `if intake.status != "draft": raise HTTPException(status.HTTP_409_CONFLICT, f"Cannot send the intake mail in status {intake.status!r}")` — placed right after the 404 gate; then set `cta_url = f"{base}/intake/{intake.id}"`, `mail_type = "intake"`, `timestamp_field = None` (NO new column/migration — no sent-at for this type), and render via `mail_render.render_intake(first_name=client, project_title=client, cta_url=cta_url, app_base_url=settings.app_base_url, locale=locale)` in the per-locale loop. Existing callers pass no new argument, so validation/reminder/results behavior is byte-identical (verify by reading the diff: no existing line's semantics change).
       - New endpoint `POST /{intake_id}/mail/intake` (`send_intake_mail`) next to the other three send verbs, delegating `_run_intake_send(intake_id, body, repo, identity, is_reminder=False, is_results=False, is_intake=True)`, docstring mirroring the siblings (404 existence-hidden D-07, D-06 membership-id recipients, draft-only 409 gate, mail.sent audit on 2xx, no timestamp column).
  </action>
  <verify>
    <automated>ls backend/app/mail/templates/nl/intake.html.j2 backend/app/mail/templates/fr/intake.html.j2 backend/app/mail/templates/en/intake.html.j2 && grep -c "def render_intake" backend/app/mail/render.py && grep -c "mail/intake" backend/app/api/intake_routes.py && grep -c "Cannot send the intake mail in status" backend/app/api/intake_routes.py && ! grep -rn "| safe" backend/app/mail/templates/*/intake.html.j2</automated>
  </verify>
  <done>3 locale templates exist with no `| safe`; render_intake defined; `/mail/intake` route registered with the draft-only 409 gate; `"intake"` subject present in all three _SUBJECTS rows; zero semantic changes to the validation/reminder/results paths.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — intake mail CTA, Intake-info modal, section-heading casing</name>
  <files>frontend/src/lib/api/intakes.ts, frontend/src/components/intake/NextStepBanner.tsx, frontend/src/components/intake/RecipientPicker.tsx, frontend/src/routes/admin.pulse.intakes.$id.tsx</files>
  <action>
    (1) Intake mail type:
    - `intakes.ts`: extend the union — `export type IntakeMailType = "intake" | "validation" | "reminder" | "results";` and update the doc comment ("four client-facing send verbs"). `sendIntakeMail` needs no change.
    - `NextStepBanner.tsx`: add `"sendIntake"` to `BusyKey` and `onSendIntakeMail: () => void` to `Props`. In `case "awaiting_client_submission":` make the PRIMARY action `<PrimaryBtn onClick={props.onSendIntakeMail} busy={busy.sendIntake}>{t("nextStep.sendIntakeMail")}</PrimaryBtn>` and keep the existing copy-link button as `SecondaryBtn` (order: primary first).
    - `RecipientPicker.tsx`: add `intake: { title: t("recipients.titleIntake"), confirm: t("recipients.confirmIntake") }` to `TYPE_COPY` (tsc forces this once the union grows).
    - Route `admin.pulse.intakes.$id.tsx`: add `intake: "sendIntake"` to `MAIL_BUSY_KEY`, add `const onSendIntakeMail = () => setMailPickerType("intake");` beside `onSendValidationMail`, and pass `onSendIntakeMail={onSendIntakeMail}` to `<NextStepBanner>`. `handleSendMail` and the RecipientPicker mount are type-generic — no changes; the validation/reminder/results paths must remain untouched.

    (2) Intake-info modal:
    - Add `const [infoModalOpen, setInfoModalOpen] = useState(false);` next to `archiveConfirmOpen`.
    - In the sticky header's action cluster (the `flex flex-wrap items-center gap-2` div holding the status select and Edit button), add a mono-outline button BEFORE the status select: `border border-ink bg-paper px-3 py-1.5 font-mono text-xs uppercase tracking-wider text-ink hover:border-2` labeled `{t("intakeDetail.info.openButton")}`, onClick `() => setInfoModalOpen(true)`.
    - DELETE the first `<section>` in `<main>` (lines ~1197-1279, the Intake-info h2 + dl) and re-house the ENTIRE `<dl>...</dl>` content VERBATIM inside a new modal rendered next to the archive dialog, following its exact overlay convention: `{infoModalOpen && (<div className="fixed inset-0 z-50 flex items-center justify-center bg-ink/40 p-4" onClick={() => setInfoModalOpen(false)}><div role="dialog" className="max-h-[85vh] w-full max-w-2xl overflow-y-auto border border-ink bg-paper p-6 shadow-lg" onClick={(e) => e.stopPropagation()}>...` — panel is max-w-2xl + scrollable (the dl is tall), unlike archive's max-w-md. Heading: `<h2 className="border-b border-ink/30 pb-2 mb-2 font-serif text-2xl font-normal text-ink">{t("intakeDetail.info.title")}</h2>` — NO `lowercase` class. Footer: a right-aligned close button reusing the archive-dialog cancel styling, labeled `{t("intakeDetail.info.close")}`. The local helpers `Meta`, `LinkRow`, `ResultsLinkRow`, `StatusPill`, `DeliveredAtEditor` stay where they are and keep working (the dl moves, nothing else).

    (3) Casing: remove ONLY the `lowercase` class from the schema-section h2 (line ~1389, `{section.title}`). LEAVE UNCHANGED: page h1 (line ~907), archive dialog h2 (line ~1102), RecipientPicker DialogTitle, and all other house chrome.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>tsc clean; draft-phase banner shows the intake-mail primary + copy-link secondary; picker handles type "intake"; Intake-info renders only inside the header-triggered modal (section gone from main flow); section h2s no longer force-lowercase; existing send paths untouched.</done>
</task>

<task type="auto">
  <name>Task 3: i18n catalogs (nl/fr/en) + build gate</name>
  <files>frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/intake.json, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <action>
    Add every key referenced in Task 2 to ALL THREE locales, placed adjacent to their sibling keys (match each file's existing ordering/style):

    `intake.json` — under `nextStep`: `sendIntakeMail` (nl "Verstuur intake-link via mail", fr "Envoyer le lien d'intake par e-mail", en "Send intake link by email"). Under `recipients`: `titleIntake` (nl "Intake-link versturen", fr "Envoyer le lien d'intake", en "Send intake link") and `confirmIntake` (nl "Verstuur intake-link", fr "Envoyer le lien", en "Send intake link") — mirror the phrasing register of the existing titleValidation/confirmValidation entries in each locale.

    `admin.json` — under `intakeDetail.info`: `openButton` (nl "Intake-info", fr "Infos intake", en "Intake info") and `close` (nl "Sluiten", fr "Fermer", en "Close"); if a suitable close label already exists under `intakeDetail` reuse it instead of adding `close`. Check the existing `intakeDetail.info.title` value in each locale: now that the modal heading has no `lowercase` class, the catalog value itself must be authored-case — set nl to "Intake-info" (fr/en equivalents capitalized) if currently lowercase.

    Then run both frontend gates. Note for the SUMMARY: backend changes are author-by-construction — Cloud Build image rebuild + deploy + full suite run happen post-task (see project memory phase-07-deployed-suite-green for the runbook).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build:dev</automated>
  </verify>
  <done>All new keys present in nl/fr/en; no raw i18n key names render (keys referenced in code all resolve); tsc and build:dev both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI -> API mail send | operator-selected membership ids cross into the send endpoint |
| API -> Resend | rendered HTML with intake-derived client_name interpolations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ji9-01 | Spoofing/Info disclosure | POST /mail/intake | mitigate | reuse _run_intake_send unchanged: repo.get 404 existence-hidden (D-07), recipients only server-issued membership ids re-validated against the intake's own space (D-06), no free-text address |
| T-ji9-02 | Tampering (XSS in mail) | intake.html.j2 x3 | mitigate | autoescape ON via existing _env; no `\| safe` in any variant (T-10-01 gate in Task 1 verify) |
| T-ji9-03 | Elevation (send outside lifecycle) | POST /mail/intake | mitigate | draft-only 409 gate mirroring the transition-guard idiom; other types' behavior unchanged |
| T-ji9-SC | Tampering | package installs | accept | no new dependencies introduced by this plan |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` and `npm run build:dev` pass.
- Backend grep gates (Task 1 verify) pass; no existing send-path line semantically changed (diff review).
- No changes under `frontend/src/components/ui/`; no DB migration/column added.
</verification>

<success_criteria>
- Draft-phase banner: primary "Verstuur intake-link via mail" opens RecipientPicker(type "intake"); copy-link demoted to secondary; picker send hits POST /intakes/{id}/mail/intake.
- Server 409s the intake mail for any non-draft status; validation/reminder/results endpoints byte-identical in behavior.
- Intake-info lives only in the header-button modal (house overlay style, authored-case heading); section h2s render schema titles as authored.
- nl/fr/en coverage complete on both sides (catalogs + mail templates/subjects).
</success_criteria>

<output>
Create `.planning/quick/260716-ji9-intake-mail-type-intake-info-modal-secti/260716-ji9-SUMMARY.md` when done.
Note in the SUMMARY: backend deploy (image rebuild) + Cloud Build suite run are post-task follow-ups.
</output>
