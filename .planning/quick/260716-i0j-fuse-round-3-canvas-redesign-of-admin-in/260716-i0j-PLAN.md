---
phase: quick-260716-i0j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/components/intake/NextStepBanner.tsx
  - frontend/src/components/admin/ProductShell.tsx
  - frontend/src/components/intake/FieldRenderer.tsx
  - frontend/src/components/intake/FieldDisplay.tsx
  - frontend/src/components/intake/ContextPackBlock.tsx
  - frontend/src/components/intake/RecipientPicker.tsx
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/fr/admin.json
  - frontend/src/locales/en/admin.json
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/en/intake.json
autonomous: true
requirements: []   # quick task — no roadmap REQ ids; scope defined by DIFF-NOTES-3.md + locked decisions

must_haves:
  truths:
    - "Workflow panel renders as ONE bordered card in order: stepper → status strip → NextStepBanner → semantic search / scope-note"
    - "Sticky header shows only the status select (no StatusPill); the Intake-info Status row still shows the badge"
    - "Sidebar stays pinned (sticky, own scroll) while the page scrolls"
    - "Archiving asks via a house-style dialog, not native confirm()"
    - "In edit mode, a removed file stays visible with strikethrough + 'wordt verwijderd bij opslaan' chip + working Herstel undo; save still deletes queued paths, cancel still drops the queue"
    - "Context-pack card shows an inline preview of the pack's first section without opening the modal"
    - "Recipient rows show 'Naam · email' inline in both validation and results pickers"
    - "Every new user-facing string resolves in nl, fr, and en"
  artifacts:
    - path: "frontend/src/routes/admin.pulse.intakes.$id.tsx"
      provides: "Merged workflow panel, restyles R3–R8, archive dialog state, scope-note block, onUndoDeferRemove wiring"
    - path: "frontend/src/components/intake/FieldRenderer.tsx"
      provides: "Deferred-delete visualization (dashed row + Herstel) in FileControl"
      contains: "onUndoDeferRemove"
    - path: "frontend/src/components/intake/ContextPackBlock.tsx"
      provides: "Inline first-section preview card"
      contains: "contextPack.previewLabel"
    - path: "frontend/src/locales/fr/admin.json"
      provides: "archiveDialog + scopeNote keys (proof all 3 locales filled)"
      contains: "archiveDialog"
  key_links:
    - from: "frontend/src/components/intake/FieldRenderer.tsx"
      to: "frontend/src/routes/admin.pulse.intakes.$id.tsx"
      via: "onUndoDeferRemove prop filtering pendingRemovals ref"
      pattern: "onUndoDeferRemove"
    - from: "frontend/src/routes/admin.pulse.intakes.$id.tsx"
      to: "NextStepBanner"
      via: "banner rendered as border-t section INSIDE the mb-8 panel wrapper"
      pattern: "border border-ink/15 bg-paper"
---

<objective>
Fuse the round-3 Claude Design canvas redesign of the admin intake detail page into the React frontend: the merged workflow panel + all recurring restyles (R1–R9), the four KEPT proposals as new additive functionality (archive dialog, deferred-delete visualization, inline context-pack preview, inline recipient emails), the scope-note block, with all new strings in nl/fr/en.

Purpose: the operator endorsed these design changes on claude.ai/design (project "Nestor Pulse — Pages"); this lands them in the real app before parity UAT.
Output: updated route + 6 components + 6 locale catalogs, `npx tsc --noEmit` clean per task, `npm run build:dev` green at the end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/design/intake-detail-round3/DIFF-NOTES-3.md   ← the authoritative worklist (R1–R9, S1–S7, D1–D6, IGNORE list)
@frontend/src/routes/admin.pulse.intakes.$id.tsx
@frontend/src/components/intake/NextStepBanner.tsx
@frontend/src/components/admin/ProductShell.tsx
@frontend/src/components/intake/FieldRenderer.tsx
@frontend/src/components/intake/FieldDisplay.tsx
@frontend/src/components/intake/ContextPackBlock.tsx
@frontend/src/components/intake/RecipientPicker.tsx

Pulled snapshots (design target, read ONLY if a specific block's markup is ambiguous — ~40-75KB each):
.planning/design/intake-detail-round3/pulled/

Do NOT read .planning/design/intake-detail-round3/raw.diff (4845 lines — DIFF-NOTES-3.md distills it fully).
Do NOT modify anything under frontend/src/components/ui/ (using alert-dialog/dialog as-is is fine).
</context>

<interfaces>
Key contracts the executor needs (already verified against source at commit 792237d):

Route `admin.pulse.intakes.$id.tsx` — WR-04 deferred-delete contract (MUST keep identical apart from the new undo):
```typescript
const pendingRemovals = useRef<string[]>([]);          // line ~214, a ref, never drives render
// edit loop passes: onDeferRemove={(paths) => { pendingRemovals.current.push(...paths); }}  (~line 1367)
// handleSave flushes pendingRemovals AFTER saveAnswers succeeds (~line 814)
// handleCancel clears pendingRemovals.current = [] (~line 783)
// onArchive (~line 699): if (!confirm(t("intakeDetail.confirm.archive"))) return;  ← replace with dialog
```

FieldRenderer `Props` (line 7): `{ field, value, onChange, intakeId, error?, disabled?, onDeferRemove?: (paths: string[]) => void }`
FileControl `removeStoredObject(path)` (line ~418): defers via `onDeferRemove([path])` when present, else deletes inline.
File value entries have shape `{ path, filename, size, uploaded_at }`.

ContextPackBlock: `latestPack: Pack | null` with `output: string | null` (full pack markdown, already fetched by `loadLatest`); the "Laatst gegenereerd" meta line renders at ~line 393; the questions accordion (`questions.length > 0`) starts at ~line 404.

RecipientPicker row label (line 139): `const label = m.name ?? m.email ?? t("recipients.noName");` — `SpaceMember` has `id`, `name`, `email`.

NextStepBanner outer div (line 319-321): `className="mb-5 border border-ink/30 border-l-4 bg-paperLight px-6 py-5"` with inline `style={{ borderLeftColor: accentColor }}`.

Locale catalogs: `frontend/src/locales/{nl,fr,en}/{admin,intake}.json`. Route + Meta/LinkRow use `useTranslation("admin")`; FieldRenderer/ContextPackBlock/RecipientPicker/NextStepBanner use `useTranslation("intake")`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Merged workflow panel + all recurring restyles (R1–R9)</name>
  <files>frontend/src/routes/admin.pulse.intakes.$id.tsx, frontend/src/components/intake/NextStepBanner.tsx, frontend/src/components/admin/ProductShell.tsx, frontend/src/components/intake/FieldDisplay.tsx</files>
  <action>
Presentation-only pass. Preserve every handler, effect, condition, and prop exactly — only wrappers, class strings, and JSX ORDER change. Line numbers are as of commit 792237d.

**ProductShell.tsx (R1):**
- Root div (line 39): `flex min-h-screen bg-paper` → add `overflow-x-clip`.
- `<aside>` (line 40): add `sticky top-0 h-screen overflow-y-auto` to the existing classes.

**NextStepBanner.tsx (R2):**
- Outer div (line 320): `mb-5 border border-ink/30 border-l-4 bg-paperLight px-6 py-5` → `border-t border-ink/10 border-l-4 bg-paperLight px-6 py-5`. Keep the inline `borderLeftColor` accent and ALL inner markup unchanged.

**Route — the big JSX reorder (R2 + R9).** Current order after the sticky header: NextStepBanner (~977) → RecipientPicker mount (~1001) → semantic search (~1015) → stepper (~1057) → AI-review block (~1068) → success modal (~1084) → editBanner (~1094) → statusBanner (~1105) → grid. Rebuild as:

1. Sticky header (unchanged position).
2. NEW panel wrapper `<div className="mb-8 border border-ink/15 bg-paper">` containing, in order:
   a. Stepper: the existing `<div className="mb-6"><IntakeWorkflowStepper …/></div>` moves here with wrapper class changed to `px-6 pt-6 pb-4`. IntakeWorkflowStepper props and internals unchanged.
   b. Status strip: the existing `{!editMode && !reviewMode && intake.status && STATUS_WITH_BANNER.has(intake.status) && (…)}` block, with its div restyled from `mb-6 border border-ink/10 bg-paper2 px-4 py-3 text-sm text-ink/70` to `border-t border-ink/10 bg-paper2 px-6 py-3 text-xs text-ink/70`. Condition unchanged.
   c. `<NextStepBanner …/>` with the exact same prop list (it renders its own `border-t` section now; returns null for unknown phases — panel then shows stepper-only, which matches the snapshots).
   d. Semantic search: move the whole `{showSemanticSearch && (<section …>)}` block here as-is EXCEPT the section classes: `border border-ink/20 bg-paperLight p-4 mb-6` → `border-t border-ink/10 bg-paperLight p-4` (locked decision: normalize the nested double border, drop the inner mb-6 — this is DIFF-NOTES D6's normalization variant). Inner markup, input, 🔍 button, and result rows byte-identical.
   e. Scope-note placeholder: leave a `{/* scope-note (S6) — Task 2 */}` comment as the panel's last child slot.
3. RecipientPicker mount (dialog — keep right after the panel, unchanged).
4. AI-review block (`data-ai-review-block`), success modal, editBanner (`border-l-agenic-yellow`) — all stay OUTSIDE/below the panel in their current relative order, unchanged.
5. Grid (sidebar nav + main) unchanged in position.

**Route — header restyles (R3):**
- Status `<select>` (line 925): `border border-ink/30 bg-paper px-2.5 py-1.5 font-mono text-xs uppercase tracking-wider text-ink focus:border-ink focus:outline-none` → `border border-ink bg-paper px-3 py-1.5 font-mono text-xs uppercase tracking-wider text-ink focus:outline-none`.
- DELETE `<StatusPill status={intake.status} />` at line 934 (header only). KEEP the `StatusPill` component definition and its usage in the Intake-info Meta row (line ~1167) — per D1, accepted as designed.

**Route — content restyles (R4–R8):**
- Intake-info h2 (line 1141): `text-sm font-semibold uppercase tracking-wide text-ink/60` → `border-b border-ink/30 pb-2 mb-2 font-serif text-2xl font-normal lowercase text-ink`. The `dl mt-4` stays.
- Meta helper `<dd>` (line 1416): `font-sans text-ink` → `min-w-0 font-sans text-ink`. Grep the route for any other inline `<dd` and add `min-w-0` there too.
- Section loop `<section>` (line 1330): `scroll-mt-32` → `scroll-mt-32 border border-ink/10 bg-paper p-6`. (Intake-info section already has the box — unchanged.)
- LinkRow + ResultsLinkRow Kopieer buttons (lines 1506 and 1550): both become `inline-flex items-center gap-1.5 border border-ink px-2.5 py-1 font-mono text-[10px] uppercase tracking-wider text-ink hover:bg-ink/5`. (There is no separate primary Kopieer variant in the current route — nothing else to touch.)
- Edit-mode Annuleer button (line 950): → `inline-flex items-center gap-1.5 border border-ink bg-paper px-3 py-1.5 font-mono text-xs uppercase tracking-wider text-ink hover:bg-ink/5`.
- Edit-mode Opslaan button (line 959): → `inline-flex items-center gap-1.5 bg-ink px-3 py-1.5 font-mono text-xs uppercase tracking-wider text-paper hover:bg-ink/90 disabled:opacity-50`. Keep Loader2/Save icon logic.
- DeliveredAtEditor save button (line 1460): NOT in any snapshot — leave as-is (do not restyle).

**FieldDisplay.tsx (R5):** `<dd>` (line 115): `font-sans text-ink` → `min-w-0 font-sans text-ink`.

IGNORE-list guard: no `.html` hrefs, no `<option selected>`, no sample content, no logo/entity changes leak in from the snapshots.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Route compiles; panel wrapper `mb-8 border border-ink/15 bg-paper` contains stepper → status strip → NextStepBanner → semantic search in that JSX order; header has no StatusPill; grep confirms `min-w-0 font-sans text-ink` in both Meta and FieldDisplay dd; all existing props/handlers untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Four KEPT proposals + scope-note (S1–S4, S6) — additive functionality</name>
  <files>frontend/src/routes/admin.pulse.intakes.$id.tsx, frontend/src/components/intake/FieldRenderer.tsx, frontend/src/components/intake/ContextPackBlock.tsx, frontend/src/components/intake/RecipientPicker.tsx</files>
  <action>
All frontend-only, all additive. Use the i18n keys listed in Task 3 (call `t()` with those keys now; catalogs are filled in Task 3 — missing keys render the key string until then, tsc is unaffected).

**S3 — Archive confirm dialog (route).** Replace the native `confirm()` in `onArchive` (~line 701):
- Add `const [archiveConfirmOpen, setArchiveConfirmOpen] = useState(false);`
- `onArchive` becomes: `if (!intake) return; setArchiveConfirmOpen(true);`
- New `confirmArchive` handler with the previous body: `setArchiveConfirmOpen(false); setBusyKey("archive", true); await handleStatusChange("archived"); setBusyKey("archive", false);`
- Render a hand-rolled fixed-overlay dialog (this matches the codebase's existing modal convention — ContextPackBlock viewer + ReviewSuccessModal are hand-rolled; do NOT fight shadcn AlertDialog default styling): overlay `fixed inset-0 z-50 flex items-center justify-center bg-ink/40 p-4` (close on overlay click), card `w-full max-w-md border border-ink bg-paper p-6 shadow-lg` with `role="alertdialog"` and `onClick={(e) => e.stopPropagation()}`. Inside:
  - h2 `font-serif text-2xl font-normal lowercase text-ink` → `t("intakeDetail.archiveDialog.title")`
  - body p `mt-3 font-sans text-sm leading-relaxed text-ink/70` → `t("intakeDetail.archiveDialog.body")`
  - footer `mt-6 flex justify-end gap-2`: Annuleer button `border border-ink bg-paper px-4 py-2 font-mono text-xs uppercase tracking-wider text-ink hover:border-2` → closes; Archiveer button `bg-ink px-4 py-2 font-mono text-xs uppercase tracking-wider text-paper hover:bg-ink/85` → `confirmArchive`.
- The `confirm(discardChanges)` in `handleCancel` (line 779) was NOT redesigned — leave it.
- The existing `intakeDetail.confirm.archive` key stays in the catalogs (unused is fine; do not remove).

**S6 — Scope-note block (route, panel's last child slot from Task 1).** Persistent note for decomposed-and-later statuses (locked decision: "decomposed+"):
```
{["decomposed", "in_research", "delivered"].includes(intake.status ?? "") && ( ... )}
```
Div classes normalized to the panel idiom (matches the status strip): `border-t border-ink/10 bg-paper2 px-6 py-3 text-sm text-ink/70`, containing `<span className="mr-2 font-mono text-[10px] uppercase tracking-wider text-ink/50">{t("intakeDetail.scopeNote.label")}</span>` followed by the body via `<Trans i18nKey="intakeDetail.scopeNote.body" ns="admin" components={[<span className="font-mono text-xs" />]} />` (import `Trans` from react-i18next; NextStepBanner shows the pattern). Keep the existing `onStartAutoResearch` toast unchanged (D5: harmless duplication, keep both).

**S2 — Deferred-delete visualization (FieldRenderer + route).** The WR-04 contract stays identical: value changes still drop the file, `pendingRemovals` still flushes on save / clears on cancel. Add only visualization + undo:
- Extend `Props` (and FileControl's props) with `onUndoDeferRemove?: (paths: string[]) => void;` — thread it through `FieldControl` → `FileControl` for `file`/`files` cases exactly like `onDeferRemove`.
- In `FileControl`, add local state `const [deferredFiles, setDeferredFiles] = useState<any[]>([]);` (entries are the full `{ path, filename, size, uploaded_at }` objects). Only used when `onDeferRemove` is present (edit-mode draft). The control unmounts when edit mode exits (read mode renders FieldDisplay), so the state cannot leak across save/cancel.
- In `removeFile(idx)`: when `onDeferRemove` is present and the entry has a `path`, push the entry into `deferredFiles` before the existing `removeStoredObject` + `onChange` calls. (Non-deferred/client mode: behavior unchanged.) Leave `handleSlot`'s replace-path deferral exactly as-is — a replaced slot shows the new file; no viz per the snapshots.
- Render the deferred rows: when `deferredFiles.length > 0`, append after the existing file list (plain-list variant) AND after the slots grid (slots variant) a `<ul className="space-y-1 mt-2">` of rows per the kept snapshot markup:
```
<li className="flex items-center justify-between border border-dashed border-ink/30 bg-paper2/50 px-3 py-2 text-sm">
  <span className="truncate text-ink/40 line-through">{f.filename}</span>
  <span className="ml-3 flex shrink-0 items-center gap-2">
    <span className="font-mono text-[10px] uppercase tracking-wider text-ink/50">{t("field.pendingDelete")}</span>
    <button type="button" className="text-xs font-medium text-ink underline underline-offset-2 hover:text-ink/70">{t("field.restore")}</button>
  </span>
</li>
```
- Herstel handler: remove the entry from `deferredFiles`, call `onUndoDeferRemove?.([entry.path])`, and re-add the entry to the value: `multi ? onChange([...files, entry]) : onChange(entry)` (single-file restore only when the slot is empty — if a new file was uploaded meanwhile for a non-multi field, keep the restore button disabled via `disabled={!multi && files.length > 0}` with `disabled:opacity-40`).
- Route: pass the new prop in the edit loop next to `onDeferRemove` (~line 1367): `onUndoDeferRemove={(paths) => { pendingRemovals.current = pendingRemovals.current.filter((p) => !paths.includes(p)); }}`.

**S4 — Inline context-pack preview (ContextPackBlock).** Between the "Laatst gegenereerd" meta div (ends ~line 401) and the `{error && …}` line, when `latestPack?.output` is non-empty render:
```
<div className="mt-4 border border-ink/15 bg-paper p-4">
  <div className="mb-2 font-mono text-[10px] uppercase tracking-wider text-ink/50">{t("contextPack.previewLabel")}</div>
  <p className="font-serif text-lg lowercase text-ink">{preview.heading}</p>
  <p className="mt-1 font-sans text-sm leading-relaxed text-ink/80">{preview.paragraph}</p>
</div>
```
Add a small pure helper `extractPackPreview(markdown: string): { heading: string; paragraph: string } | null` in the same file (module scope, next to `downloadContextPackPDF`): strip the top-level `# Context Pack…` line (reuse the existing `replace(/^#\s+Context Pack[^\n]*\n+/i, "")` idiom), find the FIRST line starting with `## `, take its text as `heading`, then collect subsequent non-empty lines until the next blank line or next heading as `paragraph` (join with a space, strip `**`). If no `## ` heading exists, fall back to the first non-empty paragraph with heading = first line. Return null on empty input (render nothing). First-section-only per D4 (matches the snapshot) — plain text slicing, NO dangerouslySetInnerHTML, no new markdown rendering.

**S1 — Recipient rows show email inline (RecipientPicker).** Per locked decision D2, the step-15 variant applies to BOTH validation and results pickers. Replace the label span (line ~149):
```
<span className="font-sans text-sm text-ink">
  {m.name ?? m.email ?? t("recipients.noName")}
  {m.name && m.email && <span className="text-ink/50"> · {m.email}</span>}
</span>
```
(No duplicate email when the member has no name.) No other picker changes.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Native `confirm(t("intakeDetail.confirm.archive"))` no longer exists in the route; `onUndoDeferRemove` is threaded FieldRenderer Props → FileControl and wired to a pendingRemovals filter in the route; ContextPackBlock renders the preview card when `latestPack.output` exists; RecipientPicker rows include the `· email` span; scope-note renders inside the panel for decomposed/in_research/delivered; discardChanges confirm() untouched.</done>
</task>

<task type="auto">
  <name>Task 3: i18n catalogs (nl/fr/en) + final build gate</name>
  <files>frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/intake.json</files>
  <action>
Add ALL new keys to all three locales. Match each file's existing nesting style (the `intakeDetail.*` keys in admin.json and `field.*` / `contextPack.*` keys in intake.json show the structure — nest under the existing objects, do not create flat dotted keys if the file uses nesting).

**admin namespace** (`intakeDetail.archiveDialog.*`, `intakeDetail.scopeNote.*`):

| Key | nl | fr | en |
|---|---|---|---|
| archiveDialog.title | Intake archiveren? | Archiver l'intake ? | Archive intake? |
| archiveDialog.body | Het project wordt afgesloten en verdwijnt uit de actieve intake-lijst. De intake wordt alleen-lezen; de resultaten-pagina van de klant blijft bereikbaar. | Le projet sera clôturé et disparaîtra de la liste des intakes actifs. L'intake passe en lecture seule ; la page de résultats du client reste accessible. | The project will be closed and removed from the active intake list. The intake becomes read-only; the client's results page remains available. |
| archiveDialog.confirm | Archiveer | Archiver | Archive |
| archiveDialog.cancel | Annuleer | Annuler | Cancel |
| scopeNote.label | Einde platform-scope | Fin du périmètre plateforme | End of platform scope |
| scopeNote.body | Automatische research valt buiten deze fase (stopt bij <0>decomposed</0>). De onderzoeksfase wordt buiten dit systeem gestart en opgevolgd. | La recherche automatique sort du cadre de cette phase (s'arrête à <0>decomposed</0>). La phase de recherche est lancée et suivie en dehors de ce système. | Automated research is out of scope for this phase (stops at <0>decomposed</0>). The research phase is started and tracked outside this system. |

(`<0>…</0>` is the Trans component slot — keep `decomposed` inside it in all three locales; it is a status literal, never translated.)

**intake namespace** (`field.*`, `contextPack.*`):

| Key | nl | fr | en |
|---|---|---|---|
| field.pendingDelete | wordt verwijderd bij opslaan | sera supprimé à l'enregistrement | will be deleted on save |
| field.restore | Herstel | Restaurer | Restore |
| contextPack.previewLabel | Inhoud — preview | Contenu — aperçu | Contents — preview |

Then cross-check: grep the Task-2 diff for every `t("` call using a new key and confirm each of the 9 keys exists in all three locale files (27 entries total).

**Final gates** (in `frontend/`):
1. `npx tsc --noEmit` — zero errors.
2. `npm run build:dev` — build completes.
3. Locale-completeness spot check: `node -e "['nl','fr','en'].forEach(l=>{const a=require('./src/locales/'+l+'/admin.json');const i=require('./src/locales/'+l+'/intake.json');const miss=[];if(!a.intakeDetail?.archiveDialog?.title)miss.push(l+':admin.archiveDialog');if(!a.intakeDetail?.scopeNote?.body)miss.push(l+':admin.scopeNote');if(!i.field?.pendingDelete||!i.field?.restore)miss.push(l+':intake.field');if(!i.contextPack?.previewLabel)miss.push(l+':intake.contextPack');if(miss.length){console.error('MISSING',miss);process.exit(1)}});console.log('i18n OK')"` — adapt the property paths if a catalog nests differently, but the check must assert all 9 keys × 3 locales.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build:dev</automated>
  </verify>
  <done>All 9 new keys present in nl/fr/en (verified by the node check), tsc clean, build:dev green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pack markdown → preview card | AI-generated pack content rendered inline (previously modal-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q3-01 | Tampering | ContextPackBlock preview | mitigate | `extractPackPreview` is plain-string slicing rendered as React text nodes — no dangerouslySetInnerHTML, no rehype-raw on the preview path |
| T-q3-02 | Elevation | Archive dialog | accept | Dialog is UX-only; `handleStatusChange("archived")` unchanged, backend remains the transition authority |
| T-q3-03 | Tampering | Deferred-delete undo | accept | Un-defer only filters a client-side ref; all deletes still go through the authenticated storage.removeFile seam after save |
| T-q3-SC | Tampering | package installs | accept | No new dependencies installed in this task |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` after each task; `npm run build:dev` as final gate.
- Grep gates: `grep -c "confirm(t(\"intakeDetail.confirm.archive\"))" frontend/src/routes/admin.pulse.intakes.\$id.tsx` returns 0; `grep -c "onUndoDeferRemove" frontend/src/components/intake/FieldRenderer.tsx` ≥ 3 (prop, thread, call).
- Contract invariants (code review, no behavior test exists): `pendingRemovals` still flushed only after successful save, still cleared on cancel; `sectionDirty`/changedKeys logic untouched; no backend/API changes; no `frontend/src/components/ui/` edits.
</verification>

<success_criteria>
- All R1–R9 recurring restyles applied exactly per DIFF-NOTES-3.md, including the route JSX reorder into the single workflow panel.
- All four KEPT proposals built: archive dialog (house-style, replaces confirm()), deferred-delete visualization with working Herstel undo, inline context-pack first-section preview, inline recipient emails (step-15 variant in both pickers).
- Scope-note persistent block inside the panel for decomposed+ statuses; existing toast retained.
- IGNORE-list items absent; shadcn primitives unmodified; zero data-layer/status-flow changes.
- 9 new i18n keys × 3 locales; tsc + build:dev green.
</success_criteria>

<output>
Create `.planning/quick/260716-i0j-fuse-round-3-canvas-redesign-of-admin-in/260716-i0j-SUMMARY.md` when done.
Note: `.planning/` is gitignored in this repo — stage planning artifacts with `git add -f`.
</output>
