---
phase: quick-260907-uq4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/main.tf
  - infra/DEPLOY-RUNBOOK.md
  - .planning/STAKEHOLDER-NOTES.md
  - .planning/STATE.md
autonomous: true
requirements: [UQ4-01]
must_haves:
  truths:
    - "A `terraform apply` cannot revert Cloud SQL backups/PITR back to off, because the config now declares them."
    - "The declared Terraform values match the live `nestor-pg` read-back exactly, so this resource would show no diff."
    - "`min_instance_count` (DEF-23.3-14) is untouched by this change."
    - "The runbook carries a dated record of the 2026-09-07 backup enablement, with the command, operation id and backup id."
    - "No project doc still asserts that enabling PITR on Cloud SQL for PostgreSQL requires an instance restart."
  artifacts:
    - path: "infra/main.tf"
      provides: "backup_configuration block inside google_sql_database_instance.main settings"
      contains: "backup_configuration"
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "2026-09-07 Cloud SQL backup enablement record"
      contains: "Cloud SQL backups + PITR"
  key_links:
    - from: "infra/main.tf"
      to: "live nestor-pg settings.backupConfiguration"
      via: "value-for-value match with the 2026-09-07 read-back"
      pattern: "point_in_time_recovery_enabled\\s*=\\s*true"
---

<objective>
Codify the Cloud SQL backup + PITR configuration that was enabled by hand on `nestor-pg`
earlier today (2026-09-07) into `infra/main.tf`, so a routine `terraform apply` cannot silently
revert the production database to zero backups.

Purpose: the block was never declared at all — that omission is exactly why the instance ran with
automated backups off and ZERO backups in existence. Hand-fixing the live instance without
declaring it leaves the same trap armed, one `apply` away. This is the same shape as DEF-23.3-14
(`min_instance_count`), and it is being closed rather than repeated.

Output: a `backup_configuration` block in `infra/main.tf` whose values match the live read-back
value-for-value; a dated enablement record in `infra/DEPLOY-RUNBOOK.md`; and a correction to the
project docs that assert PITR needs an instance restart (measured today: it does not, for
PostgreSQL).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md

Target file and resource: `infra/main.tf`, `resource "google_sql_database_instance" "main"`
(lines 48-71). Its `settings` block currently declares ONLY `tier`, one `database_flags` block and
one `ip_configuration` block. There is **no `backup_configuration` block at all**.

## MEASURED live state — `nestor-pg`, read back 2026-09-07

Project `project-cb01b861-cb4a-438d-b9a`, account `tools@dotto.be`, region `europe-west1`.
`settings.backupConfiguration`:

| field | live value |
|---|---|
| `enabled` | `true` |
| `startTime` | `"22:00"` |
| `pointInTimeRecoveryEnabled` | `true` |
| `transactionLogRetentionDays` | `7` |
| `backupRetentionSettings.retainedBackups` | `7` |
| `backupRetentionSettings.retentionUnit` | `"COUNT"` |

Instance stayed `state: RUNNABLE` throughout. `availabilityType: ZONAL` (unchanged, out of scope).
One backup now exists: id `1788811361290`, `AUTOMATED`, `SUCCESSFUL`.

## MEASURED: PITR required NO restart

Enabling PITR on Cloud SQL **for PostgreSQL** did not restart the instance. The instance stayed
`RUNNABLE` across the UPDATE operation (`a6db2b00-…`, 20:01:13 → 20:04:18Z) and `/readyz` returned
`{"status":"ready","db":"ok"}` after. Earlier project notes claimed a restart WAS required — that
claim is now known to be wrong for PostgreSQL and is corrected in Task 3.

## The exact block to add — copy these values verbatim

```
backup_configuration {
  enabled                        = true
  start_time                     = "22:00"
  point_in_time_recovery_enabled = true
  transaction_log_retention_days = 7

  backup_retention_settings {
    retained_backups = 7
    retention_unit   = "COUNT"
  }
}
```

`location` is deliberately NOT declared — it is unset live (provider/API default), so declaring it
would introduce a diff rather than remove one.

## Comment style of the neighbouring blocks (match it)

The surrounding blocks carry short prose comments with decision refs, e.g.
`# db-custom-1-3840 -> default max_connections=100 (A4/D-04)`, `# public IP; the connector secures
the channel (D-03).`, `# ... (T-02-14). Adding one is the anti-pattern we avoid.`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare backup_configuration in infra/main.tf</name>
  <files>infra/main.tf</files>
  <action>
Add a `backup_configuration` block inside the `settings` block of
`resource "google_sql_database_instance" "main"`, using the exact HCL given in `<context>` above.
Place it after the `ip_configuration` block (last child of `settings`), or immediately after `tier`
— either position is fine; do not reorder the existing children.

Values MUST match the live read-back value-for-value. Do not "improve" them: do not raise retention,
do not add `location`, do not switch `retention_unit`, do not touch `availabilityType`/HA. The
purpose of this task is to make the declared state equal the live state so `terraform plan` would
report no diff on this resource. Any value you change here becomes a diff against production.

Add a comment in the SAME style as the neighbouring blocks (short prose, decision/measurement ref in
parentheses). It must record three things:
  1. backups + PITR were enabled on the live instance 2026-09-07;
  2. enabling PITR required NO instance restart — MEASURED, instance stayed RUNNABLE (this corrects
     an earlier project note that claimed the opposite);
  3. this block was previously ABSENT — never declared, not declared-then-disabled — and that
     omission is what allowed the instance to run with zero backups. Same class as DEF-23.3-14.

⛔ HARD SCOPE FENCES:
  - DO NOT touch `min_instance_count` on or near line 381. That is DEF-23.3-14, tracked separately
    and deliberately out of scope for this plan. Leave it reading `0`.
  - DO NOT run `terraform apply`. Terraform state here is LOCAL and unmigrated; an apply is
    dangerous and is explicitly forbidden.
  - `infra/main.tf` is the ONLY code file this plan may change.

`terraform fmt -check` / `terraform validate`: run ONLY if the binary is present. It was MEASURED
ABSENT on this machine (`command -v terraform` → nothing). Re-check; if absent, SKIP SILENTLY, do
not install it, and do not treat its absence as a failure — say so in the summary instead. If it IS
present, note that `terraform validate` may need `terraform init`; if init would touch remote state
or download providers and that fails, skip and say so rather than forcing it. Align your indentation
and `=` alignment with the surrounding file by hand so a later `fmt` is a no-op.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" || exit 1
awk '/^resource "google_sql_database_instance" "main"/{f=1} f{print} f&&/^}$/{exit}' infra/main.tf \
  | sed 's/#.*//' > ./.tf_sqlblk.tmp
B=./.tf_sqlblk.tmp
fail=0
chk(){ n=$(grep -Ec "$2" "$B"); [ "$n" = "$3" ] || { echo "FAIL $1 (found $n, want $3)"; fail=1; }; }
chk backup_configuration 'backup_configuration[[:space:]]*\{' 1
chk enabled '(^|[^_[:alnum:]])enabled[[:space:]]*=[[:space:]]*true' 1
chk start_time 'start_time[[:space:]]*=[[:space:]]*"22:00"' 1
chk pitr 'point_in_time_recovery_enabled[[:space:]]*=[[:space:]]*true' 1
chk txlog 'transaction_log_retention_days[[:space:]]*=[[:space:]]*7' 1
chk retention_block 'backup_retention_settings[[:space:]]*\{' 1
chk retained 'retained_backups[[:space:]]*=[[:space:]]*7' 1
chk unit 'retention_unit[[:space:]]*=[[:space:]]*"COUNT"' 1
n=$(git diff -- infra/main.tf | grep -Ec '^[+-].*min_instance_count')
[ "$n" = "0" ] || { echo "FAIL scope fence: min_instance_count was touched ($n diff lines)"; fail=1; }
rm -f "$B"
[ $fail = 0 ] && echo "PASS: backup_configuration codified inside google_sql_database_instance.main; DEF-23.3-14 untouched"
exit $fail
    </automated>
  </verify>
  <done>
`infra/main.tf` declares `backup_configuration` inside `google_sql_database_instance.main`'s
`settings` block with all six values matching the live read-back; the block carries a
neighbour-style comment recording the 2026-09-07 enablement, the measured no-restart finding and
the prior omission; the gate above prints PASS; `min_instance_count` is untouched; no
`terraform apply` was run.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append the dated backup-enablement record to the deploy runbook</name>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <action>
Append a new dated record at the END of `infra/DEPLOY-RUNBOOK.md` (currently ends at the
DEF-23.3-14 note, ~line 6518). Match the existing record style: an `###` heading with the date, a
short markdown table of read-back values, `####` sub-sections, and the file's habit of separating
what was PROVED from what was merely inferred.

Heading suggestion: `### 2026-09-07 — Cloud SQL backups + PITR ENABLED on `nestor-pg` (no deploy, no build)`

The record must contain:

  - **What was wrong:** `settings.backupConfiguration.enabled = False`, no PITR, **ZERO backups in
    existence**, `availabilityType: ZONAL`. Include the trap that made it read as configured: a
    `startTime` of `22:00` and `retainedBackups` of `7` WERE set, but are inert while `enabled` is
    false — reading those two fields alone makes the instance look backed up.
  - **The command actually used** (verbatim, in a fenced block):

    ```
    gcloud sql instances patch nestor-pg \
      --account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a \
      --backup-start-time=22:00 --retained-backups-count=7 \
      --enable-point-in-time-recovery --retained-transaction-log-days=7
    ```

  - **Operation id and window:** `a6db2b00-…`, 20:01:13 → 20:04:18Z. Record the id EXACTLY as you
    can evidence it. It is recorded here in truncated form; you MAY recover the full id with a
    READ-ONLY `gcloud sql operations list --instance=nestor-pg --account=tools@dotto.be
    --project=project-cb01b861-cb4a-438d-b9a --limit=5`, pinning account and project on the command
    per the standing operator rule (the gcloud config drifts mid-session). If that read fails or
    auth has drifted, DO NOT fight it — record the truncated id and label it truncated. Never write
    an id you did not observe.
  - **⭐ The measured finding:** enabling PITR required **NO instance restart**. The instance stayed
    `RUNNABLE` across the whole UPDATE operation and `/readyz` returned
    `{"status":"ready","db":"ok"}` afterwards. State plainly that this CORRECTS the earlier project
    note (`.planning/STAKEHOLDER-NOTES.md`, 2026-09-07) which warned a restart would be required —
    it is not required for Cloud SQL **for PostgreSQL**. Scope the correction to PostgreSQL; do not
    generalise it to MySQL/SQL Server.
  - **Read-back after** — the six-row table from `<context>`, plus the one existing backup:
    id `1788811361290`, `AUTOMATED`, `SUCCESSFUL`.
  - **What this does NOT prove** (the runbook always has this section, keep the habit):
      * a backup has **never been restored** — a backup never restored is a hope, not a guarantee.
        A restore rehearsal (clone to a new instance, check schema + row counts, delete) is still
        outstanding.
      * the instance is still **ZONAL** — no HA. Losing the zone is still an outage; backups
        bound the data loss, they do not remove the outage.
      * no deploy, no build, no migration and no provider spend accompanied this change.
  - **Terraform:** note that `infra/main.tf` now DECLARES this block (Task 1), and why that mattered
    — the block was previously absent entirely, so a routine `terraform apply` would have reverted
    the live instance to no backups. Same class as DEF-23.3-14, which remains OPEN and deliberately
    untouched by this change.

⛔ Do not modify any existing runbook record. Append only.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" || exit 1
F=infra/DEPLOY-RUNBOOK.md
fail=0
chk(){ grep -qE "$2" "$F" || { echo "FAIL missing: $1"; fail=1; }; }
chk "2026-09-07 heading" '^#{2,4} .*2026-09-07.*[Bb]ackup'
chk "gcloud patch command" 'gcloud sql instances patch nestor-pg'
chk "PITR flag" 'enable-point-in-time-recovery'
chk "operation id" 'a6db2b00'
chk "backup id" '1788811361290'
chk "no-restart finding" '(NO|no) (instance )?restart'
chk "RUNNABLE evidence" 'RUNNABLE'
# append-only: the diff must add lines and remove none
d=$(git diff --numstat -- "$F" | awk '{print $2}')
[ "${d:-0}" = "0" ] || { echo "FAIL runbook is not append-only ($d lines removed)"; fail=1; }
[ $fail = 0 ] && echo "PASS: dated backup-enablement record appended to the runbook"
exit $fail
    </automated>
  </verify>
  <done>
`infra/DEPLOY-RUNBOOK.md` ends with a dated 2026-09-07 record carrying the verbatim gcloud command,
the operation id (full or explicitly labelled truncated), the measured no-restart finding, the
post-change read-back table, the existing backup id, an honest "does NOT prove" section, and the
Terraform note. Zero existing lines were removed; the gate prints PASS.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the PITR-restart claim and the now-stale blocker text</name>
  <files>.planning/STAKEHOLDER-NOTES.md, .planning/STATE.md</files>
  <action>
Two surgical corrections. Both docs currently assert or endorse a claim that is now MEASURED FALSE,
and both still present the backups blocker as OPEN when it is closed.

**A. `.planning/STAKEHOLDER-NOTES.md`** (the "⛔ THE BLOCKER: the production database has NO
BACKUPS" section, ~lines 288-320):

  - Lines ~305-306 currently read: *"⚠ verify the restart behaviour first — enabling automated
    backups is online, but enabling PITR on Cloud SQL for PostgreSQL turns on WAL archiving and
    requires an INSTANCE RESTART; if so, split it — backups now, PITR in a window"*. Replace that
    caveat with the measured result: PITR was enabled 2026-09-07 with **no restart** — the instance
    stayed `RUNNABLE` throughout and `/readyz` was healthy after. Keep the correction scoped to
    Cloud SQL **for PostgreSQL**. Preserve the record that the caveat was raised and then settled by
    measurement — do not delete the history, correct it in place and mark it CORRECTED with the
    date. That is this file's convention.
  - Update the blocker's status. The `enabled | False` / `ZERO backups exist` table is now historic:
    mark the section RESOLVED 2026-09-07 and add the post-fix values (see `<context>` table) plus
    backup id `1788811361290`. Do NOT delete the original measured table — it is the origin record
    and this project keeps those; label it as the pre-fix state.
  - The paragraph asking "whether `infra/main.tf` declares backup config at all, because if it does
    not then a `terraform apply` could revert a hand-set value — the same shape as DEF-23.3-14" is
    now ANSWERED: it did not declare it, and Task 1 closed that. Record the answer.
  - Leave the ZONAL → REGIONAL item alone. Its restart remark is about REGIONAL HA, not PITR, and
    HA is out of scope. Leave the restore-rehearsal item OPEN — it is genuinely still owed.

**B. `.planning/STATE.md`** (the `## Current Position` block, ~lines 28-34):

  - The `⛔ NEXT ACTION: TURN ON CLOUD SQL BACKUPS` paragraph is now stale and it points readers at
    the wrong restart caveat (`Command + the PITR-restart caveat: .planning/STAKEHOLDER-NOTES.md`).
    Rewrite that paragraph to record backups + PITR as ENABLED 2026-09-07 and codified in
    `infra/main.tf`, and to drop the restart caveat pointer (or restate it as CORRECTED — no
    restart, measured). Name what is still owed so the closure is not oversold: **no restore has
    ever been rehearsed**, and the instance is **still ZONAL (no HA)**.
  - ⛔ Edit ONLY that paragraph. Do NOT restructure `## Current Position`, do NOT touch the
    frontmatter counters, and do NOT touch any other block in the file. This file mixes curated
    prose with machine-read counters and has been corrupted by tooling twice; keep the edit
    minimal and hand-made.
  - ⛔ DO NOT run `gsd-sdk query state.advance-plan` — it has corrupted this file before.

`.planning/` is gitignored in this repo. Both files must be staged with `git add -f` or the change
is invisible to anything that reads the repo.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" || exit 1
fail=0
S=.planning/STAKEHOLDER-NOTES.md
T=.planning/STATE.md
# The false claim must be gone: no doc may still say PITR requires a restart.
if grep -nEi 'requires an INSTANCE RESTART' "$S" "$T" >/dev/null 2>&1; then
  echo "FAIL: the PITR-requires-restart claim is still asserted"; fail=1
fi
grep -qE '(no|NO|without) .{0,20}restart' "$S" || { echo "FAIL: STAKEHOLDER-NOTES lacks the measured no-restart correction"; fail=1; }
grep -q '1788811361290' "$S" || { echo "FAIL: STAKEHOLDER-NOTES lacks the post-fix backup id"; fail=1; }
grep -qE 'RESOLVED|CORRECTED' "$S" || { echo "FAIL: blocker section not marked resolved/corrected"; fail=1; }
grep -qiE 'backups.{0,40}(enabled|on)|(enabled|on).{0,40}backups' "$T" || { echo "FAIL: STATE.md does not record backups as enabled"; fail=1; }
grep -qi 'ZONAL' "$T" || { echo "FAIL: STATE.md does not keep the still-owed ZONAL/no-HA caveat"; fail=1; }
# STATE.md edit must be minimal: frontmatter (lines 1-15) untouched.
if git diff -U0 -- "$T" | grep -E '^@@' | grep -qE '^@@ -([1-9]|1[0-5]),'; then
  echo "FAIL: STATE.md frontmatter region was modified"; fail=1
fi
[ $fail = 0 ] && echo "PASS: PITR-restart claim corrected; blocker recorded as closed with caveats intact"
exit $fail
    </automated>
  </verify>
  <done>
No project doc asserts that enabling PITR requires an instance restart. `.planning/STAKEHOLDER-NOTES.md`
marks the backups blocker RESOLVED 2026-09-07 with the post-fix values and backup id, keeps the
pre-fix table as the origin record, and answers the `infra/main.tf` question. `.planning/STATE.md`'s
Current Position records backups+PITR enabled and codified, with the restore-rehearsal and ZONAL
gaps still named; its frontmatter and every other block are untouched. Both files staged with
`git add -f`. The gate prints PASS.
  </done>
</task>

</tasks>

<verification>
1. Task 1's gate prints PASS — the block exists inside the right resource with the six live values,
   and `min_instance_count` is untouched.
2. Task 2's gate prints PASS and the runbook diff removes zero lines.
3. Task 3's gate prints PASS.
4. `git diff --name-only` lists EXACTLY these four files and no others:
   `infra/main.tf`, `infra/DEPLOY-RUNBOOK.md`, `.planning/STAKEHOLDER-NOTES.md`, `.planning/STATE.md`.
5. `terraform` was NOT applied. If the binary was absent, that is recorded as skipped-not-failed.
</verification>

<success_criteria>
- The declared Terraform backup configuration equals the live `nestor-pg` read-back value-for-value,
  so a future `terraform apply` preserves backups + PITR instead of reverting them.
- The runbook carries a dated, evidenced record of the enablement — command, operation id, backup
  id, and an explicit "does NOT prove" section (no restore rehearsal; still ZONAL).
- The PITR-restart claim is corrected wherever it appears, scoped to PostgreSQL, with the history
  preserved rather than deleted.
- Zero cloud mutations, zero deploys, zero builds, zero provider spend. Read-only `gcloud describe`
  / `operations list` is permitted; nothing else touches GCP.
</success_criteria>

<output>
Commit all four files in ONE commit (`.planning/` is gitignored — use `git add -f` for the two
planning files, and force-add this PLAN.md too):

```
git add infra/main.tf infra/DEPLOY-RUNBOOK.md
git add -f .planning/STAKEHOLDER-NOTES.md .planning/STATE.md \
  .planning/quick/260907-uq4-codify-cloud-sql-backup-configuration-in/
```

Commit message: `fix(infra): codify Cloud SQL backup + PITR config so apply cannot revert it`

Body must record: the block was ABSENT (never declared), values match the 2026-09-07 live
read-back, PITR needed NO restart (measured), and DEF-23.3-14 is deliberately untouched.

End the commit message with:

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Bo5vqqiPx6yS6KM6S8bht2
```

Then write
`.planning/quick/260907-uq4-codify-cloud-sql-backup-configuration-in/260907-uq4-SUMMARY.md`.
</output>
