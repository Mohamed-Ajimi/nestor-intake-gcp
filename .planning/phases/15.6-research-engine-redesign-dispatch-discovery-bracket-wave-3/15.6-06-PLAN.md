---
phase: 15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3
plan: 06
type: execute
wave: 3
depends_on: ["15.6-03", "15.6-04", "15.6-05"]
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
  - tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The groups stage B decided are the groups research is dispatched on — end to end, through the checkpoint."
    - "The discovery questions the evidence raised reach the report annotated onto the conflicts that provoked them."
    - "The operator's feed says how many groups there were and that every one went to all three streams."
    - "A run whose paid-call count exceeds the 5 x 3 ceiling says so loudly in the log."
    - "The stubbed end-to-end run exercises the whole new path, and a grouping failure inside it degrades rather than breaks."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      provides: "groups threaded into divide(), discovery provenance into brief_conflicts, and the attribution logging"
      contains: "groups="
  key_links:
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py"
      via: "workshop_result['groups'] passed to divide(..., groups=...)"
      pattern: "groups="
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/discovery_bracket.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py"
      via: "annotate_conflicts applied to brief_conflicts before the report section is built"
      pattern: "annotate_conflicts"
---

<objective>
Connect the three halves: stage B's groups reach `divide()`, and its discovery reaches the report.

Purpose: plans 15.6-03, 04 and 05 each built one end of a wire. Nothing dispatches by topic and no
discovered question is visible until this plan joins them. It also carries the phase's attribution
duty into the operator-facing surfaces — this phase deliberately changes which claims reach paid
verification, so the run must SAY what it decided.

Output: three wiring edits in `pipeline.py`, the loud over-ceiling warning, and end-to-end coverage.

**NO DEPLOY.** This is the last wiring plan of Wave 3 and it still deploys nothing. Waves 1-4 land in
git only; there is ONE deploy and ONE measuring run at the end of phase 15.8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-CONTEXT.md
@.planning/CONTINUE-HERE.md

Worktree hygiene, before any edit:
- **Your worktree may be cut from a STALE ref** — 3 of 3 executors in phase 15.5, one an outright
  ancestor of the whole phase. Assert `git merge-base --is-ancestor <phase-base-sha> HEAD`.
- **Per-dependency assertions. STOP if any fails** — all of wave 2 must be in your base:
  1. `research_division.divide` accepts a keyword-only `groups` parameter (check the AST, not a grep).
  2. `research_division._D6_STREAMS` has exactly three entries.
  3. `workshop_rank.run_workshop_stage_b`'s result carries `groups` and `discovery`.
  4. `discovery_bracket.annotate_conflicts` exists.
  5. `synthesis/steps.py` `_SECTION_STRINGS["dutch"]` contains `brief_raised_question`.
- **File ownership.** You own `pipeline.py` and `test_engine_e2e_stubbed.py`. Do NOT touch
  `research_division.py`, `workshop_rank.py`, `synthesis/steps.py`, `question_grouping.py`,
  `discovery_bracket.py` or `cloudbuild.test-engine.yaml`.
- Do NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md`.

**THE SEAM-SCAN RULE — it will turn a build red if you break it.**
`test_the_facet_resolution_seam_has_no_production_caller_in_this_wave`
(`tests/test_research_division_assignment.py:970`) walks `rglob("*.py")` over the WHOLE of
`tribunal/nestor_pulse_sdk/`, excluding only `claim_attribution.py` (by filename) and `test_*.py` (by
prefix), and FAILS if the literal substrings `resolved_facet` or `parent_index` appear anywhere else.
**Do not write either literal into any file this plan touches.** Refer to it in prose as "the
facet-resolution seam in `claim_attribution`" instead. The full explanation of why the seam stays
uncalled lives in `claim_attribution.py`, which the scan excludes by name — plan 15.6-04 owns it.


<interfaces>
Verified against the tree at planning time; line numbers are pre-wave-2 and will have drifted — find
by symbol, not by number.

**`pipeline.py:1733-1742`** reads the workshop result tolerantly: `winners`, `workshop_fallback`,
`run_language`, `deep_research_prompt`, `client_questions`, `brief_conflicts`. Every key is read with
`.get()` even though stage B guarantees them (phase rule 4). Two more keys join that block.

**`pipeline.py:1840-1845`** — `build_mission_brief_from_winners(winners=winners, ...)`. **This call
does not change.** Discovery questions are deliberately NOT in `winners`, so no new focus area and no
new report section is created (D4 — depth may grow, scope may not).

**`pipeline.py:1893`** — `angles = divide(mission_brief, winners=winners, trim_out=_trims)`. One
keyword joins it.

**`pipeline.py:1895-1914`** — the angle checkpoint and `angles_digest`. The digest is derived from
each angle's `query` ALONE, and Wave 3 legitimately changes the query text, so a run resumed from a
pre-Wave-3 checkpoint discards its recorded research and re-buys the angles. That is CORRECT — the
questions genuinely changed — and the existing `log.warning` at `:1904-1913` already says so. Do not
try to preserve the digest.

**`pipeline.py:1921-1930`** — the D-12 loop over `_trims` that turns a degrading trim into a
degradation reason. Unchanged.

**`pipeline.py:1934-1971`** — the division feed block. `_corroborated` counts distinct non-empty
`corroboration_key`s among angles with `corroboration` truthy; under Wave 3 that equals the group
count, because every angle is a corroboration copy. The header currently reads
`"N client question(s) -> M workshop question(s) -> K research angle(s)"` plus
`", X of them checked by several streams"` plus a clause naming where the questions came from.

**`pipeline.py:3278-3292`** — `brief_conflicts` is placed into `report_sections`, and the comment
there records that this is where it IS populated. **That is the annotation point.**

**`pipeline.py:3745`** — `brief_conflicts=report_sections.get("brief_conflicts")` is handed to the
section builder.

**`pipeline.py:3920-3923`** — `_angle_copies` / `_group_size`, the fourth reader of
`corroboration_key`. It counts angles sharing a key. Under Wave 3 that returns 3 for every angle
rather than 4-for-three-angles-and-1-for-the-rest. No code change; verify the feed line it feeds
still reads sensibly and say so in the SUMMARY.

**`pipeline.py:3869`** — the `own` provider display label. **KEEP IT** (D-W3-3): `own` is out of the
rotation but its runner, timeout and label stay so reinstating is one line.

**`pipeline.py:1720-1728`** — the workshop `stage_exit` log line, currently `questions_in` and
`winners_out`.

**`pipeline.py:2311`** — `_propagate_stakes(claims, mission_brief)` matches `claim["facet"]` against
focus-area labels. Unchanged, and it is why a group's `focus_area` must remain a client label.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread the groups into divide(), and the discovery into the report</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` lines 1620-1980 — the workshop call, the tolerant result read, the mission brief, `divide()`, the angle checkpoint, the D-12 trim loop and the whole division feed block
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` lines 3260-3300 and 3730-3760 — where `brief_conflicts` enters `report_sections` and where it leaves for the section builder
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py` — the stage-B contract docstring, for the two new keys
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/discovery_bracket.py` — `annotate_conflicts`'s exact signature and its annotate-never-append rule
  </read_first>
  <action>
**(a) Read the two new keys, tolerantly.** Beside `brief_conflicts` in the `:1733-1742` block add
`groups = list(workshop_result.get("groups") or [])` and
`discovery = list(workshop_result.get("discovery") or [])`. `.get()` and `or []`, matching every
other line there: a run RESTORED from a checkpoint written before Wave 3 carries neither key, and a
subscript would raise at exactly the moment the resume exists to avoid re-buying paid angles.

**(b) Pass them to `divide`.** `angles = divide(mission_brief, winners=winners, groups=groups,
trim_out=_trims)`. Nothing else on that line changes.
When `winners` is truthy and `groups` is empty, `_divide_from_winners` already falls back to
one-group-per-client-question and logs it (plan 15.6-03) — **do not add a second fallback here.**

**(c) Annotate the conflicts.** At the point where `brief_conflicts` enters `report_sections`
(`:3278-3292`), replace the value with
`discovery_bracket.annotate_conflicts(brief_conflicts, discovery)`. Extend the numbered comment block
there with a new item: discovered questions are annotated ONTO the conflict that provoked them and
never appended as extra rows, because the section would otherwise print the same conflict twice; and
this is what carries Art. 12 provenance into a client-facing document.
Import `discovery_bracket` at module scope alongside the other `pipeline.tribunal` imports, unless
that creates a cycle — if it does, use the function-local import idiom this file already uses for
`strip_unresolved_cite_markers` and say why in a comment.

**(d) The over-ceiling warning.** Immediately after `divide()` returns, emit a `log.warning` when
`len(angles) > question_grouping._D6_MAX_GROUPS * len(research_division._D6_STREAMS)`. Import both
rather than retyping 5 or 3. The sentence must name: the actual angle count, the ceiling, that this
is the D-W3-2 one-group-per-client-question fallback path, and the T-15.2-61 fact that the budget
governor is inert under `NESTOR_TRIBUNAL_UNCAPPED=1` so angle count is the only real spend control
this engine has left. D-W3-2 accepted this overshoot **on condition that it is logged loudly.**

**(e) The feed header.** Extend the `_division_items[0]["name"]` string to read
`"N client question(s) -> M workshop question(s) -> G research group(s) -> K research angle(s)"`,
keep the question-source clause, and replace `", X of them checked by several streams"` with a
clause naming that every group went to all three streams when `_corroborated == len(groups)` and
falling back to the existing wording otherwise. Add a second clause naming the discovery count when
`discovery` is non-empty — the operator should be able to see on the page that the engine asked a
question the client did not.
Add a THIRD clause, only when at least one angle carries `mixed_parents`, naming how many groups hold
**two different CLIENT questions**. That is the one place the run tells the operator that some claims'
per-question attribution is an approximation (plan 15.6-03(f) logs the detail). Emit nothing when no
group is mixed. **A group carrying a discovery rider is NOT mixed** — under D-W3-5.2 that is the
intended shape, and saying so on the feed would be the crying-wolf warning D-W3-5 forbids. Add a
FOURTH clause instead, when any angle carries `discovery_riders`, naming how many discovered questions
rode along inside a client question's own group at no extra cost — that is the saving D-W3-5 was
chosen for, and the operator should see it.

**(f) The stage_exit log line** at `:1720-1728` gains `groups_out=len(groups)` and
`discovery_out=len(discovery)`, read off values that already exist. Nothing is computed for the log.

Do NOT touch `_propagate_stakes`, `_angle_copies`, `_dr_model_display`, the `own` label at `:3869`,
the angle checkpoint, the `angles_digest` logic, or the D-12 trim loop.
  </action>
  <acceptance_criteria>
    - `py_compile` clean under `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`.
    - AST assertion: the `divide(` call in `run()` has a keyword argument named `groups`, and the two new reads use `Call(attr='get')`, not `Subscript`.
    - AST assertion: `pipeline.py` still references the string `"own"` at the provider-label site — proved by locating the display-label mapping node, not by grep, because grep passes on a comment.
    - Driving the stubbed end-to-end harness (Task 2) end to end: `divide` receives the SAME group ids stage B returned, asserted by recording the call arguments.
    - A `workshop_result` with NO `groups` key at all (the pre-Wave-3 checkpoint shape) completes the run and logs the dispatch fallback exactly once — no exception, no second fallback message.
    - The over-ceiling warning fires for a 6-group fallback over 3 streams (18 angles) and does NOT fire for 5 groups (15 angles). Asserted with `caplog`, checking the sentence names both the count and the ceiling.
    - The rendered `brief_conflicts` handed to the report carries `researched_as` on exactly the conflicts that became discovery questions, and the list is the same length as the input — asserted against `annotate_conflicts`'s real output, not a hand-typed dict.
    - `caplog` on the `stage_exit` line shows `groups_out` and `discovery_out`.
    - The feed header names the mixed-group count when an angle carries `mixed_parents`, and contains no such clause when none does — asserted in both directions.
    - **The crying-wolf check, at the feed level:** a run whose only multi-parent groups are ride-alongs produces NO mixed-group clause, and DOES produce the rider clause. Asserted on the rendered header string.
    - `discovery` non-empty with zero cross-cutting questions produces group ids that are all `g*` — no `d1` — and the paid-call count is `len(client_questions_grouped) * 3`, which for a 3-question brief is 9 to 12 and never 15. This is D-W3-5's stated saving, asserted end to end rather than assumed.
    - `ast.unparse` of `_propagate_stakes`, `_angle_copies` and `_dr_model_display` IDENTICAL to the phase base.
    - Mutation controls, each turning a NAMED test red: `groups=` dropped from the `divide` call; `annotate_conflicts` replaced by list concatenation; the over-ceiling comparison changed to `>=`; the two new reads changed to subscripts.
  </acceptance_criteria>
  <done>Groups decided in the workshop are the groups research is bought on, and the questions the evidence raised are visible to the client.</done>
</task>

<task type="auto">
  <name>Task 2: Make the stubbed end-to-end run exercise the new path — including its failure</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py` in full — the stub harness, what it fakes and what it deliberately does not, and its existing degradation-reason assertions
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py` — `group_winners`'s four fallback triggers, so your fake can hit the one you intend
    - `.planning/phases/15.5-research-engine-redesign-claim-attribution-wave-2/15.5-02-SUMMARY.md` — the AST-lift harness and the mutation discipline
  </read_first>
  <action>
**Read this before writing anything: the grouping call is a NEW audited LLM call on the stubbed
harness's path.** Whatever the existing fake returns for it, `group_winners` will not find an
`emit_question_groups` tool_use block, will fall back, and will add a D-12 degradation reason. That
will move any existing assertion that counts or enumerates degradation reasons — including
`test_the_run_has_exactly_one_degradation_reason_list` if it asserts on contents. **Find out which
existing assertions move, and fix them by teaching the fake to group properly, not by loosening the
assertion.** A stubbed run that silently degrades is a stubbed run that stops proving the happy path.

Add to the fake audited client a grouping response: a tool_use block named `emit_question_groups`
whose `input` is `{"groups": [{"member_numbers": [...]}, ...]}` partitioning the stub's winners.

Then add, at minimum:
- `test_the_stubbed_run_dispatches_by_group_not_by_position` — every angle carries a non-empty
  `corroboration_key`, the distinct key count equals the group count, and each key appears exactly
  three times.
- `test_every_client_question_survives_the_stubbed_run_into_a_group` — the § 8 coverage requirement,
  end to end.
- `test_a_grouping_failure_degrades_the_stubbed_run_and_does_not_break_it` — a fake whose grouping
  turn returns no tool_use block: the run still completes, produces angles for every client question,
  and carries exactly one new degradation sentence naming the grouping fallback.
- `test_a_discovery_question_reaches_the_delivered_report_in_the_stubbed_run` — orientation emits a
  sourced `brief_conflict`; the rendered report section contains the discovery question text. **Judge
  from the rendered report string, never from the claim table and never from the logs** — the
  standing caution.
- `test_no_angle_is_dispatched_to_own` — the D-W3-3 rotation change, seen end to end.

Every existing test must stay byte-unchanged unless the grouping call genuinely moved its premise; if
one did, say which and why in the SUMMARY, and keep the RULE it protected.

**Then run the MUTATION BATTERY** from Task 1, plus: the grouping fake's partition made incomplete
(the totality assertion must bite end to end). Record mutation -> test. Any BLIND mutation is a gap.

Execute the committed test bodies with the AST-lift + `pytest`-stub harness and mark the pytest run
PENDING-CLOUDBUILD. `test_engine_e2e_stubbed.py` is ALREADY registered in
`cloudbuild.test-engine.yaml` — do not add it and do not touch `EXPECTED_FILES`. Name plan
**15.6-07** as the owner of the run.
  </action>
  <acceptance_criteria>
    - Every test body executes GREEN under the AST-lift harness; the SUMMARY states `N/N`, pre-existing and new separately, and names any pre-existing test whose premise the grouping call moved.
    - `test_no_angle_is_dispatched_to_own` passes, and the SUMMARY confirms `degraded_parallel.ALL_PROVIDERS` still lists `own` — the two facts coexist by design (D-W3-3's accepted gap).
    - The discovery test asserts against the rendered report STRING, not against claims or logs.
    - Each mutation turns a NAMED test red; zero blind.
    - `git diff --stat <phase-base> HEAD` for this plan lists exactly the two files in `files_modified`.
  </acceptance_criteria>
  <done>The whole Wave 3 path is exercised end to end, including the path it takes when grouping fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpointed workshop result -> dispatch | `groups` crosses a checkpoint boundary and then decides paid spend. |
| discovery provenance -> client-facing report | Model-authored quote and URL render into a delivered document. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.6-25 | Tampering | a restored pre-Wave-3 checkpoint with no `groups` key | mitigate | Read with `.get(...) or []`; `_divide_from_winners` falls back deterministically and logs it once. Asserted with a checkpoint shape that carries neither key. |
| T-15.6-26 | Denial of service | the D-W3-2 fallback's paid-call overshoot | mitigate | A loud `log.warning` naming the count, the ceiling and the inert budget governor (T-15.2-61); `_MAX_ANGLES` still bounds the absolute number and `_trim_ladder` still records every removal. |
| T-15.6-27 | Repudiation | what the operator saw the engine decide | mitigate | The feed header names groups and discovery; the `stage_exit` line carries `groups_out` and `discovery_out`; both read off values that already exist. |
| T-15.6-28 | Information disclosure | discovery provenance in a delivered report | accept | It is the point: D5/D-01 forbids an operator gate, so transparency is the control. Bounded by `_sanitize` and the http(s) URL check in plan 15.6-05's renderer. |
| T-15.6-SC | Tampering | package installs | accept | No package installed, no `requirements.txt` change. |
</threat_model>

<verification>
- `py_compile` clean on both files.
- `ast.unparse` IDENTICAL vs base for `_propagate_stakes`, `_angle_copies`, `_dr_model_display`, and
  the angle-checkpoint / `angles_digest` block.
- Every committed test body executed by the AST-lift harness, `N/N` green.
- The mutation battery: every named mutation red, zero blind.
- **PENDING-CLOUDBUILD:** the pytest run, resolved by
  `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml`, owned by plan
  15.6-07. Read the build TEXT — `| tail` reports exit 0 on a FAILED build.
- **STATED AS OWED, NEVER CLAIMED:** no live run, no deploy. Judging Wave 3 from a delivered report
  happens once, at the end of phase 15.8.
</verification>

<success_criteria>
- Stage B's groups are the groups research is bought on, through the checkpoint.
- A discovered question is annotated onto its conflict and reaches the delivered report.
- The operator can see the group count, the discovery count and any over-ceiling spend.
- A grouping failure degrades the stubbed run and does not break it.
</success_criteria>

<output>
Create `.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-06-SUMMARY.md`
when done. Name every pre-existing e2e assertion the new grouping call moved, and how it was fixed.
</output>
</content>
