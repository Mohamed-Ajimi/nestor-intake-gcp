---
phase: 15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3
plan: 05
type: execute
wave: 2
depends_on: ["15.6-02"]
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py
  - tribunal/nestor_pulse_sdk/tests/test_report_sections.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The delivered report says, in the client's own language, which questions the client asked and which the evidence raised."
    - "A discovered question appears in the report exactly ONCE, annotated onto the conflict that provoked it — never as a second row."
    - "A conflict that never became a research question renders exactly as it does today."
    - "The provenance clause exists in all four report languages; no English sentence lands in a Dutch report."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py"
      provides: "the `researched_as` clause in the brief-vs-world subsection, in four languages"
      contains: "researched_as"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/discovery_bracket.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py"
      via: "the `researched_as` key annotate_conflicts adds to a brief_conflict"
      pattern: "researched_as"
---

<objective>
Make a discovered question visible to the client, beside the quote and URL that provoked it.

Purpose: `D5 / D-01` states the workshop is FULLY AUTOMATIC — *"Nothing in this module pauses for an
operator."* So discovery cannot be gated on an approval click. The governance instead is
**transparency**: the client sees plainly which questions they asked and which the evidence raised.
The section already exists — `"### Where the brief did not match what the research found"`
(`steps.py:532`, populated at `:709-751`) — and on V-01 it rendered nothing at all. This plan adds
one clause to it. **Provenance is also required for the Art. 12 audit trail, deadline 2026-08-02.**

Output: one new `_SECTION_STRINGS` key in four languages, one branch in the existing flag loop, and
tests.

**NO DEPLOY.** Waves 1-4 land in git only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-CONTEXT.md
@.planning/CONTINUE-HERE.md

Worktree hygiene, before any edit:
- **Your worktree may be cut from a STALE ref** — 3 of 3 executors in phase 15.5. Assert
  `git merge-base --is-ancestor <phase-base-sha> HEAD`.
- **Per-dependency assertion. STOP if it fails:**
  `pipeline/tribunal/discovery_bracket.py` exists and defines `annotate_conflicts`, and that function
  adds the key spelled exactly `researched_as`.
- **File ownership.** You own `synthesis/steps.py` and `tests/test_report_sections.py`. Do NOT touch
  `research_division.py` (15.6-03), `workshop_rank.py` (15.6-04), `pipeline.py` (15.6-06),
  `discovery_bracket.py` (15.6-02) or `cloudbuild.test-engine.yaml` (15.6-07).
- **`steps.py` IS THE DISTILLER FILE AND MOST OF IT IS FENCED.** You may touch ONLY
  `_SECTION_STRINGS` and `build_disputed_and_changed`. Do NOT touch `_build_distiller_prompt`,
  `_parse_distiller_response`, `_split_distiller_line`, `claim_distiller`, `_chunk_text`,
  `_make_distiller_config`, `_normalise_fact_claim`, `_FACT_CLAIM_KEYS`, `_dedupe_claims` or
  `collect_provider_facts`. The `FACET ||| CLAIM_TEXT ||| EVIDENCE` contract is what Wave 1 proved
  against the real V-01 audit blobs (141/137/43/143) and what Wave 2 left byte-identical.
- Do NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md`.

<interfaces>
Verified against the tree at planning time.

**`_SECTION_STRINGS` (`steps.py:526-600`)** is a `dict[str, dict[str, str]]` with exactly four
language keys — `english` (`:528`), `dutch` (`:546`), `german` (`:564`), `french` (`:582`) — each
carrying the same key set. `_norm_lang` resolves the run language onto one of them and falls back to
`english` with a warning. The block's own comment says the section is *"Deterministic by
construction: same input, same bytes, no model, no clock."*

**`sub_brief` (`:532`, `:550`, `:568`, `:586`)** is the heading
`"### Where the brief did not match what the research found"` and its three translations.

**The flag loop (`steps.py:709-751`)** reads each `brief_conflicts` entry:
- a `str` entry is used as-is;
- a `dict` entry tries the three LEGACY keys `note` / `text` / `finding` first, in that order;
- otherwise it composes the producer's own shape:
  `f"The brief assumes: {assumption} The research found: {world_says}"`, then appends
  `f" ({source_url})"` when the URL starts with `http://` or `https://`.
- `_sanitize` is applied to every field; a falsy result is skipped; `_truncated_items` bounds the list.
The comment above it records that the `assumption`/`world_says` branch is the one the engine
ACTUALLY uses, and that the whole subsection **silently never appeared in any report** before it was
added — producer and consumer were tested in different files against different hand-made fixtures.
Do not repeat that mistake: your test must drive `discovery_bracket.annotate_conflicts` output into
`build_disputed_and_changed`, not a hand-typed dict that only resembles it.

**The composed sentence is ALREADY hardcoded English** inside a function that otherwise renders four
languages. That is a pre-existing defect, it is NOT this plan's to fix, and you must not fix it
opportunistically — it would change the bytes of a section four committed tests already pin. Note it
in the SUMMARY as an observation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The provenance clause, in four languages</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py` lines 500-620 — `_norm_lang`, `_LANG_ALIASES` and the whole `_SECTION_STRINGS` block, all four language dicts
    - `tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py` lines 620-770 — `build_disputed_and_changed` in full, including `_sanitize` and `_truncated_items`
  </read_first>
  <action>
**(a) One new key in each of the four language dicts**, placed immediately after `sub_brief` so the
four dicts stay parallel. Key name: `brief_raised_question`. Values, verbatim:

- `english`: `"This raised a question the client did not ask, and the research answered it:"`
- `dutch`: `"Dit riep een vraag op die de klant niet had gesteld; het onderzoek heeft die beantwoord:"`
- `german`: `"Daraus ergab sich eine Frage, die der Kunde nicht gestellt hatte; die Recherche hat sie beantwortet:"`
- `french`: `"Cela a soulevé une question que le client n'avait pas posée, et la recherche y a répondu :"`

**(b) One branch in the flag loop.** After the `assumption` / `world_says` / `source_url` sentence is
composed and BEFORE `text_value = _sanitize(raw_flag)`, read the optional key:

- `researched = _sanitize(item.get("researched_as"))` — `.get()`, never a subscript: the vast
  majority of conflicts never become research questions and carry no such key.
- when it is non-empty, append `f" {strings['brief_raised_question']} {researched}"` to `raw_flag`.

**Three rules to write beside it as a comment:**
1. **The clause is APPENDED to the existing sentence, never emitted as a second bullet.** A second
   row would print the same conflict twice — once with the clause and once without — which is why
   `discovery_bracket.annotate_conflicts` annotates in place rather than appending.
2. The clause applies to the composed `assumption`/`world_says` branch AND to the three legacy-key
   branches, because a producer that supplies `note` plus `researched_as` is legitimate. Place the
   read so it runs for every dict entry.
3. `_sanitize` and `_truncated_items` still bound everything — the question text is engine-authored
   (`discovery_bracket.discovery_question_text`) but the fields inside it are model-authored, and
   this function's contract is that a hostile item costs that one item and never the section.

**(c) Nothing else changes.** No new function, no signature change, no reordering of the three
subsections, no change to `disputed_empty`. `build_disputed_and_changed` still never raises, still
takes no clock and no LLM, and still renders byte-identically for input carrying no `researched_as`.
  </action>
  <acceptance_criteria>
    - `py_compile` clean under `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`.
    - AST assertion: all four `_SECTION_STRINGS` sub-dicts have IDENTICAL key sets, and all four contain `brief_raised_question`. Assert set-equality across the four, not a hardcoded list — that way a fifth language added later fails loudly rather than silently.
    - `build_disputed_and_changed(brief_conflicts=[<one conflict with no researched_as>], language=...)` returns bytes IDENTICAL to the phase base, for all four languages. Drive the base and the edited version side by side and compare strings.
    - With `researched_as` present, the rendered bullet contains the conflict sentence, then the language's clause, then the question text, in that order and on ONE bullet — asserted by counting `"*   "` occurrences in the subsection: exactly one.
    - A Dutch run renders the Dutch clause and contains no substring of the English one; same for German and French.
    - A `researched_as` of `""`, `None`, `0`, a list, and an object with a hostile `__str__` each render exactly as the no-clause case, without raising.
    - `ast.unparse` of `_build_distiller_prompt`, `_parse_distiller_response`, `_split_distiller_line`, `claim_distiller`, `_chunk_text`, `_make_distiller_config`, `_normalise_fact_claim`, `_dedupe_claims` and `collect_provider_facts` is IDENTICAL to the phase base, and `_FACT_CLAIM_KEYS` still has twelve entries.
    - Mutation controls, each turning a NAMED test red: the clause emitted as a second bullet; `item.get("researched_as")` changed to a subscript; the clause hardcoded to English; `brief_raised_question` removed from the Dutch dict only.
  </acceptance_criteria>
  <done>A discovered question is visible to the client, once, in their language, beside the quote and URL that provoked it.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test_report_sections.py, and drive the real producer into the real consumer</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_report_sections.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/test_report_sections.py` in full — the four-language register and the existing `brief_conflicts` fixtures you must leave byte-unchanged
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/discovery_bracket.py` — `allocate_discovery` and `annotate_conflicts`, the producer you must drive rather than imitate
    - `.planning/phases/15.5-research-engine-redesign-claim-attribution-wave-2/15.5-02-SUMMARY.md` — the AST-lift harness and the mutation discipline
  </read_first>
  <action>
Extend, do not restructure. Every existing test stays byte-unchanged and green — the no-clause path
must render identically, and those tests are the proof.

**The one test that matters most** is the hand-off, because the comment at `steps.py:700-717` records
that this exact producer/consumer pair silently never worked: both sides were tested against
different hand-made fixtures and nobody drove the hand-off.

`test_a_dispatched_discovery_question_reaches_the_report_from_the_real_producer`:
build orientation-shaped `brief_conflicts`, run `discovery_bracket.allocate_discovery`, run
`discovery_bracket.annotate_conflicts`, feed the RESULT into `build_disputed_and_changed`, and assert
the discovery question text appears in the rendered section. **No hand-typed `researched_as` anywhere
in this test.**

Also add:
- the four-language clause tests;
- `test_an_unsourced_conflict_renders_with_no_clause_because_it_was_never_researched` — the
  no-source-no-slot rule, seen from the report end;
- `test_a_discovery_question_that_never_got_dispatched_renders_with_no_clause` — a sourced conflict
  that WAS allocated a slot but was shed for prompt space, or whose cross-cutting group lost its slot
  to the mandate (plan 15.6-04's GAP A and GAP B). It must render as a plain brief-vs-world conflict:
  the evidence raised it, this run did not research it, and the report must not imply otherwise;
- `test_a_conflict_renders_once_not_twice`;
- `test_the_no_clause_rendering_is_byte_identical_to_before` — pin the exact expected string, so a
  future edit to the composed sentence is a deliberate act;
- the hostile-`researched_as` cases.

Keep the file PURE — no Postgres, no provider key, no network.

**Then run the MUTATION BATTERY** from Task 1 plus: `allocate_discovery`'s source filter removed (the
hand-off test must then render a clause for an unsourced conflict and go red). Record mutation ->
test in the SUMMARY. Any BLIND mutation is a gap in the tests, not a pass.

Execute the committed test bodies with the AST-lift + `pytest`-stub harness and mark the pytest run
PENDING-CLOUDBUILD. `test_report_sections.py` is ALREADY registered in
`cloudbuild.test-engine.yaml` — do not add it and do not touch `EXPECTED_FILES`. Name plan
**15.6-07** as the owner of the run.
  </action>
  <acceptance_criteria>
    - Every test body executes GREEN under the AST-lift harness; the SUMMARY states `N/N`, pre-existing and new separately.
    - `git diff` on `test_report_sections.py` shows additions only — no pre-existing test function appears in a hunk.
    - The hand-off test imports `discovery_bracket` and contains no string literal `researched_as` — asserted by grepping the test function body.
    - Every mutation from Task 1, plus the source-filter mutation, turns a NAMED test red. Zero blind.
    - `git diff --stat <phase-base> HEAD` for this plan lists exactly the two files in `files_modified`.
  </acceptance_criteria>
  <done>The producer and the consumer are tested against each other, not against two hand-made fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orientation model -> client-facing report | `assumption`, `world_says`, `source_url` and the composed question text render into a document the client reads as HTML/PDF. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.6-21 | Tampering | `researched_as` in the rendered bullet | mitigate | `_sanitize` on the value and `_truncated_items` on the list, exactly as the three existing fields; a hostile item costs that item, never the section. |
| T-15.6-22 | Spoofing | `source_url` rendered as a link | mitigate | Unchanged: the existing `http(s)`-only check at `steps.py:717-719` still gates the URL, and `workshop._parse_orientation` already dropped every other scheme upstream. |
| T-15.6-23 | Repudiation | which questions the client did not ask | mitigate | The clause names them explicitly in the client's own language, beside the quote and URL — the Art. 12 audit-trail requirement, deadline 2026-08-02. |
| T-15.6-24 | Information disclosure | a Dutch report carrying an English sentence | mitigate | The clause is a `_SECTION_STRINGS` key in all four languages, asserted by cross-language key-set equality rather than a hardcoded list. |
| T-15.6-SC | Tampering | package installs | accept | No package installed, no `requirements.txt` change. |
</threat_model>

<verification>
- `py_compile` clean on both files.
- `ast.unparse` IDENTICAL vs base for every fenced distiller function named in the context block, and
  `_FACT_CLAIM_KEYS` still twelve entries.
- The no-clause rendering is byte-identical to the base for all four languages.
- Every committed test body executed by the AST-lift harness, `N/N` green.
- The mutation battery: every named mutation red, zero blind.
- **PENDING-CLOUDBUILD:** the pytest run for `test_report_sections.py`, resolved by
  `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml`, owned by plan
  15.6-07. Read the build TEXT, never `| tail`'s exit status.
</verification>

<success_criteria>
- The delivered report distinguishes the client's questions from the ones the evidence raised.
- A conflict that never became a question renders exactly as it does today, byte for byte.
- The producer and the consumer are proven against each other.
</success_criteria>

<output>
Create `.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-05-SUMMARY.md`
when done. Note the pre-existing hardcoded-English composed sentence as an observation, not a fix.
</output>
</content>
