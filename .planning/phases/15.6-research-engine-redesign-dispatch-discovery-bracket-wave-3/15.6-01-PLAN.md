---
phase: 15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/tools.py
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py
  - tribunal/nestor_pulse_sdk/tests/test_question_grouping.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "An LLM proposes groups; Python clamps them to AT MOST 5, and fewer is a normal, unremarked outcome."
    - "Every winner lands in exactly one group — the assignment is total, and a winner the model forgot is placed deterministically rather than dropped."
    - "A group's `parent`, `parents` and member texts are stamped in Python from the winners list; nothing the grouping model wrote reaches a research provider."
    - "When grouping fails or returns nothing usable, the run falls back to ONE GROUP PER CLIENT QUESTION and records a D-12 degradation."
    - "Whether a group may span two client questions is a POLICY the caller sets, not a property baked into the clamp — and both settings are proven."
    - "The fallback logs its paid-call count loudly whenever it exceeds the happy-path ceiling of 5 x 3."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py"
      provides: "the group record contract, the pure validator/clamp/fallback, and the audited grouping call"
      exports: ["GROUP_BRACKET_MANDATE", "GROUP_BRACKET_DISCOVERY", "validate_groups", "clamp_groups", "fallback_groups", "build_groups", "group_winners"]
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/tools.py"
      provides: "EMIT_QUESTION_GROUPS_TOOL and force_emit_question_groups"
      contains: "emit_question_groups"
    - path: "tribunal/nestor_pulse_sdk/tests/test_question_grouping.py"
      provides: "the clamp, the totality proof, the injection bound and the fallback"
      min_lines: 200
  key_links:
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/tools.py"
      via: "forced client tool on the final grouping turn"
      pattern: "force_emit_question_groups"
---

<objective>
Define the group — the unit this whole phase dispatches on — and build the machine that produces it:
a forced-tool LLM call that PROPOSES groups, and pure Python that CLAMPS them.

This is the interface-first plan. Plans 15.6-03 (dispatch), 15.6-04 (coverage guard) and 15.6-06
(pipeline) all consume the group record defined here. Get the record right and they are plumbing.

Purpose: today `_divide_from_winners` deals the top 3 winners to all four streams and the remainder
round-robin by POSITION (`research_division.py:867-874`). V-01's coffee question landed on gemini
because of where it fell in the deal, not because gemini suits Benelux retail. Grouping by shared
research groundwork replaces position with topic, and sending every group to all three providers is
what finally populates `corroboration_key` for every claim instead of ~3 of 15.

Output: `question_grouping.py` (new), one new forced client tool, and a new test file.

**NO DEPLOY.** Waves 1-4 land in git only. No `gcloud run deploy`, no image build, no migration
applied to any database. There is ONE deploy and ONE measuring run at the end of phase 15.8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-CONTEXT.md
@.planning/CONTINUE-HERE.md

Worktree hygiene, before any edit:
- **Your worktree may be cut from a STALE ref.** It fired on 3 of 3 executors in phase 15.5, one of
  them an outright ancestor of the whole phase. `git rev-list --count HEAD ^<base>` returning `0`
  means "not ahead", NOT "in sync". Assert the MERGE-BASE:
  `git merge-base --is-ancestor <phase-base-sha> HEAD` must succeed.
- Assert these exist before editing anything, and STOP if any fails — they are phase 15.5's output
  and this phase is built on it:
  1. `tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py` defines `parent_index` and
     `resolved_facet`.
  2. `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` contains `_corroboration_key`
     in its `_enriched` literal.
- Do NOT edit `tribunal/cloudbuild.test-engine.yaml`. Plan **15.6-07** owns it and registers
  `test_question_grouping.py` there, AFTER this file exists. Adding the name here would put two
  owners on a file whose whole purpose is to be reconciled by one.
- Do NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md` — parallel executors race them.
- `.planning/` is gitignored — `git add -f` if you write anything there.

<interfaces>
Verified against the tree at planning time.

**`research_division.py:480-517` — `_normalise_winners(raw_winners, default_parent)`** is the
tolerant reader for winner dicts. It drops a winner with empty text, coerces a bad `rank` to the
1-based list position, defaults a missing `parent`, and sorts by `(rank, original_index)`. Every
winner dict this plan handles has at least `text` (str), `rank` (int), `parent` (str), `langs`.

**`workshop_rank.py:2298-2306`** documents the full winner shape stage B returns: `text`, `langs`,
`parent`, `rank`, plus `parents`, `index`, `source`, `scope_injected`, `wins`, `elo`, `byes`,
`critique`, `flaw`.

**`workshop_rank.py:293` — `_parents_of(entry)`** is the existing reader for a winner's `parents`
list. A collapse can carry two client questions onto one winner, which is why `parents` and not
`parent` is what a coverage union reads.

**`tools.py:254-321` — `EMIT_ORIENTATION_TOOL`** and **`tools.py:324-334` —
`force_emit_orientation()`** are the pattern to follow: a module-level dict with a `name`,
`description`, `input_schema`, and a tiny function returning
`{"type": "tool", "name": "<name>"}`. Note the repeated convention in that file: a field the model
might not have is deliberately NOT in `required`, because forcing it produces a fabricated value.

**`workshop.py:747-950` — `_one_orientation`** is the audited forced-tool call shape:
`with_retry(lambda: audited.anthropic_messages(...), attempts=..., base_s=..., label=...,
breaker=...)`, an `audit_out` dict, a `_extract_*_block` scan over `resp.content` for the tool_use
block, and a `_parse_*` function that pre-fills every field and never raises.

**`workshop_rank.py:1059-1140` — `_judge_batch`** is the accounting shape: an `acc` / `stats` dict
gaining `calls`, `audit_id` and `cost` via `_add_cost`, and a bare `return {}` on any exception so a
failed call degrades rather than raising.

**`workshop.py:130` — `_WORKSHOP_MODEL`** is the workshop's Anthropic model constant, read from
`NESTOR_TRIBUNAL_WORKSHOP_MODEL`.

**`workshop_rank.py:186` — `_IGNORE_INSTRUCTIONS`** is the existing literal that names model text as
DATA in a prompt. Reuse the same register; do not invent a second wording.

**`research_division.py:130` — `_D6_STREAMS`** is `("gemini", "openai", "claude", "own")` TODAY.
Plan 15.6-03 makes it three. **Do not import it here and do not hardcode 3** — this module computes
its call-count warning from a number it is GIVEN, not from a tuple another plan is editing in the
same wave-group. That is the exact-set trap that turned phase 15.5's merged tree red.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The forced client tool — emit_question_groups, addressed by index</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/tools.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/tools.py` lines 238-340 — `EMIT_ORIENTATION_TOOL` and `force_emit_orientation()`, the pattern this follows, including the `#:` comment style above the constant and the "deliberately NOT in required" convention
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/tools.py` lines 1-60 — the module header, including the HTTP 400 trap note, so you place the new block consistently
  </read_first>
  <action>
Add `EMIT_QUESTION_GROUPS_TOOL` and `force_emit_question_groups()` to `tools.py`, in a new
`# ---` section placed AFTER the `emit_orientation` section and BEFORE the `serpapi_search` section.

**The schema is INDEX-ADDRESSED, and that is the security control, not a convenience.** The model
receives a numbered list of winners and returns integers. It never returns question text. Say this
in the `#:` comment above the constant, in the same register the file already uses for
`EMIT_ORIENTATION_TOOL`'s scope note: the winner text reaches three third-party research providers
verbatim, so allowing the grouping model to REWRITE it would hand a second model a channel into a
paid provider prompt (T-15.2-60). `gates.py` already addresses untrusted items by index for the same
reason.

`input_schema`:
- `groups`: array of objects, each with
  - `member_numbers`: array of integers — "the numbers of the questions that belong in this group,
    taken from the numbered list above. Use each number at most once, across all groups."
  - `why_grouped`: string — "one short sentence on the shared research groundwork these questions
    need. Used for the run log only."
  - `required`: `["member_numbers"]` only. `why_grouped` is optional for the reason this file states
    twice already: requiring a field the model does not have produces a fabricated one.
- `required`: `["groups"]`.

The `description` must state, in words the model reads: group by SHARED RESEARCH GROUNDWORK (the
same sources, the same market, the same regulatory regime), not by surface wording; a group MAY span
two client questions where they genuinely overlap and one client question MAY split across two
groups when it is really two topics; return **at most `{max_groups}` groups** — the description is a
format string the caller fills, because the ceiling moves when the discovery bracket takes a slot;
return FEWER groups when the material has fewer topics and never pad to reach the maximum; every
number in the list must appear in exactly one group.

**The cross-question sentence is a SECOND format slot, `{cross_question_rule}`,** filled by the
caller with one of two engine-authored sentences: either the D-R4 wording above (a group MAY span
two client questions), or `"Every group must contain questions from ONE client question only -- the
bracketed label at the start of each line. Never mix two labels in one group."` Which one is used is
plan 15.6-04's decision and is pending an operator ruling (see that plan's PENDING RULING block);
this plan supplies both strings and hard-codes neither. Name the two constants
`GROUP_RULE_CROSS_QUESTION_ALLOWED` and `GROUP_RULE_SINGLE_PARENT_ONLY` and export them.

`force_emit_question_groups()` returns `{"type": "tool", "name": "emit_question_groups"}`, with a
docstring in the same shape as `force_emit_orientation()`.

Do NOT touch `EMIT_ORIENTATION_TOOL`, `EMIT_FACT_LIST_TOOL`, `SERPAPI_SEARCH_TOOL` or
`EMIT_GROUP_VERDICT_TOOL`.
  </action>
  <acceptance_criteria>
    - `python -c "import ast,sys; ast.parse(open('tribunal/nestor_pulse_sdk/pipeline/tribunal/tools.py',encoding='utf-8').read())"` succeeds (use the gcloud bundled Python: `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`).
    - An AST scan of `tools.py` finds a module-level assignment named `EMIT_QUESTION_GROUPS_TOOL` and a `FunctionDef` named `force_emit_question_groups`.
    - `EMIT_QUESTION_GROUPS_TOOL["input_schema"]["properties"]["groups"]["items"]["properties"]` has exactly the keys `{"member_numbers", "why_grouped"}` and `...["items"]["required"] == ["member_numbers"]`.
    - `EMIT_QUESTION_GROUPS_TOOL["input_schema"]["properties"]["groups"]["items"]["properties"]["member_numbers"]["items"]["type"] == "integer"` — the schema admits integers, never strings.
    - No property anywhere in the new schema has `"type": "string"` for question text: an AST/JSON walk of the tool dict finds no property named `text`, `question`, `label` or `title`.
    - `ast.unparse` of `EMIT_ORIENTATION_TOOL` and of `force_emit_orientation` is IDENTICAL to the pre-edit file.
  </acceptance_criteria>
  <done>`tools.py` carries a forced grouping tool whose entire question-identifying surface is integers.</done>
</task>

<task type="auto">
  <name>Task 2: question_grouping.py — the group record, the validator, the clamp, the fallback</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` lines 122-172 (the `_D6_*` constants block and its stated arithmetic) and lines 480-517 (`_normalise_winners`) — you are matching both the constant style and the tolerance discipline
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py` lines 2002-2175 — `_covered_labels`, `_rerank`, `_parents_of` and `enforce_scope_guard`, for the never-raises / idempotent / repair-at-the-top house style plan 15.6-04 extends
    - `tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py` lines 392-499 — `parent_index` and `resolved_facet`, whose FIRST-WINS and exact-string rules this module must match rather than re-decide
  </read_first>
  <action>
Create `tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py`. The whole module is PURE
except `group_winners` (Task 3). Nothing here raises.

**Constants**, in the `research_division.py:126-172` style — a comment saying what the number is FOR
above each one:

- `_D6_MAX_GROUPS = min(5, max(1, int(os.environ.get("NESTOR_TRIBUNAL_D6_MAX_GROUPS", "5"))))`.
  The `min(5, ...)` is not defensive tidiness: **D-W3-1 makes 5 a HARD CEILING taken by the
  operator**, so the env knob may only lower it. Write that in the comment, and write the
  consequence the operator accepted: a brief with 7 genuinely distinct topics gets one grab-bag
  group, and there is no rule that lets the ceiling rise.
- `_D6_MAX_GROUP_SIZE = max(3, int(os.environ.get("NESTOR_TRIBUNAL_D6_MAX_GROUP_SIZE", "4")))`.
  Comment the arithmetic: `research_division._D6_MAX_WINNERS` is 15 and the ceiling is 5 groups, so
  15 / 5 = 3 is the infeasibility floor and 4 leaves slack. State the PRECEDENCE explicitly: the
  ceiling is an operator decision and the size cap is not, **so when the two collide the ceiling
  wins** — an oversized group is accepted and logged loudly rather than split into a sixth group.
- `GROUP_BRACKET_MANDATE = "mandate"` and `GROUP_BRACKET_DISCOVERY = "discovery"` — exported, because
  four other modules test against them and none of them may retype a string literal.
- `_WHY_MAX_CHARS = 200` — the bound on the model's `why_grouped`.

**The group record.** Write it once, in a module-level docstring block, as the contract every
consumer reads:

```
group_id   str    engine-authored, "g1".."g5" for mandate, "d1" for discovery. Becomes the
                  angle corroboration_key, so it must be stable within a run.
bracket    str    GROUP_BRACKET_MANDATE or GROUP_BRACKET_DISCOVERY
members    list   winner dicts COPIED from the winners list, in ascending rank
parents    list   ordered, deduped union of the members' `parents`/`parent` — stamped in Python
parent     str    the highest-ranked member's parent; becomes the angle focus_area
rank       int    min member rank; drives stakes via _stakes_for_rank
why        str    the model's own sentence, bounded to _WHY_MAX_CHARS. LOG AND TELEMETRY ONLY —
                  it is NEVER interpolated into a research query and never reaches a provider.
```

State the rule above `parents`/`parent` in a comment: **stamped in Python from the assignment, never
read from grouping output** — the same rule `enforce_scope_guard` and `workshop._parse_orientation`
already apply, and the reason is that a grouping LLM's output is untrusted input on a path to three
third-party providers (T-15.2-60).

**`build_groups(assignment, winners, *, bracket=GROUP_BRACKET_MANDATE, whys=None) -> list[dict]`** —
pure. Turns a list-of-index-lists into the record above. Members are `dict(winners[i])` copies,
sorted by `(rank, index-in-assignment)`. `parents` unions each member's `parents` when it is a
non-empty list of strings, else falls back to `[member["parent"]]`. `group_id` is `f"g{n}"` for the
mandate bracket and `f"d{n}"` for discovery. Never raises; a group whose members all resolve away is
omitted.

**`validate_groups(raw_groups, winners) -> tuple[list[list[int]], list[str]]`** — pure, never raises.
Reads ONLY `member_numbers` (ints) and `why_grouped` (str) off each entry; anything else on the model
object is ignored. Rules, each with a one-line comment:

1. The model is given 1-BASED numbers; convert to 0-based here, in one place.
2. An index outside `range(len(winners))` is DROPPED and counted. Note the count.
3. A winner claimed by two groups goes to the FIRST group that names it — first-wins, the same rule
   `claim_attribution.parent_index` and D-W2-3 already use.
4. A group left empty after dedupe is dropped.
5. **TOTALITY.** Any winner named by no group is placed deterministically: into the group that
   already holds the highest-ranked winner sharing its `parent`; if no group holds that parent, into
   the group with the fewest members (ties broken by lowest group position). Count the placements
   and note them. **Assert totality in code** before returning — the union of the returned index
   lists must be exactly `set(range(len(winners)))` and the lists must be pairwise disjoint. If the
   assertion fails, log at ERROR and return `([], notes)` so the caller falls back rather than
   dispatching an incomplete set.
6. Unusable input (not a list, no usable entry) returns `([], notes)`.

Return the notes as plain-words sentences, never codes — the Shared Pattern 5 rule this codebase
already follows.

**`clamp_groups(assignment, winners, *, max_groups, max_size, prefer_single_parent) ->
tuple[list[list[int]], list[str]]`** — pure, never raises, deterministic. In this order:

0. **PARENT POLICY, when `prefer_single_parent` is true.** Split any group whose members carry more
   than one distinct `parent` into one group per parent, **only while `len(assignment) <
   max_groups`**. When the ceiling forbids the split, keep the mixed group and NOTE it — the ceiling
   is an operator decision and this policy is not, the same precedence the size cap already yields
   to. When the flag is false this step does nothing at all.
   **Read the rationale before implementing:** on the primary D8 fact-list path a claim's `facet` is
   stamped in Python from the ANGLE (`steps.py:2400`), and the D8 contract
   (`facts.py:298` — `STATEMENT<TAB>SOURCE_URL<TAB>QUALITY<TAB>CERTAINTY<TAB>EVIDENCE`) carries **no
   facet column**, so a mixed group gives every one of its claims the top-ranked member's parent —
   including the claims answering the other client question. A single-parent group makes `facet`
   exact by construction. This is a POLICY PARAMETER with exactly one production value chosen at one
   call site (plan 15.6-04); it is **not** a feature flag and there is **no dual run** — D-03 forbids
   the latter, not the former. Say so in the docstring.
1. **CEILING NEXT.** While `len(assignment) > max_groups`, merge the two weakest groups into one.
   Weakest = highest minimum member rank; ties broken by the later position. **When
   `prefer_single_parent` is true, prefer a merge of two SAME-PARENT groups over a merge that would
   create a mixed group; only mix when no same-parent merge is available.** Note each merge, and note
   separately when a merge was forced to mix.
2. **THEN SIZE.** For any group longer than `max_size`, split the weakest-ranked tail into a new
   group — but **only while `len(assignment) < max_groups`**. When the ceiling forbids the split,
   keep the oversized group and emit `log.warning` naming the group id, its size, `max_size` and
   `max_groups`, plus the sentence that the ceiling is an operator decision and the size cap is not.
   Add a NOTE, not a degradation: every question is still researched.
3. Sort groups by minimum member rank ascending, so `g1` holds rank 1. Renumbering happens in
   `build_groups`.

**`fallback_groups(winners, client_questions) -> tuple[list[list[int]], str]`** — pure, never raises.
D-W3-2's deterministic replacement for the deleted top-k/round-robin machinery: ONE GROUP PER CLIENT
QUESTION, in client-question order, each holding every winner whose `parent` matches that label. A
winner whose parent matches no label joins the FIRST group — the same orphan rule
`build_mission_brief_from_winners` already applies at `research_division.py:577-587`. Empty groups
are dropped. **It is NOT clamped to `_D6_MAX_GROUPS`**, and that is the accepted spend consequence
of D-W3-2: six client questions is 18 paid calls, above the happy-path ceiling. Write that in the
docstring. Returns the D-12 degradation sentence for the caller to record — plain words, naming that
the questions were grouped one-per-client-question because the grouping step produced nothing usable,
so shared groundwork was searched once per question instead of once per topic.

**`warn_if_over_ceiling(n_groups, n_streams) -> str | None`** — pure. Returns a sentence and emits
`log.warning` when `n_groups * n_streams > _D6_MAX_GROUPS * n_streams`. `n_streams` is a PARAMETER,
never imported from `research_division._D6_STREAMS`: that tuple is being edited by plan 15.6-03 in
the same phase, and pinning it from here is the exact-set trap that turned 15.5's merged tree red.
The warning text must name the T-15.2-61 fact: the budget governor is inert under
`NESTOR_TRIBUNAL_UNCAPPED=1`, so angle count is the only real spend control this engine has left.

Keep the module import surface small: `logging`, `os`, `typing`, and `from
nestor_pulse_sdk.pipeline.tribunal import tools` (Task 3 only). No database, no SQLAlchemy, no
provider SDK at module scope.
  </action>
  <acceptance_criteria>
    - `py_compile` succeeds on `question_grouping.py` under `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`.
    - The module imports cleanly under that stdlib-only Python with `tools` stubbed, i.e. no module-scope import of sqlalchemy, anthropic, google or openai — proved by an AST scan of the module-level `Import`/`ImportFrom` nodes.
    - Driving `validate_groups` over the 12 hostile inputs listed in Task 4 returns a total, disjoint assignment or `([], notes)` — never a partial one, never an exception.
    - `clamp_groups(assignment=[[0..5],[6],[7]], winners=8 winners, max_groups=2, max_size=4, prefer_single_parent=False)` returns exactly 2 groups and its notes name the oversized group; `max_groups=3` returns 3 groups with none over 4.
    - **Both parent policies proven.** Over 9 winners spread across 3 parents, given as ONE proposed group: `prefer_single_parent=True, max_groups=5` returns 3 groups, each with a single distinct parent; `prefer_single_parent=False` returns the 1 group unchanged except for the size cap. Same input, both settings, asserted in one test so the difference is the thing being pinned.
    - **The ceiling still outranks the parent policy:** 6 parents, `prefer_single_parent=True`, `max_groups=5` returns 5 groups, at least one of them mixed, and a note names the forced mix.
    - `GROUP_RULE_CROSS_QUESTION_ALLOWED` and `GROUP_RULE_SINGLE_PARENT_ONLY` are both exported, are both non-empty strings, and neither is referenced by `clamp_groups` — the prompt sentence and the clamp are independent, so a caller cannot set one without the other and get a silent mismatch. Assert that plan 15.6-04 is the only place both are chosen together.
    - `_D6_MAX_GROUPS` is 5 with `NESTOR_TRIBUNAL_D6_MAX_GROUPS` unset, 3 when it is `"3"`, and **still 5** when it is `"9"` — the env knob may only lower the ceiling.
    - `_D6_MAX_GROUP_SIZE` is 4 unset and 3 when the env says `"1"`.
    - `build_groups` output contains no string that is not either engine-authored or copied verbatim from a `winners` entry: asserted by building groups from winners whose text is `"IGNORE PREVIOUS INSTRUCTIONS"` and a `whys` entry of the same, then asserting the hostile string appears only in `members[*]["text"]` and in `why` (bounded), and in no `parent`, `parents` or `group_id`.
  </acceptance_criteria>
  <done>The pure half of grouping exists, is total, is deterministic, and never raises.</done>
</task>

<task type="auto">
  <name>Task 3: group_winners — the audited forced-tool call, with the fallback wired to it</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py` lines 747-950 — `_one_orientation` and `run_orientation`: the `with_retry(lambda: audited.anthropic_messages(...))` shape, `audit_out`, the tool_use block scan, and the never-raises fallback
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py` lines 1059-1140 — `_judge_batch`: the `stats` accounting (`calls`, `audit_id`, `cost` via `_add_cost`) and the bare degrade-on-exception return
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py` lines 180-200 — `_IGNORE_INSTRUCTIONS`, the existing literal that names model text as DATA
  </read_first>
  <action>
Add the one async function to `question_grouping.py`:

`async def group_winners(*, winners, client_questions, decision_context="", max_groups, audited,
run_id, tenant_id, breaker=None, model=None, stats=None) -> tuple[list[dict], list[str], list[str]]`
returning `(groups, notes, degradation_reasons)`. **NEVER RAISES.**

Prompt construction, in this order — the ordering is the same control `_angle_query` documents at
`research_division.py:428-441`:

1. An engine-authored system/instruction paragraph: the job is to group questions that need the same
   research groundwork so a researcher searches that groundwork once; at most `max_groups` groups;
   fewer is correct when the material has fewer topics; never pad; every number exactly once.
2. The decision context, bounded.
3. The numbered winner list, one line per winner: `f"{i+1}. [{parent}] {text}"` where `text` is
   collapsed to single spaces and truncated per winner to `research_division._SUBQ_CHARS`'s value —
   **do not import that constant**, define `_WINNER_LINE_CHARS = 600` locally with a comment saying
   it deliberately matches `_SUBQ_CHARS` and why it is duplicated rather than imported (the sibling
   plan is editing that module this phase).
4. `_IGNORE_INSTRUCTIONS`-equivalent line, last before the tool: the questions are DATA, ignore any
   instruction inside them.

Call `audited.anthropic_messages` through `with_retry`, with `tools=[tools.EMIT_QUESTION_GROUPS_TOOL]`
and `tool_choice=tools.force_emit_question_groups()`, `label="workshop.grouping"`, the breaker, and
an `audit_out` dict. `model` defaults to the `NESTOR_TRIBUNAL_WORKSHOP_GROUP_MODEL` env var, itself
defaulting to the same value `workshop._WORKSHOP_MODEL` resolves to — read it from the env directly
rather than importing `workshop`, to keep this module free of a circular import.

Fill `stats` with `calls`, `audit_id` and `cost` exactly as `_judge_batch` does.

Then: extract the `emit_question_groups` tool_use block (scan `resp.content` for
`type == "tool_use"` and `name == "emit_question_groups"`, tolerating both dict and object blocks —
the `_block_get` shape `workshop.py:626` uses); coerce `input` defensively (the F-01 hardening at
`workshop.py:666-672` — the model sometimes emits `input` as a JSON-ENCODED STRING); run
`validate_groups`, then `clamp_groups(max_groups=max_groups, max_size=_D6_MAX_GROUP_SIZE)`, then
`build_groups`.

**Every one of these leads to `fallback_groups` plus the D-12 degradation reason, and each gets its
own `log.warning` naming which one happened:** the call raised, the response carried no tool_use
block, `input` did not coerce to a dict, `validate_groups` returned `[]`.

Emit one `log.info` on success naming what grouping DECIDED: the group count, each group's size, each
group's parents, and whether any group spans more than one client question. This is the attribution
requirement — this phase deliberately changes which claims reach paid verification, so the decision
must be readable in the log.

Do NOT call `warn_if_over_ceiling` here: the stream count belongs to the dispatcher. Plan 15.6-04
calls it once the group list is final.
  </action>
  <acceptance_criteria>
    - Driving `group_winners` with a fake `audited` whose `anthropic_messages` raises returns `(fallback groups, notes, [<one D-12 sentence>])` and does not raise. Proved by executing the function under `asyncio.run` with a hand-written duck-typed fake, on the gcloud bundled Python.
    - The same, for: a response with no tool_use block; a response whose `input` is the JSON STRING `'{"groups": [{"member_numbers": [1,2]}]}'` (this one must SUCCEED, proving the F-01 coercion is present); a response whose `groups` is `[]`.
    - A successful call with 8 winners and `max_groups=3` returns at most 3 groups whose member texts, unioned, equal the 8 winner texts exactly.
    - `stats` gains `calls == 1` on the success path and `calls == 0` when the call raised.
    - The prompt string handed to the fake contains each winner's text at most once and contains no `why_grouped` value; grep the recorded prompt for `IGNORE PREVIOUS INSTRUCTIONS` planted in a winner and assert it appears exactly once, after the ignore-instructions line has already been emitted... i.e. assert the ignore-instructions sentence's index in the prompt is GREATER than the numbered list start and the tool is forced.
    - Mutation control: delete the `clamp_groups` call from `group_winners`; the test asserting `max_groups=3` over an LLM proposing 7 groups must turn RED. Restore afterwards and say so in the SUMMARY.
  </acceptance_criteria>
  <done>Grouping either produces at most `max_groups` clamped groups or falls back deterministically, and says which in the log.</done>
</task>

<task type="auto">
  <name>Task 4: test_question_grouping.py — drive it, then mutate it</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_question_grouping.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py` lines 1-60 and 200-300 — the `_winner(text, rank, parent, langs)` / `_wbrief(*labels)` helper style and the pure-function test register this repo uses
    - `tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py` lines 1-80 — the hand-written duck-typed fake style for an audited client, and the `@pytest.mark.asyncio` convention
  </read_first>
  <action>
Create `tribunal/nestor_pulse_sdk/tests/test_question_grouping.py`. It must be PURE — no Postgres, no
provider key, no network — because plan 15.6-07 registers it in `cloudbuild.test-engine.yaml`, which
provisions none of those. Every LLM call goes through a hand-written duck-typed fake in this file.

Name the tests after the RULE, not the function. Cover, at minimum:

- **The ceiling.** An LLM proposing 7 groups over 15 winners yields exactly 5. An LLM proposing 2
  groups over 15 winners yields exactly 2 — **no floor padding**, asserted directly, because
  D-W3-1's "fewer is allowed and expected" is the half a reader will assume away.
- **`NESTOR_TRIBUNAL_D6_MAX_GROUPS="9"` still yields at most 5.**
- **Totality.** For a proposal that omits winners 4 and 11, the returned assignment is a partition of
  all 15 indices, and winner 4 lands in a group holding its own parent.
- **First-wins on a duplicate claim**, matching D-W2-3.
- **Out-of-range and negative indices are dropped and noted**, and do not shift the remaining ones.
- **The size cap yields to the ceiling.** With `max_groups=5`, `max_size=4` and 15 winners the LLM
  put in ONE group, the result is 5 groups, one of which exceeds 4, and a note says so.
- **The parent policy, both ways**, and **the parent policy yields to the ceiling too** — the two
  cases named in the acceptance criteria.
- **The fallback.** One group per client question; a winner with an unmatched parent joins the first
  group; the returned reason is a plain-words sentence, not a code.
- **The fallback's overshoot is logged.** Six client questions and three streams produces the
  over-ceiling warning; five does not.
- **Never raises**, driven over the 12 hostile inputs: `None`, a string, a list of `None`, a group
  that is a string, `member_numbers` as a string, as a list of strings, as a list of floats, an empty
  list, a negative index, an index of `len(winners)`, a duplicate index inside one group, and a
  `why_grouped` of 10,000 characters (which must come back bounded to `_WHY_MAX_CHARS`).
- **Nothing model-authored becomes a parent.** A `why_grouped` and a `member_numbers` entry cannot
  set `group["parent"]`, `group["parents"]` or `group["group_id"]`.
- **The four fallback triggers in `group_winners`**, each asserted separately.

**Then run the MUTATION BATTERY and record it in the SUMMARY.** At minimum these mutations must each
turn a NAMED test red: the `min(5, ...)` removed from `_D6_MAX_GROUPS`; the totality assertion
removed from `validate_groups`; first-wins changed to last-wins; the ceiling-before-size ordering in
`clamp_groups` reversed; `fallback_groups` clamped to 5; the `why` bound removed; the F-01 string
coercion removed from `group_winners`; **the `prefer_single_parent` split made unconditional**; and
**the split allowed to exceed `max_groups`.** Report any mutation that comes back BLIND as a gap in the
tests, not as a pass — that is how phase 15.5 found two untested call sites.

You cannot run pytest on this machine. Execute the committed test BODIES by AST-lifting them and
stubbing `pytest` (the phase 15.5 harness — see `.planning/phases/15.5-.../15.5-02-SUMMARY.md`
"Verification"), and mark the pytest run itself PENDING-CLOUDBUILD, naming
`cloudbuild.test-engine.yaml` as the build that resolves it and plan **15.6-07** as its owner.
  </action>
  <acceptance_criteria>
    - Every test body in the file executes GREEN when driven by the AST-lift harness under `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`; the SUMMARY states the count as `N/N`.
    - Each of the seven named mutations turns at least one NAMED test red; the SUMMARY lists mutation -> test name. Zero BLIND mutations, or a new test closing each blind one.
    - The file contains no `import sqlalchemy`, no `psycopg`, no `DATABASE_URL` and no live network call — asserted by an AST scan of its imports.
    - The file is NOT added to `tribunal/cloudbuild.test-engine.yaml` by this plan: `git diff --name-only` for the whole plan does not list that path.
  </acceptance_criteria>
  <done>The pure grouping core is proven by execution, and the proof is shown to bite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grouping LLM -> engine | A second model's output now shapes what three paid providers are asked. |
| engine -> gemini / openai / claude | Grouped winner text reaches three third-party processors verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.6-01 | Tampering | `EMIT_QUESTION_GROUPS_TOOL` | mitigate | The schema carries INTEGERS only; no question text field exists, so the grouping model cannot rewrite a winner on its way to a provider. Asserted by Task 1's schema walk. |
| T-15.6-02 | Elevation of privilege | `build_groups` | mitigate | `parent`, `parents` and `group_id` are stamped in Python from the winners list; grouping output supplies membership and nothing else. Asserted by the hostile-string test. |
| T-15.6-03 | Information disclosure | `why_grouped` | mitigate | Bounded to `_WHY_MAX_CHARS` and used for the log only — never interpolated into a research query, never rendered in the report. Asserted by grepping the prompt built for a provider. |
| T-15.6-04 | Denial of service | grouping call on the critical path | mitigate | `with_retry` + breaker, and four separate failure paths all landing on `fallback_groups`; `group_winners` never raises, so a wedged endpoint degrades the run instead of failing a $50 job. |
| T-15.6-05 | Repudiation | which claims reached paid verification | mitigate | One `log.info` naming group count, sizes, parents and whether a group spans questions; a named `log.warning` per fallback trigger. |
| T-15.6-SC | Tampering | package installs | accept | This plan installs no package and changes no `requirements.txt`, so the Package Legitimacy Gate has nothing to audit. |
</threat_model>

<verification>
- `py_compile` clean on both modified files and the new test file.
- Every committed test body executed by the AST-lift harness, `N/N` green.
- The mutation battery: every named mutation red, zero blind.
- `ast.unparse` of `EMIT_ORIENTATION_TOOL`, `force_emit_orientation`, `EMIT_FACT_LIST_TOOL` and
  `SERPAPI_SEARCH_TOOL` identical to the phase base.
- `git diff --stat <phase-base> HEAD` lists exactly the three files in `files_modified`, no extras.
- `git diff --diff-filter=D <phase-base> HEAD` is empty.
- **PENDING-CLOUDBUILD:** the pytest run itself. Resolved by
  `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml` AFTER plan 15.6-07
  registers the file. Read the build TEXT, never the shell exit status — `| tail` reports 0 on a
  failed build. Baseline to beat: engine **1118 passed / 33 of 33 files**.
</verification>

<success_criteria>
- An LLM can propose any number of groups and Python returns at most 5, never fewer than the material
  supports, and never a partition that loses a winner.
- Grouping failure is a named, logged, deterministic one-group-per-client-question fallback carrying a
  D-12 degradation, and the old top-k machinery has no successor here.
- No string the grouping model wrote can become a group's parent, id, or a provider-bound query.
</success_criteria>

<output>
Create `.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-01-SUMMARY.md`
when done. State the mutation battery in full, and state PENDING-CLOUDBUILD honestly — it is not a pass.
</output>
</content>
</invoke>
