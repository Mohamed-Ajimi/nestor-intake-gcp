---
phase: 15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3
plan: 03
type: execute
wave: 2
depends_on: ["15.6-01", "15.6-02"]
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py
  - tribunal/nestor_pulse_sdk/pipeline/deep_researchers/degraded_parallel.py
  - tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Research is dispatched by TOPIC: every group goes to all three providers, and no angle is placed by its position in a deal."
    - "`own` is gone from the rotation and ONLY from the rotation — its runner, its timeout and its report label still exist, so reinstating it is one line."
    - "`corroboration_key` is populated on EVERY angle, because the group IS the key — the column stops being NULL for 12 of 15."
    - "A group spanning two client questions dispatches one angle per provider carrying ALL of its sub-questions, and no claim is given a fabricated single sub-question."
    - "A group that spans two client questions is FLAGGED on the angle and WARNED about in the log, because every one of its claims will carry the top member's parent as `facet` and nothing downstream corrects that."
    - "`divide()`'s focus-area path is byte-behaviour identical — only the D6 branch changed."
    - "`_D6_TOP_K` and the round-robin remainder deal are gone, and `NESTOR_TRIBUNAL_D6_TOP_K` no longer exists to mislead whoever tunes the 15.8 run."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py"
      provides: "group-driven dispatch over three uniform streams"
      contains: "groups"
    - path: "tribunal/nestor_pulse_sdk/pipeline/deep_researchers/degraded_parallel.py"
      provides: "the ACCEPTED-GAP comment at ALL_PROVIDERS — commented, deliberately not closed"
      contains: "ALL_PROVIDERS"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py"
      via: "the group record and the deterministic fallback"
      pattern: "fallback_groups"
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py"
      via: "_corroboration_key on _enriched, now populated for every angle"
      pattern: "_corroboration_key"
---

<objective>
Replace position with topic. `_divide_from_winners` stops dealing and starts dispatching groups.

Purpose: today the top 3 winners go to all four streams and **the remainder is dealt round-robin**
(`research_division.py:867-874`). That is why V-01's coffee question landed on gemini — position in
the deal, not fit. Under grouping, every group goes to gemini + openai + claude, `own` leaves the
rotation, and `corroboration_key` finally populates for every claim instead of ~3 of 15.

**This plan DELIBERATELY CHANGES WHICH CLAIMS REACH PAID VERIFICATION. That is the deliverable, not
a regression.** Unlike phase 15.5 the constraint is not "no behaviour change" — it is
**attributability**. Every change must be readable in the log.

Output: a group-driven `_divide_from_winners`, a three-entry `_D6_STREAMS`, the deleted top-k
machinery, one accepted-gap comment, and a reworked test file.

**NO DEPLOY.** Waves 1-4 land in git only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-CONTEXT.md
@.planning/CONTINUE-HERE.md

Worktree hygiene, before any edit:
- **Your worktree may be cut from a STALE ref** — 3 of 3 in phase 15.5, one an outright ancestor of
  the whole phase. `git rev-list --count HEAD ^BASE` returning `0` means "not ahead", NOT "in sync".
  Assert `git merge-base --is-ancestor <phase-base-sha> HEAD`.
- **Per-dependency assertions. STOP if any fails** — plans 15.6-01 and 15.6-02 must be in your base:
  1. `pipeline/tribunal/question_grouping.py` exists and defines `fallback_groups`, `build_groups`,
     `GROUP_BRACKET_MANDATE`, `GROUP_BRACKET_DISCOVERY` and `warn_if_over_ceiling`.
  2. `pipeline/tribunal/discovery_bracket.py` exists and defines `DISCOVERY_PARENT`.
  3. `pipeline/tribunal/tools.py` defines `EMIT_QUESTION_GROUPS_TOOL`.
- **File ownership.** You own `research_division.py`, `degraded_parallel.py` and
  `test_research_division_assignment.py` for this phase. Do NOT touch `workshop_rank.py` (plan
  15.6-04), `synthesis/steps.py` (15.6-05), `pipeline.py` (15.6-06) or
  `cloudbuild.test-engine.yaml` (15.6-07).
- **Do NOT pin an exact set over a file a sibling edits.** In particular do not assert an exact
  module list, an exact import allowlist, or an exact set of angle keys over anything outside
  `research_division.py`. That is the trap that turned phase 15.5's merged tree red while both
  worktrees were green.
- Do NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md`.

<interfaces>
Verified against the tree at planning time.

**`_STAKES_PROVIDER` (`:122`) CANNOT BE DELETED, and the spec's "delete the inverted map outright" is
not implementable as written.** Its live consumers are `divide()`'s focus-area path (`:948`, `:980`,
`:1017`) — which this phase must NOT touch — and `run_angles`' defensive default (`:1310-1311`,
`:1628`). The D6 branch has never consulted it: `_angle` sets `"provider": stream` directly. What
this plan removes is the LAST place stakes influenced which stream a winner reached — the
`sorted(..., key=(stakes, rank))` ordering of the round-robin remainder deal at `:868-871`. Record
that distinction in the constants block; do not delete the map.

**`_D6_TOP_K` (`:135`) has a SECOND consumer**: `_stakes_for_rank` (`:473`) uses `r <= _D6_TOP_K` as
the "high stakes" boundary, and stakes flows on to `pipeline._propagate_stakes` (`:4271`), the gates'
priority and the report. Deleting the constant without replacing that boundary would silently move
gate priority in a phase already changing dispatch.

**`_D6_MIN_CORROBORATION` (`:140`) still has a job.** `_trim_ladder` reads it at `:714` to decide
whether shedding a copy is a P2 (non-degrading) or a P1 (degrading) trim. Under uniform 3-provider
allocation the floor is satisfied by construction AT DISPATCH, but the ladder is where it earns its
keep. It is NOT a second dead knob — say so in its comment.

**`_MAX_ANGLES` (`:170`) and its arithmetic comment (`:153-169`) are now wrong.** They derive 28 from
`_D6_MAX_WINNERS=15` and `_D6_TOP_K` at its clamp of 4. Re-derive.

**`_angle_query` (`:423-455`)** composes ONE angle's query in four parts and three of them are
security controls (T-15.2-60): parent assignment first and verbatim; the sub-question collapsed to
single spaces and truncated to `_SUBQ_CHARS`; a framing sentence plus a literal ignore-instructions
line naming it as DATA; the D7 language paragraph LAST so injected text is never the final word.

**`_angle` (`:836-861`)** already emits `query`, `stakes`, `focus_area`, `provider`, `sub_question`,
`rank`, `langs`, `language`, `corroboration`, `corroboration_key`. It resolves an unknown `parent` to
`labels[0]`. That existing orphan rule is what keeps `__discovery__` from ever becoming a
`focus_area` — do not add a second rule for it.

**Angles are read DEFENSIVELY, and new code must match**:
`angle.get('sub_question') or angle.get('focus_area') or ''` at `:1337`, `:1470`. Never a subscript.
The focus-area path produces angles with **no `sub_question` key at all**, and that is the precedent
for omitting it.

**`run_angles` reads two values onto `_enriched` (`:1441-1442`, `:1544-1549`)**:
`sub_q = angle.get("sub_question") or None` and `corr_key = angle.get("corroboration_key") or None`.
D-W2-2: absent is NULL, **never the empty string**. `_enriched` is ONE dict literal on purpose.
**Do not edit that block.** What changes upstream of it is that `corroboration_key` is now always
populated and `sub_question` is now absent on a multi-member group.

**THE MIXED-GROUP ATTRIBUTION FACT, verified 2026-07-29 — read this before writing `_group_angle`.**
On the PRIMARY path a claim's `facet` is stamped in Python from the angle and **cannot** be corrected
downstream:
- `synthesis/steps.py:2400` — `facet = str(result.get("_angle") or "") or "general"`, i.e. the
  angle's `focus_area`, with the comment that it is CALLER-SUPPLIED and never read out of report text
  (T-15.2-60);
- it is passed to `_normalise_fact_claim(..., facet=facet, fact_source="fact_list")` at `:2337`
  (forced-tool branch), `:2435` (fact-list branch), `:2498` (re-ask branch);
- `facts.py:298` — the D8 contract is
  `STATEMENT<TAB>SOURCE_URL<TAB>QUALITY<TAB>CERTAINTY<TAB>EVIDENCE`. **No facet column exists**, so a
  provider cannot say which sub-question a fact answers even if it wanted to;
- the model-supplied FACET seeded from the client labels at `steps.py:1627` is the
  **`distiller_fallback` (D-14) path only** — the path a stream takes when it IGNORED the fact-list
  instruction. It is not the primary path and must not be cited as the primary path's correction.

Consequence: a group spanning two client questions gives EVERY one of its claims the top-ranked
member's parent. Bounded by the fact that `_one_section` receives every provider report in full
(`steps.py:981`), so the section writer still sees the prose; what is lost is the facet-scoped anchor
ledger entry (`steps.py:952` -> `citations/anchors.py:254`), the correct skeptic tier
(`pipeline.py:4291`) and the honest per-question count (`pipeline.py:3234`).

**`checkpoints.angles_digest`** derives its fingerprint from each angle's `query` ALONE. The query
text CHANGES in this plan, so the digest legitimately moves — a resumed run from a pre-Wave-3
checkpoint will discard its recorded research and re-buy the angles. That is correct (the questions
genuinely changed) and it is already handled and logged at `pipeline.py:1904-1913`. Note it in the
SUMMARY; do not try to preserve the digest.

**`degraded_parallel.ALL_PROVIDERS` (`deep_researchers/degraded_parallel.py:77`)** is
`("gemini", "claude", "openai", "own")`, is live and reachable (imported by `pipeline.py:93`,
`research_division.py:68`, `orchestrator.py:37`), and **stays at four**. `InsufficientProvidersError`
counts it and `test_insufficient_providers_error_counts_four_streams` pins it — that test must stay
GREEN and unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The rotation and the constants — own leaves, top-k dies, the arithmetic is re-derived</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py, tribunal/nestor_pulse_sdk/pipeline/deep_researchers/degraded_parallel.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` lines 1-30 (the module docstring, which names `_D6_TOP_K`) and lines 109-260 (the whole constants block, `_PROVIDER_RUNNERS`, `_RESUMABLE_PROVIDERS`, `_D8_PROMPT_PROVIDERS`, `_PROVIDER_TIMEOUTS`)
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` lines 457-478 — `_stakes_for_rank`, the second consumer of `_D6_TOP_K`
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` lines 639-766 — `_trim_ladder`, the consumer of `_D6_STREAMS` order and `_D6_MIN_CORROBORATION`
    - `tribunal/nestor_pulse_sdk/pipeline/deep_researchers/degraded_parallel.py` lines 55-100 — `ALL_PROVIDERS`, the flags and `MIN_SUCCESSES`
  </read_first>
  <action>
**(a) `_D6_STREAMS` (`:130`) becomes `("gemini", "openai", "claude")`.** Extend the existing comment:
D-W3-3 removed `own` from the ROTATION AND ONLY THE ROTATION — 2 of its 4 angles failed outright
(`own_researcher_no_fact_list`), it reported English in a Dutch run, and it contributed 2 unique URLs
across the whole run. `_PROVIDER_RUNNERS["own"]` (`:182`), the `own` entry in `_PROVIDER_TIMEOUTS`
(`:258`) and the report label in `pipeline.py:3869` are **KEPT deliberately** — the spec keeps it as
a targeted fact-lookup tool, and reinstating it must stay a one-line change. Do not touch
`_RESUMABLE_PROVIDERS` or `_D8_PROMPT_PROVIDERS`.

**(b) DELETE `_D6_TOP_K` and the `NESTOR_TRIBUNAL_D6_TOP_K` env read.** D-W3-2: an env var that no
longer does anything is a trap for whoever tunes the 15.8 run. Grep the whole repo for
`D6_TOP_K` afterwards and confirm zero hits outside your own removal commit.

**(c) ADD `_D6_HIGH_RANKS = 3`, NOT env-backed**, immediately above `_stakes_for_rank`'s constants
neighbourhood, and change `_stakes_for_rank`'s `if r <= _D6_TOP_K:` to `if r <= _D6_HIGH_RANKS:`.
The comment must say exactly why this is not the deleted knob wearing a hat: it is the NUMERIC
BOUNDARY the old constant happened to supply, kept FIXED so that stakes — and therefore
`_propagate_stakes`, the gates' priority and the report — does not silently move in a phase that is
already changing dispatch. It is not a dispatch strategy, it is not tunable, and it selects no
provider.

**(d) `_D6_MIN_CORROBORATION` (`:136-140`) — KEEP, and answer the open question in its comment.**
Under uniform 3-provider allocation every group has three copies by construction, so the floor never
binds at dispatch; it binds inside `_trim_ladder`, which is what decides whether shedding a copy
DEGRADES the run. It is therefore **not** a second dead knob like `_D6_TOP_K`. Write that sentence.

**(e) `_STAKES_PROVIDER` (`:122`) — KEEP, and correct the record.** Add to the block above it: the D6
branch has never consulted this map (`_angle` sets the provider from the stream tuple); its live
consumers are `divide()`'s focus-area path, which this phase must not touch, and `run_angles`'
defensive default. What Wave 3 removes is the LAST place stakes influenced WHICH STREAM a winner
reached — the `(stakes, rank)` ordering of the deleted round-robin remainder deal.

**(f) Re-derive `_MAX_ANGLES`'s arithmetic comment (`:153-169`).** The worst normal case is now
`question_grouping._D6_MAX_GROUPS` (5) x `len(_D6_STREAMS)` (3) = **15 angles**, so 28 leaves 13
slots of headroom and a normal run never trims at all. State the ONE case that can exceed it: the
D-W3-2 fallback is capped by client-question count, not by 5, so it reaches 28 angles at **10 client
questions** — name that number. Keep the F5 REVERSAL paragraph verbatim; it is still true and still
load-bearing. Do not change the value 28.

**(g) The module docstring at `:1-30`** names four streams and `_D6_TOP_K`. Rewrite those sentences
to describe group dispatch over three streams. Do not rewrite the parts about `emit_safe`, run
events, or the focus-area path.

**(h) `degraded_parallel.py` — the ACCEPTED-GAP comment. NO CODE CHANGE.** Extend the `#:` block
above `ALL_PROVIDERS` (`:74-77`) with a paragraph naming this a **deliberate Wave 3 boundary**:
phase 15.6 removed `own` from `research_division._D6_STREAMS` per D-W3-3, but this tuple keeps it;
this module is live and reachable (`pipeline.py:93`, `research_division.py:68`, `orchestrator.py:37`),
so a DEGRADED broadcast run can still route research to the provider the main rotation just dropped;
the operator was shown the wider option and chose rotation-only; and phase 15.8's measuring run must
know that a degraded path can reintroduce `own`. End with the explicit line that this is **not an
oversight and must not be "fixed" as one**. `ALL_PROVIDERS` itself stays `("gemini", "claude",
"openai", "own")` and `MIN_SUCCESSES` stays 2.
  </action>
  <acceptance_criteria>
    - `py_compile` clean on both files under `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`.
    - AST assertion: the module-level assignment `_D6_STREAMS` evaluates to exactly `("gemini", "openai", "claude")`.
    - `grep -rn "D6_TOP_K" tribunal/ .planning/ROADMAP.md` returns hits ONLY in `.planning/` prose and in this plan's own SUMMARY — zero in `tribunal/**/*.py` and zero in any `cloudbuild*.yaml`.
    - AST assertion: `_PROVIDER_RUNNERS` still gains an `"own"` key under `if own_research is not None`, and `_PROVIDER_TIMEOUTS` still has an `"own"` entry. Both proved by AST, not grep — grep passes on a comment that merely mentions the name.
    - `_stakes_for_rank(1, 15) == "high"`, `_stakes_for_rank(3, 15) == "high"`, `_stakes_for_rank(4, 15) == "med"` — identical to the pre-edit behaviour, driven by AST-lifting the function from BOTH the base and the edited file and comparing over ranks 1..15 for n in {5, 10, 15}.
    - `ast.unparse` of `_trim_ladder`, `_record_trim`, `_filter_langs`, `_with_fact_list_block`, `_run_language_code`, `_d7_language_sentence`, `_normalise_winners` and `build_mission_brief_from_winners` is IDENTICAL to the phase base.
    - `degraded_parallel.py`: `ast.unparse` of the WHOLE module is identical to the phase base (comments are not in the AST, so a comment-only change must show zero AST diff), while `git diff --stat` shows the file changed.
    - `grep -c "own" deep_researchers/degraded_parallel.py` is not used as evidence anywhere; the `ALL_PROVIDERS` value is asserted by AST literal comparison to `("gemini", "claude", "openai", "own")`.
  </acceptance_criteria>
  <done>Three streams in the rotation, no top-k, no dead env knob, and the `own` gap is written down instead of quietly closed.</done>
</task>

<task type="auto">
  <name>Task 2: Group-driven dispatch — divide() gains groups, _divide_from_winners loses the deal</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` lines 423-455 (`_angle_query`) and 769-931 (`_divide_from_winners` and `divide`) — the code you are replacing and the code you must NOT touch, in one read
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py` — the group record contract, `build_groups`, `fallback_groups`, `warn_if_over_ceiling` (plan 15.6-01's output)
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/discovery_bracket.py` — `DISCOVERY_PARENT` (plan 15.6-02's output)
    - `tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py` lines 392-499 — `parent_index` / `resolved_facet`, whose exact-string, first-wins contract explains why the sub-question key must be absent rather than fabricated on a multi-member group
  </read_first>
  <action>
**(a) `_angle_query` takes a LIST.** Change the second parameter from `sub_question: str` to
`sub_questions: Any` and accept **either a bare string (treated as a one-item list) or a sequence**.
Composition, keeping all four existing parts and their order:

1. the parent assignment, first and verbatim — unchanged;
2. the sub-question block. For ONE member keep today's singular sentence byte-for-byte. For N
   members emit a plural framing sentence — "Sub-questions to answer within this assignment (research
   ALL of the following as one connected piece of work; sibling assignments are handled separately):"
   — followed by one numbered line per member;
3. the ignore-instructions line, worded for the plural case — unchanged for the singular case;
4. `_d7_language_sentence` LAST — unchanged.

**The injection bound is PER MEMBER.** Each member's text is collapsed to single spaces and
truncated to `_SUBQ_CHARS` **individually**, so a 4-member group carries at most 4 x 600 characters
of model-authored text. State that budget change in the docstring next to the existing T-15.2-60
paragraph: the bound per item is unchanged, the number of items is what grew, and the cap on that
number is `question_grouping._D6_MAX_GROUP_SIZE`.

**(b) `divide()` gains a keyword-only `groups: Any = None`.** Signature becomes
`divide(mission_brief, *, winners=None, groups=None, trim_out=None)`. Pass `groups` straight through
to `_divide_from_winners`. **The `winners`-falsy branch and everything below `:933` is BYTE-UNCHANGED**
— it is the workshop-fallback path, has its own seven tests and is out of scope. Update only the
D6 bullet of the TWO PATHS docstring block; leave the focus-area bullet and the Returns block alone
except to add the new angle keys.

**(c) `_divide_from_winners(mission_brief, winners, groups, trim_out)` — the rewrite.** Keep steps 1
and 4 of today's stated distribution rule and replace 2 and 3:

1. **NORMALISE AND BOUND — unchanged.** `_normalise_winners`, the empty-winners fallback to
   `divide(mission_brief)`, and the `_D6_MAX_WINNERS` truncation with its existing warning. Keep the
   existing text of both log lines.
2. **RESOLVE THE GROUPS.** When `groups` is falsy, call
   `question_grouping.fallback_groups(ordered, labels)` and `build_groups(...)`, and emit a
   `log.warning` saying dispatch received no grouping and fell back to one group per client
   question. **One implementation, two call sites** — plan 15.6-01's `group_winners` owns the other
   one; do not write a second fallback here.
   Then enforce the mandate bound: a MANDATE member whose text is not among the `_D6_MAX_WINNERS`
   strongest by `(rank, position)` is dropped with a warning, and a group emptied that way is
   dropped. **Discovery members are exempt from the 15-winner bound** — they are not winners and are
   already bounded by their own 5-slot cap. Say so.
3. **DISPATCH.** For each surviving group, for each stream in `_D6_STREAMS`, append one angle. There
   is no top-k, no remainder, no round robin, and no per-angle provider preference. Then call
   `question_grouping.warn_if_over_ceiling(len(groups), len(_D6_STREAMS))`.
4. **TRIM — unchanged.** `_trim_ladder(angles, trim_out)`.

**(d) `_angle` becomes group-shaped.** Replace the inner `_angle(w, stream, corroboration, key)` with
`_group_angle(group, stream)` producing:

- `query`: `_angle_query(assignment_prompt, [m["text"] for m in members], langs, run_language)`.
  `assignment_prompt` is `parent_prompt.get(label, label)` for a mandate group; for a DISCOVERY group
  whose resolved parent is `discovery_bracket.DISCOVERY_PARENT` it is the run's
  `deep_research_prompt` (falling back to `labels[0]`'s prompt, then to `labels[0]`) — a cross-cutting
  question must not be framed as a Q1 assignment.
- `focus_area`: the existing rule, unchanged — the highest-ranked member's `parent` when it is a
  known label, else `labels[0]`. **That existing orphan rule is what maps `__discovery__` onto
  `labels[0]`; do not add a second rule.**
  **STATE THE KNOWN IMPRECISION IN A COMMENT, and state it accurately.** A mixed group's
  `focus_area` is the top-ranked member's parent, and on the PRIMARY D8 fact-list path that value
  becomes the `facet` of **every** claim from the group — including the claims answering the other
  client question. There is no downstream correction: `steps.py:2400` stamps
  `facet = str(result.get("_angle") or "")` in Python and passes it to `_normalise_fact_claim`, and
  the D8 contract at `facts.py:298` is
  `STATEMENT<TAB>SOURCE_URL<TAB>QUALITY<TAB>CERTAINTY<TAB>EVIDENCE` — **there is no facet column in
  it at all.** The model-supplied FACET seeded at `steps.py:1627` belongs to the `distiller_fallback`
  (D-14) path only. Do NOT write that the distiller corrects this; it does not.
  Name the four things a wrong facet reaches, so the next reader can price it:
  `_propagate_stakes` (`pipeline.py:4291`, wrong skeptic tier), the facet-scoped anchor ledger
  (`steps.py:952` -> `citations/anchors.py:254`, the fact lands in the wrong section's ledger),
  `claims_per_facet` (`pipeline.py:3234`, the operator-facing per-question count), and the
  `claim.facet` column. Note the one thing it does NOT reach: `_one_section` receives
  `reports_concatenated` — every provider report in full (`steps.py:981`) — so the section writer
  still SEES the prose; what it loses is the anchored, citable version of the fact.
  **Whether mixed groups occur at all is plan 15.6-04's `prefer_single_parent` setting, pending an
  operator ruling.** This plan makes the imprecision impossible to miss and measurable; it does not
  decide the policy.
- `stakes`: `_stakes_for_rank(group_rank, n)` where `group_rank` is the minimum member rank and `n`
  is the total member count across all groups.
- `provider`: `stream`. `rank`: `group_rank`. `language`: `run_language`.
- `langs`: `_filter_langs` over the concatenation of the members' `langs`, so the cap
  `_D7_MAX_LANGS` still applies once.
- `corroboration`: **always `True`**. `corroboration_key`: **always `group["group_id"]`**. Comment
  this as the D-W3-1/D-R4 payoff: the group IS the key, so the column that was NULL for ~12 of 15
  winners now populates for every claim, and phase 15.5 deliberately did not fake it.
- `sub_question`: present ONLY when the group has exactly one member, carrying that member's text.
  For a multi-member group the key is **OMITTED ENTIRELY** — not set to `None`, not set to a join,
  not set to the first member. Write the reason in the code: `run_angles` reads it as
  `angle.get("sub_question") or None` and D-W2-2 makes absent mean NULL; writing the first member's
  text would be a FABRICATED attribution, which phase 15.5 already ruled worse than a NULL because it
  would look like a real corroboration partner. The focus-area path already produces angles with no
  such key, so this is the existing precedent, not a new shape.
- `sub_questions`: the ordered list of member texts. Additive, in-memory only — nothing here enters
  the frozen audit payload (T-15.2-64), the same note `_angle` already carries.
- `parents`: the group's ordered parent list. `bracket`: `group["bracket"]`.
- `mixed_parents`: `True` when `len(parents) > 1`, else absent. This is the measurement hook — it is
  what lets plan 15.6-06's feed line and the 15.8 measuring run count how often a claim's `facet` is
  an approximation rather than a fact.

**(e) The attribution log line.** Replace the existing `log.info` at `:876-880` with one that names
what dispatch DECIDED: the group count, each group's `group_id`, size, parents and bracket, the
stream count, and the total paid calls. This phase deliberately changes which claims reach paid
verification; that decision must be recoverable from the log without the claim table.

**(f) The mixed-group WARNING, separate from the info line.** For every group with
`len(parents) > 1`, emit a `log.warning` naming the group id, its parent labels, the label its
angles will carry as `focus_area`, and the consequence in plain words: every claim from this group
will be attributed to that one label, so the per-question claim counts for the other label(s) will
understate reality. It is a WARNING and not a note because it is the one place this phase knowingly
records something it cannot fully substantiate, and phase 15.8 must be able to grep for it. Emit
NOTHING when every group is single-parent — a warning that is always on is the alarm fatigue D-12
rejects.

Do NOT touch `run_angles`, `_enriched`, `_one_angle`, the coverage retry, or anything below `:1041`.
  </action>
  <acceptance_criteria>
    - `py_compile` clean.
    - `ast.unparse` of `divide`'s focus-area body: extract the `if winners:` early return and compare EVERYTHING after it to the phase base — IDENTICAL. Also assert the seven focus-area tests named in `test_research_division_assignment.py` still pass unchanged (`test_divide_assigns_providers_by_stakes`, `test_divide_broadcast_fallback_uses_med_provider`, `test_divide_angle_cap_trims_redundancy_copies_first`, `test_divide_legacy_focus_area_path_is_unchanged_without_winners`, and the three `run_angles` preference tests).
    - Driving the edited `_divide_from_winners` over 15 winners in 5 groups yields exactly **15 angles**, three per group, one per stream, and `{a["provider"] for a in angles} == {"gemini", "openai", "claude"}`.
    - `all(a["corroboration"] for a in angles)` and `all(a["corroboration_key"] for a in angles)` — **no angle carries an empty key**. Assert with `!= ""` and `is not None` separately, because the empty string is falsy too and telling them apart is the whole reason D-W2-2 exists.
    - A 3-member group produces angles with **no `sub_question` key**: `"sub_question" not in angle` asserted directly, and `angle.get("sub_question") or None is None`. A 1-member group produces `angle["sub_question"] == that member's text`.
    - A group spanning `["Q1", "Q2"]` produces `angle["focus_area"] == "Q1"` (the top member's parent), `angle["parents"] == ["Q1", "Q2"]` and `angle["mixed_parents"] is True`; a single-parent group has NO `mixed_parents` key.
    - The mixed-group `log.warning` fires exactly once per mixed group, names both labels and the surviving `focus_area`, and does NOT fire at all when every group is single-parent — asserted with `caplog` in both directions.
    - A discovery group whose top member is parented `__discovery__` produces `angle["focus_area"] == labels[0]` and an `angle["query"]` that starts with the run's `deep_research_prompt`, not with `labels[0]`'s research prompt.
    - `_angle_query(prompt, ["a", "b", "c"], [], "")` places the ignore-instructions line AFTER all three member lines and the language paragraph LAST; `_angle_query(prompt, "a", [], "")` is byte-identical to the pre-edit `_angle_query(prompt, "a", [], "")`.
    - A member whose text is 5,000 characters is truncated to 600 in the query, and a 4-member group's total model-authored text in the query is at most 2,400 characters.
    - `divide(mission_brief)` with no `winners` and no `groups` returns the same angle list as the phase base for the same brief — byte-compared.
    - Mutation controls, each turning a NAMED test red: `corroboration_key` set back to `""` for a group; the per-member truncation removed; `sub_question` set to `members[0]["text"]` unconditionally; `_D6_STREAMS` restored to four entries; the `_D6_MAX_WINNERS` bound removed.
  </acceptance_criteria>
  <done>Every group goes to all three providers, every angle carries a real corroboration key, and no claim is handed a sub-question it did not answer.</done>
</task>

<task type="auto">
  <name>Task 3: Rework test_research_division_assignment.py — retire the deal, keep the rules it proved</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py` in full — you are editing roughly a dozen of its 60 tests and must leave the rest byte-unchanged
    - `.planning/phases/15.5-research-engine-redesign-claim-attribution-wave-2/15.5-02-SUMMARY.md` — the AST-lift harness, the mutation-battery discipline, and the two call sites a BLIND mutation uncovered
  </read_first>
  <action>
This file is already registered in `cloudbuild.test-engine.yaml`; **do not add it and do not touch
`EXPECTED_FILES`.**

**Tests whose PREMISE the deal removed. Rewrite them; do not delete the rule they protect:**

- `test_d6_top_k_winners_go_to_every_stream` -> `test_every_group_goes_to_every_stream`. Same
  assertion shape, over groups.
- `test_d6_remainder_is_dealt_round_robin_over_the_streams` -> **replace with**
  `test_there_is_no_remainder_deal_left`: over 15 winners in 5 groups, assert every angle has
  `corroboration is True` and a non-empty key, i.e. no single-stream angle exists at all. The old
  test's `monkeypatch.setattr(rd, "_D6_TOP_K", 3)` must be gone.
- `test_run_angles_routes_to_all_four_streams` -> `..._to_all_three_streams`. The name lied the
  moment `own` left.
- `test_d_r3_enriched_carries_the_dispatch_sub_question_and_corroboration_key` — its premise line
  `assert len(corroborated) == 3 * len(rd._D6_STREAMS)` is arithmetic over the deleted top-k. Re-base
  it on `groups x len(_D6_STREAMS)` and keep the actual D-R3 assertion intact.
- `test_d_r3_a_remainder_angle_records_none_and_never_the_empty_string` — **THE RULE MUST SURVIVE,
  THE FIXTURE CANNOT.** There is no remainder angle any more. Rename to
  `test_an_angle_with_no_key_records_none_and_never_the_empty_string` and drive it from the
  **focus-area path**, which genuinely produces angles with neither key. Keep every `is None`
  assertion exactly as it is — D-W2-2 exists precisely because the empty string is falsy too, and
  weakening these to falsiness checks would silently retire phase 15.5's central invariant.
- `test_d6_trim_ladder_sacrifices_surplus_depth_before_corroboration`,
  `test_d6_trim_ledger_records_every_removal`,
  `test_d6_trim_below_the_corroboration_floor_is_degrading` — the ladder is unchanged, but their
  fixtures build angles via the deal. Rebuild the fixtures from groups; the P3/P2/P1 assertions stay.
  Note in the test file that the P3 rung (surplus non-corroboration depth) is now **empty by
  construction on the D6 path**, and that this is why the ladder is exercised with a hand-built angle
  list rather than only through `divide()`.
- `test_d6_angle_cap_survives_a_pathological_winner_list` — keep; re-derive its expected numbers.
- `test_d6_distribution_is_deterministic` — keep; it is now a stronger claim, because grouping is
  the only nondeterministic input and the fallback path must replay byte-identically.

**Tests that must stay BYTE-UNCHANGED and GREEN — assert this explicitly in the SUMMARY:**
`test_insufficient_providers_error_counts_four_streams` (it counts `degraded_parallel.ALL_PROVIDERS`,
which stays at four), `test_own_stream_unavailable_reason_names_the_condition_and_leaks_nothing`,
`test_own_stream_has_its_own_shorter_timeout`, the four `test_d03_*` tests, the two
`test_invariant_2_*` tests, `test_the_facet_resolution_seam_is_pure_and_never_raises`, and the seven
focus-area / `run_angles` preference tests named in Task 2.

**`test_the_facet_resolution_seam_has_no_production_caller_in_this_wave` — READ IT BEFORE YOU TOUCH
IT.** Phase 15.5 committed it as a tree scan asserting `resolved_facet` and `parent_index` are
uncalled. This phase's plan **15.6-04** is their first production caller. If that test is written to
scan the whole tree, it will go RED on the merged tree from a change in a file you do not own.
**Do not edit it and do not delete it.** Instead: record in your SUMMARY, under a heading
`HAND-OFF TO 15.6-04`, the test's exact name, its file, its line number and what it scans, so plan
15.6-04 can retire or re-scope it in the same commit that adds the caller. Reporting it is your
deliverable; changing it is not.

**New tests to add:**
- Every group's members appear in exactly one angle per stream, and the union of all angles'
  `sub_questions` equals the union of all group member texts.
- `warn_if_over_ceiling` fires for the fallback shape (6 client questions) and not for 5 groups.
- The attribution log line names the group count and the spanning flag — assert with `caplog`.

**Then run the MUTATION BATTERY** listed in Task 2's acceptance criteria, plus: the group loop
changed to dispatch one stream per group; `_D6_HIGH_RANKS` changed to 4; the `sub_questions` union
assertion's own fixture inverted. Zero BLIND mutations, or a new test per blind one.

Execute the committed test bodies with the AST-lift + `pytest`-stub harness (phase 15.5's method) and
mark the pytest run PENDING-CLOUDBUILD, naming `cloudbuild.test-engine.yaml` and plan **15.6-07**.
  </action>
  <acceptance_criteria>
    - Every test body in the file executes under the AST-lift harness; the SUMMARY states `N/N` and names each rewritten test old -> new.
    - `grep -n "_D6_TOP_K" tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py` returns nothing.
    - The `is None` assertions in the renamed remainder test are present and are `is None`, not falsiness — asserted by reading the AST `Compare` node ops, not by grep.
    - The byte-unchanged list above is verified with `git diff` on the file: those test function bodies appear in no hunk.
    - The mutation battery: every named mutation red, zero blind, mutation -> test listed in the SUMMARY.
    - The SUMMARY contains a `HAND-OFF TO 15.6-04` section naming `test_the_facet_resolution_seam_has_no_production_caller_in_this_wave`, its file and line, and what it scans.
    - `git diff --stat <phase-base> HEAD` for this plan lists exactly the three files in `files_modified`.
  </acceptance_criteria>
  <done>The suite proves group dispatch, still proves everything the deal used to, and hands the one cross-plan collision forward instead of silently touching another plan's file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grouping output -> research query | Group membership now decides what three paid providers read. |
| engine -> gemini / openai / claude | Up to four winners' text per query instead of one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.6-11 | Tampering | `_angle_query` with N sub-questions | mitigate | Per-member collapse and truncation to `_SUBQ_CHARS`, member count capped by `_D6_MAX_GROUP_SIZE`, parent assignment still first and verbatim, ignore-instructions line still after the members, D7 language paragraph still last. |
| T-15.6-12 | Elevation of privilege | `focus_area` / `parents` on a group angle | mitigate | Both stamped in Python from member `parent` values, resolved against `parent_prompt`'s known labels; the existing orphan rule maps an unknown parent to `labels[0]`. No group field is read from model output. |
| T-15.6-13 | Repudiation | which claims reached paid verification | mitigate | The rewritten `log.info` names group ids, sizes, parents, brackets, stream count and total paid calls. |
| T-15.6-33 | Repudiation | `facet` on a mixed-group claim | **accept, and instrument** | On the primary D8 path a mixed group gives every claim the top member's parent, and there is no facet column in the provider contract to correct it. Not closed here: the angle carries `mixed_parents`, a `log.warning` names every mixed group and its consequence, and whether mixed groups happen at all is plan 15.6-04's `prefer_single_parent` setting, pending an operator ruling. **Nothing in this plan may describe the distiller FACET column as a correction — it is a different code path.** |
| T-15.6-14 | Information disclosure | `own` still reachable via `degraded_parallel` | accept | Recorded as a deliberate Wave 3 boundary in a comment at `ALL_PROVIDERS`; the operator was shown the wider option and chose rotation-only. Phase 15.8 must know a degraded run can reintroduce `own`. |
| T-15.6-15 | Denial of service | angle explosion on the D-W3-2 fallback | mitigate | `_MAX_ANGLES` at 28 still bounds it (reached at 10 client questions), `_trim_ladder` still trims by priority and records every removal, and `warn_if_over_ceiling` names the overshoot in the T-15.2-61 register. |
| T-15.6-SC | Tampering | package installs | accept | No package installed, no `requirements.txt` change. |
</threat_model>

<verification>
- `py_compile` clean on all three files.
- `ast.unparse` IDENTICAL vs the phase base for: `divide`'s focus-area body, `_trim_ladder`,
  `_record_trim`, `_normalise_winners`, `build_mission_brief_from_winners`, `_filter_langs`,
  `_with_fact_list_block`, `_d7_language_sentence`, `run_angles`, `_enriched`'s enclosing function,
  and the WHOLE of `degraded_parallel.py`.
- Every committed test body executed by the AST-lift harness, `N/N`.
- The mutation battery: every named mutation red, zero blind.
- `git diff --diff-filter=D <phase-base> HEAD` empty; `git diff --stat` lists exactly three files.
- **PENDING-CLOUDBUILD:** the pytest run. Resolved by
  `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml`, owned by plan
  15.6-07. Read the build TEXT — `| tail` reports exit 0 on a FAILED build. Baseline to beat:
  engine **1118 passed / 33 of 33 files**.
- **STATED AS OWED, NEVER CLAIMED:** nothing in this plan runs against a live engine, and the
  `angles_digest` change means a resumed pre-Wave-3 run re-buys its angles. That is correct and is
  observed only at the 15.8 run.
</verification>

<success_criteria>
- 5 groups x 3 providers = 15 paid calls on the happy path, against V-01's 19, with every question
  covered by three independent streams.
- `corroboration_key` is non-empty on every angle.
- `own` is out of the rotation, still one line from returning, and its remaining reachability is
  written down.
- `divide()`'s focus-area path is byte-behaviour identical.
</success_criteria>

<output>
Create `.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-03-SUMMARY.md`
when done. It MUST contain the `HAND-OFF TO 15.6-04` section and the full mutation battery.
</output>
</content>
