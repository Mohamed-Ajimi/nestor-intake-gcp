---
phase: 15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3
plan: 07
type: execute
wave: 4
depends_on: ["15.6-01", "15.6-02", "15.6-03", "15.6-04", "15.6-05", "15.6-06"]
files_modified:
  - tribunal/cloudbuild.test-engine.yaml
autonomous: true
requirements: []

must_haves:
  truths:
    - "The engine gate RUNS the two new test files rather than silently skipping them."
    - "`EXPECTED_FILES` and the file list moved in the SAME edit, and the printed `collecting:` count reconciles against an explicit intended list."
    - "Both builds are green and both counts ROSE from the phase 15.5 baseline."
    - "Every PENDING-CLOUDBUILD raised by plans 01-06 is named, with the build that resolved it and the result."
  artifacts:
    - path: "tribunal/cloudbuild.test-engine.yaml"
      provides: "the two new test files registered and the count assertion moved with them"
      contains: "EXPECTED_FILES=35"
  key_links:
    - from: "tribunal/cloudbuild.test-engine.yaml"
      to: "tribunal/nestor_pulse_sdk/tests/test_question_grouping.py"
      via: "the WANTED list read twice — once by ls, once by the count assertion"
      pattern: "test_question_grouping"
---

<objective>
Turn the phase's proofs into a gate, and then run it.

Every plan in this phase deferred one thing it could not do on this machine: the pytest run itself.
This plan registers the two new test files, reconciles the count, runs BOTH builds, and closes every
PENDING it inherited. It is deliberately LAST — the file's own rule is that a name is added in the
commit that creates the file, and the only way to honour that with a single owner is to add all the
names once the files exist.

Output: two lines and one number in one YAML edit, two green builds, and an honest PENDING ledger.

**NO DEPLOY.** A Cloud Build test run is not a deploy: it provisions no database, requires no
provider key, and touches no Cloud Run service. Do not run `gcloud run deploy`, do not build an
application image, do not apply a migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-CONTEXT.md
@.planning/CONTINUE-HERE.md

Worktree hygiene, before any edit:
- **Your worktree may be cut from a STALE ref.** This plan is the LAST in the phase and needs all six
  predecessors. Assert `git merge-base --is-ancestor <phase-base-sha> HEAD`, then assert each of the
  six SUMMARY files exists AND each of these paths exists:
  `pipeline/tribunal/question_grouping.py`, `pipeline/tribunal/discovery_bracket.py`,
  `tests/test_question_grouping.py`, `tests/test_discovery_bracket.py`.
  **STOP if any is missing** — registering a name whose file is absent is precisely the silent-skip
  window this config exists to close.
- **You are the ONLY owner of `tribunal/cloudbuild.test-engine.yaml` in this phase.** No other plan
  may touch it, and you may touch nothing else.
- Do NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md` — the orchestrator rolls those up.
- `.planning/` is gitignored — `git add -f` for your SUMMARY.

<interfaces>
**`tribunal/cloudbuild.test-engine.yaml` IS A LOADED GUN. Read its header before editing.**

- The whole script is ONE single-quoted `bash -c '...'` argument, capped by Cloud Build at **10,000
  characters**. It currently sits at roughly 5,150. **NO APOSTROPHE ANYWHERE INSIDE IT** — not in a
  comment, not to quote a rule. One apostrophe closes the string and the build dies with
  `exit 127` / `python: command not found` long after pip has succeeded. It cost a red build on
  2026-07-27. Commentary goes in YAML comments OUTSIDE the block, where it costs nothing.
- The `WANTED` variable is at `:284-316` and lists **33** paths. `EXPECTED_FILES=33` is at `:322`.
  The file's own instruction, stated three times: **change the list and the number in the SAME
  edit.** Splitting them is how a gate goes green having run less than it claims.
- `FILES=$$(ls $$WANTED 2>/dev/null || true)` at `:323`. The `|| true` is LOAD-BEARING — under
  `set -e` a bare `ls` of a missing file aborts the script — and it is also what makes a missing file
  a SILENT SKIP. The `EXPECTED_FILES` assertion is the half that converts that skip into a red build.
  **Do not remove either.**
- `echo "collecting: $$COLLECTED of $$EXPECTED_FILES expected files"` at `:350` is the line to read
  in the build output. Reconcile it against your explicit intended list, not against a memory of it.
- The `-m "not live"` filter at `:352` is LOAD-BEARING: without it a live-marked test fires a real
  LLM call against an Anthropic account at its monthly cap.

**`tribunal/cloudbuild.test-gates.yaml`** is the second gate and owns `test_claim_distiller.py`. No
file in this phase belongs to it, but it must still be run: plan 15.6-05 edits `steps.py`, which that
gate exercises.

**Baselines to beat** (phase 15.5, builds `3f57de7a` and `e9e75413`):
engine **1118 passed / 33 of 33 files**, gates **187 passed**. **Both counts must RISE.** This phase
adds two files and dozens of test functions across six more.

**`gcloud builds submit ... | tail` REPORTS EXIT 0 ON A FAILED BUILD.** Read the build TEXT, or
`gcloud builds list --limit=5`, and never the shell status. This has cost this repository real time.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the two new test files — list and number in one edit</name>
  <files>tribunal/cloudbuild.test-engine.yaml</files>
  <read_first>
    - `tribunal/cloudbuild.test-engine.yaml` in FULL — the header, every YAML comment above `steps:`, and the whole script block. The no-apostrophe rule and the two load-bearing guards are stated there and you must not tidy either away.
  </read_first>
  <action>
**(a) Add exactly two paths to `WANTED`**, at the END of the list, in the file's existing continuation
style (`\` line continuations, aligned):

- `nestor_pulse_sdk/tests/test_question_grouping.py`
- `nestor_pulse_sdk/tests/test_discovery_bracket.py`

**(b) In the SAME edit, change `EXPECTED_FILES=33` to `EXPECTED_FILES=35`.**

**(c) Add the phase's entry to the YAML COMMENTS above `steps:`**, following the shape 15.3, 15.4 and
15.5 already use in that block. It must state:
- `15.6-01 -> test_question_grouping.py` — the group record, the hard ceiling of 5, the totality of
  the assignment, the deterministic one-group-per-client-question fallback, and the index-only tool
  schema. It is PURE: every function under test is a pure string/list transform, and the one async
  function is driven through a hand-written duck-typed fake. No Postgres, no provider key, no network.
- `15.6-02 -> test_discovery_bracket.py` — no source no slot, the global pool with a per-parent cap
  of 3, the roll-back to the mandate, and the `__discovery__` sentinel. PURE for the same reasons.
- The `33 -> 35` bump, and the reason both names were added by ONE plan at the END of the phase
  rather than by their own wave-1 plans: this file has a single owner per phase, and adding the names
  after the files exist means the `ls ... || true` silent-skip window never opens at all.

**NO APOSTROPHES anywhere inside the `bash -c '...'` block.** Your comment text goes in the YAML
comment region above `steps:`, where apostrophes are safe — but write it without any anyway if you
are unsure which side of the boundary you are on. After editing, count the apostrophes inside the
script region and confirm the number is still exactly **2** (the two that are legitimately there
today) and that the region is still under 10,000 characters.

Change NOTHING else: not the `|| true`, not the assertion block, not the `-m "not live"` filter, not
`machineType`, not `timeout`, not `logging`.
  </action>
  <acceptance_criteria>
    - `WANTED` names exactly 35 paths, counted programmatically from the YAML text, and `EXPECTED_FILES=35` — both in the same commit, verified with `git show --stat` and by reading the diff hunks.
    - Every one of the 35 paths exists on disk: run the same `for f in $WANTED; do [ -f "$f" ] || echo MISSING $f; done` loop locally against the repo before submitting a build. Zero MISSING.
    - The script region (from `bash -c '` to its closing quote) contains exactly 2 apostrophes and is under 10,000 characters — both measured with a script, not by eye.
    - `python -c "import yaml"` is NOT required; validate the YAML by submitting the build. A local YAML parse is optional, not evidence.
    - `git diff` on the file shows changes ONLY inside `WANTED`, on the `EXPECTED_FILES` line, and in the YAML comment region above `steps:`.
  </acceptance_criteria>
  <done>The gate names 35 files, asserts 35, and all 35 exist.</done>
</task>

<task type="auto">
  <name>Task 2: Run both builds, reconcile the counts, and close the PENDING ledger</name>
  <files>tribunal/cloudbuild.test-engine.yaml</files>
  <read_first>
    - The six SUMMARY files `15.6-01-SUMMARY.md` … `15.6-06-SUMMARY.md` in this phase directory — every `PENDING-CLOUDBUILD` item you are closing is named in one of them
    - `.planning/phases/15.5-.../15.5-VERIFICATION.md` — the 1118 / 187 baselines and the build ids they came from
  </read_first>
  <action>
Run BOTH builds:

```
gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT"
gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml  --project="$GOOGLE_PROJECT"
```

**Read the build TEXT.** `| tail` reports exit 0 on a FAILED build; the shell status is not evidence.
Use `gcloud builds list --limit=5` and the build log for the verdict.

From the engine build, record verbatim:
- the `collecting: N of 35 expected files` line, and **reconcile N against your explicit list of 35**
  — a matching number is only meaningful once you have confirmed the list is the list you intended;
- the pytest summary line (`N passed`, and any `failed` / `error` / `skipped`);
- the `-rs` SKIP summary. `test_stale_reclaim.py` is DB-bound and skips here by contract; that is
  **not a pass**. Any OTHER skip is a finding — name it.

From the gates build, record the pytest summary line.

**Both counts must RISE** from engine 1118 / gates 187. If either did not, that is the finding, not a
rounding error: say which file's tests did not collect and why.

**Then write the PENDING ledger into your SUMMARY.** One row per PENDING-CLOUDBUILD item raised in
plans 15.6-01 through 15.6-06, with: the plan, the item, the build that resolved it, and the result.
An item you could not close stays OPEN with a named reason — do not convert it to a pass.

**Finally, state what remains OWED and is NOT closed by this plan**, so the 15.8 session inherits an
accurate list:
- **No deploy and no live run.** Nothing in phases 15.4-15.7 has run against a live engine. Judging
  this engine from a delivered report (`output` row, `format='markdown'`) happens once, at 15.8.
- **The two alembic proofs already owed** — the literal lines `Running upgrade 0015 -> 0016` and
  `Running upgrade 0016 -> 0017`. Neither migration has ever touched a database. **This phase adds no
  third** — confirm that by asserting no new file exists under
  `tribunal/nestor_pulse_sdk/alembic/versions/` versus the phase base, and say so explicitly.
- Whether any plan reported a `SEAM: CALLER FOUND` (plan 15.6-04) or a
  `HAND-OFF TO 15.6-04` item (plan 15.6-03) that was left unresolved.

If a build is RED, fix forward within this plan's file only. **A failure in another plan's file is
NOT yours to fix** — record it, name the plan that owns the file, and report it to the orchestrator.
That is the boundary phase 15.5's cross-plan gate failure was caught at.
  </action>
  <acceptance_criteria>
    - Both builds report SUCCESS, verified from the build text or `gcloud builds list`, never from a piped shell status. Both build ids are recorded in the SUMMARY.
    - The engine build printed `collecting: 35 of 35 expected files`, and the SUMMARY reconciles that 35 against an explicit enumerated list.
    - Engine passed count > 1118; gates passed count > 187. Both numbers quoted verbatim from the build output.
    - Zero failures and zero errors in both builds. The `-rs` skip list is quoted in full, and every skip other than `test_stale_reclaim.py` is named and explained.
    - The SUMMARY contains a PENDING ledger table covering every PENDING-CLOUDBUILD item from plans 15.6-01 to 15.6-06 — no item silently absent.
    - The SUMMARY states, as OWED and not claimed: no deploy, no live run, the two outstanding alembic proofs, and that this phase added no migration (asserted by `git diff --name-only <phase-base> HEAD -- tribunal/nestor_pulse_sdk/alembic/` being empty).
    - `git diff --stat <phase-base> HEAD` for this plan lists exactly one file: `tribunal/cloudbuild.test-engine.yaml`.
  </acceptance_criteria>
  <done>Both gates are green, both counts rose, and every deferred proof is either closed with a build id or left open with a reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI config -> what the phase claims to have proven | A gate that runs less than it names converts an unproven change into a green tick. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.6-29 | Repudiation | the `ls ... \|\| true` silent skip | mitigate | The list and `EXPECTED_FILES` move in one edit; all 35 paths are checked to exist locally BEFORE the build; the printed `collecting:` line is reconciled against an explicit enumerated list, not against a remembered number. |
| T-15.6-30 | Tampering | the single-quoted script region | mitigate | Apostrophe count and character count are measured with a script after the edit; all new commentary lives in the YAML comment region outside the argument. |
| T-15.6-31 | Repudiation | `gcloud builds submit \| tail` reporting 0 on a red build | mitigate | The verdict is read from the build text or `gcloud builds list`; the shell status is explicitly not evidence, and both build ids are recorded. |
| T-15.6-32 | Repudiation | a PENDING silently becoming a pass | mitigate | A ledger row per inherited PENDING; an unclosable item stays OPEN with a named reason; the two alembic proofs and the absent live run are restated as OWED. |
| T-15.6-SC | Tampering | package installs | accept | This plan installs nothing. The build's `pip install -r requirements.txt` uses the existing pinned file, unchanged by any plan in this phase. |
</threat_model>

<verification>
- `WANTED` names 35 paths; `EXPECTED_FILES=35`; both in one commit.
- All 35 paths exist on disk, checked locally before the build.
- Script region: exactly 2 apostrophes, under 10,000 characters, measured.
- Engine build SUCCESS, `collecting: 35 of 35`, passed > 1118, zero failed, zero errored.
- Gates build SUCCESS, passed > 187, zero failed, zero errored.
- The PENDING ledger covers plans 01-06 with build ids.
- `git diff --name-only <phase-base> HEAD -- tribunal/nestor_pulse_sdk/alembic/` is empty.
</verification>

<success_criteria>
- The phase's tests are RUN, not merely written, and the gate's own count assertion says so.
- Both baselines rose, and the numbers are quoted from the build rather than asserted.
- Everything this phase could not prove is named as owed, with the two alembic lines at the top of
  the list for phase 15.8.
</success_criteria>

<output>
Create `.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-07-SUMMARY.md`
when done. It is the phase's proof-of-work record: build ids, the two counts, the full skip list, the
PENDING ledger, and the OWED list.
</output>
</content>
