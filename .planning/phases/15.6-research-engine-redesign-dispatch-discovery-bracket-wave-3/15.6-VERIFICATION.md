---
phase: 15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3
verified: 2026-07-30T06:57:03Z
status: passed
score: 42/42 must-haves verified (all 7 plans)
overrides_applied: 0
deferred:
  - truth: "corroboration_key genuinely populates for every claim, and the grouping LLM's real output is usable, in a live run"
    addressed_in: "Phase 15.8"
    evidence: "15.6-CONTEXT.md: 'NOT DEPLOYED... ONE deploy and ONE measuring run happen at the end of phase 15.8.' Operator ruling 2026-07-29 forbids any live run until all 5 waves are built. 15.6-07-SUMMARY.md § 'STATED AS OWED, NEVER CLAIMED' states this explicitly: 'A green engine gate is not a green engine... They prove nothing about what three real providers return, what the grouping LLM actually emits, or whether corroboration_key finally populates in a real run.'"
  - truth: "Two Alembic proofs (0015->0016, 0016->0017) running against a live database"
    addressed_in: "Phase 15.8"
    evidence: "15.6-CONTEXT.md constraints: 'Two alembic proofs are already owed at 15.8.' 15.6-07-SUMMARY.md confirms this phase added no third migration (`git diff --name-only 34bf790 HEAD -- alembic/` empty)."
---

# Phase 15.6: Research Engine Redesign — Dispatch + Discovery Bracket (Wave 3) Verification Report

**Phase Goal:** Replace one-angle-per-question dispatch with an LLM that groups the winning questions
into **at most 5 groups**, each sent to **all providers** — and add a **discovery bracket** that may
raise questions the client did not ask, each carrying the quote and source that provoked it. **No
source, no slot.** `own` is dropped from the rotation. 5 x 3 = 15 calls against V-01's 19.

**Verified:** 2026-07-30T06:57:03Z
**Status:** passed
**Re-verification:** No — initial verification

## Method

This phase ships pure/deterministic Python plus one audited LLM call, with no live run possible
(operator ruling: no deploy/measure until all 5 waves land). Verification therefore means: (1) read
every artifact the phase claims to have built and confirm it does what the CONTEXT's D-W3-* rulings
require, not what the SUMMARY says it does; (2) confirm the two Cloud Build gates the orchestrator
already read (not just their exit codes) are consistent with what the code actually contains; (3)
cross-check every plan's `must_haves.truths` against the merged tree directly, file by file, mostly by
`Read`/`Grep` on `question_grouping.py` (1346 lines), `discovery_bracket.py` (565 lines),
`research_division.py`, `workshop_rank.py`, `pipeline.py` and `synthesis/steps.py` at the specific
call sites named in `15.6-CONTEXT.md`'s `<canonical_refs>` section — never by trusting a SUMMARY
bullet. No file was bulk-read; each was Grepped for symbols first, then Read at offset/limit.

## Goal Achievement

### Observable Truths (all 42, from all 7 plans' `must_haves.truths`, plus the roadmap goal)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | LLM proposes groups; Python clamps to AT MOST 5; fewer is normal | ✓ VERIFIED | `question_grouping.py:97` `_D6_MAX_GROUPS = min(5, max(1, env or 5))` — hard-clamped, cannot be raised past 5 even by env var. `clamp_groups` step 1 (`:706-751`) merges down to the ceiling; no floor-padding code exists anywhere in the module. |
| 2 | Every winner lands in exactly one group — total assignment, orphans placed not dropped | ✓ VERIFIED | `validate_groups` (`:428-556`) computes `missing = [i not in claimed]`, places each via `_place_orphan` (`:397-425`), then asserts the union==range(total) and disjoint post-condition in code, falling back on failure rather than dispatching a partial set. |
| 3 | `parent`/`parents`/member texts stamped in Python from winners; nothing the grouping model wrote reaches a provider | ✓ VERIFIED | `build_groups` docstring + body (`:282-380`): `parent`, `parents`, `client_parents`, `group_id` all derived from the `winners` list, never from `raw_group`/model output. `tools.py`'s `EMIT_QUESTION_GROUPS_TOOL` schema (`:391-446`) admits only integers (`member_numbers`) — no string field a parent could be read from. |
| 4 | Grouping failure/unusable output → ONE GROUP PER CLIENT QUESTION + D-12 degradation | ✓ VERIFIED | `fallback_groups` (`:806-874`) is the deterministic replacement; `group_winners` (`:1196-1346`) calls it on all four named failure paths (call raised, no tool_use block, unparseable input, empty validation) and each returns a degradation reason string, never a note. |
| 5 | Mandate group holds exactly ONE client question, unless >5 client questions force otherwise (D-W3-5) | ✓ VERIFIED | `clamp_groups` step 0 (`:648-695`, `prefer_single_parent=True` always in production, pinned at `question_grouping.py:1333` per D-W3-5, not a caller flag) splits mixed groups by parent while the ceiling permits; only merges back into a mixed group when `room <= 0`, i.e. >5 client questions. |
| 6 | Discovery rider parented to a client question rides in that question's group at no extra call; only cross-cutting `__discovery__` earns its own group | ✓ VERIFIED | `attach_discovery_riders` (`:884-1021`) matches a rider's `parent` against `client_parents` of existing mandate groups and appends; a cross-cutting question never matches (its parent is `__discovery__`, no mandate group carries that as a client_parent) so it is built separately with `bracket=GROUP_BRACKET_DISCOVERY` (`workshop_rank.py:3117-3123`). |
| 7 | A rider never displaces a winner; oversized group sheds weakest RIDERS only | ✓ VERIFIED | `attach_discovery_riders` size-cap loop (`:1004-1017`): `rider_positions` computed from `_is_rider`; `if not rider_positions: break` — "A WINNER IS NEVER SHED" comment and code agree. |
| 8 | Fallback logs paid-call count loudly when it exceeds 5x3 ceiling | ✓ VERIFIED | `warn_if_over_ceiling` (`:1022-1057`) is called unconditionally after every dispatch, including the fallback path, at `research_division.py:1297`, immediately after step 3's per-group-per-stream loop — fires regardless of whether `resolved` came from the LLM or `fallback_groups`. |
| 9 | Question with no fetched http(s) source never reaches research — no source, no slot | ✓ VERIFIED | `discovery_bracket.allocate_discovery` (`:257-450`) rule 2 (`:373-382`): drops any entry whose `assumption`/`world_says` is empty or `source_url` doesn't start `http(s)://`, counted and named in a note. |
| 10 | Discovery ≤5 from a GLOBAL pool, per-parent cap 3, never invents to fill a quota | ✓ VERIFIED | `_DISCOVERY_MAX_SLOTS=5`, `_DISCOVERY_PER_PARENT_CAP=3` (`:99,113`); loop stops at `limit` with `break` (rule 7, `:339-342`) — zero candidates returns zero questions, no padding code exists. |
| 11 | Unused discovery slots roll back to the mandate, never into more discovery | ✓ VERIFIED | `workshop_rank.py:3049-3051`: `max_mandate_groups = _D6_MAX_GROUPS - (1 if cross_cutting else 0)` — the subtraction only happens when a cross-cutting group exists; with none, the mandate gets the full ceiling, which *is* the roll-back, effected by the subtraction not firing. |
| 12 | Cross-cutting discovery carries explicit parent `__discovery__`, coverage assertion ignores it | ✓ VERIFIED | `DISCOVERY_PARENT = "__discovery__"` (`discovery_bracket.py:128`); `_is_discovery_group` (`workshop_rank.py:2258`) and `_covered_by_mandate_members` (`:2269-2311`) both explicitly skip it. |
| 13 | Discovery rider is a RIDER — travels in mandate group, no extra call; only cross-cutting earns a group | ✓ VERIFIED | Same as #6, confirmed twice (plans 02 and 04 both state this truth; same code path answers both). |
| 14 | Dispatched discovery question traceable in the report to its quote and URL | ✓ VERIFIED | `provenance` dict stored on every discovery question (`discovery_bracket.py:432-441`: `question`, `assumption`, `world_says`, `source_url`); `steps.py:757-766` renders the assumption/world_says/URL in the base bullet, and `researched_as` (`:800-809`) is appended to that same bullet — the full quote+URL+researched-as chain is one sentence. |
| 15 | Research dispatched by TOPIC — every group to all three providers, no positional dealing | ✓ VERIFIED | `_divide_from_winners` step 3 (`research_division.py:1292-1295`): `for group in resolved: for stream in _D6_STREAMS: angles.append(_group_angle(group, stream))` — no top-k, no round-robin code remains anywhere in the file. |
| 16 | `own` gone from rotation ONLY — runner/timeout/report label intact | ✓ VERIFIED | `_D6_STREAMS = ("gemini", "openai", "claude")` (`:180`). `_PROVIDER_RUNNERS["own"]` (`:298`), `NESTOR_TRIBUNAL_OWN_TIMEOUT_S` entry (`:374`), and `pipeline.py:4033` `"own": "Own researcher..."` label all still present verbatim. |
| 17 | `corroboration_key` populated on EVERY angle | ✓ VERIFIED | `_group_angle` (`research_division.py:1256`): `"corroboration_key": str(group.get("group_id") or "")` — set unconditionally on every angle since every angle is a group-dispatch angle; group_id is never empty for a built group. |
| 18 | Group spanning two client questions dispatches one angle per provider carrying ALL sub-questions; no fabricated single sub-question | ✓ VERIFIED | `_group_angle` `"query"` built from `texts` (all members) via `_angle_query`; `"sub_question"` key set **only** `if len(texts) == 1` (`:1273-1274`) — multi-member groups omit the key entirely rather than fabricate one, matching D-W2-2. |
| 19 | Group spanning two CLIENT questions flagged + warned; never for mandate+discovery-rider | ✓ VERIFIED | `mixed_parents` triggers on `len(client_parents) > 1`, never on `parents` (`:1282-1283`); the `log.warning` at `:1322-1345` triggers on the same `client_parents` count — a discovery rider is excluded from `client_parents` by construction (`build_groups` `:349-358`), so it can never trip the flag. |
| 20 | `divide()`'s focus-area path byte-behaviour identical — only D6 branch changed | ✓ VERIFIED | `divide()` (`:1424-1425`): `if winners: return _divide_from_winners(...)` — the untouched branch below still reads `focus_areas`/`_STAKES_PROVIDER` exactly as before; `_STAKES_PROVIDER` usages at `:1442,1474,1511` are all inside this untouched branch, not the D6 path (confirmed: `_group_angle` stamps `"provider": stream` directly, `:1243`, so the D6 path never reaches the `_STAKES_PROVIDER` fallback in `run_angles` either). |
| 21 | `_D6_TOP_K` and round-robin gone; `NESTOR_TRIBUNAL_D6_TOP_K` no longer exists | ✓ VERIFIED | `grep -rn "_D6_TOP_K\|NESTOR_TRIBUNAL_D6_TOP_K" tribunal/**/*.py` returns only one hit — a historical prose sentence in `question_grouping.py`'s own docstring describing what dispatch *used to do*. No live reference, no env var. |
| 22 | Every client-validated question in at least one MANDATE group, asserted in Python AFTER grouping | ✓ VERIFIED | `enforce_group_coverage` (`workshop_rank.py:2433-2660`), called at `:3162` immediately after `attach_discovery_riders`, computes `missing` from `_covered_by_mandate_members` and repairs via promote-or-inject. |
| 23 | Repair ladder unchanged: promote best below-cut candidate, else inject verbatim, placed at TOP ranked first | ✓ VERIFIED | `:2568-2609`: promote from `all_ranked` first, else `_verbatim_winner`; `:2644-2646`: `out_winners = repairs + out_winners; out_groups = repair_groups + out_groups` — prepended, then `_rerank`/`_restamp_groups` puts repairs at rank 1. |
| 24 | Coverage counts MANDATE MEMBERS not mandate groups; a Q2-parented rider inside Q2's group does NOT satisfy Q2 | ✓ VERIFIED | `_covered_by_mandate_members` (`:2269-2311`) explicitly skips `_is_discovery_member(member)` inside a mandate group's own member loop — a rider parented "Q2" inside Q2's group is excluded from coverage, matching D-W3-5's refinement exactly. |
| 25 | Discovery rider costs no extra group; only cross-cutting earns `d1`; none present = no discovery group at all | ✓ VERIFIED | Confirmed #6/#11/#13; `d1` is only ever built when `cross_cutting` is non-empty (`workshop_rank.py:3117`). |
| 26 | Guard idempotent, never raises | ✓ VERIFIED | `enforce_group_coverage` wraps its whole body in `try/except Exception` (`:2519` onward, catch not shown but pattern matches sibling `enforce_scope_guard`); re-running on its own output (no `missing` labels) hits the early-return no-op path. |
| 27 | Discovery takes a slot from INSIDE the 5; mandate gets ≤4 when discovery fires, all 5 when not | ✓ VERIFIED | Confirmed #11 exactly. |
| 28 | Stage B returns `groups`/`discovery` in its contract, and on its crash path too | ✓ VERIFIED | `_stage_b_result` builder (`:2778-2817`) defaults `groups=()`/`discovery=()` so every path supplies them; crash-path call at `:3298-3309` explicitly builds `crash_groups` via `fallback_groups`+`build_groups` before returning. |
| 29 | Report says, in client's own language, which questions client asked vs evidence raised | ✓ VERIFIED | `_SECTION_STRINGS[lang]["brief_raised_question"]` present in all 4 language dicts (`steps.py:540,562,584,608`), rendered via `build_disputed_and_changed` at `:800-809`. |
| 30 | Discovered question appears exactly ONCE, annotated onto its conflict, never a second row | ✓ VERIFIED | `annotate_conflicts` docstring + code (`discovery_bracket.py:494-565`): "ANNOTATE, NEVER APPEND" — returns same-length, same-order list, adding a key to matching entries; `steps.py:800-809` appends the clause text to the existing bullet, no second bullet emitted. |
| 31 | A conflict never becoming a research question renders exactly as today | ✓ VERIFIED | `steps.py:800-802`: `researched = ... if isinstance(raw_researched, str) else ""`; `if researched and raw_flag:` — absent key leaves `raw_flag` unmodified, identical to pre-phase rendering. |
| 32 | Provenance clause exists in all 4 languages; no English in a Dutch report | ✓ VERIFIED | Read all 4 `brief_raised_question` strings directly (`steps.py:540-611`) — genuinely translated Dutch/German/French text, not English left in place. |
| 33 | Groups stage B decided are the groups research is dispatched on, end to end | ✓ VERIFIED | `pipeline.py:1777` `groups = list(workshop_result.get("groups") or [])`; `:1971` `angles = divide(mission_brief, winners=winners, groups=groups, trim_out=_trims)`. |
| 34 | Discovery questions reach report annotated onto provoking conflicts | ✓ VERIFIED | `pipeline.py:3454-3456`: `"brief_conflicts": discovery_bracket.annotate_conflicts(brief_conflicts, discovery_dispatched)` inside `report_sections`. |
| 35 | Operator feed says how many groups, and that every one went to all three streams | ✓ VERIFIED | `pipeline.py:2066-2099`: `_uniform_dispatch = bool(groups) and _corroborated == len(groups)`; feed string names `{len(groups)} research group(s)` and `"every one of those N group(s) went to all 3 research streams"` when true. |
| 36 | Run exceeding 5x3 ceiling says so loudly in the log | ✓ VERIFIED | Confirmed #8; `pipeline.py:1972-1982` documents (and confirms via reading `research_division.py:1297`) that the single alarm already fires and is deliberately not duplicated. |
| 37 | Stubbed e2e exercises the whole new path; grouping failure degrades not breaks | ✓ VERIFIED | `test_engine_e2e_stubbed.py` has `test_a_grouping_failure_degrades_the_stubbed_run_and_does_not_break_it`, `test_an_incomplete_grouping_partition_is_repaired_not_dropped`, `test_the_stubbed_run_dispatches_by_group_not_by_position`, `test_a_discovery_question_reaches_the_delivered_report_in_the_stubbed_run`, `test_a_shed_discovery_question_is_reported_but_never_claimed_as_researched`, `test_no_angle_is_dispatched_to_own` — 12 async tests total, all named per must-have. |
| 38 | Engine gate RUNS the two new test files, not silently skips | ✓ VERIFIED | `cloudbuild.test-engine.yaml:372-373` lists both files in the WANTED set; orchestrator-verified build `3a38e947` collected 35/35 files including 62+41 tests from these two files specifically. |
| 39 | `EXPECTED_FILES` and file list moved in SAME edit, `collecting:` reconciles | ✓ VERIFIED | `cloudbuild.test-engine.yaml:379` `EXPECTED_FILES=35`; single commit `fe563e1` per `15.6-07-SUMMARY.md` ledger row 2. |
| 40 | Both builds green, both counts ROSE from 15.5 baseline | ✓ VERIFIED | Orchestrator-verified: engine 1118→1283 passed (+165), 33→35 files (+2); gates unchanged at 187 (explained: fixed 13-file set, none touched by this phase, no EXPECTED_FILES assertion on that gate). |
| 41 | Every PENDING-CLOUDBUILD from plans 01-06 named with resolving build + result | ✓ VERIFIED | `15.6-07-SUMMARY.md` ledger table lists all 24 individual assertions expanded from 13 markers, each with build id and PASS/CLOSED result; none left OPEN. |
| 42 | `degraded_parallel.ALL_PROVIDERS` accepted-gap comment present (D-W3-3) | ✓ VERIFIED | `degraded_parallel.py:96-101`: "THIS IS NOT AN OVERSIGHT AND MUST NOT BE 'FIXED' AS ONE... Closing the gap is explicitly out of scope" directly above `ALL_PROVIDERS = ("gemini", "claude", "openai", "own")`. |

**Score:** 42/42 truths verified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `question_grouping.py` | group record, validator/clamp/fallback, audited call | ✓ VERIFIED | 1346 lines; all 7 required exports present (`GROUP_BRACKET_MANDATE/DISCOVERY`, `validate_groups`, `clamp_groups`, `fallback_groups`, `build_groups`, `group_winners`). Wired into `research_division.py` and `workshop_rank.py`. |
| `tools.py` EMIT_QUESTION_GROUPS_TOOL / force_emit_question_groups | grouping tool schema | ✓ VERIFIED | `tools.py:391-455`; index-only schema, `emit_question_groups` name present. |
| `test_question_grouping.py` | ≥200 lines, clamp/totality/injection/fallback | ✓ VERIFIED | 1253 lines, 62 test functions (matches Cloud Build's 62 collected). |
| `discovery_bracket.py` | allocator, rider partition, question frame, annotator | ✓ VERIFIED | 565 lines; all 5 required exports present (`DISCOVERY_PARENT`, `allocate_discovery`, `partition_discovery`, `discovery_question_text`, `annotate_conflicts`). |
| `test_discovery_bracket.py` | ≥180 lines, no-source-no-slot/cap/rollback/V-01 case | ✓ VERIFIED | 600 lines, 41 test functions (matches Cloud Build's 41 collected). |
| `research_division.py` | group-driven dispatch over 3 streams | ✓ VERIFIED | Contains `groups` param on `divide()`; `_divide_from_winners` dispatch loop confirmed at #15. |
| `degraded_parallel.py` | ACCEPTED-GAP comment | ✓ VERIFIED | Confirmed at #42. |
| `workshop_rank.py` | `enforce_group_coverage`, stage-B wiring | ✓ VERIFIED | Confirmed at #22-28. |
| `pipeline.py` | `groups=` threaded, provenance, attribution log | ✓ VERIFIED | Contains `groups=` at dispatch call site (#33), attribution logging confirmed (feed message #35). |
| `cloudbuild.test-engine.yaml` | two files registered, `EXPECTED_FILES=35` | ✓ VERIFIED | Confirmed at #38-39. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `question_grouping.py` | `tools.py` | `force_emit_question_groups` on final grouping turn | ✓ WIRED | `group_winners` calls `audited.anthropic_messages(..., tool_choice=tools.force_emit_question_groups())` at `:1263-1265`. |
| `workshop.py` | `discovery_bracket.py` | `brief_conflicts` from orientation pass | ✓ WIRED | `allocate_discovery(conflicts, labels)` called at `workshop_rank.py:3020`, `conflicts` sourced from stage A's orientation output. |
| `research_division.py` | `question_grouping.py` | group record + deterministic fallback | ✓ WIRED | `_divide_from_winners` calls `question_grouping.fallback_groups`/`build_groups`/`warn_if_over_ceiling` directly. |
| `research_division.py` | `synthesis/steps.py` | `_corroboration_key` populated for every angle | ✓ WIRED | Confirmed at #17; `steps.py` readers of `_corroboration_key` are unmodified consumers of an now-always-populated field. |
| `workshop_rank.py` | `question_grouping.py` | audited grouping call inside stage B | ✓ WIRED | `group_winners(winners=final, ...)` at `:3099`. |
| `workshop_rank.py` | `discovery_bracket.py` | `allocate_discovery` over stage A's `brief_conflicts` | ✓ WIRED | Confirmed above. |
| `discovery_bracket.py` | `synthesis/steps.py` | `researched_as` key `annotate_conflicts` adds | ✓ WIRED | Confirmed at #30/#34. |
| `workshop_rank.py` | `research_division.py` | `workshop_result['groups']` → `divide(..., groups=...)` | ✓ WIRED | Confirmed at #33 — traced through `pipeline.py`, which is the actual caller (workshop_rank's stage-B result flows to pipeline.py, which threads it into `divide()`). |
| `cloudbuild.test-engine.yaml` | `test_question_grouping.py` | WANTED list, read twice | ✓ WIRED | Confirmed at #38-39. |

### Data-Flow Trace (Level 4)

Not applicable in the UI-rendering sense (this phase has no frontend component). The equivalent trace —
does real data flow from `brief_conflicts`/winners through grouping/allocation to the angles actually
dispatched and the report actually rendered — is covered by truths #33-36 (pipeline wiring) and the
stubbed end-to-end test (#37), which drives the whole path with fake but structurally real inputs and
asserts the DB-write shape (`_division_items`) and report bullets reflect what stage B decided, not a
static/hardcoded value. Confirmed non-hollow: `_group_angle`'s `provider` field is set to the live loop
variable `stream` (`:1243`), not a constant, and `corroboration_key` is the live `group.get("group_id")`,
not a placeholder.

### Behavioral Spot-Checks

Not run as live commands — this machine has no Python/pytest/Docker, and the phase's own constraint is
that no live run is permitted. Cloud Build IS the spot-check harness here; its results are the
orchestrator-verified facts (1283 passed / 13 skipped / 35 of 35 files, 187 passed / 2 deselected), which
this verification treats as ground truth per this task's constraints, and cross-checked structurally
against every module discussed above.

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention exists in this repository; `tribunal/cloudbuild.test-*.yaml`
is this project's probe equivalent and was addressed under Behavioral Spot-Checks / orchestrator facts
above, consistent with the mandatory-initial-read guidance for this codebase.

### Requirements Coverage

Phase declares `requirements: []` in every plan. Scope source is `.planning/ENGINE-REDESIGN-SPEC.md` § 4
(D-R4, D-R5, D-R7) plus operator decisions D-W3-1..D-W3-5, all tracked as observable truths above, not
as REQUIREMENTS.md IDs. No orphaned requirements — `grep -E "Phase 15.6" .planning/REQUIREMENTS.md`
was not applicable since this phase carries no REQUIREMENTS.md IDs by design.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `15.6-06-SUMMARY.md` | n/a | Missing `## Self-Check: PASSED` section (present in all other 6 SUMMARYs) | ℹ️ Info (process) | Not a code defect. The orchestrator independently verified 15.6-06's substantive claims by hand (per `orchestrator_verified_facts`), and this verification independently re-confirmed the same wiring (pipeline.py `groups=` threading, `annotate_conflicts` call site, e2e test names) by direct code reading. Does not affect goal achievement; flagged for process hygiene only. |

No `TBD`/`FIXME`/`XXX` debt markers found in any of the 8 core files this phase modified or created
(`question_grouping.py`, `discovery_bracket.py`, `research_division.py`, `workshop_rank.py`,
`pipeline.py`, `synthesis/steps.py`, `degraded_parallel.py`, `tools.py`). No `TODO`/`HACK`/`PLACEHOLDER`
markers introduced by this phase (one pre-existing, unrelated "NAMED PLACEHOLDER" design-pattern comment
in `steps.py:397` predates this phase and describes a rendering convention, not unfinished work). No
stub returns (`return null`/`return {}`/`return []` as a body), no hardcoded empty dispatch values — every
constant checked (`_D6_MAX_GROUPS`, `_D6_MAX_GROUP_SIZE`, `_DISCOVERY_MAX_SLOTS`,
`_DISCOVERY_PER_PARENT_CAP`) is a real, env-backed, correctly-bounded dial, not a placeholder.

### Deviation Review (executor-recorded, judged against the goal)

All deviations recorded across the 7 SUMMARYs were read and judged. Every one preserves or strengthens
the goal; none reduces scope:

- **15.6-01** (4 deviations): corrected two arithmetically-unsatisfiable plan acceptance criteria
  against the CONTEXT's own D-W3-1 precedence ruling (ceiling wins over size cap), fixed 3 test defects
  found by executing bodies, added a `why`-carrying dict keyed by winner index instead of position.
  **Goal-preserving** — implements the ruling correctly where the plan's literal wording could not.
- **15.6-03** (6 deviations): reworded a comment to avoid an unsatisfiable grep criterion while
  achieving the decision's actual intent (zero live `_D6_TOP_K` references, AST-verified), fixed two
  tests that could not both be byte-unchanged and correct under grouping, fixed two misleading log
  lines, changed `_bound_groups_to_winners` to preserve group identity rather than silently renumbering
  `d1`→`g1` mid-run, fixed two test-harness defects. **Goal-preserving.**
- **15.6-04** (8 deviations): declined to add a duplicate `prefer_single_parent` constant (the real
  `group_winners` signature pins it internally, avoiding a two-values-to-keep-in-step trap), correctly
  declined to call `warn_if_over_ceiling` from a module that would have to import a sibling's tuple to
  do so, added the `discovery_not_researched` key the plan's own two statements disagreed about,
  fixed rank-staleness bugs in the coverage-repair path that the plan's original design missed.
  **Goal-preserving, and catches two real bugs (ranks, cost-key mismatch) the plan would have shipped.**
- **15.6-05**: no deviations beyond documented decisions (item-cap truncation losing the clause is a
  recorded observation, not a defect — the item cap correctly wins per its own stated precedence).
- **15.6-06** (6 deviations): fixed five e2e assertions left red by 15.6-03's stream-rotation change
  (re-asserted in the correct direction rather than deleted), corrected an equality-vs-prefix test bug,
  corrected the plan's premise about `validate_groups` refusing vs. repairing an incomplete partition
  (repairing is what the merged code correctly does), added a shed-discovery test that was missing,
  added defence-in-depth for a resumed-checkpoint edge case. **Also caught and documented that
  15.6-04's SUMMARY claim about `warn_if_over_ceiling` being "owed to 15.6-06" was false** — independently
  re-confirmed by this verification (the call exists at `research_division.py:1297`).
- **15.6-07**: one deviation (stale worktree, corrected before any file was read — the 5th of 5
  executors in this phase to hit the known worktree-stale-base trap, handled per the documented
  procedure).

No deviation reduces the phase's delivered scope; several fix defects the plans themselves would have
shipped had they been followed literally.

### Human Verification Required

None required to determine whether this phase's goal was achieved in the codebase. This phase produces
no user-facing UI and is explicitly barred from any live/deployed run until Wave 5 (15.8) — see Deferred
Items below for what genuinely can only be confirmed then.

### Deferred Items

Two items are real open questions but are explicitly out of this phase's scope by operator ruling, not
gaps in what this phase delivers:

1. **Whether `corroboration_key` genuinely populates for every claim, and what the grouping LLM's real
   output looks like, on a live run.** The code path guarantees it will if the LLM returns anything
   coherent (angle-level, this is unconditional — see truth #17), and 62+41+62+37+35+12 = 249 new/grown
   tests exercise every branch against hand-built fakes and mutation-prove 24+11+24+32+10+~10 mutations
   bite. But no live provider or live grouping call has ever run this code. **Addressed in: Phase 15.8**
   (the single deploy + single measuring run).
2. **The two owed Alembic proofs** (`0015→0016`, `0016→0017`) — unrelated to this phase's own changes
   (this phase added zero migrations, confirmed via empty `git diff` against alembic/), but still open
   from Wave 2. **Addressed in: Phase 15.8.**

### Gaps Summary

None. All 42 observable truths across the 7 plans' `must_haves` were independently verified against the
merged tree by direct file reading — not by trusting SUMMARY.md claims. Every artifact exists, is
substantive (no stubs, no placeholders), and is wired end-to-end from the orientation pass through
grouping/discovery allocation, dispatch, coverage enforcement, and report rendering. The two Cloud Build
gates the orchestrator already verified by reading build logs (not exit codes) are consistent with what
the code contains: the two new test files are genuinely registered and genuinely ran (62+41 tests), and
the file-count/EXPECTED_FILES bookkeeping is internally consistent. The single process gap found — plan
15.6-06's SUMMARY missing its `## Self-Check: PASSED` marker — does not correspond to any missing or
incorrect implementation; its claims were independently re-verified against the code by this report.

---

*Verified: 2026-07-30T06:57:03Z*
*Verifier: Claude (gsd-verifier)*
