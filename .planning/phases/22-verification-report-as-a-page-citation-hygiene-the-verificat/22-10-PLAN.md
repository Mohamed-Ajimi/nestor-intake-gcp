---
phase: 22-verification-report-as-a-page-citation-hygiene-the-verificat
plan: 10
type: execute
wave: 6
depends_on: ["22-09"]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md
autonomous: false
requirements: [D-22-1, D-22-2, D-22-3, D-22-4, D-22-5]

must_haves:
  truths:
    - "The deploy surface was DERIVED from this phase's actual diff, not inherited from a prior phase's ruling"
    - "Every service the diff touches is deployed, and each one is proven by its revision's imageDigest"
    - "The engine fast gate ran and reported zero FAILURES before anything was built"
    - "No research run was triggered by this deploy"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "A § Phase 22 section with the derived surface, the ordered deploy and the read-back proofs"
      contains: "Phase 22"
  key_links:
    - from: "infra/DEPLOY-RUNBOOK.md"
      to: "tribunal/cloudbuild.api.yaml"
      via: "the tribunal-api image build command with an explicit _IMAGE substitution"
      pattern: "cloudbuild.api.yaml"
---

<objective>
Get this phase's code onto the services that actually run it, and prove it with digests.

Purpose: this project has a standing, repeatedly-bitten trap — **work gets built, gate-verified, and
never deployed.** It has also been bitten by the opposite: on 2026-08-06 a standing note said two
services, the diff said three, and skipping `nestor-api` would have left the whole fix **inert while
reading as deployed**. The generalisation is on the record: *"Which services a change touches is a
MEASUREMENT WITH AN EXPIRY DATE, not a fact."*

⛔ **THIS DEPLOY TRIGGERS NO RESEARCH RUN. Not one.**

Output: a derived surface table, the deployed services, the digest proofs, and a runbook section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-CONTEXT.md

<measured_facts>
Read live, read-only, during planning on 2026-08-11.

1. **Identity, and the trap.** `gcloud` account **`tools@dotto.be`**, project
   **`project-cb01b861-cb4a-438d-b9a`**, region **`europe-west1`**.
   ⚠ **The persisted gcloud config REVERTS MID-SESSION on this machine** — it did so during Phase
   21's deploy. **Pin `--account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a` on EVERY
   gcloud command.** Any shell script that calls bare `gcloud` takes no such flag, so wrap it:
   `CLOUDSDK_CORE_ACCOUNT=tools@dotto.be CLOUDSDK_CORE_PROJECT=project-cb01b861-cb4a-438d-b9a bash …`.
   ⚠ If a `--format='value(...)'` read comes back EMPTY, re-run it without `--format` before
   believing the resource is gone.

2. **Live services (read 2026-08-11):**
   | Service | Latest ready revision |
   |---|---|
   | `tribunal-api` | `tribunal-api-20260810-193000-200954` |
   | `tribunal-worker` | `tribunal-worker-00007-l8x` |
   | `nestor-frontend` | `nestor-frontend-20260810-193000` |
   | `nestor-api` | `nestor-api-00045-hdw` |

3. **Assumption A4 is RESOLVED — the service serving `GET /api/runs/{id}/verification` is
   `tribunal-api`.** Evidence, read live: `nestor-api`'s env carries
   `TRIBUNAL_SERVICE_URL = https://tribunal-api-ybkr7metoq-ew.a.run.app`, and the tribunal API image
   runs `uvicorn nestor_pulse_sdk.server:app`, which mounts the runs router whose
   `@router.get("/{run_id}/verification")` is at `runs/api.py:968`. **The worker image runs
   `python -m nestor_pulse_sdk.runs.worker` and serves no HTTP at all.**
   ⚠ **This is a MEASUREMENT, not a fact.** Task 1 re-derives it before anything is built.

4. **`tribunal-api`'s live image and digest, pre-deploy:**
   tag `europe-west1-docker.pkg.dev/project-cb01b861-cb4a-438d-b9a/nestor/tribunal-api:20260810-193000`,
   digest `sha256:3a8f2dbb079803a71f648696524c9705687d55c285c7105f151116890b3ade4b`.
   ⛔ **`containers[0].image` is a MUTABLE TAG and proves nothing.** Read `status.imageDigest` off the
   REVISION. The new digest must DIFFER from the one above.

5. ⛔ **`tribunal/infrastructure/cloud-run/build-and-push.sh` is STALE.** It builds to
   `…/nestor-pulse/api` and `…/nestor-pulse/worker`, while the live services run
   `…/nestor/tribunal-api`. It reports SUCCESS anyway. **Use `tribunal/cloudbuild.api.yaml` with an
   explicit `_IMAGE` substitution instead**, exactly as that file's own header documents.

6. ⛔ **`gcloud builds submit … | tail` reports exit 0 even when the build FAILED.** Capture the build
   id and query its status: `gcloud builds describe "$BUILD_ID" --format='value(status)'` — the
   passing value is `SUCCESS`.

7. **The engine fast gate:** `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml`.
   Plan 22-01 moved it from 44 to **45** files, changing both the `WANTED` list and `EXPECTED_FILES`.
   **Its pass condition is 0 FAILURES.**
   ⚠ On a local Windows run the gate shows **6 PRE-EXISTING errors** (a `PYTEST_CURRENT_TEST`
   32767-char env limit in `test_dispatch_pii.py` and `test_fact_list_parser.py`); Cloud Build runs
   on Linux and does not hit them.

8. **NO migration is expected in this phase** — Phase 22 touches no file under
   `tribunal/nestor_pulse_sdk/alembic/`. **Derive this rather than assume it** (Task 1). The
   write-side source-identity fix, which DOES need Alembic 0019 and a unique-index drop, was
   deliberately sequenced into its own phase (D-22-4, 22-RESEARCH § 2d).

9. **Frontend gates, measured at the phase base commit:** `npx tsc --noEmit` exit 0;
   `npx vitest run` 61/61; `node scripts/i18n-audit.mjs` `RESULT: PASS` exit 0 with 107 CHECK D
   advisories; `npm run build` exit 0.
   ⛔ `npm run lint` **cannot** exit 0 (DEF-21-01) — **do not gate on it**.
   ⛔ Never write a "zero i18n advisories" criterion. ⛔ Installs are `npm ci`, never `npm install`.

10. ⛔ **THE WORKER IS THE LAST DEPLOYABLE if it is in the surface at all**, and an empty queue is the
    only protection: `min-instances=0` does NOT stop a worker booting — **the loop CLAIMS FIRST and
    SLEEPS LAST.** On the derivation in fact 3 the worker is expected to be OUT of this phase's
    surface; if Task 1's derivation disagrees, follow the derivation and apply the empty-queue rule.

11. **`.planning/` is gitignored** — stage planning files with `git add -f` and confirm with
    `git diff --cached --name-only`.
</measured_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DERIVE the deploy surface and write the runbook section</name>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § "Phase 21" in full (its § 1 "DERIVED deploy surface" block, its § 3 ordered deploy and its § 4 read-back proofs — copy that structure verbatim, including the ⛔ callouts)
    - tribunal/cloudbuild.api.yaml (its header documents the exact invocation and why the build context is `tribunal`)
    - frontend/cloudbuild.yaml (its header documents the five required `--substitutions`)
    - tribunal/cloudbuild.test-engine.yaml lines 480-560 (the WANTED list and `EXPECTED_FILES`, now 45)
  </read_first>
  <action>
**(a) DERIVE the surface. Do not inherit it.** From the repo root, with the phase base commit as
`$BASE` (the commit before plan 22-01's first commit — state which sha you used):

```
git diff --name-only $BASE..HEAD
git diff --name-only $BASE..HEAD | awk -F/ '{print $1}' | sort -u
git diff --stat $BASE..HEAD -- backend/
git diff --name-only $BASE..HEAD -- tribunal/nestor_pulse_sdk/alembic/
```

Map each path group to a service and record the RULE you applied, not just the answer:
- `frontend/` → `nestor-frontend` (rebuild + deploy).
- `tribunal/nestor_pulse_sdk/` → the image(s) that RUN the changed code. This phase's engine change is
  on the READ path served by `nestor_pulse_sdk.server:app` → `tribunal-api`. The worker
  (`python -m nestor_pulse_sdk.runs.worker`) serves no HTTP; include it only if the derivation shows
  it runs changed code.
- `backend/` → `nestor-api`. **Expected empty this phase — and the empty diff IS the evidence.**
- `tribunal/nestor_pulse_sdk/alembic/` → a migration step. **Expected empty.**
- `.planning/`, `infra/DEPLOY-RUNBOOK.md` → no service.

⛔ **If the derivation disagrees with measured fact 3, the DERIVATION WINS.** Record the discrepancy
in the runbook rather than resolving it silently.

**(b) Confirm the read-path service, read-only:**
```
gcloud run services describe nestor-api --region=europe-west1 \
  --account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a \
  --format='value(spec.template.spec.containers[0].env)' | tr ';' '\n' | grep -i tribunal
```
Record the `TRIBUNAL_SERVICE_URL` value verbatim and the service name it resolves to.

**(c) Write `infra/DEPLOY-RUNBOOK.md` § Phase 22**, appended after § Phase 21, with:
1. **What shipped** — the five decisions in one paragraph each, plus the ⭐ statement that this deploy
   changes a READ path and the UI only: no claim, verdict, cost or dispatch behaviour changed, and
   the write-side source-identity fix is deliberately NOT in it.
2. **The DERIVED deploy surface** — the commands from (a), their actual output, and the mapping
   table, with the ⛔ "derived at deploy time, not copied" callout and the 2026-08-06 three-services
   incident as the reason.
3. **The gates**, in order, with the exact commands: the frontend four (`npx tsc --noEmit`,
   `npx vitest run`, `node scripts/i18n-audit.mjs`, `npm run build`) and the engine fast gate via
   Cloud Build — each with its PASS CONDITION stated (`RESULT: PASS` for i18n; **0 FAILURES** for the
   engine gate, not exit 0), plus the ⛔ notes that `npm run lint` is not a gate and that CHECK D
   advisories never fail.
4. **The ordered deploy**, with the identity flag pinned on every command:
   - build `tribunal-api` via `gcloud builds submit tribunal --config=tribunal/cloudbuild.api.yaml
     --substitutions=_IMAGE=europe-west1-docker.pkg.dev/<project>/nestor/tribunal-api:<SHARED_TAG>`;
   - build `nestor-frontend` via `gcloud builds submit frontend --config=frontend/cloudbuild.yaml`
     with all five substitutions **copied, not retyped** — a typo breaks the LIVE frontend;
   - deploy each built image to its service, both at the same `<SHARED_TAG>`;
   - `nestor-api` and `tribunal-worker`: **CONFIRM-ONLY** unless the derivation says otherwise —
     record their existing revision names.
   Include the ⛔ note that `build-and-push.sh` is STALE and must not be used (measured fact 5), and
   the ⛔ note that `builds submit | tail` lies about failure (measured fact 6).
5. **Read-back proofs**, copied from § Phase 21 § 4: per-service
   `status.latestReadyRevisionName` + `status.traffic`, then the REVISION's `status.imageDigest`
   (expect an `@sha256:` value that DIFFERS from `sha256:3a8f2dbb…`), the CONFIRM-ONLY service names,
   secret bindings BY NAME only, and both alembic heads read from their own tables and expected
   UNCHANGED.
6. **NO RUN WAS TRIGGERED** — re-list the audit bucket and record that the newest-write timestamp is
   unchanged from the pre-deploy reading. A newer timestamp means something claimed: STOP and report.
7. **Closing note:** the operator UAT is `.planning/phases/22-…/22-UAT.md`, it runs on RECORDED data
   with ZERO spend, and DEF-21-01 / DEF-21-03 / DEF-21-04 / DEF-22-01 will be visible and are ruled
   out of scope.
  </action>
  <verify>
    <automated>grep -c "Phase 22" infra/DEPLOY-RUNBOOK.md &amp;&amp; grep -c "imageDigest" infra/DEPLOY-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `infra/DEPLOY-RUNBOOK.md` contains a `## Phase 22` section (`grep -c "^## Phase 22" infra/DEPLOY-RUNBOOK.md` returns `1`).
    - That section contains the literal derivation commands: `grep -c "git diff --name-only" infra/DEPLOY-RUNBOOK.md` increased by at least 2 from its pre-task value (measure it first; state both numbers in the SUMMARY).
    - `grep -c "status.imageDigest" infra/DEPLOY-RUNBOOK.md` increased by at least 1.
    - The Phase 22 section carries `--account=tools@dotto.be` on every `gcloud` invocation it lists — inspect each one; a bare `gcloud` in the new section is a defect.
    - `grep -c "cloudbuild.api.yaml" infra/DEPLOY-RUNBOOK.md` returns at least `1`, and the section explicitly warns against `build-and-push.sh`.
    - The section states the engine gate's pass condition as **0 FAILURES** and does NOT state it as exit 0.
    - The section contains no `npm run lint` gate and no "zero advisories" criterion.
    - The derived surface table names every service and, for each, either "deploy" or "CONFIRM-ONLY" with the rule that produced it.
  </acceptance_criteria>
  <done>The runbook carries a Phase 22 section whose surface was derived from this phase's own diff, with pinned identity, the correct build config, and digest-based proofs.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run the gates and execute the deploy</name>
  <files>none (Cloud Build + Cloud Run only)</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 22 (written in Task 1 — the ordered procedure being executed)
    - infra/DEPLOY-RUNBOOK.md § Phase 21 § 3 and § 4 (the ordering correction that exists because following the as-written order once caused an incident, and the read-back proof format)
    - tribunal/cloudbuild.test-engine.yaml lines 480-560 (the WANTED list and `EXPECTED_FILES=45`, so the collection-count line can be read and recorded)
    - tribunal/cloudbuild.api.yaml and frontend/cloudbuild.yaml (the two build configs and their required substitutions)
  </read_first>
  <action>
PAUSE. This is a `checkpoint:human-action` because a Cloud Build submit and a Cloud Run deploy need
the operator's session — the agent's gcloud is READ-ONLY on this machine (`describe` and `list` work;
`builds submit` and `run deploy` are writes and have been classifier-blocked before).

Do every read-only pre-flight the agent CAN do first and present the results: the identity check, the
four live service revisions, `tribunal-api`'s current `status.imageDigest`, and the audit bucket's
newest-write timestamp (record that last one — Task 3 compares against it). Then hand the operator
the procedure in `<how-to-verify>` and WAIT.

Record their pasted build ids, statuses, revision names and digests VERBATIM in the SUMMARY. Do not
proceed to Task 3 until the engine gate is reported with zero FAILURES and every service in Task 1's
derived surface table is reported deployed.

⛔ Do not deploy a service the derived table does not list, and do not skip one it does.
⛔ Trigger no research run.
  </action>
  <what-built>
    Every code change in Phase 22 is committed and merged: the shared `normalize_source_url` +
    `collapse_citations_by_url` with their tests inside the engine fast gate, the dedupe seam in
    `build_verification_report`, the new `/admin/pulse/runs/:runId/verification` route with the run
    page's toggle replaced by a link, the instrumented-document restyle with its stat strip, funnel,
    nav rail, collapsed citation list and page-level citation sheet, the `Published` → `Retrieved`
    label fix across all three locales, and the activity feed removed from the intake detail page
    with the "Open run" link kept. `infra/DEPLOY-RUNBOOK.md` § Phase 22 (Task 1) carries the derived
    surface, the ordered commands with the identity flag pinned, and the read-back proofs.
    **Nothing has been built or deployed yet.**
  </what-built>
  <how-to-verify>
    Follow `infra/DEPLOY-RUNBOOK.md` § Phase 22 exactly, in order.

    **0. VERIFY THE ACCOUNT AND THE PROJECT FIRST, every time.** Four accounts exist on this machine
    and the persisted config has reverted mid-session before:
    ```
    gcloud config list account project
    ```
    Expect `tools@dotto.be` and `project-cb01b861-cb4a-438d-b9a`. Pin
    `--account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a` on every command below
    regardless of what it says. If a `value()` read comes back empty, re-run it without `--format`
    before believing a resource is gone.

    **1. The four frontend gates**, from `frontend/`:
    `npx tsc --noEmit` (exit 0) · `npx vitest run` (0 failing) ·
    `node scripts/i18n-audit.mjs` (`RESULT: PASS`) · `npm run build` (exit 0).
    ⛔ `npm run lint` is NOT a gate — it is red tree-wide at the base commit (DEF-21-01) and its
    failure says nothing about this phase. ⛔ CHECK D advisories never fail the build.

    **2. The engine fast gate**, from the repo root:
    ```
    gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml \
      --account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a
    ```
    ⛔ **Do not read it through `| tail` and do not trust exit 0.** Capture the build id and run
    `gcloud builds describe "$BUILD_ID" --account=… --project=… --format='value(status)'`.
    **The pass condition is `SUCCESS` and ZERO FAILURES in the pytest summary.** Record the gate's
    collection-count line verbatim — the config now names **45** files and asserts 45, and a
    mismatch fails loudly by design and is NOT to be worked around.

    **3. Build and deploy**, following § Phase 22 § 4 of the runbook exactly, using ONE shared tag
    for both images. Build `tribunal-api` from `tribunal/cloudbuild.api.yaml` — ⛔ **not**
    `tribunal/infrastructure/cloud-run/build-and-push.sh`, which is stale, pushes to
    `nestor-pulse/api` instead of `nestor/tribunal-api`, and reports success anyway. Copy the
    frontend's five `--substitutions` from the runbook; do not retype them — a typo breaks the LIVE
    frontend.

    **4. Do NOT deploy `tribunal-worker` or `nestor-api`** unless Task 1's derivation says to. Record
    their existing revision names instead. If the worker IS in the surface, it deploys LAST and only
    after the queue is re-proven empty immediately beforehand — `min-instances=0` does not stop a
    worker booting, because the loop claims first and sleeps last.

    **5. Run every read-back proof** in § Phase 22 § 5. Digests come from `status.imageDigest` on the
    REVISION, never from `containers[0].image`.

    ⛔ **TRIGGER NO RESEARCH RUN.** Not from the UI, not from a script, not "just to see". The
    ~$45 measuring run is a separate, deliberate decision and is not part of this deploy.
  </how-to-verify>
  <verify>
    <human-check>The engine gate's build id with a status of SUCCESS read from `gcloud builds describe`, its collection-count line quoted verbatim, its pytest summary showing zero failures, and every new revision name with an `@sha256:` digest read from `status.imageDigest`.</human-check>
  </verify>
  <resume-signal>Paste the build ids with their statuses, the shared image tag, the new revision names with their digests, and each frontend gate's final line — then type "deployed". Or describe what failed.</resume-signal>
  <acceptance_criteria>
    - Every Cloud Build id is recorded with a status of `SUCCESS` read from `gcloud builds describe`, not from a shell exit code.
    - The engine gate's pytest summary is recorded and shows **zero failures**; its collection-count line is quoted verbatim and reflects 45 files.
    - All four frontend gates are recorded with their final lines: `tsc` exit 0, vitest 0 failing, `RESULT: PASS` from the i18n audit, `npm run build` exit 0. No lint result is recorded as a gate.
    - Every deployed revision's `status.imageDigest` is recorded as an `@sha256:` value; no revision resolves to `:latest`.
    - The set of deployed services EQUALS Task 1's derived surface table — no extra, none missing. State the comparison explicitly.
    - The CONFIRM-ONLY services' existing revision names are recorded unchanged.
    - No run was triggered: the audit bucket's newest-write timestamp is recorded both before and after and the two are equal.
  </acceptance_criteria>
  <done>Both gate families green, every service in the derived surface deployed and digest-proven, nothing else touched, and no run triggered.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the deploy by digest, and record it</name>
  <files>infra/DEPLOY-RUNBOOK.md, .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 22 § 5 (the read-back proof block written in Task 1 — you are filling in its actual values)
    - infra/DEPLOY-RUNBOOK.md § Phase 21 § 4 (the format for recording proofs VERBATIM rather than as "looks right")
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md (created by plan 22-01 — append, never recreate)
  </read_first>
  <action>
All read-only. Run each with `--account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a
--region=europe-west1` pinned.

**(a) For every service in the derived surface:** read `status.latestReadyRevisionName` and
`status.traffic`, then read that REVISION's `status.imageDigest`.
⛔ `containers[0].image` is a mutable tag and is NOT proof. Record the `@sha256:` values.
**`tribunal-api`'s new digest MUST DIFFER from
`sha256:3a8f2dbb079803a71f648696524c9705687d55c285c7105f151116890b3ade4b`** — an equal digest means
the deploy did not land, whatever the tag says.

**(b) For every CONFIRM-ONLY service** (`nestor-api`, and `tribunal-worker` if the derivation excluded
it): record the existing revision name unchanged.

**(c) Secret bindings BY NAME only, never a value** — confirm `ANTHROPIC_API_KEY` and
`SERPAPI_API_KEY` are still bound on the tribunal services.

**(d) Both alembic heads, each from its OWN table** — expect them UNCHANGED (this phase ships no
migration). One does not imply the other.

**(e) NO RUN WAS TRIGGERED.** Re-list the audit bucket (`gcloud storage ls` on the audit prefix —
read-only, no build, no spend) and confirm the newest-write timestamp EQUALS the pre-deploy reading
captured in Task 2's pre-flight. A newer timestamp means something claimed: STOP and report rather
than continuing.

Write every value VERBATIM into `infra/DEPLOY-RUNBOOK.md` § Phase 22 § 5. Not "looks right" — the
actual strings.

Then append to `.planning/phases/22-…/deferred-items.md` a note recording that the write-side
source-identity fix is the named next phase: it needs Alembic **0019** adding a `normalized_url`
column plus a partial unique index on `(tenant_id, normalized_url)`, **and it must DROP
`idx_source_tenant_content_hash` in the SAME migration.** State the reason as money loss, not
tidiness: if the old partial unique index survives, `ON CONFLICT (tenant_id, normalized_url)` does not
cover a violation of it, so two same-text/different-URL sources raise an unhandled `IntegrityError`
**inside the persist transaction of a ~$45 run**. Also record the second-order behaviour change to
validate on a real run: `ON CONFLICT … DO NOTHING` means the FIRST row wins and keeps its `title`,
`provider`, `resolved_url` and `resolution_status`, so merging more rows changes which provider is
credited for a source.

`git add -f` both files (`.planning/` is gitignored) and confirm with
`git diff --cached --name-only`.
  </action>
  <verify>
    <automated>grep -c "sha256:" infra/DEPLOY-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `infra/DEPLOY-RUNBOOK.md` § Phase 22 contains at least one `@sha256:` digest per deployed service, recorded verbatim.
    - The recorded `tribunal-api` digest is NOT `sha256:3a8f2dbb079803a71f648696524c9705687d55c285c7105f151116890b3ade4b`.
    - The section records `status.traffic` at 100% for each deployed service's new revision.
    - The section records the CONFIRM-ONLY services' existing revision names.
    - The section records both alembic heads and states they are unchanged.
    - The section records the audit bucket's newest-write timestamp before and after, and they are EQUAL.
    - `grep -c "0019" .planning/phases/22-…/deferred-items.md` returns at least `1` — the write-side follow-up is named with its migration number and its money-loss reason.
    - `git diff --cached --name-only` lists `infra/DEPLOY-RUNBOOK.md` and the deferred-items file.
  </acceptance_criteria>
  <done>Every deployed service is proven by a revision digest that differs from the pre-deploy one, the untouched services are recorded, the audit bucket shows nothing ran, and the write-side follow-up is named with its trap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → GCP | Build and deploy are the only write operations in this phase, and they run under an identity that has reverted mid-session before. |
| image tag → running code | A tag is mutable; only a digest identifies what is running. |
| deploy → the paid queue | A worker boot claims work before it sleeps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-31 | Spoofing | a gcloud command running under the wrong account/project | mitigate | `--account` and `--project` pinned on EVERY command; identity re-read at step 0 of the checkpoint; scripts wrapped in `CLOUDSDK_CORE_ACCOUNT` / `CLOUDSDK_CORE_PROJECT`. |
| T-22-32 | Repudiation | a failed build reading as a success | mitigate | `builds submit \| tail` is banned; status is queried by build id and the passing value is `SUCCESS`. The engine gate's own collection-count assertion fails loudly on a shortfall. |
| T-22-33 | Repudiation | a deploy that did not land reading as deployed | mitigate | Proof is the REVISION's `status.imageDigest`, and the pre-deploy digest is recorded here so an unchanged digest is detectable. `containers[0].image` is explicitly rejected as proof. |
| T-22-34 | Denial of Service / money loss | a worker boot claiming queued work and spending ~$45 | mitigate | The worker is expected OUT of this phase's surface; if the derivation includes it, it deploys LAST and only after the queue is re-proven empty immediately beforehand. The audit-bucket timestamp check in Task 3 detects any claim after the fact. |
| T-22-35 | Tampering | pushing to the wrong Artifact Registry path | mitigate | `build-and-push.sh` is documented as STALE (`nestor-pulse/api` vs the live `nestor/tribunal-api`) and banned; the build uses `cloudbuild.api.yaml` with an explicit `_IMAGE`. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** Image builds install from the committed `requirements.txt` / `package-lock.json` inside Cloud Build; frontend installs are `npm ci`, never `npm install`. No new dependency was introduced anywhere in Phase 22, so there is no legitimacy audit surface and no install checkpoint. |
</threat_model>

<verification>
- The engine fast gate reports **0 FAILURES** and its build status is `SUCCESS` (queried by id).
- All four frontend gates pass, with `RESULT: PASS` for i18n and no lint gate.
- Every deployed service's new revision carries an `@sha256:` digest that differs from the recorded
  pre-deploy digest, at 100% traffic.
- Both alembic heads are unchanged.
- The audit bucket's newest-write timestamp is unchanged: **nothing ran.**
</verification>

<success_criteria>
- The deploy surface was derived from this phase's diff and the derivation is written down.
- Every service that runs changed code is deployed; every service that does not is recorded as
  CONFIRM-ONLY with its existing revision.
- The runbook carries the commands and the verbatim proofs, so the next reader can repeat it.
- No research run was triggered.
</success_criteria>

<output>
Create `.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-10-SUMMARY.md` when done.

State the shared image tag, every revision name and digest, the build ids, and the before/after audit
bucket timestamps. Then state plainly that the operator UAT (`22-UAT.md`) is the next action and that
it costs nothing.
</output>
