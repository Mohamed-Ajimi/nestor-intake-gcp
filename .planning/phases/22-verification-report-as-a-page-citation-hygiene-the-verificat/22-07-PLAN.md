---
phase: 22-verification-report-as-a-page-citation-hygiene-the-verificat
plan: 07
type: execute
wave: 3
depends_on: ["22-06", "22-03"]
files_modified:
  - frontend/src/components/intake/VerificationReport.tsx
  - frontend/src/components/intake/ResearchRunProgress.tsx
autonomous: true
requirements: [D-22-2]

must_haves:
  truths:
    - "Every section the report rendered before still renders, in the same order, with nothing dropped or summarised away"
    - "An operator sees how many claims were checked and how many were refuted without scrolling"
    - "The gate funnel reads as proportion, not as a comma-separated list of numbers"
    - "Section headings are visibly heavier than the metadata inside them, and each is a real h2 with an id"
    - "The page states no percentage, no ratio, no trend and no duplicates-removed figure"
  artifacts:
    - path: "frontend/src/components/intake/VerificationReport.tsx"
      provides: "The instrumented-document report: stat strip, proportional funnel, and the unchanged section set as anchored blocks"
      min_lines: 400
    - path: "frontend/src/components/intake/ResearchRunProgress.tsx"
      provides: "The now-orphaned verification toggle removed, so VerificationReport has exactly one mount"
  key_links:
    - from: "frontend/src/routes/admin.pulse.runs.$runId.verification.tsx"
      to: "frontend/src/components/intake/VerificationReport.tsx"
      via: "the page's single mount, props intakeId + runId"
      pattern: "VerificationReport"
---

<objective>
Restyle the report as an instrumented document: the answer above the fold, proportion made visible,
and the same document beneath it — not one section moved, not one dropped.

Purpose: D-22-2, operator verbatim — *"verification report contains very good information , so style
it better , like a dashboard"*. The CONTENT is endorsed. This is a presentation change.
⛔ **A restyle that hides, truncates, reorders or summarises a section is solving the wrong problem**
and is explicitly forbidden by D-22-2. The visual direction is **CLOSED** by operator ruling
(22-UI-SPEC "Visual Direction — RESOLVED"): direction **A, "instrumented document"**. Direction B,
the tile/card grid, was shown to the operator and REJECTED. Proposing a card grid here reverses a
ruling; do not.

Output: a stat strip, a proportional funnel, and the existing sections as anchored, headed blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-CONTEXT.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UI-SPEC.md

<measured_facts>
Measured on the tree at `658591b`. Do not re-derive.

1. **The section order E1–E9 in `VerificationReport.tsx` today**, and it does NOT change:
   refuted (`verdicts.refute`, line 290) → support (`verdicts.support`, 297) → insufficient
   (`verdicts.insufficient`, 303) → superseded VERDICTS (`verdicts.superseded`, 317) → superseded /
   scoped (`superseded`, 326) → reconciled (`reconciled`, 335) → unverified accounting (345) →
   numbered citations (364) → true itemized cost (391).

2. ⛔ **E4 and E5 must NOT be merged and must keep separate headings.**
   `VerificationReport.tsx:310-316` and `lib/api/research.ts:105-118` both document the deliberate
   name collision: E4 is a skeptic VERDICT CLASS ("was true, has since changed"), E5 is
   reconciliation-derived scoped findings carrying a canonical value. Same word, different question.
   One shared heading would destroy a distinction the backend went out of its way to preserve.

3. **Row rendering is UNCHANGED.** `VerdictItemRow`, `MdText`, `refToText`, the amber
   `reconciliation.note` / `superseded_note` fallback, and `showEffect` all keep their current
   behaviour and their comments. Only the section CONTAINER and HEADING change.

4. **`VerdictSection` returns `null` on an empty list** (line 158). That stays: omitting an empty
   section is not hiding information, and it is today's behaviour.

5. **The component's outer chrome is being removed.** Today it renders its own bordered container
   (`mb-5 border border-ink/30 border-l-4 bg-paperLight px-6 py-5`), its own
   `t("verification.title")` header and an optional Close button (lines 242-262). Plan 22-06's page
   now owns the header, the `border-l-4` identity mark and the back link, so the component's copies
   are duplicates. `onClose` therefore has no caller.

6. **`ResearchRunProgress.tsx` still mounts `VerificationReport` at line ~694 behind its own
   `showVerification` toggle.** That component has had ZERO render sites since plan 22-04 removed
   the intake page's element, so the mount is orphaned — and it is the second copy of the very
   toggle D-22-1 replaced. Removing it is what lets `onClose` be dropped cleanly.
   ⛔ **Do NOT delete `ResearchRunProgress.tsx` itself, and do NOT delete the
   `ResearchRunProgress` component body.** The FILE must survive:
   `admin.pulse.runs.$runId.index.tsx:11` imports `useActiveResearchRun` from it, and plan 22-04
   added `IntakeOpenRunLink` there. The body's removal is logged as DEF-22-01 and deliberately
   deferred (22-RESEARCH § Open Question 2).

7. **Locale keys are already in place** (plan 22-03, wave 1): `verification.statClaims`,
   `statWithVerdict`, `statRefuted`, `statUnverified`, `statSources`, `emptyReport`, `retry`.
   Existing keys reused: `verification.funnelTitle`, `verification.funnelStage`,
   `verification.costTitle`, `verification.costTotal`, `verification.costTotalWithPending`,
   `verification.costPending`, `verification.loadError`, `verification.unverifiedTitle`,
   `verification.unverifiedNone`, `verification.unverifiedSummary`, the seven verdict-section title
   keys, `verification.citationsTitle`, and **`research.cost`** for the Cost tile's label.
   ⛔ **Add NO new locale key in this plan.** Every string it needs already exists in all three files.

8. **The house scale this plan uses** (22-UI-SPEC Typography/Spacing/Color) — values, not inventions:
   meta/label 11px mono uppercase `tracking-wider` `text-ink/50`; body 13px sans `leading-relaxed`;
   **section heading 13px/600 mono uppercase `tracking-[0.1em]` `text-ink`** (an upgrade from today's
   11px/400, which is the same weight as the metadata inside it — one reason the report reads as a
   wall); stat figure 24px mono `tabular-nums`. Two weights only: 400 and 600.
   Section block padding `px-6 py-5`, `mb-8` between major sections, `bg-paperLight` on
   `bg-paper` page.
   **Accent `#FF2D87` is reserved and NONE of its five uses is in this plan** — the page header's
   rule (22-06), the `[n]` glyph (existing), the hover card's rule (22-03), the sheet's rule
   (22-08), the active nav-rail item (22-08). ⛔ Do not put accent on a heading, a button, a link or
   a stat tile.
   The ONE semantic colour here: `#DC2626` on the REFUTED stat figure when `> 0` and as the Refuted
   section's `border-l-4`. `#6B7280` for zero/absent figures.

9. **Baselines:** `npx tsc --noEmit` exit 0; `npx vitest run` ≥ 61 passing, 0 failing;
   `node scripts/i18n-audit.mjs` `RESULT: PASS` exit 0 with 107 CHECK D advisories (advisories never
   fail); `npm run build` exit 0. ⛔ `npm run lint` cannot exit 0 (DEF-21-01) — verify per file with
   `npm exec --prefix frontend -- eslint --config frontend/eslint.config.js <files>`.
   ⛔ Do NOT commit a change to `frontend/src/routeTree.gen.ts` from this plan; it is owned by 22-06.
   If a build regenerates it, `git checkout -- frontend/src/routeTree.gen.ts`.
</measured_facts>

<interfaces>
```tsx
// The component's props AFTER this plan. `onClose` is removed.
export function VerificationReport({
  intakeId,
  runId,
}: {
  intakeId: string;
  runId: string;
}): JSX.Element;
```

```tsx
// ONE computed value for the source count, declared once near `citations`.
// Stat tile 5 reads it here; plan 22-08's collapsed-list trigger reads the SAME const.
// 22-UI-SPEC §3.2: "Two places showing the same figure must read from one computed value —
// not two independent .length calls that can drift."
const sourcesCited = citations.length;
```

```tsx
// Section anchor ids consumed by plan 22-08's nav rail. Use exactly these.
"refuted" | "support" | "insufficient" | "superseded-verdicts" | "superseded" |
"reconciled" | "unverified" | "citations" | "cost"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The sections become anchored, headed blocks — and nothing else about them changes</name>
  <files>frontend/src/components/intake/VerificationReport.tsx, frontend/src/components/intake/ResearchRunProgress.tsx</files>
  <read_first>
    - frontend/src/components/intake/VerificationReport.tsx (the WHOLE 416-line file — especially the module header at 15-27, `VerdictItemRow` at 74-142, `VerdictSection` at 145-181, the E4/E5 collision comment at 310-316, and the outer container at 242-262)
    - frontend/src/components/intake/ResearchRunProgress.tsx around line 680-710 (the orphaned toggle + `VerificationReport` mount being removed)
    - frontend/src/routes/admin.pulse.runs.$runId.verification.tsx (plan 22-06's page — see what header chrome it already owns, so you remove the duplicate rather than both)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UI-SPEC.md § 1.3, § 1.4, § 1.9 and § States
  </read_first>
  <action>
**(a) Remove the duplicated chrome from `VerificationReport.tsx`.** Drop the outer bordered
container's border/`border-l-4`/`bg-paperLight`/padding, the `t("verification.title")` header line,
the Close button, and the `onClose` prop from the props type. Keep the `role="region"` +
`aria-label={t("verification.regionLabel")}` wrapper — it is an existing key and a real landmark.
⛔ **Do NOT add an `aria-live` region.** The run page owns the product's one live region
(T-15.3-82); a second would double-announce.

**(b) Remove the orphaned mount from `ResearchRunProgress.tsx`:** the `showVerification` state, the
toggle button that reads `verification.viewAction` / `verification.hideAction`, the
`<VerificationReport …>` element and the now-unused `VerificationReport` import. Nothing else in that
file. ⛔ The file, the `ResearchRunProgress` component body, `useActiveResearchRun` and
`IntakeOpenRunLink` all stay.

**(c) Give `VerdictSection` a block, a heading and an anchor.** Extend its props with
`id: string`, and render:
- an outer `<section id={id} className="mb-8 scroll-mt-6 bg-paperLight px-6 py-5">`;
- an `<h2>` carrying the section title in **13px/600 mono uppercase `tracking-[0.1em]` `text-ink`**,
  with `items.length` beside it in `text-ink/50`;
- the existing `<ul>` of `VerdictItemRow`s, unchanged.
It still returns `null` when `items` is empty.
Pass the ids from `<interfaces>` at each of the six `VerdictSection` call sites, in the SAME order
they appear today.

**(d) Give the three hand-rolled sections the same treatment** — unverified accounting
(`id="unverified"`), numbered citations (`id="citations"`), true itemized cost (`id="cost"`). The
citations section keeps its current flat list in this plan; plan 22-08 makes it collapsible. The
cost section keeps its `border-t border-ink/10 pt-3` and its EXACT facts-only branch: when
`cost_pending` is true it renders `costTotalWithPending` plus the amber `costPending` chip, and
**never a numeric placeholder for the pending class**.

**(e) The Refuted section — and only that one — additionally carries `border-l-4` with
`style={{ borderLeftColor: "#DC2626" }}`.** It is the one semantic rule in the document, marking the
section that answers "what broke". Colour is not the sole carrier — the heading says "Refuted".

**(f) States.** Replace the single spinner line with a `Skeleton`-based loading state (import
`Skeleton` from `@/components/ui/skeleton`): a stat-strip row of 6 tiles plus 3 section blocks. A
full page showing one spinner reads as broken. Keep the error branch's inline
`text-red-600` `verification.loadError` and ADD a `verification.retry` button that re-runs the fetch
(hoist the fetch into a `useCallback` and call it from both the effect and the button) — plus keep
the existing `toast.error` (RETURN-NO-THROW). When the report loads but every verdict list is empty,
render `t("verification.emptyReport")`; the funnel and cost still render if present, because an empty
verdict set is a finding, not a blank page.

⛔ **Not one section moves, is dropped, is truncated, is merged or is collapsed by default in this
plan.** E4 and E5 keep their separate headings (measured fact 2). `VerdictItemRow`, `MdText`,
`refToText` and the amber-note fallback are byte-identical.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run &amp;&amp; node scripts/i18n-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npx vitest run` reports 0 failing and at least 61 passing.
    - `cd frontend && node scripts/i18n-audit.mjs` prints `RESULT: PASS` and exits 0.
    - `grep -c "<VerdictSection" frontend/src/components/intake/VerificationReport.tsx` returns `6` — the same six sections as before, none merged and none added.
    - `grep -cE 'id="refuted"|id="support"|id="insufficient"|id="superseded-verdicts"|id="superseded"|id="reconciled"|id="unverified"|id="citations"|id="cost"' frontend/src/components/intake/VerificationReport.tsx` returns `9`.
    - `grep -c "supersededVerdictsTitle" frontend/src/components/intake/VerificationReport.tsx` returns `1` and `grep -c "supersededTitle"` returns `1` — E4 and E5 still have two separate headings.
    - `grep -c "onClose" frontend/src/components/intake/VerificationReport.tsx` returns `0`.
    - `grep -c "VerificationReport" frontend/src/components/intake/ResearchRunProgress.tsx` returns `0`.
    - `grep -c "export function useActiveResearchRun\|export function IntakeOpenRunLink" frontend/src/components/intake/ResearchRunProgress.tsx` returns `2` — both exports survive.
    - `grep -c "aria-live" frontend/src/components/intake/VerificationReport.tsx` returns `0`.
    - `grep -c "costTotalWithPending" frontend/src/components/intake/VerificationReport.tsx` returns `1` — the facts-only pending branch is intact.
    - `git diff --name-only` does NOT list `frontend/src/routeTree.gen.ts`.
  </acceptance_criteria>
  <done>Every existing section renders in its existing order as an anchored, headed block; the component owns no duplicate page chrome; the orphaned toggle is gone and both surviving exports remain.</done>
</task>

<task type="auto">
  <name>Task 2: The stat strip — the trust question answered above the fold</name>
  <files>frontend/src/components/intake/VerificationReport.tsx</files>
  <read_first>
    - frontend/src/components/intake/VerificationReport.tsx (as edited in Task 1 — the `report` object's shape at the top of the render branch)
    - frontend/src/lib/api/research.ts (the `VerificationReport` type — `unverified`, `verdicts`, `true_cost`, `citations`)
    - frontend/src/routes/admin.pulse.runs.$runId.index.tsx lines 253-269 (the run page's big-mono-figure stat block — the language being borrowed)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UI-SPEC.md § 1.6 (the six tiles, their sources, and the three things the strip must NOT do)
  </read_first>
  <action>
Add a stat strip directly above the funnel, inside the loaded branch of `VerificationReport.tsx`.

`grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4`, each tile `bg-paperLight px-6 py-5` with NO
border between tiles — the paper/paperLight contrast is the division. Label: 11px mono uppercase
`text-ink/50`. Figure: 24px mono `tabular-nums`.

| # | Label key | Figure | Zero / absent |
|---|---|---|---|
| 1 | `verification.statClaims` | `report.unverified?.total_claims` | `—` in `#6B7280` |
| 2 | `verification.statWithVerdict` | `report.unverified?.claims_with_verdict` | `—` in `#6B7280` |
| 3 | `verification.statRefuted` | `report.verdicts?.refute?.length ?? 0` | `0` in `#6B7280`, **not** red |
| 4 | `verification.statUnverified` | `report.unverified?.count` | `0` in `#6B7280` |
| 5 | `verification.statSources` | `sourcesCited` | `0` |
| 6 | `research.cost` | `report.true_cost?.cost_usd_total` | `—` in `#6B7280` |

Declare `const sourcesCited = citations.length;` ONCE, beside the existing `citations` const, exactly
as `<interfaces>` specifies, with the comment saying plan 22-08's collapsed-list count reads this
same value so the two figures cannot drift.

**Tile 3 is the only tile that ever takes a colour:** `#DC2626` when `> 0`, `#6B7280` when `0`.
Colour is not the sole carrier — its label says "Refuted".

**Tile 6 is C1 facts-only, unchanged in substance.** When `report.true_cost?.cost_pending` is true,
show `cost_usd_total` and an amber `bg-amber-50` chip reading `t("verification.costPending")`
beneath it. **Never a numeric placeholder for the pending class.**

⛔ **Three things the strip must NOT do:**
1. **No percentages, no ratios, no "N% verified".** `total_claims` can be `0`; a derived rate is a
   division-by-zero away from rendering `NaN%` as if it were a fact.
2. **No trend and no comparison to a previous run.** There is no such data.
3. **No "N duplicates removed" figure, and no corroboration claim anywhere on this page.**
   Read-time dedupe changes DISPLAY only — cost and corroboration still count the duplicate rows
   until the write-side fix lands (D-22-4). Tile 5's label is "Sources cited", deliberately not
   "Sources fetched", because the deduped number is the count of distinct sources SHOWN.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; node scripts/i18n-audit.mjs &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && node scripts/i18n-audit.mjs` prints `RESULT: PASS` and exits 0 — every `t()` in the strip resolves in all three locales.
    - `grep -cE 'verification\.stat(Claims|WithVerdict|Refuted|Unverified|Sources)' frontend/src/components/intake/VerificationReport.tsx` returns `5`.
    - `grep -c "sourcesCited" frontend/src/components/intake/VerificationReport.tsx` returns `2` — one declaration, one read. (Plan 22-08 raises it to 3.)
    - `grep -cE "%|toFixed|Math.round|percent|ratio|/ *total" frontend/src/components/intake/VerificationReport.tsx` shows **no arithmetic producing a rate** — inspect each hit; a `%` inside a Tailwind class or a template string is fine, a computed percentage is not.
    - `grep -ciE "duplicate|deduped|corrobora" frontend/src/components/intake/VerificationReport.tsx` returns `0` in RENDERED strings (a code comment explaining the rule is permitted and expected).
    - `grep -c "#DC2626" frontend/src/components/intake/VerificationReport.tsx` returns `2` — the Refuted tile figure and the Refuted section's left rule, and nothing else.
    - `grep -c "#FF2D87" frontend/src/components/intake/VerificationReport.tsx` returns `0` — none of the accent's five reserved uses lives in this component.
  </acceptance_criteria>
  <done>Six facts-only tiles put "how much was checked and how much broke" in the first viewport, with one shared source count and no derived rate anywhere.</done>
</task>

<task type="auto">
  <name>Task 3: The gate funnel as proportion</name>
  <files>frontend/src/components/intake/VerificationReport.tsx</files>
  <read_first>
    - frontend/src/components/intake/VerificationReport.tsx lines 275-288 as they stand after Task 2 (today's funnel: a flex-wrap of `t("verification.funnelStage", { stage, count })` spans)
    - frontend/src/lib/api/research.ts (`funnel: Record<string, number> | null`)
    - frontend/src/locales/en/intake.json (`verification.funnelTitle`, `verification.funnelStage` — both existing)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UI-SPEC.md § 1.7
  </read_first>
  <action>
Replace the funnel's flex-wrap of text spans with one row per stage:

- Stage key on the left, rendered **RAW, exactly as today**. It is an engine identifier and is
  deliberately NOT translated — inventing labels for keys this build has never seen is exactly the
  fabrication this project bars.
- A bar: a `div` with `style={{ width: `${(count / max) * 100}%` }}`, `bg-ink h-2`, on a `bg-paper2`
  track.
- The count on the right: 11px mono `tabular-nums`, right-aligned.
- `const max = Math.max(...counts, 1)` — a funnel of all zeros must not divide by zero.
- The whole row carries `aria-label={t("verification.funnelStage", { stage, count })}` — the
  **existing** key — and the bar itself is `aria-hidden`.
- Section heading: `t("verification.funnelTitle")` (existing key) at the same 13px/600 mono
  treatment as the document's other headings.

⛔ **No chart library.** `recharts` is installed, but a proportional `div` matches the house language,
costs zero bundle weight and needs no responsive container.
⛔ No new locale key. ⛔ The funnel is not a nav-rail entry and gets no `id` — it sits above the
document, beside the stat strip.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run &amp;&amp; node scripts/i18n-audit.mjs &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npx vitest run` reports 0 failing and at least 61 passing.
    - `cd frontend && node scripts/i18n-audit.mjs` prints `RESULT: PASS` and exits 0.
    - `cd frontend && npm run build` exits 0.
    - `grep -c "recharts" frontend/src/components/intake/VerificationReport.tsx` returns `0`.
    - `grep -c "verification.funnelStage" frontend/src/components/intake/VerificationReport.tsx` returns `1`, and it is on an `aria-label`.
    - `grep -F -c "Math.max(" frontend/src/components/intake/VerificationReport.tsx` returns at least `1` — the divide-by-zero guard exists.
    - `git diff --name-only` lists no file outside this plan's `files_modified`; in particular NOT `frontend/src/routeTree.gen.ts` (run `git checkout -- frontend/src/routeTree.gen.ts` if the build touched it).
    - `npm exec --prefix frontend -- eslint --config frontend/eslint.config.js frontend/src/components/intake/VerificationReport.tsx frontend/src/components/intake/ResearchRunProgress.tsx` reports zero non-`prettier/prettier` rule violations introduced by this plan.
  </acceptance_criteria>
  <done>The funnel reads as shape rather than as a list of numbers, with no chart dependency, no invented stage label and no division by zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → browser | `report.funnel` keys are ENGINE-AUTHORED strings rendered raw; verdict prose is skeptic-authored markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-20 | Tampering | a funnel stage key rendered raw | mitigate | Rendered as a React text child, never through `MdText` / `ReactMarkdown`, so `rehype-raw` cannot reach it. React escapes text children. |
| T-22-21 | Tampering | skeptic-authored markdown in `evidence_refs` / `reconciliation.canonical` | accept | UNCHANGED behaviour — `MdText` already renders these and this plan does not alter `VerdictItemRow`. Any change to that posture is out of scope and would need its own ruling. |
| T-22-22 | Repudiation | a derived rate presented as a measured fact | mitigate | No percentage, ratio, trend or duplicates-removed figure is rendered; the pending-cost branch shows a LABEL, never a number. Pinned by the grep criteria in Task 2. |
| T-22-23 | Information Disclosure | a section silently disappearing in the restyle | mitigate | Section count, the six `VerdictSection` call sites, the nine anchor ids and the two separate superseded headings are all pinned by grep criteria. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** `Skeleton` is already vendored; `recharts` is deliberately NOT used. `package.json` / `package-lock.json` unchanged. No install checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` → exit 0.
- `cd frontend && npx vitest run` → 0 failing.
- `cd frontend && node scripts/i18n-audit.mjs` → `RESULT: PASS`, exit 0.
- `cd frontend && npm run build` → exit 0.
- `git diff --name-only` lists exactly the two files in `files_modified`.
- ⛔ Whether the restyle READS better is **not** provable by a gate. D-22-2 is verified by operator
  UAT (plan 22-09). A DOM-shape assertion would pin the styling this phase is deliberately changing.
</verification>

<success_criteria>
- Every section that rendered before renders now, in the same order, with the same rows.
- The claims/refuted answer is in the first viewport and the funnel reads as proportion.
- Section headings are `13px/600` with real `h2` ids; E4 and E5 keep separate headings.
- The page states no percentage, no trend, no duplicates-removed figure and no corroboration claim.
- `VerificationReport` has exactly one mount in the app: plan 22-06's page.
</success_criteria>

<output>
Create `.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-07-SUMMARY.md` when done.

Append any out-of-scope discovery to
`.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md`
(created by plan 22-01 in wave 1 — append, never recreate).
</output>
