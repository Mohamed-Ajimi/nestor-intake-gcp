---
phase: 22-verification-report-as-a-page-citation-hygiene-the-verificat
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/intake/ResearchRunProgress.tsx
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
autonomous: true
requirements: [D-22-5]

must_haves:
  truths:
    - "The intake detail page no longer renders the research activity feed, stage list or verification report"
    - "The intake detail page still offers an 'Open run' link on every post-research status, and it still reaches the run page"
    - "No handler, import or constant on the intake page is left orphaned by the removal"
    - "frontend/src/components/intake/ResearchRunProgress.tsx still exists and still exports useActiveResearchRun"
  artifacts:
    - path: "frontend/src/components/intake/ResearchRunProgress.tsx"
      provides: "useActiveResearchRun (unchanged) plus a new link-only surface for the intake page"
      exports: ["useActiveResearchRun", "IntakeOpenRunLink"]
    - path: "frontend/src/routes/admin.pulse.intakes.$id.tsx"
      provides: "The intake detail page with the embedded feed removed and the Open run link kept"
  key_links:
    - from: "frontend/src/routes/admin.pulse.intakes.$id.tsx"
      to: "frontend/src/components/intake/ResearchRunProgress.tsx"
      via: "import of IntakeOpenRunLink only — never the ResearchRunProgress component"
      pattern: "IntakeOpenRunLink"
---

<objective>
Take the activity feed off the intake detail page without taking the way into the run page with it.

Purpose: D-22-5, operator verbatim — *"activity shouldnt show on the intake page , we already have a
open run button that opens it in a different page and it is exactly the same so no need to have it
there."* The feed goes; the link stays and becomes the single way in.

⚠ This REVERSES Phase 21's R2, which deliberately kept the embedded card (21-CONTEXT, explicitly out
of scope). The operator reversed it with the reversal stated in front of them. Recorded here so a
later reader does not read the removal as a Phase 21 regression.

Output: an intake detail page with no research feed and one working "Open run" link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-CONTEXT.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-RESEARCH.md

<measured_facts>
Measured on the tree at `658591b`. Do not re-derive.

1. ⛔ **THE "OPEN RUN" LINK LIVES INSIDE THE COMPONENT BEING REMOVED. This is the trap in this plan
   and 22-RESEARCH § 4c missed it.** `OpenRunLink` is defined at
   `ResearchRunProgress.tsx:214-225` and rendered from FOUR branches of that component
   (`:703`, `:777`, `:835`, `:929`). A repo-wide grep for `/admin/pulse/runs` across
   `frontend/src/**/*.tsx` returns exactly **three** non-generated hits:
   `ResearchRunProgress.tsx:218` (this link), `RunActions.tsx:192` (a post-retrigger `navigate`
   that only fires ON the run page), and the run route's own `createFileRoute`.
   ⇒ **A naive element removal leaves the app with NO navigation into the run page at all** — only
   a bookmark. That directly contradicts D-22-5's own words: *"The 'Open run' link stays and is the
   single way in."* Research's "the removal strands no capability" finding is **wrong on this
   point**; its RunActions equivalence argument is circular, because you can no longer reach the
   page that hosts `RunActions`.

2. ⛔ **DO NOT DELETE `ResearchRunProgress.tsx`.** `frontend/src/routes/admin.pulse.runs.$runId.tsx`
   line 11 imports `useActiveResearchRun` from it, and that is the run page this phase is built
   around. Remove the ELEMENT from the intake page; keep the FILE. (Plan 22-06 renames that route
   file to `…$runId.index.tsx`; the import line is unchanged either way.)

3. **The dead-code inventory on the intake page**, each verified to have exactly one caller:

   | Item | Line | Only caller | Action |
   |---|---|---|---|
   | `<ResearchRunProgress …>` element | 1220-1226 | — | replace (see Task 2) |
   | `onRetryResearch` | 801 | 1223 | delete |
   | `onResumeResearch` | 817 | 1224 | delete |
   | `onCancelResearch` | 838 | 1225 | delete |
   | `import { ResearchRunProgress }` | 54 | — | replace with `IntakeOpenRunLink` |
   | `cancelResearch`, `resumeResearch` in the named import | 55 | the two handlers above | delete those two names only |
   | `triggerResearch` in that same import | 55 | `onStartAutoResearch` at 786 | **KEEP** |
   | `onStartAutoResearch` | 783 | `NextStepBanner` at 1250 | **KEEP** |

4. ⛔ **`RESEARCH_SURFACE_STATUSES` (line 174) SURVIVES.** 22-RESEARCH § 4b says to delete it; that
   was written on the assumption nothing would render under that gate afterwards. The kept
   "Open run" link uses the same gate, so the constant still has a caller. **A criterion asserting
   `RESEARCH_SURFACE_STATUSES` is gone would be unsatisfiable** — do not write one, and do not
   delete the constant.

5. **`useActiveResearchRun(intakeId)` is called INSIDE `ResearchRunProgress` (`:603`), never by the
   intake route.** `grep -c useActiveResearchRun frontend/src/routes/admin.pulse.intakes.$id.tsx`
   returns 0 today. It returns `{ run }` where `run.id` is the research run id
   (`ResearchRun.id`, `research.ts:250`).

6. **`bannerActiveRun` (`:261-272`, passed to `NextStepBanner` at `:1240`) comes from
   `useActiveSkillRun` — a SKILL run, not a research run. Completely unaffected.** Do not touch it.

7. **Frontend baselines at the base commit:** `npx tsc --noEmit` exit 0; `npx vitest run` 61/61;
   `node scripts/i18n-audit.mjs` `RESULT: PASS` exit 0 with 107 CHECK D advisories (advisories never
   fail). ⛔ `npm run lint` cannot exit 0 (DEF-21-01) — verify per file with
   `npm exec --prefix frontend -- eslint --config frontend/eslint.config.js <files>`.

8. **`research.openRun` already exists in all three locales** (line 540). This plan needs **no new
   locale key**.
</measured_facts>

<interfaces>
```tsx
// frontend/src/components/intake/ResearchRunProgress.tsx — the ONE new export

/**
 * The intake detail page's link-only research surface (D-22-5). Resolves the intake's latest
 * research run through the existing hook and renders the SAME `OpenRunLink` the four card
 * branches render. Renders nothing until a run id is known.
 */
export function IntakeOpenRunLink({ intakeId }: { intakeId: string }): JSX.Element | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A link-only research surface for the intake page</name>
  <files>frontend/src/components/intake/ResearchRunProgress.tsx</files>
  <read_first>
    - frontend/src/components/intake/ResearchRunProgress.tsx lines 130-230 (the `useActiveResearchRun` hook and the `OpenRunLink` definition with its docstring about being rendered in all four branches)
    - frontend/src/lib/api/research.ts lines 245-275 (the `ResearchRun` type — `id` is the run id)
    - frontend/src/routes/admin.pulse.runs.$runId.tsx line 11 (the import that makes this file undeletable)
  </read_first>
  <action>
Edit `frontend/src/components/intake/ResearchRunProgress.tsx` ADDITIVELY. Add exactly one new
exported component immediately below `OpenRunLink`:

```
export function IntakeOpenRunLink({ intakeId }: { intakeId: string })
```

It calls `useActiveResearchRun(intakeId)` and returns `<OpenRunLink runId={run.id} />` when
`run?.id` is a non-empty string, and `null` otherwise. Keep `OpenRunLink` itself non-exported — one
definition, two consumers inside this file.

Its docstring must record why it exists, in these terms: D-22-5 removes the embedded feed from the
intake detail page, but the operator's own ruling keeps the "Open run" link and makes it the single
way in. `OpenRunLink` is defined here and was rendered ONLY from this component's four card
branches, so removing the element without this wrapper would leave the app with no navigation into
the run page at all. The hook is the only client-side way to learn an intake's latest run id —
`Intake` (`lib/api/intakes.ts`) carries no research run field, and `locateResearchRun` goes the other
way (run id → intake id).

Also note in the docstring that this mounts ONE SSE connection, which is the same connection the
removed component already opened on that page — strictly less network than before, not more, and the
stream closes itself on a terminal run.

⛔ **Change NOTHING else in this file.** Do not delete the `ResearchRunProgress` component body, do
not touch `useActiveResearchRun`, do not touch the four `OpenRunLink` render sites, and do not
remove the pre-existing dead `export { triggerResearch }` at line 938. Those are logged as DEF-22-01
in the phase's `deferred-items.md` by plan 22-01; a large deletion here widens the blast radius for
no operator-visible gain (22-RESEARCH § Open Question 2).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `grep -c "export function IntakeOpenRunLink" frontend/src/components/intake/ResearchRunProgress.tsx` returns `1`.
    - `grep -c "export function useActiveResearchRun" frontend/src/components/intake/ResearchRunProgress.tsx` returns `1` — the run page's import still resolves.
    - `grep -c "function OpenRunLink" frontend/src/components/intake/ResearchRunProgress.tsx` returns `1` — one definition, not a copy.
    - `grep -c "<OpenRunLink" frontend/src/components/intake/ResearchRunProgress.tsx` returns `5` (the four existing card branches plus the new wrapper). It returns 4 before this task.
    - `git diff --stat frontend/src/components/intake/ResearchRunProgress.tsx` shows **insertions only, zero deletions**.
  </acceptance_criteria>
  <done>A link-only surface exists that the intake page can mount, reusing the single `OpenRunLink` definition, with the file and the run page's import untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the embedded feed from the intake detail page</name>
  <files>frontend/src/routes/admin.pulse.intakes.$id.tsx</files>
  <read_first>
    - frontend/src/routes/admin.pulse.intakes.$id.tsx lines 48-60 (the two import lines being edited)
    - frontend/src/routes/admin.pulse.intakes.$id.tsx lines 168-180 (`RESEARCH_SURFACE_STATUSES` and its comment — this constant SURVIVES)
    - frontend/src/routes/admin.pulse.intakes.$id.tsx lines 780-860 (the four handlers — three die, `onStartAutoResearch` lives)
    - frontend/src/routes/admin.pulse.intakes.$id.tsx lines 1205-1255 (the element being replaced, and the `NextStepBanner` mount directly below it that must keep `onStartAutoResearch`)
    - frontend/src/components/intake/ResearchRunProgress.tsx (the `IntakeOpenRunLink` written in Task 1)
  </read_first>
  <action>
Edit `frontend/src/routes/admin.pulse.intakes.$id.tsx` only.

**(a) Replace the element.** At lines 1220-1226, replace the `<ResearchRunProgress …>` element with
`<IntakeOpenRunLink intakeId={intake.id} />`, keeping the `intake.status &&
RESEARCH_SURFACE_STATUSES.has(intake.status)` guard exactly as it is. Replace the block's comment
with one recording D-22-5: the operator ruled the activity feed off this page because the run page
already shows the same thing; what stays is navigation to it, not the run body. Note that this
REVERSES Phase 21's R2, with the reversal stated in front of the operator, so a later reader does not
read it as a regression.

**(b) Delete the three now-dead handlers** — `onRetryResearch` (801), `onResumeResearch` (817),
`onCancelResearch` (838) — with their doc comments. Each had exactly one caller, and it was the
element removed in (a).

**(c) Fix the two import lines.** Line 54: import `IntakeOpenRunLink` instead of
`ResearchRunProgress` from the same module. Line 55: drop `cancelResearch` and `resumeResearch`;
**KEEP `triggerResearch`** — `onStartAutoResearch` still calls it at line 786.

**(d) Keep `RESEARCH_SURFACE_STATUSES`.** It still gates the link. Do not delete it. Update its
comment so it describes what it now gates (the Open run link, on every post-research status) rather
than the removed replay card.

⛔ Do not touch `bannerActiveRun`, `useActiveSkillRun`, `NextStepBanner`, or anything else on this
page. Do not add `useActiveResearchRun` to this file — the hook stays inside the component, which is
what keeps the route free of a research stream it does not own.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run &amp;&amp; node scripts/i18n-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npx vitest run` reports 0 failing and at least 61 passing.
    - `cd frontend && node scripts/i18n-audit.mjs` prints `RESULT: PASS` and exits 0.
    - `grep -c "ResearchRunProgress" frontend/src/routes/admin.pulse.intakes.$id.tsx` returns `0` — neither the import nor the element remains.
    - `grep -c "IntakeOpenRunLink" frontend/src/routes/admin.pulse.intakes.$id.tsx` returns `2` (the import and the one mount).
    - `grep -cE "onRetryResearch|onResumeResearch|onCancelResearch" frontend/src/routes/admin.pulse.intakes.$id.tsx` returns `0`.
    - `grep -c "triggerResearch" frontend/src/routes/admin.pulse.intakes.$id.tsx` returns `2` (the import and the call in `onStartAutoResearch`).
    - `grep -c "RESEARCH_SURFACE_STATUSES" frontend/src/routes/admin.pulse.intakes.$id.tsx` returns `2` — the declaration and the surviving guard. **It must NOT be 0.**
    - `grep -c "useActiveResearchRun" frontend/src/routes/admin.pulse.intakes.$id.tsx` returns `0`.
    - `frontend/src/components/intake/ResearchRunProgress.tsx` still exists on disk.
    - `npm exec --prefix frontend -- eslint --config frontend/eslint.config.js frontend/src/routes/admin.pulse.intakes.\$id.tsx frontend/src/components/intake/ResearchRunProgress.tsx` reports zero non-`prettier/prettier` rule violations introduced by this plan.
  </acceptance_criteria>
  <done>The intake page renders no research feed and no verification report, still offers "Open run" under the same status gate, and carries no orphaned handler, import or constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | The intake page keeps one SSE connection (`openResearchStream`) via `IntakeOpenRunLink`; every other research read moves to the run page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Denial of Service | a second long-lived SSE connection per intake page | accept | Net connections DECREASE: the removed component already opened exactly this one stream, and it still closes itself on a terminal run (`onTerminal → stream.close()`). No new transport is introduced. |
| T-22-10 | Elevation of Privilege | the run id reaching a URL | mitigate | `IntakeOpenRunLink` builds `to="/admin/pulse/runs/$runId"` from `run.id` returned by the SERVER's own stream for the resolved intake — no id is taken from user input, and the run page re-resolves its intake from the run id server-side (T-15.3-71 / TENANT-02). Unchanged posture. |
| T-22-11 | Information Disclosure | removing a surface that was the only place some evidence rendered | mitigate | The run page renders the same feed, status card and (after 22-06) the verification report. Task 2's criteria prove the link survives, and the UAT (plan 22-09) walks the intake→run path explicitly. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed.** `package.json` / `package-lock.json` unchanged. No slopcheck surface, no install checkpoint. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` → exit 0.
- `cd frontend && npx vitest run` → 0 failing.
- `cd frontend && node scripts/i18n-audit.mjs` → `RESULT: PASS`, exit 0.
- `git diff --name-only` lists exactly the two files in `files_modified`.
- ⛔ Do NOT run `npm run build` in this plan — `frontend/src/routeTree.gen.ts` is owned by plan 22-06
  and must not appear in this plan's diff. If a build is run for any reason,
  `git checkout -- frontend/src/routeTree.gen.ts` before committing.
</verification>

<success_criteria>
- The intake detail page shows no activity feed, no stage list and no verification report.
- It still shows "Open run" on `in_research`, `delivered` and `archived`, and that link still reaches
  `/admin/pulse/runs/:runId`.
- `ResearchRunProgress.tsx` still exists and still exports `useActiveResearchRun`.
- No orphaned handler, import or constant remains, and `RESEARCH_SURFACE_STATUSES` is still used.
</success_criteria>

<output>
Create `.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-04-SUMMARY.md` when done.

The SUMMARY must state explicitly that the "Open run" link was preserved and by what mechanism —
this is the one thing a later reader will want to check, because the naive reading of D-22-5 removes
it by accident.

⚠ This is a wave-1 plan. Do NOT create `deferred-items.md` — plan 22-01 owns its creation this wave.
Record any out-of-scope discovery in this SUMMARY instead.
</output>
