---
phase: 22-verification-report-as-a-page-citation-hygiene-the-verificat
plan: 06
type: execute
wave: 2
depends_on: ["22-03"]
files_modified:
  - frontend/src/routes/admin.pulse.runs.$runId.index.tsx
  - frontend/src/routes/admin.pulse.runs.$runId.verification.tsx
  - frontend/src/routeTree.gen.ts
autonomous: true
requirements: [D-22-1]

must_haves:
  truths:
    - "The verification report has its own URL: /admin/pulse/runs/:runId/verification"
    - "The run page still serves /admin/pulse/runs/:runId and still renders its card and feed"
    - "The run page offers navigation to the report, gated by canHaveVerificationReport, and no longer renders the report body"
    - "The report page learns its intake only from locateResearchRun — never from a query parameter"
    - "Neither route grew an Outlet, a useMatches workaround, or a second scroll container"
  artifacts:
    - path: "frontend/src/routes/admin.pulse.runs.$runId.verification.tsx"
      provides: "The verification report page: cold open, states, header, and the mounted report"
    - path: "frontend/src/routes/admin.pulse.runs.$runId.index.tsx"
      provides: "The run page, renamed to an index sibling, with a Link where the toggle was"
    - path: "frontend/src/routeTree.gen.ts"
      provides: "Both routes registered as leaves under AdminPulseRoute"
      contains: "verification"
  key_links:
    - from: "frontend/src/routes/admin.pulse.runs.$runId.index.tsx"
      to: "frontend/src/routes/admin.pulse.runs.$runId.verification.tsx"
      via: "Link to=\"/admin/pulse/runs/$runId/verification\""
      pattern: "runs/\\$runId/verification"
    - from: "frontend/src/routes/admin.pulse.runs.$runId.verification.tsx"
      to: "frontend/src/lib/research/verificationGate.ts"
      via: "NOT imported — see the gate decision in measured facts"
      pattern: "canHaveVerificationReport"
---

<objective>
Give the report its own page and leave navigation, not a report body, on the run page.

Purpose: D-22-1, operator verbatim — *"verification report should open its own page not a dropdown
(too long)"*. The complaint was LENGTH, not discoverability; they found the toggle fine. So this
plan changes where the document lives, and nothing about how it is reached.

Output: two sibling leaf routes, a regenerated route tree, and a run page whose verification
affordance is a link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-CONTEXT.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UI-SPEC.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-RESEARCH.md

<measured_facts>
Measured on the tree at `658591b`. Research proved the route facts by RUNNING the real
`@tanstack/router-generator` against a scratch copy of `src/routes/`. Do not re-derive them, and do
not write criteria that contradict them.

1. **Both files register as LEAVES — no parent, no `Outlet`, no scar.** With
   `admin.pulse.runs.$runId.index.tsx` and `admin.pulse.runs.$runId.verification.tsx` present and
   **no** `admin.pulse.runs.$runId.tsx`, the generator emits both with
   `parentRoute: typeof AdminPulseRoute`. `AdminPulseRoute` already renders `<ProductShell>` around
   its own outlet.
   ⛔ **Do NOT reproduce the `routes/intake.$id.tsx:41-49` `useMatches` + conditional-`Outlet`
   workaround.** That scar exists because a leaf became a layout route; this rename is precisely
   what prevents that. There is an existing precedent in this repo:
   `admin.pulse.intakes.index.tsx` / `.new.tsx` / `.$id.tsx` all exist with **no**
   `admin.pulse.intakes.tsx`, and all three register under `AdminPulseRoute`.

2. ⛔ **THE TWO EXISTING `to:` CALL SITES MUST NOT BE EDITED.** The generator emits three different
   path unions: `fullPath` and `id` gain a trailing slash for an index route, but **`to` keeps
   `'/admin/pulse/runs/$runId'` WITHOUT one**. So
   `frontend/src/components/intake/ResearchRunProgress.tsx:218` and
   `frontend/src/components/research/RunActions.tsx:192` stay valid untouched. **A criterion
   asserting those two lines changed would be UNSATISFIABLE.** Neither file is in this plan's
   `files_modified`, and `git diff --name-only` not listing them is the criterion instead.

3. **The generator rewrites the renamed file's OWN route id.** It changes
   `createFileRoute("/admin/pulse/runs/$runId")` to `createFileRoute("/admin/pulse/runs/$runId/")`.
   ⇒ **A generation step is MANDATORY.** Do not hand-edit `routeTree.gen.ts`.

4. **How to regenerate:** the generator runs via `@tanstack/router-plugin` on `vite dev` / `vite
   build`. There is no `tsr` script in `package.json`. **`cd frontend && npm run build` is the
   step** — measured at the base commit it exits 0 in ~25 s and leaves `routeTree.gen.ts` with a
   CONTENT diff of zero (the committed tree is in sync). After the rename it WILL produce a real
   content diff, and that diff must be committed.
   ⛔ Installs use `npm ci`, never `npm install`. `node_modules` is already present.

5. ⛔ **`locateResearchRun` returns NO run status, deliberately.** Its docstring
   (`lib/api/research.ts:487-489`): *"It returns exactly two ids and NO run state: a status here
   would be a second source of truth that can disagree with the SSE frame (D-05)."*
   ⇒ **THE VERIFICATION PAGE DOES NOT RE-DERIVE RUN STATUS AND DOES NOT CALL
   `canHaveVerificationReport`.** This is a deliberate planning decision, recorded here because it
   narrows 22-UI-SPEC §1.1:
   - D-22-1's own wording is that the Phase 21 rule *"still governs whether **that navigation** is
     offered"* — the navigation lives on the run page, which HAS the status, and where the rule
     already has exactly one call site. That call site is kept, unchanged.
   - The only other way for this page to learn a status is `useActiveResearchRun(intakeId)`, which
     streams the intake's **LATEST** run. For a historical run that is a DIFFERENT run, so gating
     run X's report on run Y's status would be wrong — and 22-UI-SPEC §1.2 bars an SSE connection on
     this page anyway.
   - A deep link to a run with no verdicts is therefore answered by the report's own data: the
     endpoint returns HTTP 200 with empty lists, which renders `verification.emptyReport`. That is
     honest and needs no status.
   ⇒ Consequently `verification.notAvailable` is NOT a key in this phase (plan 22-03 deliberately
   omits it). **Do not add it, and do not write a criterion asserting a status gate on this page.**

   **⚠ ACCEPTED RESIDUAL — DEF-22-02, added 2026-08-11 from plan-checker warning 4. Routed, not
   absorbed.** The analysis above verified the TRIBUNAL endpoint (`runs/api.py:968`) is gate-free by
   design — its own docstring says so and `test_status_gates.py` pins it. But the **intake-backend
   proxy carries its own separate gate**: `backend/app/api/research_routes.py` (~line 906) raises
   `HTTPException(404)` when `run.tribunal_run_id` is NULL.

   So for a run still `queued` and not yet acknowledged by the tribunal engine, a **bookmarked or
   hand-typed** verification URL 404s at the proxy before it ever reaches the gate-free endpoint.
   `getVerification`'s `ApiResult` does not distinguish failure reasons, so `VerificationReport.tsx`
   renders the generic `verification.loadError` ("could not be loaded, try again") rather than the
   "not available yet" state `22-UI-SPEC`'s States table specified for exactly this arrival.

   **Decision: accept the residual for this phase.** Reasons: it is unreachable through any
   UI-exposed path (the run-page link is gated, so it never offers this URL for a queued run); it is
   an honest 404-derived failure, not a wrong answer and not data leakage; and the retry button
   succeeds on its own once the run progresses. Adding `verification.notAvailable` would mean a new
   key across all three locales plus 404-discrimination in the API layer — real scope for a window
   measured in seconds.

   ⛔ **Do not silently "fix" this by adding the key.** If it should be closed, it is a follow-up,
   and the honest fix is 404-discrimination in `getVerification`, not a status gate on the page.

6. **The cold-open block to reuse VERBATIM** is `admin.pulse.runs.$runId.tsx:52-76` + `162-185`:
   `locateResearchRun(runId)` → `intakeId`, with `locating` / `locateFailed` states, an
   existence-hidden failure message (`research.runPage.notFound`) that does not distinguish
   "no such run" from "not yours", and NO intake id accepted from a query parameter
   (T-15.3-71 / TENANT-02).

7. ⛔ **The report page must NOT copy the run page's shell.** The run page is a fixed-height column
   with one internal scroll region (`h-[calc(100vh-7.75rem)]`) and a live footer ticker. The report
   page **scrolls naturally in the document flow** — no internal scroll container, no live region, no
   footer ticker, no SSE, no feed. That is what makes find-in-page, deep anchors and print work on a
   long document (22-UI-SPEC §1.2).

8. ⛔ **No `aria-live` region on the report page.** The run page owns the product's one live region
   (`RunStatusCard`, T-15.3-82); a second would double-announce.

9. **Locale keys are already in place** (plan 22-03, wave 1): `verification.backToRun`,
   `verification.retry`, `verification.emptyReport`, plus the stat/nav/hover keys. Existing keys this
   plan reuses: `verification.title`, `verification.viewAction`, `verification.loadError`,
   `research.runPage.breadcrumbProduct`, `research.runPage.breadcrumbIntakes`,
   `research.runPage.breadcrumbRun`, `research.runPage.metaRun`, `research.runPage.notFound`,
   `research.runPage.backToIntakes`. ⛔ **Leave `verification.hideAction` in all three locales** even
   though it becomes unused — CHECK A tests parity, not usage.

10. **Baselines:** `npx tsc --noEmit` exit 0; `npx vitest run` 61/61; `node scripts/i18n-audit.mjs`
    `RESULT: PASS` exit 0 with 107 CHECK D advisories (advisories never fail); `npm run build`
    exit 0. ⛔ `npm run lint` cannot exit 0 (DEF-21-01) — verify per file with
    `npm exec --prefix frontend -- eslint --config frontend/eslint.config.js <files>`.

11. **`VerificationReport`'s props today** are `{ intakeId, runId, onClose? }`. Mount it with
    `intakeId` and `runId` only. Its internal restyle is plan 22-07's job; between this plan and that
    one the page will briefly show the component's own bordered container and title beneath the page
    header. That intermediate duplication is expected and 22-07 removes it.
</measured_facts>

<interfaces>
```tsx
// The two route declarations. Let the GENERATOR settle the exact id strings — write them as below
// and then run `npm run build`, which will add the index route's trailing slash for you.

// frontend/src/routes/admin.pulse.runs.$runId.index.tsx
export const Route = createFileRoute("/admin/pulse/runs/$runId")({ component: ResearchRunPage });

// frontend/src/routes/admin.pulse.runs.$runId.verification.tsx
export const Route = createFileRoute("/admin/pulse/runs/$runId/verification")({
  component: VerificationReportPage,
});
```

```tsx
// Already exists; mount it with exactly these props.
<VerificationReport intakeId={intakeId} runId={runId} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the run route to an index sibling and add the verification route</name>
  <files>frontend/src/routes/admin.pulse.runs.$runId.index.tsx, frontend/src/routes/admin.pulse.runs.$runId.verification.tsx, frontend/src/routeTree.gen.ts</files>
  <read_first>
    - frontend/src/routes/admin.pulse.runs.$runId.tsx (the WHOLE file — the module header at 18-42 explaining the flat-route and resolve-never-accept decisions, the cold-open block at 52-76, and the two early returns at 162-185)
    - frontend/src/routes/intake.$id.tsx lines 30-52 (the `useMatches` + conditional-`Outlet` scar — read it so you can see what NOT to reproduce)
    - frontend/src/routes/admin.pulse.intakes.index.tsx (the in-repo precedent for an index sibling with no parent file)
    - frontend/src/routeTree.gen.ts (skim the `FileRoutesByTo` / `FileRoutesByFullPath` unions around the existing `/admin/pulse/runs/$runId` entry — do NOT hand-edit this file)
    - frontend/src/components/intake/VerificationReport.tsx lines 189-250 (the props contract and its self-owned loading/error state)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UI-SPEC.md § 1.1, § 1.2, § 1.5 and § States
  </read_first>
  <action>
**(a) Rename.** `git mv frontend/src/routes/admin.pulse.runs.$runId.tsx
frontend/src/routes/admin.pulse.runs.$runId.index.tsx`. Change **nothing inside the file** in this
task — the generator rewrites its `createFileRoute` id in step (c), and its content edits are Task 2.

**(b) Create `frontend/src/routes/admin.pulse.runs.$runId.verification.tsx`.** A new page component
`VerificationReportPage`:

- **Cold open, copied verbatim from the run page** (`:52-76`): `locateResearchRun(runId)` →
  `intakeId`, with `locating` / `locateFailed` state and a cancelled-cleanup flag. It accepts NO
  intake id from a query parameter and never will (T-15.3-71 / TENANT-02). Also fetch `getIntake`
  for `clientName`, exactly as the run page does — cosmetic only, a failure must not block the page.
- **`locating` →** a `Skeleton` header copied from the run page's cold open (`h-4 w-48`,
  `mt-3 h-8 w-96`, `mt-8 h-64 w-full`) so the two pages feel identical.
- **`locateFailed || !intakeId` →** the run page's existence-hidden block verbatim:
  `t("research.runPage.notFound")` plus the back link. The page must not distinguish "no such run"
  from "not yours".
- **Header (22-UI-SPEC §1.5).** Reuse the run page header's STRUCTURE so the two read as one product:
  - breadcrumb, mono `text-[10.5px] uppercase tracking-[0.1em] text-ink/50`:
    `Pulse · Intakes · {clientName} · Run · Verification`. The `Run` crumb is a
    `<Link to="/admin/pulse/runs/$runId" params={{ runId }}>`; `Verification` is the current,
    unlinked crumb — add it as a plain `<span>` reusing `t("verification.title")`.
  - `h1`: `t("verification.title")`, `font-serif text-xl font-semibold leading-tight text-ink`.
  - meta row, mono `text-[11px] text-ink/50`: `t("research.runPage.metaRun", { id: runId.slice(0,8) })`
    and the client name.
  - a **Back to run** `<Link to="/admin/pulse/runs/$runId" params={{ runId }}>` with lucide
    `ArrowLeft`, label `t("verification.backToRun")`.
  - the header block carries `border-l-4` with `style={{ borderLeftColor: "#FF2D87" }}` — the
    report's inherited identity mark, moved from the old inline container onto the page header.
- **Body:** `<VerificationReport intakeId={intakeId} runId={runId} />`.
- **Page shell:** a plain block in the document flow with `mx-auto max-w-4xl`. ⛔ **NO
  `h-[calc(100vh-…)]`, no `overflow-y-auto` container, no footer ticker, no `aria-live`, no SSE, no
  feed.**
- **Module header comment** recording, for the next editor: why this route is a leaf sibling and not
  a child (measured fact 1, including the `intake.$id.tsx` scar it avoids); that the intake id is
  RESOLVED and never accepted (T-15.3-71 / TENANT-02); that the page is superadmin-only BY PLACEMENT
  under `admin.pulse` and adds no verb, no parameter and no new caller; that it deliberately does
  NOT re-derive run status (measured fact 5, with the reason); and that it deliberately does not copy
  the run page's fixed-height single-scroll shell (measured fact 7, with the reason).

**(c) Regenerate the route tree.** `cd frontend && npm run build`. Then confirm the generator added
the trailing slash to the index file's own `createFileRoute` id and that both routes appear in
`routeTree.gen.ts`. **Commit the regenerated `routeTree.gen.ts`.** Do not hand-edit it.

⛔ Do NOT add an `<Outlet/>` to either file. Do NOT touch
`frontend/src/components/intake/ResearchRunProgress.tsx` or
`frontend/src/components/research/RunActions.tsx` — their `to:` literals stay valid unchanged
(measured fact 2).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run build &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0 (it exits 0 at the base commit and must stay there — this is the gate for the whole rename).
    - `cd frontend && npx vitest run` reports 0 failing and at least 61 passing.
    - `test ! -f frontend/src/routes/admin.pulse.runs.\$runId.tsx` succeeds — the old file is gone, which is what keeps both siblings leaves.
    - `test -f "frontend/src/routes/admin.pulse.runs.\$runId.index.tsx"` and `test -f "frontend/src/routes/admin.pulse.runs.\$runId.verification.tsx"` both succeed.
    - `grep -F -c '/admin/pulse/runs/$runId/verification' frontend/src/routeTree.gen.ts` returns a value ≥ 1.
    - `grep -F -c "'/admin/pulse/runs/\$runId'" frontend/src/routeTree.gen.ts` returns a value ≥ 1 — the un-slashed `to` form survives, which is why the two existing call sites need no edit.
    - `git diff --name-only` does **NOT** list `frontend/src/components/intake/ResearchRunProgress.tsx` or `frontend/src/components/research/RunActions.tsx`.
    - `grep -cE "Outlet|useMatches" "frontend/src/routes/admin.pulse.runs.\$runId.index.tsx" "frontend/src/routes/admin.pulse.runs.\$runId.verification.tsx"` returns `0` for both files.
    - `grep -cE "calc\(100vh|overflow-y-auto|aria-live" "frontend/src/routes/admin.pulse.runs.\$runId.verification.tsx"` returns `0`.
    - `grep -c "canHaveVerificationReport" "frontend/src/routes/admin.pulse.runs.\$runId.verification.tsx"` returns `0` — the gate stays on the run page (measured fact 5).
    - `grep -cE "search|useSearch|intakeId=" "frontend/src/routes/admin.pulse.runs.\$runId.verification.tsx"` shows no intake id read from a query parameter; `locateResearchRun` is the only source.
  </acceptance_criteria>
  <done>Both routes register as leaf siblings, the report has its own URL, the regenerated route tree is committed, and neither existing `to:` call site needed an edit.</done>
</task>

<task type="auto">
  <name>Task 2: The run page's toggle becomes a link</name>
  <files>frontend/src/routes/admin.pulse.runs.$runId.index.tsx</files>
  <read_first>
    - frontend/src/routes/admin.pulse.runs.$runId.index.tsx lines 296-350 (the `canHaveVerificationReport` guard, the toggle button and the mounted report, plus the three-decision comment above them)
    - frontend/src/lib/research/verificationGate.ts (the rule being kept — read its docstring so you keep the ONE call site)
    - frontend/src/locales/en/intake.json (confirm `verification.viewAction` exists; it does)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UI-SPEC.md § "Copywriting Contract" (the run-page CTA change)
  </read_first>
  <action>
Edit `frontend/src/routes/admin.pulse.runs.$runId.index.tsx` only.

**(a) Replace the toggle block.** Inside the existing `canHaveVerificationReport(status) && (…)`
guard — **keep that guard exactly as it is; it is the ONE call site of the rule and D-22-1 says it
still governs whether the navigation is offered** — replace the `<button>` and the conditionally
mounted `<VerificationReport>` with a single:

```
<Link to="/admin/pulse/runs/$runId/verification" params={{ runId }} …>
```

styled with the **same** bordered mono classes the button carried
(`inline-flex items-center gap-2 border border-ink/30 px-4 py-2 font-mono text-xs uppercase
tracking-wider text-ink hover:bg-ink/5`), labelled `t("verification.viewAction")` — the existing key,
so i18n CHECK B stays green with no locale edit — and carrying a lucide `ArrowRight` icon.

**(b) Remove what the toggle needed and the page no longer does:** the
`const [showVerification, setShowVerification] = useState(false)` declaration, and the
`import { VerificationReport } from "@/components/intake/VerificationReport"` line. `Link` is already
imported from `@tanstack/react-router`; add `ArrowRight` to the existing `lucide-react` import.

**(c) Update the block's comment.** Keep decisions 1 and 2 from the existing comment (it is a SIBLING
of the card and the feed and must not move inside `RunStatusCard`; the gate is
`canHaveVerificationReport` applied to the run's own status, not to the card's success branch, so
`failed` and `cancelled` keep their verdicts). Replace decision 3 and the lazy-mount paragraph with:
D-22-1 moved the report to its own route because the operator judged it too long for a dropdown, so
what remains here is NAVIGATION, not the report body; the CTA reuses `verification.viewAction`, and
`verification.hideAction` is now unused but stays in all three locales because i18n CHECK A tests key
parity, not usage.

⛔ Do NOT touch the header, the status line, the card, the feed, the audit drill-down, the footer
ticker or the fixed-height shell. ⛔ Do NOT add a second `canHaveVerificationReport` call anywhere.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run &amp;&amp; node scripts/i18n-audit.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `cd frontend && npx vitest run` reports 0 failing and at least 61 passing — including `src/lib/research/verificationGate.test.ts`'s 10 tests, which must be untouched.
    - `cd frontend && node scripts/i18n-audit.mjs` prints `RESULT: PASS` and exits 0.
    - `grep -c "canHaveVerificationReport" "frontend/src/routes/admin.pulse.runs.\$runId.index.tsx"` returns `2` — the import and exactly ONE call. It is 2 before this task and must stay 2.
    - `grep -c "showVerification" "frontend/src/routes/admin.pulse.runs.\$runId.index.tsx"` returns `0`.
    - `grep -c "VerificationReport" "frontend/src/routes/admin.pulse.runs.\$runId.index.tsx"` returns `0` — the component is no longer imported or mounted here.
    - `grep -F -c 'to="/admin/pulse/runs/$runId/verification"' "frontend/src/routes/admin.pulse.runs.\$runId.index.tsx"` returns `1`.
    - `grep -c "verification.viewAction" "frontend/src/routes/admin.pulse.runs.\$runId.index.tsx"` returns `1`, and `grep -c "verification.hideAction"` returns `0`.
    - `grep -c '"hideAction"' frontend/src/locales/en/intake.json` still returns `1` — the key survives in the locales.
    - `git diff --name-only` lists no file outside this plan's `files_modified`.
    - `npm exec --prefix frontend -- eslint --config frontend/eslint.config.js "frontend/src/routes/admin.pulse.runs.\$runId.index.tsx" "frontend/src/routes/admin.pulse.runs.\$runId.verification.tsx"` reports zero non-`prettier/prettier` rule violations introduced by this plan.
  </acceptance_criteria>
  <done>The run page offers a link to the report under the same, single availability rule, renders no report body, and every gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → page | `runId` is a route parameter supplied by whoever opens the link, including a bookmark or a guess. |
| page → API | Every read is the existing superadmin-gated, space-scoped proxy verb. |
| route placement | `admin.pulse` inherits the admin guard; the new route adds no verb, no parameter and no new caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-16 | Elevation of Privilege | a tenant hint smuggled into the verification page's URL | mitigate | The page resolves its intake ONLY from `locateResearchRun(runId)` and reads no query parameter (T-15.3-71 / TENANT-02). Pinned by the grep criterion on Task 1. |
| T-22-17 | Information Disclosure | a guessed `runId` revealing another tenant's run exists | mitigate | `locateResearchRun` is superadmin-gated and space-scoped and existence-hides as a 404; the page renders one message for "no such run" and "not yours" and cannot distinguish them. Copied verbatim from the run page. |
| T-22-18 | Denial of Service | the report page opening an SSE stream per visit | mitigate | The page opens NO stream. `useActiveResearchRun` is deliberately not called here — the gate decision in measured fact 5 is what removes the need. |
| T-22-19 | Spoofing | a client-facing route reaching the superadmin report | accept | Superadmin-only BY PLACEMENT under `admin.pulse`, plus `_superadmin_gate` on every verb server-side (`research_routes.py:880`). The route adds no new authorization surface. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed and no registry is contacted.** `npm run build` uses the committed lockfile's existing `node_modules`; installs are `npm ci`, never `npm install`. `package.json` / `package-lock.json` unchanged. No install checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run build` → exit 0, and `routeTree.gen.ts` carries both routes.
- `cd frontend && npx tsc --noEmit` → exit 0. This is the gate for the rename: a `to` value that no
  longer exists is a TYPE error, not a runtime surprise.
- `cd frontend && npx vitest run` → 0 failing.
- `cd frontend && node scripts/i18n-audit.mjs` → `RESULT: PASS`, exit 0.
- `git diff --name-only` lists exactly the three files in `files_modified` (with the rename shown as
  a delete + add or as a rename, depending on git's similarity detection — either is fine).
- ⛔ The runtime match (does `/admin/pulse/runs/abc` still render the run page?) is **not** provable by
  a gate. It is confirmed once in operator UAT (plan 22-09), which is where it belongs.
</verification>

<success_criteria>
- `/admin/pulse/runs/:runId/verification` exists and renders the report.
- `/admin/pulse/runs/:runId` still renders the run page — no `Outlet`, no `useMatches` workaround.
- The run page offers a link, gated by the single existing `canHaveVerificationReport` call.
- `ResearchRunProgress.tsx` and `RunActions.tsx` were not edited.
- The regenerated `routeTree.gen.ts` is committed.
</success_criteria>

<output>
Create `.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-06-SUMMARY.md` when done.

Record which route option was taken — the primary `$runId/verification` sibling, not the
`admin.pulse.verification.$runId` fallback — and state that no `to:` call site required an edit, so a
later reader does not go looking for one.
</output>
