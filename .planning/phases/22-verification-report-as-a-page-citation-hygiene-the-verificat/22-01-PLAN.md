---
phase: 22-verification-report-as-a-page-citation-hygiene-the-verificat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/citations/dedupe.py
  - tribunal/nestor_pulse_sdk/tests/test_citation_dedupe.py
  - tribunal/cloudbuild.test-engine.yaml
  - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md
autonomous: true
requirements: [D-22-4]

must_haves:
  truths:
    - "Two citation entries whose URLs normalize to the same key collapse to ONE entry"
    - "The surviving entry keeps the ORIGINAL n it was assigned by number_citations — no number is ever reassigned"
    - "A claim whose only source was absorbed still has its claim id reachable, via also_claim_ids on the survivor"
    - "A URL that cannot be parsed is never merged with another unparseable URL"
    - "normalize_source_url never raises, on any input, including non-strings"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/citations/dedupe.py"
      provides: "The ONE source-identity normalization shared by the read path now and the write path in the follow-up phase"
      exports: ["normalize_source_url", "collapse_citations_by_url"]
    - path: "tribunal/nestor_pulse_sdk/tests/test_citation_dedupe.py"
      provides: "Unit assertions over normalization and collapse, including the never-renumber rule"
    - path: "tribunal/cloudbuild.test-engine.yaml"
      provides: "The new test file inside the engine fast gate"
      contains: "test_citation_dedupe.py"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/citations/dedupe.py"
      to: "urllib.parse"
      via: "urlparse / parse_qsl / urlencode — never a regex"
      pattern: "from urllib.parse import"
---

<objective>
Build the ONE source-identity function, as a pure module with no DB and no I/O, and prove it.

Purpose: D-22-4 requires that read-time display dedupe and the eventual write-time INSERT key agree
about what "the same source" means. The only way two layers can agree is if they call the same
function. This plan builds that function and its collapse helper; plan 22-05 wires it into the read
path; the write path lands in its own phase (D-22-4 sequencing, 22-RESEARCH §2d).

Output: `citations/dedupe.py`, its test file, and the test file inside the engine fast gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-CONTEXT.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-RESEARCH.md

<measured_facts>
Measured on the tree at `658591b` during planning. Do not re-derive these; do not write a criterion
that contradicts them.

1. **The live write path already keys on the URL.** `persist_tribunal_claims` calls
   `_upsert_source(..., snapshot_text=url)` at `citations/extractor.py:1100`, so
   `content_hash = sha256(url)` on every real run. The defect D-22-4 names is therefore
   **raw URL vs normalized URL**, not "text vs URL". 22-CONTEXT.md records this as an AMENDMENT to
   its own original wording.

2. **`resolved_url` is load-bearing, not a refinement.** The dominant duplicate generator is
   Gemini `vertexaisearch.cloud.google.com` grounding redirects, where every citation of one page
   arrives as a different opaque token. Stripping `www.`, a trailing slash and UTM params collapses
   **none** of those. Only `resolved_url` can, and only where the best-effort HEAD resolution
   succeeded.
   ⛔ **Therefore NO criterion in this plan, and no claim in its SUMMARY, may assert a reduction in
   citation count, a percentage, or "duplicates eliminated".** That number depends on runtime data
   and is not knowable before a run. Assert MECHANISM only.

3. **NEVER RENUMBER — the highest-risk rule in this phase.** The verification report PAGE bakes zero
   `[n]`; every marker renders from `citation.n` at paint time. The **deliverable markdown is the
   opposite** — `apply_citation_anchors` (`pipeline/tribunal/pipeline.py:4533`) bakes `[n]` at
   synthesis and freezes it. Dedupe cannot desynchronise the page from itself, but it CAN
   desynchronise the page from the frozen deliverable, and **only if it renumbers**.
   ⇒ Keep every survivor's original `n`. The emitted list goes **sparse — 1, 2, 4, 7, …** and that
   sparseness is **CORRECT**, not a defect to tidy. Say so in a code comment; a future editor will
   try to close the gaps.

4. **The dedupe must NOT live inside `number_citations`.** `tests/test_citation_numbering.py` pins
   CONTIGUOUS `1..N` (`assert ns == list(range(1, len(first) + 1))`) and an EXACT 10-key entry shape
   (`{n, source_id, title, url, provider, publication_date, quality_tier, single_source,
   first_claim_id, first_claim_position}`). Dropping entries or adding a key there goes RED.
   **`citations/numbering.py` is not edited by this plan at all.**

5. **`source` carries the two columns the normalizer needs.** `db/models/source.py`:
   `resolved_url: str | None` and `resolution_status: str | None`
   (`NULL` = never attempted | `'resolved'` | `'unresolved'`).

6. **Suite hygiene constraints on a new test file** (`tests/test_suite_hygiene.py`, in the fast
   gate): no name may be bound twice in one namespace (module level or inside one class), the
   `ast`-lift harness is banned, and the module-count floor is 80 (98 files on disk — adding one is
   safe).

7. **Engine fast gate today:** `tribunal/cloudbuild.test-engine.yaml` names **44** files in `WANTED`
   and asserts `EXPECTED_FILES=44`. Both must move to **45** together — the config's own comment
   says "Change this number and the list above TOGETHER."
</measured_facts>

<interfaces>
The exact signatures downstream plans (22-05, and the write-side follow-up phase) will import.
Do not change them without changing 22-05.

```python
# tribunal/nestor_pulse_sdk/citations/dedupe.py

def normalize_source_url(
    url: str | None,
    resolved_url: str | None = None,
    resolution_status: str | None = None,
) -> str | None: ...

def collapse_citations_by_url(
    numbered: list[dict[str, Any]],
    resolution: Mapping[str, tuple[str | None, str | None]] | None = None,
) -> list[dict[str, Any]]: ...
```

`resolution` maps `source_id` (str) -> `(resolved_url, resolution_status)`. Absent or incomplete is
fine: a source with no entry normalizes from its raw `url`.

The entry dicts fed to `collapse_citations_by_url` are `number_citations` entries — the 10-key shape
in measured fact 4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: normalize_source_url — the one identity key</name>
  <files>tribunal/nestor_pulse_sdk/citations/dedupe.py, tribunal/nestor_pulse_sdk/tests/test_citation_dedupe.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/citations/numbering.py (the module docstring and `_domain`'s `except Exception` idiom around line 82 — copy that never-raise posture)
    - tribunal/nestor_pulse_sdk/citations/redirect_resolver.py (lines 60-63 for the env knobs, 150-173 for `is_redirect_url` — read to understand when `resolved_url` exists; do NOT import from it)
    - tribunal/nestor_pulse_sdk/db/models/source.py (the `resolved_url` / `resolution_status` columns and their comment)
    - tribunal/nestor_pulse_sdk/tests/test_suite_hygiene.py (the duplicate-binding rule and the `ast`-lift ban this new test file must not trip)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-RESEARCH.md § "1b. Exact URL normalization specification"
  </read_first>
  <behavior>
    - `normalize_source_url("https://www.example.com/a/", None, None)` == `normalize_source_url("http://example.com/a", None, None)` (scheme excluded, `www.` stripped, one trailing slash stripped)
    - `normalize_source_url(raw, "https://publisher.example/x", "resolved")` keys off the resolved URL, not `raw`
    - `normalize_source_url(raw, None, "unresolved")` keys off `raw`
    - `normalize_source_url(raw, "https://publisher.example/x", None)` keys off `raw` — the status gate is explicit
    - `?utm_source=x&b=2` and `?b=2` produce the same key; `?ref=abc` and `?` produce DIFFERENT keys
    - `?a=1&b=2` and `?b=2&a=1` produce the same key (survivors sorted)
    - `#section` is dropped; `https://EXAMPLE.com:443/a` == `https://example.com/a`; `/A` and `/a` stay DIFFERENT
    - `None`, `""`, `"   "`, `123`, `object()` all return `None` and none of them raises
  </behavior>
  <action>
Create `tribunal/nestor_pulse_sdk/citations/dedupe.py` — a NEW pure module. It imports only from
`urllib.parse` and `typing`. It must not import from `numbering.py`, `extractor.py`,
`redirect_resolver.py`, sqlalchemy, or anything that touches a DB or the network.

Module docstring must state, in these terms: this is the ONE source-identity key; it is used by the
read path now (`verification/report.py`, plan 22-05) and by the INSERT conflict key in the follow-up
phase; two layers that normalize differently reintroduce the exact defect D-22-4 exists to fix, one
level down.

Implement `normalize_source_url(url, resolved_url=None, resolution_status=None) -> str | None` in
this exact order:

1. **Pick the input.** Use `resolved_url` when `resolution_status == "resolved"` AND `resolved_url`
   is a non-empty string; otherwise use `url`. Gate on the status explicitly — `'unresolved'` means
   resolution was attempted and failed, and the explicit gate survives a future partial write.
2. **Guard.** A non-string, an empty/whitespace-only string, or any `urlparse` failure returns
   `None`. **Never raise** — this parses remote-host-supplied strings (`resolved_url` comes from a
   `Location` header, `extractor.py:109-115`), and read-path code that raises takes down a report
   the operator has already paid for. Wrap the parse in `except Exception` the way
   `numbering.py::_domain` does.
3. **Trim** with `.strip()`.
4. **Scheme:** lowercase it, then **drop it from the key entirely** — `http://` and `https://` of the
   same page are ONE source (22-CONTEXT.md records this as an orchestrator decision that widens
   D-22-4's literal wording, made openly rather than silently).
5. **Host:** lowercase; strip a leading `www.`; drop a default port (`:80` on http, `:443` on https).
6. **Fragment:** drop entirely — a fragment addresses a position within one document.
7. **Query:** drop every parameter whose name is in the tracking set below; sort the survivors by
   `(key, value)` and re-encode with `urlencode`. Use `parse_qsl(..., keep_blank_values=True)` so a
   blank-valued parameter is not silently lost.
8. **Path:** strip exactly ONE trailing `/` unless the path is exactly `/`, in which case use `""`.
   **Do NOT lowercase the path** — paths are case-sensitive on most origins and lowercasing merges
   genuinely different documents.
9. **Assemble:** `f"{host}{path}"`, plus `f"?{query}"` when the query is non-empty.

The tracking set is a CLOSED list, written as a module-level frozenset. Exactly these names:
`utm_source`, `utm_medium`, `utm_campaign`, `utm_term`, `utm_content`, `utm_id`, `utm_name`,
`utm_reader`, `gclid`, `gbraid`, `wbraid`, `dclid`, `msclkid`, `fbclid`, `yclid`, `twclid`,
`ttclid`, `igshid`, `mc_cid`, `mc_eid`, `_hsenc`, `_hsmi`, `vero_id`, `vero_conv`, `ref_src`,
`ref_url`, `spm`, `scm`.

⚠ `ref` is DELIBERATELY NOT in that set, and the code must carry a comment saying why: `ref` is a
real, meaningful parameter on git hosts, docs sites and APIs, and stripping it would merge distinct
documents — the opposite failure from the one being fixed, and a worse one. `ref_src` / `ref_url`
(Twitter) are unambiguous and ARE stripped.

Write a "Do NOT do any of these" comment block naming each merge-hazard the function deliberately
refuses: lowercase the path, strip `index.html` / `default.aspx`, normalize percent-encoding, strip
ALL query parameters, resolve relative or `..` segments.

Then create `tribunal/nestor_pulse_sdk/tests/test_citation_dedupe.py` covering every case in
`<behavior>` above, one named test per property (not one table-driven loop) — a future edit that
breaks `ref` preservation must fail a test whose NAME says what was lost. Bind no name twice at
module level or inside one class, and do not use the `ast`-lift harness (both are pinned by
`test_suite_hygiene.py`).
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; C:/Users/ajimimo/AppData/Local/Temp/claude/C--Users-ajimimo-Desktop-MOELD-nestor-intake-gcp/04a721f7-4d76-4e8a-8a4b-0403693a545e/scratchpad/venv21/Scripts/python.exe -m pytest nestor_pulse_sdk/tests/test_citation_dedupe.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd tribunal && <venv21 python> -m pytest nestor_pulse_sdk/tests/test_citation_dedupe.py -q` reports **0 failures and 0 errors**, with at least 8 tests passing.
    - `grep -c "^ref\b\|'ref'\|\"ref\"" tribunal/nestor_pulse_sdk/citations/dedupe.py` — the bare name `ref` does NOT appear as a member of the tracking frozenset. Verify by reading the frozenset literal: it contains `ref_src` and `ref_url` and no bare `ref`.
    - The test file contains a test asserting `normalize_source_url("https://example.com/a?ref=abc")` differs from `normalize_source_url("https://example.com/a")`.
    - `grep -nE "^from |^import " tribunal/nestor_pulse_sdk/citations/dedupe.py` lists ONLY `__future__`, `urllib.parse` and `typing` imports — no sqlalchemy, no `nestor_pulse_sdk.*`.
    - `git diff --name-only` does NOT list `tribunal/nestor_pulse_sdk/citations/numbering.py`.
  </acceptance_criteria>
  <done>`normalize_source_url` exists as a pure, total function; every normalization property in `<behavior>` is pinned by a named test; `numbering.py` is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: collapse_citations_by_url — one number per source, and NEVER a new number</name>
  <files>tribunal/nestor_pulse_sdk/citations/dedupe.py, tribunal/nestor_pulse_sdk/tests/test_citation_dedupe.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/citations/numbering.py (`number_citations`' docstring at lines 264-295 — the EXACT entry shape being fed in, and the first-appearance ordering guarantee)
    - tribunal/nestor_pulse_sdk/tests/test_citation_numbering.py (the two pinned assertions: contiguous 1..N, and the exact 10-key entry set — read them so you can see why this function lives DOWNSTREAM of them and must not be folded in)
    - tribunal/nestor_pulse_sdk/citations/dedupe.py (as written in Task 1)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-RESEARCH.md § "Pattern 2" and § "Pattern 3"
  </read_first>
  <behavior>
    - Two entries with the same normalized URL collapse to ONE entry, and the survivor is the one with the LOWER `n`
    - The survivor's `n` is UNCHANGED — for input `n = 1, 2, 3` where 2 and 3 both duplicate 1, the output is a single entry with `n == 1`
    - For input `n = 1, 2, 3, 4` where 3 duplicates 2, the output `n` values are exactly `[1, 2, 4]` — SPARSE, with 3 absent, and nothing renumbered
    - The survivor carries `also_claim_ids` listing the `first_claim_id` of every entry it absorbed, with no duplicates and without its own `first_claim_id`
    - An entry whose URL normalizes to `None` is kept as-is and is NEVER merged with another such entry
    - `resolution` entries flip which key an entry gets: two different redirect tokens that both resolve to one publisher URL collapse; two that do not resolve stay separate
    - Called twice on the same input, the output is byte-identical
    - `collapse_citations_by_url([])`, `collapse_citations_by_url(None)` and a call with no `resolution` argument all return without raising
  </behavior>
  <action>
Add `collapse_citations_by_url(numbered, resolution=None)` to
`tribunal/nestor_pulse_sdk/citations/dedupe.py`, matching the `<interfaces>` signature exactly.

Algorithm — walk `numbered` IN ORDER, once:

- For each entry, read `source_id`, look up `(resolved_url, resolution_status)` in `resolution`
  (default `(None, None)`), and compute `key = normalize_source_url(entry.get("url"),
  resolved_url, status)`.
- `key is None` → append the entry to the output **unchanged** and continue. An unparseable URL is
  not evidence that two rows are the same source; merging on "both failed to parse" would collapse
  unrelated citations — the opposite defect, and a worse one.
- First time a key is seen → **copy** the entry (`dict(entry)`), set `also_claim_ids = []` on the
  copy, record it as that key's canonical, and append it to the output. Copy rather than mutate: the
  caller's list must not change under it.
- A repeat key → do NOT append. Take the duplicate's `first_claim_id` and, when it is truthy and not
  already the canonical's own `first_claim_id` and not already present, append it to the canonical's
  `also_claim_ids`.

Because the input arrives in `_CLAIM_SOURCE_SQL`'s pinned first-appearance order, "first key seen"
IS "lowest `n`" — deterministic without a sort. Say that in the docstring.

⛔ **THE FUNCTION ASSIGNS NO NUMBERS.** There is no `enumerate(..., start=1)`, no `n = i + 1`, and
no write to any entry's `n` anywhere in this module. Write a docstring paragraph stating why, in
these terms: the deliverable markdown's `[n]` markers were baked at synthesis by
`apply_citation_anchors` and can no longer be updated, so renumbering here would make `[7]` on the
verification page a different source from `[7]` in the report the operator downloaded. **The
displayed list going sparse (1, 2, 4, 7) is the intended, honest cost — a future editor WILL try to
close those gaps, and this comment is what stops them.**

Do NOT emit a `duplicate_count` field or any other yield figure. 22-UI-SPEC §1.6 bars an
"N duplicates removed" reading from the page, and a field that exists is a field somebody will
render.

The function is PURE: no DB, no I/O, and it never raises.

Extend `tests/test_citation_dedupe.py` with one named test per property in `<behavior>`. The
sparse-numbering test is the load-bearing one: build a 4-entry fixture where entry 3 duplicates
entry 2 and assert the output `n` list is exactly `[1, 2, 4]`.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; C:/Users/ajimimo/AppData/Local/Temp/claude/C--Users-ajimimo-Desktop-MOELD-nestor-intake-gcp/04a721f7-4d76-4e8a-8a4b-0403693a545e/scratchpad/venv21/Scripts/python.exe -m pytest nestor_pulse_sdk/tests/test_citation_dedupe.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd tribunal && <venv21 python> -m pytest nestor_pulse_sdk/tests/test_citation_dedupe.py -q` reports **0 failures and 0 errors**, with at least 16 tests passing in total across Tasks 1 and 2.
    - `grep -nE "enumerate\(|\bn\b *= *[a-z_]* *\+ *1|\[.n.\] *=" tribunal/nestor_pulse_sdk/citations/dedupe.py` returns NO match — nothing in the module assigns an `n`.
    - The test file contains an assertion whose expected value is the literal list `[1, 2, 4]` (the sparse-numbering proof).
    - `grep -n "duplicate_count" tribunal/nestor_pulse_sdk/citations/dedupe.py` returns no match.
    - `git diff --name-only` does NOT list `tribunal/nestor_pulse_sdk/citations/numbering.py`.
  </acceptance_criteria>
  <done>Collapse keeps one entry per normalized URL, preserves every survivor's original number, carries the absorbed claim ids forward, and refuses to merge unparseable URLs — all pinned by named tests.</done>
</task>

<task type="auto">
  <name>Task 3: Put the new test file inside the engine fast gate</name>
  <files>tribunal/cloudbuild.test-engine.yaml, .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md</files>
  <read_first>
    - tribunal/cloudbuild.test-engine.yaml (lines 480-560 — the `WANTED` list, the `EXPECTED_FILES=44` assertion and its "Change this number and the list above TOGETHER" comment)
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/deferred-items.md (the house format for a deferred item — copy its structure)
  </read_first>
  <action>
(a) In `tribunal/cloudbuild.test-engine.yaml`, add exactly one line to the `WANTED` string:

    nestor_pulse_sdk/tests/test_citation_dedupe.py

Keep the existing indentation and the trailing ` \` continuation style; the LAST entry in the list
carries no continuation, so if you append at the end you must move the `"` accordingly. Appending
immediately before `test_suite_hygiene.py` is the lower-risk edit.

Then change `EXPECTED_FILES=44` to `EXPECTED_FILES=45`. **Both edits in the same commit** — the
config's own comment says the number and the list change together, and a mismatch fails the gate
loudly by design.

Change nothing else in that file. Do not touch the pytest invocation, the timeout, or any other
cloudbuild config.

(b) Create
`.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md`
with a short header explaining that executors append out-of-scope discoveries here, and seed it with
one item recorded during planning:

**DEF-22-01 — `ResearchRunProgress`'s component body becomes unrendered but stays compiled.**
After D-22-5 (plan 22-04) the `ResearchRunProgress` component has zero render sites, while the FILE
must survive because `admin.pulse.runs.$runId.index.tsx` imports `useActiveResearchRun` from it.
22-RESEARCH § Open Question 2 recommends leaving the ~350-line body in place: D-22-5's ask is
"don't show it on the intake page", which the element removal satisfies in full, and a large
deletion in the same phase widens the blast radius for no operator-visible gain. Also record that
`export { triggerResearch }` at `ResearchRunProgress.tsx:938` has zero importers and is
pre-existing dead code, and that `AuditBodyPanel.tsx:45`'s comment "imported only from
ResearchRunProgress" is already stale.

⚠ **This file is created ONCE, by this plan, in wave 1.** Phase 21 produced an add/add merge
conflict because two same-wave plans each created their own copy. The other wave-1 plans (22-02,
22-03, 22-04) must record deferred discoveries in their own SUMMARY instead; plans in wave 2 and
later append here directly.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; grep -c "nestor_pulse_sdk/tests/test_" cloudbuild.test-engine.yaml &amp;&amp; grep -n "EXPECTED_FILES=" cloudbuild.test-engine.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nestor_pulse_sdk/tests/test_citation_dedupe.py" tribunal/cloudbuild.test-engine.yaml` returns `1`.
    - `grep -n "EXPECTED_FILES=" tribunal/cloudbuild.test-engine.yaml` shows `EXPECTED_FILES=45` and no other value.
    - The number of distinct `nestor_pulse_sdk/tests/test_*.py` paths inside the `WANTED` string is **45** (it was 44 before this task). Count them, do not estimate.
    - `.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md` exists and contains the string `DEF-22-01`.
    - `git diff --stat tribunal/cloudbuild.test-engine.yaml` shows changes on at most 3 lines.
  </acceptance_criteria>
  <done>The engine fast gate names 45 files and asserts 45, with the new dedupe test among them; the phase's deferred-items file exists with its single planning-time entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote host → engine | `resolved_url` originates in a `Location` header read by `redirect_resolver` (`extractor.py:109-115`). It reaches `normalize_source_url` as an attacker-influenceable string. |
| engine → operator screen | The collapsed list is what the verification page renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | `normalize_source_url` parsing a remote `Location` header | mitigate | Total function: non-string / empty / unparseable → `None`; the parse is wrapped in `except Exception`; the result is used ONLY as a dict key and is NEVER used to build a request. Pinned by the never-raises tests in Task 1. |
| T-22-02 | Denial of Service | a pathological URL (very long, deeply nested query) | accept | `urlparse` is linear and the input is already bounded by the `source.url` column write path. No regex backtracking exists in this module — the "don't hand-roll a regex" rule is what removes the catastrophic-backtracking class entirely. |
| T-22-03 | Information Disclosure | over-merging two genuinely different documents into one `[n]` | mitigate | Path case is preserved, `ref` is preserved, percent-encoding is untouched, unparseable URLs are never merged. Each is pinned by a named test. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | **No package is installed by this plan.** The module imports stdlib only, and its import list is an acceptance criterion. No slopcheck surface, so no install checkpoint. |
</threat_model>

<verification>
- `cd tribunal && <venv21 python> -m pytest nestor_pulse_sdk/tests/test_citation_dedupe.py -q` → 0 failures, 0 errors.
- `cd tribunal && <venv21 python> -m pytest nestor_pulse_sdk/tests/test_suite_hygiene.py -q` → 0 failures (the new test file must not trip the duplicate-binding or `ast`-lift rules).
- `cd tribunal && <venv21 python> -m pytest nestor_pulse_sdk/tests/test_citation_anchors.py nestor_pulse_sdk/tests/test_verification_report_endpoint.py -q` → 0 failures. `test_verification_report_endpoint.py` measured **9 passed** at `658591b` and must stay 9 passed: nothing in this plan is wired into the report yet.
- `git diff --name-only` lists no file outside this plan's `files_modified`.
</verification>

<success_criteria>
- One shared normalization function exists that the Python write path can import unchanged.
- Collapse is proven to keep one entry per normalized URL AND to leave every number exactly as
  `number_citations` assigned it.
- The sparse-number outcome is proven by a test asserting the literal `[1, 2, 4]`.
- `citations/numbering.py` is byte-identical to its state at the phase base commit.
- The engine fast gate runs 45 files and says so.
</success_criteria>

<output>
Create `.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-01-SUMMARY.md` when done.

⛔ The SUMMARY must NOT state or estimate how many duplicates this will remove. State the mechanism
and the measured test results only.
</output>
