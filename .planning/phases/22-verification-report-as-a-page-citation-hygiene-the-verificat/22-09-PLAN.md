---
phase: 22-verification-report-as-a-page-citation-hygiene-the-verificat
plan: 09
type: execute
wave: 5
depends_on: ["22-04", "22-05", "22-08"]
files_modified:
  - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UAT.md
  - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md
  - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/deferred-items.md
  - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md
autonomous: true
requirements: [D-22-1, D-22-2, D-22-3, D-22-4, D-22-5]

must_haves:
  truths:
    - "Phase 22 has an operator UAT document that can be walked on RECORDED data with zero spend"
    - "DEF-21-02's B2, B3 and B4 are recorded as SUPERSEDED by a deliberate design change, each with a named Phase 22 successor — not silently abandoned"
    - "DEF-21-02's B1, B5 and B6 have Phase 22 successors checking the same property against the new navigation"
    - "The UAT tells the operator that sparse citation numbers are CORRECT, and asks them to rule rather than assuming"
  artifacts:
    - path: ".planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UAT.md"
      provides: "The Phase 22 operator walkthrough, with per-check verdict slots and the DEF-21-02 successors"
      contains: "SUPERSEDES"
    - path: ".planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md"
      provides: "B2/B3/B4 annotated as superseded, pointing at their Phase 22 successors"
  key_links:
    - from: ".planning/phases/21-.../21-UAT.md"
      to: ".planning/phases/22-.../22-UAT.md"
      via: "an explicit supersession note naming each successor check"
      pattern: "22-UAT.md"
---

<objective>
Write the Phase 22 operator walkthrough, and close the loop on the three Phase 21 UAT steps this
phase's own design change made meaningless.

Purpose: D-22-1 obsoletes part of Phase 21's deferred UAT. `21-UAT.md` PART B (DEF-21-02) carries six
operator checks B1–B6 that were deferred by operator ruling and are still unfilled. **B2, B3 and B4
are about the toggle sitting between the status card and the activity feed, and the feed surviving
beneath the open report.** Once the report is its own page there is no feed for it to sit beside, so
those three are meaningless AS WRITTEN. B1, B5 and B6 survive in spirit and still need checking
against the new navigation.

⛔ **If this is not done explicitly, a deferral that was already sequenced once quietly becomes a
skip.** That is the exact failure mode `21-UAT.md`'s own PART B header was written to prevent.

Output: `22-UAT.md`, plus supersession annotations in both Phase 21 documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-CONTEXT.md
@.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UI-SPEC.md
@.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md

<measured_facts>
1. **`.planning/` is gitignored in this repo.** Commit these files with `git add -f`, and verify with
   `git diff --cached --name-only` that they actually staged.

2. **⛔ ZERO SPEND. DO NOT TRIGGER A RESEARCH RUN to complete this document.** There are nine run
   prefixes in the audit bucket already, and the deployed code has not been exercised by a run since
   2026-08-05. If a check cannot be answered without a new run, its verdict is **NOT-OBSERVABLE** —
   that is a legitimate answer and it is the correct one.

3. **The recording rules carried from Phase 21, and they are load-bearing:**
   (a) record the operator's answer VERBATIM, never paraphrased — a paraphrase of a UAT observation
   is how a defect becomes a rumour;
   (b) every deviation must be ROUTED — a follow-up task or an explicitly deferred item with the
   operator's agreement — **never absorbed silently**;
   (c) NOT-OBSERVABLE is a real verdict and must be used honestly;
   (d) **"looks good" is not a PASS on a check nobody named.**

4. **Known and OUT OF SCOPE — do not report these as new Phase 22 failures:** DEF-21-01 (`npm run
   lint` red tree-wide, operator ruled it stays deferred), DEF-21-03 (empty `coverage` stage summary),
   DEF-21-04 (`workshop` has body rows but no divider). All three are Phase 21 items, ruled, and
   deliberately not carried into Phase 22.

5. **Two things this UAT must ASK rather than assume:**
   - **The sparse `[n]` numbers.** After dedupe the list reads `1, 2, 4, 7, …`. That is CORRECT: each
     survivor keeps the number `number_citations` assigned it, because the deliverable markdown's
     `[n]` markers were frozen at synthesis and cannot be updated. Renumbering would make `[7]` on
     the page a different source from `[7]` in the downloaded report. The UAT must say this plainly
     and ask whether the operator wants an on-screen explanation — 22-RESEARCH recommends shipping
     without one, because a note edges toward the "N duplicates removed" claim D-22-4 forbids.
   - **The hover card's fourth line** (`verification.hoverClickHint`, "Click to open the stored
     snapshot"). 22-UI-SPEC §2.2 flags it as the ONE addition beyond the operator's ruled three
     fields and explicitly offers it for the operator to strike. It is one JSX element and one
     locale key; nothing depends on it.

6. **⛔ Do NOT put a duplicate-reduction figure anywhere in this document, and do not ask the operator
   to count duplicates as a pass criterion.** Only redirects that resolved to a publisher URL can
   merge, and that ceiling is not knowable before a run. The observable property is
   **"no two list entries point at the same source"**, never "N fewer than before".

7. **What Phase 22 shipped, for the checks to be written against:**
   - D-22-1: the report lives at `/admin/pulse/runs/:runId/verification`; the run page offers a
     `View verification report` LINK in the same position the toggle occupied, still gated by
     `canHaveVerificationReport`; the run route is now `…$runId.index.tsx` and `/admin/pulse/runs/:runId`
     still serves the run page.
   - D-22-2: stat strip (6 tiles) + proportional gate funnel above the fold, then the SAME nine
     sections in the SAME order as an instrumented document with a sticky section nav rail.
   - D-22-3: hover on a `[n]` shows title + retrieved date + quality tier (+ the strike-candidate
     click hint) and makes NO network call; the citation list is collapsed by default with its source
     count visible; clicking opens the panel in a right-hand sheet.
   - D-22-4: one entry per normalized source URL, numbers preserved (sparse).
   - D-22-5: the intake detail page renders no activity feed; the "Open run" link stays.
   - Citation hygiene: `Published:` became `Retrieved:` on every surface, in all three locales.
</measured_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 22-UAT.md</name>
  <files>.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UAT.md</files>
  <read_first>
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md (the WHOLE file — copy its structure, its recording rules, its "KNOWN AND OUT OF SCOPE" table idiom and its verdict-slot format; and read PART B in full, because its six steps are what you are succeeding)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-CONTEXT.md (D-22-1 … D-22-5, verbatim operator quotes — quote them in the document rather than paraphrasing)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UI-SPEC.md § 2.2 (the strike-candidate hover line) and § States
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/deferred-items.md (DEF-21-01/03/04 — the three items the UAT must list as out of scope)
  </read_first>
  <action>
Create
`.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UAT.md`,
following `21-UAT.md`'s structure and its recording rules verbatim.

**Header block:** status `AWAITING OPERATOR`; the zero-spend rule (measured fact 2) as a ⛔ callout;
the four recording rules (measured fact 3); preconditions (Phase 22 merged and the derived deploy
surface deployed per plan 22-10; operator signed in as superadmin).

**A "⛔ KNOWN AND OUT OF SCOPE" table** listing DEF-21-01, DEF-21-03, DEF-21-04 and DEF-22-01, each
with what the operator will see and why it is not a Phase 22 failure.

**PART A — the five decisions**, one section each, with `Verdict` / `Observed (VERBATIM)` slots left
`_(awaiting operator)_`:

- **A1 — D-22-1, the report has its own page.** Open a run with a report from the intake page's
  "Open run" link. Confirm: the run page shows a `View verification report` link where the toggle
  used to be; clicking it changes the URL to `/admin/pulse/runs/<id>/verification`; the report
  renders there; the run page itself no longer renders the report body; `Back to run` returns; and a
  browser reload of the verification URL still works (it is a real route, not a modal). Blocker: yes.
- **A2 — D-22-2, the restyle.** Quote the operator verbatim. Confirm the stat strip and the funnel
  are visible without scrolling; that the nav rail lists every section that has rows and jumps to it;
  and — the load-bearing half — that **nothing was dropped**: funnel, refuted, support, insufficient,
  superseded verdicts, superseded/scoped, reconciled, unverified accounting, citations, true cost are
  ALL still present, in that order. Then ask the open question plainly: *does it read better?*
  Blocker: yes on "nothing dropped"; the aesthetic judgement is recorded, not blocking.
- **A3 — D-22-3, hover and collapse.** Confirm: hovering a `[n]` shows a card with the title, a date
  labelled **Retrieved** (never "Published") and a quality tier, **and nothing else** — no URL, no
  provider, no snapshot; the card appears without a page flicker or a spinner (it makes no request);
  clicking the marker closes the card and opens the citation panel in a right-hand sheet; Esc closes
  the sheet; **and a `[n]` clicked from a VERDICT ROW while the citation list is still collapsed
  opens the panel visibly** — this last one is the specific defect the phase was built to avoid, so
  give it its own verdict slot. Also confirm the citation list is closed on arrival and shows its
  source count, and that expanding it reveals every source with no inner scrollbar.
  **Then ask the strike question** (measured fact 5b): should the fourth hover line, *"Click to open
  the stored snapshot"*, stay or go? Record the ruling.
- **A4 — D-22-4, one number per source.** ⛔ **Do NOT ask for a count and do not compare to a
  previous reading.** The observable property is: **no two entries in the citation list point at the
  same source.** Then explain the sparse numbering in the document itself, in plain words — the list
  may read `1, 2, 4, 7`; a missing number means that source was a duplicate of an earlier one; the
  numbers are deliberately NOT renumbered because the downloaded report's `[n]` markers were written
  at synthesis and can no longer be changed, so renumbering would make the page and the report
  disagree about what `[7]` means. **Then ask the operator to rule** whether they want that explained
  on screen. Also confirm no verdict row that carried a `[n]` before has lost it.
  ⚠ State in the document that read-time dedupe changes DISPLAY only: cost and corroboration still
  count the duplicate rows until the write-side fix lands in its own phase.
- **A5 — D-22-5, the intake page.** Quote the operator verbatim. Confirm the intake detail page shows
  no activity feed and no stage list, **and that the "Open run" link is still there and still
  works** — call this out as its own slot, because the naive reading of D-22-5 removes it by accident.
  Blocker: yes on both halves.

**PART B — the DEF-21-02 reconciliation.** Open with a ⚠ block explaining that Phase 21's six
deferred steps are being CLOSED here: three carried forward and three superseded by a deliberate
design change, with the operator's own D-22-1 ruling as the reason. Then a mapping table:

| Phase 21 step | Status | Phase 22 successor |
|---|---|---|
| B1 — run page loads via "Open run" | **CARRIED FORWARD** | 22-B1 |
| B2 — toggle sits BETWEEN the status card and the feed | **SUPERSEDED by D-22-1** | 22-B2 |
| B3 — report opens AND the feed survives beneath it | **SUPERSEDED by D-22-1** | 22-B3 |
| B4 — toggle collapses cleanly, feed untouched | **SUPERSEDED by D-22-1** | 22-B4 |
| B5 — offered on failed / cancelled runs (D-11) | **CARRIED FORWARD** | 22-B5 |
| B6 — NO affordance on a queued / running run | **CARRIED FORWARD** | 22-B6 |

Then the six successor checks with their own verdict slots:
- **22-B1** — from the intake list, open an intake with a research run, click "Open run": the run page
  at `/admin/pulse/runs/:runId` loads. Blocker: yes.
- **22-B2** *(supersedes B2)* — the `View verification report` LINK sits between the status card and
  the activity feed. **What changed and why:** B2 asked about a TOGGLE in that position; the position
  is unchanged, the control is now a link because the report moved off the page. Blocker: yes.
- **22-B3** *(supersedes B3)* — clicking the link NAVIGATES to the report page, where funnel, verdict
  sections and cost block all render. **What changed and why:** B3's real subject was "the report must
  not replace, hide or unmount the feed". That question **cannot arise any more** — the report is a
  separate route, so the feed is not on the same page to be lost. The equivalent property is that
  the run page is intact when you come back, which is 22-B4. Blocker: yes.
- **22-B4** *(supersedes B4)* — `Back to run` returns to the run page with the status card and the
  full activity feed intact, and the elapsed clock NOT restarted. **What changed and why:** B4 asked
  whether collapsing the toggle left the feed untouched; the equivalent for a page is whether
  navigating back leaves the run page whole. Blocker: yes.
- **22-B5** *(from B5)* — on a **failed** or **cancelled** run, the link IS offered (D-11: those are
  the two states whose evidence matters most). ⚠ **NOT a blocker if no such run exists** — record
  "none available" and move on. Do not manufacture one and above all do not trigger a run.
- **22-B6** *(from B6)* — on a **queued** or **running** run, **NO link appears.** ⚠ **Its ABSENCE is
  the CORRECT result.** A link that IS present here is the failure. Blocker: yes, in that direction.

**Regression checks:** R1 — the run page's elapsed clock does not restart on close/reopen (15.3
D-01/D-09). R2 — the run page's activity feed, "Show more" toggles and audit drill-down still work
(this phase renamed that route file). R3 — no screen anywhere reads `Published:` for a citation date.

**Roll-up table** with a row per check and an `Operator sign-off` line.

Every verdict cell is left `_(awaiting operator)_`. ⛔ **Fill in NOTHING from inference.**
  </action>
  <verify>
    <automated>test -f ".planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UAT.md" &amp;&amp; grep -c "awaiting operator" ".planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UAT.md"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/22-…/22-UAT.md` exists.
    - `grep -c "SUPERSEDED" .planning/phases/22-…/22-UAT.md` returns at least `3` — one per superseded Phase 21 step.
    - `grep -cE "22-B1|22-B2|22-B3|22-B4|22-B5|22-B6" .planning/phases/22-…/22-UAT.md` returns at least `12` (each id appears in the mapping table and again at its own check).
    - `grep -c "awaiting operator" .planning/phases/22-…/22-UAT.md` returns at least `20` — every verdict slot is unfilled.
    - `grep -ciE "duplicates removed|N fewer|reduced by|[0-9]+% fewer" .planning/phases/22-…/22-UAT.md` returns `0` — no yield claim and no yield question.
    - `grep -c "Retrieved" .planning/phases/22-…/22-UAT.md` returns at least `1` — the label check is present.
    - `grep -cE "DEF-21-01|DEF-21-03|DEF-21-04|DEF-22-01" .planning/phases/22-…/22-UAT.md` returns at least `4` — all four out-of-scope items are listed.
    - `grep -ciE "do not trigger|zero spend|no spend" .planning/phases/22-…/22-UAT.md` returns at least `1`.
  </acceptance_criteria>
  <done>A walkable, zero-spend Phase 22 UAT exists with unfilled verdict slots, the six DEF-21-02 successors, and no question that asks the operator to count duplicates.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate the Phase 21 documents so the supersession is visible from where it happened</name>
  <files>.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md, .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/deferred-items.md, .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/deferred-items.md</files>
  <read_first>
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md PART B in full (lines 211-303 — the six steps, the "WHY THIS SECTION EXISTS" block and the six verdict slots you are annotating)
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/deferred-items.md § DEF-21-02 (lines 108-157 — the ruling, the obligation on plan 21-08, and the six steps repeated in full)
    - .planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-UAT.md (as written in Task 1 — you are pointing at its successor ids)
  </read_first>
  <action>
**(a) `21-UAT.md` PART B.** Insert a dated ⚠ block immediately under the "WHY THIS SECTION EXISTS"
header, and add a one-line annotation to each of the six verdict slots B1–B6.

The block must say, in these terms:
- **2026-08-11 — RECONCILED BY PHASE 22, not abandoned.** Phase 22's D-22-1 moved the verification
  report onto its own route by operator ruling (*"verification report should open its own page not a
  dropdown (too long)"*). **B2, B3 and B4 asked about a toggle sitting beside a feed and about the
  feed surviving beneath an open report. Neither situation exists any more**, so those three are
  UNANSWERABLE AS WRITTEN and are marked **SUPERSEDED**, each with a named Phase 22 successor.
- B1, B5 and B6 test properties that survive the redesign and are **CARRIED FORWARD** to Phase 22
  successors against the new navigation.
- **This is a reconciliation, not a skip.** The successors live in
  `.planning/phases/22-…/22-UAT.md` and carry the same blocker status. A verifier reading this file
  must follow the pointer rather than treat these six as unfilled.

Then annotate each slot:
- B1 → `**CARRIED FORWARD → 22-B1** (see 22-UAT.md). Not answered here.`
- B2 → `**SUPERSEDED by D-22-1 → 22-B2** (see 22-UAT.md). The toggle no longer exists; the link in the same position is checked there.`
- B3 → `**SUPERSEDED by D-22-1 → 22-B3** (see 22-UAT.md). The report is a separate page, so "the feed survives beneath it" cannot arise; the equivalent property is checked at 22-B4.`
- B4 → `**SUPERSEDED by D-22-1 → 22-B4** (see 22-UAT.md). "Back to run leaves the run page whole" replaces "the toggle collapses cleanly".`
- B5 → `**CARRIED FORWARD → 22-B5** (see 22-UAT.md). Still NOT a blocker if no failed/cancelled run exists.`
- B6 → `**CARRIED FORWARD → 22-B6** (see 22-UAT.md). The expected result is still the AFFORDANCE'S ABSENCE.`

⛔ **Do NOT fill in any Phase 21 verdict**, do not touch PART A, do not touch R1/R2, and do not alter
the operator's verbatim walkthrough quote at the top of the file. Annotating is the whole job.

**(b) `21-…/deferred-items.md` § DEF-21-02.** Append a dated closure section stating: DEF-21-02 is
**RECONCILED, 2026-08-11, by Phase 22 plan 22-09** — three steps carried forward, three superseded by
D-22-1, all six with named successors in `22-UAT.md`. Note that the obligation the item placed on
plan 21-08 is now discharged by pointer rather than by answering the six steps in their original
form, and that the reason is a deliberate operator-ruled design change, recorded so a later reader
does not read it as an abandoned deferral. **Change nothing about DEF-21-01, DEF-21-03 or
DEF-21-04.**

**(c) `22-…/deferred-items.md`.** Append a short cross-reference noting that DEF-21-02 was reconciled
here and pointing at `22-UAT.md`'s PART B, so the phase's own deferred-items file records the closure
too. Append — the file was created by plan 22-01; do not recreate it.

⚠ `.planning/` is gitignored: stage with `git add -f` and confirm with
`git diff --cached --name-only`.
  </action>
  <verify>
    <automated>grep -c "SUPERSEDED" .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md &amp;&amp; grep -c "RECONCILED" .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SUPERSEDED" .planning/phases/21-…/21-UAT.md` returns at least `4` (the header block plus the three annotated slots).
    - `grep -c "CARRIED FORWARD" .planning/phases/21-…/21-UAT.md` returns at least `3`.
    - `grep -c "22-UAT.md" .planning/phases/21-…/21-UAT.md` returns at least `7` — the block plus one pointer per step.
    - `grep -c "awaiting operator" .planning/phases/21-…/21-UAT.md` is **unchanged from its value before this task** — measure it first and state both numbers in the SUMMARY. No Phase 21 verdict may be filled in by this plan.
    - `grep -c "RECONCILED" .planning/phases/21-…/deferred-items.md` returns at least `1`, and `grep -c "DEF-21-01" .planning/phases/21-…/deferred-items.md` is unchanged.
    - `grep -c "DEF-21-02" .planning/phases/22-…/deferred-items.md` returns at least `1`.
    - `git diff --cached --name-only` (after `git add -f`) lists all four files in this plan's `files_modified`.
  </acceptance_criteria>
  <done>A reader arriving at Phase 21's PART B is sent to the named Phase 22 successor for every one of the six steps, and the deferred item records its own closure with the reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future verifier | These files are what a later agent or verifier reads to decide whether an obligation was met. A pointer that does not resolve is how a deferral becomes a skip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-28 | Repudiation | a deferred UAT step quietly disappearing | mitigate | Every one of the six steps gets an explicit status and a named successor id, in BOTH the Phase 21 UAT and the deferred-items file. Pinned by the pointer-count criteria. |
| T-22-29 | Tampering | a verdict filled in from inference rather than observation | mitigate | Every slot ships `_(awaiting operator)_`; the unchanged-count criterion on `21-UAT.md` makes an accidental fill-in visible in the diff. |
| T-22-30 | Repudiation | a UAT question that cannot be answered honestly | mitigate | No question asks for a duplicate count (the yield is not knowable before a run); NOT-OBSERVABLE is declared a legitimate verdict; the zero-spend rule is a ⛔ callout. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | **Documentation only. No package, no code, no registry.** |
</threat_model>

<verification>
- Both files exist and every criterion grep above returns its stated value.
- `git add -f` the four files and confirm with `git diff --cached --name-only` that they staged —
  `.planning/` is gitignored and a plain `git add` silently does nothing.
- ⛔ No research run is triggered by this plan, under any circumstance.
</verification>

<success_criteria>
- Phase 22 has a walkable, zero-spend UAT with unfilled verdict slots.
- All six DEF-21-02 steps have an explicit status and a named successor, visible from Phase 21.
- No Phase 21 verdict was filled in.
- Nothing in either document asks the operator to count duplicates or implies a yield.
</success_criteria>

<output>
Create `.planning/phases/22-verification-report-as-a-page-citation-hygiene-the-verificat/22-09-SUMMARY.md` when done.

State the before/after `awaiting operator` counts for `21-UAT.md` — they must be equal.
</output>
