---
phase: 15.2-research-engine-redesign-engine-core-inserted-2026-07-24
plan: 17
type: execute
wave: 11
depends_on:
  - "15.2-01"
  - "15.2-02"
  - "15.2-03"
  - "15.2-04"
  - "15.2-05"
  - "15.2-06"
  - "15.2-07"
  - "15.2-08"
  - "15.2-09"
  - "15.2-10"
  - "15.2-11"
  - "15.2-12"
  - "15.2-13"
  - "15.2-14"
  - "15.2-15"
  - "15.2-16"
  - "15.2-19"
files_modified:
  - tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py
  - tribunal/nestor_pulse_sdk/tests/test_gate_replay.py
  - tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py
  - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py
  - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/__init__.py
autonomous: true
requirements: [ENGINE-05, ENGINE-11]

must_haves:
  truths:
    - "One keyless, DB-less, network-less command proves the redesigned pipeline runs start to finish: workshop -> dispatch -> per-provider fact lists -> cross-provider merge -> gates -> verify -> report."
    - "The stubbed run asserts ZERO live provider calls: the object the pipeline talked to is defined in the test module, carries none of the real client's attributes, and served every single call."
    - "A run that loses the SerpApi stream and falls back to the distiller on one third-party provider finishes as `completed_degraded` with BOTH losses named in plain sentences a human reads — never a silent green."
    - "The recorded 1,162-claim population reproduces the recorded funnel EXACTLY when it arrives at the gates as merged provider facts instead of as distilled prose (D-04)."
    - "The gates preserve the D-13 provenance fields (`certainty`, `found_by`, `provider_quality`) they do not own — the merge's output survives the gate stage intact."
    - "`verify_chain` is green and every field 15.2 added is outside the frozen 11-field hashed payload (EU AI Act Art. 12)."
    - "All six CI gates are green in order, and every full-suite failure is named as a pre-existing live-key failure rather than ignored."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py"
      provides: "D-02's single gating end-to-end test: _ScriptedProvidersAudited + the no-DB session harness + the clean four-stream run + the degraded run"
      contains: "_ScriptedProvidersAudited"
      min_lines: 420
    - path: "tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py"
      provides: "load_merged_fact_claims() — the recorded 1,162 claims in the post-merge provider-fact shape, same text, same ids"
      contains: "def load_merged_fact_claims"
    - path: "tribunal/nestor_pulse_sdk/tests/test_gate_replay.py"
      provides: "D-04 rewire — the replay feeds merged provider facts straight into the gate machinery; RECORDED_FUNNEL_COUNTS stays the single source"
      contains: "load_merged_fact_claims"
    - path: "tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py"
      provides: "The 15.2 additive-not-a-chain-member proof over the frozen 11-field payload"
      contains: "provider_quality"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      via: "TribunalPipeline's existing injected-audited constructor parameter"
      pattern: "TribunalPipeline\\(audited="
    - from: "tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/facts.py"
      via: "the D8 format constants, imported rather than hard-coded"
      pattern: "from nestor_pulse_sdk.pipeline.tribunal.facts import"
    - from: "tribunal/nestor_pulse_sdk/tests/test_gate_replay.py"
      to: "tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py"
      via: "the merged-fact fixture reader"
      pattern: "load_merged_fact_claims"
    - from: "tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py"
      to: "tribunal/nestor_pulse_sdk/audit/hash_chain.py"
      via: "the frozen payload introspection"
      pattern: "_payload_for_row"
---

<objective>
**This plan writes ZERO production code.** It is the phase's proof, and it is built almost entirely
out of machinery that already exists.

**What it REUSES (name it before building anything):**

| Need | Reused thing | Do NOT build |
|------|--------------|--------------|
| A duck-typed fake `audited` that answers real prompts from a committed key | `test_gate_replay.py::_AnswerKeyGateAudited` (142-175) + its `_OutageAnswerKeyGateAudited` subclass (224-256) | a mocking-library provider stub |
| The deep-research adapter half of that fake (`start_call` / `end_call` / `write_failure` / `gemini_deep_research_raw` / `openai_deep_research_raw`) | `test_graceful_degradation.py::FakeAudited` (55-105) | a second adapter fake |
| A no-DB session that records the statements a production writer executes | `test_gate_replay.py:777-802` `_FakeSession` / `_FakeSessionmakerCtx` / `fake_get_sessionmaker` | a testcontainer, a sqlite shim |
| The injection seam into the pipeline | `TribunalPipeline.__init__(audited=...)` — a **production** constructor parameter (pipeline.py:335) | a test-only classifier/provider parameter in production code |
| The recorded population + the two blind answer keys + the recorded funnel | `fixtures/run_4cbb5311/loader.py` — `load_selection_experiment()`, `RECORDED_FUNNEL_COUNTS` | a second fixture reader |
| Provider enable/disable control | `degraded_parallel.ALLOW_DEEP_RESEARCH_{GEMINI,CLAUDE,OPENAI}` + the autouse `monkeypatch.setattr` fixture at `test_graceful_degradation.py:112-120` | new env plumbing |
| The exception-safety that makes a DB-less run possible at all | `set_stage` (stages.py:96-126), `is_cancelled` (137-163), `_read_output`/`_write_output` (pipeline.py:1226-1268) and the `persist_tribunal_claims` try/except (pipeline.py:1096-1119) are ALL already best-effort | new guards in production code |
| The CI gate that runs the new file | `tribunal/cloudbuild.test-engine.yaml` — **owned exclusively by 15.2-02**, already pre-lists `test_engine_e2e_stubbed.py` and `test_hash_chain_replay.py` | **do not edit that file** |

Purpose: close D-02. The operator's proof bar is *one* stubbed-provider pipeline test showing the new
wiring runs start to finish, plus the per-component contract tests plans 01–16 already own. LLM
judgment quality is explicitly **not** a CI gate — it lands on the August V-01/V-02 run.

Output:
1. `test_engine_e2e_stubbed.py` — the single gating end-to-end test, in two cases (clean four-stream,
   and degraded three-stream-plus-fallback).
2. `test_gate_replay.py` rewired per D-04 so the recorded 1,162 claims arrive at the gates as **merged
   provider facts**, not as "claims distilled from prose".
3. `test_hash_chain_replay.py` extended with the 15.2 additive-not-a-chain-member proof.
4. Six CI gates green, in order, with the full suite's live-key failures triaged by name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-CONTEXT.md
@.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase and from the sibling plans'
     frontmatter. Verify each against the merged tree before use — waves 1-9 landed first. -->

The pipeline entry point (`pipeline/tribunal/pipeline.py:325-344`):

    class TribunalPipeline:
        def __init__(self, audited: Optional["AuditedLLMClient"] = None) -> None: ...
        async def run(self, *, brief: str, run_id: uuid.UUID,
                      tenant_id: uuid.UUID) -> dict[str, Any]: ...

The full set of `audited.*` methods the tribunal package calls today
(`grep -o "audited\.[a-z_]*" pipeline/ citations/`): `gemini_generate` (10 sites),
`anthropic_messages` (4 sites), `write_failure` (3 sites). The deep-research adapters additionally
call `start_call` / `end_call` / `gemini_deep_research_raw` / `openai_deep_research_raw`
(`test_graceful_degradation.py::FakeAudited`). Waves 3-9 add call sites in `workshop.py`,
`workshop_rank.py` and `own_researcher.py` — all through the same two methods.

`anthropic_messages` returns the raw provider response; the tool-use loops read `resp.content`
(a list of blocks with `.type` / `.name` / `.input`) and `resp.stop_reason`
(`group_skeptic.py:243-262`). `gemini_generate` returns an object with `.text`
(`gates.py`, `grouping.py`).

`db/base.get_engine()` does `os.environ["DATABASE_URL"]` and is `functools.lru_cache(maxsize=1)`d;
`get_sessionmaker(engine=None)` calls it. `pipeline.py:81` binds `get_sessionmaker` at MODULE level
(used un-guarded at `pipeline.py:591`); `runs/stages.py`, `pipeline._read_output` and
`pipeline._write_output` import it lazily INSIDE the function from `nestor_pulse_sdk.db.base`.

`fixtures/run_4cbb5311/loader.py`:

    RECORDED_FUNNEL_COUNTS: dict[str, int | bool]     # the single source of the recorded numbers
    def _report_dir() -> Path                          # Pitfall 8 — the ONLY fixture path resolver
    def load_selection_experiment() -> tuple[list[dict], dict[int,str], dict[int,str]]
        # claims: [{"id": 1-based line no, "facet": str, "text": str, "evidence": str}, ...]
    def build_verification_summary() -> dict           # deliberately `dict(RECORDED_FUNNEL_COUNTS)`

Sibling-plan exports this plan drives (read each plan's own frontmatter `artifacts.exports` and the
merged source before writing against them):

- 15.2-02 `pipeline/tribunal/reliability.py` — `with_retry`, `CircuitBreaker`, `terminal_state()`.
- 15.2-04 `pipeline/tribunal/facts.py` — `FACTS_START`, `FACTS_END`, `NOT_FOUND_START`,
  `NOT_FOUND_END`, `QUALITY_VALUES`, `CERTAINTY_VALUES`, `build_fact_list_prompt_block`,
  `parse_fact_list`.
- 15.2-05 `citations/anchors.py` — `ANCHOR_RE`, `anchor_token`, `apply_citation_anchors`.
- 15.2-06 report headings — the literals `"## Disputed & changed"` and
  `"## What we could not establish"`, rendered by Python, before `"## Verification"`.
- 15.2-08 `verification_summary` keys — the funnel gains `checked_incidentally` (subtracted from
  bucket 2, and **never** added to `_ACCOUNTING_KEYS`), plus the sibling keys `unresolved_anchors`
  and `degradation_reasons`.
- 15.2-12 `pipeline/tribunal/serpapi.py` — `search()`, `fetch_plan()`, `unavailable_reason()`
  (returns `"serpapi_key_missing"` when `SERPAPI_API_KEY` is unset), `get_breaker()`.
- 15.2-03 `runs/stages.py::ENGINE_STAGES["tribunal"]` — now also declares `workshop`, `merge`,
  `own_research`.
</interfaces>

Source files to have open (do NOT re-read a range already read):
@tribunal/nestor_pulse_sdk/tests/test_gate_replay.py
@tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py
@tribunal/nestor_pulse_sdk/tests/test_graceful_degradation.py
@tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the zero-cost harness and the clean four-stream end-to-end proof</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py</files>

  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` — the WHOLE file. Lines 19-23 (the
      zero-LLM docstring that is the standard), 77-84 (`_PROMPT_TRUNCATION = 240` and its
      security-control comment — the coupling-guard pattern to copy), 142-221
      (`_FakeResponse` / `_AnswerKeyGateAudited`, the clone target), 224-256 (the outage subclass —
      the "variant fake by subclass" shape Task 2 reuses), 710-752 (the zero-live-call assertions to
      copy verbatim in spirit: exact counts, `type(audited).__module__ == __name__`, the
      `_a`/`_g`/`_audit`/`_gcs` attribute probe), 777-802 (`_FakeSession` / `_FakeSessionmakerCtx` /
      `fake_get_sessionmaker` — the no-DB session precedent).
    - `tribunal/nestor_pulse_sdk/tests/test_graceful_degradation.py` lines 30-120 — `FakeAudited`
      (`start_call` / `end_call` / `write_failure` / `gemini_deep_research_raw` /
      `openai_deep_research_raw` / `_FakeHandle`) and the autouse
      `monkeypatch.setattr(degraded_parallel, "ALLOW_DEEP_RESEARCH_*", True)` fixture.
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` — `TribunalPipeline.run` in full as
      the merged tree left it (waves 2-9 restructured it). Specifically: the injected-audited branch
      at 357-360, the resume-from-cache `_read_output` probe at 373-386, the un-guarded
      `_sm = get_sessionmaker()` at 591, the `persist_tribunal_claims` try/except at 1096-1119, and
      whatever `_write_final_report` became after 15.2-05/06.
    - `tribunal/nestor_pulse_sdk/runs/stages.py` lines 38-76 (`ENGINE_STAGES["tribunal"]` after
      15.2-03 added `workshop` / `merge` / `own_research`) and 78-170 (`set_stage`,
      `is_cancelled`, `raise_if_cancelled` — all exception-safe, which is what makes a DB-less run
      possible without touching production code).
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` lines 60-90 and 207-300 —
      `_PROVIDER_RUNNERS` (four entries after 15.2-13), `divide()`, `run_angles`'s `_one_angle`
      timeout/`write_failure` path, and how `enabled` is computed from
      `degraded_parallel._enabled_providers`.
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/facts.py` — the D8 format constants and
      `parse_fact_list`'s exact expectations, so the scripted provider reports are generated FROM the
      production constants and never from hand-typed literals.
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/own_researcher.py` and
      `pipeline/tribunal/serpapi.py` — how the own-researcher reaches SerpApi (module attribute vs
      direct import), which decides where the scripted `search` / `fetch_plan` are installed.
    - `tribunal/nestor_pulse_sdk/citations/extractor.py` `persist_tribunal_claims` / `_upsert_source`
      / `_link_claim_source` — enumerate EVERY `AsyncSession` method they call (`add`, `execute`,
      `flush`, `refresh`, `scalars`, `scalar`, `get`) so the no-DB session implements exactly that
      surface and the persistence path completes instead of falling into the caller's `except`.
    - `tribunal/cloudbuild.test-engine.yaml` — confirm `nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py`
      is already in the `FILES=$$(ls …)` list. **Do not edit this file** (15.2-02 owns it).
  </read_first>

  <action>
Create `tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py`.

**Module docstring** — write it in the voice of `test_gate_replay.py:1-46`: what this file is
(D-02's single gating end-to-end proof), why it exists (the redesigned pipeline must be shown to run
start to finish before it is deployed against an Anthropic account that cannot be exercised until
2026-08-01), and the load-bearing sentence, adapted verbatim in structure from the analog:
*THIS FILE MAKES ZERO LLM CALLS. Every provider call is served by `_ScriptedProvidersAudited`, a
hand-written duck-typed fake. No network, no database, no mocking library, no API key, no spend, and
nothing that can flake.* Then state, explicitly, the **non-goal**: this file does not judge whether
the tournament picks good questions, whether real providers emit well-formed fact lists, whether the
merge clusters real phrasings, or whether the own-researcher is useful — per D-02 those are V-02
items for the August live run, and a CI test that pretended to judge them would be judging its own
script. Finally list the Cloud Build gate:
`gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml`.

**No pytest marker.** This file touches no provider and no Postgres, so it carries neither
`live` (Pitfall 9) nor `slow` nor `integration` — the engine gate runs it with `-m "not live"` and it
must be selected. `asyncio_mode = "auto"` is set in `tribunal/pyproject.toml`, so write plain
`async def test_…` (do not copy the analog's `_run()` / `run_until_complete` helper).

**1. Coupling guards, at module scope, each with the analog's style of comment.** Import the D8
format constants (`FACTS_START`, `FACTS_END`, `NOT_FOUND_START`, `NOT_FOUND_END`) and the anchor
token builder from production (`facts.py`, `citations/anchors.py`) and BUILD the scripted provider
reports out of them. Add a comment saying, in the analog's terms, that the script is keyed on the
production format on purpose: if `facts.py` changes its sentinels the scripted reports stop parsing
and `_ScriptedProvidersAudited.unexpected` fills up, which the assertions report by name rather than
swallowing into a silent default. Do the same for the two D-08 headings and the
`ENGINE_STAGES["tribunal"]` key list — read them from production, never retype them.

**2. `_ScriptedProvidersAudited`** — the clone. Class docstring in the analog's register: it is NOT a
stub of the pipeline, it is a stand-in for the MODEL and the third-party research providers only;
everything between `TribunalPipeline.run(...)` and this object — the workshop loop, the tournament
pairing, the fan-out batching, the semaphores, the fact-list parser, the clusterer, the gate parser,
the funnel arithmetic, the anchor post-pass, the deterministic report sections — is production code
doing its real job. Implement exactly the surface the run touches:

- `async def gemini_generate(self, *, run_id, tenant_id, model, contents, **kwargs)` — route on
  markers read from the prompt **header** (the text before the embedded item block), never from the
  item text itself, exactly as the analog does with `_STABILITY_MARKER`. Routes needed: the two gate
  prompts (reuse the analog's answer-key logic against `load_selection_experiment()` for the small
  scripted claim set, or a deterministic keep/verify rule — whichever the merged gate prompt makes
  unambiguous), the clusterer (`grouping._CLUSTER_PROMPT` line format), the workshop's critique /
  tournament / evolve batch prompts (15.2-11's plain-text line formats), and the report-writing
  prompts. Every response is rendered in the **consumer's own plain-text line format** so the
  production parser reads it exactly as it would read a model's answer.
- `async def anthropic_messages(self, *, run_id, tenant_id, model, **kwargs)` — return a hand-written
  `_FakeMessage` with `.stop_reason` and `.content` (a list of hand-written `_FakeToolUseBlock`s
  carrying `.type == "tool_use"`, `.name`, `.input`). Serve the client tools the tool-use loops force
  on their final turn: `emit_group_verdict`, `emit_orientation`, `emit_fact_list`, and the per-claim
  `emit_verdict` used by the coverage re-entry path. Route on the tool names present in
  `kwargs["tools"]`, not on prompt text.
- `async def gemini_deep_research_raw(self, query, **kwargs)` and
  `async def openai_deep_research_raw(self, query, **kwargs)` — return
  `{"status": "success", "report": <scripted report text>}` where the report ends with a
  well-formed D8 fact block built from the production constants.
- `async def start_call(...) -> _FakeHandle` and `async def end_call(handle, *, response, status)`
  and `async def write_failure(*, run_id, tenant_id, provider, error)` — copy `FakeAudited`'s bodies,
  recording into lists.
- Counters and fail-loud ledgers: `calls`, `models: list[str]`, per-route counters, and
  `unexpected: list[str]` for any prompt the router does not recognise. **Never default an
  unrecognised prompt to a plausible answer** — record it and let the assertions name it. This is the
  `unmatched` discipline from the analog, and it is the difference between a test that proves the
  wiring and a test that proves the script.

**3. The scripted research corpus.** Three third-party provider reports plus one own-researcher fact
list, all short (a handful of facts each) and all deliberately shaped to exercise the merge:
- at least one fact stated by **two** providers in different phrasings (D9 corroboration — the merge
  must cluster them and the D-13 `found_by` list must end up with two entries);
- at least one **contradictory pair** across two providers on the same entity/attribute (they must
  land in ONE shared skeptic session — the D9/D11 reorder is exactly what makes that possible);
- at least one **single-source** fact (raises checking priority);
- a non-empty per-provider "couldn't find" list on at least one provider, so
  `## What we could not establish` renders with real content;
- a Gemini-style `vertexaisearch` redirect URL behind a markdown link label, so the display-domain
  extraction (Pitfall 10) is exercised on the path that actually matters.
Keep the corpus in one module-level structure with a comment explaining each element's purpose, so a
future reader can tell scripted data from an accidental fixture.

**4. `_NoDbSessionmaker` / `_NoDbSession`** — hand-written, ~50 lines, modelled on
`test_gate_replay.py:777-802`. The session records `(str(stmt), params)` for every `execute`, records
every `add()`ed ORM object, implements `flush()` (assign `uuid.uuid4()` to any `id` that is `None`
so the persistence path can link rows), `refresh()`, and returns an empty result object supporting
`.first() -> None`, `.scalar() -> None`, `.scalars().all() -> []`, `.scalars().first() -> None`.
`begin()` returns an async context manager; the sessionmaker itself is an async context manager
factory. Install it with `monkeypatch.setattr` at **both** bindings — `nestor_pulse_sdk.db.base`
(the lazy importers: `runs.stages`, `pipeline._read_output`, `pipeline._write_output`,
`citations.extractor`) and `nestor_pulse_sdk.pipeline.tribunal.pipeline` (the module-level binding
used un-guarded at `pipeline.py:591`). `monkeypatch` is a pytest fixture, not a mocking library —
the "no mocking library" rule is about the provider fake.

**5. The run driver** — one module-level `async def _engine_run(audited, *, monkeypatch, **overrides)`
helper that: installs the no-DB sessionmaker at both bindings; enables all providers via
`monkeypatch.setattr(degraded_parallel, "ALLOW_DEEP_RESEARCH_GEMINI"/"_CLAUDE"/"_OPENAI", True)`;
sets `NESTOR_TRIBUNAL_UNCAPPED=1` and any `NESTOR_TRIBUNAL_*` knob needed to keep the run small
(batch sizes, concurrency, tournament rounds — set them explicitly rather than inheriting defaults,
and comment why); installs the scripted SerpApi seam (`monkeypatch.setattr` on the `serpapi` module's
`search` and `fetch_plan`, plus `monkeypatch.setenv("SERPAPI_API_KEY", "scripted-not-a-real-key")`);
constructs `TribunalPipeline(audited=audited)`; and returns
`(result, captured_statements, audited)`. Every seam it installs must be listed in a docstring
table with one line each saying what is stubbed and what is therefore NOT under test.

**6. `async def test_clean_four_stream_run_completes_end_to_end(monkeypatch)`** — the D-02 gate.
Assert, each with a failure message that says what breaks in production if it fires:
- `result["output_text"]` is non-empty, and contains `"## Disputed & changed"`,
  `"## What we could not establish"` and the graded `## Sources` heading, with
  `index(disputed) < index(could_not_establish) < index("## Verification")`.
- No `[[c:` token survives into `output_text`, and every `[n]` marker in the body resolves to an
  entry in that report's own `## Sources` list.
- `result["verification_summary"]` carries every funnel key including `checked_incidentally`,
  `unresolved_anchors` and `degradation_reasons`, and the one-claim-one-bucket arithmetic reconciles:
  `checked + (dropped + skipped_stable - checked_incidentally) + should_have_been_checked ==
  distilled` (read the exact bucket-2 expression from `verification/report.py::_accounting` as
  15.2-08 left it, and assert against that shaper rather than re-deriving it).
- `degradation_reasons == []` and `terminal_state(...)` — called with the run's own degradation
  inputs, read from `reliability.py`'s signature — returns `"completed"`.
- The stage sequence recovered from the captured `set_stage` UPDATE statements (decode the bound
  `:stage` parameter, in execution order) is a superset of
  `{"workshop", "research_division", "deep_research", "own_research", "merge", "gate", "verify",
  "synthesize"}`, ends with `"done"`, and every observed key is declared in
  `ENGINE_STAGES["tribunal"]` (the WR-03 guard, restated here at run level).
- **Merge before gates (D9/D11):** the first index of `"merge"` in that sequence is strictly less
  than the first index of `"gate"`.
- A fact stated by two providers arrives at persistence with `len(found_by) == 2`; the
  single-source fact with `len(found_by) == 1`. Read this off the ORM objects the no-DB session
  captured via `add()`.
- The contradictory pair reached ONE shared group-skeptic session (assert on the fake's
  per-route call log: exactly one `anthropic_messages` call whose prompt block contains both
  claim indices).

**7. `async def test_the_stubbed_run_made_no_live_calls(monkeypatch)`** — copy the analog's
assertions (710-752) and extend them:
- `type(audited).__module__ == __name__` — the object the pipeline talked to was defined here.
- `for attr in ("_a", "_g", "_audit", "_gcs"): assert not hasattr(audited, attr)`.
- `audited.unexpected == []`, reported by name.
- Every route counter is `> 0` for the routes the pipeline must have used, and
  `audited.calls == sum(route counters)` — no unaccounted call.
- `nestor_pulse_sdk.db.base.get_engine.cache_info().misses == 0` — the real engine was never
  constructed, so no TCP connect was ever attempted. (Clear the cache in the fixture first if any
  earlier test in the session populated it, and say so in a comment.)
- The three provider key env vars (`ANTHROPIC_API_KEY`, `GOOGLE_API_KEY`, `SERPAPI_API_KEY`) hold no
  value that could reach a real endpoint: assert the scripted SerpApi seam recorded every search and
  that `serpapi`'s real `search` was never entered (a module-level call counter the seam increments).

Follow the shared rules: untrusted-output parsing discipline is production's job and must not be
bypassed here (never hand the parser pre-parsed objects — always hand it TEXT); every prompt the fake
inspects is read by header marker and index, never by trusting claim text.
  </action>

  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>

  <acceptance_criteria>
    - The engine-gate build log prints `nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py` under
      `collecting:` and reports 0 failures for it. (The config's `ls … || true` guard means a
      missing or misnamed file is SILENTLY SKIPPED — absence from `collecting:` is a failure of this
      task, not a pass.)
    - `test_clean_four_stream_run_completes_end_to_end` and
      `test_the_stubbed_run_made_no_live_calls` both appear as PASSED in the `-v` output.
    - Source assertion: `grep -c 'pytest.mark.live\|pytest.mark.slow\|pytest.mark.integration' tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py`
      returns 0 — the file carries no marker that would deselect it from the engine gate.
    - Source assertion: `grep -v '^\s*#' tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py | grep -c 'unittest.mock'`
      returns 0 — the provider fake is hand-written.
    - Source assertion: `grep -c 'TribunalPipeline(audited=' tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py`
      returns at least 1 — the pipeline is driven through its production constructor parameter.
    - Source assertion: `git diff --name-only` after this task lists exactly
      `tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py` — no production file, and in
      particular not `tribunal/cloudbuild.test-engine.yaml`, was modified to make the test pass.
    - Behavior: the test completes in under 60 s wall-clock in Cloud Build (the engine gate's whole
      budget is ~120 s of feedback latency).
  </acceptance_criteria>

  <done>
The redesigned pipeline is proven to run workshop -> dispatch -> fact lists -> merge -> gates ->
verify -> report with zero LLM calls, zero database, zero network and zero API keys, and the test
proves it served every call itself.
  </done>
</task>

<task type="auto">
  <name>Task 2: The degraded-run proof — lost stream, D-14 fallback, honest terminal state</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py</files>

  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py` — the file as Task 1 left it.
    - `tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` lines 224-256
      (`_OutageAnswerKeyGateAudited`: how a variant fake is expressed as a thin subclass that
      overrides ONE method and counts what it broke) and 629-703 (the operator's headline test — the
      assertion register for "a gutted run must not report green", including the
      `"VERIFICATION DEGRADED"` / `"not checked"` / `str(count) in text` triple).
    - `tribunal/nestor_pulse_sdk/verification/report.py` — `_NOT_CHECKABLE_KEYS`,
      `_ACCOUNTING_KEYS`, `_accounting` and the degradation-text builder as 15.2-08 left them. Note
      the trap: `_accounting` returns `None` when a key is missing (that is how it detects a
      pre-15.1 run), so `checked_incidentally` is read with `funnel.get(..., 0)` and is NOT in
      `_ACCOUNTING_KEYS`.
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/reliability.py` — `terminal_state()`'s exact
      signature and D-17 truth table, and `CircuitBreaker`'s open/half-open API.
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/serpapi.py` — `unavailable_reason()` returning
      `"serpapi_key_missing"` with the key unset, and `get_breaker()`.
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/steps` D-14 fallback wiring from 15.2-14: where
      the per-provider fallback is recorded and under which key it reaches the feed and the
      verification report.
    - `.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-CONTEXT.md`
      § D-12 and § D-17 — the degrading conditions in the operator's own words, and the two things
      that must NOT degrade a run (a recovered retry; `cost_pending`).
  </read_first>

  <action>
Extend `test_engine_e2e_stubbed.py` with the degraded path. No new file, no new harness — one thin
subclass and two tests.

**1. `_LostStreamProvidersAudited(_ScriptedProvidersAudited)`** — override exactly one thing: one of
the three third-party providers returns a report with **no D8 fact block at all** (prose only,
ending in a plain numbered markdown source list, which is precisely the shape of the recorded
run-4cbb5311 deep-research output). Count it (`self.factless_providers: list[str]`). Everything else
inherits.

**2. `async def test_lost_stream_and_fallback_run_completes_degraded(monkeypatch)`.** Drive
`_engine_run` with the subclass and with `monkeypatch.delenv("SERPAPI_API_KEY", raising=False)` so
the own-researcher is refused before any call (15.2-12's invariant: a missing key is treated exactly
like an open breaker). Assert:
- The run **completes** — it does not raise, and it does not park. Two independent losses that each
  still permit an honest deliverable are D-17's `completed_degraded`, not a park.
- `terminal_state(...)`, fed the run's own degradation inputs, returns `"completed_degraded"`.
- `result["verification_summary"]["degradation_reasons"]` contains a reason naming the SerpApi
  stream loss (the literal `"serpapi_key_missing"` must be present in the machine-readable reason
  list) AND a reason naming the D-14 distiller fallback with the failing provider's name. Apply the
  fail-loud bar from `test_fail_loud.py`: at least one human-readable degradation sentence exists,
  `len(text) > 40`, and the affected provider name appears in it as a literal. *"This must be a
  sentence a human reads, not a code."*
- The run still produced a report: `output_text` non-empty, both D-08 sections present, and
  `## What we could not establish` names the losses rather than omitting them.
- **Three streams, not four:** the observed stage sequence still contains `deep_research` and
  `merge`, and `own_research` is either absent or present with an explicitly failed/skipped status —
  never present-and-spinning (the F2 defect class).
- The funnel still reconciles: every distilled claim lands in exactly one bucket, with
  `checked_incidentally` subtracted from bucket 2 (assert through `shape_verification_report`'s own
  `accounting`, not by re-deriving the arithmetic).
- The fallback provider's claims still reached the gates: `distilled > 0` and the fallback
  provider contributed at least one claim with `certainty` and `provider_quality` left null and the
  domain heuristic supplying the tier — proving D-14 degrades one stream instead of the run.

**3. `async def test_recovered_retry_and_cost_pending_do_not_degrade(monkeypatch)`** — D-12's
explicit carve-outs, and the reason the degraded marker keeps its meaning. Drive the clean fake but
have one `gemini_generate` call raise a transient error on its first attempt and succeed on the
retry (a second thin subclass, or a per-instance `fail_once_on` set). Assert: the run finishes
`completed`, `degradation_reasons == []`, and a retry row is nevertheless visible in the captured
stage detail (R5 — recovery is SHOWN, not punished). Then assert that a `cost_pending` result does
not add a degradation reason either. A marker that is always on is a marker the operator learns to
ignore — say so in the failure message.

Keep every assertion message in the analog's register: name the production consequence, not the
mismatch.
  </action>

  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>

  <acceptance_criteria>
    - `test_lost_stream_and_fallback_run_completes_degraded` and
      `test_recovered_retry_and_cost_pending_do_not_degrade` both PASS in the engine gate's `-v`
      output.
    - Behavior: with `SERPAPI_API_KEY` unset the run reaches a terminal state of
      `completed_degraded`, never `failed` and never `parked`.
    - Behavior: `"serpapi_key_missing"` appears in the machine-readable
      `verification_summary["degradation_reasons"]`, and a separate human-readable degradation
      sentence longer than 40 characters names the provider that fell back to the distiller.
    - Source assertion: `grep -c 'class _LostStreamProvidersAudited(_ScriptedProvidersAudited)' tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py`
      returns 1 — the variant is a subclass, not a second fake.
    - Source assertion: `git diff --name-only` after this task lists only
      `tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py`.
  </acceptance_criteria>

  <done>
The two honest-failure paths D-12 and D-14 describe are proven in CI: a run that loses a stream and
falls back on another still ships a report, and it says out loud, in words, exactly what it lost.
  </done>
</task>

<task type="auto">
  <name>Task 3: D-04 — rewire the recorded replay onto merged provider facts, and prove the 15.2 fields are not chain members</name>
  <files>tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py, tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/__init__.py, tribunal/nestor_pulse_sdk/tests/test_gate_replay.py, tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py</files>

  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py` — the whole file as 15.2-08
      left it. Specifically the `RECORDED_FUNNEL_COUNTS` block and its per-key provenance comment
      (44-105, plus 15.2-08's `checked_incidentally` addition), `_report_dir()` (108-127, Pitfall 8 —
      the ONLY fixture path resolver), `build_verification_summary()`'s deliberate
      `dict(RECORDED_FUNNEL_COUNTS)` body and the comment naming the two full-dict-equality callers
      (230-245), and `load_selection_experiment()`'s documented TSV contract (248-354).
    - `tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` — already read in Task 1. Re-check only
      the ranges 1-46 (docstring), 100-140 (fixture load + `_prompt_key` + `_TEXT_TO_ID`), 263-308
      (`_replay()` and `_pipeline_funnel`), 315-364 (the fixture-integrity test) and 510-546 (the
      full-dict-equality propagation test).
    - `tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py` lines 764-800 — TEST 11
      (`test_chain_green_after_cost_migration`), the 0011 precedent this task follows: `verify_chain`
      green, `len(_payload_for_row(...)) == 11`, and an explicit per-name exclusion loop.
    - `tribunal/nestor_pulse_sdk/audit/hash_chain.py` — `_payload_for_row`'s frozen field set and
      `verify_chain`'s signature.
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/gates.py` `apply_gates` as 15.2 left it — confirm
      it labels claims with `claim["gate"]` and returns the same list objects, so extra provenance
      keys on the input dicts survive to the output.
    - `.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-CONTEXT.md`
      § D-04 (lines 112-120) — the rewire, in the operator's own words.
  </read_first>

  <action>
**1. `loader.py` — add `load_merged_fact_claims()`.** D-04's whole point: after D8 the gates' input is
the merged provider fact list, not prose distilled by `claim_distiller`. The recorded population is
still the right population — the gates key on claim TEXT — so serve the same 1,162 rows in the
post-merge shape.

Signature: `def load_merged_fact_claims() -> list[dict]`. Body: call `load_selection_experiment()`
(never re-read the TSVs — one correct reader, not two) and decorate each claim dict with the D8/D-13
fields the merge stage attaches, deterministically derived from the claim's own 1-based id so two
calls return identical data:
- `found_by: list[str]` — provider names drawn from a fixed rotation over the four stream names, with
  every Nth id given a second provider so the corroborated/single-source mix is represented;
- `certainty: str` — clamped to a value from `facts.CERTAINTY_VALUES`;
- `provider_quality` — a value from `facts.QUALITY_VALUES`;
- `source_urls: list[str]` — a stable synthetic https URL per id.
**Do not touch `id`, `text`, `facet` or `evidence`.** Docstring must state, as the load-bearing
contract: *the claim TEXT is byte-identical to `load_selection_experiment()`'s, so the recorded funnel
must reproduce EXACTLY — a different funnel means the gates started reading provenance, which they
must not.* Also state the D-04 rationale and that `RECORDED_FUNNEL_COUNTS` remains the single source
of the recorded numbers (G-13 unchanged).

While in this file, reconcile the documented invariant block (77-84) with 15.2-08's
`checked_incidentally`: the one-claim-one-bucket line must read as bucket 2 minus
`checked_incidentally`, with the arithmetic spelled out against the recorded values as 15.2-08 set
them. Do **not** change any recorded value.

Export `load_merged_fact_claims` from `fixtures/run_4cbb5311/__init__.py` (import + `__all__`),
alongside `load_selection_experiment`.

**2. `test_gate_replay.py` — the rewire.** Four surgical changes; leave the file's structure, its ten
numbered sections and its assertion voice intact.

(a) **Docstring.** The paragraph that frames the population as "1,162 claims distilled" is now the
history, not the input shape. Rewrite the WHY to say: this file proves the gates and the funnel
arithmetic — the thing 15.1 actually built — and after 15.2's D8/D11 the population reaches them as
**merged provider facts**, which is how it is now fed. Add a sentence recording that `claim_distiller`
is not gone: D-15 keeps its full-extraction mode as D-14's per-provider safety net, with its own
tests (`test_claim_distiller.py`, `test_distiller_coverage.py`) staying green through V-03. Keep the
G-13 boundary paragraph and the Cloud Build gate line unchanged.

(b) **`_replay()`** feeds `load_merged_fact_claims()`. Keep `_CLASSIFIED` / `_STRICT` from
`load_selection_experiment()` — the answer keys are unchanged — and keep `_CLAIMS` for the
fixture-integrity test. Keep `_prompt_key`, `_TEXT_TO_ID`, `_KEY_COLLISIONS` and, above all,
`_PROMPT_TRUNCATION = 240` **and its comment**: that comment documents a security control, and the
map is keyed on the same truncation so a width change surfaces as `unmatched` rather than as a silent
default.

(c) **New test — `test_gates_are_indifferent_to_claim_provenance`.** Run `apply_gates` twice with two
fresh `_AnswerKeyGateAudited` instances: once over `load_selection_experiment()[0]` (the pre-15.2
input shape) and once over `load_merged_fact_claims()`. Assert the two `funnel` dicts are equal, and
equal to `RECORDED_FUNNEL_COUNTS` on the five gate-computed keys plus the three drop reasons. Failure
message: the gates started reading provenance, so the selection this phase is measured against would
change with the engine that fed it. Then assert the **preservation** property on the merged run's
output claims: every claim still carries its `found_by`, `certainty`, `provider_quality` and
`source_urls`, and additionally carries `gate` — the gate stage may LABEL a claim, never strip the
merge's provenance, because persistence (D-13) reads those keys downstream of here.

(d) **`_pipeline_funnel`** must publish the funnel the 15.2 verify stage publishes, so tests 6-8 keep
reconciling: add `checked_incidentally` (read from the input funnel with a `.get(..., 0)` default,
mirroring `_accounting`'s own tolerance) and any other key 15.2-08 made the report shaper read.
Update the helper's docstring to name the new keys and to restate the trap in one line: the shaper
returns `None` accounting when a key it REQUIRES is missing, which is how it recognises a pre-15.1
run — so `checked_incidentally` is read tolerantly and is never added to `_ACCOUNTING_KEYS`.

(e) **Verify, do not change:** `test_funnel_reaches_verification_summary`'s
`run.verification_summary == RECORDED_FUNNEL_COUNTS` (line 543) is one of the full-dict-equality
assertions Pitfall 3 warns about. It holds by construction because `build_verification_summary()` is
`dict(RECORDED_FUNNEL_COUNTS)`. Confirm it still passes; if it does not, the bug is that 15.2-08
diverged the builder from the constant — fix the builder, never the assertion.

**3. `test_hash_chain_replay.py` — the 15.2 additive proof.** Add a new test beside TEST 11 (do not
mutate TEST 11; the 0011 proof stays as its own regression):
`async def test_chain_green_after_15_2_additive_fields()`. Same shape as the 0011 precedent:
- build a valid chain with `_build_chain_rows(4, …)`, assert `verify_chain` returns `(True, None)`;
- introspect `frozen_fields = set(_payload_for_row(rows[0]).keys())` and assert
  `len(frozen_fields) == 11`;
- loop over every name 15.2 introduced and assert each is absent from `frozen_fields`:
  `certainty`, `found_by`, `provider_quality`, `research_gap`, `checked_incidentally`,
  `unresolved_anchors`, `degradation_reasons`, and the run-status literals `completed_degraded` /
  `parked`. Failure message, in the 0011 test's own terms: adding any of these to the hashed payload
  would break every existing chain, and `verify_chain` green is a legal gate under EU AI Act Art. 12
  with a 2026-08-02 deadline.
Extend `test_recorded_stage_detail_enriched` only to the extent that 15.2-08's new keys must be
present on `run.verification_summary` — its existing
`run.verification_summary == RECORDED_FUNNEL_COUNTS` full-dict-equality assertion must keep holding
untouched.
  </action>

  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT" && gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>

  <acceptance_criteria>
    - `cloudbuild.test-gates.yaml` is green: `test_gate_replay.py` PASSES with the rewired input, and
      `test_verification_buckets.py`, `test_fail_loud.py`, `test_gate_selector.py`,
      `test_tribunal_grouping.py`, `test_claim_distiller.py` and `test_stage_schema.py` are unchanged
      and still pass.
    - `test_gates_are_indifferent_to_claim_provenance` PASSES: the two funnels are equal to each other
      and to `RECORDED_FUNNEL_COUNTS` on `distilled`, `kept`, `dropped`, `selected_verify`,
      `skipped_stable`, `not_falsifiable`, `not_load_bearing`, `both`.
    - Behavior: after `apply_gates` over merged-fact input, every returned claim dict still has the
      keys `found_by`, `certainty`, `provider_quality`, `source_urls` AND `gate`.
    - `cloudbuild.test-engine.yaml` is green and its `-v` output shows
      `test_chain_green_after_15_2_additive_fields` PASSED (the engine gate is the config that runs
      `test_hash_chain_replay.py`).
    - Source assertion: `grep -v '^\s*#' tribunal/nestor_pulse_sdk/tests/test_gate_replay.py | grep -c '_PROMPT_TRUNCATION = 240'`
      returns 1 — the coupling guard survived the rewire.
    - Source assertion: `grep -c 'def load_merged_fact_claims' tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py`
      returns 1, and `grep -c 'load_merged_fact_claims' tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/__init__.py`
      returns 2 (the import and the `__all__` entry).
    - Source assertion: `grep -v '^\s*#' tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py | grep -c 'return dict(RECORDED_FUNNEL_COUNTS)'`
      returns 1 — `build_verification_summary()` is still a pure copy, so both full-dict-equality
      callers hold by construction.
    - CLI: `git diff --stat` shows no change to any file under
      `tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/recorded/` — the recorded evidence is
      read-only.
  </acceptance_criteria>

  <done>
The 15.1 phase gate survives the 15.2 redesign at zero cost: the recorded 1,162 claims now reach the
gates as merged provider facts and still land on the recorded funnel exactly, and no field this
phase added is a member of the frozen hash payload.
  </done>
</task>

<task type="auto">
  <name>Task 4: The six-gate green sweep, in order, with the full suite triaged by name</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py, tribunal/nestor_pulse_sdk/tests/test_gate_replay.py, tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py</files>

  <read_first>
    - `.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-VALIDATION.md`
      § Sampling Rate and § Test Infrastructure — the six gates, their commands and their runtimes.
    - `tribunal/cloudbuild.test-engine.yaml` — the `collecting:` echo and the `ls … || true` guard,
      so the log check below is meaningful.
    - `tribunal/cloudbuild.test.yaml` — the full-suite config, its testcontainers preamble and its
      3600 s timeout, so a triage decision can distinguish "needs a key" from "needs Docker" from
      "regression".
    - `backend/scripts/ci_no_run_research.sh` — what INTAKE-05 actually scans (`backend/app/**` and
      `frontend/src`, NOT `tribunal/`), so a SerpApi symbol inside the SDK is correctly a pass.
  </read_first>

  <action>
This task writes no new test. It runs the phase gate and closes the loop. Run the six gates **in this
order** and do not proceed past a red one without fixing the cause:

1. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT"`
2. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"`
3. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT"`
4. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test.yaml --project="$GOOGLE_PROJECT"`
5. `gcloud builds submit --config=backend/cloudbuild.test.yaml --project="$GOOGLE_PROJECT"`
6. `bash backend/scripts/ci_no_run_research.sh`

**Gate 1 has a second, non-obvious check.** The engine config builds its file list with
`ls … 2>/dev/null || true`, so a test file that does not exist — or whose name was typed differently
from the pre-registered entry — is silently skipped and the gate goes green having run nothing.
Read the `collecting:` block in the gate-1 log and confirm every 15.2 test filename the phase
produced is listed, notably `test_engine_e2e_stubbed.py` and `test_hash_chain_replay.py`. If a
filename is missing, the FILE is wrong, not the config — **do not edit
`tribunal/cloudbuild.test-engine.yaml`; 15.2-02 owns it exclusively.** Rename the test file to match
the pre-registered name.

**Gate 4 triage is the judgment call, and it is not optional.** The full suite includes tests that
need a real provider key (the August gate-calibration test and anything marked `live`) and tests that
need Docker/testcontainers. For every failure, record a row: test id · failure class
(`missing-key` / `needs-docker` / `pre-existing` / `regression`) · the evidence for that
classification (the exception type and message, and whether the same test failed on the pre-15.2
tree). **Any failure that cannot be evidenced as one of the first three classes is a regression and
blocks this plan.** "Known failing" is not a class; a named, evidenced cause is.

Record the triage table and the six build ids in the plan SUMMARY. Do not create a separate report
file.

If gate 3 or 5 is red for an environment reason rather than a code reason (no Postgres, no
credentials), that is a blocking result, not a skip: say so plainly in the SUMMARY and stop, per the
fail-loud rule.
  </action>

  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT" && gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT" && gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT" && gcloud builds submit --config=backend/cloudbuild.test.yaml --project="$GOOGLE_PROJECT" && bash backend/scripts/ci_no_run_research.sh</automated>
  </verify>

  <acceptance_criteria>
    - CLI: gates 1, 2, 3, 5 and 6 each report `STATUS: SUCCESS` from `gcloud builds submit`
      (gate 6 exits 0).
    - CLI: the gate-1 log's `collecting:` block contains the literal
      `nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py`.
    - Gate 4 (`cloudbuild.test.yaml`) completes and every failing test in its output is classified in
      the SUMMARY's triage table as `missing-key`, `needs-docker` or `pre-existing`, each with the
      exception type quoted. Zero rows classified `regression`.
    - Behavior: `test_hash_chain_replay.py` passes in gates 1, 3 and 4 — `verify_chain` is green
      unconditionally.
    - Behavior: `test_claim_distiller.py` and `test_distiller_coverage.py` pass (D-15 —
      `claim_distiller` survives; nothing in this phase may delete it before V-03).
    - CLI: `bash backend/scripts/ci_no_run_research.sh` exits 0, confirming INTAKE-05 holds — the
      SerpApi client lives in `tribunal/` and never in `backend/app/**` or `frontend/src`.
  </acceptance_criteria>

  <done>
All six gates are green in order, the phase's own fast gate is proven to actually collect the new
tests rather than silently skipping them, and every full-suite failure carries an evidenced,
non-regression cause.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → provider endpoints | The one boundary this plan exists to keep CLOSED. Any crossing costs money against a capped Anthropic account and, before 2026-08-01, returns nothing but 400s. |
| test process → Cloud SQL | Must also stay closed: the engine gate provisions no database. A test that opens a socket hangs the fast gate. |
| scripted model output → production parsers | The scripted reports and tool blocks are, from production's point of view, untrusted model text. They must be handed to the real parsers as TEXT, so the ASVS V5 discipline is exercised rather than bypassed. |
| committed fixture → the phase's own pass/fail | `RECORDED_FUNNEL_COUNTS` and the two answer keys decide whether this phase is judged reproducible. A silent fixture drift re-baselines the gate. |
| audit chain → the legal artifact | `verify_chain` green is an EU AI Act Art. 12 gate with a 2026-08-02 deadline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-90 | Tampering | `TribunalPipeline` / `gates.py` / `facts.py` — the temptation to add a test-only injection seam | mitigate | The ONLY injection points used are the production `TribunalPipeline(audited=...)` constructor parameter and `monkeypatch` over `db.base.get_sessionmaker` / the `serpapi` module functions. RESEARCH's anti-recommendation is honoured verbatim: no injectable classifier/provider parameter is added to production code, because a test-only seam is a second production path that skips exactly the machinery under test. Enforced by the Task 1 acceptance criterion that `git diff --name-only` lists only the test file. |
| T-15.2-91 | Information disclosure / Denial-of-wallet | a real `AuditedLLMClient` or a real SerpApi GET reaching CI | mitigate | `type(audited).__module__ == __name__`; no `_a`/`_g`/`_audit`/`_gcs` attribute; every route counter accounted for; `audited.unexpected == []`; the scripted SerpApi seam counts calls and the real `serpapi.search` is asserted never entered; the engine gate runs `-m "not live"` and this file carries no marker at all. |
| T-15.2-92 | Tampering | `fixtures/run_4cbb5311/` — silent fixture drift re-baselining the phase gate | mitigate | Recorded evidence under `recorded/` is read-only (`git diff --stat` criterion). `load_merged_fact_claims()` decorates but never edits `id`/`text`/`facet`/`evidence`. `unmatched` / `missing_strict` / `_KEY_COLLISIONS` are asserted empty and reported by name rather than defaulted away. `RECORDED_FUNNEL_COUNTS` stays the single source and no recorded value is changed. |
| T-15.2-93 | Repudiation (legal) | `audit/hash_chain.py::_payload_for_row` — a 15.2 field slipping into the frozen payload | mitigate | `test_chain_green_after_15_2_additive_fields` asserts the frozen set is exactly 11 fields and that each of `certainty`, `found_by`, `provider_quality`, `research_gap`, `checked_incidentally`, `unresolved_anchors`, `degradation_reasons`, `completed_degraded`, `parked` is absent. Blocking on failure. |
| T-15.2-94 | Tampering | prompt-injection controls silently drifting (`_PROMPT_TRUNCATION = 240`, index addressing) | mitigate | The 240-char coupling guard and its comment survive the D-04 rewire (grep criterion); the replay map is keyed on the same truncation, so a width change surfaces as `unmatched` rather than as a silent default. The scripted fake routes on prompt HEADER markers and tool names, never on item text — the same control the production prompts rely on. |
| T-15.2-95 | Denial of service | the fast gate hanging on a real TCP connect via `db.base.get_engine()` | mitigate | The no-DB sessionmaker is installed at BOTH bindings, and `get_engine.cache_info().misses == 0` is asserted — the real engine is never constructed, so no connect is ever attempted. Reinforced by the < 60 s runtime criterion. |
| T-15.2-96 | Information disclosure | captured SQL/bound parameters echoed into a public Cloud Build log | mitigate | Assertions read specific bound-parameter KEYS and decode only the funnel/stage values; no test prints a full parameter map. No secret is in scope for these tests — the SerpApi key is either absent (Task 2) or a literal scripted placeholder (Task 1). |
| T-15.2-97 | Elevation of privilege | none — this plan adds no endpoint, no table, no column and no status literal | accept | Nothing crosses a tenant boundary; the no-DB harness never calls `set_tenant_context` against a real database. Tenant isolation for the 15.2 surfaces is owned by 15.2-01 (`research_gap` denial test, non-superuser DSN) and 15.2-16 (Resume endpoint denial trio). |
| T-15.2-SC | Tampering | supply chain — package installs | mitigate | **This plan installs no package.** `pytest`, `pytest-asyncio` and everything imported are already pinned in `tribunal/requirements.txt` / `pyproject.toml`; RESEARCH § Package Legitimacy Audit records zero `[ASSUMED]` / `[SUS]` / `[SLOP]` entries for this phase. `google-search-results` is rejected. If execution discovers a genuinely missing dependency, STOP: the Package Legitimacy Gate runs first and a blocking `checkpoint:human-verify` precedes any install. |
</threat_model>

<verification>
Phase-level checks for this plan, all runnable while the Anthropic account is capped:

1. **The D-02 gate exists and is collected.** `cloudbuild.test-engine.yaml` green AND its
   `collecting:` block names `test_engine_e2e_stubbed.py`. A green gate that collected nothing is the
   failure mode this check exists to catch.
2. **Zero live calls, asserted rather than assumed.** The fake served every call; the object under
   the pipeline was defined in the test module; `get_engine` was never constructed.
3. **The 15.1 gate is unbroken.** `cloudbuild.test-gates.yaml` green with the D-04-rewired
   `test_gate_replay.py`, landing on `RECORDED_FUNNEL_COUNTS` exactly.
4. **The legal gate is green.** `test_hash_chain_replay.py` passes and the frozen payload is still
   11 fields with every 15.2 name outside it.
5. **D-15 holds.** `test_claim_distiller.py` and `test_distiller_coverage.py` still pass — nothing in
   this phase deletes `claim_distiller`.
6. **The scope guard holds.** `ci_no_run_research.sh` exits 0.
7. **No production file changed.** `git diff --name-only` across the whole plan lists exactly the
   five files in `files_modified`.
</verification>

<success_criteria>
- [ ] `tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py` exists, carries no pytest marker,
      imports no mocking library for its provider fake, and drives `TribunalPipeline(audited=…)`.
- [ ] The clean four-stream run asserts the full chain workshop → dispatch → fact lists → merge →
      gates → verify → report, with `merge` observed strictly before `gate`.
- [ ] The degraded run finishes `completed_degraded` with `"serpapi_key_missing"` in
      `degradation_reasons` and a >40-character human sentence naming the D-14 fallback provider.
- [ ] A recovered retry and a `cost_pending` result do NOT degrade the run (D-12's carve-outs).
- [ ] `load_merged_fact_claims()` exists in `loader.py`, is exported from the fixture package, and
      leaves `id`/`text`/`facet`/`evidence` untouched.
- [ ] `test_gate_replay.py` feeds merged provider facts to `apply_gates`, reproduces
      `RECORDED_FUNNEL_COUNTS` exactly, proves the two input shapes yield an identical funnel, and
      proves the gates preserve `found_by` / `certainty` / `provider_quality` / `source_urls`.
- [ ] `_PROMPT_TRUNCATION = 240` and its security-control comment survive the rewire.
- [ ] `test_chain_green_after_15_2_additive_fields` proves `verify_chain` green, the frozen payload at
      11 fields, and every 15.2 name outside it.
- [ ] All six gates green in order; every gate-4 failure carries an evidenced non-regression class in
      the SUMMARY's triage table.
- [ ] `tribunal/cloudbuild.test-engine.yaml` is byte-unchanged (15.2-02 owns it).
</success_criteria>

<output>
Create `.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-17-SUMMARY.md`
when done. It must include the six build ids and the gate-4 triage table (test id · failure class ·
evidence), plus an explicit statement of what this plan proved and what it deliberately did NOT
prove (the seven V-01/V-02 items D-02 defers to the August live run).
</output>
