# Phase 15.2: Research Engine Redesign — Engine Core - Context

**Gathered:** 2026-07-26
**Status:** Ready for planning

<domain>
## Phase Boundary

**Phase 15.2 restructures the pipeline core** — everything upstream and downstream of the
verification gates that Phase 15.1 already built. It is the third and last of the
redesign's three phases (B-01/B-02: 15 surfaces → 15.1 gates → 15.2 engine core).

Seven deliverables, all locked in the operator decision files (see canonical refs):

1. **Question workshop** (D2–D7) — orientation searches → 30–50 candidate sub-questions →
   cluster → critique → pairwise tournament → evolve → 10–15 winners. Replaces the one-call
   `adaptive_intake` step. **This IS the evolved plan-critique pass — ENGINE-05 is absorbed
   here (S-02), not built separately.** It may add depth but never changes scope (D4);
   client-validated questions are untouchable. Fully automatic, no operator pause (D5).
2. **Structured per-provider fact lists** (D8) — every provider ends its report with a
   structured facts section (statement, source link, source quality, certain vs
   found-only-once, plus a "couldn't find" list). That section becomes the primary claim
   source; the distiller shrinks to a per-provider safety net.
3. **SerpAPI-fueled own researcher** (D10) — a fourth peer research stream that searches,
   reads pages, and returns the same structured fact list. New `SERPAPI_API_KEY` secret on
   both Tribunal images.
4. **Cross-provider merge** (D9/B-04) — all streams' facts clustered by the LLM clusterer
   15.1 already built. Agreement = corroboration (lowers checking priority); single-source
   = raises it; contradictions land in ONE shared skeptic session.
5. **Language tagging** (D7) — the workshop tags each sub-question with the languages worth
   searching in; providers are instructed accordingly. Report output language unchanged.
6. **Reliability R1–R7** — retry classification + backoff, per-provider circuit breaker,
   checkpointing, park/resume (F-01/F-02/F-03), four honest terminal states (R6), visible
   retries in the feed (R5), provider background/continuation modes (R7).
7. **Report changes** (D13/D14) — `[n]` citation placement wired into the report body, and
   two new deterministic sections.

Plus **two operator-approved carry-ins from 15.1's gap-closure review** (`15.1-UAT.md`
§ Deferred to Phase 15.2): **WR-01** (the coverage re-entry path is unreachable dead code)
and **WR-10** (verdicts attributed to gate-DROPped members break the one-claim-one-bucket
invariant). Both live in `pipeline.py`'s verify stage, which this phase restructures.

**Validation & cutover:** V-01 (live run on a test intake vs the recorded 4cbb5311
baseline — no A/B double-run), V-02 (hard checklist + operator sign-off), V-03 (old engine
path removed on acceptance, no fallback flag).

**Out of scope:**
- Embedding machinery — Phase 19 (B-04 locked LLM-based clustering).
- Cost caps / budget ceiling flip-on — Phase 20 (16-D-02; the governor stays uncapped).
- Draft tournament (ENGINE-06) — DROPPED per S-03. Tournament exists at question level only.
- Any client-visible research or verification surface — D-08 stands; the client PDF is
  hand-crafted by the superadmin (Phase 18).
- Q&A chat over run output — Phase 19.

**Hard constraints carried forward:**
- `verify_chain` stays green — frozen audit payload fields must not be renamed; new fields
  ADD only (EU AI Act Art. 12, deadline 2026-08-02).
- **No live LLM runs before 2026-08-01** (Anthropic monthly cap). The phase is deliberately
  buildable and deployable without one; V-01/V-02/V-03 land after the reset.
- Author-by-construction + Cloud Build for tests/images (no local Python/Docker).
- Every new table/endpoint joins the cross-tenant denial suite day one.
- Phase-16 D-07 dynamic-stage-list contract: new stages must render without frontend
  changes — and must be **declared in `runs/stages.py::ENGINE_STAGES`** (the WR-03 lesson:
  15.1 wrote a `gate` stage key the schema never declared).
- D-01 no-pause-gates: the workshop must not introduce an operator pause.

</domain>

<decisions>
## Implementation Decisions

> **Numbering note.** Decisions below are `D-01`…`D-16` (this phase). They are distinct from
> the engine brainstorm decisions `D1`–`D15` / `R1`–`R7` / `C1` in
> `.planning/RESEARCH-ENGINE-DECISIONS.md` (no hyphen), from Phase 15's `S-`/`B-`/`V-`/`F-`
> decisions, from Phase 15.1's `G-01`–`G-14`, and from Phase 16's decisions (cited here as
> `16-D-NN`). Where this phase's decisions resolve or supersede an earlier one, it is stated.

### Phase shape and the 2026-08-01 wall

- **D-01: 15.2 stays ONE phase, left open across the wall.** No roadmap split, no new 15.3.
  All buildable work executes and deploys now; the phase then parks at UAT waiting for the
  cap reset, exactly as Phase 15's browser UAT already does. Rejected: splitting build from
  validate+remove (roadmap churn for a ~1-week wait); splitting by engine area (cuts across
  the pipeline mid-flow and multiplies integration seams).

- **D-02: The execution proof bar mirrors G-01 — a stubbed end-to-end run plus per-component
  contract tests close the phase; LLM judgment quality is NOT a CI gate.**
  - **Gating (CI):** ONE end-to-end pipeline test with **stubbed providers** proving the new
    wiring runs start to finish (workshop → dispatch → fact lists → merge → gates → verify →
    report), plus per-component contract tests for everything provable without an LLM:
    R1–R7 state machine (retry classification, breaker, checkpoint/resume, four terminal
    states), funnel accounting including the WR-01/WR-10 invariants, citation placement and
    the two D14 sections rendered from fixture data, fact-list parsing against recorded
    provider output, and C1 cost arithmetic (all four Anthropic token classes, search fees,
    Gemini `usageMetadata`, SerpAPI unit-price arithmetic).
  - **Not gating:** whether the tournament picks *good* questions, whether real providers
    return well-formed fact lists, whether the merge clusters real cross-provider phrasings,
    whether the own-researcher's research is useful. These are V-02 checklist items judged
    on the August live run.
  - Rationale: identical to G-01 — demanding live-LLM quality from CI would park the phase
    as incomplete until August and block deployment of code that is otherwise finished.

- **D-03: The old engine path stays in-tree but unreferenced until sign-off.** The build
  rewires so ONLY the new path executes — the August run is genuinely the new engine — but
  the old modules (`intake.adaptive_intake`, the distiller-as-primary-source wiring, the
  exact-key grouping remnants) are not deleted. A **separate cleanup commit after operator
  sign-off** removes them, which is literally what V-03 says ("on acceptance … removed
  immediately"). **No feature flag and no dual-run** — V-03's no-fallback-period rule holds;
  recovery means reverting one small wiring change, not restoring deleted code.
  See D-15 for the one piece of the old distiller that survives deletion on purpose.

- **D-04: 15.1's recorded-fixture replay survives by feeding the gates directly.** The new
  engine no longer produces "1,162 claims distilled from prose" (D8 makes the provider's
  fact list the primary source; D11 puts the merge before the gates), so the replay must
  stop asserting that input shape. Rewire the test to hand the 1,162 fixture claims
  **straight into the gate machinery**, which still takes a claim list. It keeps proving the
  gates + funnel arithmetic — the thing 15.1 actually built — at zero cost.
  `RECORDED_FUNNEL_COUNTS` stays the single source of the recorded numbers (G-13 unchanged).
  `test_hash_chain_replay.py` and `verify_chain` stay green unconditionally — separate,
  legally-gated concern.

### Citations and the report

- **D-05: The model marks anchors; Python assigns the numbers.** Phase 15 already built the
  numbering half (`citations/numbering.py::number_citations` — deterministic, tested, byte-
  identical across calls); what is missing is placement. The synthesis prompt requires an
  **opaque anchor** (the claim/source identity the statement came from) on every
  load-bearing statement; a **Python post-pass** rewrites each anchor to its `[n]` from
  `number_citations`. The writing model **never chooses a number** — that is exactly what
  produced last run's 28 stripped markers. Rejected: post-hoc prose→claim matching (it is
  the clustering problem again, and a wrong match cites the wrong source, which is worse
  than no citation); citing only pipeline-provable statements (leaves most load-bearing
  prose unmarked, since the distiller never extracted it).

- **D-06: An anchor that does not resolve is stripped, COUNTED, and stated in words.** The
  sentence stays, the dangling marker goes (never ship a broken `[n]`), and the count lands
  in the superadmin verification report and the feed's closing summary — in words, not a
  subtle icon. Same shape as G-08's bucket 3 and G-10's marker: no silent losses. Today
  `pipeline.py:1307` deletes these with only a log line. Rejected: retrying synthesis on
  any unresolved anchor (synthesis is among the most expensive calls, and the model may
  legitimately cite prose the distiller never extracted); rendering a visible `[?]` (puts
  damage into the text the operator must hand-clean out of every report).

- **D-07: G-14 governs over D13's "client-facing" framing — the citation surface is the
  superadmin's working tool.** D13 predates G-14's ruling that *"the client never receives
  this generated report — only the final report the superadmin submits"*, and Phase 18 built
  exactly that hand-crafted delivery. So `[n]` markers, the graded source list and the
  clickable snapshot panel exist to make hand-crafting the client PDF **fast and safe** —
  click any statement, see its source, date, tier and stored snapshot while writing. Client-
  facing citation style stays entirely the operator's call in Phase 18, unchanged. Rejected:
  making numbering survive into the delivered PDF (re-litigates Phase 18's model); building
  the generated report to a deliver-as-is standard (contradicts G-14).

- **D-08: D14's two new sections are deterministic Python blocks, like `## Sources`.**
  "Disputed & changed" (skeptic-settled contradictions, `superseded` verdicts from G-06,
  and the workshop's brief-vs-world flags from D4) and "What we could not establish" (merged
  from the providers' couldn't-find lists) are rendered directly from pipeline data and
  appended — **the writing model never sees or rewrites them.** Precedent: `## Sources` was
  moved to deterministic Python (`synthesis/steps.py:275`) after the model was observed
  truncating URLs mid-link. Also makes both sections testable from fixture data with no LLM
  in the loop, which D-02's proof bar needs. Rejected: model-presents-from-a-supplied-list
  (D14's literal wording, but the model can still omit or merge items and proving it didn't
  needs an LLM-judged test); deterministic list plus a model-written intro (one more thing
  that can drift from the list beneath it).

### Run states and funnel semantics

- **D-09: One named shared predicate replaces all four `status != "completed"` gates;
  degraded gets full access, parked gets inspection only.** The four sites are
  `runs/api.py:587` (compare critique), `:691` (compare content-compare), `:947`
  (`GET /{run_id}/report`), `:1046` (`GET /{run_id}/research-bundle`). Replace the inline
  comparisons with a single shared set/predicate so there is **one place to audit**.
  - `completed-degraded` → everything `completed` gets. That is G-10's entire rationale:
    never lock the superadmin out of a ~$45 run's output because one stage was gutted.
  - `parked` → research bundle and verification report (you must be able to see *why* it
    stopped), but **no report download** — there is no report yet.
  Rejected: parked exposes nothing (loses the audit bundle exactly when it is most wanted);
  widening each gate inline (four places to keep in sync — the shape that made G-10 defer
  this in the first place).

- **D-10: WR-10 resolves to the review's Option 2 — keep the checking, add a
  `checked_incidentally` funnel line.** A group is selected when ANY member is `VERIFY`, and
  `_parse_group_verdict` fills a verdict for **every** member — including gate-DROPped and
  `SKIP_STABLE` ones. Those verdicts are real: they reach adjudication, can refute a claim,
  and `scrub_research` deletes the refuted passage. Keep them filed. Add a
  `checked_incidentally` funnel line, **subtracted from bucket 2**, so every distilled claim
  still lands in exactly one bucket and the accounting stops under-claiming what was
  checked. Consistent with G-11 ("fails toward MORE checking") and G-10 (never discard
  checking already paid for). Rejected: Option 1 (skip dropped members — smaller and matches
  the documented three buckets, but it silently stops scrubbing passages that are removed
  today, making the report *less* verified in exchange for cleaner bookkeeping); making the
  incidental checks deliberate (partly undoes the gates' purpose and changes session cost).

- **D-11: WR-01 is fixed AND re-entry dispatch is gated on the R2 circuit breaker.** Apply
  the review's recorded one-line fix (`adjudications` built from
  `verdicts_by_claim.get(id(c))` rather than seeded `True` for every claim) plus the
  correction of the inverted comment at `pipeline.py:845-847`. **Additionally**, re-entry
  must check the breaker before dispatching: a tripped provider means no re-entry, and those
  claims go to bucket 3 with a named reason. Rationale: on 2026-07-22, 776 skeptic sessions
  were hard-400'd by the monthly cap in 55 seconds; with WR-01 fixed and nothing else,
  coverage would have failed for all of them and re-entry would have fired **three more
  skeptics each** into the same wall. **The budget governor is NOT a backstop here — it is
  currently uncapped** (`NESTOR_TRIBUNAL_UNCAPPED=1`, D-15 / 16-D-02 until Phase 20), so
  `over_budget()` always returns `False` and every `budget_exceeded` guard in `pipeline.py`
  is inert. Rejected: the fix verbatim relying on `MAX_REENTRY` + budget bounds (the budget
  bound does not exist today); accounting-only with re-entry left off (keeps the status quo
  the review flagged — a crashed skeptic means the claim is simply never checked).

- **D-12: `completed-degraded` means the OUTPUT fell short, with every reason named.**
  Degrading conditions: bucket 3 non-zero; a research stream lost to a tripped breaker; the
  workshop fell back to client-validated questions only; a stage skipped. **Recovered
  retries do NOT degrade a run** — R5 already shows those in the feed as recovery, and
  demoting them would make nearly every run degraded and drain the status of meaning.
  **C1's `cost_pending` never counts as degradation** — pending-then-backfill-exact is the
  designed path (Gemini grounding fees), not a shortfall. Every degraded run carries an
  explicit reason list, stated in words. Rejected: any-incident-demotes (alarm fatigue);
  bucket-3-only (a run that lost an entire Gemini stream would still report plain
  `completed` — silent-green in a new place).
  **Vocabulary sites that must stay in sync:** `runs/schemas.py:39` (`RunStatus` literal),
  the DB `ck_run_status` CHECK (`db/models/run.py`), the frontend `RESEARCH_TERMINAL` set
  (`ResearchRunProgress.tsx:40`), and `tests/test_fail_loud.py:95` — which currently asserts
  `"completed_degraded" not in statuses` and **must be updated, not deleted** (it is G-10's
  deliberate negative test; 15.2 is the phase that promotes the marker).

### The new moving parts

- **D-13: A D8 fact's extra metadata gets real columns — additive migration.** The `claim`
  table holds only `id, tenant_id, run_id, text, facet, position, created_at`; source
  linkage lives in `source` + `claim_source`. A D8 fact carries three things with nowhere to
  go. Land them as:
  - **certainty** ("certain" vs "found only once — double-check") → additive column on `claim`
  - **`found_by`** (which providers found it) → additive column on `claim`; promotes G-12's
    in-memory provenance into a persisted, queryable signal
  - **provider-stated source quality** → additive column on `claim_source`, with
    `numbering.derive_quality_tier()`'s domain heuristic kept as the **fallback** when a
    provider omits it (two sources of truth, provider-stated wins)
  - **the per-provider "couldn't find" list** → a small new table, read by D14's "What we
    could not establish" section (D-08)
  All tenant-scoped with FORCE RLS and cross-tenant denial tests day one, like every other
  table. **Chain-safe** — these are DB columns, not audit payload fields, so `verify_chain`
  is unaffected. Rejected: one JSONB blob on `claim` (cheapest migration, but corroboration
  and priority logic would read JSON instead of columns); in-memory only (nothing is
  reconstructable after the run, so the August live run could not be re-examined for why a
  fact was prioritised — unacceptable for the one run that has to prove the phase).

- **D-14: A provider that returns no usable fact list falls back to full distillation, and
  it is recorded.** These are third-party deep-research APIs following a prompt instruction,
  and the format cannot be forced — structured-output mode is unavailable ("citations ⊗
  structured-outputs = 400", noted twice in `synthesis/steps.py`). D8's safety-net distiller
  covers "the list is incomplete", not "there is no list". So: that provider's prose goes
  through the distiller as a **complete extraction** rather than a safety net, its research
  still reaches the merge, certainty and provider-stated quality are left null (domain
  heuristic fills the tier), and the fallback is recorded **per provider** and surfaced in
  the feed and verification report. Degrades one stream, not the run; nothing paid-for is
  lost. Rejected: one corrective-retry first (an extra deep-research call is among the most
  expensive in the run — on Gemini a full re-run); dropping the stream (discards a paid call
  and weakens corroboration for every question that provider covered).

- **D-15: Consequence of D-14 — the distiller's full-extraction mode SURVIVES V-03's
  removal.** What D-03/V-03 deletes is the distiller being the *primary* claim source (the
  "distiller-as-shredder" wiring). Its ability to extract claims from a provider's prose
  stays alive as the per-provider fallback path and must keep its own tests. The planner
  must not treat "remove the old engine path" as "delete `claim_distiller`".

- **D-16: SerpAPI spend = recorded search count × published per-search unit price, exact and
  live.** SerpAPI is sold as a prepaid plan, so the unit price is `plan price ÷ plan search
  quota` — a **published** figure, making `count × unit` a correct calculation, not an
  estimate. This is the treatment C1 already gives Anthropic `web_search` fees. The unit
  price in force is **recorded alongside the run** so the number stays reproducible when the
  plan changes. Rejected: `tool fees: pending` + invoice reconciliation (a prepaid plan bills
  a flat monthly charge, so there may never be a per-run figure to backfill — the row would
  stay pending forever and the run's cost would never be marked final); counts-without-money
  (puts a known, quantifiable cost class outside the total, which is the exact gap C1 exists
  to close).

- **D-17: Park only when no honest deliverable is possible; otherwise finish degraded.**
  R2's breaker is per-provider; R4's park is whole-run. The boundary:
  - **Finish `completed-degraded`** — one or two research streams lost; the run produces a
    report from what remains and names what it lost (D-12).
  - **Park** — nothing usable can come out: every research provider walled, the verification
    stage entirely unable to run, or the synthesis model walled. Also the settled R4 case:
    a hard wall such as the Anthropic monthly cap or exhausted credits.
  Resume is superadmin-click-only with a Resume button + mail (F-01/F-03); checkpoint
  resumes are free and unlimited and do not count toward the 3-attempt rule (F-02).
  Rationale: park should mean "this genuinely needs you", not a stream of emails asking
  permission to carry on. Rejected: park on any hard wall (runs park often, each holding
  checkpoint state until a human appears); never park (discards R4's core promise — a
  cap-walled run would finish near-empty instead of resuming free after the reset).

### Claude's Discretion

Carried from 15-CONTEXT and extended — the operator explicitly declined to narrow these:

- **Workshop internals** — candidate counts, clustering of near-duplicates, critique prompt,
  tournament pairing scheme and round count, evolve-step mechanics, orientation-search
  budget. Target shape is D2's plain language (30–50 candidates → 10–15 winners), adjustable
  by evidence. Follow the `group_skeptic.py` hand-written audited tool-use loop as the agent
  template (Tribunal convention: no agent frameworks).
- **D6 distribution mechanics** — how sub-questions are allocated across the now-four
  streams, and which "top few" go to all of them.
- **D7 language-tagging mechanics** — how the workshop decides which languages a question
  warrants, and how that reaches each provider's research prompt.
- **Feed/trace data model** for the new agent-level rows (extend `stage_detail` items vs new
  rows/tables) — under the frozen-audit-payload constraint and the D-07 dynamic-list
  contract; new stage keys MUST be declared in `runs/stages.py::ENGINE_STAGES`.
- **R1/R2/R3 parameters** — retry counts, backoff and jitter, breaker thresholds and reset,
  checkpoint granularity. Follow the best-practice sources cited in
  `RESEARCH-ENGINE-DECISIONS.md` Area 9.
- **R7 mechanics** — which providers' background/continuation modes are used and how
  reconnection is driven.
- **The exact anchor token format** for D-05 and how the post-pass parses it.
- **Storage shape for D-06's unresolved-anchor count and D-12's degradation reason list** —
  additive keys on `run.verification_summary` and/or new columns; must not rename existing
  funnel keys (Phase-15 surfaces and `test_hash_chain_replay.py` assert on them).
- **Stub design for D-02's end-to-end test** — recorded provider fixtures vs synthetic.
- **Exact V-02 checklist items** — derive from the decision files during planning.
- **Which test intake the August V-01 run uses** — a smoke space exists; smoke tenant
  `1464b60d` was noted for cleanup, so check suitability or create fresh.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The requirement source (operator-agreed decision files — read FIRST)
- `.planning/RESEARCH-ENGINE-DECISIONS.md` — **the primary source for this phase**:
  D2–D7 (question workshop, placement, scope guard, automatic, language tagging),
  D6 (deliberate distribution), D8 (structured fact lists + safety-net distiller),
  D9 (cross-provider merge, corroboration, shared skeptic session for contradictions),
  D10 (SerpAPI own-researcher stream), D11 (gates apply after the merge),
  D12/D15 (live agent feed + the agreed mockup), D13 (numbered graded citations),
  D14 (two new report sections), R1–R7 (reliability), C1 (cost, no estimates ever).
- `.planning/STAKEHOLDER-NOTES.md` §"2026-07-24 — Verification redesign" (the verification
  package this phase's funnel changes sit on top of) and §"2026-07-22 — OPERATOR HOLD"
  (the P0–P3 defect list the redesign answers).
- `replit view.png` (repo root) — the visual reference for the live feed (D15).

### Prior phase decisions that bind this phase
- `.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-CONTEXT.md`
  — the parent redesign context: **S-02** (ENGINE-05 absorbed by the workshop), **S-03**
  (ENGINE-06 dropped), **B-01/B-02/B-03** (three-phase split and order), **B-04** (LLM
  clustering, no embeddings), **V-01/V-02/V-03** (validation + cutover), **F-01/F-02/F-03**
  (park/resume policy).
- `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-CONTEXT.md`
  — **G-01** (the proof-bar pattern D-02 mirrors), **G-02** (gates replaced stakes triage as
  selector), **G-03** (the LLM clusterer this phase's merge inherits), **G-06/G-07**
  (`superseded` verdict + forced inline caveat, which D14's "Disputed & changed" surfaces),
  **G-08** (three honest buckets — D-10 amends), **G-10** (the degraded marker D-12
  promotes), **G-11** (fail toward more checking), **G-12** (corroboration provenance D-13
  persists), **G-14** (the ruling D-07 rests on). Also its `<code_context>` — the
  subtractive-verification finding the planner MUST internalise.
- `.planning/phases/15.1-.../15.1-UAT.md` § "Deferred to Phase 15.2" — **WR-01** and
  **WR-10** in full, with file/line, the defect, and the recorded fix for each.
- `.planning/phases/16-research-trigger-progress-bridge/16-CONTEXT.md` — D-01/D-01b (no
  pause gates — binds D5), D-02 (uncapped until Phase 20), D-04 (3-attempt rule, amended by
  F-02), D-07 (dynamic stage list — the contract the feed extends), D-08 (client sees
  nothing), D-09/D-10/D-11 (summary card + email rules that F-03 reuses).
- `.planning/REQUIREMENTS.md` — **ENGINE-05** (absorbed per S-02) and **ENGINE-11** are this
  phase's requirement IDs.
- `.planning/ROADMAP.md` § Phase 15.2 — goal + the five success criteria.

### Evidence base + fixtures
- `docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md` — forensic audit of the
  baseline run (§3.5 verdict accounting, §4 cost reality, the 776 cap errors). **This is the
  baseline V-01 compares against.**
- `docs/tribunal-run-reports/run-20260722-4cbb5311/GROUPS.md` — the 176 groups / 92%
  singleton evidence and the four shipped contradictions.
- `docs/tribunal-run-reports/run-20260722-4cbb5311/selection-experiment/` — the 1,162-claim
  fixture and blind classifications.
- `tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py` —
  `RECORDED_FUNNEL_COUNTS` + `load_recorded_run`. Cloud Build ships only the `tribunal/`
  subtree, so tests MUST resolve fixtures through `loader._report_dir()`, never the repo-root
  `docs/` path.

### Code this phase changes
- `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` — the orchestration loop this
  phase restructures. Key sites: `_PER_CLAIM_SKEPTICS`/`_MAX_BUDGET_USD` (~195-199), the
  per-claim verify loop (~843-871), the WR-01 `adjudications` build (~893-898) and its
  inverted comment (845-847), the coverage re-entry loop (~906-935), WR-10's `_flush_groups`
  attribution (~699-703), and the unresolved-`[cite:]` strip (~1307).
- `tribunal/nestor_pulse_sdk/pipeline/tribunal/intake.py` — `adaptive_intake`, the one-call
  step the question workshop replaces (D-03: unwired, not deleted, until sign-off).
- `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` — where D6's deliberate
  distribution across the now-four streams lands.
- `tribunal/nestor_pulse_sdk/pipeline/tribunal/grouping.py` — 15.1's LLM clusterer (G-03);
  D9's cross-provider merge inherits it rather than building a second one.
- `tribunal/nestor_pulse_sdk/pipeline/tribunal/group_skeptic.py` + `skeptic.py` — the
  hand-written audited tool-use loop; the TEMPLATE for the workshop agents and the SerpAPI
  researcher. WR-10's over-attribution is at `group_skeptic.py:150-156`.
- `tribunal/nestor_pulse_sdk/pipeline/tribunal/coverage_gate.py` — `check_coverage`,
  `MAX_REENTRY`; the `id(claim) not in adjudications` test at line 61 that WR-01 unblocks.
- `tribunal/nestor_pulse_sdk/pipeline/tribunal/budget.py` — the governor. **Currently inert**
  (`NESTOR_TRIBUNAL_UNCAPPED=1`); `DEFAULT_MAX_BUDGET_USD` is 5.00 but unused. Do not plan
  around it as a backstop.
- `tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py` — `claim_distiller` (~727, D-14/
  D-15 fallback mode + D8 safety-net mode), `_dedupe_claims` (G-12 merge), the deterministic
  `_build_sources_section` (~275, the precedent D-08 follows), `scrub_research`, and the
  synthesis prompts (~78 preserve-markers, ~229 Sources instruction).
- `tribunal/nestor_pulse_sdk/citations/numbering.py` — `number_citations` (already built,
  deterministic, tested) — D-05's post-pass consumes it.
- `tribunal/nestor_pulse_sdk/citations/renderer.py` — `get_source`, the snapshot panel D-07
  relies on.
- `tribunal/nestor_pulse_sdk/citations/extractor.py` — `_insert_claim`, `_upsert_source`,
  `_link_claim_source`, `persist_tribunal_claims`; D-13's additive columns land here.
- `tribunal/nestor_pulse_sdk/db/models/claim.py` — `id, tenant_id, run_id, text, facet,
  position, created_at`. D-13 extends it.
- `tribunal/nestor_pulse_sdk/db/models/run.py` — the `ck_run_status` CHECK (D-12).
- `tribunal/nestor_pulse_sdk/runs/api.py` — the four status gates at 587 / 691 / 947 / 1046
  that D-09 replaces with one predicate (848 is display-only).
- `tribunal/nestor_pulse_sdk/runs/schemas.py:39` — the `RunStatus` literal (D-12);
  `:273` documents the `number_citations` entry shape.
- `tribunal/nestor_pulse_sdk/runs/stages.py:38` — `ENGINE_STAGES["tribunal"]`. New stages
  (workshop, merge, …) MUST be declared here — the WR-03 lesson.
- `tribunal/nestor_pulse_sdk/runs/worker.py` — claim/park/resume states (R3/R4/F-01).
- `tribunal/nestor_pulse_sdk/verification/report.py` — the funnel/bucket shaper; WR-10's
  bucket-2 mis-booking is at `:96, 120-126`. D-06's count and D-12's reason list surface here.
- `tribunal/nestor_pulse_sdk/audit/audited_llm_client.py` — `resolve_url_citations`
  (~106/163/169) and the C1 cost table (cache-write counting, search fees, Gemini
  `usageMetadata`, and D-16's SerpAPI unit-price arithmetic).
- `tribunal/nestor_pulse_sdk/tests/test_fail_loud.py:95` — asserts `completed_degraded` is
  NOT in the vocabulary. D-12 updates it; do not delete it.
- `tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py` — the additive-field contract.
- `frontend/src/components/intake/ResearchRunProgress.tsx:40` — `RESEARCH_TERMINAL`
  (D-12) and the feed the D15 agent rows extend.
- `backend/app/research/` — the intake-side seam and `research_runs` bridge; park state and
  the Resume endpoint land here.
- `backend/scripts/ci_no_run_research.sh` + `backend/tests/test_scope_guard_ai.py` — the
  INTAKE-05 guard. **It scans `backend/app/**` and `frontend/src` only, not `tribunal/`** —
  so D10's SerpAPI use inside the SDK does not trip it, exactly as D10 intends. Do not add
  SerpAPI references to the scanned trees.
- `infra/DEPLOY-RUNBOOK.md` — already earmarks `SERPAPI_API_KEY` as "Phase 15.2 / D10", a new
  secret + env on BOTH Tribunal images. Extend the runbook for this phase.

</canonical_refs>

<code_context>
## Existing Code Insights

### The architectural finding the planner MUST internalise (carried from 15.1, still true)

**The report is written from scrubbed PROSE, not from claims.** Providers write essays →
`claim_distiller` extracts assertions out of that prose (claims are *probes*, not report
content) → fact-checking runs on the probes → only **refuted** claims enter `dropped`, and
`scrub_research` physically deletes the passages that state or depend on them → synthesis
writes from the scrubbed prose. This is why D-05 needs the model to place anchors: there is
no pre-existing link from "this sentence" to "this claim row", and inventing one after the
fact is the clustering problem again.

D8 shifts *where claims come from* (provider fact list instead of prose extraction) but does
**not** change this: the report is still written from prose. An unchecked claim still leaves
its passage standing untouched.

### Reusable Assets
- **`number_citations()` already exists and works** (Phase 15, `citations/numbering.py`) —
  deterministic `[n] → source` from `claim.position` ordering, with quality tier and a
  `single_source` flag, proven byte-identical across calls in `test_citation_numbering.py`.
  It is consumed by an API surface (`runs/schemas.py:273`) but **not wired into the report
  body** — that gap is D-05's whole job.
- **`get_source()` + stored snapshots** (`citations/renderer.py`, `source.snapshot_text` +
  `content_hash`) — the clickable source panel D-07 depends on already exists and survives
  dead links.
- **`grouping.py`'s LLM clusterer** (G-03, built in 15.1) — D9's cross-provider merge
  inherits it. Also its flash-tagging conventions: batched (~40), bounded concurrency,
  `thinking_budget=0`, **plain-text line output, never JSON mode** ("citations ⊗
  structured-outputs = 400"), all egress via `audited.gemini_generate`.
- **`group_skeptic.py`'s hand-written audited tool-use loop** — server-side web_search/
  web_fetch, client-tool termination, prompt caching. The ready-made template for the
  workshop agents and the SerpAPI researcher; clone it, don't invent.
- **`_build_sources_section`** (`synthesis/steps.py:275`) — the deterministic-Python
  precedent D-08 follows, introduced after the model truncated URLs mid-link.
- **3-table citation model** (`source` / `claim` / `claim_source`) + `persist_tribunal_claims`
  — D-13 extends it additively rather than building new storage.
- **Budget governor, advisory locks, worker claim/stale logic** — R1–R6 build on these. But
  see the caveat below: the governor is currently a no-op.
- **`loader.load_recorded_run`** — seeds a full recorded run with no GCS access; D-04's
  rewired gate test builds on it.

### Established Patterns
- Hand-written async loops over agent SDKs — Tribunal convention, keep it for the new agents.
- **Frozen audit payload + hash chain: new fields ADD, never rename.** `verify_chain` green
  is a legal gate (EU AI Act Art. 12, 2026-08-02). `test_hash_chain_replay.py` asserts
  additive fields are NOT chain members — every new 15.2 field needs the same proof.
- New stage keys must be declared in `runs/stages.py::ENGINE_STAGES` or the UI renders a raw
  key with no label and `RunMetrics.stages` omits it (the WR-03 defect from 15.1).
- Recorded audit blobs carry `seq=0` — order by GCS mtime, not seq.
- Tests run in Cloud Build (no local Python/Docker); fixtures resolve via the in-package
  `recorded/` copy because only `tribunal/` ships.
- Intake-side: sync `def` handlers on pg8000 except deliberate SSE `async def`.
- Plain-language operator decision files in `.planning/*.md` ARE the requirement source for
  this phase — unusual but deliberate; derive acceptance criteria from them.

### Caveats the planner must not get wrong
- **The budget governor is inert.** `NESTOR_TRIBUNAL_UNCAPPED=1` (D-15 / 16-D-02, uncapped
  until Phase 20) makes `over_budget()` always return `False`, so every `budget_exceeded`
  branch in `pipeline.py` is dead today. Do not plan any safety property that depends on it
  (this is why D-11 gates re-entry on the R2 breaker instead).
- **`[cite: N]` markers are provider artifacts, not ours.** Gemini deep research emits them;
  `resolve_url_citations` swaps them for URLs at extraction time; unresolved ones are
  stripped at `pipeline.py:1307`. D-05's anchors are a **separate, new** mechanism — don't
  conflate the two.
- **The INTAKE-05 scope guard does not scan `tribunal/`** — verified in
  `ci_no_run_research.sh`. SerpAPI inside the SDK is fine; SerpAPI inside `backend/app/**`
  or `frontend/src` breaks CI.

### Integration Points
- Workshop consumes the context pack via the existing brief-assembly path (16-D-01 entry
  point) and must NOT alter client-validated questions (D4).
- New stages (workshop, merge, own-researcher) → `ENGINE_STAGES` → `stage_detail` JSONB →
  SSE bridge → `ResearchRunProgress.tsx` feed rows with per-row cost.
- D8 facts → additive claim/claim_source columns (D-13) → merge/clustering → gates (15.1) →
  skeptics → adjudication → funnel + `checked_incidentally` (D-10) → verification report.
- D-05 anchors → synthesis output → Python post-pass → `number_citations` → `[n]` in body;
  unresolved count (D-06) → verification report + feed summary.
- Park/resume: worker (tribunal) ↔ `research_runs` bridge (intake) ↔ Phase-10 mail stack ↔
  Resume button → seam client call (F-01/F-03).
- SerpAPI: new secret on both Tribunal images; per-search count → C1 cost table (D-16).

</code_context>

<specifics>
## Specific Ideas

- The operator's plain-language bar, carried from 15.1 and still governing: **a run must
  never report "completed — green" while a stage was gutted.** D-12 extends that from
  verification to every stage.
- **"NO ESTIMATES — facts and correct calculations only"** (operator, verbatim, C1). D-16's
  SerpAPI arithmetic qualifies because the unit price is published; anything that would
  require guessing must show as pending instead.
- The D15 feed mockup embedded in `RESEARCH-ENGINE-DECISIONS.md` was explicitly agreed —
  treat it as the wireframe, with `replit view.png` as the visual bar.
- Google co-scientist's pairwise tournament is the reference for the workshop's ranking
  step, scaled to 30–50 candidates.
- The economic bar from 15.1 still applies downstream: ~950 checks → ~150 on a run that cost
  ~$43–45 real (against a ~€5 displayed figure — the gap C1 closes).
- The four shipped contradictions (Aral 16% vs 21%, LUKOIL NL 46 vs ~70/75 stations, Zeeland
  Carlyle vs TotalEnergies, Gunvor vs Carlyle) are the concrete bar for D9's merge: each pair
  must collide in one skeptic session.
- Baseline for "did we actually improve": the LUKOIL BeNeLux dynamic-pricing run (4cbb5311).
  Same intake domain should be re-runnable for V-01.
- The first live run also validates the deployed F-01 skeptic fix by construction
  (roadmap SC5).

</specifics>

<deferred>
## Deferred Ideas

Carried forward from 15 / 15.1 and unchanged by this discussion:

- **Embedding-based clustering** — B-04 chose LLM-based; embeddings stay a Phase-19+ upgrade
  path if LLM grouping proves weak.
- **Draft tournament (ENGINE-06)** — dropped this milestone (S-03); first candidate to
  revisit if report quality disappoints after the redesign ships.
- **Cost caps / budget-ceiling flip-on** — Phase 20 (16-D-02). This phase must not depend on
  the governor (see D-11).
- **Auto-resume with a time window for parked runs** — F-01 chose click-only; revisit if
  parked runs pile up in practice.
- **Live A/B comparison runs (old vs new engine)** — V-01 chose baseline comparison; the
  `comparison_id` harness stays available but unused.
- **Superadmin review surface for low-confidence gate drops** — rejected in G-08; gate
  quality is measured by the August calibration instead.
- **Operator override to force-check a specific gated-out claim** — a new capability;
  candidate for Phase 20 or later.
- **A client-facing verification/coverage statement** — G-09/D-07 keep this superadmin-only.
- **Blocking superseded claims from carrying conclusions** — G-07 rejected as unenforceable
  in a writing model.

Raised and set aside during this discussion (not scope creep — deliberately left to the
planner under Claude's Discretion rather than pre-decided): the workshop's tournament shape
and orientation-search budget, D6's distribution mechanics across four streams, D7's
language-tagging mechanics, and the concrete V-02 checklist for the August session.

</deferred>

---

*Phase: 15.2-Research Engine Redesign — Engine Core*
*Context gathered: 2026-07-26*
