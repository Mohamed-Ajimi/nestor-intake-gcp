---
phase: 15.2
status: parked-awaiting-2026-08-01
created: 2026-07-27
signed_off: null
---

# Phase 15.2 — Operator UAT: V-01 live run + the 16-item V-02 checklist

Phase 15.2's code is deployed (§ Phase 15.2 of `infra/DEPLOY-RUNBOOK.md`, Steps 15.2.a-15.2.h).
This file is the **parked half**: one live run, sixteen checks, one signature.

**The park is not a failure.** D-01, verbatim: *"All buildable work executes and deploys now; the
phase then parks at UAT waiting for the cap reset, exactly as Phase 15's browser UAT already does."*

---

## § Blocking prerequisites

None of the sections below may start until all four hold:

- **The Anthropic monthly cap has reset — 2026-08-01.** No live LLM run may be triggered before
  then. This is the wall the whole park exists for.
- **The SerpApi tier is live and `Nestor_SerpApi` is seeded** (runbook Step 15.2.b), *or* an explicit
  operator decision to accept a 3-stream degraded V-01 has been recorded — in which case pre-mark
  V-02 #6 as an expected miss with the reason, before the run, not after.
- **A fresh test intake exists**, reproducing the baseline brief domain, in a clean space.
- **The operator is present.** V-02 #16 is a human read, not an assertion.

---

## § V-01 — the live run

**ONE run. Not two.** The run happens on a fresh intake reproducing the baseline brief domain —
LUKOIL BeNeLux: dynamic pricing, coffee, Germany-entry 2027 — in a clean space.

**No A/B double-run.** The `comparison_id` harness remains available and deliberately **unused**:
V-01 compares against the *recorded* 4cbb5311 baseline, not against a second live run. Spending two
capped runs to compare the engine with itself would buy nothing the recorded baseline does not
already give, and the baseline is the only artifact the four recorded contradictions are pinned to.

**Use a fresh space, not the smoke tenant.** Smoke tenant `1464b60d` was flagged for cleanup (Phase
14). Either confirm its suitability explicitly or — recommended — create fresh, so the one run that
has to prove the phase is not entangled with the cleanup backlog.

Record before proceeding to V-02:

| Field | Value |
|---|---|
| Tribunal `run_id` | TBD (V-01) |
| `research_run_id` | TBD (V-01) |
| Intake id | TBD (V-01) |
| Space / tenant id | TBD (V-01) |
| Started (UTC) | TBD (V-01) |
| Ended (UTC) | TBD (V-01) |
| Terminal status | TBD (V-01) |
| Deployed image `$SHA` | TBD (V-01) |
| SerpApi tier in force | TBD (V-01) |

Then fill `docs/tribunal-run-reports/V-01-COMPARISON.md` — the baseline column is already populated.

---

## § V-02 — the 16-item checklist

Each item is recorded **pass / fail with named evidence**. "Looks right" is not evidence; name the
feed row, the report section, the log query or the build id.

| # | Check | Pass condition (concrete) | Source | Evidence | Verdict |
|---|---|---|---|---|---|
| 1 | Workshop winners visible in the feed as they are chosen | The `workshop` stage shows candidate → cluster → critique → tournament → evolve rows, each with its own cost, and the winning questions are **readable in the feed while the run is still in flight** — not only after completion | D5, D12, V-02 | TBD (V-01) | TBD |
| 2 | No operator pause between trigger and completion | Zero `needs_input` / `needs_report_spec` states in the stage trace. **If the run parked (D-17):** the park mail arrived, the Resume click resumed from the last checkpoint **without re-dispatching a paid stage**, and the attempt counter did **not** increase (F-01/F-02/F-03). If it did not park, record "not exercised" — honest, not a failure | D-01, D5, 16-D-04 | TBD (V-01) | TBD |
| 3 | Every client-validated question is represented in the winners | The winners' `PARENT` set is a **superset** of `detect_explicit_questions(brief)`; zero client questions dropped | D4 | TBD (V-01) | TBD |
| 4 | At least one brief-vs-world flag surfaced | `### Where the brief did not match what the research found` renders with real content — or its absence is explainable in **one sentence**. (15.2-17 fixed the bug that made every real D4 flag resolve to an empty string and be dropped; this item is its live proof) | D4 | TBD (V-01) | TBD |
| 5 | Per-provider structured fact lists present | Fact lists for **≥3 of 4** streams; every D-14 full-distillation fallback is **named per provider** in both the feed and the verification report. A fallback degrades ONE STREAM, not the run — it must **not** appear in `degradation_reasons` | D8, D-14 | TBD (V-01) | TBD |
| 6 | The own-researcher stream contributed facts | Facts attributable to the fourth stream, with its **search count** and its **$ figure** both shown, the unit price sourced live from `/account.json`. A free-tier run reads "free-tier plan, $0.00/search" — **never a blank, never an estimate**. If `Nestor_SerpApi` was not seeded, this is a pre-marked expected miss | D10, D-16 | TBD (V-01) | TBD |
| 7 | Gate funnel recorded and reconciles | Every distilled claim lands in **exactly one** bucket, with `checked_incidentally` subtracted from bucket 2; the totals add up | D-10, G-08 | TBD (V-01) | TBD |
| 8 | A contradictory pair collided in a single skeptic session | At least one of the four recorded pairs reached **one** session, not two: Aral 16% vs 21% · LUKOIL NL 46 vs ~70/75 stations · Zeeland Carlyle vs TotalEnergies · Gunvor vs Carlyle. This is what the merge-before-gate reordering exists to produce | D9 | TBD (V-01) | TBD |
| 9 | Every `[n]` resolves, unresolved count stated in words | Every `[n]` in the report body resolves to a source, and the unresolved-anchor count is stated **in words** in both the verification report and the feed's closing summary. **This count is the D-05 emission-rate metric — record the number, not just pass/fail** (baseline: 28 stripped) | D-05, D-06, D13 | TBD (V-01) | TBD |
| 10 | Graded source list complete | Title, link, date and tier present for **every** entry, and single-source entries are marked as such | D13 | TBD (V-01) | TBD |
| 11 | Both D-08 sections render with real content | "Disputed & changed" and "What we could not establish" both render, and both are **byte-identical to what the pipeline data implies** — the writing model never touched them. (15.2-17 fixed the bug that discarded the fourth stream's `not_found` lines entirely) | D14, D-08 | TBD (V-01) | TBD |
| 12 | `verify_chain` GREEN on the new run's audit data | Chain verifies green. **A RED chain is a STOP — no sign-off**, regardless of every other item | Legal, EU AI Act Art. 12 (deadline 2026-08-02) | TBD (V-01) | TBD |
| 13 | Cost fully itemised per C1 | All four Anthropic token classes, `web_search`/`web_fetch` server fees, Gemini `usageMetadata`, and SerpApi unit × billable count. **Nothing silently missing** — only Gemini grounding may read "pending". Baseline defect for contrast: ~€5 displayed against ≈$43-45 real | C1, D-16, D-12 | TBD (V-01) | TBD |
| 14 | The feed matches the D15 design | Per-row cost, per-agent cards, narration and per-block summaries all present, with `replit view.png` as the visual bar | D12, D15 | TBD (V-01) | TBD |
| 15 | The terminal state is honest | `completed` **only** if nothing was lost; otherwise `completed_degraded` with **every reason named in words**; `parked` only where no honest deliverable was possible. A recovered retry and a `cost_pending` must **not** have degraded the run (D-12 carve-outs) | D-12, R6 | TBD (V-01) | TBD |
| 16 | Operator sign-off | The operator has read the new report **beside** `docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md` and signed § Sign-off below | V-01, V-02 | TBD (V-01) | TBD |

---

## § SC5 — the F-01 skeptic fix, proven by construction

The F-01 `reconciliation`-as-string fix shipped 2026-07-23 and **has never run live**.

**Pass condition: ZERO group-skeptic `'str' object has no attribute 'get'` failures** in the run's
logs, against the baseline's **24**, on an image built from a tree containing the fix.

This is proven by construction rather than by a dedicated test: the V-01 run either exhibits the
crash or it does not. Record the actual count, not a tick.

---

## § Deploy Record — executed 2026-07-27 (Task 3 COMPLETE)

Shared build SHA: **`20260727-085533`**. All four deployables rebuilt; no env-flip-only deploy.

| Service | Revision | Image | Traffic |
|---|---|---|---|
| `tribunal-worker` | `tribunal-worker-20260727-085533-090959` | `tribunal-worker:20260727-085533` | 100% |
| `tribunal-api` | `tribunal-api-20260727-085533-091105` | `tribunal-api:20260727-085533` | 100% |
| `nestor-api` | `nestor-api-00041-9xp` | `backend:20260727-085533` | 100% |
| `nestor-frontend` | `nestor-frontend-00027-zcr` | `frontend:20260727-085533` | 100% |

**Migration:** execution `tribunal-migrate-mjjp9`, job repinned to the `$SHA` api image FIRST. Log
carried the literal line verbatim — `exit(0)` was NOT accepted as proof:
`INFO [alembic.runtime.migration] Running upgrade 0012 -> 0013, 0013 D-13 fact metadata + research_gap + D-12 run states (Phase 15.2, ENGINE-11).`

**Secret bindings, confirmed by NAME (never a value):**

| Service | `ANTHROPIC_API_KEY` | `SERPAPI_API_KEY` |
|---|---|---|
| `tribunal-api` | `Nestor_Claude2` | `Nestor_SERP` |
| `tribunal-worker` | `Nestor_Claude2` | `Nestor_SERP` |
| `nestor-api` | `Nestor_Claude2` | *(absent — INTAKE-05, verified 0 matches)* |

**IAM applied and read back** (both were genuinely missing): `tribunal-run@` → `secretAccessor` on
`Nestor_SERP` (which had **no bindings at all**); `nestor-run@` → `secretAccessor` on `Nestor_Claude2`.

**Post-deploy checks:** `tribunal-api /readyz` → 200 · `nestor-api /readyz` → 200 with
`{"status":"ready","db":"ok"}` (real `SELECT 1`) · frontend → 307 (normal redirect) ·
`ci_no_run_research.sh` → exit 0 (INTAKE-05 green) · `cloudbuild.test-critical.yaml` re-run
POST-migration → **34 passed, 0 failed, 0 skipped**, including
`test_chain_green_after_15_2_additive_fields` — the EU AI Act Art. 12 chain is green on the
deployed schema.

**Observation, not a defect:** `/healthz` returns a **Google Front End** HTML 404 on both
`nestor-api` and `tribunal-api`, while a genuinely unknown path returns FastAPI's JSON
`{"detail":"Not Found"}` and the app's own OpenAPI lists `/healthz`. GFE intercepts that path
project-wide; the request never reaches the container. Pre-existing, unrelated to this phase (no
routing changed), and `/readyz` — the check the runbook specifies — is green.

**Sanctioned deviation:** plan 18's must-have "never silently repoint ANTHROPIC away from the secret
the live revision mounts" was deliberately deviated from. Self-healing from the live value was
proven to *ratify* drift rather than correct it (2026-07-21 manual switch to Claude2 → 2026-07-25
script deploy re-adopted the reverted `Nestor_Claude` → all three services found on `Nestor_Claude`
on 2026-07-27). The committed default now wins and divergence is merely reported. This honours the
rule's intent — preventing *silent* drift. Operator decision, 2026-07-27.

### TEMPORARY ANTHROPIC KEY IN FORCE — 2026-07-27 (operator decision)

`Nestor_Claude2` is NOT topped up. Until it is, both Tribunal services run on a **temporary burner
key** held in a separate secret, `Nestor_Claude_Temp` (own secret, so the committed
`Nestor_Claude2` default and the topped-up key stay untouched):

| Service | Revision | `ANTHROPIC_API_KEY` |
|---|---|---|
| `tribunal-worker` | `tribunal-worker-20260727-085533-093952` | **`Nestor_Claude_Temp`** |
| `tribunal-api` | `tribunal-api-20260727-085533-094029` | **`Nestor_Claude_Temp`** |
| `nestor-api` | `nestor-api-00041-9xp` | `Nestor_Claude2` *(NOT yet swapped — see below)* |

Applied via the `TRIBUNAL_ANTHROPIC_SECRET=Nestor_Claude_Temp` override, so **nothing committed
changed** — the scripts still default to `Nestor_Claude2` and the swap back is simply a redeploy
with no override.

**VALIDATED LIVE 2026-07-27:** a 1-token `POST /v1/messages` returned **HTTP 200**. So the key is
valid AND is **not subject to the tripped monthly cap**.

> **This unblocks V-01 before 2026-08-01.** The park was caused solely by the Anthropic cap; SerpApi
> (Starter, 835 searches left), Gemini and OpenAI were never blocked. V-01 can therefore run as soon
> as the operator chooses. The 2026-08-01 date is no longer a hard gate — it is now only the date
> `Nestor_Claude2` is expected back.

**THREE DEBTS THIS CREATES — do not let them go silent:**

1. **The burner key transited the assistant chat in plaintext.** Treat it as compromised: rotate it
   once testing is done, exactly like the Resend key already scoped to Phase 20 CLOSE-02. It is a
   burner, not a long-lived credential.
2. **Revert to `Nestor_Claude2` when topped up** — redeploy both Tribunal scripts with NO
   `TRIBUNAL_ANTHROPIC_SECRET` override, then confirm by name. A temporary key becoming the
   permanent one by forgetfulness is precisely the drift class this phase just fixed.
3. **`nestor-api` was NOT swapped** (the update was blocked mid-session). It still holds
   `Nestor_Claude2`, so the intake AI skills will fail while that secret is empty. Tribunal runs are
   unaffected. To swap:
   ```bash
   gcloud run services update nestor-api --region europe-west1 \
     --project project-cb01b861-cb4a-438d-b9a \
     --update-secrets ANTHROPIC_API_KEY=Nestor_Claude_Temp:latest
   ```
   (`--update-secrets`, never `--set-secrets` — the latter replaces the ENTIRE secret set and would
   drop `RESEND_API_KEY`, `DATABASE_URL` and the rest.)

**STILL UNVERIFIED before V-01:** the audit-blob redaction check (above) — the SerpApi key rides in
a query parameter, and an unredacted body freezes a live credential into the audit bucket under
7-year retention. Blocking, not advisory.

---

## § Deploy verification carried from the runbook

- `SERPAPI_API_KEY` bound **by secret name** on both `tribunal-worker` and `tribunal-api` (Step
  15.2.e) — the reference, never a value.
- Tribunal alembic head reads **`0013`** in `tribunal.tribunal_alembic_version`, and the migrate-job
  log carried the literal `Running upgrade 0012 -> 0013`.
- All four services on new revisions serving 100% of traffic, each pinned to an `@sha256:` digest.
- **The audit-blob redaction check — a security must-fix, not a nice-to-have.** The SerpApi key rides
  in a **query parameter**, so a naive audit writer would persist it in plaintext into the audit GCS
  bucket, where it would then be frozen under 7-year object retention. Pull one `provider="serpapi"`
  audit body from the audit bucket for the V-01 run and assert:
  - the request dict contains **no full URL** (no `serpapi.com/search?…` with a live query string), and
  - any `api_key`-like field reads exactly `[REDACTED]`.

  A failure here is a leaked credential in immutable storage. Treat it as blocking, rotate the key,
  and do not sign off.

---

## § Batched-in from earlier phases

Both were deferred because they need a **real live run** — run 4cbb5311 exists only as a pytest
fixture and was never seeded into the live DB (STATE.md, operator decision 2026-07-24):

- **Phase 15** — the deferred populated-surface browser UAT, SC1-SC4.
- **Phase 15.1** — the verdict / gate surfaces (`verdicts.superseded` and the accounting buckets).

Walk both in the same session, on the V-01 run, before releasing the operator.

---

## § Known engine gap to expect (not a V-02 failure)

**D17-1 — `own_research` is a declared stage that nothing writes.** The fourth stream works, but the
stage write was never added, so the feed shows a **permanently-pending `own_research` stage** on
every run. Pinned by a self-retiring test in 15.2-17. Do not record this as a V-02 #6 failure — #6
asks whether the stream *contributed facts*, not whether its stage row ticks over. Note it under
"gaps found" so it reaches `/gsd:plan-phase --gaps`.

---

## § Sign-off

**V-02 #16 is this block.** The phase is not complete until it is filled in by a human.

```
V-01 run id:       ______________________________
Read beside:       docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md
Verdict:           [ ] ACCEPTED   [ ] ACCEPTED WITH GAPS   [ ] REJECTED
Operator:          ______________________________
Date:              ______________________________
```

**Gaps found** — anything listed here feeds `/gsd:plan-phase --gaps`; an empty list means the phase
closes on V-03:

1. _______________________________________________
2. _______________________________________________
3. _______________________________________________

**Only after sign-off:** V-03 lands as a **separate commit**, removing only unreferenced old-path
code. `claim_distiller` (D-15), `detect_explicit_questions` and `extract_and_persist_citations` all
**survive**, with green tests.
