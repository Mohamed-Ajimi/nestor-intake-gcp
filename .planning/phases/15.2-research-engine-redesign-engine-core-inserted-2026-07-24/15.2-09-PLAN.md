---
phase: 15.2-research-engine-redesign-engine-core-inserted-2026-07-24
plan: 09
type: execute
wave: 2
depends_on: ["15.2-01", "15.2-02"]
files_modified:
  - tribunal/nestor_pulse_sdk/runs/schemas.py
  - tribunal/nestor_pulse_sdk/runs/api.py
  - tribunal/nestor_pulse_sdk/runs/worker.py
  - tribunal/nestor_pulse_sdk/tests/test_fail_loud.py
  - tribunal/nestor_pulse_sdk/tests/test_status_gates.py
  - backend/app/research/run_status.py
  - backend/app/research/run_task.py
  - backend/app/api/research_routes.py
  - frontend/src/components/intake/ResearchRunProgress.tsx
  - frontend/src/lib/api/research.ts
  - frontend/src/locales/en/intake.json
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
autonomous: true
requirements: [ENGINE-11]

must_haves:
  truths:
    - "A superadmin can download the report AND the research bundle of a completed_degraded run exactly as of a completed run (D-09)."
    - "A superadmin can read the research bundle and the verification report of a parked run, but the report download still 409s — there is no report yet (D-09)."
    - "A cross-tenant run_id still reads as 404 — never 403 — on every widened endpoint (ASVS V4)."
    - "A run whose output fell short is written to the DB as completed_degraded, not completed, and carries its reasons in words (D-12)."
    - "A completed_degraded run shows a finished stage list, not a permanently-spinning stage (F2)."
    - "A completed_degraded run renders as a finished-with-caveats card — never as the cancelled card — and keeps the raw-output download, the verification-report button and the frozen feed."
    - "Every terminal-status vocabulary site recognises completed_degraded: RunStatus, the intake poll driver, the intake SSE handler, the SSE client and the UI terminal set."
    - "Recovered retries and cost_pending never make a run degraded (D-12)."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/runs/schemas.py"
      provides: "RunStatus 9-literal vocabulary + report_readable()/bundle_readable() — the ONE place to audit"
      contains: "def report_readable"
    - path: "tribunal/nestor_pulse_sdk/runs/api.py"
      provides: "all four gates + the F2 display line routed through the predicates"
      contains: "report_readable("
    - path: "tribunal/nestor_pulse_sdk/runs/worker.py"
      provides: "completion UPDATE writes a computed terminal status instead of the literal 'completed'"
      contains: ":final_status"
    - path: "tribunal/nestor_pulse_sdk/tests/test_status_gates.py"
      provides: "predicate truth tables, the four-site source gate, F1/F2 regressions, the cross-tenant 404 regression"
    - path: "backend/app/research/run_status.py"
      provides: "the intake-side mirror of the same vocabulary + predicate, imported by run_task and research_routes"
      contains: "def is_research_success"
    - path: "frontend/src/components/intake/ResearchRunProgress.tsx"
      provides: "completed_degraded is terminal AND renders the finished-with-caveats card"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/runs/api.py"
      to: "tribunal/nestor_pulse_sdk/runs/schemas.py"
      via: "import report_readable, bundle_readable"
      pattern: "report_readable|bundle_readable"
    - from: "tribunal/nestor_pulse_sdk/runs/worker.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/reliability.py"
      via: "terminal_state() from 15.2-02"
      pattern: "terminal_state\\("
    - from: "tribunal/nestor_pulse_sdk/runs/worker.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      via: "top-level result key degradation_reasons — produced by plan 15.2-07's ONE run-scoped accumulator (wave 4, lands after this plan; no producer at merge time, so the [] default keeps behaviour unchanged)"
      pattern: "result\\.get\\(\"degradation_reasons\""
    - from: "backend/app/api/research_routes.py"
      to: "backend/app/research/run_status.py"
      via: "import is_research_success, RESEARCH_TERMINAL"
      pattern: "run_status import"
    - from: "frontend/src/components/intake/ResearchRunProgress.tsx"
      to: "frontend/src/locales/*/intake.json"
      via: "t(\"research.degradedTitle\") / t(\"research.degradedBody\")"
      pattern: "research\\.degraded"
---

<objective>
D-09's one shared status predicate and D-12's four-state vocabulary.

**What this plan REUSES — read this before writing a line.** Nothing new is invented here.
The four gate sites are already three-line-identical, so replacing them is a mechanical
substitution, not a redesign:

| Reused | Where | This plan's relationship to it |
|---|---|---|
| `terminal_state()` (R4/R6/D-17 truth table) | `pipeline/tribunal/reliability.py`, built by **15.2-02** | **CALL it.** Do NOT write a second degradation rule. |
| `ck_run_status` CHECK + `db/models/run.py` literal | alembic **0013**, shipped by **15.2-01** | **Already done.** This plan must NOT touch `db/models/run.py` or write a migration. |
| `RunStatus` sync-rule comment | `runs/schemas.py:32-37` (CR-03 failure mode) | **Extend the existing comment**, keep its style. |
| `get_db_session` → `set_tenant_context` → FORCE RLS | every handler in `runs/api.py` | **Untouched.** The predicate changes *status* scope only, never *tenant* scope. |
| `GET /{run_id}/verification` | `runs/api.py:854` | **Already gate-free and already correct for `parked` (F1).** Add a regression test and a docstring note — do NOT add a gate, do NOT "fix" it. |
| `claim_distiller`, the feed model, `StageDetailItem`, `"retry"` status | elsewhere in the SDK | Out of scope. No feed-model work in this plan. |
| `verification_summary` funnel keys + `build_verification_summary()` | `verification/report.py`, `loader.py:87` | **Do NOT rename or add funnel keys** (Pitfall 3: two full-dict-equality assertions). The worker only *adds* a sibling key on the write side. |

Purpose: today four inline `run.status != "completed"` comparisons are the entire access
model for a ~$45 run's output. G-10 deferred the degraded marker precisely because those four
places drift. This plan collapses them to one named, tested predicate and promotes the marker
into a real terminal state everywhere the vocabulary lives.

Output: two predicates + a 9-literal `RunStatus` in `runs/schemas.py`; four call sites plus
the F2 display line in `runs/api.py`; a computed terminal status in `runs/worker.py`; one new
intake-side mirror module; the intake poll driver, the intake SSE handler, the SSE client and
the UI terminal card all speaking the same four states; `test_status_gates.py`; and an
**updated** (never deleted) `test_fail_loud.py:95`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-CONTEXT.md
@.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-PLAN-OUTLINE.md

<interfaces>
<!-- Contracts the executor needs. Do NOT go exploring for these. -->

FROM 15.2-07 (`pipeline/tribunal/pipeline.py`) — lands in **wave 4, AFTER this plan**:

    # TribunalPipeline.run() returns a dict carrying, additively:
    #   result["degradation_reasons"]: list[str]   <- the key runs/worker.py reads (Task 2)
    # Produced by the ONE run-scoped accumulator plan 07 declares at the top of run()
    # (`degradation_reasons: list[str] = []` + the `_note_degradation(reason)` closure) and
    # published from _write_final_report, so both the zero-touch and the resume-from-cache
    # paths carry it. Plan 15.2-08 (wave 5) normalises the SAME list onto the funnel as
    # `verification_summary["degradation_reasons"]`; the worker reads the TOP-LEVEL key.
    # Because 07 is wave 4 and this plan is wave 2, the key has no producer at merge time —
    # the worker's `.get(...)` default of [] keeps today's behaviour unchanged. Intentional.

FROM 15.2-02 (`nestor_pulse_sdk/pipeline/tribunal/reliability.py`) — already merged in Wave 1:

    def terminal_state(*, streams_lost: int, streams_total: int,
                       verify_ran: bool, synthesis_ran: bool,
                       hard_wall: bool, degradation_reasons: list[str]) -> str
    # returns "parked" | "completed_degraded" | "completed"

FROM 15.2-01 (alembic 0013 + `db/models/run.py`) — already merged in Wave 1:
    ck_run_status now permits 9 literals, including "completed_degraded" and "parked".

CURRENT (`nestor_pulse_sdk/runs/schemas.py:38-41`):
    RunStatus = Literal["queued","running","completed","failed","cancelled",
                        "needs_input","needs_report_spec"]

CURRENT gate shape, identical at `runs/api.py` 587 / 691 / 947 / 1046:
    587, 691 — a FILTER inside a loop: `if r.status != "completed": continue`
    947  — a GUARD: `if run.status != "completed": raise HTTPException(409, "report not available yet")`
    1046 — a GUARD: `if run.status != "completed": raise HTTPException(409, "bundle not available yet")`

CURRENT display line (`runs/api.py:847-849`, inside `get_run_metrics`):
    current_stage=("done" if run.status == "completed" else run.current_stage)

CURRENT intake vocabulary:
    backend/app/research/run_task.py:72   _RESEARCH_TERMINAL = {"completed","failed","cancelled"}
    backend/app/api/research_routes.py:632 RESEARCH_TERMINAL  = {"completed","failed","cancelled"}
    frontend/src/lib/api/research.ts:210   RESEARCH_TERMINAL  = new Set([...])
    frontend/src/components/intake/ResearchRunProgress.tsx:40  RESEARCH_TERMINAL = new Set([...])
</interfaces>
</context>

<discovered_site_drift>
**The outline's site list is INCOMPLETE. Verified against the working tree at `c6440db`.**

The outline states the frontend delta is one line and names six vocabulary sites. Six further
`"completed"`-literal sites exist that are load-bearing for D-09/D-12. They are named here so
the executor treats them as planned work, not as scope creep:

| # | Site | Why it is in this plan |
|---|---|---|
| **D1** | `frontend/src/lib/api/research.ts:210` `RESEARCH_TERMINAL` | The SSE **client** stop condition. Left stale, a `completed_degraded` run's stream is closed by the server, the client reads it as a drop and enters the `retry()` reconnect loop until unmount. Same one-line class as `ResearchRunProgress.tsx:40`. |
| **D2** | `frontend/src/components/intake/ResearchRunProgress.tsx:584` `if (status === "completed")` | Adding `completed_degraded` to the terminal set **alone** routes a degraded run past this branch into the `// failed \| cancelled` card at 649 — grey `AlertTriangle`, `research.cancelledTitle`, and **no** raw-output download, **no** verification-report button, **no** frozen feed. That is the exact opposite of D-09 and a silent lie under D-12. |
| **D3** | `backend/app/api/research_routes.py:632` `RESEARCH_TERMINAL` | The SSE **handler** close condition. Left stale, a degraded run's stream runs to the 10-min `MAX_STREAM_SECONDS` cap. |
| **D4** | `backend/app/api/research_routes.py:367` `run.status != "completed" or run.chain_status != "verified"` | The intake-side raw-output download gate. This is the *client-visible* mirror of D-09's `/report` + `/research-bundle` gates; leaving it means a degraded run is inspectable in Tribunal but not downloadable from the intake UI. |
| **D5** | `backend/app/research/run_task.py:465` and `:481` | `if metrics.get("status") == "completed"` gates `build_completion` (bundle build + upload) and `finalize_completed` + the completion mail. A degraded run would reach a terminal state with **no bundle, no report row and no mail**. |
| **D6** | `backend/app/research/run_task.py:181` | `status=metrics.get("status", "completed")` — carries the Tribunal literal verbatim, so it already works; verify only, no change. |

**`parked` is deliberately NOT added** to any terminal or stream-closing set in this plan.
`parked` needs the Resume button, the `render_research_parked` mail and the F-02 free-resume
rule — **all owned by 15.2-16**. Adding `parked` to a terminal set here would ship a dead-end
UI state. `parked` DOES get: the `RunStatus` literal (CR-03 — a read of a parked run must not
500), `bundle_readable` (D-09), and the `finalize_failed` clamp (so a parked run is not
mislabelled `failed`).
</discovered_site_drift>

<constraints>
1. **Do NOT touch `db/models/run.py` or write a migration.** 15.2-01 owns `ck_run_status`.
2. **Do NOT edit `tribunal/cloudbuild.test-engine.yaml`.** 15.2-02 owns it and has already
   pre-listed `test_status_gates.py`.
3. **Do NOT modify `build_verification_summary()` or `RECORDED_FUNNEL_COUNTS`** (Pitfall 3 —
   `loader.py:239-245` compares by full dict equality, two tests).
4. **No new packages.** Nothing in this plan needs one.
5. **No audit-payload change.** No field is added to `canonical_json`; `verify_chain` and
   `test_hash_chain_replay.py` are untouched by construction (EU AI Act Art. 12, 2026-08-02).
6. **No local Python or Docker.** Author by construction; every Python command runs in Cloud
   Build. Node/npm commands DO run locally.
7. **No SerpAPI / research-invocation references** may be added to `backend/app/**` or
   `frontend/src` (INTAKE-05, `ci_no_run_research.sh`).
8. Frontend package manager: **`npm ci`**, never `npm install` (a `package-lock.json` IS
   committed; CLAUDE.md's "no lockfile" line is stale).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: The four-state vocabulary and the ONE shared predicate</name>

  <read_first>
    - tribunal/nestor_pulse_sdk/runs/schemas.py (lines 1-60 — the RunStatus block and its CR-03 sync-rule comment at 32-37)
    - tribunal/nestor_pulse_sdk/tests/test_fail_loud.py (lines 60-120 — the G-10 negative test at :95 that this task updates)
    - tribunal/nestor_pulse_sdk/db/models/run.py (the ck_run_status block at 118-122 — READ ONLY, to copy the literal set 15.2-01 shipped)
    - tribunal/nestor_pulse_sdk/runs/api.py (lines 854-885 — get_run_verification, the F1 endpoint the predicate docstring must name)
  </read_first>

  <files>tribunal/nestor_pulse_sdk/runs/schemas.py, tribunal/nestor_pulse_sdk/tests/test_fail_loud.py</files>

  <action>
Extend `RunStatus` at `runs/schemas.py:38-41` to nine literals by adding `"completed_degraded"`
and `"parked"`. Place `"completed_degraded"` immediately after `"completed"` and `"parked"`
immediately after `"cancelled"`, so the literal order reads as the lifecycle. Verify the
resulting set is character-for-character the same nine literals 15.2-01 put in
`db/models/run.py`'s `ck_run_status`; if it is not, stop and report the mismatch rather than
inventing a reconciliation.

Extend the existing sync-rule comment at `schemas.py:32-37` in its own style (do not rewrite
it): state that `completed_degraded` means the output fell short with every reason named
(D-12), that `parked` means no honest deliverable was possible and a superadmin click resumes
it (D-17/F-01), and that `completed` continues to mean CLEAN — a run that lost anything is
`completed_degraded`.

Immediately below `RunStatus`, add the D-09 access model as module-level constants plus two
predicates. Define `REPORT_READABLE_STATUSES: frozenset[str]` = `completed`,
`completed_degraded`. Define `BUNDLE_READABLE_STATUSES: frozenset[str]` =
`REPORT_READABLE_STATUSES | {"parked"}`. Define `def report_readable(status: str) -> bool` and
`def bundle_readable(status: str) -> bool`, each a membership test against its frozenset, each
accepting a plain `str` (callers pass `run.status` straight off the ORM row, which is `str`,
not the `Literal`).

Write the predicate docstrings as the single audit surface D-09 asks for. `report_readable`
must state: this is the gate for the report body and for anything derived from it — the report
download, the blind-critique filter and the content-compare filter; `completed_degraded` gets
everything `completed` gets (G-10 — never lock the superadmin out of a ~$45 run's output
because one stage was gutted); `parked` is FALSE because there is no report yet. It must also
state the two invariants a future editor must not break: (a) widening a *status* gate never
widens *tenant* scope — every caller still reads through `get_db_session` →
`set_tenant_context` → FORCE RLS, and a cross-tenant `run_id` stays a **404, never a 403**
(the deliberate non-distinguishability documented at `runs/api.py:869-873`); and (b) **F1** —
`GET /{run_id}/verification` (router `runs/api.py:854`) is deliberately gate-free and already
correct for `parked`, because a parked run must be able to show *why* it stopped; name it by
path and say "do not add a gate here", and name
`tests/test_status_gates.py::test_verification_endpoint_has_no_status_gate` as the test that
pins it. `bundle_readable` must state that `parked` is TRUE (D-09: inspection only) and that
the bundle handler still returns EXACTLY `cleaned_reports` — the D-01 exclusion of
`rejected_claims` is unaffected by this widening.

In `tests/test_fail_loud.py`, **update** the test currently named
`test_degraded_run_still_reports_completed_status` at :76-100. Do not delete it — it is G-10's
deliberate negative test and 15.2 is the phase that promotes the marker. Rename it to
`test_four_terminal_states_and_completed_still_means_clean`. Keep the existing G-10 rationale
paragraph in the docstring verbatim and append a sentence recording that 15.2/D-12 promoted
the marker and that this is that update. Replace the body: assert `set(get_args(RunStatus))`
equals the nine literals; assert `"completed_degraded" in statuses`; assert `"parked" in
statuses`; keep `assert "degraded" not in statuses` (the bare word is still not a status);
and keep the `"completed" in statuses` assertion, rewording its message to "completed still
means CLEAN — a run that lost anything is completed_degraded (D-12)". Leave every other test
in the file untouched.
  </action>

  <acceptance_criteria>
    - `python -c "from typing import get_args; from nestor_pulse_sdk.runs.schemas import RunStatus; print(sorted(get_args(RunStatus)))"` prints exactly: `['cancelled', 'completed', 'completed_degraded', 'failed', 'needs_input', 'needs_report_spec', 'parked', 'queued', 'running']`
    - `report_readable("completed") is True`, `report_readable("completed_degraded") is True`, `report_readable("parked") is False`, `report_readable("running") is False`
    - `bundle_readable("parked") is True`, `bundle_readable("completed_degraded") is True`, `bundle_readable("failed") is False`
    - The `report_readable` docstring contains the literal substrings `404`, `403`, `/verification` and `test_status_gates.py`
    - `grep -c "def test_degraded_run_still_reports_completed_status" tribunal/nestor_pulse_sdk/tests/test_fail_loud.py` returns `0` and `grep -c "def test_four_terminal_states_and_completed_still_means_clean" tribunal/nestor_pulse_sdk/tests/test_fail_loud.py` returns `1`
    - `tribunal/nestor_pulse_sdk/db/models/run.py` shows **no** diff against wave 1 (`git diff 15.2-01-merge -- tribunal/nestor_pulse_sdk/db/models/run.py` is empty)
  </acceptance_criteria>

  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>

  <done>`RunStatus` carries nine literals matching `ck_run_status`; `report_readable`/`bundle_readable` exist with the D-09 access model and the F1 + 404-not-403 notes in their docstrings; `test_fail_loud.py`'s G-10 test is updated in place and green.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the predicate at all four sites, fix F2, and make the worker write an honest terminal status</name>

  <read_first>
    - tribunal/nestor_pulse_sdk/runs/api.py (lines 26-40 for the existing schemas import block; 560-600 site 587; 680-700 site 691; 826-900 the F2 display line + get_run_verification; 931-960 site 947; 1016-1060 site 1046)
    - tribunal/nestor_pulse_sdk/runs/schemas.py (the predicates added in Task 1)
    - tribunal/nestor_pulse_sdk/runs/worker.py (lines 178-240 — the funnel read, the run_verification_degraded log, and the SUCCESS completion UPDATE)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/reliability.py (terminal_state — the signature and truth table 15.2-02 shipped)
    - tribunal/nestor_pulse_sdk/tests/test_stage_schema.py (lines 1-70 — the pure source-scanning test idiom: resolve the module path through `Path(module.__file__)`, never a repo-root relative path)
    - tribunal/nestor_pulse_sdk/tests/test_research_bundle_endpoint.py (lines 95-225 — the existing DB-backed 409/404/cross-tenant contract this task must not break)
  </read_first>

  <files>tribunal/nestor_pulse_sdk/runs/api.py, tribunal/nestor_pulse_sdk/runs/worker.py, tribunal/nestor_pulse_sdk/tests/test_status_gates.py</files>

  <action>
**`runs/api.py` — five edits, all mechanical.** Add `report_readable` and `bundle_readable` to
the existing `from nestor_pulse_sdk.runs.schemas import (...)` block at line 26.

Site 587 (`create_comparison_critique`) and site 691 (the content-compare handler): the loop
body `if r.status != "completed": continue` becomes `if not report_readable(r.status):
continue`. These are FILTERS, not guards — keep the `continue`, keep the surrounding
`latest_completed` dict build and the `len(...) != 2` → 409 check untouched. Rename nothing:
the local is still `latest_completed`.

Site 947 (`get_run_report`): `if run.status != "completed"` becomes `if not
report_readable(run.status)`. Keep the 409 status code and keep the message string exactly
`"report not available yet"` — the intake seam and `test_research_bundle_endpoint.py`'s
sibling tests read these. Keep the preceding `scalar_one_or_none()` → `raise
HTTPException(404, "run not found")` block byte-identical: that is the cross-tenant
non-distinguishability, and it must not move above or below the widened gate.

Site 1046 (`get_run_research_bundle`): `if run.status != "completed"` becomes `if not
bundle_readable(run.status)`. Keep the 409 message `"bundle not available yet"` and the
subsequent `no cached research for this run` 409 — a `parked` run with no `synthesis_cache`
Output still 409s there, which is correct and is what makes `parked` inspection-only rather
than a crash. Update the handler docstring's "409 until the run is `completed`" sentence to
name the predicate and state that `parked` is readable by design (D-09).

**F2** at `runs/api.py:847-849` inside `get_run_metrics`: the expression becomes `current_stage=("done" if report_readable(run.status) else run.current_stage)`. Update the
comment above it (currently "Completed/terminal runs report 'done'") to say that
`completed_degraded` reports `done` too, and why: otherwise the feed shows a permanently
spinning stage on exactly the runs an operator most needs to read.

**`GET /{run_id}/verification` (router 854): change NOTHING.** Add one sentence to its existing
docstring stating that this endpoint is deliberately gate-free — a `parked` run must be able
to show why it stopped (D-09/F1) — and that
`tests/test_status_gates.py::test_verification_endpoint_has_no_status_gate` pins it.

**Comment hygiene (load-bearing for Task 2's source test):** anywhere prose in `runs/api.py`
needs to refer to the old comparison, write it WITHOUT quotes — `status != completed` — so the
test's regex for the code pattern cannot be satisfied by a comment.

**`runs/worker.py` — the SUCCESS-path terminal status.** Add
`from nestor_pulse_sdk.pipeline.tribunal.reliability import terminal_state` to the module
imports. Immediately after the existing `run_verification_degraded` `log.warning` block
(around line 199, before the `# SUCCESS: update status to completed` comment), build the
degradation reason list and compute the final status:

Read `_reasons` as a list of `str` from `result.get("degradation_reasons")` when `result` is a
dict, defaulting to an empty list. **That top-level key is the contract, and its producer is
`TribunalPipeline.run()` in plan 15.2-07 (wave 4).** Plan 07 declares the run's ONE
`degradation_reasons` accumulator plus a `_note_degradation` closure at the top of `run()`, and
publishes it on exactly two surfaces: this **top-level** result key (yours) and
`synthesis_bundle["verification"]["degradation_reasons"]`, which plan 15.2-08 (wave 5) normalises onto
`run.verification_summary`. Do **not** read `result["verification_summary"]["degradation_reasons"]`
instead — that nested copy is plan 08's funnel surfacing and does not exist yet either.

This plan is **wave 2** and plan 07 is **wave 4**, so at the moment you land, nothing writes the key
yet: `_reasons` is `[]` from the `result` side and today's behaviour is therefore unchanged, exactly as
before. **That is intentional and correct, not a bug** — say so in a comment naming plan 15.2-07 as the
producer and plans 15.2-11 / 12 / 14 / 16 as the stages that append reasons through
`_note_degradation`, so a later reader does not "fix" a read that has no writer yet. Until then the only
reason this branch adds is the bucket-3 sentence below, which is computed here from
`verification_summary`.

When `_has_summary` and `_summary.get("should_have_been_checked")` is truthy, append a worded reason
naming the count — a full sentence a human reads, following the `verification/report.py:184-190`
register, not a code. Then set `_final_status = terminal_state(...)` with: `streams_lost` from
`result.get("streams_lost")` defaulting to `0`; `streams_total` from
`result.get("streams_total")` defaulting to `1`; `verify_ran` from `result.get("verify_ran")`
defaulting to `True`; `synthesis_ran=True`; `hard_wall=False`; and `degradation_reasons=_reasons`.
Add a comment stating that `synthesis_ran` and `hard_wall` are pinned on this branch because
this IS the success branch — the park path (F6's `InsufficientProvidersError` catch and the
hard-wall park) is **15.2-16's** work, and pinning them here keeps `terminal_state()` from
returning `parked` down a path that has a report in hand.

When `_reasons` is non-empty, persist them: copy `_summary` into a new dict (never mutate the
pipeline's dict in place), set key `degradation_reasons` to `_reasons`, and set `_has_summary`
to `True` so the existing `verification_summary = CAST(:vsummary AS JSONB)` fragment writes it.
Add a comment recording that this is a **sibling** key added on the WRITE side only:
`build_verification_summary()` and `RECORDED_FUNNEL_COUNTS` are NOT touched (Pitfall 3), and
that when there is no funnel the row carries `degradation_reasons` alone — `_accounting()`
returns `None` on missing funnel keys, so the report still honestly says "no gate data" while
naming the degradation.

Change the UPDATE statement's `status='completed'` to `status=:final_status` and add
`_params["final_status"] = _final_status`. **Keep `WHERE id=:id AND status='running'` exactly
as it is** — that guard is what makes a user cancel win, and it is a `WHERE`-clause literal,
not the written status. Do not touch the `needs_input` UPDATE (~line 145) or the
`needs_report_spec` UPDATE (~line 168).

**`tests/test_status_gates.py` — new, pure: no DB, no network, no LLM, no mocking library, no
API key.** Open with a module docstring in the `test_stage_schema.py` register explaining what
it pins and how it is invoked in Cloud Build. Resolve module source with
`Path(<module>.__file__).read_text(encoding="utf-8")` — never a repo-root relative path,
because the build context ships only the `tribunal/` subtree (Pitfall 8). Write these tests:

`test_report_readable_truth_table` — iterate all nine `get_args(RunStatus)` values; assert
`report_readable` is `True` for exactly `completed` and `completed_degraded`.

`test_bundle_readable_truth_table` — assert `True` for exactly `completed`,
`completed_degraded`, `parked`.

`test_parked_gets_inspection_not_the_report` — assert `bundle_readable("parked") and not
report_readable("parked")`, with an assertion message stating the D-09 rationale in words.

`test_all_four_gates_route_through_the_predicate` — read `runs/api.py` as text; assert
`re.findall(r'status\s*!=\s*"completed"', src)` is empty; assert
`src.count("report_readable(")` is at least 4 (three call sites plus the F2 line) and
`src.count("bundle_readable(")` is at least 1. State in the docstring that prose references to
the old comparison must be written unquoted so this regex stays honest.

`test_verification_endpoint_has_no_status_gate` (**F1**) — take
`inspect.getsource(get_run_verification)`; assert it contains neither `report_readable` nor
`bundle_readable` nor `409`; assert its docstring mentions `parked`. Assertion message: a
parked run must be able to show why it stopped; adding a gate here is the regression.

`test_metrics_reports_done_for_a_degraded_run` (**F2**) — assert `runs/api.py` source contains
`"done" if report_readable(run.status)` and does NOT contain
`"done" if run.status == "completed"`.

`test_widened_handlers_keep_the_404_non_distinguishability` (**ASVS V4**) — for each of
`get_run_report` and `get_run_research_bundle`, take `inspect.getsource`; assert it contains
`scalar_one_or_none()`, contains `HTTPException(404`, and contains no `403`. Assertion
message: a cross-tenant run_id is invisible, not forbidden.

`test_worker_writes_the_status_terminal_state_computed` — read `runs/worker.py` as text; assert
it contains `status=:final_status` and `terminal_state(`; assert
`"UPDATE run SET status='completed'"` does not appear; assert `WHERE id=:id AND
status='running'` still appears (the cancel guard survived).

`test_recovered_retries_and_cost_pending_never_degrade` (**D-12**) — call `terminal_state` with
an empty `degradation_reasons` and today's defaults and assert it returns `completed`; assert
`runs/worker.py` source contains neither `cost_pending` nor the substring `"retry"` inside the
reason-building region, so a recovered retry or a pending Gemini grounding fee can never enter
the reason list.
  </action>

  <acceptance_criteria>
    - `grep -n 'status != "completed"' tribunal/nestor_pulse_sdk/runs/api.py` returns nothing
    - `grep -c 'report_readable(' tribunal/nestor_pulse_sdk/runs/api.py` returns `>= 4`; `grep -c 'bundle_readable(' tribunal/nestor_pulse_sdk/runs/api.py` returns `>= 1`
    - `grep -n '"done" if report_readable(run.status)' tribunal/nestor_pulse_sdk/runs/api.py` returns exactly one line
    - `grep -n "UPDATE run SET status='completed'" tribunal/nestor_pulse_sdk/runs/worker.py` returns nothing; `grep -c "status=:final_status" tribunal/nestor_pulse_sdk/runs/worker.py` returns `1`; `grep -c "WHERE id=:id AND status='running'" tribunal/nestor_pulse_sdk/runs/worker.py` returns `>= 1`
    - `grep -c 'result.get("degradation_reasons"' tribunal/nestor_pulse_sdk/runs/worker.py` returns `1`
      — the worker reads the **top-level** result key, which is the surface plan 15.2-07 publishes — and
      `grep -c 'verification_summary"\]\["degradation_reasons' tribunal/nestor_pulse_sdk/runs/worker.py`
      returns `0`: never the nested funnel copy, which is plan 15.2-08's read-side surfacing of the same
      list. One producer, two surfaces, and this side reads the top-level one.
    - `git diff --stat -- tribunal/nestor_pulse_sdk/verification/report.py tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py` is empty (Pitfall 3 respected)
    - The engine gate collects and passes `test_status_gates.py` — its 9 tests all green
    - `test_research_bundle_endpoint.py` and `test_verification_report_endpoint.py` are unchanged and still green in the critical gate
  </acceptance_criteria>

  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT" && gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>

  <done>All four gates plus the F2 display line go through the two predicates; zero `status != "completed"` comparisons remain in `runs/api.py`; the verification endpoint is unchanged but documented and pinned; the worker writes a `terminal_state()`-computed status with its reasons persisted as a sibling key; `test_status_gates.py` is green in the engine gate.</done>
</task>

<task type="auto">
  <name>Task 3: Propagate the vocabulary to the intake seam, the SSE bridge and the operator UI</name>

  <read_first>
    - backend/app/research/run_task.py (lines 15-80 for the module docstring + `_RESEARCH_TERMINAL`; 165-215 for `finalize_completed` and the `finalize_failed` clamp; 445-505 for the poll loop terminal branch and `write_fn`)
    - backend/app/api/research_routes.py (lines 335-375 for the raw-output download gate; 620-715 for the SSE handler and its `RESEARCH_TERMINAL`)
    - frontend/src/lib/api/research.ts (lines 1-30 for the header comment naming the terminal set; 200-215; 390-425 for the stream stop condition and `retry()`)
    - frontend/src/components/intake/ResearchRunProgress.tsx (lines 30-45; 555-700 — the whole terminal-card region: the `isTerminal` branch, the `status === "completed"` success card with RawOutputControls + the verification button + AgentFeed, and the failed/cancelled card)
    - frontend/src/locales/en/intake.json (the `research` namespace around lines 495-510)
    - frontend/scripts/i18n-audit.mjs (lines 1-30 — CHECK A 3-way parity and CHECK B literal-key resolution are HARD gates)
  </read_first>

  <files>backend/app/research/run_status.py, backend/app/research/run_task.py, backend/app/api/research_routes.py, frontend/src/components/intake/ResearchRunProgress.tsx, frontend/src/lib/api/research.ts, frontend/src/locales/en/intake.json, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json</files>

  <action>
**New `backend/app/research/run_status.py`** — the intake-side mirror of D-09's one-place-to-audit
rule, so `run_task.py` and `research_routes.py` stop each carrying their own literal set. Keep
it tiny and dependency-free (no SQLAlchemy, no FastAPI imports). Export:
`RESEARCH_SUCCESS = frozenset({"completed", "completed_degraded"})`;
`RESEARCH_TERMINAL = RESEARCH_SUCCESS | frozenset({"failed", "cancelled"})`;
`def is_research_success(status: str | None) -> bool`; and
`def is_research_terminal(status: str | None) -> bool`. Both must tolerate `None` and return
`False` — `metrics.get("status")` can be absent.

The module docstring must state four things: (1) these literals are the Tribunal run-status
contract carried VERBATIM across the seam (the D-05 boundary) and are NEVER the skill-run
`{"succeeded","failed"}` vocabulary (16-RESEARCH Pitfall 3); (2) `completed_degraded` gets
everything `completed` gets — bundle, report row, download and mail (D-09/G-10); (3) `parked`
is deliberately absent from both sets — the poll driver must keep polling a resumable run, and
the Resume button, the `render_research_parked` mail and F-02's free-resume rule are
**15.2-16's** work; adding `parked` here before that lands ships a dead-end state; (4) the
tribunal-side source of truth is `nestor_pulse_sdk/runs/schemas.py`'s `RunStatus` and its
`report_readable`/`bundle_readable` predicates — keep the two in sync.

**`backend/app/research/run_task.py`** — five edits. Delete the local `_RESEARCH_TERMINAL` at
:72 and import `RESEARCH_TERMINAL`, `is_research_success` from `app.research.run_status`; keep
the poll-loop call site at :454 reading `metrics.get("status") in RESEARCH_TERMINAL` so the
diff there is zero. Update the module docstring's terminal-set sentence at ~:23 to name the
four states and point at `run_status.py`. At :465 replace `if metrics.get("status") ==
"completed":` with `if is_research_success(metrics.get("status")):` — a degraded run MUST get
`build_completion` (the D-06 chain check, the D-01-scrubbed bundle fetch, the zip and the GCS
upload). At :481 in `write_fn`, make the same substitution — a degraded run MUST get
`finalize_completed` plus the normal completion mail; there is no degraded mail variant in this
plan (mail copy for park/degrade is 15.2-16's `render_research_parked` work), and gating the
mail would silently drop the operator's only notification. In `finalize_failed` (~:209) widen
the clamp from `if status not in {"failed", "cancelled"}` to `if status not in {"failed",
"cancelled", "parked"}` and add a comment: a parked run is not a failure — mislabelling it
`failed` would destroy the resume affordance 15.2-16 builds. Leave :181
(`status=metrics.get("status", "completed")`) unchanged — it already carries the Tribunal
literal verbatim; add a one-line comment saying so, so a later reader does not "fix" it.

**`backend/app/api/research_routes.py`** — two edits. Delete the module-level
`RESEARCH_TERMINAL` at :632 and import it from `app.research.run_status`, keeping the
`:data:` docstring cross-reference and both `view["status"] in RESEARCH_TERMINAL` call sites
byte-identical. At :367 change the availability gate's status half only: `if not
is_research_success(run.status) or run.chain_status != "verified":`. **The `chain_status !=
"verified"` conjunct, the superadmin-first 404 at :355, and the existence-hidden 404s for a
missing intake/run all stay exactly as they are** — this widening is a *status* widening, not
an authorization change. Update the docstring bullet that reads ``status != "completed"`` to
name the predicate and state that a degraded run is downloadable (D-09).

**`frontend/src/lib/api/research.ts`** — two edits (D1). Add `"completed_degraded"` to the
`RESEARCH_TERMINAL` set at :210 and to the header comment's literal list at :11. Extend the
:206-209 docstring to say `completed_degraded` is terminal for the stream and that `parked` is
deliberately absent pending 15.2-16.

**`frontend/src/components/intake/ResearchRunProgress.tsx`** — two edits (the line-40 change
plus D2). Add `"completed_degraded"` to `RESEARCH_TERMINAL` at :40. Then, inside the
`if (isTerminal)` block, change the success-branch condition at :584 from `if (status ===
"completed")` to `if (status === "completed" || status === "completed_degraded")`, and inside
that branch derive `const isDegraded = status === "completed_degraded";`. Use `isDegraded` in
exactly four places and nowhere else: the left border colour (keep `#DFF940` when clean, use
`#D97706` when degraded), the icon (`CheckCircle2` when clean, the already-imported
`AlertTriangle` when degraded), the title (`t("research.completedTitle")` vs
`t("research.degradedTitle")`) and the body (`t("research.completedBody")` vs
`t("research.degradedBody")`). Leave the icon/label colour tokens, the metadata row
(completedAt/totalCost/duration), `RawOutputControls`, the verification-report toggle button,
the `VerificationReport` panel and the frozen `AgentFeed` **unconditional** — that is the
literal content of D-09 ("degraded gets everything completed gets"). Do NOT add a reason list
here: D-12's `degradation_reasons` are shaped into the verification report by 15.2-08 and
reached from this card's existing "View verification report" button. Add a short comment above
the branch recording that the degraded run deliberately renders the finished card rather than
the failure card, and that reaching the failure card would strip the download and the
verification report from a ~$45 run.

**Locales** — add two sibling keys next to `completedTitle`/`completedBody` inside the
`research` namespace of all three `intake.json` files (`en` around :499, `nl` and `fr` at the
same key). Key names: `degradedTitle`, `degradedBody`. English copy, verbatim:
`"degradedTitle": "Research completed — degraded"` and `"degradedBody": "The deep research
finished, but part of the output fell short. Open the verification report below to see exactly
what was lost."` Write the `nl` and `fr` values in the register the neighbouring
`completedBody`/`failedBody` values already use — full sentences, no placeholders, no English
left in. Do not add any other key; CHECK A (3-way parity) and CHECK B (literal-key resolution)
are HARD gates.
  </action>

  <acceptance_criteria>
    - `grep -rn '_RESEARCH_TERMINAL' backend/app/` returns nothing; `grep -rn 'RESEARCH_TERMINAL = {' backend/app/` returns exactly one line, in `backend/app/research/run_status.py`
    - `grep -n '== "completed"' backend/app/research/run_task.py backend/app/api/research_routes.py` returns nothing
    - `grep -c 'is_research_success' backend/app/research/run_task.py` returns `>= 2`; `grep -c 'is_research_success' backend/app/api/research_routes.py` returns `>= 1`
    - `grep -n '"parked"' backend/app/research/run_task.py` matches only the `finalize_failed` clamp line
    - `grep -c 'completed_degraded' frontend/src/lib/api/research.ts` returns `>= 2`; `grep -c 'completed_degraded' frontend/src/components/intake/ResearchRunProgress.tsx` returns `>= 3`
    - `cd frontend && node scripts/i18n-audit.mjs` exits 0 (CHECK A/B/C all pass)
    - `cd frontend && npx tsc --noEmit -p tsconfig.json` reports no errors
    - `cd frontend && npm run lint` exits 0
    - `bash backend/scripts/ci_no_run_research.sh` exits 0 (no SerpAPI / run-research reference entered `backend/app/**` or `frontend/src`)
    - The three locale files each gained exactly two keys: `git diff --numstat -- frontend/src/locales/*/intake.json` shows `2` added lines per file
  </acceptance_criteria>

  <verify>
    <automated>cd frontend && npm ci && node scripts/i18n-audit.mjs && npx tsc --noEmit -p tsconfig.json && npm run lint && cd .. && bash backend/scripts/ci_no_run_research.sh && gcloud builds submit --config=cloudbuild.test.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>

  <done>One intake-side vocabulary module imported by both consumers; a degraded run gets its bundle, its report row, its mail and its download on the intake side; both SSE terminal sets close on `completed_degraded`; the operator UI renders a degraded run as a finished-with-caveats card retaining every `completed` affordance; the i18n hard gates, typecheck, lint, scope guard and the backend denial gate are all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| superadmin browser → intake FastAPI (`backend/app/api/research_routes.py`) | Identity-Platform-authenticated, space-scoped; untrusted `intake_id` / `run_id` cross here |
| intake FastAPI → Tribunal seam (`runs/api.py`) | Service-to-service with `X-Nestor-Tenant-Id` setting the RLS tenant GUC; a forged/foreign `run_id` crosses here |
| Tribunal worker → `run` table | The only writer of the terminal status; the row is the operator's sole record of whether the output is trustworthy |
| pipeline result dict → worker | In-process, but `degradation_reasons` originates in later plans and is treated as untrusted-shaped input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| **T-15.2-21** | Information disclosure / Elevation | `runs/api.py` `get_run_report`, `get_run_research_bundle`, sites 587/691 | **high** | mitigate | Widening the *status* gate must not widen *tenant* scope. Every widened handler keeps `Depends(get_db_session)` → `set_tenant_context` → FORCE RLS and its `scalar_one_or_none()` → `HTTPException(404, ...)` block **above and unchanged by** the gate edit. `test_status_gates.py::test_widened_handlers_keep_the_404_non_distinguishability` asserts each handler's source contains `scalar_one_or_none()` + `HTTPException(404` and **no** `403`. The DB-backed `test_research_bundle_endpoint.py::test_cross_tenant_run_not_visible` stays untouched and green in the critical gate. |
| **T-15.2-22** | Information disclosure | `bundle_readable` / `report_readable` | **high** | mitigate | A `parked` run has no finished, scrubbed report; granting it `/report` would ship unverified prose as a deliverable. `report_readable("parked") is False` is pinned by `test_parked_gets_inspection_not_the_report`. The bundle handler's D-01 exclusion (returns EXACTLY `cleaned_reports`, never `rejected_claims` / `contested_notes` / `verification`) is not touched by this plan. |
| **T-15.2-23** | Repudiation | `runs/worker.py` completion UPDATE | **high** | mitigate | A run that lost a stream reported as clean `completed` leaves no record that the output fell short — the exact silent-green the operator bar forbids. Status is computed by `terminal_state()` (15.2-02) from an explicit reason list, and the reasons are persisted in the **same statement** as the status (the G-10 / T-15.1-36 rule: a run must never be able to say `completed` while its marker is missing). `test_worker_writes_the_status_terminal_state_computed` pins the shape. |
| **T-15.2-24** | Denial of service | `runs/schemas.py` `RunStatus` | **high** | mitigate | A DB-legal status missing from the `Literal` turns every read of such a run into an HTTP 500 (CR-03). `RunStatus` is extended in the same plan as the gates and pinned to the nine-literal set by `test_fail_loud.py`; 15.2-01 shipped the matching `ck_run_status`, and Task 1's acceptance criteria require a byte-match check against it. |
| **T-15.2-25** | Information disclosure | `research_routes.py:367` raw-output download | **medium** | mitigate | Only the *status* half of the conjunct is widened. The superadmin-first existence-hidden 404 at :355, the cross-space 404s, and the `chain_status != "verified"` conjunct (the D-06 complete-but-LOCKED gate) are unchanged — a broken-chain degraded run is still not downloadable. Existing `test_seam_denial.py` / `test_seam_rls_denial.py` stay green in the backend gate. |
| **T-15.2-26** | Denial of service | `research_routes.py:632` + `research.ts:210` SSE terminal sets | **medium** | mitigate | A stale terminal set leaves a degraded run's SSE stream open to the 10-min `MAX_STREAM_SECONDS` cap server-side and drives the client into its `retry()` reconnect loop — a self-inflicted request amplifier on every degraded run. Both sets gain the literal; the client set is imported-in-spirit from the one backend module so they cannot drift again. |
| **T-15.2-27** | Tampering | `result["degradation_reasons"]` consumed by the worker | **low** | mitigate | Reasons are read defensively (`or []`, coerced with `str()`), never `json.loads`ed from model text, and never interpolated into SQL — they ride as a JSONB bind parameter on the existing `CAST(:vsummary AS JSONB)` fragment. They are display-only; no control flow but the boolean "is this list empty" depends on their content. |
| **T-15.2-28** | Repudiation | audit hash chain | **low** | accept | No field is added to `canonical_json` and no audit call site changes, so `verify_chain` and `test_hash_chain_replay.py` are unaffected by construction (EU AI Act Art. 12, deadline 2026-08-02). Accepted with the standing phase-gate re-run of `cloudbuild.test-critical.yaml` as the check. |
| **T-15.2-29** | Information disclosure | new i18n copy + the degraded card | **low** | accept | The degraded card adds two static strings and no new data surface; `ResearchRunProgress.tsx` remains imported only by the admin detail route (T-16-12 unchanged), so no client-facing surface gains anything (D-08). |
| **T-15.2-SC** | Tampering | npm / pip installs | — | mitigate | **This plan installs no packages.** `npm ci` restores the committed lockfile only. If any dependency change becomes necessary, stop and run the Package Legitimacy Gate with a blocking `checkpoint:human-verify` first. |
</threat_model>

<verification>
Run in this order after the last task. All Python runs in Cloud Build; the Node commands run locally.

1. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT"` — `test_status_gates.py` (9 tests) and `test_fail_loud.py` green.
2. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"` — the 15.1 gate suite unbroken.
3. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT"` — `test_hash_chain_replay.py`, `test_research_bundle_endpoint.py`, `test_verification_report_endpoint.py`, `test_rls_isolation.py` green (the cross-tenant 404 contract survives the widening).
4. `cd frontend && npm ci && node scripts/i18n-audit.mjs && npx tsc --noEmit -p tsconfig.json && npm run lint` — all exit 0.
5. `gcloud builds submit --config=cloudbuild.test.yaml --project="$GOOGLE_PROJECT"` — the backend cross-tenant denial gate green.
6. `bash backend/scripts/ci_no_run_research.sh` — exit 0.
7. Source sweep, all must return nothing:
   `grep -rn 'status != "completed"' tribunal/nestor_pulse_sdk/runs/api.py`
   `grep -rn '== "completed"' backend/app/research/run_task.py backend/app/api/research_routes.py`
   `grep -rn '_RESEARCH_TERMINAL' backend/app/`
8. Confirm no file owned by another plan moved:
   `git diff --stat -- tribunal/nestor_pulse_sdk/db/models/run.py tribunal/cloudbuild.test-engine.yaml tribunal/nestor_pulse_sdk/verification/report.py tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py tribunal/nestor_pulse_sdk/pipeline/` — must be empty.
</verification>

<open_items>
Named, not silently absorbed. Neither blocks this plan.

1. **`parked` UI + mail is 15.2-16's.** After this plan a `parked` run keeps its SSE stream
   open to the 10-min cap and renders as an in-progress run. That is intentional: adding
   `parked` to a terminal set before the Resume button, `render_research_parked` and F-02's
   free-resume rule exist would ship a dead-end state. 15.2-16 closes it, and nothing ships to
   production before 15.2-18.
2. **Degraded-card copy is provisional.** `research.degradedTitle` / `degradedBody` are written
   to the D-12 bar ("never green while a stage was gutted") without operator review. Worth one
   line on the V-02 checklist (#15 already covers "terminal state is honest") when the operator
   reads the August run.
</open_items>

<success_criteria>
- `runs/api.py` contains zero `status != "completed"` comparisons; all four gates plus the F2 display line call `report_readable` / `bundle_readable`.
- `report_readable` is `True` for exactly `{completed, completed_degraded}`; `bundle_readable` for exactly `{completed, completed_degraded, parked}`; both truth tables are pinned by test.
- `GET /{run_id}/verification` is byte-unchanged in behaviour, documented as deliberately gate-free, and pinned by a regression test (F1).
- `runs/worker.py` writes a `terminal_state()`-computed status and persists its reason list in the same statement; the `WHERE ... status='running'` cancel guard survives.
- `RunStatus` carries nine literals matching 15.2-01's `ck_run_status`; `test_fail_loud.py`'s G-10 test is **updated in place**, not deleted.
- `completed_degraded` is recognised by the intake poll driver, the intake SSE handler, the SSE client and the UI terminal set; the intake download gate and the completion bundle/mail path treat it as a success.
- A `completed_degraded` run renders the finished-with-caveats card with the raw-output download, the verification-report button and the frozen feed intact — never the cancelled card.
- All six gates in `<verification>` green; no file owned by another 15.2 plan was modified.
</success_criteria>

<output>
Create `.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-09-SUMMARY.md` when done.
</output>
