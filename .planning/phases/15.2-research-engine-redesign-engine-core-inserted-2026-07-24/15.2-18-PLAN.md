---
phase: 15.2-research-engine-redesign-engine-core-inserted-2026-07-24
plan: 18
type: execute
wave: 12
depends_on: ["15.2-17"]
autonomous: false
requirements: [ENGINE-05, ENGINE-11]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - tribunal/infrastructure/cloud-run/deploy-worker.sh
  - tribunal/infrastructure/cloud-run/deploy-api.sh
  - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-UAT.md
  - docs/tribunal-run-reports/V-01-COMPARISON.md
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/intake.py
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/__init__.py
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/grouping.py
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
  - tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py
  - tribunal/nestor_pulse_sdk/scripts/probe_intake.py
  - tribunal/nestor_pulse_sdk/tests/test_tribunal_intake.py
  - tribunal/nestor_pulse_sdk/tests/test_tribunal_grouping.py

user_setup:
  - service: serpapi
    why: "D10 own-researcher stream — the fourth research stream searches through SerpApi. The account, the plan/tier choice and the API key value can only come from a human at serpapi.com."
    env_vars:
      - name: SERPAPI_API_KEY
        source: "serpapi.com -> Dashboard -> Your Account -> Api Key (seeded into Secret Manager secret Nestor_SerpApi via stdin, never typed inline)"
    dashboard_config:
      - task: "Choose the plan/tier (Free 250/mo @ 50/hr @ $0.00000 per search · Starter $25/1,000 @ 200/hr @ $0.02500 · Developer $75/5,000 @ 1,000/hr @ $0.01500 · Production $150/15,000 @ 3,000/hr @ $0.01000 · Big Data $275/30,000 @ 6,000/hr @ $0.00917). Free's 50 searches/hour will throttle a four-stream run."
        location: "serpapi.com -> Pricing"

must_haves:
  truths:
    - "SERPAPI_API_KEY resolves from Secret Manager on BOTH the tribunal-worker and the tribunal-api revisions that serve 100% of traffic, and no secret VALUE is printed anywhere in the session, the runbook or the git history."
    - "The tribunal alembic line reads head 0013 and the migrate-job execution log carries the literal line `Running upgrade 0012 -> 0013` — an exit(0) alone is never accepted as proof."
    - "Every deployable that carries phase-15.2 code is REBUILT (tribunal-worker, tribunal-api, nestor-api, nestor-frontend) — no env-flip-only deploy, and the built tree provably contains all of plans 01-17."
    - "Re-running deploy-worker.sh or deploy-api.sh never drops SERPAPI_API_KEY and never silently repoints ANTHROPIC_API_KEY away from the secret the live revision actually mounts."
    - "infra/DEPLOY-RUNBOOK.md carries an enumerated § Phase 15.2 section and the five SERPAPI earmarks now point forward to it instead of saying 'not this phase'."
    - "The phase PARKS at V-01 without failing: no task in this plan requires a live Anthropic call before 2026-08-01."
    - "V-01 is ONE live run on a fresh test intake, recorded beside the recorded 4cbb5311 baseline — no A/B double-run, the comparison_id harness stays unused."
    - "V-02's 16 checklist items are each recorded pass/fail with named evidence, ending in a dated operator sign-off line."
    - "V-03 lands as a SEPARATE commit after sign-off and removes only unreferenced old-path code; claim_distiller (D-15), detect_explicit_questions and extract_and_persist_citations all survive with green tests."
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "§ Phase 15.2 — Steps 15.2.a-15.2.i: preflight, SERPAPI secret, dual tribunal build at one $SHA, migrate 0013 with the image pin, dual redeploy, nestor-api rebuild, frontend rebuild, verify-without-printing-secrets, and the park"
      contains: "## Phase 15.2 —"
    - path: "tribunal/infrastructure/cloud-run/deploy-worker.sh"
      provides: "SERPAPI_API_KEY secret binding (existence-probed) + ANTHROPIC secret self-heal from the live revision"
      contains: "SERPAPI_API_KEY="
    - path: "tribunal/infrastructure/cloud-run/deploy-api.sh"
      provides: "SERPAPI_API_KEY secret binding (existence-probed) + ANTHROPIC secret self-heal from the live revision"
      contains: "SERPAPI_API_KEY="
    - path: ".planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-UAT.md"
      provides: "the finalised 16-item V-02 checklist with per-item pass condition, evidence slot and verdict, plus the sign-off block"
      contains: "V-02 #16"
    - path: "docs/tribunal-run-reports/V-01-COMPARISON.md"
      provides: "the V-01 new-run-vs-4cbb5311-baseline comparison table (outcome, duration, cost, claims, sessions, skeptic parse crashes, cap-400s, unresolved anchors, funnel)"
      contains: "4cbb5311"
  key_links:
    - from: "tribunal/infrastructure/cloud-run/deploy-worker.sh"
      to: "Secret Manager secret Nestor_SerpApi"
      via: "--set-secrets SERPAPI_API_KEY=${TRIBUNAL_SERPAPI_SECRET}:latest, appended only when the secret exists"
      pattern: "SERPAPI_API_KEY=\\$\\{TRIBUNAL_SERPAPI_SECRET\\}:latest"
    - from: "tribunal/infrastructure/cloud-run/deploy-api.sh"
      to: "Secret Manager secret Nestor_SerpApi"
      via: "--set-secrets SERPAPI_API_KEY=${TRIBUNAL_SERPAPI_SECRET}:latest, appended only when the secret exists"
      pattern: "SERPAPI_API_KEY=\\$\\{TRIBUNAL_SERPAPI_SECRET\\}:latest"
    - from: "infra/DEPLOY-RUNBOOK.md § Phase 15.2 Step 15.2.d"
      to: "Cloud Run Job tribunal-migrate"
      via: "gcloud run jobs update --image ...tribunal-api:${SHA} BEFORE execute (the Phase-17 image-pin lesson)"
      pattern: "gcloud run jobs update tribunal-migrate"
    - from: "docs/tribunal-run-reports/V-01-COMPARISON.md"
      to: "docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md"
      via: "baseline column, read side by side at sign-off"
      pattern: "run-20260722-4cbb5311"
---

<objective>
Ship phase 15.2 to production and then park it cleanly at UAT until the Anthropic monthly cap
resets on **2026-08-01**, after which V-01 (one live run against the recorded baseline), V-02 (the
16-item checklist + operator sign-off) and V-03 (the separate old-path cleanup commit) close the
phase.

Purpose: plans 01-17 are code. Nothing in this project is real until it is deployed — and this
phase's deploy carries three things every prior phase has been bitten by: a **new secret on both
Tribunal images**, a **migration the Job will silently skip if the image is not repinned**, and a
**frontend that ships stale unless it is genuinely rebuilt**. D-01 is explicit that all buildable
work executes and deploys NOW and the phase then waits at UAT, exactly as Phase 15's browser UAT
already does.

Output: an enumerated `infra/DEPLOY-RUNBOOK.md` § Phase 15.2, two deploy scripts that bind
`SERPAPI_API_KEY` without dropping anything else, four rebuilt-and-deployed services, tribunal
alembic at `0013`, a finalised 16-item V-02 checklist in `15.2-UAT.md`, a V-01 comparison document
next to the baseline, and — only after sign-off — the V-03 cleanup commit.
</objective>

<what_this_plan_reuses>
**Read this first. The dominant failure mode for phase 15.2 is building a second one of something
that already exists.** This plan writes NO new deploy tooling, NO new CI config, NO new test
harness, and NO new secret-handling convention.

| Capability | Reused from | Do NOT build |
|---|---|---|
| Dual-Tribunal build + deploy at one `$SHA` | `infra/DEPLOY-RUNBOOK.md` § Phase 15.1 Step **15.1.f** (executed live 2026-07-25) | a hand-rolled `gcloud run deploy` for the tribunal services |
| Migrate-job image pin + `Running upgrade` proof | § Phase 15 Step **15.b** + § Phase 15.1 Step **15.1.f** items 2-4 | a "the job exited 0 so it worked" check |
| `nestor-api` rebuild idiom | § Phase 17 Step **17.b** (`gcloud builds submit backend --tag`, then `gcloud run services update --image`) | a Dockerfile change, a new build config |
| Frontend rebuild idiom | § Phase 12 Step **12.3** / § Phase 15.1 Step **15.1.g** (`frontend/cloudbuild.yaml`, `_IMAGE`/`_API_BASE_URL`/`_FB_*`, `--port 8080`) | `npm install`, a new cloudbuild config, `routeTree.gen.ts` regeneration |
| Secret create + resource-scoped accessor + stdin value seed | § Phase 10 Step **10.1** / § Phase 13 Step **13.b** | `--data=` inline, a project-wide IAM grant |
| Seam-env self-heal guard (read the live service, fall back, fail loud) | `deploy-api.sh` lines 60-80 (`TRIBUNAL_SERVICE_URL` / WR-01 guard) | a new guard shape |
| The six phase gates | `15.2-VALIDATION.md` + § *Sampling rate* in `15.2-RESEARCH.md`; the configs themselves are owned by **15.2-02** (`cloudbuild.test-engine.yaml`) and predate this phase | any edit to a cloudbuild config |
| The recorded baseline to compare against | `docs/tribunal-run-reports/run-20260722-4cbb5311/{REPORT.md,GROUPS.md,index.json}` | a second baseline, a re-run of the old engine |
| SerpApi plan probe + exact-`Decimal` unit price | `pipeline/tribunal/serpapi.py::fetch_plan` / `resolve_unit_price` (**created by 15.2-12**) | a hardcoded tier price anywhere |

**Also out of scope for this plan** (owned elsewhere, do not touch): every `tribunal/cloudbuild.*.yaml`
(15.2-02 owns the engine gate and pre-listed every test file), the frontend source (15.2-09 owns the
single `RESEARCH_TERMINAL` line and the locale keys), and the SerpApi client itself (15.2-12).
</what_this_plan_reuses>

<phase_shape>
## This plan has a NOW half and a PARKED half — execute-phase must not treat the park as a failure

| Task | Runs | Blocked by |
|---|---|---|
| **Task 1** — runbook + deploy scripts + V-02 checklist authoring | **NOW** | nothing |
| **Task 2** — SerpApi tier decision + `Nestor_SerpApi` secret seeded (`checkpoint:decision`) | **NOW** | operator availability only |
| **Task 3** — the live deploy session (4 services + alembic 0013) | **NOW** | Task 2 |
| **Task 4** — V-01 live run (`checkpoint:human-verify`) | **PARKED** | Anthropic monthly cap → **2026-08-01** |
| **Task 5** — V-02 16-item checklist + sign-off (`checkpoint:human-verify`) | **PARKED** | Task 4 |
| **Task 6** — V-03 cleanup commit | **PARKED** | Task 5 sign-off |

D-01, verbatim: *"All buildable work executes and deploys now; the phase then parks at UAT waiting
for the cap reset, exactly as Phase 15's browser UAT already does."* Tasks 4-6 pause the phase; they
do not fail it. **Treat the Task-4 park as a context boundary** — tasks 4-6 are executed in a fresh
session on or after 2026-08-01.

Two Phase-15/15.1 items are deliberately batched into the same August operator session (STATE.md,
operator decision 2026-07-24): the deferred Phase-15 populated-surface browser UAT (SC1-SC4) and the
Phase-15.1 gate/verdict surfaces, neither of which could be walked on the recorded fixture because
run 4cbb5311 lives only as a pytest fixture and was never seeded into the live DB.
</phase_shape>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-CONTEXT.md
@.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-PLAN-OUTLINE.md
@.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-VALIDATION.md

<interfaces>
<!-- Concrete deploy-surface facts, verified against the working tree at c6440db and the runbook. -->
<!-- Use these directly. Do not go exploring for them, and do not invent a value that is not here. -->

**GCP identifiers**
- Project: `project-cb01b861-cb4a-438d-b9a` (§ Phase 15.1 Step 15.1.f, line 2219) — export as `$GOOGLE_PROJECT`
- Region: `europe-west1` · Cloud SQL instance: `nestor-pg`
- Tribunal runtime SA: `tribunal-run@${GOOGLE_PROJECT}.iam.gserviceaccount.com` (Phase-14 least-privilege SA)
- Intake runtime SA: `nestor-run@${GOOGLE_PROJECT}.iam.gserviceaccount.com`
- Artifact Registry repo: `${REGION}-docker.pkg.dev/${GOOGLE_PROJECT}/nestor/{tribunal-api,tribunal-worker,backend,frontend}`

**Services / jobs in play**
| Deployable | Build config | Deploy path | Carries 15.2 code from |
|---|---|---|---|
| `tribunal-worker` | `tribunal/cloudbuild.worker.yaml` | `IMAGE_TAG=$SHA tribunal/infrastructure/cloud-run/deploy-worker.sh` | 01-08, 10-16 (the whole engine) |
| `tribunal-api` | `tribunal/cloudbuild.api.yaml` | `IMAGE_TAG=$SHA tribunal/infrastructure/cloud-run/deploy-api.sh` | 09 (status predicate), 03 (feed) |
| `tribunal-migrate` (Job) | — (runs the `tribunal-api` image) | `gcloud run jobs update … --image` **then** `execute --wait` | 01 (alembic 0013) |
| `nestor-api` | `gcloud builds submit backend --tag "$IMAGE"` | `gcloud run services update nestor-api --image "$IMAGE"` | 09 (`run_status.py`, `run_task.py`), 16 (Resume route + parked mail) |
| `nestor-frontend` | `frontend/cloudbuild.yaml` (`_IMAGE`,`_API_BASE_URL`,`_FB_API_KEY`,`_FB_AUTH_DOMAIN`,`_FB_PROJECT_ID`) | `gcloud run deploy nestor-frontend --allow-unauthenticated --port 8080` | 09 (`RESEARCH_TERMINAL` + `research.ts` + 3 locale files) |

**Secrets bound today by the deploy scripts** (`--set-secrets` REPLACES the entire set — this is the
whole reason both scripts must be edited):
`DATABASE_URL=DATABASE_URL:latest` (api) / `DATABASE_URL=DATABASE_URL_WORKER:latest` (worker),
`AUDIT_GCS_BUCKET=AUDIT_GCS_BUCKET:latest`, `ANTHROPIC_API_KEY=Nestor_Claude:latest`,
`GOOGLE_API_KEY=Nestor_Gemini:latest`, `OPENAI_API_KEY=Nestor_OpenAI:latest`.

> ⚠️ **LIVE-VS-SCRIPT DRIFT, observed 2026-07-21.** Both Tribunal services were switched live to
> mount `ANTHROPIC_API_KEY` from **`Nestor_Claude2`** (the account with credits); the scripts still
> say `Nestor_Claude`. Running either script as-is **silently repoints the Anthropic key back to the
> low-credit secret** — which would wall the ~$45 V-01 run. `nestor-api` deliberately stays on
> `Nestor_Claude`. The preflight `describe` is authoritative; the literal above is only what was
> observed.

**New secret this plan creates**
- Secret Manager resource name: **`Nestor_SerpApi`** (house style: `Nestor_Claude` / `Nestor_Gemini` / `Nestor_OpenAI`)
- Env var name read by the code: **`SERPAPI_API_KEY`** (`serpapi.py::api_key()` — `os.environ.get("SERPAPI_API_KEY", "").strip()`)
- Bound to: `tribunal-worker` **and** `tribunal-api`. **Never** to `nestor-api` or the frontend —
  `backend/scripts/ci_no_run_research.sh` scans `backend/app/**` and `frontend/src` and INTAKE-05 must stay green.
- Absent-secret behaviour (15.2-12, already tested): `unavailable_reason() == "serpapi_key_missing"`
  ⇒ breaker open at startup ⇒ clean 3-stream `completed_degraded` run. A missing secret degrades; it never crashes.

**SerpApi published plans** [CITED: serpapi.com/pricing, fetched 2026-07-26 — re-verify before V-01]
| Plan | $/month | Searches/month | Unit = price ÷ quota | Throughput/hour |
|---|---|---|---|---|
| Free | 0 | 250 | **$0.00000** | 50 |
| Starter | 25 | 1,000 | $0.02500 | 200 |
| Developer | 75 | 5,000 | $0.01500 | 1,000 |
| Production | 150 | 15,000 | $0.01000 | 3,000 |
| Big Data | 275 | 30,000 | $0.00917 | 6,000 |

The **code is plan-agnostic** — `serpapi.fetch_plan()` reads `plan_monthly_price` /
`searches_per_month` from `GET https://serpapi.com/account.json` at run start (free, off-quota) and
records them with the run (D-16 reproducibility). **The tier itself is a blocking operator decision
(Task 2). Do not invent one.**

**Alembic lines (two, independent — Pitfall 11)**
- Tribunal line: version table **`tribunal.tribunal_alembic_version`** (NOT `alembic_version` —
  querying the latter raises `UndefinedTableError` and looks like a failed migration). Head today
  **`0012`**; this phase's 15.2-01 adds **`0013`**. Expected log line: `Running upgrade 0012 -> 0013`.
- Intake `nestor` line: head `0012` (Phase 17). **This phase adds NO intake migration** — F3:
  `nestor.research_runs.status` has no CHECK constraint, so the new status literals need no DDL.

**Recorded baseline — `docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md`**
- `research_run_id` `4cbb5311-9f5f-4504-84bb-b0dda2aedf48`, tribunal `run_id` `9c84e5a9-…`, intake `e08620c5`
- Outcome `completed` (green) · 2026-07-22 · ~48 min · subject: LUKOIL BeNeLux (dynamic pricing, coffee, Germany-entry 2027)
- 228 audited calls · 6 deep-research calls · 8 distiller calls → **1,162 unique claims** · 30 grouping calls → 176 groups (92% singletons)
- 176 group-skeptic sessions, of which **24 crashed on the F-01 `reconciliation`-as-string bug** (§4.1) and **776 hard-400 cap errors in 55 s** (§4.2)
- **28 unresolved `[cite:]` markers stripped** (§4.4) · real Anthropic-side cost **≈ $43-45** against a ~€5 displayed figure
- The four shipped contradictions (GROUPS.md): Aral 16% vs 21% · LUKOIL NL 46 vs ~70/75 stations · Zeeland Carlyle vs TotalEnergies · Gunvor vs Carlyle
- **SC5:** the F-01 skeptic fix shipped 2026-07-23 but has never run live. The V-01 run validates it
  by construction — the pass condition is **zero** `'str' object has no attribute 'get'` group-skeptic
  failures against the baseline's 24.

**The six phase gates** (all run in Cloud Build; no local Python or Docker)
1. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT"`
2. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"`
3. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT"`
4. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test.yaml --project="$GOOGLE_PROJECT"`
5. `gcloud builds submit . --config=cloudbuild.test.yaml --project="$GOOGLE_PROJECT"` — **source is `.` (repo root), never `backend`**, and the config lives at the repo root. (`15.2-VALIDATION.md` says `backend/cloudbuild.test.yaml`; **that file does not exist** — the runbook's § Phase 15/17 form is the correct one. Fix the VALIDATION row while you are in the runbook.)
6. `bash backend/scripts/ci_no_run_research.sh`

> ⚠️ **The gates can be green while proving nothing.** `cloudbuild.test-*.yaml` builds its file list
> with `ls … 2>/dev/null || true`, so a submitted tree that predates a plan collects fewer files and
> still exits SUCCESS. **Always read the `collecting:` block, not just the exit status.**
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author § Phase 15.2 of the runbook, bind SERPAPI in both deploy scripts, and finalise the 16-item V-02 checklist</name>

  <files>
    infra/DEPLOY-RUNBOOK.md
    tribunal/infrastructure/cloud-run/deploy-worker.sh
    tribunal/infrastructure/cloud-run/deploy-api.sh
    .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-UAT.md (new)
    docs/tribunal-run-reports/V-01-COMPARISON.md (new)
    .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-VALIDATION.md
  </files>

  <read_first>
    - infra/DEPLOY-RUNBOOK.md lines 1719-1960 — § Phase 15 Steps 15.a-15.e: the dual-tribunal build, the migrate-job image-pin lesson in full, the frontend rebuild, and the "verify without printing secrets" step. Three of the five SERPAPI earmarks live here (1725, 1754, 1896, 1949).
    - infra/DEPLOY-RUNBOOK.md lines 2202-2367 — § Phase 15.1 Steps 15.1.f/15.1.g: the load-bearing **build → migrate → deploy** order and why (a writer shipping ahead of its column fails into a swallowed `try/except` and the run looks successful), the repin-confirm `describe`, the `Running upgrade` log requirement, the digest-pin proof, and the `npm ci` rule.
    - infra/DEPLOY-RUNBOOK.md lines 1377-1441 — § Phase 17 Steps 17.b/17.c: the `nestor-api` rebuild idiom (`gcloud builds submit backend --tag`) and the intake-side migrate-job pin.
    - infra/DEPLOY-RUNBOOK.md lines 403-419 — § Phase 10 Step 10.1: the secret-create + resource-scoped-accessor + stdin-value-seed idiom, verbatim.
    - infra/DEPLOY-RUNBOOK.md line 2435 and the § Summary checklist block from 2370 onward — the fifth SERPAPI earmark and the row format new steps must follow.
    - tribunal/infrastructure/cloud-run/deploy-api.sh — the whole file (~120 lines). Lines 60-80 are the `TRIBUNAL_SERVICE_URL` self-heal + WR-01 fail-fast guard: the exact shape the two new secret-name guards must copy. Lines 105-112 are the `--set-secrets` block being extended.
    - tribunal/infrastructure/cloud-run/deploy-worker.sh — the whole file (~100 lines). Same `--set-secrets` block, plus `DATABASE_URL_WORKER` and the always-on/`min-instances=1` settings that must not change.
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-RESEARCH.md lines 1629-1648 — § *Proposed V-02 checklist*, the 16 rows this task finalises, and lines 1728-1777 § *Open Questions* (the tier decision, the V-01 intake choice, and the audit-redactor query-param must-fix).
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-VALIDATION.md lines 85-105 — the manual-only table these checklist items must stay consistent with, and the `backend/cloudbuild.test.yaml` row that is wrong.
    - docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md — §1 run overview, §3.5 the skeptic arm, §4.1 the F-01 crash, §4.2 the cap wall, §4.4 the 28 stripped markers, §6 token accounting. These supply the baseline column of the comparison document.
  </read_first>

  <action>
Three deliverables, no live GCP command in this task.

**(a) `infra/DEPLOY-RUNBOOK.md` — add `## Phase 15.2 — Research engine core (SERPAPI secret on BOTH tribunal images + dual REBUILD + 0013 migrate + nestor-api REBUILD + frontend REBUILD, then PARK)`.**
Place it after the § Phase 15.1 section and before § Summary checklist. Open with the same four
preamble blocks every recent section uses: the IaC-drift carry-over (`terraform apply` is FORBIDDEN —
it rotates the BUILT_IN DB passwords), the recurring deploy-gap warning ("nothing is real until it is
deployed, and a config-only env flip ships a STALE image"), the shared `export` block
(`GOOGLE_PROJECT`, `REGION`, `INSTANCE_NAME`, `INTAKE_SA`, `TRIBUNAL_SA`), and a "what changes this
phase" table naming all four deployables plus the Job. State plainly that this is the FIRST phase
since Phase 13 to add a secret, and that `--set-secrets` replaces the entire set.

Enumerate nine steps:

- **15.2.a — Preflight, read-only.** (1) **Stale-base guard** — `git status --porcelain` empty,
  `git log --oneline -1` recorded into the step, and a positive on-disk assertion for one artifact
  per wave: `tribunal/nestor_pulse_sdk/alembic/versions/0013_*.py`,
  `pipeline/tribunal/reliability.py`, `runs/stage_feed.py`, `pipeline/tribunal/facts.py`,
  `citations/anchors.py`, `pipeline/tribunal/workshop.py`, `pipeline/tribunal/workshop_rank.py`,
  `pipeline/tribunal/own_researcher.py`, `pipeline/tribunal/serpapi.py`,
  `tests/test_engine_e2e_stubbed.py`, `tribunal/cloudbuild.test-engine.yaml`, and the string
  `completed_degraded` in `frontend/src/components/intake/ResearchRunProgress.tsx`. Cloud Build ships
  the tree you submit — a stale worktree produces a green-looking deploy of code that is not this
  phase. (2) The six gates green on THAT tree, with the `collecting:` block read, not just the exit
  status. (3) Capture the live wiring without printing any value:
  `gcloud run services describe tribunal-worker --region="$REGION" --project="$GOOGLE_PROJECT" --format='value(spec.template.spec.containers[0].env)'`
  piped through `tr ',' '\n' | grep -E 'ANTHROPIC_API_KEY|SERPAPI_API_KEY|DATABASE_URL|AUDIT_GCS_BUCKET'`
  — secret *names* are configuration, not secrets; values are never fetched. Same for `tribunal-api`.
  Record the current revision names, the tribunal-api URL captured **without a path** (Pitfall 4),
  and the Anthropic secret name each service actually mounts.

- **15.2.b — Create `Nestor_SerpApi` + grant + seed the VALUE out-of-band.** `gcloud secrets create
  Nestor_SerpApi --replication-policy=automatic --project="$GOOGLE_PROJECT" 2>/dev/null || true`;
  `gcloud secrets add-iam-policy-binding Nestor_SerpApi --member="serviceAccount:${TRIBUNAL_SA}"
  --role="roles/secretmanager.secretAccessor" --project="$GOOGLE_PROJECT"` — **`tribunal-run` ONLY;
  never `nestor-run`, never a project-wide binding**; then `gcloud secrets versions add
  Nestor_SerpApi --data-file=- --project="$GOOGLE_PROJECT"` (paste, Ctrl-D — never `--data=`, never
  inline where it lands in shell history). Record the chosen **plan/tier by name only** in this step
  and state that the unit price is read live from `/account.json` at run start, so no price is
  hardcoded anywhere. Verify with `gcloud secrets versions list Nestor_SerpApi
  --format='value(name,state)'` (metadata only) and
  `gcloud secrets get-iam-policy Nestor_SerpApi --format=json` showing exactly one member.

- **15.2.c — Build BOTH Tribunal images at ONE `$SHA`.** `export SHA="$(date +%Y%m%d-%H%M%S)"`, then
  the two `gcloud builds submit tribunal --config=tribunal/cloudbuild.{worker,api}.yaml
  --substitutions=_IMAGE=…:${SHA}` commands verbatim from § Phase 15.1 Step 15.1.f. **Build only —
  the deploy is Step 15.2.e.** Say why in one line: 0013 must land before the worker that writes
  `certainty` / `found_by` / `provider_quality` / `research_gap` runs, or every write fails into a
  swallowed `except` and the run completes looking clean.

- **15.2.d — Repin `tribunal-migrate` to the `$SHA` api image, THEN execute.** The repin command,
  the `gcloud run jobs describe … --format='value(spec.template.spec.template.spec.containers[0].image)'`
  confirm that it echoes `:${SHA}`, `gcloud run jobs execute … --wait`, then the `gcloud logging read`
  block requiring the literal **`Running upgrade 0012 -> 0013`**. State in bold that
  `Container called exit(0)` is NOT proof. Then the read-only schema confirm block, expecting:
  `claim.certainty` (nullable), `claim.found_by` (`text[]`, nullable), `claim_source.provider_quality`
  (nullable), table `tribunal.research_gap` with `rls enabled=true forced=true` plus its
  tenant-isolation policy and index, `ck_run_status` accepting `completed_degraded` and `parked`, and
  `tribunal_head = 0013` read from **`tribunal.tribunal_alembic_version`**.

- **15.2.e — Deploy worker, then api, at `IMAGE_TAG=$SHA`,** through the two scripts (never a
  hand-rolled `gcloud run deploy`: the scripts preserve the Phase-14 lockdown — `tribunal-run` SA,
  `--no-allow-unauthenticated`, invoker `nestor-run` only). Follow with the binding confirm:
  `describe` each service, `tr ',' '\n'`, `grep -E 'SERPAPI_API_KEY|ANTHROPIC_API_KEY'` — expect the
  secret *reference* on both, never a value; and the digest-pin proof
  (`gcloud run revisions describe <REV> --format='value(spec.containers[0].image)'` resolving to an
  `@sha256:` digest, never `:latest`). Add the explicit warning: if the SERPAPI grep comes back empty
  the deploy is not broken — the own-researcher will simply run as a clean 3-stream degraded run —
  but V-01 cannot then prove SC2's D10 clause, so fix the binding before running.

- **15.2.f — REBUILD + deploy `nestor-api`.** `export IMAGE="${REGION}-docker.pkg.dev/${GOOGLE_PROJECT}/nestor/backend:${SHA}"`,
  `gcloud builds submit backend --tag "$IMAGE" --project="$GOOGLE_PROJECT"`, then
  `gcloud run services update nestor-api --region "$REGION" --project="$GOOGLE_PROJECT" --image "$IMAGE"`.
  Name what it carries: the D-12 status vocabulary in `app/research/run_status.py` + `run_task.py`,
  the `POST /{intake_id}/research/resume` route in `app/api/research_routes.py`, and
  `render_research_parked` in `app/mail/`. State that there is **NO intake-line migration and NO
  `nestor-migrate` run** this phase (F3 — `nestor.research_runs.status` has no CHECK) and **NO new
  env/secret on `nestor-api`**; the parked mail reuses the Phase-10 `RESEND_API_KEY` /
  `APP_BASE_URL` / `NESTOR_ADMIN_EMAIL` stack, and `APP_BASE_URL` must be present or the mail
  refuse-sends. Order: after the Tribunal services, because the Resume route calls the seam.

- **15.2.g — REBUILD + deploy the frontend.** The § Phase 15.1 Step-15.1.g block verbatim with
  `FE_IMAGE` at `${SHA}`. Constraints, each on its own line: **`npm ci`, never `npm install`**
  (`frontend/package-lock.json` IS committed and the Dockerfile runs `npm ci`; the CLAUDE.md
  "no lockfile" note is stale and refers to bun); **never pass `VITE_SUPABASE_*`** (the in-image
  bundle guard fails the build); **`--port 8080`**; **no `routeTree.gen.ts` regeneration** — 15.2-09
  adds no route, it edits `ResearchRunProgress.tsx`, `lib/api/research.ts` and the three
  `intake.json` locale files on the existing `admin.pulse.intakes.$id` anchor. One line on why the
  rebuild is mandatory for a one-line change: the terminal-status set is compiled into the bundle,
  so without a rebuild a `completed_degraded` run spins forever in the operator's browser while
  every backend surface is correct (the Phase-18 stale-SPA lesson).

- **15.2.h — Verify without printing secrets.** The six gates re-run against the deployed tree;
  `verify_chain` GREEN on the deployed audit data (gate 3 — a RED chain is a STOP, EU AI Act Art. 12,
  deadline 2026-08-02); the secret-binding confirm from 15.2.e; `/readyz` 200 on `tribunal-api` with
  `-H "Authorization: Bearer $(gcloud auth print-identity-token)"` against the path-less URL; and
  `bash backend/scripts/ci_no_run_research.sh` exiting 0 (SerpApi lives inside `tribunal/`, which the
  guard deliberately does not scan — adding any SerpApi reference to `backend/app/**` or
  `frontend/src` breaks INTAKE-05).

- **15.2.i — PARK, then V-01 / V-02 / V-03.** State the wall (Anthropic monthly cap resets
  **2026-08-01**), that no live LLM run may be triggered before it, and point at
  `15.2-UAT.md` for the checklist. List the three post-reset prerequisites: a fresh test intake
  reproducing the baseline brief domain, the SerpApi tier live, and the operator present for
  sign-off.

Finally, **update the five SERPAPI earmarks** at lines 1725, 1754, 1896, 1949 and 2435 so each says
"landed in § Phase 15.2, Step 15.2.b" instead of "not this phase" — leave the original Phase-15
statement intact and append the pointer, so the historical record of what Phase 15 did stays true.
Add the § Summary checklist rows `Step 15.2.a` … `Step 15.2.i` in the established one-line format.

**(b) Both deploy scripts — bind SERPAPI without dropping anything else.**
In `deploy-api.sh` and `deploy-worker.sh` alike, above the `gcloud run deploy` call:

1. `TRIBUNAL_SERPAPI_SECRET="${TRIBUNAL_SERPAPI_SECRET:-Nestor_SerpApi}"`.
2. `TRIBUNAL_ANTHROPIC_SECRET` — **self-heal from the live revision first**, exactly like the
   existing `TRIBUNAL_SERVICE_URL` guard: read the service's current secret mapping with
   `gcloud run services describe "${SERVICE_NAME}" --format='value(spec.template.spec.containers[0].env)'`,
   extract the secret name currently bound to `ANTHROPIC_API_KEY`, and use it; fall back to
   `${TRIBUNAL_ANTHROPIC_SECRET:-Nestor_Claude2}` only when the service is not describable (first
   deploy). Echo the chosen secret **name** — a name is configuration, not a secret. Carry a comment
   naming the 2026-07-21 drift: the scripts said `Nestor_Claude` while both live services mounted
   `Nestor_Claude2`, so an unguarded re-run repointed the Anthropic key at the low-credit account.
3. Compose the secret list into a shell variable instead of a single inline string, then **append
   `,SERPAPI_API_KEY=${TRIBUNAL_SERPAPI_SECRET}:latest` only if
   `gcloud secrets describe "${TRIBUNAL_SERPAPI_SECRET}" --project="${PROJECT}" >/dev/null 2>&1`
   succeeds.** If it does not, `echo` a loud multi-line warning that the own-researcher will start
   with its breaker open and the run will be a clean 3-stream `completed_degraded` run (D-12), and
   continue — binding a non-existent secret would fail the whole deploy, and a missing D10 stream
   must never block shipping the other sixteen plans.
4. Pass the composed variable to `--set-secrets`. Add a comment above it stating that
   `--set-secrets` REPLACES the entire secret set, which is why every mapping must appear here.

Change nothing else in either script: not the SA, not `--no-allow-unauthenticated`, not
`min-instances`, not `--timeout`, not the `TRIBUNAL_SERVICE_URL` guard, not the `NESTOR_ENV` /
`NESTOR_TRIBUNAL_UNCAPPED` env set.

**(c) `15.2-UAT.md` — the finalised V-02 checklist + the V-01 comparison template.**
Create the file with YAML frontmatter (`phase: 15.2`, `status: parked-awaiting-2026-08-01`,
`created`, `signed_off: null`). Sections:

- **§ Blocking prerequisites** — the cap reset date, the SerpApi tier + seeded secret, the fresh test
  intake, and the operator's presence.
- **§ V-01 — the live run.** ONE run, on a fresh intake reproducing the baseline brief domain
  (LUKOIL BeNeLux dynamic pricing / coffee / Germany-entry 2027) in a clean space. **No A/B
  double-run** — the `comparison_id` harness stays available and unused (deferred idea, V-01 chose
  baseline comparison). Note that smoke tenant `1464b60d` was flagged for cleanup, so either confirm
  its suitability or create fresh — RESEARCH recommends fresh so the one run that has to prove the
  phase is not entangled with the cleanup backlog. Record: tribunal `run_id`, `research_run_id`,
  intake id, space/tenant id, start/end timestamps, terminal status, and the deployed image `$SHA`.
- **§ V-02 — the 16-item checklist.** A table with columns `#` / `Check` / `Pass condition (concrete)`
  / `Source` / `Evidence` / `Verdict`, and these sixteen rows, each with the stated pass condition:
  1. Workshop winners visible in the feed the moment they are chosen — the `workshop` stage shows
     candidate → cluster → critique → tournament → evolve rows with per-row cost, and the winning
     questions are readable in the feed while the run is still in flight (D5/D12/V-02).
  2. No operator pause between trigger and completion — zero `needs_input` / `needs_report_spec`
     states in the stage trace. **If the run parked (D-17):** the park mail arrived, the Resume click
     resumed from the last checkpoint without re-dispatching a paid stage, and the attempt counter did
     NOT increase (F-01/F-02/F-03). If it did not park, record "not exercised" — this is honest, not a
     failure (D-01/D5, 16-D-04).
  3. Every client-validated question is represented in the winners — the winners' `PARENT` set is a
     superset of `detect_explicit_questions(brief)`; zero client questions dropped (D4).
  4. At least one brief-vs-world flag surfaced, or its absence is explainable in one sentence (D4).
  5. Per-provider structured fact lists present for **≥3 of 4** streams; every D-14 full-distillation
     fallback is named per provider in the feed and the verification report (D8/D-14).
  6. The own-researcher stream contributed facts, with its search count and its $ figure shown, the
     unit price sourced from `/account.json`; a free-tier run reads "free-tier plan, $0.00/search",
     never a blank (D10/D-16).
  7. Gate funnel recorded and reconciles — every distilled claim in exactly one bucket, with
     `checked_incidentally` subtracted from bucket 2; totals add up (D-10/G-08).
  8. At least one contradictory pair collided in a single skeptic session — check the four recorded
     pairs (Aral 16% vs 21%, LUKOIL NL 46 vs ~70/75 stations, Zeeland Carlyle vs TotalEnergies,
     Gunvor vs Carlyle) (D9).
  9. Every `[n]` in the report body resolves to a source, and the unresolved-anchor count is stated
     **in words** in the verification report and the feed's closing summary. This count is the D-05
     emission-rate metric — record the number, not just pass/fail (D-05/D-06/D13).
  10. The graded source list carries title, link, date and tier for every entry, and single-source
      entries are marked (D13).
  11. "Disputed & changed" and "What we could not establish" both render with real content, and both
      are byte-identical to what the pipeline data implies — the writing model never touched them (D14/D-08).
  12. `verify_chain` GREEN on the new run's audit data. **A red chain is a STOP — no sign-off**
      (EU AI Act Art. 12, deadline 2026-08-02).
  13. Cost fully itemised per C1 — all four Anthropic token classes, `web_search`/`web_fetch` server
      fees, Gemini `usageMetadata`, SerpApi unit × billable count; **nothing silently missing**; only
      Gemini grounding may read "pending" (C1/D-16/D-12).
  14. The feed carries per-row cost, per-agent cards, narration and per-block summaries, matching the
      D15 mockup with `replit view.png` as the visual bar (D12/D15).
  15. The terminal state is honest — `completed` only if nothing was lost; otherwise
      `completed_degraded` with every reason named in words, and `parked` only where no honest
      deliverable was possible. Recovered retries and `cost_pending` must NOT have degraded the run (D-12/R6).
  16. **Operator sign-off** after reading the new report beside
      `docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md` (V-01/V-02).
- **§ SC5 — F-01 by construction.** The pass condition is **zero** group-skeptic
  `'str' object has no attribute 'get'` failures in the run's logs, against the baseline's 24, on an
  image built from a tree containing the fix.
- **§ Deploy verification carried from the runbook** — `SERPAPI_API_KEY` bound on both Tribunal
  images (by name), alembic head `0013`, all four services on new revisions, and **the audit-blob
  redaction check**: pull one `provider="serpapi"` audit body from the audit GCS bucket for the V-01
  run and assert the request dict contains no full URL and that any `api_key`-like field reads
  `[REDACTED]`. RESEARCH § Open Questions #6 flags this as a **security must-fix, not a nice-to-have**,
  because the key rides in a query parameter.
- **§ Batched-in from earlier phases** — the deferred Phase-15 populated-surface browser UAT
  (SC1-SC4) and the Phase-15.1 verdict/gate surfaces, both of which needed a real run.
- **§ Sign-off** — a dated line the operator fills in, plus a "gaps found" list that feeds
  `/gsd:plan-phase --gaps` if any item fails.

Also create `docs/tribunal-run-reports/V-01-COMPARISON.md` with the two-column
baseline-vs-new table pre-filled on the baseline side from the § *Recorded baseline* block in
`<interfaces>`: outcome, duration, audited call count, deep-research calls, claims reaching the
gates, group sessions, **skeptic parse crashes (baseline 24)**, cap-400s (baseline 776), unresolved
markers (baseline 28), real cost (baseline ≈$43-45), displayed cost, and terminal status. New-run
cells stay `TBD (V-01)`.

Finally, fix the wrong command in `15.2-VALIDATION.md` § Test Infrastructure / Supporting gates:
the intake-side gate is `gcloud builds submit . --config=cloudbuild.test.yaml` from the repo root,
not `--config=backend/cloudbuild.test.yaml` (that path does not exist).

**Planning-directory trap:** `.planning/` is gitignored. `15.2-UAT.md` must be committed with
`git add -f` or it stays invisible to every later agent and worktree.
  </action>

  <verify>
    <automated>grep -c '^## Phase 15.2 —' infra/DEPLOY-RUNBOOK.md</automated>
    <automated>bash -n tribunal/infrastructure/cloud-run/deploy-api.sh && bash -n tribunal/infrastructure/cloud-run/deploy-worker.sh && echo SYNTAX_OK</automated>
    <automated>grep -vE '^\s*#' tribunal/infrastructure/cloud-run/deploy-worker.sh | grep -c 'SERPAPI_API_KEY=\${TRIBUNAL_SERPAPI_SECRET}:latest'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## Phase 15.2 —' infra/DEPLOY-RUNBOOK.md` prints `1`.
    - `grep -cE '^### Step 15\.2\.[a-i] ' infra/DEPLOY-RUNBOOK.md` prints `9`.
    - `grep -c 'Running upgrade 0012 -> 0013' infra/DEPLOY-RUNBOOK.md` prints `1` or more, and the step containing it also contains the literal `gcloud run jobs update tribunal-migrate`.
    - `grep -c 'tribunal.tribunal_alembic_version' infra/DEPLOY-RUNBOOK.md` is `2` or more (the Phase-15.1 occurrence plus the new one).
    - `bash -n tribunal/infrastructure/cloud-run/deploy-api.sh` and `bash -n tribunal/infrastructure/cloud-run/deploy-worker.sh` both exit 0.
    - `grep -vE '^\s*#' tribunal/infrastructure/cloud-run/deploy-worker.sh | grep -c 'SERPAPI_API_KEY=\${TRIBUNAL_SERPAPI_SECRET}:latest'` prints `1`; the same command against `deploy-api.sh` prints `1`.
    - Both scripts contain `gcloud secrets describe "${TRIBUNAL_SERPAPI_SECRET}"` guarding that append, so a missing secret warns and continues instead of failing the deploy.
    - `grep -vE '^\s*#' tribunal/infrastructure/cloud-run/deploy-api.sh | grep -c 'Nestor_Claude:latest'` prints `0` — the hardcoded low-credit secret reference is gone from the live code path; the same command against `deploy-worker.sh` prints `0`.
    - Both scripts still contain `--no-allow-unauthenticated`, `tribunal-run@`, and (worker only) `--min-instances=1` — `git diff` on each script touches only the secret-composition block and its comments.
    - `.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-UAT.md` exists; `grep -cE '^\| *(1[0-6]|[1-9]) *\|' 15.2-UAT.md` counts exactly 16 checklist rows, and the file contains the literals `V-02 #16`, `comparison_id`, `1464b60d`, `verify_chain`, `checked_incidentally` and `[REDACTED]`.
    - `docs/tribunal-run-reports/V-01-COMPARISON.md` exists and contains `run-20260722-4cbb5311`, the baseline literals `24`, `776`, `28` and `1,162`, and at least ten `TBD (V-01)` cells.
    - `grep -c 'backend/cloudbuild.test.yaml' .planning/phases/15.2-*/15.2-VALIDATION.md` prints `0`.
    - `grep -rn 'SERPAPI' infra/DEPLOY-RUNBOOK.md` shows every one of the five original earmark lines now also naming `Step 15.2.b`.
    - No secret VALUE appears in any changed file: `git diff | grep -iE '(api[_-]?key|secret) *= *["'"'"'][A-Za-z0-9_\-]{16,}' ` returns no match.
    - `bash backend/scripts/ci_no_run_research.sh` exits 0 — nothing SerpApi-shaped was added to `backend/app/**` or `frontend/src`.
  </acceptance_criteria>

  <done>
    The runbook has an enumerated nine-step § Phase 15.2 that an operator can execute top to bottom
    without inventing a command; both deploy scripts bind `SERPAPI_API_KEY` when the secret exists,
    self-heal the Anthropic secret name from the live revision, and no longer hardcode
    `Nestor_Claude`; `15.2-UAT.md` carries the finalised 16-item V-02 checklist with concrete pass
    conditions; `V-01-COMPARISON.md` is pre-filled on the baseline side. Zero live GCP commands were
    run in this task.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: BLOCKING operator decision — the SerpApi plan/tier — and the Nestor_SerpApi secret</name>

  <files>
    infra/DEPLOY-RUNBOOK.md (Step 15.2.b — record the chosen tier by name)
  </files>

  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 15.2 Step 15.2.b (written in Task 1) — the exact create / grant / seed commands.
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-RESEARCH.md lines 1730-1738 — Open Question 1, the published price/quota/throughput table and the recommendation that the code stay plan-agnostic.
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-CONTEXT.md D-16 — "SerpAPI spend = recorded search count × published per-search unit price, exact and live"; the unit price in force is recorded alongside the run.
  </read_first>

  <decision>
    Which SerpApi plan/tier will be in force for the August V-01 run, and therefore for production?
  </decision>

  <context>
    The D10 own-researcher is the fourth research stream. Its spend must be a **fact**, not an
    estimate (C1: "NO ESTIMATES — facts and correct calculations only"), and the code already reads
    the plan live from `/account.json` and records it with the run, so **no code depends on this
    answer**. What depends on it is whether the August run can actually complete: the Free tier caps
    throughput at **50 searches/hour**, and a four-stream run issues its searches in bursts. A
    throttled own-researcher trips its breaker and the run finishes `completed_degraded` with one
    stream lost — which V-02 #6 would then have to record as a miss.

    This plan deliberately does **not** invent a tier. RESEARCH lists it as an open operator question
    and it is the last blocking prerequisite for V-01 besides the cap reset itself.
  </context>

  <options>
    <option id="free">
      <name>Free — $0/month, 250 searches/month, 50/hour</name>
      <pros>No spend. The honest unit price is `$0.00000`, and D-16's arithmetic yields a correct `$0.00` rather than an estimate or a "pending". Enough monthly quota for several validation runs.</pros>
      <cons>50 searches/hour will throttle a four-stream run mid-flight; the likely outcome is a tripped SerpApi breaker, one stream lost and a `completed_degraded` V-01 — a weaker proof of SC2's D10 clause.</cons>
    </option>
    <option id="starter">
      <name>Starter — $25/month, 1,000 searches/month, 200/hour</name>
      <pros>200/hour clears the expected in-run burst with the concurrency semaphore at 4. Unit price $0.02500, exact and published.</pros>
      <cons>$25/month standing cost. Highest unit price of the paid tiers.</cons>
    </option>
    <option id="developer">
      <name>Developer — $75/month, 5,000 searches/month, 1,000/hour</name>
      <pros>1,000/hour removes throttling as a variable entirely; unit price drops to $0.01500; headroom for repeated calibration runs through August.</pros>
      <cons>$75/month for a capability used by one stream of one pipeline.</cons>
    </option>
    <option id="defer">
      <name>Defer — ship without the secret, run V-01 as a clean 3-stream degraded run</name>
      <pros>Nothing blocks the deploy; the missing-secret path is already built and tested (`serpapi_key_missing` ⇒ breaker open at startup ⇒ 3-stream run), and it is itself a D-12 test path.</pros>
      <cons>V-02 #6 records a miss, SC2's "SerpAPI own researcher contributes" clause is unproven on the live run, and the phase would need a second live run later to close it.</cons>
    </option>
  </options>

  <human-action>
    If a paid tier is chosen: create/upgrade the account at serpapi.com, copy the API key from
    Dashboard → Your Account, and seed it with
    `gcloud secrets versions add Nestor_SerpApi --data-file=- --project="$GOOGLE_PROJECT"` (paste,
    then Ctrl-D). **Never** `--data="<key>"` and never inline in a command that lands in shell
    history. The value is never echoed, never committed, never written into the runbook.
  </human-action>

  <action>
Automation-first: run the automatable half BEFORE pausing, and pause only for what a human alone can
supply (the account, the tier and the key value).

1. Create the secret container — `gcloud secrets create Nestor_SerpApi --replication-policy=automatic
   --project="$GOOGLE_PROJECT" 2>/dev/null || true` (idempotent; a pre-existing secret is fine).
2. Grant the resource-scoped accessor to the Tribunal runtime SA ONLY —
   `gcloud secrets add-iam-policy-binding Nestor_SerpApi
   --member="serviceAccount:tribunal-run@${GOOGLE_PROJECT}.iam.gserviceaccount.com"
   --role="roles/secretmanager.secretAccessor" --project="$GOOGLE_PROJECT"`. Never `nestor-run`,
   never a project-wide binding.
3. Confirm with metadata reads only — `gcloud secrets describe Nestor_SerpApi` and
   `gcloud secrets get-iam-policy Nestor_SerpApi --format=json`. Never `gcloud secrets versions
   access`; this plan never reads the value.
4. PAUSE and present the four options above with their published prices, quotas and hourly
   throughputs.
5. On resume, record the chosen tier **by name** in `infra/DEPLOY-RUNBOOK.md` Step 15.2.b together
   with its $/month, monthly quota and hourly throughput, and state there that the unit price is read
   live from `/account.json` at run start so no price is hardcoded. If the answer is `defer`, record
   that instead, pre-mark V-02 #6 in `15.2-UAT.md` as an expected miss with the reason, and continue —
   Task 3 is unaffected, because the deploy scripts probe for the secret's existence before binding it.
  </action>

  <verify>
    <automated>gcloud secrets versions list Nestor_SerpApi --project="$GOOGLE_PROJECT" --format='value(name,state)'</automated>
    <automated>gcloud secrets get-iam-policy Nestor_SerpApi --project="$GOOGLE_PROJECT" --format='value(bindings.members)'</automated>
  </verify>

  <acceptance_criteria>
    - The chosen tier is recorded **by name** in `infra/DEPLOY-RUNBOOK.md` Step 15.2.b, together with its published $/month, monthly quota and hourly throughput. No unit price is hardcoded in any `.py` or `.json` file: `grep -rnE '0\.025|0\.015|0\.01000|0\.00917' tribunal/nestor_pulse_sdk --include=*.py --include=*.json | grep -v tests/` returns no match.
    - `gcloud secrets describe Nestor_SerpApi --project="$GOOGLE_PROJECT"` exits 0.
    - `gcloud secrets get-iam-policy Nestor_SerpApi --format='value(bindings.members)'` lists exactly `serviceAccount:tribunal-run@${GOOGLE_PROJECT}.iam.gserviceaccount.com` — `nestor-run` is absent and there is no project-wide `roles/secretmanager.secretAccessor` binding.
    - Unless the decision is `defer`: `gcloud secrets versions list Nestor_SerpApi --format='value(state)'` shows at least one `ENABLED` version. **Metadata only — the value is never accessed.**
    - If the decision is `defer`: that is recorded in Step 15.2.b and in `15.2-UAT.md` § Blocking prerequisites, V-02 #6 is pre-marked "expected miss — no SerpApi plan", and Task 3 proceeds unchanged.
    - No secret value appears in the shell transcript, the runbook, `15.2-UAT.md` or any commit.
  </acceptance_criteria>

  <done>
    `Nestor_SerpApi` exists with exactly one resource-scoped `secretAccessor` member
    (`tribunal-run`), the tier decision is recorded by name in Step 15.2.b, and either an ENABLED
    secret version exists or `defer` is recorded with V-02 #6 pre-marked. No secret value was read,
    echoed or committed.
  </done>

  <resume-signal>
    Reply with the tier: `free`, `starter`, `developer`, `production`, `big-data`, or `defer` — plus
    confirmation that `gcloud secrets versions list Nestor_SerpApi` shows an ENABLED version (or that
    you chose `defer`).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the deploy — dual Tribunal build at one $SHA, alembic 0013, four services live</name>

  <files>
    infra/DEPLOY-RUNBOOK.md (append the dated Deploy Record to § Phase 15.2)
  </files>

  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 15.2 Steps 15.2.a-15.2.h (written in Task 1) — execute them in order; this task adds nothing the runbook does not already enumerate.
    - infra/DEPLOY-RUNBOOK.md lines 2208-2217 — why the order is build → migrate → deploy, in the authors' own words. Getting it wrong is silent both ways.
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-01-PLAN.md — the `must_haves` block: exactly which columns, table, RLS state and CHECK values migration 0013 is supposed to produce. Step 15.2.d's schema confirm asserts against this list, not against memory.
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-17-PLAN.md — the six-gate sweep. Its SUMMARY must exist and be green before a single image is built.
    - tribunal/cloudbuild.test-gates.yaml lines 18-24 and 48-62 — the `ls … || true` guard, so you know why the `collecting:` block has to be read rather than the exit status.
  </read_first>

  <action>
Run § Phase 15.2 Steps **15.2.a → 15.2.h** in order, in Cloud Shell / with `gcloud` on the dev box.
No local Python, no local Docker: every build and every test runs in Cloud Build.

**Hard preconditions — abort the task rather than work around any of them:**
- `15.2-17-SUMMARY.md` exists and records all six gates green.
- `git status --porcelain` is empty and the stale-base artifact assertions in Step 15.2.a all pass.
  This project has hit stale worktree bases on 6 of 6 and 8 of 10 executor agents in the last two
  phases; Cloud Build ships the tree you submit, so a stale base produces a confident green deploy of
  code that is not this phase.
- Gate 1 and gate 2 are green **on this tree**, with the `collecting:` block showing the 15.2 test
  files by name.

**Then, in order:**
1. `export SHA="$(date +%Y%m%d-%H%M%S)"` — one `$SHA` for all four images, so the deploy is a single
   identifiable unit.
2. Build `tribunal-worker` and `tribunal-api` (Step 15.2.c). **Do not deploy them yet.**
3. Repin `tribunal-migrate` to `tribunal-api:${SHA}`, confirm the repin with `jobs describe`, execute
   `--wait`, then read the execution log and require the literal `Running upgrade 0012 -> 0013`
   (Step 15.2.d). **`Container called exit(0)` is not proof** — this exact silent no-op was hit live
   on 2026-07-22 and again avoided on 2026-07-25 only because of the repin. Confirm the schema
   read-only against 15.2-01's `must_haves` list, reading the head from
   `tribunal.tribunal_alembic_version`.
4. Deploy `tribunal-worker`, then `tribunal-api`, at `IMAGE_TAG=$SHA` through the edited scripts
   (Step 15.2.e). Confirm on both: new revision at 100% traffic, image resolving to an `@sha256:`
   digest, and `SERPAPI_API_KEY` + `ANTHROPIC_API_KEY` present as secret **references**. Confirm the
   Anthropic secret name matches what Step 15.2.a captured — if the self-heal picked a different
   name, stop and investigate before going further.
5. Rebuild and deploy `nestor-api` (Step 15.2.f). No migration, no new env, no new secret.
6. Rebuild and deploy `nestor-frontend` (Step 15.2.g). `_API_BASE_URL` comes from
   `gcloud run services describe nestor-api --format='value(status.url)'` — captured, never guessed
   (Pitfall 4); the `_FB_*` substitutions are the public Firebase web-config values already used by
   the live frontend build. `npm ci` is what the Dockerfile runs; never switch it.
7. Run all six gates plus the deployed-data `verify_chain` and the `/readyz` probe (Step 15.2.h).
   A red chain is a **STOP** — do not proceed to the park, investigate whether a non-additive field
   slipped into `_payload_for_row`.
8. Append a **Deploy Record** block to § Phase 15.2 with the date, the `$SHA`, the commit SHA, the
   four new revision names, the migrate execution id and its `Running upgrade` line, the six gate
   build ids with their results, and the confirmed secret bindings **by name only**. This is the same
   record format § Phase 15.1 Step 15.1.f carries.

**No live research run is triggered in this task.** The Anthropic monthly cap resets 2026-08-01;
triggering a run now burns the park and proves nothing.
  </action>

  <verify>
    <automated>gcloud run services describe tribunal-worker --region="$REGION" --project="$GOOGLE_PROJECT" --format='value(status.latestReadyRevisionName,status.traffic)'</automated>
    <automated>gcloud run services describe tribunal-api --region="$REGION" --project="$GOOGLE_PROJECT" --format='value(spec.template.spec.containers[0].env)' | tr ',' '\n' | grep -c 'SERPAPI_API_KEY'</automated>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>

  <acceptance_criteria>
    - The `tribunal-migrate` execution log contains the literal line `Running upgrade 0012 -> 0013`, and its execution id is recorded in the Deploy Record. An exit code alone is not accepted.
    - The tribunal head read from `tribunal.tribunal_alembic_version` is `0013`; `claim.certainty`, `claim.found_by` (`text[]`) and `claim_source.provider_quality` exist and are nullable; `tribunal.research_gap` exists with `rls enabled=true forced=true`, its tenant-isolation policy and its index; `ck_run_status` accepts `completed_degraded` and `parked`.
    - `gcloud run services describe` shows a NEW `latestReadyRevisionName` at 100% traffic for `tribunal-worker`, `tribunal-api`, `nestor-api` and `nestor-frontend`, and every revision's image resolves to an `@sha256:` digest, never `:latest`.
    - Unless Task 2 resolved to `defer`: the env dump of BOTH `tribunal-worker` and `tribunal-api` contains `SERPAPI_API_KEY` as a secret reference. Neither dump, nor any log line, nor the Deploy Record contains a key value.
    - Both Tribunal services still mount `ANTHROPIC_API_KEY` from the SAME secret name captured in Step 15.2.a — the deploy did not repoint it.
    - `nestor-api` env still carries `TRIBUNAL_SERVICE_URL` equal to the path-less `tribunal-api` URL, and `APP_BASE_URL` / `NESTOR_ADMIN_EMAIL` / `RESEND_API_KEY` are still present (confirm-only; do not re-set).
    - `curl -s -o /dev/null -w '%{http_code}' -H "Authorization: Bearer $(gcloud auth print-identity-token)" "$TRIBUNAL_URL/readyz"` prints `200`.
    - All six gates green against the deployed tree, each build id recorded; gate 3's `verify_chain` is GREEN on the deployed audit data; the `collecting:` block of gates 1 and 2 lists the 15.2 test files by name.
    - `bash backend/scripts/ci_no_run_research.sh` exits 0.
    - `git status --porcelain` was empty before the first build, and the built commit SHA is recorded in the Deploy Record.
    - No live research run was triggered: `gcloud logging read` for the worker over the session shows no new `run_id` entering `running`.
  </acceptance_criteria>

  <done>
    Alembic `0013` is live in the tribunal line (proven by the upgrade log line), all four services
    run new revisions at 100% traffic from digest-pinned images built at one `$SHA`,
    `SERPAPI_API_KEY` is bound on both Tribunal images without disturbing any other secret, the six
    gates and `verify_chain` are green, and the runbook carries a dated Deploy Record. The phase is
    now deployed and PARKED.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: PARK until 2026-08-01, then V-01 — one live run on a fresh intake against the recorded baseline</name>

  <files>
    docs/tribunal-run-reports/V-01-COMPARISON.md
    .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-UAT.md
  </files>

  <read_first>
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-UAT.md § Blocking prerequisites and § V-01 — written in Task 1.
    - docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md — §1 run overview, §3.5 the skeptic arm, §4.1 the F-01 crash (24 occurrences), §4.2 the 776-error cap wall, §4.4 the 28 stripped markers, §6 token accounting. This is the baseline column.
    - docs/tribunal-run-reports/run-20260722-4cbb5311/GROUPS.md — the four shipped contradictions, needed for V-02 #8 in the next task.
    - infra/DEPLOY-RUNBOOK.md § Phase 15.2 Deploy Record — the `$SHA` and revisions the run must execute on.
  </read_first>

  <what-built>
    Everything phase 15.2 builds is deployed and idle: the question workshop, the four-stream
    dispatch with the SerpApi own-researcher, the D8 fact lists with the D-14 fallback, the
    cross-provider merge ahead of the 15.1 gates, the R1-R7 reliability machine with park/resume, the
    D-05 anchor post-pass, the two deterministic report sections, and alembic 0013's persistence
    columns. None of it has ever executed against a live model: the Anthropic account has been at its
    monthly cap since 2026-07-22 and **resets 2026-08-01**. CI proves the wiring (D-02's stubbed
    end-to-end run, plan 15.2-17); only a live run proves the behaviour.
  </what-built>

  <action>
**This task parks the phase.** Do not start it before **2026-08-01** — until then the checkpoint
holds, which is D-01 working as designed, not a failure. Tasks 4-6 run in a fresh session after the
cap resets.

On or after 2026-08-01, drive the operator session described in `<how-to-verify>`: confirm the
prerequisites, get a fresh test intake to `decomposed`, trigger exactly ONE run, capture the live
feed evidence while the run is in flight, then record the outcome into
`docs/tribunal-run-reports/V-01-COMPARISON.md` and `15.2-UAT.md` § V-01 and run the SerpApi
audit-blob redaction check. Claude drives every automatable part (log queries, metrics reads, GCS
blob fetch, filling in both documents); the human creates the intake, clicks trigger, watches the
feed and — if the run parks — clicks Resume.
  </action>

  <how-to-verify>
    **Do not start before 2026-08-01.**

    1. Confirm the prerequisites: the cap has reset (a trivial Anthropic call succeeds), the SerpApi
       secret has an ENABLED version (or Task 2 resolved to `defer`), and the four services still run
       the revisions in the Deploy Record.
    2. **Create a FRESH test intake** reproducing the baseline brief domain — LUKOIL BeNeLux
       strategy: dynamic pricing, coffee, Germany-entry 2027 — in a clean space, and take it through
       to `decomposed`. Smoke tenant `1464b60d` was flagged for cleanup: either confirm it is
       suitable or create fresh. RESEARCH recommends fresh so the one run that has to prove the phase
       is not entangled with the cleanup backlog.
    3. Trigger **ONE** run from the intake detail page. **No A/B double-run** — the `comparison_id`
       harness stays available and unused; V-01 compares against the *recorded* baseline, which is
       what the deferred-ideas list decided.
    4. Watch the feed live and capture what only exists while the run is in flight: the workshop's
       candidate → cluster → critique → tournament → evolve rows, the winning questions appearing as
       they are chosen, per-row cost, and any retry rows. Screenshot or record — V-02 #1 and #14
       cannot be reconstructed after the run ends.
    5. If the run **parks**: the park mail should arrive with a Resume link; click it, confirm it
       resumes from the last checkpoint (no paid stage re-dispatched) and that the attempt counter
       did not increase. Record this for V-02 #2 — parking is a legitimate outcome, not a failure.
    6. When it reaches a terminal state, record into `V-01-COMPARISON.md`: outcome, wall-clock
       duration, audited call count, deep-research calls, claims reaching the gates, group sessions,
       **group-skeptic parse crashes (baseline 24 — the SC5 metric, expected 0)**, cap-400s (baseline
       776), unresolved anchors (baseline 28 stripped `[cite:]` markers), itemised cost (baseline
       ≈$43-45 real), and the terminal status with every degradation reason quoted verbatim.
    7. Fill in `15.2-UAT.md` § V-01: tribunal `run_id`, `research_run_id`, intake id, tenant id,
       start/end timestamps, deployed `$SHA`.
    8. Pull one `provider="serpapi"` audit body from the audit GCS bucket for this run and confirm it
       contains no full request URL and no unredacted `api_key` — the deploy-time security must-fix
       from RESEARCH § Open Questions #6.

    **If the run fails outright** (not `completed`, not `completed_degraded`, not `parked`): stop,
    record the failure and the stage it died in, and route the defect through
    `/gsd:plan-phase --gaps`. Do not sign anything off and do not run V-03.
  </how-to-verify>

  <verify>
    <automated>MISSING — requires a live LLM run; blocked until 2026-08-01 by the Anthropic monthly cap (D-01/D-02). CI's equivalent is `pytest nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py`, owned by plan 15.2-17 and already green.</automated>
    <automated>grep -c 'TBD (V-01)' docs/tribunal-run-reports/V-01-COMPARISON.md</automated>
  </verify>

  <acceptance_criteria>
    - `15.2-UAT.md` § V-01 carries a real tribunal `run_id`, `research_run_id`, intake id, tenant id, both timestamps and the deployed `$SHA`.
    - `docs/tribunal-run-reports/V-01-COMPARISON.md` has zero remaining `TBD (V-01)` cells: `grep -c 'TBD (V-01)' docs/tribunal-run-reports/V-01-COMPARISON.md` prints `0`.
    - The run's terminal status is one of `completed`, `completed_degraded` or `parked`, and if it is not `completed` every degradation/park reason is quoted verbatim in the comparison document.
    - **SC5:** the group-skeptic parse-crash count for this run is `0` against the baseline's 24, evidenced by a log query for `'str' object has no attribute 'get'` over the run window returning no rows.
    - Exactly ONE run was triggered on the test intake — no A/B pair, and `comparison_id` is unset on the run record.
    - The SerpApi audit-body check passes: no full request URL and no unredacted `api_key` in the sampled blob.
    - The live-feed evidence for V-02 #1 and #14 is captured (screenshots or a recording referenced from `15.2-UAT.md`) — these cannot be reconstructed after the run ends.
  </acceptance_criteria>

  <done>
    One live run exists on a fresh test intake, executed on the recorded `$SHA`, with its outcome,
    cost, funnel, skeptic-crash count and degradation reasons recorded in `V-01-COMPARISON.md` beside
    the 4cbb5311 baseline and its identifiers recorded in `15.2-UAT.md` § V-01. The SerpApi audit-blob
    redaction check passed. No A/B double-run was performed.
  </done>

  <resume-signal>
    Reply `v01-recorded` with the run id and terminal status, or `v01-failed` with the stage it died
    in (which routes to `/gsd:plan-phase --gaps`).
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: V-02 — the 16-item checklist and the operator sign-off next to the recorded baseline</name>

  <files>
    .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-UAT.md
  </files>

  <read_first>
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-UAT.md § V-02 — the 16 rows with their pass conditions, written in Task 1 and now to be filled in.
    - docs/tribunal-run-reports/V-01-COMPARISON.md — filled in by Task 4.
    - docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md and GROUPS.md — the report read side by side at sign-off, and the four contradictions for item 8.
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-CONTEXT.md § Decisions — the source of every pass condition; where an item is ambiguous, the decision file wins.
  </read_first>

  <what-built>
    The V-01 run exists and is recorded. V-02 is the acceptance gate: sixteen concrete checks derived
    from the operator decision files, each with a named pass condition, ending in the operator reading
    the new report beside the recorded 4cbb5311 baseline and signing off. Per D-02 these are
    deliberately **not** CI gates — they are the judgements CI cannot make (whether the tournament
    picks good questions, whether real providers emit well-formed fact lists, whether the merge
    clusters real phrasings, whether the own-researcher's research is useful, and at what rate the
    anchor instruction is actually followed).
  </what-built>

  <action>
Walk `15.2-UAT.md` § V-02 top to bottom with the operator, filling in the evidence and verdict cell
for each of the sixteen items, then record the dated sign-off. Claude gathers every machine-readable
piece of evidence (funnel numbers from the verification report, cost itemisation from the metrics
endpoint, `verify_chain` output, the unresolved-anchor count, the contradiction check against the
four recorded pairs); the operator supplies the judgement calls and the sign-off itself.

Also walk the two batched-in deferrals recorded in STATE.md while the operator is in the browser: the
Phase-15 populated-surface UAT (SC1-SC4) and the Phase-15.1 verdict/gate surfaces, both of which have
been waiting for exactly this run.

Any failed item goes into § Sign-off → "gaps found" with the file or stage it implicates, and the
phase routes to `/gsd:plan-phase --gaps` instead of proceeding. **Do not run Task 6 (V-03) on a
failed or partial checklist** — V-03 deletes the only fallback there is.
  </action>

  <how-to-verify>
    For each of the sixteen items, record **evidence** (a screenshot, a log line, an API response, a
    number from the verification report) and a **verdict** of pass / fail / not-exercised. Never leave
    a verdict blank and never mark an item pass on "looked fine".

    Three items are STOPs on their own:
    - **#12 `verify_chain` GREEN** on the new run's audit data. Red = no sign-off, full stop. This is
      the EU AI Act Art. 12 artifact and the deadline is 2026-08-02.
    - **#7** funnel reconciliation — if a claim lands in two buckets or none, the accounting is wrong
      and the report cannot be trusted.
    - **#13** cost itemisation — a silently missing cost class is exactly the gap C1 exists to close.
      Only Gemini grounding may read "pending".

    Item **#9** produces a number, not just a verdict: the unresolved-anchor count is the *only* live
    measure of how often the writing model actually emits the `[[c:…]]` anchor (RESEARCH A4 rates this
    medium-high risk and deliberately un-mitigable in CI). Record the number even if it passes.

    Item **#16** is the sign-off itself: read the new report beside
    `docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md` and write a dated line in § Sign-off.
  </how-to-verify>

  <verify>
    <automated>MISSING — LLM judgment quality is deliberately not a CI gate (D-02). The CI half of SC4 is plan 15.2-17's stubbed end-to-end run plus the rewired recorded replay, both already green.</automated>
    <automated>grep -cE '\| *(pass|fail|not-exercised) *\|' .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-UAT.md</automated>
  </verify>

  <acceptance_criteria>
    - All 16 rows carry both an evidence cell and a verdict of `pass`, `fail` or `not-exercised`: `grep -cE '\| *(pass|fail|not-exercised) *\|' 15.2-UAT.md` prints `16` or more.
    - Item #12 is `pass` — `verify_chain` GREEN on the new run's audit data. Any other value blocks the whole task.
    - Items #7 and #13 are `pass`, or the phase routes to `/gsd:plan-phase --gaps` instead of signing off.
    - Item #9 records the unresolved-anchor **count** as a number, not only a verdict.
    - § Sign-off carries a dated operator line stating the new report was read beside `docs/tribunal-run-reports/run-20260722-4cbb5311/REPORT.md`, and the frontmatter `signed_off` is set to that date.
    - Any `fail` verdict appears in § Sign-off → "gaps found" with the file/stage it implicates.
    - `15.2-UAT.md` is committed with `git add -f` (`.planning/` is gitignored — an uncommitted UAT file is invisible to every later agent and to worktree executors).
  </acceptance_criteria>

  <done>
    All sixteen V-02 items are recorded with evidence and a verdict, #7/#12/#13 pass, the
    unresolved-anchor count is recorded as a number, and `15.2-UAT.md` carries a dated operator
    sign-off written after reading the new report beside the recorded baseline. SC4 is closed and V-03
    is unblocked.
  </done>

  <resume-signal>
    Reply `v02-signed-off` (all sixteen recorded, #12/#7/#13 pass, dated sign-off written) — which
    unblocks V-03 — or `v02-gaps` with the failing item numbers.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 6: V-03 — the separate cleanup commit removing the old engine path (never claim_distiller)</name>

  <files>
    tribunal/nestor_pulse_sdk/pipeline/tribunal/intake.py
    tribunal/nestor_pulse_sdk/pipeline/tribunal/__init__.py
    tribunal/nestor_pulse_sdk/pipeline/tribunal/grouping.py
    tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
    tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py
    tribunal/nestor_pulse_sdk/scripts/probe_intake.py
    tribunal/nestor_pulse_sdk/tests/test_tribunal_intake.py
    tribunal/nestor_pulse_sdk/tests/test_tribunal_grouping.py
    infra/DEPLOY-RUNBOOK.md
  </files>

  <read_first>
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-CONTEXT.md D-03, D-14 and **D-15** — what V-03 removes, and the one piece of the old distiller that survives deletion on purpose.
    - .planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-13-SUMMARY.md and 15.2-15-SUMMARY.md — the authoritative list of what those plans left **unreferenced**. Delete only what they unwired; if a summary does not name a symbol, grep before touching it.
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/intake.py — the whole file. `adaptive_intake` is at **311**, `_intake_once` at **374**, `_parse_clear_brief` at **205**, and `detect_explicit_questions` at **168**. That last one is **imported by the workshop (15.2-10/15.2-13) and MUST survive**, together with its tests.
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/grouping.py lines 66-100 and 300-320 and 470-485 — `_CLUSTER_ENABLED`, `_exact_keys` and the `if not _CLUSTER_ENABLED:` A/B branch: the exact-key grouping remnant.
    - tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py line 763 — `claim_distiller`, and 723 — `_dedupe_claims`. **Neither is deleted.** D-15: "the planner must not treat 'remove the old engine path' as 'delete claim_distiller'."
    - tribunal/nestor_pulse_sdk/citations/extractor.py line 317 — `extract_and_persist_citations`. RESEARCH flags it as superseded, but it is **NOT in V-03's named scope** and `pipeline/orchestrator.py:92` still calls it. Surface it as a follow-up; do not delete it.
    - infra/DEPLOY-RUNBOOK.md § Phase 15.1 Step 15.1.b — documents `NESTOR_TRIBUNAL_CLUSTER=false` as a live rollback lever. Deleting `_exact_keys` removes that lever, so the runbook line must be corrected in the SAME commit.
  </read_first>

  <action>
**Hard precondition:** `15.2-UAT.md` frontmatter carries a non-null `signed_off` date and V-02 items
#7, #12 and #13 read `pass`. If not, stop — V-03 has no fallback flag and no dual-run by decision
(D-03/V-03); its only recovery is reverting this commit.

Make **one commit, containing only deletions and their direct consequences**, so that reverting it is
a single, safe operation. Nothing else rides along.

**Remove (each only after `grep -rn <symbol> tribunal/` proves zero remaining references):**
- `pipeline/tribunal/intake.py` — `adaptive_intake` (311) and `_intake_once` (374), plus the constants
  and helpers that become unreferenced with them (`_INTAKE_MODEL`, `_MAX_OUTPUT_TOKENS`,
  `_INTAKE_PROMPT_TEMPLATE`, `_COVERAGE_RETRY_NOTE`, the `RESEARCH_PROMPT_*` sentinels and
  `_parse_clear_brief` — grep each one individually; the workshop clones the *shape* of the sentinel
  parser but does not import it). **The module stays. `detect_explicit_questions` stays.**
- `pipeline/tribunal/__init__.py` — the `adaptive_intake` import and its `__all__` entry. `TAXONOMY`
  and `STAKES_TIERS` are untouched. Refresh the module docstring's stage list so it describes the
  workshop, not the one-call intake.
- `pipeline/tribunal/pipeline.py` — the now-dead `from …intake import adaptive_intake` (52) and any
  comment block describing the delegator step. The workshop call that replaced it (15.2-13) stays.
- `pipeline/tribunal/grouping.py` — `_exact_keys` (306), the `if not _CLUSTER_ENABLED:` A/B branch
  (~476) and the `_CLUSTER_ENABLED` knob itself, with the docstring lines that advertise
  `NESTOR_TRIBUNAL_CLUSTER=false` as a fallback. `group_claims` and `_parse_cluster_lines` are the
  live merge path (B-04) and are untouched.
- `pipeline/synthesis/steps.py` — only the **distiller-as-primary-source** remnants named unreferenced
  by 15.2-15's summary (the whole-run prompt/entry path). **`claim_distiller` and `_dedupe_claims`
  stay in full** — D-14 routes a provider with no usable fact list through `claim_distiller` in
  full-extraction mode, so this is a live production path, not legacy.
- `scripts/probe_intake.py` — the dev probe whose only purpose is exercising `adaptive_intake`.
  Delete the file.
- `tests/test_tribunal_intake.py` — drop the `adaptive_intake` cases; **keep the
  `detect_explicit_questions` cases** (they cover a function the workshop now depends on). If nothing
  but those remain, rename the class/docstring so the file describes what it now tests.
- `tests/test_tribunal_grouping.py` — drop the `_exact_keys` / `NESTOR_TRIBUNAL_CLUSTER=false` cases;
  keep every clustering and contradiction case, including the four recorded pairs added by 15.2-15.

**Explicitly protected — deleting any of these is a task failure:**
`claim_distiller`, `_dedupe_claims`, `test_claim_distiller.py`, `test_distiller_coverage.py` (D-15) ·
`intake.detect_explicit_questions` and its tests · `grouping.group_claims` /
`grouping._parse_cluster_lines` · `citations.extract_and_persist_citations` (still called by
`pipeline/orchestrator.py:92`; superseded but out of V-03's named scope — record it in the SUMMARY as
a follow-up for a later phase, do not silently delete it) · every fixture under
`tests/fixtures/run_4cbb5311/`.

**Runbook, same commit:** correct § Phase 15.1 Step 15.1.b's rollback-lever line to say that
`NESTOR_TRIBUNAL_CLUSTER=false` was removed by V-03 (phase 15.2) and is no longer available, and add
a V-03 note to § Phase 15.2 Step 15.2.i recording the commit SHA. Removing a documented rollback lever
without correcting the runbook leaves an operator reaching for a switch that no longer exists.

**Then re-run the full six-gate sweep.** V-03 is a deletion commit: the only thing that proves it is
safe is the suite. `test_claim_distiller.py` and `test_distiller_coverage.py` must be green in the
`collecting:` block, not merely absent from the failures.

**Deploy is NOT part of this task.** V-03 removes unreferenced code; nothing about the deployed
behaviour changes. Ship it with the next deploy rather than burning a redeploy cycle on a no-op —
and record that decision in the SUMMARY.
  </action>

  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test.yaml --project="$GOOGLE_PROJECT"</automated>
    <automated>grep -rn 'adaptive_intake' tribunal/ | grep -v '\.md:' | wc -l</automated>
    <automated>grep -rn 'def claim_distiller\|def detect_explicit_questions' tribunal/nestor_pulse_sdk | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn 'adaptive_intake' tribunal/ | grep -v '\.md:'` returns no match — no import, no call, no `__all__` entry, no script reference.
    - `grep -rn '_exact_keys\|NESTOR_TRIBUNAL_CLUSTER' tribunal/nestor_pulse_sdk --include=*.py` returns no match.
    - `grep -c 'def claim_distiller' tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py` prints `1` and `grep -c 'def _dedupe_claims' …/steps.py` prints `1` — **D-15 holds**.
    - `grep -c 'def detect_explicit_questions' tribunal/nestor_pulse_sdk/pipeline/tribunal/intake.py` prints `1`, and `tests/test_tribunal_intake.py` still imports it.
    - `grep -c 'async def extract_and_persist_citations' tribunal/nestor_pulse_sdk/citations/extractor.py` prints `1` — out of scope, still called by `pipeline/orchestrator.py`.
    - The full suite (gate 4) is green with `test_claim_distiller.py` and `test_distiller_coverage.py` in the `collecting:` block; gates 1, 2, 3, 5 and 6 are green too.
    - `git show --stat HEAD` shows a single commit whose changed files are exactly the ones listed in this task's `<files>` — no unrelated change rides along, so the commit is revertible on its own.
    - `infra/DEPLOY-RUNBOOK.md` no longer advertises `NESTOR_TRIBUNAL_CLUSTER=false` as an available rollback lever, and § Phase 15.2 Step 15.2.i records this commit's SHA.
    - The SUMMARY records `extract_and_persist_citations` as a surfaced, undeleted follow-up.
  </acceptance_criteria>

  <done>
    The old engine path is gone in one revertible commit — `adaptive_intake`, the exact-key grouping
    remnant and the distiller-as-primary wiring — with no feature flag and no dual-run period.
    `claim_distiller`'s full-extraction mode, `detect_explicit_questions` and
    `extract_and_persist_citations` all survive with green tests, the runbook no longer promises a
    rollback lever that no longer exists, and all six gates are green. **ENGINE-11 and ENGINE-05 are
    closed.**
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Secret Manager | The SerpApi key value passes through a human's terminal; shell history, `gcloud` argv and CI logs are all leak surfaces |
| Secret Manager → Cloud Run revision | `--set-secrets` REPLACES the entire mapping — a partial list silently unbinds live secrets |
| local git tree → Cloud Build → deployed image | Cloud Build ships the tree that is submitted; a stale base deploys code that is not this phase, and reports success |
| migrate Job image → production schema | A stale image pin makes `alembic upgrade head` a silent no-op that exits 0 |
| deployed engine → serpapi.com | The key crosses as a **query parameter**; a naive full-URL log or exception leaks it into logs and the 7-year audit bucket |
| live run → audit GCS bucket | Anything written there is retained for 7 years and is the legal artifact (`verify_chain`) |
| V-03 deletion → engine correctness | Deleting a protected symbol removes a live production path with no flag to fall back to |

**Threat ID block: `T-15.2-90` … `T-15.2-99` is reserved for plan 15.2-18** (siblings hold 01-08,
21-29, 30-39, 40-48, 60-66, 71-79, 80-87, 101-109).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15.2-90 | Information disclosure | SerpApi key value in shell history / logs / repo | **high** | mitigate | Value seeded **only** via `gcloud secrets versions add … --data-file=-` (stdin, Ctrl-D). `--data=` is forbidden. Every verification reads names and metadata (`secrets versions list`, `services describe … env`), never `secrets versions access`. Gated by the Task-1 criterion that `git diff` contains no key-shaped literal and by the Task-2/3 criteria that no value appears in the transcript or the Deploy Record. |
| T-15.2-91 | Information disclosure | Key or full request URL inside a 7-year audit blob | **high** | mitigate | Code-side already gated by 15.2-12 (`_safe_url`, no `raise_for_status`, `request_dict` with no URL, `gcs_blob._redact_dict`). This plan adds the **deploy-time** proof RESEARCH § Open Questions #6 demands: Task 4 pulls a real `provider="serpapi"` audit body from the live run and asserts no full URL and no unredacted `api_key`. Treated as a must-fix, not a nice-to-have. |
| T-15.2-92 | Tampering | Stale build source → deployed images without the phase's code | **high** | mitigate | Step 15.2.a's stale-base guard: clean `git status`, recorded commit SHA, and a positive on-disk assertion for one artifact per wave (0013, `reliability.py`, `stage_feed.py`, `facts.py`, `anchors.py`, `workshop*.py`, `own_researcher.py`, `serpapi.py`, `test_engine_e2e_stubbed.py`, `cloudbuild.test-engine.yaml`, `completed_degraded` in the frontend). Plus: read the gates' `collecting:` block, because `ls … \|\| true` makes a missing file a silent skip. Hit 6-of-6 and 8-of-10 agents in the last two phases — this is systematic here, not hypothetical. |
| T-15.2-93 | Tampering | Stale `tribunal-migrate` image pin → silent no-op migration | **high** | mitigate | Repin → `jobs describe` confirm → `execute --wait` → **require the literal `Running upgrade 0012 -> 0013` in the execution log**; `Container called exit(0)` is explicitly not accepted. Order is build → migrate → deploy, so a writer never ships ahead of its column into a swallowed `except`. Hit live 2026-07-22; avoided 2026-07-25 only by the repin. |
| T-15.2-94 | Denial of service | `--set-secrets` drops `SERPAPI_API_KEY` or repoints `ANTHROPIC_API_KEY` to the low-credit secret | **high** | mitigate | Both scripts compose the secret list in a variable, append SERPAPI only behind a `gcloud secrets describe` existence probe, and **self-heal the Anthropic secret name from the live revision** before falling back to a documented default — the same guard shape the file already uses for `TRIBUNAL_SERVICE_URL`. Post-deploy criterion asserts the Anthropic secret name is unchanged from the Step-15.2.a capture. Without this, a script re-run walls the ~$45 V-01 run on a low-credit account. |
| T-15.2-95 | Elevation of privilege | Over-broad IAM on the new secret | medium | mitigate | Resource-scoped `roles/secretmanager.secretAccessor` on `Nestor_SerpApi` to `tribunal-run` **only**. Acceptance criterion asserts `get-iam-policy` lists exactly that one member, that `nestor-run` is absent, and that no project-wide binding exists. A compromised intake service must not reach the Tribunal search key. |
| T-15.2-96 | Denial of service (denial of wallet) | The V-01 run's own spend | medium | mitigate | Exactly ONE run, on a test intake, no A/B double-run. The budget governor is inert by decision (D-11/16-D-02), so the real bounds are R2's breaker, `_MAX_ANGLES`, `_SKEPTIC_CONCURRENCY`, the tournament round count and the WR-01 coverage-surface fix. The SerpApi tier — including its hourly throttle — is decided and recorded before the run rather than discovered during it. |
| T-15.2-97 | Repudiation | `verify_chain` red after 0013 or on the new run's data | **high** | mitigate | Gate 3 green is required both pre-deploy and post-deploy (Step 15.2.h), and V-02 #12 is a STOP on the live run's audit data. 0013 adds DB columns, not audit-payload fields, so the frozen 11-field payload is untouched (`test_hash_chain_replay.py`). EU AI Act Art. 12, deadline **2026-08-02** — a red chain blocks sign-off outright. |
| T-15.2-98 | Tampering | V-03 deletes protected production code | **high** | mitigate | Explicit protected list (`claim_distiller`, `_dedupe_claims` and their two test files per D-15; `detect_explicit_questions`; `group_claims`/`_parse_cluster_lines`; `extract_and_persist_citations`, still called by `pipeline/orchestrator.py:92`), a grep gate per protected symbol in the acceptance criteria, deletion only after `grep -rn` proves zero references, the full suite as the gate, and a **single-file-scoped commit** so revert is one operation. D-03 gives no flag and no dual-run — revertibility is the entire safety net. |
| T-15.2-99 | Information disclosure | Running V-01 against real client data | medium | mitigate | A **fresh** test intake in a clean space reproducing the baseline brief domain, never a real client intake. Smoke tenant `1464b60d` was flagged for cleanup — confirm suitability or create fresh (RESEARCH recommends fresh). Tenant isolation itself is unchanged by this plan: no new endpoint, no new table, no widened RLS surface. |
| T-15.2-9SC | Tampering | Package supply chain | **high** | mitigate | **No package is installed by this plan.** No `pip install`, no `npm install` — the frontend Dockerfile runs **`npm ci`** against the committed `frontend/package-lock.json`, and `tribunal/requirements.txt` is untouched. `google-search-results` remains rejected (15.2-12). RESEARCH § Package Legitimacy Audit records zero `[ASSUMED]`/`[SUS]`/`[SLOP]` packages for this phase, so no legitimacy checkpoint is owed. Gated by `git diff --stat -- tribunal/requirements.txt frontend/package.json frontend/package-lock.json` being empty. |

All **high**-severity threats are blocking: T-15.2-90, -91, -92, -93, -94, -97, -98 and -9SC each
have a grep gate, a required log line, or a named acceptance criterion above, and the plan is not
done until they pass.
</threat_model>

<verification>
Run in this order. No local Python or Docker on this machine — everything executes in Cloud Build.

**Before any image is built (Task 3 preconditions):**
1. `15.2-17-SUMMARY.md` exists and records six green gates.
2. `git status --porcelain` empty; commit SHA recorded; every Step-15.2.a artifact assertion passes.
3. The six gates, in order, each read for its `collecting:` block and not just its exit status:
   `cloudbuild.test-engine.yaml` → `cloudbuild.test-gates.yaml` → `cloudbuild.test-critical.yaml` →
   `cloudbuild.test.yaml` (live-key failures triaged, not ignored) →
   `gcloud builds submit . --config=cloudbuild.test.yaml` → `bash backend/scripts/ci_no_run_research.sh`.

**After the deploy (Step 15.2.h):**
4. `Running upgrade 0012 -> 0013` present in the migrate execution log; tribunal head `0013` read
   from `tribunal.tribunal_alembic_version`.
5. Four services on new revisions at 100% traffic, each image an `@sha256:` digest.
6. `SERPAPI_API_KEY` bound as a secret reference on both Tribunal images; `ANTHROPIC_API_KEY`
   unchanged from the preflight capture. Names only — no value is ever read.
7. `verify_chain` GREEN on deployed audit data (gate 3). Red = STOP.
8. `/readyz` returns 200 on `tribunal-api` with an identity token, against the path-less URL.
9. All six gates green again against the deployed tree.

**After V-03 (Task 6):**
10. Gates 1-6 green; `test_claim_distiller.py` and `test_distiller_coverage.py` present in the
    `collecting:` block and passing.

**Deliberately NOT verified in CI** (D-02 — these are V-02 items on the August live run): whether
the tournament picks good questions (#1/#3/#4), whether real providers emit well-formed fact lists
and how often D-14's fallback fires (#5), whether the merge clusters real cross-provider phrasings
(#8), whether the own-researcher's research is useful (#6), the anchor **emission rate** (#9 — the
unresolved-anchor count is the metric), true end-to-end cost and duration (V-01), and operator
sign-off (#16).
</verification>

<success_criteria>
- `infra/DEPLOY-RUNBOOK.md` carries an enumerated § Phase 15.2 (Steps 15.2.a-15.2.i) plus a dated
  Deploy Record, and all five SERPAPI earmarks point at Step 15.2.b.
- Both Tribunal deploy scripts bind `SERPAPI_API_KEY` behind an existence probe and self-heal the
  Anthropic secret name from the live revision; a re-run cannot drop or repoint a secret.
- `Nestor_SerpApi` exists with a resource-scoped `secretAccessor` to `tribunal-run` only, and the
  SerpApi tier is an operator decision recorded by name — no unit price is hardcoded anywhere.
- Tribunal alembic head is `0013`, proven by the literal `Running upgrade 0012 -> 0013` log line.
- `tribunal-worker`, `tribunal-api`, `nestor-api` and `nestor-frontend` all run new, digest-pinned
  revisions built from one `$SHA` off a provably non-stale tree.
- All six gates and `verify_chain` are green before and after the deploy.
- The phase **parks** at Task 4 without failing; no task required a live Anthropic call before
  2026-08-01.
- **V-01:** one live run on a fresh test intake, recorded in `V-01-COMPARISON.md` beside the 4cbb5311
  baseline, with zero group-skeptic parse crashes against the baseline's 24 (SC5) and no A/B
  double-run.
- **V-02:** all sixteen items recorded with evidence and a verdict; #7, #12 and #13 pass; the
  unresolved-anchor count recorded as a number; a dated operator sign-off next to the recorded
  baseline (SC4).
- **V-03:** a single revertible cleanup commit after sign-off removes `adaptive_intake`, the
  distiller-as-primary wiring and the exact-key grouping remnants, with `claim_distiller` (D-15),
  `detect_explicit_questions` and `extract_and_persist_citations` all surviving and green (SC5).
- All eight **high**-severity threats have a passing gate.
</success_criteria>

<open_decisions_surfaced>
Two things this plan deliberately does **not** invent, and one it deliberately does not delete:

1. **The SerpApi plan/tier** — Task 2, a blocking operator decision. Free is 250/month at a 50/hour
   throttle with an honest `$0.00000` unit price that would likely throttle a four-stream run; the
   paid tiers are $25/$75/$150/$275 per month. The code is plan-agnostic (`/account.json` at run
   start), so nothing is blocked technically — but the tier must be settled before V-01, because a
   throttled own-researcher costs SC2 its D10 clause.
2. **Which intake V-01 uses** — RESEARCH recommends a **fresh** intake reproducing the baseline brief
   domain in a clean space. Smoke tenant `1464b60d` was flagged for cleanup; entangling the one run
   that has to prove the phase with the cleanup backlog is the risk being avoided. Recorded in
   `15.2-UAT.md` § V-01 for the operator to confirm.
3. **`extract_and_persist_citations` (`citations/extractor.py:317`)** — RESEARCH flags it as
   superseded by `persist_tribunal_claims`, but it is **not** in V-03's named scope and
   `pipeline/orchestrator.py:92` still calls it. Task 6 surfaces it as a follow-up for a later phase
   and does not touch it. Silently deleting a symbol the decision files never named would be exactly
   the scope drift D-15 was written to prevent.
</open_decisions_surfaced>

<output>
Create `.planning/phases/15.2-research-engine-redesign-engine-core-inserted-2026-07-24/15.2-18-SUMMARY.md` when done.
</output>
