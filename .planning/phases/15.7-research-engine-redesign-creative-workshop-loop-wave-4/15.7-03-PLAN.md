---
phase: 15.7-research-engine-redesign-creative-workshop-loop-wave-4
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py
  - tribunal/nestor_pulse_sdk/tests/test_workshop_loop.py
  - tribunal/cloudbuild.test-engine.yaml
autonomous: true
requirements: [D-R9, D-W4-3, D-W4-5, D-W4-6, D-W4-7]

must_haves:
  truths:
    - "D-R9: the tournament round count is DERIVED from the candidate count, so it cannot silently under-separate as the population grows"
    - "D-W4-3: a newcomer's catch-up budget is the field's MEDIAN match count, computed deterministically without floats"
    - "D-W4-5: the winner cut is a floor of 5 per client question plus 2 cross-cutting, and a KEEP is preferred over a WEAK when filling any slot"
    - "D-W4-5: no candidate is ever barred by selection — everything below the cut comes back promotable"
    - "D-W4-6: all three exit criteria are evaluated, coverage is criterion 1 and quality is criterion 2, and a resurrected candidate cannot satisfy quality"
    - "D-W4-6 Exemption A: a cross-cutting question never counts as WEAK for the quality criterion"
    - "D-W4-7: per-round population and spend are RECORDED, and nothing is enforced"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py"
      provides: "the pure loop arithmetic: tournament_rounds, catch_up_matches, select_winners, exit_verdict, round_metrics"
      min_lines: 250
      exports: ["tournament_rounds", "catch_up_matches", "select_winners", "exit_verdict", "round_metrics"]
    - path: "tribunal/nestor_pulse_sdk/tests/test_workshop_loop.py"
      provides: "the pure-arithmetic test suite, including the REQUIRED strong-late-newcomer test"
    - path: "tribunal/cloudbuild.test-engine.yaml"
      provides: "the new test file registered and EXPECTED_FILES raised in the SAME edit"
      contains: "EXPECTED_FILES=36"
  key_links:
    - from: "tribunal/cloudbuild.test-engine.yaml WANTED list"
      to: "nestor_pulse_sdk/tests/test_workshop_loop.py"
      via: "the gate file list, whose count must move with it"
      pattern: "test_workshop_loop"
---

<objective>
Build `workshop_loop.py`: every piece of Wave 4 arithmetic that can be a PURE function, in one
stdlib-only module that is drivable on this machine's bundled interpreter.

Five functions: the derived tournament round count (D-R9), the newcomer catch-up budget (D-W4-3), the
winner selection with the floor and prefer-KEEP (D-W4-5), the three-criteria exit verdict (D-W4-6),
and the per-round instrumentation record (D-W4-7).

Purpose: the measurement harness proved this DESIGN converges — it did not prove any implementation
does. The only way Wave 4 gets the same guarantee is if the arithmetic is separable, pure and driven
over hundreds of cases here, rather than argued about inside an async LLM loop. Phase 15.6's
constraint said it in one line: prefer pure, importable helpers precisely so they are drivable.
Output: one new module, one new registered test file, one gate bump.

This plan is the SOLE editor of `cloudbuild.test-engine.yaml` in phase 15.7. No other plan may touch
it. An exact-set assertion over a file a sibling plan also edits is a scheduled failure, and it cost
phase 15.5 a red merged tree that three green verifications missed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_preflight gate="blocking">
Worktrees here are cut from a STALE ref — 7 of 7 in phase 15.6, at 486-524 commits behind, distances
GROWING. `git rev-list --count BASE..HEAD == 0` reads GREEN while stale, because the stale ref is
usually the merge-base. Assert the MERGE-BASE:

    git merge-base HEAD <PHASE_BASE_SHA>

Anything other than `<PHASE_BASE_SHA>` means stale — `git reset --hard <PHASE_BASE_SHA>` first.
Positive sentinels: `_text_key` in `research_division.py`, `_norm_url` in `discovery_bracket.py`, and
`EXPECTED_FILES=35` in `cloudbuild.test-engine.yaml`. If any is missing, STOP and report.
</worktree_preflight>

<proving_things_on_this_machine>
No pytest, no Docker, no `python3`. Stdlib-only Python 3.14 at
`C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`.

This module is designed to make that a strength rather than a workaround: it must import with NOTHING
from `nestor_pulse_sdk`, so you can `import` it directly and drive every function over hundreds of
cases in seconds. Do exactly that. Set `PYTHONIOENCODING=utf-8`.
Prove non-vacuity by mutating the real source text and confirming each check bites.
</proving_things_on_this_machine>

<context>
@.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-CONTEXT.md
@tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py
@tribunal/cloudbuild.test-engine.yaml

<interfaces>
What the existing code already decides, which these functions must NOT re-decide:

  `workshop_rank._TOURNAMENT_ROUNDS = int(env "NESTOR_TRIBUNAL_WORKSHOP_ROUNDS", "4")`   (:800)
      Today a fixed 4. Plan 15.7-08 will pass it into `tournament_rounds` as an OVERRIDE rather than
      duplicating the formula. This module owns the formula; `workshop_rank` owns the override.
  `workshop_rank.winner_count(n_candidates) -> int`                                       (:846)
      `min(15, max(10, ceil(0.35 x C)))`, bounded by C. THIS FORMULA IS NOT DUPLICATED HERE.
      `select_winners` takes a `default_cut` ARGUMENT and the caller passes `winner_count(...)`.
      Duplicating it would be the two-authorities defect this phase exists to stop repeating.
  `workshop_rank._apply_elo` docstring (:875-885): "ELO IS THE TIE-BREAK, NOT THE PRIMARY KEY."
  the standing sort (:1524): `sorted(entries, key=lambda e: (-e["wins"], -e["elo"], e["index"]))`.
      WINS IS PRIMARY. This is why D-R11's median seed is inert and why D-W4-3 replaces it with a
      SCHEDULE. This module must not touch the sort — it computes how many extra MATCHES a newcomer
      plays, and the sort then runs unmodified.
  `workshop_rank._KEEP = "KEEP"` / `_WEAK = "WEAK"` / `_KILL = "KILL"` — the verdict vocabulary.
      Re-declare these three literals locally with a comment saying WHY (this module must not import
      `workshop_rank`; that import would be circular once `workshop_rank` calls this module).
  candidate/winner dict keys this module reads: `text`, `parent`, `parents`, `index`, `rank`,
      `critique`, `flaw`, `source`, `wins`, `elo`, `byes`, `resurrected`, and two keys Wave 4 adds:
      `cross_cutting` (bool) and `born_round` (int).
  `discovery_bracket.DISCOVERY_PARENT = "__discovery__"` — re-declare the literal locally, same
      reason, same comment.

Measured targets these functions must reproduce (`exp11`, the validated configuration):
  3 client questions, 12 generated each -> 17 winners (5 + 5 + 5 + 2), none WEAK, exit in round 4.
  Catch-up, 8 rounds, newcomer entering round 6, ranking code UNCHANGED:
      strong 99.8% / median 29.5% / weak 1.8% reach the top N.
      Without it (median seed or flat 1200): 1.5% / 1.5% / 0.0%.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The tournament arithmetic — derived round count and the catch-up budget</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py, tribunal/nestor_pulse_sdk/tests/test_workshop_loop.py, tribunal/cloudbuild.test-engine.yaml</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 779-816 — the tournament tunables block and its MEDIUM-CONFIDENCE caveat, which sets the comment register for this module
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 846-970 — `winner_count`, `_apply_elo`, `_pair_key`, `_pair_round`
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 1442-1532 — the round loop and the standing sort
    - tribunal/cloudbuild.test-engine.yaml lines 320-410 — the WANTED list, EXPECTED_FILES, and the NO-APOSTROPHES caution above it
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decisions D-R9 and D-W4-3
  </read_first>
  <behavior>
    - `tournament_rounds(17)` returns 6 — at least 6 matches per candidate within one loop round,
      which is what D-R9's ">= 5-6 matches" means now that ratings persist across rounds
    - `tournament_rounds(3)` returns 2 — a field of 3 has only 2 distinct opponents, so the floor
      yields to `n - 1` rather than scheduling rematches to reach 6
    - `tournament_rounds(0)` and `tournament_rounds(1)` return 0 — there is nothing to rank
    - `tournament_rounds(200)` never exceeds the maximum, and `tournament_rounds(n)` is monotonically
      non-decreasing in n across 0..200
    - `tournament_rounds(17, override=2)` returns 2 — an explicit operator setting always wins
    - `catch_up_matches([12, 12, 11, 0, 0])` returns 11 — the LOW median, an integer, no float anywhere
    - `catch_up_matches([])` returns 0; a list containing a negative or non-integer entry does not raise
  </behavior>
  <action>
    Create `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py`.

    Module docstring: this module holds every piece of Wave 4 loop arithmetic that can be pure. It
    imports NOTHING from `nestor_pulse_sdk` — deliberately, for two reasons that must both be stated:
    `workshop_rank` will import IT, so the reverse import would be circular; and being stdlib-only is
    what makes it drivable on the one interpreter available on the development machine, which is how
    phase 15.6 proved its own work. No function here raises, and no function here makes a call.

    Constants, each with an env knob in the house `NESTOR_TRIBUNAL_*` idiom and one comment per number
    saying what the number is FOR (the `research_division.py:126-172` style this repo uses):
      `_TOURNAMENT_ROUNDS_MIN` default 6  (env `NESTOR_TRIBUNAL_WORKSHOP_ROUNDS_MIN`)
      `_TOURNAMENT_ROUNDS_MAX` default 10 (env `NESTOR_TRIBUNAL_WORKSHOP_ROUNDS_MAX`)
      `_LOOP_MAX_ROUNDS`       default 10 (env `NESTOR_TRIBUNAL_WORKSHOP_LOOP_ROUNDS`)
      `_FLOOR_PER_QUESTION`    default 5  (env `NESTOR_TRIBUNAL_WORKSHOP_FLOOR_PER_Q`)
      `_CROSS_CUTTING_SLOTS`   default 2  (env `NESTOR_TRIBUNAL_WORKSHOP_CROSS_SLOTS`)
    Guard every int conversion so a garbled env value falls back to the default instead of raising at
    import.

    Implement `tournament_rounds(n_candidates, *, override=0)`:
      returns 0 for fewer than 2 candidates; returns `override` when `override` is a positive integer;
      otherwise `min(_TOURNAMENT_ROUNDS_MAX, max(_TOURNAMENT_ROUNDS_MIN, ceil(log2(n))), n - 1)`.
      Docstring must record the measurement that forced it: `_TOURNAMENT_ROUNDS = 4` over 17
      candidates gives each candidate only 3.76 matches, and the harness reproduced V-01's exact
      symptom — three candidates finishing at Elo exactly 1200.00 with 2 wins each, straddling the
      top-10 cut, one of them losing its research slot to INDEX ORDER. Carried Elo plus 5 Swiss rounds
      plus the catch-up schedule eliminated the ties entirely: zero candidates at exactly 1200 in any
      round, and rank 10 vs 11 became a decision by Elo (1214 vs 1200) instead of by index.
      Also record WHY it is derived rather than hardcoded: the population grows every loop round, and
      a fixed number would silently under-separate again the moment it does.
      Also record the interaction that must be read with it: D-R9 makes D-R11's problem WORSE, because
      more rounds give incumbents more matches and therefore more WINS, and wins is the primary sort
      key. `catch_up_matches` below is what makes raising the rounds safe. Measured: the same newcomer
      under the same rule lands rank 6 at 4 rounds entering round 3, rank 11 at 8 rounds entering
      round 6, and rank 16 entering round 7.

    Implement `catch_up_matches(match_counts)`:
      returns the LOW median of the supplied per-candidate match counts as an int — sort a cleaned
      list of non-negative ints and take `values[(len(values) - 1) // 2]`. Deliberately NOT
      `statistics.median`: that returns a float on an even-length list and this value indexes a
      schedule. Return 0 for an empty or wholly unusable input; never raise.
      Docstring must record that the median SEED (D-R11 as originally ruled) is INERT — the standing
      sorts by `(-wins, -elo, index)` with wins primary, so median-seed and flat-1200 produce
      byte-identical output — and that this is the SCHEDULE that replaces it, per operator decision
      D-W4-3. The ranking code is not modified at all; a newcomer simply plays the matches it missed.
      Quote the measured discrimination in the docstring: 99.8 / 29.5 / 1.8 with the schedule versus
      1.5 / 1.5 / 0.0 with the seed, and note the obvious repair (rank by raw win-RATE) over-corrects
      to 95.5 / 93.8 / 5.8, i.e. it stops discriminating. Cost: about 5 extra flash judgements.
      Note that this is also Co-Scientist's own approach — newer and top-ranking hypotheses are
      prioritised for participation in tournament matches.

    Create `tribunal/nestor_pulse_sdk/tests/test_workshop_loop.py` and cover every behaviour row
    above, plus a property sweep: `tournament_rounds` monotone non-decreasing over n in 0..200, and
    never above `_TOURNAMENT_ROUNDS_MAX`, and never above `n - 1` for n >= 2.

    Register the file in `tribunal/cloudbuild.test-engine.yaml`: add
    `nestor_pulse_sdk/tests/test_workshop_loop.py` to the `WANTED` list AND change `EXPECTED_FILES`
    from 35 to 36 IN THE SAME EDIT. The file carries an explicit caution you must obey: the whole
    block is ONE single-quoted `bash -c` string capped at 10,000 characters, and a single APOSTROPHE
    anywhere inside it closes the string early and kills the build with exit 127. Write no apostrophes.
    Do not add commentary inside the block; the YAML comments above `steps:` are where prose lives.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py nestor_pulse_sdk/tests/test_workshop_loop.py && grep -q "EXPECTED_FILES=36" cloudbuild.test-engine.yaml && grep -q "test_workshop_loop.py" cloudbuild.test-engine.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import workshop_loop"` succeeds with `nestor_pulse_sdk` NOT on `sys.path` — proving
      the module reaches only the standard library. Assert this as a PROPERTY (no `nestor_pulse_sdk`
      import, and the module lifts and runs), never as an exact allowlist of module names: an exact
      set over a file is what turned phase 15.5's merged tree red
    - all seven behaviour rows drive green on the bundled interpreter
    - the monotonicity sweep over n in 0..200 passes
    - `tournament_rounds` FAILS its 17 -> 6 assertion against a source-text mutant that restores a
      hardcoded 4; both columns recorded in the SUMMARY
    - `catch_up_matches` returns an `int`, asserted with `isinstance(..., int)` and
      `not isinstance(..., bool)`
    - the awk/sed-free check `grep -c "'" cloudbuild.test-engine.yaml` is UNCHANGED from the phase base
      — no apostrophe was added anywhere in that file
    - the WANTED list has exactly one more entry than at the phase base, and EXPECTED_FILES reads 36
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, comments included
  </acceptance_criteria>
  <done>The round count follows the field, the catch-up budget is an integer median, and the gate knows the new file exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: select_winners — the floor at the cut, the cross-cutting slots, and prefer-KEEP</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py, tribunal/nestor_pulse_sdk/tests/test_workshop_loop.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 846-868 — `winner_count`, whose docstring explains why it is NOT the D4 floor
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 2130-2250 — `enforce_scope_guard`, whose repair ladder PROMOTES below-the-cut candidates and therefore depends on losers staying available
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 2983-2990 — the current cut, `cut = winner_count(len(ranked)); top = ranked[:cut]`
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decision D-W4-5 in full, including the per-question-brackets comparison table and the anti-quota paragraph
  </read_first>
  <behavior>
    - 3 client questions over a ranked pool of 36 yields exactly 17 winners: 5 per client question
      plus 2 cross-cutting
    - every client question gets 5 winners even when its best candidate ranks 30th
    - when a client question has only 2 candidates in the pool at all, it gets 2 and the shortfall does
      not steal another question's floor
    - given a KEEP at rank 9 and a WEAK at rank 4 competing for the last slot of the same client
      question, the KEEP is chosen — this is the whole of prefer-KEEP
    - with `prefer_keep=False` the WEAK at rank 4 is chosen, so the rule is demonstrably doing the work
    - with an empty `client_questions` list the cut falls back to the supplied `default_cut` and no
      floor is applied
    - nothing is ever dropped: `winners + below_cut` is a permutation of the input, every time
    - the output is in ascending input rank order and is byte-identical across two identical calls
  </behavior>
  <action>
    Implement `select_winners(ranked, *, client_questions, default_cut, floor_per_question=None,
    cross_cutting_slots=None, prefer_keep=True) -> tuple[list, list]` in `workshop_loop.py`, returning
    `(winners, below_cut)`.

    Algorithm, in this order:
      1. `target = floor_per_question * len(labels) + cross_cutting_slots` when there are labels, else
         `default_cut`; bounded above by `len(ranked)`.
      2. For each label in CLIENT-QUESTION ORDER, reserve up to `floor_per_question` candidates that
         cover it (a candidate covers a label if the label is in its `parents`, falling back to
         `parent`), skipping anything already taken.
      3. Reserve up to `cross_cutting_slots` cross-cutting candidates — a candidate is cross-cutting
         when its `parents` span two or more client-question labels, or its `parent` is the
         `__discovery__` sentinel. Stamp the boolean onto the returned copy as `cross_cutting` so the
         exit check does not have to re-derive it.
      4. Fill any remaining slots up to `target` from what is left, by rank.
      5. Return the winners in ascending input rank order, and everything else as `below_cut` with its
         relative order preserved.

    prefer-KEEP is applied at EVERY one of steps 2, 3 and 4, and it is one rule: within the eligible
    pool for a slot, take the best-ranked candidate whose `critique` is KEEP if any KEEP is eligible,
    otherwise the best-ranked non-KEEP. Docstring must record why this is the single
    highest-leverage rule the measurement found: exit criterion 2 CHECKS for WEAK winners and nothing
    anywhere ever PREVENTED one from being selected — a smoke alarm with no fire door. Adding the
    preference took WEAK winners to 0 and made criterion 2 satisfiable BY CONSTRUCTION rather than by
    luck. And record its dependency: prefer-KEEP only works when there are spare KEEP candidates to
    prefer, which is a property of the SELECTION RATIO, not of the rule — at 6 generated per question
    against a 5-slot floor it is a 5-of-6 choice and the rule is inert.

    Two things the docstring must state plainly, because a future reader will get both wrong:
      - THIS FUNCTION NEVER BARS ANYTHING. Losing the tournament is not a defect, and
        `enforce_scope_guard`'s documented repair ladder PROMOTES a below-the-cut candidate when a
        client question has no winner. Bar losers and that repair path breaks. `below_cut` is returned
        for exactly that reason.
      - THE FLOOR OVERRIDES `_WINNERS_MIN` / `_WINNERS_MAX`. `winner_count` would cap the cut at 15
        and the validated configuration is 17. D-W4-5 is an operator decision and the floor wins;
        `default_cut` is consulted ONLY when there are no client questions. Say so, and pin it in a
        test, or a later reader restores the cap and silently deletes two research questions.
      - `discovery_bracket`'s anti-quota docstring argues against quotas by name and it DOES NOT
        GOVERN THIS FLOOR. Its argument is that a per-question DISCOVERY quota would force the engine
        to MANUFACTURE a discovery question. A mandate floor manufactures nothing — the per-question
        candidates already exist. A future reader will cite that docstring to block this floor; state
        here that it does not apply.

    Add the tests for every behaviour row, including the two-column prefer-KEEP demonstration and the
    permutation invariant driven over 200 pseudo-random-but-seeded pools.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py nestor_pulse_sdk/tests/test_workshop_loop.py</automated>
  </verify>
  <acceptance_criteria>
    - the 3-questions/36-candidates case returns exactly 17 winners with a per-label count of 5/5/5 and
      2 cross-cutting, driven on the bundled interpreter
    - the prefer-KEEP case is shown BOTH ways in one test: `prefer_keep=True` picks the rank-9 KEEP and
      `prefer_keep=False` picks the rank-4 WEAK
    - `set(w["index"] for w in winners) | set(w["index"] for w in below_cut)` equals the input index
      set over 200 seeded pools, with no duplicates
    - a mutant that clamps `target` to 15 FAILS the 17-winner test; both columns recorded
    - a mutant that removes the prefer-KEEP branch FAILS the prefer-KEEP test; both columns recorded
    - calling the function twice on the same input returns equal results (determinism), asserted
    - the function is pure: the caller's `ranked` list and its dicts are proven unmutated by a
      deep-copy comparison
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, comments included
  </acceptance_criteria>
  <done>Seventeen winners, five per client question, two cross-cutting, none WEAK while a KEEP was available, and every loser still promotable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: exit_verdict and round_metrics — the three criteria and the instrumentation</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py, tribunal/nestor_pulse_sdk/tests/test_workshop_loop.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 674-712 — Guard 1 (which SETS `resurrected`) and Guard 2 (which does NOT)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 233-271 — the degradation/note vocabulary style every sentence in this repo follows
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decisions D-W4-6 (including BOTH exemptions and the boxed off-by-one correction) and D-W4-7
    - .planning/ENGINE-REDESIGN-SPEC.md lines 777-843 — the exit criteria section, whose boxed warning carries the same corrected off-by-one
  </read_first>
  <behavior>
    - criterion 1 is COVERAGE: every client question has at least one winner whose `critique` is KEEP
    - criterion 2 is QUALITY: no winner is WEAK, subject to the two exemptions
    - Exemption A: a winner carrying `cross_cutting: True` never counts as WEAK for criterion 2
    - Exemption B: a winner carrying `resurrected: True` FAILS criterion 2 — it may still satisfy
      criterion 1, which is the whole point of the corrected off-by-one
    - criterion 3 is SATURATION: no winner in the current set carries `born_round == round_no`
    - the verdict exits only when all three hold — and a case where saturation passes while quality
      fails, and the reverse, are both represented in the tests, because the criteria were observed to
      gate each other in turn rather than one being permanently unreachable
    - at `round_no >= max_rounds` the verdict reports `cap_reached: True` and, if quality failed,
      carries a plain-words degradation sentence naming the count
    - `round_metrics` returns a plain, JSON-safe dict and enforces nothing
  </behavior>
  <action>
    Implement `exit_verdict(*, winners, client_questions, round_no, max_rounds=None) -> dict` in
    `workshop_loop.py`, returning a plain dict with at least `coverage_ok`, `quality_ok`,
    `saturation_ok`, `should_exit`, `cap_reached`, `weak_winners`, `resurrected_winners` and
    `degradation_reason` (empty string when there is none).

    Criterion numbering is load-bearing and has already been wrong once in three files at the same
    time. Write it in the docstring as: 1 = COVERAGE, 2 = QUALITY, 3 = SATURATION. Record the
    correction of 2026-07-31 explicitly — until then `15.7-OPEN-ITEMS.md`, spec § 5's boxed warning
    and § 8's Wave 4 row all said criterion 1, and that inverted the rule's own purpose, because
    excluding a resurrected candidate from COVERAGE would break the exact guarantee resurrection
    exists to provide.

    EXEMPTION A MUST BE STRUCTURAL, NOT TEXTUAL, and the docstring must say why. A cross-cutting
    question is compound BY CONSTRUCTION — it joins two topics on purpose — so the flaw clause "two
    questions in one" must not count against it. The obvious implementation is to match that phrase in
    the flaw text. Do NOT do that: the critique prompt is English but a Dutch or French run's flaw
    clause is model prose in the run's own language, so a text matcher would silently NEVER FIRE and
    criterion 2 would be built to reject exactly the highest-value questions the loop exists to
    produce. Key off the `cross_cutting` boolean `select_winners` stamps in Task 2. Record that
    `exp9` marked both its best questions WEAK for precisely this reason.

    EXEMPTION B: a winner carrying `resurrected: True` fails criterion 2. Record why the direction is
    "fails" and not "is ignored": `_reason_critique_resurrected` and `_reason_critique_population` are
    COVERAGE FALLBACKS, not quality passes, and Guard 2 rewrites EVERY candidate to KEEP when critique
    kills everything — so the one case where quality most needs to read as FAILED would otherwise read
    as a perfect pass. Note that `workshop_rank.py`'s Guard 2 does not set the flag today and that
    plan 15.7-08 makes it do so; this function must be correct whether or not that has landed, i.e. it
    reads the flag and does not infer it.

    Saturation: a winner whose `born_round` equals `round_no` is a new entrant produced by the last
    evolve pass. Record the measured failure mode this must survive: in the harness, COVERAGE FAILED
    in rounds 4 and 5 before recovering, because barring WEAK-after-two-passes stripped every KEEP
    candidate from one client question. The exit AND correctly refused to exit — so a `False` here is
    a normal, expected reading and must not be treated as a bug.

    Cap behaviour: on hitting the cap with WEAK winners still present, the loop SHIPS and records a
    degradation reason. That matches D-12 — degraded means honest, not broken. Compose the sentence in
    the module's own vocabulary style, over 40 characters, naming its count as a literal digit and
    stating the CONSEQUENCE: V-01 would have carried "3 of 10 winners could not be sharpened past
    WEAK".

    Implement `round_metrics(*, round_no, candidates_in, new_candidates, winners, weak_winners,
    barred, dropped_as_reproposal, lookups, calls, cost_usd) -> dict` returning a plain JSON-safe dict
    of ints and strings. It ENFORCES NOTHING — no ceiling, no truncation, no exception. Docstring must
    record D-W4-7: neither the spend ceiling nor a population cap nor a per-round grounded-lookup cap
    is binding at the measured scale (population 23-41 across all three global configurations, largest
    prompt ~9k chars, total cost $0.24 against the spec's original ~$3.00 estimate for 10 rounds), so
    none is enforced. An enforced ceiling nobody has measured a need for is a knob that will one day
    truncate a run for no reason; a logged number is what tells you whether a ceiling is ever
    warranted. And: if runs routinely hit 10 rounds, that is evidence the cap should go HIGHER.

    Add tests for every behaviour row, including the two gate-each-other cases and the cap-with-WEAK
    degradation sentence.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py nestor_pulse_sdk/tests/test_workshop_loop.py</automated>
  </verify>
  <acceptance_criteria>
    - a winner set in which the ONLY non-KEEP is a `cross_cutting: True` winner passes criterion 2
    - a winner set in which one winner carries `resurrected: True` FAILS criterion 2 and PASSES
      criterion 1 — this single test is the corrected off-by-one and must be named so a reader finds it
    - a mutant that swaps the resurrected exclusion onto criterion 1 fails that test; both columns
      recorded in the SUMMARY
    - a mutant that implements Exemption A by matching the string "two questions in one" is shown to
      pass an English fixture and FAIL a Dutch one, while the structural implementation passes both —
      record this, it is the reason the structural form was chosen
    - `should_exit` is False in a round where saturation passes but quality fails, and False in the
      reverse case, and True only when all three hold
    - `round_metrics` output survives `json.dumps` and contains no float
    - `exit_verdict` and `round_metrics` never raise on a hostile input battery of at least 5 shapes
      (None, a string, a list of non-dicts, a dict with unhashable values, an empty everything)
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, comments included
  </acceptance_criteria>
  <done>Coverage is 1, quality is 2, saturation is 3; a resurrected candidate cannot buy a clean exit; and the ceilings are numbers in a log, not gates in the path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-authored candidate dicts -> this module | untrusted `text`, `flaw`, `parent` values arrive here as data |
| env vars -> loop bounds | operator-tunable values governing round count and slot count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.7-03-01 | Denial of service | a hostile `text` or `flaw` value reaching a pure function | mitigate | every function in this module is total: it coerces defensively, never subscripts, and never raises. Asserted by the 5-shape hostile battery in Task 3 |
| T-15.7-03-02 | Denial of wallet | `tournament_rounds` growing without bound as the population grows | mitigate | bounded above by `_TOURNAMENT_ROUNDS_MAX` (10) AND by `n - 1`; monotonicity and the ceiling are both asserted over n in 0..200 |
| T-15.7-03-03 | Tampering | the gate config being edited by two plans at once | mitigate | this plan is the SOLE editor of `cloudbuild.test-engine.yaml` in phase 15.7, and the apostrophe count is asserted unchanged |
| T-15.7-03-04 | Repudiation | a silent quality pass on a resurrected candidate | mitigate | Exemption B makes a resurrected winner FAIL criterion 2, pinned by a named test and by a mutant that swaps it onto criterion 1 |
| T-15.7-03-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NOTHING and this module imports nothing outside the standard library |
</threat_model>

<verification>
- `workshop_loop.py` imports and runs on the bundled interpreter with `nestor_pulse_sdk` absent.
- All five functions are pure, total and deterministic — each proven, not asserted in prose.
- Every named mutant is recorded with BOTH columns. A test passing in both columns is named as a
  behaviour-preservation guard or it is vacuous.
- `EXPECTED_FILES` reads 36, the WANTED list gained exactly one path, and the apostrophe count in
  `cloudbuild.test-engine.yaml` is unchanged.
</verification>

<success_criteria>
- One new stdlib-only module with five pure functions and docstrings that carry the measured reasons,
  not just the rules.
- One new test file, registered in the gate in the same edit as the count.
- The REQUIRED strong-late-newcomer property is set up here (the catch-up budget) and asserted
  end-to-end in plan 15.7-08 — this plan must not claim it alone.
- No deploy, no migration, no live run.
</success_criteria>

<output>
Create `.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-03-SUMMARY.md`
when done, listing every exported signature verbatim — plans 15.7-08 and 15.7-09 code against them.
`.planning/` is gitignored: `git add -f`.
</output>
