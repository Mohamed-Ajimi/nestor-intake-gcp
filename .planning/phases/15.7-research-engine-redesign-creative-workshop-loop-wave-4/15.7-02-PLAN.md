---
phase: 15.7-research-engine-redesign-creative-workshop-loop-wave-4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py
  - tribunal/nestor_pulse_sdk/tests/test_workshop_tournament.py
  - tribunal/nestor_pulse_sdk/tests/test_workshop_critique.py
  - tribunal/nestor_pulse_sdk/tests/test_workshop_languages.py
autonomous: true
requirements: [D-W4-8, D-W4-5]

must_haves:
  truths:
    - "D-W4-8: the critic is shown WHOLE questions — a 373-character candidate reaches the critique prompt uncut"
    - "D-W4-8: the tournament judge is shown whole questions on BOTH sides of a match"
    - "D-W4-8: raising the generation count to 12 actually yields 12 — the parse-side bound no longer clips it to 10"
    - "D-W4-8: an evolved winner is not clipped below what dispatch will carry"
    - "The truncation is still A BOUND — it is raised, never deleted, and a hostile candidate still cannot forge another candidate's output line"
    - "All the length and count constants are asserted TOGETHER in one test, so a future edit cannot move one and leave the others"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py"
      provides: "raised generation count, raised parse-side bound, raised stored-candidate width"
      contains: "_CANDIDATES_PER_QUESTION"
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py"
      provides: "raised prompt-truncation width and raised evolved-winner width"
      contains: "_CANDIDATE_PROMPT_CHARS"
  key_links:
    - from: "workshop._CANDIDATE_MAX_CHARS"
      to: "workshop_rank._CANDIDATE_PROMPT_CHARS"
      via: "the stored candidate must not be wider than what the prompt will show"
      pattern: "_CANDIDATE_PROMPT_CHARS"
    - from: "workshop._CANDIDATES_PER_QUESTION"
      to: "workshop._CANDIDATES_PER_QUESTION_MAX"
      via: "the parse-side hard bound applied as out[:cap] at workshop.py:1139"
      pattern: "_CANDIDATES_PER_QUESTION_MAX"
---

<objective>
Raise the five constants that between them produced the entire `WEAK` epidemic and, if left alone,
would silently make the validated configuration unreachable (D-W4-8, D-W4-5's selection ratio).

Measured, on the real V-01 candidates through the real critique prompt: at
`_CANDIDATE_PROMPT_CHARS = 240`, 17 of 18 candidates reached the critic **cut off mid-word**, and the
result was `KEEP=1 / WEAK=17` with just **two distinct flaw clauses** — sixteen of them the identical
"two questions in one". Raised, the same prompt on the same candidates gives `KEEP=9 / WEAK=9` with
many specific flaws, and the ranking reshuffles hard (#7 rank 1 -> 15; #15 rank 13 -> 1). The critic
was never shown the questions. Neither was the judge: `_match_block` truncates BOTH sides to the same
width and hands the judge the truncated critique flaw with "a flaw counts AGAINST that side" — so when
both sides get the identical truncation-induced flaw, that signal cancels to zero.

Purpose: this is one plan of constants that unlocks every other plan in the phase. Get it wrong in
either direction and the rest of Wave 4 is measuring the cap.
Output: five constants raised together, their comments re-derived, and ONE test that asserts the whole
ladder at once.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_preflight gate="blocking">
Executor worktrees here are cut from a STALE ref — 7 of 7 in phase 15.6, at 486-524 commits behind,
and the distances GROW. `git rev-list --count BASE..HEAD == 0` reads GREEN while stale because the
stale ref is usually the merge-base. Assert the MERGE-BASE:

    git merge-base HEAD <PHASE_BASE_SHA>

Anything other than `<PHASE_BASE_SHA>` means stale. `git reset --hard <PHASE_BASE_SHA>` first.
Positive sentinels for this base: `_text_key` in `research_division.py`, `_norm_url` in
`discovery_bracket.py`. If either is missing, STOP and report.
</worktree_preflight>

<proving_things_on_this_machine>
No pytest, no Docker, no `python3`. A stdlib-only Python 3.14 ships with gcloud at
`C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`. Use it to `py_compile`, to
`ast`-lift real functions and `exec` them with stub globals, and to prove NON-VACUITY by mutating the
real source text and confirming the check bites. `PYTHONIOENCODING=utf-8` or accented text crashes the
console. Never `importlib.reload` a lifted module — it silently un-mutates your mutant.
</proving_things_on_this_machine>

<context>
@.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-CONTEXT.md
@tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py
@tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py

<interfaces>
The five constants, verbatim as they stand, with the sites that consume them:

workshop.py
  `_CANDIDATES_PER_QUESTION      = int(env "NESTOR_TRIBUNAL_WORKSHOP_CANDIDATES_PER_Q", "6")`   (:190)
       -> interpolated as `{n}` at BOTH `:1070` ("Output EXACTLY {n} lines") and `:1075`
          ("<your {n} lines go here>"). BOTH read the SAME format variable from ONE source, so the
          "states the count twice" defect the spec warns about is NOT present in this repo — it was an
          artefact of the measurement harness's own patch. Verify that before changing anything.
  `_CANDIDATES_PER_QUESTION_MAX  = int(env "..._CANDIDATES_PER_Q_MAX", "10")`                   (:193)
       -> the PARSE-SIDE HARD BOUND, applied as `out = out[:cap]` at `:1139` inside
          `_candidates_from_lines`. This is the real second authority: raising generation to 12
          without raising this yields TEN, not twelve.
  `_MAX_CANDIDATES               = int(env "..._MAX_CANDIDATES", "60")`                          (:196)
       -> the global bound, trimmed round-robin at `:1437`.
  `_CANDIDATE_MAX_CHARS          = int(env "..._CANDIDATE_CHARS", "300")`                        (:197)
       -> applied at `:1132` `text = text[:_CANDIDATE_MAX_CHARS]`, i.e. BEFORE the candidate is ever
          stored. Raising the prompt cap while leaving this at 300 still hands the critic a clipped
          question.
  `_CANDIDATE_MIN_CHARS = 12` — a garble filter, NOT a knob. Leave it.

workshop_rank.py
  `_CANDIDATE_PROMPT_CHARS = int(env "NESTOR_TRIBUNAL_WORKSHOP_PROMPT_CANDIDATE_CHARS", "240")`  (:168)
       -> `_candidate_block` (:443, the critique prompt), `_match_block` (:1022, BOTH sides of every
          tournament match), `_winners_block` (:1875, the evolve prompt). All three go through
          `_flatten(text, cap)` at :300, which is documented as a SECURITY CONTROL.
  `_FLAW_MAX_CHARS = int(env "..._FLAW_CHARS", "160")`                                           (:171)
       -> leave at 160. D-R6 fixes the blind judge by giving it the parent question and the
          orientation findings (plan 15.7-08), not by widening the flaw clause.
  `_WINNER_MAX_CHARS = int(env "..._WINNER_CHARS", "400")`                                       (:1579)
       -> applied at `:1855` to the EVOLVED text. Real candidates already run to 373 chars before
          evolve adds specificity, so 400 clips the sharpened question. Same defect class, one level
          down.

The downstream width this ladder must agree with:
  `research_division._SUBQ_CHARS = 600` — the bound already applied to the same text on its way to
  three paid third-party providers. DO NOT import it across the seam (that is the exact-set trap that
  turned phase 15.5's merged tree red). Write 600 as a literal here and state the agreement in a
  comment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raise the four generation-side constants in workshop.py</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py, tribunal/nestor_pulse_sdk/tests/test_workshop_critique.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 160-216 — the constants block and its comments
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 1041-1080 — `_CANDIDATE_PROMPT_TEMPLATE` and both `{n}` interpolations
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 1104-1150 — `_candidates_from_lines` and the `out[:cap]` bound
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 1420-1450 — the `_MAX_CANDIDATES` round-robin trim
    - tribunal/nestor_pulse_sdk/tests/test_workshop_critique.py lines 590-670 and 1260-1300 — the tests reading these constants and the two sites hardcoding 240
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decisions D-W4-8 and D-W4-5
  </read_first>
  <behavior>
    - a candidate line of 500 characters survives parsing at its full length (today it is clipped to 300)
    - 12 parsed candidate lines for one client question all survive (today the 13th onward is dropped
      at 10, and asking for 12 yields 10)
    - the generation prompt asks for 12 in BOTH places it states the count, and both places still read
      the same single source
    - a candidate line of 12 characters or fewer is still ignored as garble
    - 8 client questions at 12 each no longer hit the global cap
  </behavior>
  <action>
    Set the defaults: `_CANDIDATES_PER_QUESTION` 6 -> 12; `_CANDIDATES_PER_QUESTION_MAX` 10 -> 24;
    `_CANDIDATE_MAX_CHARS` 300 -> 600; `_MAX_CANDIDATES` 60 -> 120. Every env var name stays exactly
    as it is — this plan changes defaults, never knob names.

    Rewrite the comment block at lines 176-202 so each number carries its NEW reason:
      - 12 is the SELECTION RATIO and it is the measured lever, not a cosmetic bump. Record the
        exp10-vs-exp11 before/after: identical architecture, identical 17 slots, six generated versus
        twelve; at six a five-slot floor is a 5-of-6 choice, i.e. no selection at all, prefer-KEEP is
        inert, and the loop needed 9 rounds and $0.48; at twelve the winner set is clean from round 1,
        the loop exits in round 4 and costs $0.24. Read as a range the two runs say nothing; read as
        before/after they identify the lever.
      - 24 is the parse-side hard bound and it MUST stay above the generation count. State the trap by
        name: raising generation to 12 and leaving this at 10 silently yields ten, and the selection
        ratio is the whole lever of the validated configuration. Same defect class as CR-01 in Wave 3.
      - 600 matches the bound `research_division._SUBQ_CHARS` already applies to the same text on its
        way to three paid providers, so nothing here widens the attacker surface beyond what already
        reaches a provider. Say that the value is duplicated as a literal ON PURPOSE and not imported,
        because a constant imported across a seam two plans both touch is the trap that turned phase
        15.5's merged tree red.
      - 120 is `_CANDIDATES_PER_QUESTION` x 10 client questions, because the old 60 is exactly
        `12 x 5` and a brief with more than five client questions would have had the round-robin trim
        silently eat the selection ratio it just paid to generate.

    Verify — do not assume — that `:1070` and `:1075` both interpolate the SAME `{n}`. If they do,
    record in the comment that the spec's "the prompt states the count twice" warning does NOT
    describe this repository and name what it did describe (the harness's own patch). If they do NOT,
    that is a code-contradicts-decision finding: STOP and report it rather than silently choosing.

    Update `test_workshop_critique.py`: the assertions at :606 and :608 already read the constants
    symbolically and survive. The two sites hardcoding 240 at :656 and :1276-1283 do NOT — they build
    an expected prompt line with `c['text'][:240]` and assert `"A" * 240 in prompt`. Rewrite both to
    read `workshop_rank._CANDIDATE_PROMPT_CHARS` rather than a literal, so they cannot rot again the
    next time a width moves.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop.py nestor_pulse_sdk/tests/test_workshop_critique.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *#' workshop.py | grep -c '"300"'` and the same for `"6"` on the
      `_CANDIDATES_PER_QUESTION` line both return 0 — comments excluded so historical prose cannot
      self-invalidate the gate
    - driving the real `_parse_candidate_lines` on the bundled interpreter: 12 CANDIDATE lines yield
      12; a 500-character line comes back at 500; an 11-character line is still ignored
    - the same three cases run against a source-text mutant restoring `"10"` and `"300"` FAIL — the
      two-column result is recorded in the SUMMARY
    - the rendered generation prompt contains the number 12 in both the "Output EXACTLY" sentence and
      the placeholder line, and a grep proves both come from one `{n}`
    - no literal `240` survives in `test_workshop_critique.py` outside a comment
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, including comments
  </acceptance_criteria>
  <done>Twelve candidates are generated, twelve are parsed, and each may be 600 characters wide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raise the two prompt-side widths in workshop_rank.py and pin the whole ladder in one test</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py, tribunal/nestor_pulse_sdk/tests/test_workshop_tournament.py, tribunal/nestor_pulse_sdk/tests/test_workshop_languages.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 140-216 — the constants block, including the comment calling `_CANDIDATE_PROMPT_CHARS` a SECURITY CONTROL
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 300-320 — `_flatten`, the one place the truncation is applied
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 443-458 — `_candidate_block`
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 1022-1049 — `_match_block`, which truncates BOTH sides and renders FLAW_A / FLAW_B
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 1573-1590 and 1844-1872 — `_WINNER_MAX_CHARS` and where it clips the evolved text
    - tribunal/nestor_pulse_sdk/tests/test_workshop_tournament.py lines 350-380 — the hostile-injection test using "X" * 240
    - tribunal/nestor_pulse_sdk/tests/test_workshop_languages.py lines 285-310 — the site hardcoding 240
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decision D-W4-8, including the boxed security warning
  </read_first>
  <behavior>
    - a 373-character candidate reaches the critique prompt whole, with its question mark intact
    - both sides of a tournament match render whole
    - a 550-character evolved winner is stored at 550, not clipped to 400
    - the truncation still BITES: a 5,000-character candidate is still cut, and a candidate carrying a
      newline plus a forged `99 | A: ...` line still cannot address another match's slot
    - one test asserts all five constants and their ordering relations together
  </behavior>
  <action>
    Set `_CANDIDATE_PROMPT_CHARS` 240 -> 600 and `_WINNER_MAX_CHARS` 400 -> 600. Leave
    `_FLAW_MAX_CHARS` at 160 and say in its comment why: D-R6 cures the blind judge by passing it the
    parent client question in full plus the orientation findings (plan 15.7-08), not by widening a
    flaw clause.

    Rewrite the comment on `_CANDIDATE_PROMPT_CHARS` at lines 145-148. Keep the sentence that it is a
    SECURITY CONTROL and not formatting — that is the boxed warning D-W4-8 is emphatic about: this
    truncation bounds attacker-influenced text so an injected candidate cannot forge another
    candidate's output line, the same channel Wave 3's CR-02 closed on `source_url`. It needs A bound;
    it did not need 240. Record what 240 cost, with the measured numbers: real V-01 candidates are
    245-373 characters, 17 of 18 were cut mid-word, 920 characters were discarded with no ellipsis and
    no surviving question mark, and the critic was being asked whether each question is sharp and
    answerable AS IT STANDS. Two distinct flaw clauses across seventeen rejections is itself the tell.
    Record the second-order effect too: `_match_block` truncates both sides to this same width and
    hands the judge the truncated flaw with "a flaw counts AGAINST that side", so an identical
    truncation-induced flaw on both sides cancels to zero.

    Also record the correction so nobody re-inherits the wrong diagnosis: thinking-disabled is NOT the
    cause. `gates._make_config()` gives both the critique (:490) and the judge (:1115)
    `temperature=0.0` and `thinking_budget=0`, and enabling thinking swings the critic to 17-18 KEEP —
    a critic that rejects nothing, which breaks the KILL path and the whole rejected register. Full
    text with reasoning still off is what works. Do NOT enable thinking.

    Add ONE test to `test_workshop_tournament.py` that asserts the whole ladder together, because
    D-W4-8's failure mode is one value moving while the others stay: `workshop._CANDIDATE_MAX_CHARS`,
    `workshop._CANDIDATES_PER_QUESTION`, `workshop._CANDIDATES_PER_QUESTION_MAX`,
    `workshop_rank._CANDIDATE_PROMPT_CHARS` and `workshop_rank._WINNER_MAX_CHARS`. Assert the VALUES
    and the RELATIONS that must hold: the per-question generation count is strictly below the
    parse-side bound; the stored candidate width is not greater than the prompt width; the evolved
    winner width is not greater than the prompt width; and every one of the widths is a real bound
    (finite, positive, and below 5,000). Name in the docstring that this test exists because the
    same-value-two-authorities defect is CR-01's class and § 8's Wave 4 row asserts it.

    Add the negative arm in the same file: a candidate 5,000 characters long is still truncated by
    `_flatten`, and the existing hostile-injection test at :363 still cannot forge a match line — check
    whether its `"X" * 240` payload is asserting a truncation width or merely padding, and only change
    it if it is asserting a width.

    In `test_workshop_languages.py`, the site at :293-304 hardcodes 240 in the evolve prompt
    assertion. Rewrite it to read `workshop_rank._CANDIDATE_PROMPT_CHARS`.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py nestor_pulse_sdk/tests/test_workshop_tournament.py nestor_pulse_sdk/tests/test_workshop_languages.py</automated>
  </verify>
  <acceptance_criteria>
    - the ladder test exists in `test_workshop_tournament.py`, names all five constants, and FAILS
      against a source-text mutant that raises `_CANDIDATE_PROMPT_CHARS` while leaving
      `_CANDIDATE_MAX_CHARS` at 300 — this is the exact defect the test is for, and it must be shown
      to bite
    - driving the real `_candidate_block` and `_match_block`: a 373-character candidate renders whole
      on both sides; a 5,000-character candidate is still cut
    - `grep -v '^ *#' workshop_rank.py | grep -c '"240"'` returns 0
    - the phrase naming this truncation a security control is still present in the source
    - no `thinking` or `thinking_budget` setting is changed anywhere by this plan
    - `test_workshop_languages.py` contains no literal 240 outside a comment
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, including comments
  </acceptance_criteria>
  <done>The critic and the judge see whole questions, the bound still bounds, and one test would catch the next person who moves only one of the five.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generation LLM -> candidate text | model output stored, then rendered into three further prompts |
| candidate text -> critique / tournament / evolve prompts | untrusted text that could forge another candidate's output line |
| candidate text -> three paid third-party providers | via `research_division`, bounded there at `_SUBQ_CHARS` = 600 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.7-02-01 | Tampering | `_CANDIDATE_PROMPT_CHARS` 240 -> 600 | mitigate | the bound is RAISED, never removed. `_flatten` still collapses `\|`, `\r` and `\n` to spaces before truncating, so a forged extra record is still impossible, and every answer is still addressed by INDEX. 600 equals the width the same text already reaches a paid provider at, so the attacker surface is not widened beyond an existing boundary. Asserted by the negative arm in Task 2 |
| T-15.7-02-02 | Tampering | `_CANDIDATE_MAX_CHARS` 300 -> 600 | mitigate | same argument, applied at parse time. `_CANDIDATE_MIN_CHARS` garble filter unchanged |
| T-15.7-02-03 | Denial of wallet | 6 -> 12 candidates per question | accept | measured: twelve HALVED the cost ($0.48 -> $0.24) and more than halved the rounds (9 -> 4), because selection converges faster than grinding does. `_MAX_CANDIDATES` (120) remains the global bound and the round-robin trim still protects every parent |
| T-15.7-02-04 | Information disclosure | wider candidate text in an audited prompt | accept | the audit bucket already stores these prompts; the candidates are derived from client-validated questions and are already stored at 600 downstream |
| T-15.7-02-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NOTHING and adds no import to any file it touches |
</threat_model>

<verification>
- Both source files byte-compile on the bundled interpreter.
- The ladder test fails on the one-constant-moved mutant and passes on the edited source.
- The truncation still cuts a 5,000-character candidate and still prevents a forged match line.
- No hardcoded 240 survives in any of the three test files outside a comment.
- No `thinking` setting is touched.
</verification>

<success_criteria>
- Five constants raised, each comment re-derived with its measured reason rather than its number swapped.
- One test asserts all five together, and is proven non-vacuous by a mutant.
- The security bound survives as a bound.
- No deploy, no migration, no live run, no `gcloud builds submit`.
- `cloudbuild.test-engine.yaml` NOT touched — plan 15.7-03 is its sole editor this phase.
</success_criteria>

<output>
Create `.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-02-SUMMARY.md`
when done, and record the `:1070` / `:1075` single-`{n}` verification result explicitly — a later plan
depends on that having been checked rather than assumed. `.planning/` is gitignored: `git add -f`.
</output>
