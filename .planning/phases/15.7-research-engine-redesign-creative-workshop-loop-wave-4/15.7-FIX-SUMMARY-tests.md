# 15.7 — stale-test repair (TEST FILES ONLY)

Base: `5615379` (`Merge branch 'worktree-agent-ae10bb5351deb0940'`).
Scope: repair tests left stale by the nine Critical fixes. **No file under
`pipeline/` was modified.** Written incrementally.

---

## 0. Preflight

**The stale-base trap fired again — 18 of 18.**

| check | value |
|---|---|
| worktree HEAD on arrival | `a3a0c96` "docs(state): record phase 15 context session" |
| `git rev-list --count 5615379..HEAD` | **`0` — GREEN, and wrong** |
| `git merge-base HEAD 5615379` | `a3a0c96` ≠ `5615379` → **STALE** |
| distance | **641 commits behind** |
| action | `git reset --hard 5615379`, re-verified merge-base == BASE |

`rev-list --count` read green because the stale ref is an *ancestor*. Only the
merge-base check caught it. This is now 18/18 in this project.

**Path correction:** the brief's `backend/nestor_pulse_sdk/pipeline/...` does not
exist; the tree is `tribunal/nestor_pulse_sdk/pipeline/tribunal/...`. Located via
`git ls-files`, no fallback.

Sentinels (all present, confirming the 15.7 fixes are in this tree):

- `workshop_admission.py` imports `DISCOVERY_PARENT` — line 92
- `question_grouping.py` defines `_D6_MAX_RIDERS_PER_GROUP` — line 213
- `research_division.py` composes from `parent_prompts` — line 759, 806

---

## Verification harness — REAL IMPORTS, not an `ast` lift

Every claim in this document rests on **real imports of the real source**. No
`ast` lift is used anywhere. Stubbed modules are non-stdlib leaves only
(`sqlalchemy*`, `fastapi*`, `httpx`, `google*`, …) — none is called on any tested
path; they exist purely so import chains resolve.

**The harness had to be proved faithful before it could prove anything.** A first
cut produced a degraded 3-winner run: the `sqlalchemy` stub was an *instance*, so
`class Base(DeclarativeBase)` raised `__mro_entries__ must return a tuple`,
`workshop_admission` failed to import, and `workshop_rank` swallowed it in a
broad `except` and returned a fallback. Every number measured in that state would
have been a lie. Fixed by making the stub subclassable.

Fidelity check against Cloud Build `4de8e770` ground truth:

| quantity | gate | harness |
|---|---|---|
| winners | 17 | **17** |
| `counts["rounds"]` | 3–5 | **4** |
| `loop_born_winners` | 8 | **8** |
| `len(cross)` at seed 0 | **8** (the failure) | **8** |

Results below are **replay predictions**. The Cloud Build gate is the authority.

---

## A. `test_workshop_loop.py` — the seam test — FIXED

`test_the_whole_phase_shape_three_questions_seventeen_winners_reach_a_provider`

`assert len(cross) == 2` → got 8. Applied the specified replacement verbatim.
`workshop_loop` was **already imported** (line 28); no import added.

**I independently reproduced the investigator's evidence rather than taking it on
trust.** Cross-cutting count over `_script(seed=0..11)`:

```
[8, 6, 3, 8, 8, 8, 8, 0, 7, 3, 2, 4]
```

Exact match to the reported sequence, **including seed 7 = 0** — which is what
makes `>= _CROSS_CUTTING_SLOTS` invalid too, not just `== 2`. Also confirmed
`merged_from` is empty on **every winner at every one of the 12 seeds**
(clustering is inert — `_script` has no `"cluster"` branch), so the surviving
two-label spans are genuine `COMBINE` children carrying `source_indices`.

**Replay prediction: green at all 12 seeds** (full test body incl. the
`research_division.divide` hop, 0 of 17 winners missing from a provider query).

**What the rewrite catches** — three mutants, all caught:

| mutant | result |
|---|---|
| over-merge shape: a 2nd client label with no `merged_from`/`source_indices` | **CAUGHT** |
| every winner made cross-cutting (kills the non-vacuity guard) | **CAUGHT** |
| a winner with empty `parents` | **CAUGHT** |

The old `== 2` caught none of these — it was a hard number that read 2 only
because CR-02 was deleting five of every six loop-born candidates.
