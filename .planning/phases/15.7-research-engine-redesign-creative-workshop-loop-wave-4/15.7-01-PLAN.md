---
phase: 15.7-research-engine-redesign-creative-workshop-loop-wave-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py
  - tribunal/nestor_pulse_sdk/tests/test_question_grouping.py
  - tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py
  - tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py
autonomous: true
requirements: [D-W4-4a, D-W4-5]

must_haves:
  truths:
    - "D-W4-4a: setting NESTOR_TRIBUNAL_D6_MAX_GROUPS above 5 raises the ceiling instead of silently staying at 5"
    - "D-W4-4a: one group per client question is the PRIMARY grouping path — no LLM call, no degradation reason"
    - "D-W4-4a: topic grouping survives as an explicitly selected option, not as dead code"
    - "D-W4-5: a winner set of 5-per-client-question plus 2 cross-cutting reaches dispatch without being silently truncated by research_division._D6_MAX_WINNERS"
    - "A mandate group holding 5 winners still accepts a discovery rider instead of shedding it"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py"
      provides: "declamped _D6_MAX_GROUPS, raised _D6_MAX_GROUP_SIZE, per-question primary grouping path"
      contains: "_GROUPING_MODE"
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py"
      provides: "a winner bound that cannot clip the D-W4-5 floor"
      contains: "_D6_MAX_WINNERS"
  key_links:
    - from: "question_grouping.group_winners"
      to: "question_grouping.fallback_groups"
      via: "the per-question PRIMARY path, taken before any prompt is built"
      pattern: "_GROUPING_MODE"
    - from: "research_division._normalise_winners"
      to: "_D6_MAX_WINNERS"
      via: "the winners truncation at research_division.py:1199"
      pattern: "_D6_MAX_WINNERS"
---

<objective>
Close the three numeric bounds that stand between Wave 4's design and the code, and make
one-group-per-client-question the primary grouping path (D-W4-4a).

Two of the three are SILENT CLIPS of the exact shape decision D-W4-5 measures its whole result by —
17 winners, a floor of 5 per client question plus 2 cross-cutting. `research_division._D6_MAX_WINNERS`
is 15, so 2 of those 17 never reach a provider. `question_grouping._D6_MAX_GROUP_SIZE` is 4, so a
mandate group already holding 5 winners sheds every discovery rider Wave 3 built. The third is the
`min(5, ...)` clamp D-W4-4a names by line number.

Purpose: without this plan, every later plan in this phase delivers a winner set that is truncated on
its way to dispatch. All three failures are silent, all three are one constant, and all three live in
the SEAM between the workshop and the dispatcher — the class of defect Wave 3 shipped twice behind
42/42 verification and 1283 green tests.
Output: `question_grouping.py` and `research_division.py` with declamped / re-sized bounds and a
primary per-question grouping path; three test files updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_preflight gate="blocking">
Executor worktrees in this project are cut from a STALE ref. It fired on 7 of 7 executors in phase
15.6 at 486-524 commits behind, and the distances GROW as master advances.

`git rev-list --count BASE..HEAD` returning 0 reads GREEN while stale, because the stale ref is
usually the MERGE-BASE. Assert the MERGE-BASE, not the count:

    git merge-base HEAD <PHASE_BASE_SHA>

If that prints anything other than `<PHASE_BASE_SHA>`, you are on a stale tree. Run
`git reset --hard <PHASE_BASE_SHA>` before reading a single source file.

Then assert two positive sentinels for this phase base, because "not ahead" is not "in sync":
  - `_text_key` is present in `research_division.py` (Wave 3's CR-01 fix, commit 18065fa)
  - `_norm_url` is present in `discovery_bracket.py` (Wave 3's CR-02 fix, commit efc01a4)
If either is missing your tree predates the Wave 3 critical fixes. STOP and report.
</worktree_preflight>

<proving_things_on_this_machine>
There is no pytest, no Docker and no `python3` on this box. A stdlib-only Python 3.14 ships with
gcloud at `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`. Waves 1-3 used it to:
  - `py_compile` every edited file;
  - `ast`-lift the real functions out of a module and `exec` them with stub globals, so nothing is
    retyped and the result is about OUR engine (refuse to run if a lift target is missing);
  - execute committed test bodies with a minimal `pytest` / `monkeypatch` / `caplog` shim;
  - prove NON-VACUITY by mutating the real source text and confirming the check bites.
Set `PYTHONIOENCODING=utf-8` or Dutch/accented corpus text crashes the console.
NEVER use `importlib.reload` on a lifted module: it re-finds the spec through the parent package
`__path__` and silently un-mutates your mutant.
Match that bar. A claim you did not drive is not a result.
</proving_things_on_this_machine>

<context>
@.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-CONTEXT.md
@.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-CONTEXT.md
@tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py
@tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py

<interfaces>
Read these directly — do not go exploring for them.

question_grouping.py, as it stands:
  `_D6_MAX_GROUPS = min(5, max(1, int(os.environ.get("NESTOR_TRIBUNAL_D6_MAX_GROUPS", "5"))))`  (:97)
  `_D6_MAX_GROUP_SIZE = max(3, int(os.environ.get("NESTOR_TRIBUNAL_D6_MAX_GROUP_SIZE", "4")))`  (:111)
  `GROUP_BRACKET_MANDATE = "mandate"` / `GROUP_BRACKET_DISCOVERY = "discovery"`  (:115-116)
  `fallback_groups(winners, client_questions) -> tuple[list[list[int]], str]`  (:806) — PURE, already
      implements one-group-per-client-question in client order; orphans join the FIRST group; empty
      groups are dropped. Its returned `reason` is a D-12 DEGRADATION sentence.
  `build_groups(assignment, pool, bracket=...) -> list[dict]`
  `attach_discovery_riders(groups, riders, *, max_size)`  (:884) — the ONLY consumer of
      `_D6_MAX_GROUP_SIZE`. It sheds RIDERS when a group is at the cap, never a winner.
  `warn_if_over_ceiling(n_groups, n_streams) -> Optional[str]`  (:1022) — reads `_D6_MAX_GROUPS`.
  `_log_grouping_decision(groups)`
  `group_winners(*, winners, client_questions, decision_context, max_groups, audited, run_id,
      tenant_id, breaker, model, stats) -> (groups, notes, degradation_reasons)`  (:1196).
      Its inner `_fallback(trigger)` closure at :1226 is what calls `fallback_groups` today, and it
      is reached from FOUR named triggers (call raised / no tool_use block / input did not coerce /
      `validate_groups` returned nothing usable).

research_division.py, as it stands:
  `_D6_STREAMS = ("gemini", "openai", "claude")`  (:130)
  `_D6_MAX_WINNERS = int(os.environ.get("NESTOR_TRIBUNAL_D6_MAX_WINNERS", "15"))`  (:230)
  `_MAX_ANGLES = int(os.environ.get("NESTOR_TRIBUNAL_MAX_ANGLES", "28"))`  (:279)
  `_text_key(value) -> str` — Wave 3 CR-01's whitespace-insensitive join, applied on BOTH sides.
  `_bound_groups_to_winners(groups, allowed_texts, labels)`  (:999)
  the truncation itself: `if len(ordered) > _D6_MAX_WINNERS:` at :1199, inside `_normalise_winners`.

Callers that must keep working — DO NOT EDIT THESE FILES IN THIS PLAN:
  `workshop_rank.run_workshop_stage_b` reads `question_grouping._D6_MAX_GROUPS` twice (the
  `max_mandate_groups` arithmetic and the `enforce_group_coverage(max_groups=...)` argument) and
  passes `question_grouping._D6_MAX_GROUP_SIZE` into `attach_discovery_riders`. Both keep working
  unchanged: this plan changes the VALUES those names resolve to, never the names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declamp the group ceiling and re-size the group cap</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py, tribunal/nestor_pulse_sdk/tests/test_question_grouping.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py lines 83-165 — the whole constants block and every comment on it
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py lines 884-1014 — `attach_discovery_riders`, the only consumer of the size cap
    - tribunal/nestor_pulse_sdk/tests/test_question_grouping.py lines 220-260 and 370-395 — the tests that PIN today's values
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decision D-W4-4a
  </read_first>
  <behavior>
    - `NESTOR_TRIBUNAL_D6_MAX_GROUPS=9` yields `_D6_MAX_GROUPS == 9` (today it yields 5)
    - `NESTOR_TRIBUNAL_D6_MAX_GROUPS=3` still yields 3 — the knob still lowers
    - `NESTOR_TRIBUNAL_D6_MAX_GROUPS=0` yields 1 (the existing `max(1, ...)` floor); a non-integer
      value yields the default 5 and does NOT raise at import
    - unset yields 5 — the default is unchanged; only the clamp on the knob is gone
    - `_D6_MAX_GROUP_SIZE` default is 7; `NESTOR_TRIBUNAL_D6_MAX_GROUP_SIZE=1` still floors at 3
    - a mandate group carrying 5 winners accepts a discovery rider through the real
      `attach_discovery_riders` instead of shedding it
  </behavior>
  <action>
    Replace the `min(5, ...)` wrapper on `_D6_MAX_GROUPS` (`question_grouping.py:97`) so the value is
    `max(1, <int of NESTOR_TRIBUNAL_D6_MAX_GROUPS, default 5>)`, with the int conversion guarded so a
    non-integer env value falls back to 5 rather than raising at import time.

    Rewrite the comment block above it (lines 88-96). It currently argues the `min(5, ...)` is
    load-bearing and "not defensive tidiness". Mark that argument SUPERSEDED in place — this repo's
    convention is strikethrough-in-place, never deletion — and record: D-W3-1 made 5 an operator
    ceiling for TOPIC grouping; operator decision D-W4-4a (2026-07-30, homed in phase 15.7) makes
    one-group-per-client-question the PRIMARY path and removes the clamp so the number FOLLOWS THE
    CLIENT. Name the trap in the comment: a knob that looks like the control and is not is the same
    defect class as `_CANDIDATE_PROMPT_CHARS = 240`.

    Raise `_D6_MAX_GROUP_SIZE`'s default from 4 to 7, keeping the `max(3, ...)` infeasibility floor.
    Rewrite its arithmetic comment (lines 99-110) against the NEW arithmetic: under D-W4-5 a mandate
    group carries a floor of 5 winners for its client question plus up to 2 discovery riders, so
    7 = 5 + 2, and a cap of 4 would shed EVERY rider on arrival — silently deleting the discovery
    bracket Wave 3 built. Leave the precedence rule unchanged and restate it (the group ceiling is an
    operator decision, the size cap is the engine's own, and when they collide the ceiling wins).

    Update `test_question_grouping.py`:
      - the assertion at :234 (`"9 must NOT raise the ceiling"`) INVERTS. Rewrite the test name and
        its message to state what is now true, and add the two negative arms (non-integer, zero).
      - the assertion at :247 (`_D6_MAX_GROUP_SIZE == 4`) becomes 7; keep the `== 3` floor arm at :250.
      - the docstring at :380 states "`_D6_MAX_WINNERS` is 15 and the cap is 4, so ceil(15/4) = 4
        groups". That arithmetic is stale after this plan and Task 3. Re-derive it; if the test still
        passes for a DIFFERENT reason, say which reason in the docstring rather than leaving a stale
        justification that reads correct.
      - add a test proving a 5-member mandate group accepts a rider, driving the REAL
        `build_groups` + `attach_discovery_riders` (never a hand-typed group record), and asserting
        the rider is present in `group["members"]` and absent from `shed`.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" &amp;&amp; "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/question_grouping.py nestor_pulse_sdk/tests/test_question_grouping.py &amp;&amp; grep -v "^ *#" nestor_pulse_sdk/pipeline/tribunal/question_grouping.py | grep -q "^_D6_MAX_GROUPS" &amp;&amp; ! grep -v "^ *#" nestor_pulse_sdk/pipeline/tribunal/question_grouping.py | grep -q "min(5,"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *#' question_grouping.py | grep 'min(5,'` returns NOTHING (comment lines excluded so
      a historical mention in prose cannot self-invalidate the gate)
    - the six behaviour rows above are each driven on the bundled interpreter by setting the env var
      and re-evaluating the constant expression — NOT by `importlib.reload` on a lifted module
    - `_D6_MAX_GROUP_SIZE` evaluates to 7 with the env unset and 3 with the env set to 1
    - the new rider test FAILS against a source-text mutant that restores `"4"` as the size-cap
      default, and PASSES against the edited source — both columns recorded in the SUMMARY
    - `test_question_grouping.py` contains no assertion that a value above 5 is clamped
    - no line in either file contains the literal `resolved_facet` or `parent_index`
  </acceptance_criteria>
  <done>The ceiling knob raises as well as lowers, the size cap is 7, and the two tests that pinned the old values now pin the new ones with their reasoning rewritten rather than their numbers swapped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make one group per client question the PRIMARY grouping path</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py, tribunal/nestor_pulse_sdk/tests/test_question_grouping.py, tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py lines 800-880 — `fallback_groups` and its accepted-spend docstring at :816
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py lines 1196-1260 — `group_winners` and its `_fallback` closure
    - tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py lines 1060-1160 — the three `AT MOST {…} groups` prompt assertions this task breaks
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decision D-W4-4a(a)
    - .planning/phases/15.6-*/15.6-CONTEXT.md, decision D-W3-2 (the degradation-vs-note distinction)
  </read_first>
  <behavior>
    - with `NESTOR_TRIBUNAL_D6_GROUPING_MODE` unset, `group_winners` returns one group per client
      question, makes ZERO calls on the `audited` client, and returns an EMPTY
      `degradation_reasons` list
    - with the same input, `stats` records zero calls and zero cost
    - with `NESTOR_TRIBUNAL_D6_GROUPING_MODE=topic`, today's behaviour is byte-identical: the prompt
      is built, the tool is called, and the four named failure triggers still fall back to
      `fallback_groups` WITH the D-12 degradation reason
    - the primary path still emits `_log_grouping_decision` so a run can show what was grouped
    - the primary path is NOT clamped to `_D6_MAX_GROUPS`, and that overshoot is logged loudly
      through the existing `warn_if_over_ceiling` channel by the caller, unchanged
  </behavior>
  <action>
    Add a module constant `_GROUPING_MODE` reading `NESTOR_TRIBUNAL_D6_GROUPING_MODE`, lower-cased,
    defaulting to `per-question`, with `topic` as the only other recognised value and any unrecognised
    value falling back to `per-question` with a `log.warning` naming the value it did not recognise.
    Put it in the constants block beside `_D6_MAX_GROUPS`, with a comment recording that this is
    operator decision D-W4-4a and that topic grouping is KEPT as an option, not deleted.

    In `group_winners`, before the prompt is built, branch on `_GROUPING_MODE == "per-question"`:
    call `fallback_groups(pool, labels)`, build the groups with `build_groups`, call
    `_log_grouping_decision`, and return `(groups, notes, [])` — an EMPTY degradation list.

    The degradation distinction is the whole point and must not be fudged. `fallback_groups` returns a
    D-12 DEGRADATION sentence describing a step that produced nothing usable; on the primary path
    nothing failed, so that sentence MUST NOT be emitted. Discard the returned reason on this path and
    say in a comment why: D-W3-2's sentence describes a FULL FALLBACK of the grouping step, and
    reporting a degradation for the chosen primary path is exactly the alarm fatigue D-12 forbids.
    Add a plain-words NOTE instead, in the existing note vocabulary style, saying the questions were
    grouped one group per client question by configuration.

    Leave the `topic` branch — the prompt build, the filled tool copy, the four named triggers and
    the `_fallback` closure — untouched and reachable.

    Update `test_question_grouping.py` with: the primary path makes zero calls and returns no
    degradation reason; the primary path with 7 client questions returns 7 groups (proving it is not
    clamped); `topic` mode still calls the model and still degrades on each of the four triggers;
    an unrecognised mode falls back to per-question and logs.

    Update `test_workshop_scope_guard.py`: the three assertions at :1071, :1116 and :1153 assert the
    grouping PROMPT contains `AT MOST {question_grouping._D6_MAX_GROUPS} groups`. On the primary path
    there is no prompt at all, so those three tests must either set
    `NESTOR_TRIBUNAL_D6_GROUPING_MODE=topic` for their duration or assert the primary path's shape
    instead. Choose per test according to what each test is actually FOR, and say which you chose and
    why in the SUMMARY. Do not delete a test to make a build green.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" &amp;&amp; "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/question_grouping.py nestor_pulse_sdk/tests/test_question_grouping.py nestor_pulse_sdk/tests/test_workshop_scope_guard.py &amp;&amp; grep -v "^ *#" nestor_pulse_sdk/pipeline/tribunal/question_grouping.py | grep -q "_GROUPING_MODE"</automated>
  </verify>
  <acceptance_criteria>
    - driving the real `group_winners` on the bundled interpreter with a stub `audited` client that
      RAISES on any method call returns one group per client question — proving no call was made
    - the returned `degradation_reasons` is `[]` on the primary path and length 1 on each of the four
      `topic`-mode triggers
    - with 7 client questions the primary path returns 7 groups while `_D6_MAX_GROUPS` is 5
    - `grep -v '^ *#' question_grouping.py | grep -c 'GROUP_RULE_SINGLE_PARENT_ONLY'` is unchanged
      from the phase base — the topic path was not gutted
    - every previously-passing test in `test_question_grouping.py` and `test_workshop_scope_guard.py`
      that this task did not deliberately change still passes
    - the three `AT MOST … groups` assertions are accounted for individually in the SUMMARY, each with
      the reason for the choice made
  </acceptance_criteria>
  <done>Per-question grouping is the default and costs nothing; topic grouping is one env var away and still degrades exactly as D-W3-2 specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Raise the dispatch winner bound so the D-W4-5 floor is not silently clipped</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py, tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py lines 220-290 — `_D6_MAX_WINNERS`, `_MAX_ANGLES` and their comments
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py lines 1140-1235 — `_normalise_winners` and the truncation at :1199
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py lines 999-1135 — `_bound_groups_to_winners` (Wave 3 CR-01 lives here; do not disturb `_text_key`)
    - tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py lines 420-470 — the tests asserting `== rd._D6_MAX_WINNERS == 15`
    - tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py lines 1770-1830 — `test_the_facet_resolution_seam_has_no_production_caller_in_this_wave`, which scans the WHOLE package for two literals
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decision D-W4-5
  </read_first>
  <behavior>
    - 17 winners (5 per client question over 3 client questions, plus 2 cross-cutting) all reach
      dispatch: none is dropped by `_normalise_winners`
    - the bound still BITES: a caller supplying more winners than the raised bound still has the
      surplus dropped, with the existing warning wording intact
    - the bound is still whitespace-insensitive on both sides — Wave 3's CR-01 fix is untouched
    - a discovery rider is still exempt from the bound
  </behavior>
  <action>
    Raise `_D6_MAX_WINNERS`'s default from 15 to 32 (env `NESTOR_TRIBUNAL_D6_MAX_WINNERS` unchanged),
    and rewrite its comment with the arithmetic that produces the number: D-W4-5 sets the winner count
    at `5 x <client questions> + 2 cross-cutting`, so six client questions is 32. State plainly that
    15 would have clipped the validated configuration's 17 winners down to 15 — silently, at
    `_normalise_winners`, after the tournament had already paid to rank them.

    State in the same comment why this is NOT a spend increase, because T-15.2-61 says the angle count
    is the only real spend control this engine has left and a reader will challenge it: under group
    dispatch the paid-call count is `groups x len(_D6_STREAMS)`, bounded by `_D6_MAX_GROUPS`, and
    `_MAX_ANGLES` (28) is unchanged. Raising the WINNER bound changes how many questions ride inside
    the same groups, not how many calls are issued.

    Do not touch `_text_key`, `_bound_groups_to_winners`' body, or the discovery exemption branch.

    Update `test_research_division_assignment.py`: the assertions at :433, :455 and :456 read
    `== rd._D6_MAX_WINNERS` (symbolic, survives) and `== rd._D6_MAX_WINNERS == 15` (a hard 15, breaks).
    Replace the hard 15 with the new value and add the D-W4-5 shape as its own test: build 17 winners
    across 3 client-question labels through the REAL `build_groups`, drive `divide()`'s D6 branch, and
    assert all 17 are researched. Add the negative arm: 40 winners still truncate to the bound.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" &amp;&amp; "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/research_division.py nestor_pulse_sdk/tests/test_research_division_assignment.py &amp;&amp; grep -v "^ *#" nestor_pulse_sdk/pipeline/tribunal/research_division.py | grep -q "_D6_MAX_WINNERS"</automated>
  </verify>
  <acceptance_criteria>
    - the 17-winner test FAILS on a source-text mutant restoring `"15"` and PASSES on the edited
      source; both columns recorded in the SUMMARY
    - the 40-winner negative test passes in BOTH columns (the bound still bites) — recorded as a
      behaviour-preservation guard, not as vacuity
    - `grep -c "_text_key" research_division.py` is unchanged from the phase base
    - `test_the_facet_resolution_seam_has_no_production_caller_in_this_wave` still passes: neither the
      source nor the tests nor any COMMENT written by this plan contains the literals `resolved_facet`
      or `parent_index`. Phase 15.6 nearly turned this red with its own explanatory comment.
    - `_MAX_ANGLES` is unchanged at 28 and `_D6_STREAMS` is unchanged at three providers
  </acceptance_criteria>
  <done>Seventeen winners reach three providers; forty still do not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grouping LLM -> group records | model output that decides which questions share a paid research call |
| winner text -> three third-party providers | model-authored text reaching paid external APIs verbatim |
| env vars -> spend bounds | an operator-tunable value that governs paid call volume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.7-01-01 | Denial of wallet | `_D6_MAX_GROUPS` declamp | accept | the knob may now RAISE the ceiling. The default is unchanged at 5, the paid-call count stays `groups x 3`, and `warn_if_over_ceiling` still fires. D-W4-4a is an operator decision and the accepted-spend consequence is already recorded at `fallback_groups`' docstring :816 |
| T-15.7-01-02 | Denial of wallet | `_D6_MAX_WINNERS` 15 -> 32 | mitigate | winners ride INSIDE existing groups; the paid-call count is `groups x len(_D6_STREAMS)` and is untouched. `_MAX_ANGLES` (28) is left at its value as the second bound, asserted in Task 3 |
| T-15.7-01-03 | Tampering | winner text on a provider prompt | mitigate | unchanged. `_SUBQ_CHARS` (600) still bounds each item and `_text_key` still joins whitespace-insensitively on both sides. This plan touches neither |
| T-15.7-01-04 | Elevation of privilege | per-question primary path skipping the coverage assertion | mitigate | `enforce_group_coverage` in `workshop_rank.py` is unchanged and still runs after grouping; `fallback_groups` is already coverage-exact by construction (one group per label) |
| T-15.7-01-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NOTHING. No dependency is added to any file it touches |
</threat_model>

<verification>
- `question_grouping.py` and `research_division.py` byte-compile on the bundled interpreter.
- `grep -v '^ *#' question_grouping.py | grep 'min(5,'` returns nothing.
- The per-question primary path is proven to make zero calls by driving it with an `audited` stub
  that raises on every method.
- 17 winners survive dispatch; 40 do not.
- A 5-member mandate group keeps its rider.
- Every mutation column is recorded with BOTH results. A test that passes in both columns is named as
  a behaviour-preservation guard or it is vacuous.
</verification>

<success_criteria>
- All three constants changed, each with its comment re-derived rather than its number swapped.
- Per-question grouping is the default, costs no call, and emits no degradation reason.
- Topic grouping is still reachable and still degrades on all four triggers.
- No test was deleted; the three `AT MOST … groups` assertions are individually accounted for.
- No deploy, no migration, no `gcloud builds submit`, no live run.
- `cloudbuild.test-engine.yaml` NOT touched — plan 15.7-03 is its sole editor this phase.
</success_criteria>

<output>
Create `.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-01-SUMMARY.md`
when done. `.planning/` is gitignored — `git add -f` it or the orchestrator cannot see it.
</output>
