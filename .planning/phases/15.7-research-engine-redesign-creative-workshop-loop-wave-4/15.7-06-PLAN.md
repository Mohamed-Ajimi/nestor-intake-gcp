---
phase: 15.7-research-engine-redesign-creative-workshop-loop-wave-4
plan: 06
type: execute
wave: 3
depends_on: ["15.7-02", "15.7-04"]
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_evolve.py
  - tribunal/nestor_pulse_sdk/tests/test_workshop_languages.py
autonomous: true
requirements: [D-R6, D-R10, D-W4-1, D-W4-2]

must_haves:
  truths:
    - "D-R6: evolve is GENERATIVE — it produces NEW questions ADDED to the pool, never swapping out their parents"
    - "D-R6: the four mutation moves exist and are named — COMBINE, EXTEND, INVERT, SPECIALISE — plus INVENT under D-R10"
    - "D-R6: a meta-review call reads every critique flaw and judge reason for the round and writes short guidance for the next round"
    - "D-W4-2: a discovery candidate's own admitting quote and URL ARE its enrichment anchor; a cross-cutting question gets BOTH parents' orientation findings"
    - "D-W4-1: the barred list travels with every evolve call, each entry carrying its flaw"
    - "Each produced question carries the move that made it and the round it was born in, so saturation and the INVENT-survival question are both answerable"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_evolve.py"
      provides: "generative evolve (5 moves), the enrichment anchors, and the meta-review call"
      min_lines: 350
      exports: ["evolve_generative", "meta_review", "MOVE_COMBINE", "MOVE_EXTEND", "MOVE_INVERT", "MOVE_SPECIALISE", "MOVE_INVENT"]
  key_links:
    - from: "workshop_evolve.evolve_generative"
      to: "workshop_register.barred_block"
      via: "the barred list with flaws, in every evolve prompt"
      pattern: "barred_block"
    - from: "workshop_evolve.evolve_generative"
      to: "the orientation findings for a candidate's parent"
      via: "the enrichment anchor block (D-W4-2)"
      pattern: "anchor"
---

<objective>
Build `workshop_evolve.py`: the generative evolve step (D-R6, D-R10) and the meta-review call (D-R6).

What is wrong today, stated as the spec states it: Co-Scientist ranks novel hypotheses it invented and
its evolution step "refines, COMBINES, builds upon"; ours ranks rewordings of client questions and its
evolve prompt says "Do NOT merge two questions into one, and do NOT broaden one". Co-Scientist feeds a
meta-review back into generation; we have none. Co-Scientist iterates; we run once.

Evolve becomes generative: take the top-ranked and produce NEW questions ADDED TO THE POOL, NOT
SWAPPING OUT THEIR PARENTS. COMBINE two winners into one sharper question covering both. EXTEND — "if
the answer is yes, what is the next thing we would need to know?". INVERT — "what would have to be
true for this to matter?". SPECIALISE — name the entity, geography, timeframe. And under D-R10,
INVENT — an angle nobody asked for, which a grounded lookup then admits or drops.

Purpose: this module is where the loop stops being a rephraser. Measured: 5 of 10 research slots were
loop-generated, and ALL FIVE of the top-10 newcomers were COMBINE / INVERT / EXTEND. Do not judge the
feature by INVENT's survival rate.
Output: one new module; the existing `workshop_rank.evolve_winners` is NOT deleted and NOT edited here
— plan 15.7-09 decides its fate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_preflight gate="blocking">
Worktrees here are cut from a STALE ref — 7 of 7 in phase 15.6, distances GROWING, and
`git rev-list --count BASE..HEAD == 0` reads GREEN while stale. Assert the MERGE-BASE:

    git merge-base HEAD <PHASE_BASE_SHA>

Anything else means stale — `git reset --hard <PHASE_BASE_SHA>` first.
Positive sentinels for THIS plan's base (waves 1 and 2 are merged):
  - `workshop_rank._CANDIDATE_PROMPT_CHARS` default is 600
  - `nestor_pulse_sdk/pipeline/tribunal/workshop_register.py` EXISTS and exports `barred_block`
  - `nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py` EXISTS
If any is missing your tree predates a dependency. STOP and report.
</worktree_preflight>

<proving_things_on_this_machine>
No pytest, no Docker, no `python3`. Stdlib-only Python 3.14 at
`C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`. Split the module so the PURE
parts — the prompt builders, the line parser, the anchor-block builder, the move stamping — are
drivable without a network. `ast`-lift the real functions with stub globals and refuse to run if a
lift target is missing. `PYTHONIOENCODING=utf-8`. Never `importlib.reload` a lifted module.
NO OUTBOUND HTTP from any test (`tests/conftest.py:164`).
</proving_things_on_this_machine>

<context>
@.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-CONTEXT.md
@tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py
@tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py

<interfaces>
The existing evolve step, which this module GENERALISES rather than replaces:

  `workshop_rank._EVOLVE_PROMPT`                                                  (:1749-1786)
      Its SHARPENING RULE reads: "Keep the SAME subject and the SAME scope... Do NOT merge two
      questions into one, and do NOT broaden one." (:1761). D-R6 says DELETE that sentence — for
      generative evolve. Keep the scope lock FOR THE MANDATE BRACKET; discovery questions are governed
      by the evidence-anchor rule instead. This plan writes the NEW prompt in the new module and does
      NOT edit `workshop_rank.py` at all — plan 15.7-09 owns that deletion, so the two do not race.
  `workshop_rank._parse_winner_lines(text, n)`                                    (:1798)
      The fenced `INDEX | text | LANGS:` parser. Its comment at :1787-1796 states the rule this module
      must also follow: PARENT is not requested and would not be believed if supplied; the line is
      addressed by INDEX and `parent` and `rank` are stamped in Python. A line carrying a `PARENT:`
      segment is read only far enough to log a DEBUG disagreement, then discarded.
  `workshop_rank._winners_block(winners)`                                         (:1875)
      The indexed block with each WEAK's flaw beneath it.
  `workshop_rank._WINNERS_START / _WINNERS_END`                                   (:1589-1590)
      The fenced sentinel contract. Asking for JSON is NOT an option on a citation-bearing call — that
      combination is an HTTP 400, recorded twice in `steps.py` and once at `tools.py:14-16`.
  `workshop_rank._flatten(text, cap)` (:300), `_CANDIDATE_PROMPT_CHARS` (600 after plan 15.7-02),
      `_FLAW_MAX_CHARS` (160), `_WINNER_MAX_CHARS` (600 after plan 15.7-02), `_LANGS_MAX`,
      `_normalise_langs`, `_IGNORE_INSTRUCTIONS`, `_render_decision`, `_EVOLVE_MODEL`,
      `_EVOLVE_MAX_TOKENS`, `_EVOLVE_ENABLED`.
      IMPORT these from `workshop_rank` rather than re-declaring them. This module is imported BY
      plan 15.7-09 from inside `workshop_rank`, so import it FUNCTION-LOCALLY there to avoid the cycle
      — state that requirement in this module's docstring so plan 15.7-09 does the right thing.
  `workshop.py` orientation shape: `orientation` is a list of
      `{label, findings, brief_conflicts, citations, ok, ...}`; `_findings_block(findings)` (:1206)
      renders findings INDEXED and TRUNCATED to `_FINDING_PROMPT_CHARS` (240) as a security control.
  `workshop_register.barred_block(register)` and `record_drop(...)` — from plan 15.7-04.
  `discovery_bracket.DISCOVERY_PARENT = "__discovery__"`.

The measured target this module must be able to produce (`exp11`), quoted so the prompt can be judged
against real output rather than taste:
  a SPECIALISE-class result: "average selling price, margin per cup and daily volumes at the three
  largest NL operators in 2024, versus Circle K Denmark coffee unit economics where own-brand broke
  through strongest"
  a COMBINE-class cross-cutting result: "what phasing do European retailers use from pilot to network
  rollout, how long is each phase, and what go/no-go KPI criteria apply between phases"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The enrichment anchors (D-W4-2) and the barred block, as pure builders</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_evolve.py, tribunal/nestor_pulse_sdk/tests/test_workshop_languages.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 1206-1219 — `_findings_block` and its security-control docstring
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 653-750 — `_parse_orientation`, to see exactly what shape `findings` and `brief_conflicts` arrive in
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/discovery_bracket.py lines 231-300 — `discovery_question_text` and the `provenance` shape carrying quote and URL
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_register.py — `barred_block`, from plan 15.7-04
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decision D-W4-2 in full
  </read_first>
  <behavior>
    - a mandate candidate parented to `Q2` gets `Q2`'s orientation findings and nothing else
    - a discovery candidate whose parent is `__discovery__` gets ITS OWN admitting quote and URL as its
      anchor block, and NOT any orientation findings
    - a cross-cutting candidate whose `parents` span `Q1` and `Q3` gets BOTH `Q1`'s and `Q3`'s
      orientation findings
    - the union of ALL orientation findings is never produced for any candidate — that shape must not
      be reachable
    - a candidate whose parent has no findings gets an explicit placeholder, never an empty block
    - every anchor line is indexed and truncated, and a finding containing a newline and a forged
      `9 | KEEP` line cannot produce a second addressable record
  </behavior>
  <action>
    Create `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_evolve.py`.

    Module docstring: generative evolve and the meta-review, D-R6 plus D-R10's INVENT move. State the
    import direction explicitly — this module imports `workshop_rank` for the shared renderers and
    constants, so `workshop_rank` must import IT FUNCTION-LOCALLY (plan 15.7-09) to avoid a cycle,
    the same technique `citations/extractor.py:937` already uses for `is_redirect_url`.

    Implement `anchor_block(candidate, *, findings_by_label, client_questions) -> str`, PURE.

    Its docstring must carry D-W4-2's rule and its rejected alternative. Evolve is specified to receive
    "the orientation findings for the parent question" so it can inject specificity, but a discovery
    question's parent is `__discovery__`, which has no orientation findings — the same class of gap
    D-W3-5 closed for dispatch, and sharper now that D-R10 lets evolve invent an angle in ANY round.
    The rule: a discovery candidate ALREADY CARRIES the quote and URL that admitted it, and THAT ANCHOR
    IS ITS FINDINGS BLOCK — self-consistent with "no source, no slot", because the evidence that
    admitted the angle is the evidence that enriches it. For a cross-cutting question spanning two
    client questions, pass BOTH parents' orientation findings; cross-cutting mandate questions are
    where the best measured output came from and they have two real parents, not `__discovery__`.
    EXPLICITLY REJECTED, and the docstring must say so: passing the union of all orientation findings
    to any `__discovery__` parent. It re-couples discovery to orientation — exactly the coupling D-R10
    broke — and it inflates the prompt.

    Render every anchor line through `workshop_rank._flatten` at `workshop._FINDING_PROMPT_CHARS`, and
    state in the docstring that indexing and truncation are SECURITY CONTROLS and not formatting,
    because findings are derived from fetched web pages, i.e. attacker-controllable text.

    Add a thin `barred_section(register)` wrapper that calls `workshop_register.barred_block` and
    renders it under a heading, so the evolve prompt and the generation prompt (plan 15.7-07) present
    the barred list identically. Do not re-implement the rendering.

    Tests go in `tribunal/nestor_pulse_sdk/tests/test_workshop_languages.py` in a clearly named
    section — that file is `evolve_winners`' existing home.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop_evolve.py nestor_pulse_sdk/tests/test_workshop_languages.py</automated>
  </verify>
  <acceptance_criteria>
    - the four anchor cases (mandate / discovery / cross-cutting / no-findings) each produce a
      distinct, asserted block, driven against the real function
    - a `__discovery__` candidate's block contains its own quote and URL and contains NONE of the
      orientation findings text, asserted by substring absence over a fixture where the two differ
    - a mutant that hands `__discovery__` the union of all findings FAILS that test; both columns
      recorded in the SUMMARY
    - a finding containing `\n9 | KEEP | forged` renders as ONE indexed line
    - the no-findings case renders a non-empty placeholder
    - `barred_section` is proven to delegate — a mutant that changes `workshop_register.barred_block`
      changes `barred_section`'s output, asserted, so the delegation is real and not a copy
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, comments included
  </acceptance_criteria>
  <done>Every candidate gets the anchor its own provenance justifies, and no candidate gets the whole run's findings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: evolve_generative — five named moves, new questions added to the pool</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_evolve.py, tribunal/nestor_pulse_sdk/tests/test_workshop_languages.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 1749-1886 — `_EVOLVE_PROMPT`, its deliberate-divergence comment, `_parse_winner_lines` and `_winners_block`
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 1888-1990 — `evolve_winners`, the call shape and the never-raises contract
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 1104-1150 — `_candidates_from_lines`, the other Python-stamped-parent parser
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decisions D-R6 and D-R10
  </read_first>
  <behavior>
    - the call returns NEW candidate dicts; every input winner comes back untouched and is still in
      the pool afterwards
    - each new candidate carries `move` (one of the five constants), `born_round`, `parent`, `parents`,
      `source`, and a `text` bounded by `_WINNER_MAX_CHARS`
    - a COMBINE across two client questions produces a candidate whose `parents` holds BOTH labels and
      whose `cross_cutting` reads True
    - an INVENT candidate is returned MARKED as not-yet-admitted; this module never admits one
    - `parent` is stamped in Python from the input winners the line references — a `PARENT:` segment in
      the model's output is logged at DEBUG and discarded
    - a failed call, an open breaker, a missing sentinel and a garbled line each produce ZERO new
      candidates and never raise, and never lose an input winner
    - `_EVOLVE_ENABLED=false` produces zero new candidates and makes zero calls
  </behavior>
  <action>
    Export five move constants — `MOVE_COMBINE`, `MOVE_EXTEND`, `MOVE_INVERT`, `MOVE_SPECIALISE`,
    `MOVE_INVENT` — and write `_GENERATIVE_EVOLVE_PROMPT` in `workshop_evolve.py`.

    The prompt states the four mutation moves in D-R6's own words — combine two winners into one
    sharper question covering both; extend, "if the answer is yes, what is the next thing we would need
    to know?"; invert, "what would have to be true for this to matter?"; specialise, name the entity,
    the geography and the timeframe — plus INVENT under D-R10, described as an angle nobody asked for
    that will be checked against real sources before it can take a slot.

    THE SCOPE LOCK IS NOT DELETED, IT IS SCOPED. The prompt must keep the mandate scope lock for
    MANDATE-bracket output (a mandate question stays inside what the client asked) and state that
    discovery output is governed by the evidence-anchor rule instead. Write that distinction into the
    prompt text and into the docstring, because "delete line 1761" read carelessly removes the mandate
    guarantee D4 depends on.

    Carry the enrichment anchors from Task 1 into the prompt per candidate, and the barred section, and
    the meta-review guidance string from Task 3 when the caller supplies one. Carry
    `workshop_rank._IGNORE_INSTRUCTIONS` verbatim.

    Output contract: fenced between `workshop_rank._WINNERS_START` / `_WINNERS_END`, one line per new
    question, addressed by an index into a list the PROMPT numbers, in the form
    `INDEX | MOVE | SOURCE_INDICES | <new question> | LANGS: de,en`. Parse it with the same ASVS V5
    discipline `_parse_winner_lines` uses: pre-filled output, regex-extracted and bounds-checked
    indices, a partially valid row rejected whole, the move clamped to the five allowed values, raw
    model text never decoded as structured data, nothing raises. Clamp `SOURCE_INDICES` to indices that
    exist in the supplied winners, and derive `parent` / `parents` / `cross_cutting` IN PYTHON from
    those source winners — never from anything the model wrote.

    Implement `evolve_generative(*, winners, register, findings_by_label, client_questions,
    guidance="", round_no, decision_context="", run_language="", audited, run_id, tenant_id,
    feed=None, breaker=None, stats=None) -> tuple[list[dict], list[str]]` returning
    `(new_candidates, degradation_reasons)`. NEVER RAISES. One plain text completion, no tools, no
    citations — the same call shape `evolve_winners` already uses.

    Record in the docstring what measurement says to expect, so nobody deletes the feature for the
    wrong reason: 5 of 10 research slots were loop-generated, but ALL FIVE top-10 newcomers were
    COMBINE / INVERT / EXTEND, and the single surviving INVENT ranked 16, below the cut — even though
    its content was excellent. Genuine discovery arrives via inversion and combination of existing
    questions more often than from-scratch invention.

    Tests in `test_workshop_languages.py`, same named section, with a stub `audited` client.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop_evolve.py nestor_pulse_sdk/tests/test_workshop_languages.py</automated>
  </verify>
  <acceptance_criteria>
    - a stubbed response producing one line per move yields five new candidates, one per move constant,
      each with `born_round` set and `text` non-empty
    - every input winner is returned by the caller unchanged — proven by deep-copy comparison, because
      "added to the pool, not swapping out their parents" is the decision's own wording
    - a COMBINE line naming source indices from two different client questions yields
      `parents == [both labels]` and `cross_cutting is True`
    - a line supplying `PARENT: Q9` is discarded and the Python-stamped parent wins, asserted
    - the four failure shapes (raised call, open breaker, no sentinel, garbled lines) each yield zero
      new candidates and zero lost winners, asserted individually
    - a mutant that reads `parent` from model output FAILS the discard test; both columns recorded
    - the rendered prompt contains BOTH the mandate scope lock sentence and the discovery
      evidence-anchor sentence, asserted by substring — a mutant that deletes the mandate lock outright
      fails this, which is the point of scoping rather than deleting
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, comments included
  </acceptance_criteria>
  <done>Evolve produces new questions, names the move that made each one, and never re-parents anything from model output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: meta_review — one call that turns a round's flaws and judge reasons into guidance</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_evolve.py, tribunal/nestor_pulse_sdk/tests/test_workshop_languages.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 995-1049 — `_TOURNAMENT_PROMPT` and `_match_block`, which plan 15.7-08 changes to emit a reason per match
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 377-441 — `_parse_critique_lines`, the source of the flaw clauses
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 233-271 — the plain-words sentence vocabulary
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decision D-R6, the Meta-review paragraph
  </read_first>
  <behavior>
    - given a round's critique flaws and judge reasons, the call returns ONE short guidance string
    - the guidance is bounded, whitespace-collapsed, and cannot carry a newline or a pipe into the next
      round's prompt
    - an empty input produces an empty guidance string and makes NO call
    - a failed call, an open breaker and an unusable response each produce an empty guidance string and
      never raise — the loop continues without guidance rather than stopping
    - the guidance never contains an instruction that could re-parent or re-scope a candidate: it is
      rendered into the next prompt as DATA, under the same ignore-instructions sentence
  </behavior>
  <action>
    Implement `meta_review(*, flaws, judge_reasons, decision_context="", round_no, audited, run_id,
    tenant_id, breaker=None, stats=None) -> tuple[str, list[str]]` in `workshop_evolve.py`, returning
    `(guidance, degradation_reasons)`. NEVER RAISES.

    Write `_META_REVIEW_PROMPT`: it reads every critique flaw and judge reason for the round and writes
    SHORT guidance for the next generation round — what the questions keep getting wrong and what would
    make the next batch better. Render both input lists INDEXED and TRUNCATED, the same security
    control every block in this engine states. Carry `workshop_rank._IGNORE_INSTRUCTIONS` verbatim.

    The returned guidance is model output that goes straight back into another model's prompt, so bound
    it exactly as the rest of this engine bounds such text: run it through
    `workshop_rank._flatten` at a new module constant in the house `NESTOR_TRIBUNAL_*` idiom, with a
    comment saying this is a SECURITY CONTROL and not formatting. State in the docstring that the
    guidance is presented to the next round as DATA under the ignore-instructions sentence, never as an
    instruction, and that this is why it is bounded rather than trusted.

    Docstring must record what the meta-review is FOR, in D-R6's terms: today the judge sees two
    question texts, a short decision blurb and a 160-character flaw clause — it is judging blind — and
    the three effects of giving it reasons are better judgements, an audit trail of WHY 7 beat 9, and
    MATERIAL FOR THE META-REVIEW. This function is the third of those three. It depends on plan
    15.7-08 having made the judge emit reasons; state that dependency, and make the function degrade to
    flaws-only guidance if `judge_reasons` is empty rather than failing.

    Tests in `test_workshop_languages.py`, same named section.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop_evolve.py nestor_pulse_sdk/tests/test_workshop_languages.py</automated>
  </verify>
  <acceptance_criteria>
    - an empty flaws list AND an empty judge_reasons list makes zero calls, asserted on a stub that
      raises if called
    - a guidance response containing a newline and a `|` comes back collapsed to one line, asserted
    - a guidance response longer than the bound comes back truncated, asserted
    - the three failure shapes each return `("", [reason])` and never raise
    - with `judge_reasons=[]` but flaws present, the call still happens and the prompt renders the
      flaws — proving the graceful degradation rather than a hard dependency on plan 15.7-08
    - a mutant that removes the `_flatten` bound on the returned guidance fails the newline test; both
      columns recorded in the SUMMARY
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, comments included
  </acceptance_criteria>
  <done>One call per round turns the round's own criticism into the next round's brief, bounded like every other piece of model text in this engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orientation findings (fetched web pages) -> the evolve prompt | attacker-controllable text |
| barred text -> the evolve prompt | previously rejected model output re-entering a call |
| meta-review output -> the next round's prompt | model output feeding a later model call |
| evolved question text -> three paid third-party providers | via winners and dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.7-06-01 | Tampering | a forged record in the anchor block | mitigate | every anchor line goes through `workshop_rank._flatten` at `_FINDING_PROMPT_CHARS` and is addressed by INDEX — the same two controls `_findings_block` already states. Pinned by the forged-line test |
| T-15.7-06-02 | Tampering | model re-parenting a new candidate | mitigate | `parent`, `parents` and `cross_cutting` are derived IN PYTHON from the source winners the line indexes; a `PARENT:` segment is logged at DEBUG and discarded, exactly as `_parse_winner_lines` and `_candidates_from_lines` already do. Pinned by a test and a mutant |
| T-15.7-06-03 | Tampering | meta-review guidance carrying an instruction into the next round | mitigate | the guidance is `_flatten`-bounded, rendered as DATA, and sits under the ignore-instructions sentence. Pinned by the newline test and a mutant that removes the bound |
| T-15.7-06-04 | Elevation of privilege | generative evolve broadening a MANDATE question | mitigate | the mandate scope lock is KEPT in the prompt and only SCOPED, not deleted; a mutant that deletes it outright fails an assertion |
| T-15.7-06-05 | Denial of wallet | evolve inventing an unbounded number of angles | accept | D-W4-7 instruments rather than enforces; D-W3-4's dispatch allocation still bounds what actually runs, and plan 15.7-05's admission gate drops anything unevidenced |
| T-15.7-06-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NOTHING and adds no third-party dependency |
</threat_model>

<verification>
- Every input winner survives an evolve call untouched (deep-copy proven).
- The four anchor cases are distinct and the `__discovery__` union shape is unreachable.
- The Python-stamped parent beats a model-supplied one.
- The mandate scope lock survives in the prompt; the discovery evidence-anchor rule joins it.
- Guidance is bounded and collapsed.
- Every mutant is recorded with both columns.
</verification>

<success_criteria>
- One new module with five named moves, the D-W4-2 anchors, and the meta-review.
- `workshop_rank.py` is NOT edited by this plan — plan 15.7-09 owns the `:1761` deletion, so the two
  do not race on the same file.
- Tests in the already-registered `test_workshop_languages.py`;
  `cloudbuild.test-engine.yaml` NOT touched (plan 15.7-03 is its sole editor this phase).
- No deploy, no migration, no live run.
</success_criteria>

<output>
Create `.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-06-SUMMARY.md`
when done, listing every exported signature verbatim and stating the function-local-import requirement
for plan 15.7-09. `.planning/` is gitignored: `git add -f`.
</output>
