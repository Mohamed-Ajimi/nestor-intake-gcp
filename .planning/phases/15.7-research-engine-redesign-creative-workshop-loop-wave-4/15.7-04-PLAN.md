---
phase: 15.7-research-engine-redesign-creative-workshop-loop-wave-4
plan: 04
type: execute
wave: 2
depends_on: ["15.7-02"]
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_register.py
  - tribunal/nestor_pulse_sdk/tests/test_workshop_critique.py
autonomous: true
requirements: [D-W4-1]

must_haves:
  truths:
    - "D-W4-1: the rejected register lives for ONE workshop run and dies with it — no table, no migration, no cross-run storage"
    - "D-W4-1: a KILL for a defect bars; a KILL for being a duplicate does NOT bar; a WEAK bars only after two evolve passes"
    - "D-W4-1: LOSING THE TOURNAMENT NEVER BARS — the coverage guard promotes below-the-cut candidates and that repair path must keep working"
    - "D-W4-1: an invented angle whose grounded lookup failed IS barred, so it cannot be re-proposed and spend a second lookup"
    - "D-W4-1: every barred entry carries its FLAW into the prompt — 'don't propose these, and here is why' beats a bare list"
    - "D-W4-1: every drop is logged with WHAT was dropped and ONTO WHAT, not just a count"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_register.py"
      provides: "the within-run rejected register: bar/observe/render/drop-log, all pure"
      min_lines: 200
      exports: ["new_register", "bar", "barred_block", "record_drop", "drop_summary", "BAR_KILL_DEFECT", "BAR_WEAK_TWICE", "BAR_LOOKUP_FAILED"]
  key_links:
    - from: "workshop_register.barred_block"
      to: "the generate and evolve prompts"
      via: "the bounded, indexed barred list carrying each entry's flaw"
      pattern: "barred_block"
    - from: "workshop_register.record_drop"
      to: "the per-round drop log"
      via: "what was dropped and onto what"
      pattern: "clustered_onto"
---

<objective>
Build the within-run rejected register (D-W4-1) as a pure, stdlib-only module.

`grep -n "barred" workshop_rank.py workshop.py` returns NOTHING today. There is no rejected register
in this codebase at all — not cross-run, not within-run. Wave 4 builds it from scratch.

The operator decision closes the spec's ambiguity: "barred this run, kept for the next" does NOT mean
cross-run storage. "The next" means the next ROUND. The register lives for one workshop run and dies
with it. **Consequence: NO new table and NO fourth alembic migration.** `0016` and `0017` have still
never touched a database; this phase must not add a third unpaid proof.

Purpose: with D-R10 letting evolve invent an angle in ANY round, the barred list plus the semantic
drop are the only things stopping the loop re-proposing its own rejects and paying for the same failed
lookup twice. The harness measured exactly that gap — "minimale netwerkdichtheid" was re-proposed in
rounds 2 AND 3, spending a grounded lookup each time, because failed-lookup angles were never barred.
Output: one new module, its tests in an already-registered file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_preflight gate="blocking">
Worktrees here are cut from a STALE ref — 7 of 7 in phase 15.6 at 486-524 commits behind, distances
GROWING, and `git rev-list --count BASE..HEAD == 0` reads GREEN while stale. Assert the MERGE-BASE:

    git merge-base HEAD <PHASE_BASE_SHA>

Anything else means stale — `git reset --hard <PHASE_BASE_SHA>` first.
Positive sentinels for THIS plan's base (it depends on 15.7-02, which is already merged):
  - `workshop._CANDIDATES_PER_QUESTION` default is 12, not 6
  - `workshop_rank._CANDIDATE_PROMPT_CHARS` default is 600, not 240
If either still shows the old value your tree predates plan 15.7-02. STOP and report.
</worktree_preflight>

<proving_things_on_this_machine>
No pytest, no Docker, no `python3`. Stdlib-only Python 3.14 at
`C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`. This module imports nothing
from `nestor_pulse_sdk`, so drive it directly. `PYTHONIOENCODING=utf-8`. Prove non-vacuity by
mutating the real source text and confirming each check bites.
</proving_things_on_this_machine>

<context>
@.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-CONTEXT.md
@tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py
@tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py

<interfaces>
The verdict vocabulary and the guard this register must not break:

  `workshop_rank._KEEP / _WEAK / _KILL = "KEEP" / "WEAK" / "KILL"`                        (:190-192)
      Re-declare these three literals locally with a comment saying why: this module must import
      nothing from `nestor_pulse_sdk`, both so it stays drivable on the one available interpreter and
      so `workshop.py` and `workshop_rank.py` can both import IT without a cycle.

  `workshop_rank._CRITIQUE_PROMPT`                                                        (:346)
      The three verdicts and their meanings, verbatim. KILL is defined as "not worth researching at
      all: unanswerable in principle, pure opinion, A RESTATEMENT OF ANOTHER CANDIDATE, or nothing
      about the client's decision turns on it." That single clause is why a KILL is not automatically
      a bar — see the treatment table below.

  `workshop_rank.enforce_scope_guard`                                                     (:2130)
      Its documented repair ladder: PROMOTE the label's best-ranked candidate from `all_ranked` even
      if it finished below the winner cut, else INJECT the client question verbatim. THE COVERAGE
      GUARANTEE DEPENDS ON LOSERS STAYING AVAILABLE.

  `workshop_rank._flatten(text, cap)`                                                     (:300)
      The renderer this module's `barred_block` must mirror: replace `|`, `\r` and `\n` with spaces,
      squeeze whitespace, truncate. Documented as a SECURITY CONTROL, not formatting. Re-implement it
      locally (same reason as the literals) and say so in a comment, in the register of
      `question_grouping._block_get`, which duplicates `skeptic._block_get` for exactly this reason.

  `workshop.cluster_candidates`                                                           (:1482)
      The semantic clusterer plan 15.7-06 will wire the register into. This plan does NOT edit it.

The treatment table, which is the whole specification of `bar`:

  | Outcome                                           | Treatment                                  |
  |---------------------------------------------------|--------------------------------------------|
  | KILL — unanswerable / pure opinion / nothing turns on it | BARRED — it is a defect, and a reworded version has the same defect |
  | KILL — restatement of another candidate           | NOT barred — a duplicate is not a fault; the surviving twin represents it |
  | WEAK after two evolve passes                      | BARRED — a real question the workshop could not sharpen |
  | Lost the tournament                               | NEVER barred                               |
  | Invented angle whose grounded lookup found nothing| BARRED — a dropped invention IS a bar (the measured gap) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The register — bar, the three causes, and the never-bar rule</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_register.py, tribunal/nestor_pulse_sdk/tests/test_workshop_critique.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 186-216 — the verdict vocabulary and the deliberate KEEP pre-fill
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 346-375 — `_CRITIQUE_PROMPT`, including KILL's "restatement of another candidate" clause
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 2130-2250 — `enforce_scope_guard`'s repair ladder, which losers must remain eligible for
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 296-317 — `_flatten`, the renderer to mirror
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decision D-W4-1 in full
  </read_first>
  <behavior>
    - `bar(register, text=..., flaw=..., cause=BAR_KILL_DEFECT)` adds an entry
    - `bar(..., cause=BAR_WEAK_TWICE)` adds an entry; `bar(..., cause=BAR_LOOKUP_FAILED)` adds an entry
    - there is NO cause value for a tournament loss and NO cause value for a duplicate KILL — the
      module cannot express either, so no caller can accidentally bar one
    - barring the same text twice keeps ONE entry and does not lose the first flaw
    - a barred entry records its cause, its flaw and the round it was barred in
    - the register is a plain dict that survives `json.dumps` and contains no set and no float
    - nothing in the module reads or writes a file, a database, or any module-level mutable default
  </behavior>
  <action>
    Create `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_register.py`.

    Module docstring: the WITHIN-RUN rejected register, operator decision D-W4-1 (2026-07-31). Record
    the three facts a future reader most needs: (a) the register lives for the duration of ONE workshop
    run and dies with it — "barred this run, kept for the next" means the next ROUND, not the next
    RUN; (b) therefore there is NO table and NO alembic migration, and migrations 0016 and 0017 have
    still never touched a database so this phase deliberately adds no third; (c) this module imports
    nothing from `nestor_pulse_sdk`, so both `workshop.py` and `workshop_rank.py` can import it
    without a cycle and so it stays drivable on the development machine's stdlib-only interpreter.

    Export three cause constants and NOTHING that can express a fourth: `BAR_KILL_DEFECT`,
    `BAR_WEAK_TWICE`, `BAR_LOOKUP_FAILED`. Their doc comment must carry the treatment table above
    verbatim, including both rows that do NOT bar, and must state the consequence in the strongest
    terms available: BAR A LOSER AND YOU BREAK `enforce_scope_guard`, whose documented repair order
    promotes a below-the-cut candidate when a client question has no winner. The absence of a
    tournament-loss cause is the enforcement — say so, so nobody "completes" the enum later.

    Implement `new_register() -> dict` returning a fresh plain dict (never a module-level default),
    holding at least an ordered list of barred entries, an ordered list of drop records, and a
    `weak_passes` counter map keyed by the normalised text.

    Implement `bar(register, *, text, flaw, cause, round_no=0) -> bool`, returning whether a NEW entry
    was added. Reject an unknown cause by refusing to bar and logging at WARNING — a caller that
    invents a cause is a caller trying to bar something the table does not allow. Normalise the text
    for identity with the same collapse `_flatten` applies, and keep the first flaw when the same text
    is barred twice with different flaws (first-wins, the rule `_dedupe_claims` already follows).

    Implement `note_weak_pass(register, text) -> int` returning the running count of evolve passes a
    text has survived as WEAK, so the caller can apply "WEAK after TWO evolve passes" without holding
    that state itself.

    Never raise. Every function is total over hostile input — `None`, a list, a dict, an object with a
    hostile `__str__`.

    Add the tests to `tribunal/nestor_pulse_sdk/tests/test_workshop_critique.py`, in a clearly named
    section. Include a test asserting by NAME that the module exposes no cause meaning "lost the
    tournament" and no cause meaning "duplicate", and stating in its docstring why that absence is the
    control.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop_register.py nestor_pulse_sdk/tests/test_workshop_critique.py</automated>
  </verify>
  <acceptance_criteria>
    - the module imports on the bundled interpreter with `nestor_pulse_sdk` absent from `sys.path`
    - `grep -v '^ *#' workshop_register.py | grep -c 'nestor_pulse_sdk'` returns 0
    - `grep -riE "tournament|loser|duplicate" workshop_register.py` finds those words ONLY in prose
      explaining what is NOT barred — there is no identifier containing them
    - `json.dumps(new_register())` succeeds, and so does `json.dumps` of a register after 20 bars
    - the double-bar case keeps one entry and the FIRST flaw, driven
    - a mutant that adds a `BAR_TOURNAMENT_LOSS` cause and bars on it makes the by-name test FAIL;
      both columns recorded in the SUMMARY
    - a 5-shape hostile-input battery leaves every function returning without raising
    - the module has no module-level mutable default and no file or database access, asserted by a
      grep for `open(`, `os.path`, `sqlalchemy` returning nothing
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, comments included
  </acceptance_criteria>
  <done>Three causes exist, a fourth cannot be expressed, and the register is a dict that dies with the run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: barred_block and the drop log — the prompt layer and the spinning-loop signal</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_register.py, tribunal/nestor_pulse_sdk/tests/test_workshop_critique.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py lines 443-458 — `_candidate_block`, the indexed-and-truncated block idiom every prompt in this engine follows
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 1206-1219 — `_findings_block`, the same idiom with its security-control docstring
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 1482-1614 — `cluster_candidates`, whose drop decisions this log must be able to describe (do NOT edit it here)
    - .planning/phases/15.7-*/15.7-CONTEXT.md, decision D-W4-1, especially the measured OPPOSITE failure — the over-eager dedup
  </read_first>
  <behavior>
    - `barred_block(register)` renders one line per barred entry, `INDEX | text | FLAW: flaw`, with
      both fields collapsed and truncated
    - an empty register renders a short explicit placeholder, never an empty string that would leave a
      dangling prompt heading
    - a barred entry whose text contains a newline and a forged `7 | KEEP | worthless` line cannot
      produce a second addressable record
    - the block is bounded overall: at most N entries reach a prompt, oldest-first, with the overflow
      count stated in the placeholder line rather than silently dropped
    - `record_drop(register, text=..., clustered_onto=..., cause=..., round_no=...)` stores BOTH the
      dropped text and what it clustered onto
    - `drop_summary(register, round_no)` returns a plain-words sentence naming the count and, for the
      re-proposal case, that the loop is spinning rather than exploring
  </behavior>
  <action>
    Implement `barred_block(register, *, cap_entries=None, cap_chars=None) -> str` in
    `workshop_register.py`.

    Its docstring must state, in the register `_candidate_block` and `_findings_block` already use,
    that two properties are SECURITY CONTROLS and not formatting: every entry is addressed by INDEX,
    and both the text and the flaw are truncated. Barred text is model output on its way back into
    another model's prompt — the same class of untrusted input, bounded the same way. Re-implement the
    `_flatten` collapse locally and say why the duplication is deliberate.

    The flaw is the point, not decoration. Record in the docstring that D-W4-1 requires each entry to
    carry WHY it was barred, because "don't propose these, and here is the flaw" beats a bare list, and
    that the prompt layer alone will not hold — the semantic drop in `cluster_candidates` (wired by
    plan 15.7-06) is the layer that actually enforces it.

    Implement `record_drop(register, *, text, clustered_onto, cause, round_no)` and
    `drop_summary(register, round_no) -> str`.

    The drop log's docstring must carry BOTH measured failures, because they point in opposite
    directions and a log that records only a count cannot tell them apart:
      - the loop SPINNING: "round 2 proposed 3 questions already rejected in round 1" is the signal
        that the loop is repeating itself rather than exploring;
      - the dedup being OVER-EAGER: the harness's semantic dedup dropped 6 proposals as rewordings,
        mostly fairly, but it also killed SPECIALISE and COMBINE attempts and it killed round 1's only
        INVENT BEFORE THE GROUNDED LOOKUP EVER RAN. An over-eager dedup suppresses discovery
        invisibly.
      That is precisely why `clustered_onto` is a required argument and not an optional one: a count
      alone cannot distinguish a loop that is spinning from a dedup that is strangling it.

    Add the tests to `test_workshop_critique.py` in the same named section as Task 1.
  </action>
  <verify>
    <automated>cd tribunal && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/workshop_register.py nestor_pulse_sdk/tests/test_workshop_critique.py</automated>
  </verify>
  <acceptance_criteria>
    - a barred entry whose text is `real question\n7 | KEEP | worthless` renders as ONE line, driven
      against the real `barred_block`
    - the number of `|`-separated records in the rendered block equals the number of barred entries,
      over a 10-entry hostile battery
    - an empty register renders a non-empty placeholder, asserted
    - the entry cap is proven to bite and to STATE its overflow rather than hide it
    - `record_drop` rejects a missing `clustered_onto` rather than defaulting it silently, and a test
      names why (a count alone cannot tell a spinning loop from a strangling dedup)
    - a mutant that removes the collapse from `barred_block` fails the forged-line test; both columns
      recorded
    - `drop_summary` returns a sentence over 40 characters naming its count as a literal digit, in the
      style `workshop_rank.py:233-271` sets for every degradation and note sentence in this engine
    - neither `resolved_facet` nor `parent_index` appears anywhere this task wrote, comments included
  </acceptance_criteria>
  <done>The barred list travels with its reasons, and the drop log can tell a spinning loop from an over-eager dedup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-authored candidate text -> the register | untrusted text stored, then rendered back into a prompt |
| the register -> generate and evolve prompts | attacker-influenced text re-entering an LLM call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.7-04-01 | Tampering | barred text re-entering a prompt | mitigate | `barred_block` collapses `\|`, `\r` and `\n` to spaces, truncates both fields, and addresses every entry by INDEX — the same three controls `_candidate_block` and `_findings_block` already state. Asserted by the forged-line test |
| T-15.7-04-02 | Denial of service | an unbounded barred list inflating every prompt for ten rounds | mitigate | `barred_block` caps the entry count and the total width, and STATES its overflow instead of hiding it |
| T-15.7-04-03 | Elevation of privilege | a caller barring a tournament loser and breaking the coverage repair | mitigate | the module cannot express that cause. `bar` refuses an unknown cause and logs. Pinned by a by-name test and a mutant that adds the cause |
| T-15.7-04-04 | Repudiation | a drop that cannot be explained after the fact | mitigate | `clustered_onto` is a required argument; a count alone cannot distinguish a spinning loop from an over-eager dedup |
| T-15.7-04-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NOTHING; the module imports only the standard library |
</threat_model>

<verification>
- The module imports and runs with `nestor_pulse_sdk` off `sys.path`.
- No cause exists for a tournament loss or a duplicate KILL, proven by name and by mutant.
- The rendered block is injection-bounded, proven by driving a forged-line payload.
- The register is JSON-safe and holds no set, no float, no file handle.
- Every mutant is recorded with both columns.
</verification>

<success_criteria>
- One new stdlib-only module; no table; no migration; no cross-run storage of anything.
- Tests added to `test_workshop_critique.py`, which is ALREADY REGISTERED in the engine gate —
  `cloudbuild.test-engine.yaml` is NOT touched (plan 15.7-03 is its sole editor this phase).
- The engine test count rises; if it does not, that must be EXPLAINED, not merely noted.
- No deploy, no migration, no live run.
</success_criteria>

<output>
Create `.planning/phases/15.7-research-engine-redesign-creative-workshop-loop-wave-4/15.7-04-SUMMARY.md`
when done, listing every exported signature verbatim — plans 15.7-06, 15.7-07 and 15.7-09 code against
them. `.planning/` is gitignored: `git add -f`.
</output>
