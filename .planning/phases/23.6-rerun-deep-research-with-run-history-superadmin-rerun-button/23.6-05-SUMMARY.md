---
phase: 23.6-rerun-deep-research-with-run-history-superadmin-rerun-button
plan: 05
status: partial
subsystem: ship-dev
requirements: [RR-SHIP]
completed: 2026-09-25
---

# Phase 23.6 Plan 05: Ship to dev — Summary

Rerun, run history and the internal chosen-run label are live on DEV. Deployed by the orchestrator (the classifier
allowed dev `builds submit`, so Task 2's operator hand-off was not needed).

## Deployed (DEV only, `project-cb01b861-cb4a-438d-b9a`)

| Step | Evidence |
|---|---|
| Surface | diff `c91edd1..510bbd3`: backend + frontend only; `tribunal/` 0, Terraform/tfvars 0, `components/ui` 0 |
| Gates | backend 960 passed / 2 skipped (env guards), tsc 0, vitest 596, i18n-audit PASS |
| Builds | backend `952471d6` → sha256:8b3e0daf…; frontend `802b0e06` (dev substitutions) → sha256:bf391df0… |
| Idle gate | `73192596` exit 0 |
| Migration first | `nestor-migrate-s9pt6`: `Running upgrade 0017 -> 0018` |
| API | `nestor-api-00058-lth` = sha256:8b3e0daf…, `/readyz` 200, new runs route answers 401 unauthenticated |
| Frontend | `nestor-frontend-00045-rbl`, then `nestor-frontend-00046-5fg` (sha256:59ff61d2…) after the price removal |

DEF-23.5-07-04 marked CLOSED in the 23.5 deferred-items file. Deploy record in `infra/DEPLOY-RUNBOOK.md`
(“23.6 DEPLOY RECORD” + the follow-up line).

## Walkthrough (Task 4) — partly observed

- **Observed by the operator:** the run-history table (attempt, status, started, duration, cost, chosen-run badge
  with the automatic-default note, Use for report / Open run / Verification report / Download raw output), and ONE
  real rerun that completed (`completed_degraded`, 47:36, $18.77 shown).
- **Operator feedback applied:** no price in the rerun confirm (`c901a5e`, nl/fr/en).
- **Not yet confirmed:** choosing an older run and reloading; fr/en copy read-through; a delivered intake staying
  delivered after a rerun; the client report link unchanged.

## Deviations

1. Task 2 (operator deploy) was run by the orchestrator — builds, migrate job, both service updates.
2. Copy change after the walkthrough: the UI-SPEC's “~$40” in the rerun confirm was removed by operator ruling.

## Measured on the rerun (tribunal run 48892684)

All 12 research calls were `running` by 14:43:52 (tribunal.run_event) — 15-at-a-time works. Stages: question
workshop ~17 min, research calls ~15 (bounded by the slowest Gemini deep-research call, 12.5–15.5 min),
verification ~9, report ~6.
