---
phase: 23.6-rerun-deep-research-with-run-history-superadmin-rerun-button
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api/research.ts
  - frontend/src/lib/research/runHistory.ts
  - frontend/src/lib/research/runHistory.test.ts
  - frontend/src/lib/research/statusLabel.ts
  - frontend/src/routes/admin.pulse.runs.$runId.index.tsx
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/fr/admin.json
  - frontend/src/locales/en/admin.json
autonomous: true
requirements: [RR-01, RR-02, RR-03]

must_haves:
  truths:
    - "The frontend has typed seam functions for the run-history list and the choose verb, matching plan 02's frozen contract"
    - "Which run shows the chosen-run badge is decided by ONE pure tested function: the explicitly marked run, else the newest completed/completed_degraded run as a display default, else none (UI-SPEC UI-8)"
    - "Whether Rerun is visible/disabled is decided by ONE pure tested function that mirrors backend RESEARCH_IN_FLIGHT (queued, running, needs_report_spec) and treats parked as NOT in flight"
    - "A failed rerun request is classified truthfully: a pre-existing in-flight run -> 'in flight' copy; a NEW in-flight run that appeared, OR a failed refetch of the list -> the existing researchStartFailed copy (spend may have started); otherwise the 'nothing charged' copy"
    - "All 34 intakeDetail.runs.* keys exist in nl, fr and en with the UI-SPEC copy"
    - "statusLabel has exactly one definition, shared by the run page and (plan 04) the history"
  artifacts:
    - path: "frontend/src/lib/research/runHistory.ts"
      provides: "pure run-history rules"
      exports: ["RESEARCH_IN_FLIGHT_STATUSES", "sortRunsNewestFirst", "mergeLiveRun", "resolveChosenRun", "canChooseRun", "canDownloadRunZip", "rerunState", "classifyRerunFailure", "runStatusBadge"]
    - path: "frontend/src/lib/research/runHistory.test.ts"
      provides: "vitest coverage of every rule"
    - path: "frontend/src/lib/research/statusLabel.ts"
      provides: "the single statusLabel definition"
      exports: ["statusLabel"]
    - path: "frontend/src/lib/api/research.ts"
      provides: "getResearchRuns, chooseResearchRun, ResearchRunHistoryItem, ResearchRunHistory"
  key_links:
    - from: "frontend/src/routes/admin.pulse.runs.$runId.index.tsx"
      to: "frontend/src/lib/research/statusLabel.ts"
      via: "import (private copy deleted)"
      pattern: "from \"@/lib/research/statusLabel\""
    - from: "frontend/src/lib/api/research.ts"
      to: "GET /intakes/{id}/research/runs"
      via: "apiFetch"
      pattern: "research/runs"
---

<objective>
The frontend data layer and pure rules for the run history, Rerun and chosen-run label, plus the
copy. No component and no page change here (plan 04), so every rule is unit-testable in vitest's
node environment (it includes only `src/**/*.test.ts` — logic inside `.tsx` cannot be tested).

Purpose: the decisions the operator relies on (which run is "chosen", when Rerun is allowed, what a
failure toast claims about money) live in one tested module instead of inside a 2,000-line route.
Output: two API functions + types, `runHistory.ts` + tests, `statusLabel.ts` (moved), 34 keys x 3 locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-CONTEXT.md
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-UI-SPEC.md
@CLAUDE.md
@frontend/src/lib/api/research.ts
@frontend/src/lib/research/triggerOutcome.ts
</context>

<interfaces>
<!-- Backend contract, frozen in 23.6-02-PLAN.md (backend lands in wave 2; code against this shape). -->
GET /intakes/{intake_id}/research/runs -> { chosen_research_run_id: string | null, runs: ResearchRunHistoryItem[] }
ResearchRunHistoryItem = { id: string; attempt: number; status: string; created_at: string; started_at: string | null; completed_at: string | null; cost_usd_total: string | null; chain_status: string | null; chosen_at: string | null }
POST /intakes/{intake_id}/research/runs/{run_id}/choose -> { chosen_research_run_id: string; previous_research_run_id: string | null }

From frontend/src/lib/api/research.ts (HEAD):
- `export type ResearchRun = { id; status; current_stage; stage_detail; cost_usd_total: string | null; started_at; completed_at; error_message; chain_status; chain_broken_at; bundle_key; event_seq }` — the SSE frame.
- `export function triggerResearch(intakeId): Promise<ApiResult<TriggerResearchResponse>>` — pattern to mirror: one-shot `apiFetch<T>(path, { method })`, JSDoc, never throws.
- `apiFetch` / `ApiResult` from `@/lib/api/client` — failure variant `{ success: false; error: string; code?: string }` (no HTTP status field).
From frontend/src/lib/research/verificationGate.ts: `canHaveVerificationReport(status: string): boolean`.
From frontend/src/routes/admin.pulse.runs.$runId.index.tsx line ~428: private `function statusLabel(status: string, t: (key: string) => string): string` (switch over queued, running, completed, completed_degraded, failed, cancelled, parked, needs_input, default unknown; literal `t("research.runPage.status.*")` calls). Used at ~line 290 and ~line 370.
Backend mirror: `RESEARCH_IN_FLIGHT = {"queued","running","needs_report_spec"}`, `RESEARCH_SUCCESS = {"completed","completed_degraded"}` (backend/app/research/run_status.py).
Locale files: `frontend/src/locales/{nl,fr,en}/admin.json`, top-level `intakeDetail` object already exists (keys: unknownClient, status, ..., toast, archiveDialog, delete). Add a sibling `runs` object inside `intakeDetail`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runHistory.ts pure rules + API seam functions (TDD)</name>
  <files>frontend/src/lib/research/runHistory.ts, frontend/src/lib/research/runHistory.test.ts, frontend/src/lib/api/research.ts</files>
  <read_first>
    - frontend/src/lib/research/triggerOutcome.ts and triggerOutcome.test.ts (pure-module convention, test style)
    - frontend/src/lib/research/verificationGate.ts
    - frontend/src/lib/api/research.ts lines 272-440
    - frontend/vitest.config.ts
    - .planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-UI-SPEC.md sections "Status badge mapping", "Row actions", "Rerun button", "Display default vs stored choice", "Copy-truth notes"
  </read_first>
  <behavior>
    - sortRunsNewestFirst: attempts [1,3,2] -> [3,2,1]; equal attempt -> later created_at first; does not mutate input
    - mergeLiveRun(runs, live): overlays live.status/cost_usd_total/started_at/completed_at/chain_status onto the row with live.id; unknown live id or null live -> runs unchanged
    - resolveChosenRun(runs, chosenId): chosenId present in runs -> {runId: chosenId, source: "marked"}; chosenId null -> newest (sorted) run with status completed|completed_degraded, source "default"; none finished -> {runId: null, source: null}; chosenId not in runs -> falls back to the default rule; a newer failed run does NOT take the default
    - canChooseRun(run, chosen, intakeStatus): true only for completed|completed_degraded, not the MARKED run, intake not archived; true for the default-badged run (so it can be made explicit); false for parked/failed/running
    - canDownloadRunZip(run): completed|completed_degraded AND chain_status === "verified"; broken/null -> false
    - rerunState({isSuperadmin, intakeStatus, runs, loadState: "loading" | "ready" | "error"}) -> {visible, disabled, reason: "in_flight" | null, parkedNote: boolean}: not superadmin, or status not in {in_research, delivered} (so archived and decomposed) -> visible false; loadState "loading" or "error" -> visible true, disabled true, reason null (UI-SPEC: loading/error explain themselves); "ready" with 0 runs -> visible false (empty state has no Rerun); "ready" with any run in queued|running|needs_report_spec -> visible, disabled, reason "in_flight"; "ready" with newest run parked and nothing in flight -> visible, enabled, parkedNote true; otherwise visible, enabled, parkedNote false
    - classifyRerunFailure(beforeIds, afterRuns: ResearchRunHistoryItem[] | null): afterRuns null (the post-failure refetch itself failed) -> "maybe_started" (nothing proves no charge, so never the "nothing charged" copy); an in-flight run whose id WAS in beforeIds -> "in_flight"; an in-flight run whose id was NOT in beforeIds -> "maybe_started"; none in flight -> "failed"
    - runStatusBadge(status): returns {className, mark: "green" | "ink" | null, spinner: boolean, labelStatus} per the UI-SPEC table; needs_report_spec -> labelStatus "running" with spinner; unknown -> muted outline class, labelStatus as given (statusLabel maps it to unknown)
  </behavior>
  <action>
    RED: write frontend/src/lib/research/runHistory.test.ts covering every behaviour line above (plain objects typed as ResearchRunHistoryItem), run vitest, see it fail, commit.

    GREEN:
    - frontend/src/lib/api/research.ts: add `export type ResearchRunHistoryItem` and `export type ResearchRunHistory` exactly per the interfaces block; add `export function getResearchRuns(intakeId: string): Promise<ApiResult<ResearchRunHistory>>` (GET `/intakes/${intakeId}/research/runs`) and `export function chooseResearchRun(intakeId: string, runId: string): Promise<ApiResult<{ chosen_research_run_id: string; previous_research_run_id: string | null }>>` (POST `/intakes/${intakeId}/research/runs/${runId}/choose`), each with a JSDoc in the style of `getBundleUrl` stating superadmin-only, existence-hidden 404, never throws. The choose JSDoc says: internal label only (D-23.6-02 revised) — no client effect.
    - frontend/src/lib/research/runHistory.ts: pure module (no React, no i18n, no network; header comment says why, like triggerOutcome.ts). Export `RESEARCH_IN_FLIGHT_STATUSES = new Set(["queued","running","needs_report_spec"])` and `RESEARCH_SUCCESS_STATUSES = new Set(["completed","completed_degraded"])` with a comment naming backend/app/research/run_status.py as the source of truth and that parked is deliberately NOT in flight (UI-6); and the functions named in the behaviour block with those exact names. Badge class strings are the literal classes from the UI-SPEC table (`badge-outline`, `badge-ink`, `badge-dashed`, `badge-outline text-red-700 border-red-700`, `badge-outline text-ink/40 border-ink/40`) so Tailwind's scanner sees them.
    - Do NOT add RESEARCH_IN_FLIGHT to frontend/src/lib/i18n/error-codes.ts and do NOT change the backend: the 409 carries no machine code (the test apps do not register the CodedError handler), which is why classifyRerunFailure decides from a refetched list, not from an error code. Record this as a deviation from the UI-SPEC's "in-flight code" wording in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/research/runHistory.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - runHistory.test.ts has a refetch-failed case (`grep -c 'refetch' frontend/src/lib/research/runHistory.test.ts` >= 1) asserting `classifyRerunFailure(beforeIds, null)` is "maybe_started".
    - vitest reports runHistory.test.ts passed with at least 20 tests and 0 failed; `npx tsc --noEmit` exits 0.
    - `grep -c 'export function getResearchRuns\|export function chooseResearchRun' frontend/src/lib/api/research.ts` returns 2.
    - `grep -c 'from "react"\|useTranslation\|apiFetch' frontend/src/lib/research/runHistory.ts` returns 0 (pure module).
    - `git diff --name-only -- frontend/src/components/ui` prints nothing.
  </acceptance_criteria>
  <done>Every run-history rule is a named, exported, tested pure function; both seam functions exist with the frozen contract types.</done>
</task>

<task type="auto">
  <name>Task 2: Move statusLabel to lib/research + add the 34 intakeDetail.runs.* keys in nl/fr/en</name>
  <files>frontend/src/lib/research/statusLabel.ts, frontend/src/routes/admin.pulse.runs.$runId.index.tsx, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <read_first>
    - frontend/src/routes/admin.pulse.runs.$runId.index.tsx (statusLabel at ~428 and its two call sites)
    - frontend/scripts/i18n-audit.mjs (CHECK A parity, CHECK B literal keys, CHECK C no two-arg fallbacks)
    - frontend/src/locales/nl/admin.json, fr/admin.json, en/admin.json (the intakeDetail object)
    - .planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-UI-SPEC.md "Full key table (nl primary, fr, en)"
  </read_first>
  <action>
    - Create frontend/src/lib/research/statusLabel.ts exporting `statusLabel(status: string, t: (key: string) => string): string` — the function body moved VERBATIM from the run page (same literal `t("research.runPage.status.*")` calls, same default arm) with a header comment: one definition shared by the run page and the intake-page history (UI-SPEC), do not write a second copy. Delete the private copy from admin.pulse.runs.$runId.index.tsx and import it with `import { statusLabel } from "@/lib/research/statusLabel";`. No behaviour change on the run page.
    - In each of the three admin.json files add `intakeDetail.runs` with exactly the 34 leaf keys from the UI-SPEC key table (title; col.{attempt,status,started,duration,cost,actions}; startedMobile; rerun; rerunStarting; rerunBlockedInFlight; rerunConfirmTitle; rerunConfirmBody; rerunConfirmParkedNote; rerunConfirmCancel; rerunConfirmConfirm; chosenBadge; chosenBadgeMarked; chosenBadgeDefault; noChosenRun; choose; chooseConfirmTitle; chooseConfirmBody; chooseConfirmCancel; chooseConfirmConfirm; zipOnRunPage; empty; loadFailed; retry; toast.{rerunStarted,rerunInFlight,rerunFailed,chooseDone,chooseFailed}) and the exact nl / fr / en strings from that table, copied character for character (keep `{{date}}` and `{{number}}` placeholders, the "~$40" / "~40 $" figures, and the typographic apostrophes/ellipsis as written). Preserve each file's existing indentation and key order elsewhere; insert `runs` after `toast` inside `intakeDetail`.
  </action>
  <verify>
    <automated>cd frontend && node -e "for (const l of ['nl','fr','en']) { const r=require('./src/locales/'+l+'/admin.json').intakeDetail.runs; const f=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'?f(v,p+k+'.'):[p+k]); const n=f(r).length; if(n!==34) { console.error(l, n); process.exit(1) } } console.log('34 x 3 ok')" && node scripts/i18n-audit.mjs && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The node key-count check prints `34 x 3 ok`; `node scripts/i18n-audit.mjs` exits 0 (CHECK A/B/C); `npx tsc --noEmit` exits 0.
    - `grep -c '^function statusLabel' 'frontend/src/routes/admin.pulse.runs.$runId.index.tsx'` returns 0 and `grep -c 'from "@/lib/research/statusLabel"' 'frontend/src/routes/admin.pulse.runs.$runId.index.tsx'` returns 1.
    - `grep -rn 'function statusLabel' frontend/src --include=*.ts --include=*.tsx | grep -c 'lib/research/statusLabel.ts'` returns 1, and the run page no longer defines one (the ResearchRunProgress.tsx symbol, if it is a different function, is left alone).
    - `node -e "console.log(require('./frontend/src/locales/nl/admin.json').intakeDetail.runs.rerun)"` prints `Nieuwe run starten`; the same for en prints `Start new run`; for fr prints `Lancer une nouvelle exécution`.
    - `npx vitest run` (whole frontend suite, from frontend/) reports 0 failed.
  </acceptance_criteria>
  <done>One statusLabel definition in lib/research; the run page renders identically; 34 keys in each locale with the approved copy; i18n audit green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nestor-api JSON -> browser | run-history payload rendered in the operator UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.6-13 | Tampering (UI truth) | rerunState / resolveChosenRun | mitigate | backend remains the authority (409 in flight, 409 non-success choose); the pure rules only decide presentation and are unit-tested against the backend literal sets |
| T-23.6-14 | Repudiation (copy truth) | classifyRerunFailure | mitigate | "nothing charged" copy only when no NEW in-flight run appeared after the failed request; a new in-flight run routes to the existing researchStartFailed copy (UI-SPEC copy-truth note) |
| T-23.6-15 | Information Disclosure | locale copy | accept | copy is operator-only admin namespace; no client route consumes it |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npx vitest run && node scripts/i18n-audit.mjs` all green.
- `git diff --name-only c91edd1 -- frontend/src/components/ui frontend/src/routes/intake.* frontend/src/routes/results.*` prints nothing.
</verification>

<success_criteria>
- The data layer and rules plan 04 renders from exist, typed and tested.
- The approved copy is present in three languages.
</success_criteria>

<output>
Create `.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-03-SUMMARY.md` when done (`git add -f`).
</output>
