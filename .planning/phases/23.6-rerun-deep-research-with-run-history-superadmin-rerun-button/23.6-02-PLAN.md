---
phase: 23.6-rerun-deep-research-with-run-history-superadmin-rerun-button
plan: 02
type: tdd
wave: 2
depends_on: ["23.6-01"]
files_modified:
  - backend/app/api/research_routes.py
  - backend/tests/test_research_run_history.py
  - backend/tests/test_superadmin_gate.py
autonomous: true
requirements: [RR-02, RR-03]

must_haves:
  truths:
    - "A superadmin can list every research run of an intake, newest first by attempt, with id, attempt, status, created/started/completed time, cost, chain status and chosen_at (D-23.6-04)"
    - "The list response names the explicitly chosen run id, or null when none is marked — the newest-finished default is NOT computed or stored server-side (UI-SPEC UI-8)"
    - "A superadmin can mark one completed or completed_degraded run as the chosen run; the previous mark moves in the same transaction and an audit row records it (D-23.6-02 revised)"
    - "Choosing a run that is not finished-successfully, or on an archived intake, is 409 and writes nothing; choosing a run of another intake is 404"
    - "A non-superadmin (including a null-space user) and a cross-space caller get the existence-hidden 404 on both new routes"
    - "Nothing client-facing is touched: no mail, no report artifact, no intake column, no client route (D-23.6-02 revised)"
  artifacts:
    - path: "backend/app/api/research_routes.py"
      provides: "GET /intakes/{intake_id}/research/runs and POST /intakes/{intake_id}/research/runs/{run_id}/choose"
      contains: "def list_research_runs"
    - path: "backend/tests/test_research_run_history.py"
      provides: "behaviour + denial + cross-tenant suite for both routes"
    - path: "backend/tests/test_superadmin_gate.py"
      provides: "research_router route count raised 11 -> 13"
      contains: "== 13"
  key_links:
    - from: "backend/app/api/research_routes.py::choose_research_run"
      to: "backend/app/db/repository.py::ResearchRunRepository.set_chosen"
      via: "one request-scoped transaction with the audit row"
      pattern: "set_chosen\\("
    - from: "backend/app/api/research_routes.py::choose_research_run"
      to: "backend/app/research/run_status.py::is_research_success"
      via: "only a success run may be chosen"
      pattern: "is_research_success\\("
---

<objective>
The two superadmin HTTP verbs the intake-page run history needs (D-23.6-04, D-23.6-02 revised):

- `GET /intakes/{intake_id}/research/runs` — every run of the intake plus the explicitly chosen run id.
- `POST /intakes/{intake_id}/research/runs/{run_id}/choose` — mark one finished run as the internal chosen run.

Purpose: the history list and the chosen-run badge (plan 04) read and write through these. Per-run
report, verification and zip already have routes (`/{intake_id}/research/{run_id}/verification`,
`/bundle-url`, run page `admin.pulse.runs.$runId.*`) — nothing is added for those.
Output: two routes on the existing `research_router`, a new test file, the gate-count pin raised.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-CONTEXT.md
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-UI-SPEC.md
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-01-SUMMARY.md
@CLAUDE.md
@backend/app/api/research_routes.py
</context>

<interfaces>
<!-- THE CONTRACT. Plan 03 (frontend, wave 1) codes against exactly this shape — do not rename a field. -->

GET /intakes/{intake_id}/research/runs  -> 200
{
  "chosen_research_run_id": "<uuid>" | null,      // the run whose chosen_at is non-null, else null
  "runs": [                                        // attempt DESC, then created_at DESC
    {
      "id": "<uuid>",
      "attempt": 3,
      "status": "running",                         // engine literal, verbatim (never remapped)
      "created_at": "2026-09-25T14:02:11.123+00:00",
      "started_at": "..." | null,
      "completed_at": "..." | null,
      "cost_usd_total": "41.87" | null,            // str(Decimal); null when unknown, never "0"
      "chain_status": "verified" | "broken" | null,
      "chosen_at": "..." | null
    }
  ]
}
Empty `runs` + null chosen id for an in-scope intake with no runs. 404 "Intake not found" for an out-of-scope/missing intake.

POST /intakes/{intake_id}/research/runs/{run_id}/choose  (no body) -> 200
{ "chosen_research_run_id": "<run_id>", "previous_research_run_id": "<uuid>" | null }
- 404 "Run not found": run missing, out of scope, or run.intake_id != intake_id
- 404 "Intake not found": intake missing/out of scope
- 409 "Only a finished run can be chosen": run.status not in RESEARCH_SUCCESS
- 409 "An archived intake is read-only": intake.status == "archived"
- Re-choosing the already-chosen run: 200 with previous_research_run_id == run_id, no audit row, no write.

Existing code to reuse (HEAD after plan 01):
- `from app.auth.gates import superadmin_gate as _superadmin_gate`; signature ORDER rule: `identity: Identity = Depends(_superadmin_gate)` BEFORE `repo: IntakeRepository = Depends(get_tenant_repo)`; in-body defence-in-depth `if identity.role != "superadmin": raise 404` (see locate_research_run / get_bundle_url).
- `ResearchRunRepository(repo.session, identity).list_for_intake(intake_id)`, `.get(run_id)`, `.set_chosen(intake_id, run_id) -> previous id | None` (plan 01).
- `audit.log(session, actor_uid=, event_type=, target=, space_id=, metadata=)`.
- `is_research_success(status)` already imported in research_routes.py.
- The request-scoped `repo.session` commits at request end (the pattern 23.5-01's status override uses for "flip + audit in one tx on repo.session") — read `get_tenant_repo` in backend/app/db/session.py to confirm before relying on it.
- Route declaration order: `locate_research_run` must stay FIRST (tests/test_research_events_proxy.py pins it). Declare the two new routes AFTER trigger_research and BEFORE resume_research.
- Gate pins: tests/test_superadmin_gate.py asserts `checked == 11` (~line 226) and `total == 11` (~line 265) for research_router.
</interfaces>

<feature>
  <name>Run-history list + chosen-run verb</name>
  <files>backend/app/api/research_routes.py, backend/tests/test_research_run_history.py, backend/tests/test_superadmin_gate.py</files>
  <behavior>
    List:
    - intake with runs attempt 1 failed, 2 completed, 3 running -> runs ordered [3, 2, 1]; each item has exactly the 9 contract keys; cost "41.87" serialised as string; null cost stays null
    - intake with no runs -> {"chosen_research_run_id": null, "runs": []}
    - after set_chosen on attempt 2 -> chosen_research_run_id == that id and that item's chosen_at is not null, others null
    - role=user in the intake's own space -> 404; null-space user -> 404; superadmin on an intake id that does not exist -> 404
    Choose:
    - completed run -> 200, previous null, DB chosen_at set, one audit_log row event_type "research.run_chosen" with metadata {"research_run_id", "previous_research_run_id", "attempt"}
    - then completed_degraded run B -> 200, previous == A, A chosen_at null
    - re-choose B -> 200, previous == B, audit row count unchanged
    - running / failed / parked / cancelled / queued run -> 409, nothing written
    - intake archived with a completed run -> 409, nothing written
    - run id belonging to a DIFFERENT intake of the same space -> 404, nothing written
    - role=user / null-space user -> 404, nothing written
    - the intake row's status, final_report_artifact_id and results_link_sent_at are unchanged after a choose; fake_resend has zero calls
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — test_research_run_history.py for both routes</name>
  <files>backend/tests/test_research_run_history.py</files>
  <read_first>
    - backend/tests/test_research_routes.py (preamble, helpers, superadmin_engine fixture, _build_app, _patch_engines, _patch_superadmin_engine — copy what you need; each test file owns its helpers in this repo)
    - backend/tests/test_research_cross_tenant.py (two_spaces fixture, _user, null-space denial shape — e.g. test_bundle_url_user_role_404 / test_bundle_url_null_space_404)
    - backend/tests/conftest.py (engine, set_space, fake_resend fixtures)
    - backend/app/db/models/audit.py (audit_log column names for the audit assertions)
  </read_first>
  <behavior>All cases listed in the feature block, one test function each (parametrize the five non-choosable statuses).</behavior>
  <action>
    Create backend/tests/test_research_run_history.py, `pytestmark = pytest.mark.integration`, with the same importorskip preamble as test_research_routes.py. Seed runs with explicit attempt, status, cost_usd_total, chain_status and created_at through a local helper using raw SQL under set_space (mirror `_seed_research_run`). Mark a run chosen in the "list shows chosen" case by UPDATE-ing chosen_at directly in SQL (do not depend on the POST in a list test). Assert DB state by raw SQL reads, not by re-calling the API. The module docstring states the contract (copy the interfaces block) and says the newest-finished default is display-only and intentionally absent from the backend. Run it: every test must FAIL (404/405 because the routes do not exist). Commit the RED test.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_research_run_history.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - The run reports failures for every test and 0 skipped (a skip means Docker is down — start it; skipped RED is not RED).
    - `grep -c '^def test_' backend/tests/test_research_run_history.py` returns at least 12.
    - `grep -c 'research.run_chosen' backend/tests/test_research_run_history.py` returns at least 1.
  </acceptance_criteria>
  <done>A failing, committed test file that pins the contract, the ordering, the chosen-run move, every refusal, the denial arms and the no-client-effect assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — list_research_runs + choose_research_run on research_router; gate count 11 -> 13</name>
  <files>backend/app/api/research_routes.py, backend/tests/test_superadmin_gate.py</files>
  <read_first>
    - backend/app/api/research_routes.py (locate_research_run for the double-gate shape; get_bundle_url for a run-under-intake resolve; trigger_research for audit usage)
    - backend/app/db/session.py (get_tenant_repo commit semantics)
    - backend/app/db/repository.py (ResearchRunRepository incl. set_chosen from plan 01)
    - backend/tests/test_superadmin_gate.py lines 150-270
    - backend/tests/test_research_events_proxy.py lines 870-892 (locate-first ordering pin)
  </read_first>
  <action>
    In backend/app/api/research_routes.py, after trigger_research and before resume_research:
    - `@research_router.get("/{intake_id}/research/runs")` `def list_research_runs(intake_id: str, identity: Identity = Depends(_superadmin_gate), repo: IntakeRepository = Depends(get_tenant_repo)) -> dict`: in-body role re-check -> 404; `repo.get(intake_id)` None -> 404 "Intake not found"; list via ResearchRunRepository(repo.session, identity).list_for_intake, sort by (attempt, created_at) descending in Python; project each row through a small private `_run_history_item(run) -> dict` returning exactly the 9 contract keys (datetimes via `.isoformat()` or None, cost via `str(...)` or None); chosen id = the first item with chosen_at not None, else None. Sync def (not async). Docstring: D-23.6-04, superadmin-only, space-scoped existence-hidden 404, the default-chosen rule is display-only (UI-SPEC UI-8), and that it is a HISTORY read, not a second source of live status (the page's one SSE stream stays the live authority, D-05 of locate).
    - `@research_router.post("/{intake_id}/research/runs/{run_id}/choose")` `def choose_research_run(intake_id: str, run_id: str, identity: Identity = Depends(_superadmin_gate), repo: IntakeRepository = Depends(get_tenant_repo)) -> dict`: role re-check -> 404; intake None -> 404; run = ResearchRunRepository(...).get(run_id); None or `str(run.intake_id) != str(intake_id)` -> 404 "Run not found"; `intake.status == "archived"` -> 409 "An archived intake is read-only"; `not is_research_success(run.status)` -> 409 "Only a finished run can be chosen"; `previous = set_chosen(intake_id, run_id)`; if previous equals run.id return without audit; else `audit.log(repo.session, actor_uid=identity.uid, event_type="research.run_chosen", target=str(intake_id), space_id=intake.space_id, metadata={"research_run_id": str(run.id), "previous_research_run_id": str(previous) if previous else None, "attempt": run.attempt})`; call set_chosen inside `with repo.session.begin_nested():` (a SAVEPOINT, so a unique-index refusal also undoes the clear step and nothing half-written can commit with the request tx) and wrap that block in `except IntegrityError` -> 409 "The chosen run changed while this request was being prepared" (belt for the unique index). Return the contract body. Docstring: D-23.6-02 revised — INTERNAL label only; it sends no mail, writes no artifact, touches no intake column and has no client-facing consumer; the planner-level rule is that a future change wiring a client effect here needs a new operator ruling.
    - Update the module docstring's route inventory (it currently says ELEVEN routes) to THIRTEEN and add one bullet per new route.
    In backend/tests/test_superadmin_gate.py raise both pins 11 -> 13 (the `checked == 11` walk and `total == 11`), and append to the adjacent history comment "raised to 13 by plan 23.6-02 (list_research_runs, choose_research_run — both gated from birth)". Do not relax either to `>=`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_research_run_history.py tests/test_superadmin_gate.py tests/test_research_events_proxy.py tests/test_research_cross_tenant.py tests/test_client_surface_open.py tests/test_no_run_research_route.py tests/test_scope_guard_run_research.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - The pytest command above reports 0 failed AND 0 skipped.
    - `grep -c '== 13' backend/tests/test_superadmin_gate.py` returns 2 and `grep -c '== 11' backend/tests/test_superadmin_gate.py` returns 0.
    - `grep -c '@research_router.get("/{intake_id}/research/runs")' backend/app/api/research_routes.py` returns 1 and `grep -c '@research_router.post("/{intake_id}/research/runs/{run_id}/choose")' backend/app/api/research_routes.py` returns 1.
    - `grep -n 'def choose_research_run' -A60 backend/app/api/research_routes.py | grep -c 'send_\|resend\|final_report_artifact_id'` returns 0 (no mail or artifact path in the handler body).
    - `bash backend/scripts/ci_no_raw_db_access.sh` exits 0 (D-03 guard: the route module still imports no raw DB symbol).
    - Full backend suite `cd backend && python -m pytest -q` reports 0 failed.
  </acceptance_criteria>
  <done>Both routes live on research_router, gated first, space-scoped, with the exact contract; all refusal arms write nothing; the router is pinned at 13 gated routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> nestor-api GET /research/runs | operator read exposing cost and run ids |
| browser -> nestor-api POST /research/runs/{run_id}/choose | operator write of an internal label |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.6-07 | Information Disclosure | list_research_runs | mitigate | superadmin_gate declared before get_tenant_repo (existence-hidden 404 for user and null-space), in-body role re-check, space-scoped intake resolve; denial tests for user, null-space and cross-space |
| T-23.6-08 | Tampering | choose_research_run with a foreign run id | mitigate | run resolved through the scoped ResearchRunRepository AND `run.intake_id == intake_id` check -> 404; test with a run from a sibling intake |
| T-23.6-09 | Tampering | choosing a non-deliverable run | mitigate | `is_research_success` gate -> 409; parametrized test over five statuses |
| T-23.6-10 | Tampering | concurrent chooses | mitigate | FOR UPDATE lock in set_chosen + partial unique index + IntegrityError -> 409 |
| T-23.6-11 | Repudiation | chosen-run changes | mitigate | same-tx audit row `research.run_chosen` with run, previous run and attempt |
| T-23.6-12 | Information Disclosure | client surfaces | mitigate | no client route, mail or artifact touched; test asserts intake columns unchanged and zero mail calls |
</threat_model>

<verification>
- research_router has exactly 13 routes, all resolving superadmin_gate (test_superadmin_gate.py).
- locate route still declared first (test_research_events_proxy.py).
- Full backend suite green with Docker up.
</verification>

<success_criteria>
- RR-02: the history list endpoint exists with the frozen contract.
- RR-03: the chosen-run verb exists, internal only, audited, one-per-intake.
</success_criteria>

<output>
Create `.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-02-SUMMARY.md` when done (`git add -f`).
</output>
