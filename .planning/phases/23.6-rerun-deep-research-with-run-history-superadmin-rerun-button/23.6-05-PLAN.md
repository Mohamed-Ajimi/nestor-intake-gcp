---
phase: 23.6-rerun-deep-research-with-run-history-superadmin-rerun-button
plan: 05
type: execute
wave: 3
depends_on: ["23.6-02", "23.6-04"]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - .planning/STATE.md
autonomous: false
requirements: [RR-SHIP]

must_haves:
  truths:
    - "Dev (project-cb01b861-cb4a-438d-b9a) runs this phase's backend and frontend; the intake alembic head on dev is 0018"
    - "The migration ran BEFORE the new nestor-api revision took traffic (the new ORM selects chosen_at)"
    - "tribunal-api and tribunal-worker are untouched (no tribunal/ file changed — derived by diff, so no idle gate and no worker deploy)"
    - "Prod (nestor-pulse-prod) and infra/env/client.tfvars are untouched"
    - "A deploy record names the revisions, digests read off the REVISION, the migration evidence and what was and was not seen in a browser"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "23.6 DEV DEPLOY RECORD section"
      contains: "23.6 DEPLOY RECORD"
  key_links:
    - from: "nestor-migrate job"
      to: "backend image built at the phase HEAD"
      via: "jobs update --image before jobs execute --wait"
      pattern: "Running upgrade 0017 -> 0018"
---

<objective>
Ship phase 23.6 to DEV ONLY and record it. Backend (with migration 0018) and frontend; nothing
under tribunal/ changes, so neither tribunal service is deployed. No prod promotion, no
client.tfvars change, no terraform on dev.

Purpose: the operator can use Rerun, the run history and the chosen-run label on dev.
Output: dev running the phase HEAD, a deploy record, STATE.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-CONTEXT.md
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-02-SUMMARY.md
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-04-SUMMARY.md
@CLAUDE.md
@infra/DEPLOY-RUNBOOK.md
</context>

<interfaces>
Dev facts (runbook Step 23.5.a/23.5.b, promote.sh):
- DEV=project-cb01b861-cb4a-438d-b9a, REGION=europe-west1, REG=europe-west1-docker.pkg.dev/$DEV/nestor, account tools@dotto.be — pin `--account` AND `--project` on EVERY gcloud call (four accounts on this machine; the config reverts mid-session).
- Images: `$REG/backend:$TAG` (nestor-api + nestor-migrate job), `$REG/frontend:$TAG` (nestor-frontend). Frontend is built with `frontend/cloudbuild.yaml` and substitutions `_IMAGE,_API_BASE_URL,_FB_API_KEY,_FB_AUTH_DOMAIN,_FB_PROJECT_ID` (a plain `--tag` build cannot inject the VITE_* build args).
- FB_API_KEY is recovered from a prior SUCCESS dev build (`gcloud builds list ... --format='value(id,createTime,substitutions._FB_PROJECT_ID)'` then `builds describe <FULL-UUID> --format='value(substitutions._FB_API_KEY)'`, assert non-empty); never typed, never written to a file.
- Cloud Run JOBS do not follow their service's image: `gcloud run jobs update nestor-migrate --image=<digest>` BEFORE `gcloud run jobs execute nestor-migrate --wait`.
- The service's `status.imageDigest` is EMPTY; read the digest off the latest ready REVISION (`gcloud run revisions describe <rev> --format='value(status.imageDigest)'`).
- `/readyz` is `SELECT 1` and passes on an unmigrated DB — it is a liveness smoke, NOT migration proof; the migration proof is the job execution's log line.
- `gcloud builds submit` and scripts that call it are blocked by the agent permission classifier; the operator runs them via `!`, detached with `nohup ... > "$HOME/<name>.log" 2>&1 &` (their terminal times out at 2 min); confirm a build with `gcloud builds describe <FULL UUID>`, never via a pipe's exit code; never resubmit a build that is still running.
- Dev is NOT Terraform-managed: never `terraform apply` with env/dev.tfvars.
- Worktrees under `.claude/worktrees` inflate the source upload (runbook warning) — prune merged ones before a root-context build.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-ship gates on the merged tree + derived deploy surface + command sheet</name>
  <files>(none modified; writes the command sheet to the session scratchpad only)</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md Step 23.5.a and 23.5.b (lines ~7401-7506)
    - infra/promote.sh lines 280-400 (migrate repin, api update, frontend build substitutions, revision digest read-back)
    - .planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-0{1,2,3,4}-SUMMARY.md
  </read_first>
  <action>
    1. Confirm Docker Desktop is up (`docker info` exits 0). Run the suites separately with timeouts, on the merged master tree: `cd backend && python -m pytest -q -rs` (Python 3.12; 0 failed, and the integration files of this phase report passed not skipped); `cd frontend && npm ci && npx tsc --noEmit && npx vitest run && node scripts/i18n-audit.mjs && npm run build` (never `npm install`; restore `frontend/src/routeTree.gen.ts` if the build only rewrote its line endings).
    2. Derive the surface by diff, not by assumption: `git diff --name-only c91edd1..HEAD -- tribunal/` must be empty (then tribunal-api and tribunal-worker are NOT deployed and the idle gate is not needed); `git diff --name-only c91edd1..HEAD -- backend/` non-empty (nestor-api + migrate job); `git diff --name-only c91edd1..HEAD -- frontend/` non-empty (nestor-frontend); `git diff --name-only c91edd1..HEAD -- infra/env/client.tfvars infra/main.tf infra/variables.tf` must be empty. If tribunal/ is NOT empty, STOP and report — the plan does not cover a worker deploy.
    3. Run the CLAUDE.md guards explicitly: `cd backend && python -m pytest tests/test_no_run_research_route.py tests/test_scope_guard_run_research.py -q` green; `grep -c 'research_router' backend/app/main.py` unchanged from c91edd1.
    4. `git push origin master` so the pushed HEAD is the one being built; record `TAG=$(git rev-parse --short HEAD)`.
    5. Read (read-only) the current dev state for the record and for rollback: latest ready revisions + their REVISION digests for nestor-api and nestor-frontend, the nestor-api URL (`status.url`), and the nestor-migrate job's current image. Write a command sheet to the scratchpad containing, in this ORDER, the exact commands for the operator with DEV/REG/TAG filled in and `--account=tools@dotto.be --project=$DEV` on each: (a) backend build `gcloud builds submit backend --tag=$REG/backend:$TAG` under nohup; (b) resolve the backend digest; (c) `gcloud run jobs update nestor-migrate --image=$REG/backend@<digest>` then `gcloud run jobs execute nestor-migrate --wait`; (d) `gcloud run services update nestor-api --image=$REG/backend@<digest> --region=europe-west1`; (e) FB key recovery + non-empty assert, then the frontend build via `frontend/cloudbuild.yaml` with `_IMAGE=$REG/frontend:$TAG,_API_BASE_URL=<dev nestor-api url>,_FB_API_KEY=$FB_API_KEY,_FB_AUTH_DOMAIN=$DEV.firebaseapp.com,_FB_PROJECT_ID=$DEV` under nohup (take _FB_AUTH_DOMAIN from the same prior build's substitutions if it differs); (f) `gcloud run services update nestor-frontend --image=$REG/frontend@<digest>`; (g) rollback lines: the previous revision names from step 5 with `gcloud run services update-traffic <svc> --to-revisions=<prev>=100` (note: rolling nestor-api back BELOW 0018 is safe — the column is additive and the old code ignores it; no downgrade needed).
  </action>
  <verify>
    <automated>git diff --name-only c91edd1..HEAD -- tribunal/ infra/env/client.tfvars | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints 0.
    - Backend pytest summary: 0 failed; frontend tsc, vitest, i18n-audit and build all exit 0 (paste the summary lines into the SUMMARY).
    - `git status --porcelain frontend/src/routeTree.gen.ts` prints nothing before the push; `git rev-parse HEAD` equals `git rev-parse origin/master` after the push.
    - The command sheet exists in the scratchpad and orders migrate (c) BEFORE nestor-api (d).
  </acceptance_criteria>
  <done>All gates green on the pushed HEAD; the surface is backend + migrate + frontend only; the operator has an ordered, filled-in command sheet with rollback lines.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs the dev build + migrate + deploy (classifier blocks builds submit for agents)</name>
  <what-built>Gates green, surface derived (no tribunal change), command sheet prepared in Task 1.</what-built>
  <how-to-verify>
    Run the command sheet's steps (a) through (f) in order via `!` in the session, each build detached with nohup; wait for each build with `gcloud builds describe <FULL UUID> --format='value(status)'` = SUCCESS before the next step. Step (c) must finish (`jobs execute --wait` returns) BEFORE step (d). Do NOT deploy tribunal-api or tribunal-worker. Do NOT touch nestor-pulse-prod. Paste back: the backend build UUID, the frontend build UUID, the migrate job execution name, and any error text.
  </how-to-verify>
  <files>(none — dev Cloud Run services, the nestor-migrate job and Artifact Registry only)</files>
  <read_first>
    - the Task 1 command sheet in the session scratchpad
  </read_first>
  <action>Operator executes command-sheet steps (a)-(f) in order; the agent does not run any mutating gcloud command in this task and does not resubmit a build that is still running.</action>
  <verify>
    <automated>gcloud builds describe BACKEND_BUILD_UUID --project=project-cb01b861-cb4a-438d-b9a --account=tools@dotto.be --format='value(status)' (substitute the pasted UUID; repeat for the frontend UUID) prints SUCCESS</automated>
  </verify>
  <acceptance_criteria>
    - Both pasted build UUIDs describe as SUCCESS.
    - The pasted migrate execution name exists (`gcloud run jobs executions describe <name> --region=europe-west1 --project=project-cb01b861-cb4a-438d-b9a --account=tools@dotto.be` succeeds) and completed.
    - No command in the operator's pasted output names nestor-pulse-prod, tribunal-api or tribunal-worker.
  </acceptance_criteria>
  <done>Backend and frontend images built on dev, 0018 migrated, nestor-api then nestor-frontend updated, in that order.</done>
  <resume-signal>Paste the two build UUIDs + the migrate execution name, or describe the failure</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Read-back on dev + deploy record + STATE.md</name>
  <files>infra/DEPLOY-RUNBOOK.md, .planning/STATE.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md the most recent "23.5 DEPLOY RECORD" section (~line 8064) for the record format
    - .planning/STATE.md header and "Current Position"
  </read_first>
  <action>
    Read-only, every gcloud call pinned with `--account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a`:
    1. `gcloud builds describe` both UUIDs -> SUCCESS; record their image digests.
    2. Latest ready revision of nestor-api and nestor-frontend; `gcloud run revisions describe <rev> --format='value(status.imageDigest)'` equals the respective build digest (never read `containers[0].image`, a mutable tag, and never the service's empty status.imageDigest).
    3. Migration proof: `gcloud logging read` for the named nestor-migrate execution shows the line `Running upgrade 0017 -> 0018`; the execution's completion time is BEFORE the new nestor-api revision's creation time.
    4. Smoke: `curl -s -o /dev/null -w '%{http_code}' <api-url>/readyz` = 200 (liveness only); an unauthenticated `curl -s -o /dev/null -w '%{http_code}' <api-url>/intakes/00000000-0000-0000-0000-000000000000/research/runs` returns the SAME non-404 code as an unauthenticated probe of an existing route such as `<api-url>/intakes/00000000-0000-0000-0000-000000000000/research/stream` (expected 401; the route is mounted behind protected_router — a 404 on the runs path while the stream path is not 404 means the new code is not serving); frontend URL returns 200.
    5. Confirm tribunal-api and tribunal-worker latest revisions are the same names as before this plan (unchanged).
    Then append to infra/DEPLOY-RUNBOOK.md a section `### 23.6 DEPLOY RECORD — DEV ONLY, <date>, code <TAG>` with: services + revision names + REVISION digests, build UUIDs, migrate execution + the 0017 -> 0018 line and its timestamp vs the api revision time, the smoke codes, "tribunal-api / tribunal-worker NOT deployed — derived: git diff c91edd1..HEAD -- tribunal/ empty", "prod NOT touched", the rollback lines, and a "NOT PROVEN" list: no browser walkthrough yet (Task 4), no paid rerun executed unless the operator did one, the delivered-stays-delivered rule proven by test only. Update .planning/STATE.md Current Position with a 23.6 dev-deployed line naming the revisions (edit STATE.md by hand — do NOT use `gsd-sdk state.advance-plan`, it corrupts STATE.md). `git add -f .planning/STATE.md` (gitignored) and commit both files.
  </action>
  <verify>
    <automated>grep -c '23.6 DEPLOY RECORD' infra/DEPLOY-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - The grep prints 1 (0 at HEAD c91edd1, so the criterion is not pre-satisfied).
    - The record contains the literal `Running upgrade 0017 -> 0018`, two REVISION digests starting `sha256:`, and the word `NOT PROVEN`.
    - The unauthenticated runs-route probe returned the same non-404 code as the stream-route probe, and /readyz returned 200 (all three codes quoted in the record).
    - `git diff --name-only HEAD~1 -- infra/env/client.tfvars` prints nothing.
  </acceptance_criteria>
  <done>Dev is verified at the phase HEAD by revision digest and migration log; the record and STATE.md say exactly what was and was not proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator browser walkthrough on dev</name>
  <what-built>Run history, Rerun and chosen-run label deployed to dev nestor-frontend / nestor-api (Task 3 read-back).</what-built>
  <how-to-verify>
    Log in to the dev frontend as a superadmin. Use an intake that already has at least one finished research run (in_research or delivered).
    1. The workflow card shows "Research-runs (n)" with every run newest first, attempt numbers, status badges, started time, duration, cost ("—" when unknown).
    2. Exactly one row shows the yellow "Gekozen run" badge with "Automatisch: nieuwste voltooide run" when nothing was chosen.
    3. Click "Kies als basis voor rapport" on another finished run (if one exists), confirm: the badge moves with no reload and reads "Door jou gekozen…"; reload the page — it stays.
    4. "Run openen", "Verificatierapport bekijken" and (for a verified chain) "Ruwe output downloaden" work per row.
    5. "Nieuwe run starten" opens the ~$40 confirm; press Annuleren — no run appears. (Confirming spends ~$40 — only do it if you want the paid proof; if you do: the new row appears as queued on top, the button disables with the in-flight reason, and a delivered intake stays delivered.)
    6. Switch the UI to fr and en and read the block's copy.
    7. On a delivered intake open the client report link: unchanged.
  </how-to-verify>
  <files>(none — the operator's reply is recorded into infra/DEPLOY-RUNBOOK.md's 23.6 record by the executor after approval)</files>
  <read_first>
    - .planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-UI-SPEC.md (what "right" looks like)
  </read_first>
  <action>Operator performs steps 1-7 in the dev browser. On the reply the executor appends "Browser walkthrough: <approved / issues verbatim>, paid rerun: <yes/no>" to the 23.6 DEPLOY RECORD and moves the corresponding NOT PROVEN items, then commits (git add -f for .planning files).</action>
  <verify>
    <automated>grep -c 'Browser walkthrough:' infra/DEPLOY-RUNBOOK.md (must be at least 1 after the reply is recorded)</automated>
  </verify>
  <acceptance_criteria>
    - The operator replied "approved" or listed issues; the reply is quoted in the 23.6 DEPLOY RECORD.
    - If issues were listed, each is recorded as a numbered item (DEF-23.6-05-NN) in the SUMMARY and nothing is marked proven that the operator did not see.
  </acceptance_criteria>
  <done>The walkthrough result is recorded; the phase is approved on dev or has named, tracked issues.</done>
  <resume-signal>Type "approved" (say whether a paid rerun was done), or list the issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation -> GCP dev project | mutating gcloud calls |
| nestor-migrate job -> Cloud SQL (dev) | schema change on a table of paid rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.6-20 | Tampering | wrong project/account | mitigate | `--account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a` on every command in the sheet; no prod project id appears in it; client.tfvars diff gate = empty |
| T-23.6-21 | Denial of Service | api before migration | mitigate | sheet orders jobs update + execute --wait before services update; record proves order by timestamps |
| T-23.6-22 | Denial of Service (spend) | tribunal-worker boot | mitigate | no worker deploy — derived by `git diff c91edd1..HEAD -- tribunal/` = empty; if non-empty the plan stops |
| T-23.6-23 | Information Disclosure | FB web key | mitigate | recovered from a prior build into an env var, non-empty asserted, never echoed or written to a file |
| T-23.6-SC | Tampering | npm installs | accept | `npm ci` against the committed lockfile only; this phase adds no package |
</threat_model>

<verification>
- Dev nestor-api and nestor-frontend run the phase HEAD by REVISION digest; alembic 0018 applied before the api revision.
- tribunal services and prod untouched.
- Browser walkthrough result recorded (approved or issues).
</verification>

<success_criteria>
- RR-SHIP: phase 23.6 live on dev only, recorded, with an honest NOT PROVEN list.
</success_criteria>

<output>
Create `.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-05-SUMMARY.md` when done (`git add -f`).
</output>
