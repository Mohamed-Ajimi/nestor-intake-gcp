---
phase: 23.6-rerun-deep-research-with-run-history-superadmin-rerun-button
plan: 04
type: execute
wave: 2
depends_on: ["23.6-03"]
files_modified:
  - frontend/src/components/intake/ResearchRunHistory.tsx
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
autonomous: true
requirements: [RR-01, RR-02, RR-03]

must_haves:
  truths:
    - "On an in_research, delivered or archived intake a superadmin sees 'Research-runs (n)': every run newest first with attempt, status badge, started time, duration, cost and links to its run page, verification report and raw-output zip (D-23.6-04)"
    - "Exactly one row carries the yellow chosen-run badge: the marked run, or the newest finished run as a labelled display default; none when no run has finished"
    - "'Kies als basis voor rapport' opens a confirm; on confirm the badge moves without a page reload; nothing client-facing happens (D-23.6-02 revised)"
    - "'Nieuwe run starten' is shown on in_research and delivered intakes with >= 1 run, is disabled with a reason while any run is in flight, and starts a run ONLY from the ~$40 confirm dialog's action (D-23.6-01, D-23.6-03)"
    - "After a rerun starts, the new run appears on top as queued and the page's ONE research stream follows it (reopenKey bump) — no second SSE connection is opened"
    - "Non-superadmins see exactly what they saw before (the single Open run link); decomposed intakes keep the existing Start deep research path"
  artifacts:
    - path: "frontend/src/components/intake/ResearchRunHistory.tsx"
      provides: "history block, Rerun button + confirm, choose confirm"
      exports: ["ResearchRunHistory"]
      min_lines: 150
    - path: "frontend/src/routes/admin.pulse.intakes.$id.tsx"
      provides: "wiring: isSuperadmin branch, reopenKey, live run hand-down"
      contains: "ResearchRunHistory"
  key_links:
    - from: "frontend/src/components/intake/ResearchRunHistory.tsx"
      to: "frontend/src/lib/api/research.ts"
      via: "getResearchRuns / chooseResearchRun / triggerResearch / getBundleUrl"
      pattern: "getResearchRuns\\(|chooseResearchRun\\(|triggerResearch\\("
    - from: "frontend/src/components/intake/ResearchRunHistory.tsx"
      to: "frontend/src/lib/research/runHistory.ts"
      via: "rerunState / resolveChosenRun / classifyRerunFailure / runStatusBadge"
      pattern: "rerunState\\(|resolveChosenRun\\("
    - from: "frontend/src/routes/admin.pulse.intakes.$id.tsx"
      to: "useActiveResearchRun"
      via: "the page's single stream, second arg reopenKey"
      pattern: "useActiveResearchRun\\("
---

<objective>
Render the run history, the Rerun button and the chosen-run label on the intake detail page, exactly
per the approved UI-SPEC, using plan 03's pure rules and seam functions.

Purpose: D-23.6-03/04 and D-23.6-02 (revised) become usable by the superadmin.
Output: one new component; the intake page swaps its single `IntakeOpenRunLink` row for it
(superadmin only) and threads the live run + a reopen key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-UI-SPEC.md
@.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-03-SUMMARY.md
@CLAUDE.md
@frontend/src/lib/research/runHistory.ts
@frontend/src/lib/research/statusLabel.ts
</context>

<interfaces>
From frontend/src/components/intake/ResearchRunProgress.tsx:
- `export function useActiveResearchRun(intakeId: string | undefined, reopenKey?: number): { run: ResearchRun | null }` — bumping reopenKey re-opens the (possibly terminal-closed) stream; its JSDoc says it must be bumped by a completed operator action, never a timer.
- `export function IntakeOpenRunLink({ runId }: { runId: string | null | undefined })`.
- Private `OpenRunLink` classes to reuse for row links (minus `mt-4`): `inline-flex items-center gap-2 border border-ink/30 px-4 py-2 font-mono text-xs uppercase tracking-wider text-ink hover:bg-ink/5`.
From frontend/src/routes/admin.pulse.intakes.$id.tsx (HEAD):
- line ~193 `const RESEARCH_SURFACE_STATUSES = new Set(["in_research", "delivered", "archived"]);`
- line ~225 `const { session } = useAuth();` — `useAuth()` also returns `isSuperadmin: boolean`.
- line ~328 `const researchSurfaceActive = ...; const { run: researchRun } = useActiveResearchRun(researchSurfaceActive ? intake?.id : undefined);` — THE page's one stream (23-03 comment block above it; read it).
- line ~423 `const load = useCallback(async () => { ... })` — reloads the intake.
- line ~1374 `{intake.status && RESEARCH_SURFACE_STATUSES.has(intake.status) && (<div className="px-6 pb-6"><IntakeOpenRunLink runId={researchRun?.id ?? null} /></div>)}` — the row being replaced (see the D-22-5 comment block above it).
- line ~877 `onStartAutoResearch` — the decomposed Start path; leave it untouched.
From frontend/src/lib/research/runClock.ts: `fmtDate(d, fallback)`, `fmtCost(cost, fallback)`, `useElapsed(startedAt, active)`, `fmtDuration(startedAt, completedAt)`.
From frontend/src/lib/research/triggerOutcome.ts: `classifyTriggerOutcome(res) -> "started" | "needs_investigation" | "error"`; `resolveErrorKey(code)` from `@/lib/i18n/error-codes`.
From frontend/src/lib/api/research.ts: `triggerResearch`, `getBundleUrl(intakeId, runId) -> {url, expires_in}`, `getResearchRuns`, `chooseResearchRun` (plan 03).
From frontend/src/lib/research/verificationGate.ts: `canHaveVerificationReport(status)`.
shadcn primitives (import only, never edit): `@/components/ui/alert-dialog` (AlertDialog, AlertDialogTrigger?, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogCancel, AlertDialogAction), `@/components/ui/skeleton`. See how NextStepBanner.tsx builds its research-start confirm and mirror it.
Reused intake-ns keys: `research.openRun`, `research.download`, `research.downloadError`, `research.runPage.trace.attempt` (interpolates `number`), `research.costFallback`, `verification.viewAction`, `research.runPage.status.*` (via statusLabel). Existing admin key `intakeDetail.toast.researchStartFailed`, `intakeDetail.toast.researchNeedsInvestigation`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ResearchRunHistory component (list, badge, row actions, both confirms, states)</name>
  <files>frontend/src/components/intake/ResearchRunHistory.tsx</files>
  <read_first>
    - .planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-UI-SPEC.md (all of "Layout and placement", "Interaction contract", "States", "Accessibility")
    - frontend/src/components/intake/NextStepBanner.tsx (AlertDialog research-start confirm; primary button classes)
    - frontend/src/components/intake/ResearchRunProgress.tsx lines 157-300
    - frontend/src/lib/research/runHistory.ts, statusLabel.ts, runClock.ts
    - frontend/src/routes/admin.pulse.runs.$runId.index.tsx (how it calls getBundleUrl and renders the zip download, for the same toast + redirect pattern)
  </read_first>
  <action>
    Create `export function ResearchRunHistory({ intakeId, intakeStatus, liveRun, onRerunStarted }: { intakeId: string; intakeStatus: string; liveRun: ResearchRun | null; onRerunStarted: () => Promise<void> | void })`. The component assumes it is rendered only for superadmins (the page decides); it still passes `isSuperadmin: true` into `rerunState` so the rule has one input list.
    Data: local state `history` + `loadState` ("loading" | "ready" | "error"); `fetchHistory` = useCallback calling `getResearchRuns(intakeId)` with the `let cancelled` flag pattern; runs through `sortRunsNewestFirst(mergeLiveRun(history.runs, liveRun))`. Refetch ONCE whenever `liveRun?.status` or `liveRun?.id` changes (useEffect keyed on those two values) — never on a timer, and NEVER call useActiveResearchRun or openResearchStream here (one SSE per page).
    Render per UI-SPEC: shell `border-t border-ink/10 bg-paperLight px-6 py-5`; header row (title `t("intakeDetail.runs.title")` + count; Rerun button when `rerunState(...).visible`); reason line `t("intakeDetail.runs.rerunBlockedInFlight")` with an id referenced by the button's `aria-describedby` and `title` when reason is "in_flight"; desktop `<table>` (md+) with sr-only `<caption>`, `<th scope="col">` heads from `intakeDetail.runs.col.*` (actions head sr-only), and a mobile `<ol>` below md with the four-line layout; badge from `runStatusBadge(status)` + `statusLabel(labelStatus, tIntake)` + `mark-green`/`mark-ink` span + `Loader2` spinner when flagged; Started `fmtDate(started_at, "—")`; Duration `fmtDuration` for terminal runs and a small inner `RunningClock` subcomponent using `useElapsed(started_at, true)` for in-flight rows (hooks cannot be called in a loop — the subcomponent is the ONE clock use per row); Cost `fmtCost(cost_usd_total, "—")`.
    Chosen badge: `resolveChosenRun(runs, history.chosen_research_run_id)`; on that row render `badge-highlight` + `Check` with `aria-label` = chosenBadge + sub-line, and the sub-line `chosenBadgeMarked` or `chosenBadgeDefault` by `source`; when runId is null and at least one run exists show `intakeDetail.runs.noChosenRun` under the header.
    Row actions left to right: "Kies als basis voor rapport" when `canChooseRun(run, chosen, intakeStatus)` (secondary style from the spec) opening a choose AlertDialog (`chooseConfirmTitle` with `{ number: run.attempt }`, `chooseConfirmBody`, cancel/confirm keys); on AlertDialogAction call `chooseResearchRun`; success -> set `history.chosen_research_run_id` to the returned id locally (badge moves, no reload) + `toast.success(t("intakeDetail.runs.toast.chooseDone", { number }))`; failure -> `toast.error(t("intakeDetail.runs.toast.chooseFailed"))` and nothing moves. "Run openen": `<Link to="/admin/pulse/runs/$runId" params={{ runId: run.id }}>` with ExternalLink. "Verificatierapport bekijken" when `canHaveVerificationReport(run.status)`: Link to `/admin/pulse/runs/$runId/verification` (confirm the exact route id from routeTree.gen.ts) + ArrowRight, label `tIntake("verification.viewAction")`. "Ruwe output downloaden" when `canDownloadRunZip(run)`: `getBundleUrl(intakeId, run.id)` then `window.location.href = url`, Loader2 while minting, failure `toast.error(tIntake("research.downloadError"))`; when a finished run cannot download, show the `zipOnRunPage` sub-line instead. Archived intake: no Rerun, no choose button; links + download stay.
    Rerun: primary button (`RotateCw`, `intakeDetail.runs.rerun`, busy -> Loader2 + `rerunStarting`) opens the rerun AlertDialog (`rerunConfirmTitle`, `rerunConfirmBody`, plus a second paragraph `rerunConfirmParkedNote` when `parkedNote`), cancel/confirm keys. ONLY AlertDialogAction triggers: capture `beforeIds = new Set(runs.map(r => r.id))`, call `triggerResearch(intakeId)`, switch on `classifyTriggerOutcome(res)`: "started" -> `toast.success(t("intakeDetail.runs.toast.rerunStarted"))`, `await onRerunStarted()`, `fetchHistory()`; "needs_investigation" -> `toast.warning(t("intakeDetail.toast.researchNeedsInvestigation"))`; "error" -> refetch the list, then `classifyRerunFailure(beforeIds, freshRuns)`: "in_flight" -> `toast.error(t("intakeDetail.runs.toast.rerunInFlight"))`; "maybe_started" -> `toast.error(t("intakeDetail.toast.researchStartFailed"))` (spend may have started — copy-truth note) and `await onRerunStarted()`; "failed" -> `toast.error(codeKey ? t(codeKey) : t("intakeDetail.runs.toast.rerunFailed"))` with `codeKey = resolveErrorKey(res.code)`.
    States: loading = header + disabled Rerun + two `<Skeleton className="h-10 w-full" />`; empty (ready, 0 runs) = `intakeDetail.runs.empty`, no Rerun; error = `intakeDetail.runs.loadFailed` in `text-sm text-red-600`, a secondary `intakeDetail.runs.retry` button calling fetchHistory, AND `<IntakeOpenRunLink runId={liveRun?.id ?? null} />` below it.
    i18n: `const { t } = useTranslation("admin")` for runs.* and toast keys, `const { t: tIntake } = useTranslation("intake")` for reused keys; every key a LITERAL string argument (the audit cannot see interpolated keys); no two-argument `t("key", "fallback")` calls. Colours/classes only from the UI-SPEC (yellow only on the chosen badge; no pink). Do not import from or edit `frontend/src/components/ui/` beyond the two primitives' public exports.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && node scripts/i18n-audit.mjs && npx eslint src/components/intake/ResearchRunHistory.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tsc exits 0, i18n-audit exits 0, eslint reports 0 errors for the file.
    - `grep -v '^\s*//' frontend/src/components/intake/ResearchRunHistory.tsx | grep -c 'useActiveResearchRun(\|openResearchStream('` returns 0.
    - `grep -c 'AlertDialogAction' frontend/src/components/intake/ResearchRunHistory.tsx` returns at least 2 (rerun + choose confirms) and `grep -c 'triggerResearch(' frontend/src/components/intake/ResearchRunHistory.tsx` returns 1.
    - `grep -c 't("intakeDetail.runs.rerunConfirmBody")' frontend/src/components/intake/ResearchRunHistory.tsx` returns 1 and `grep -c 'badge-highlight' frontend/src/components/intake/ResearchRunHistory.tsx` returns at least 1.
    - `grep -c 'setInterval\|setTimeout' frontend/src/components/intake/ResearchRunHistory.tsx` returns 0 (no polling).
    - `git diff --name-only -- frontend/src/components/ui` prints nothing.
  </acceptance_criteria>
  <done>The component renders every state and action in the UI-SPEC from the pure rules, spends only from the confirm action, and opens no stream of its own.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the history into the intake page (superadmin branch, reopenKey, live run)</name>
  <files>frontend/src/routes/admin.pulse.intakes.$id.tsx</files>
  <read_first>
    - frontend/src/routes/admin.pulse.intakes.$id.tsx lines 185-340, 420-440, 870-920, 1355-1400 (constants, auth, the one-stream comment block, load, onStartAutoResearch, the D-22-5 row)
    - frontend/src/components/intake/ResearchRunHistory.tsx (from Task 1)
  </read_first>
  <action>
    - Destructure `isSuperadmin` alongside `session` from `useAuth()`.
    - Add `const [researchReopenKey, setResearchReopenKey] = useState(0);` and pass it as the SECOND argument of the existing `useActiveResearchRun(...)` call (do not add a second call). Extend the one-stream comment block with one paragraph: 23.6 bumps reopenKey after a completed rerun action because a stream that closed on the previous run's terminal frame would otherwise never see the new run (the hook's documented reopenKey use).
    - Add `const onRerunStarted = useCallback(async () => { await load(); setResearchReopenKey((k) => k + 1); }, [load]);`.
    - Replace the D-22-5 row body: inside the same `RESEARCH_SURFACE_STATUSES` condition render `isSuperadmin ? <ResearchRunHistory intakeId={intake.id} intakeStatus={intake.status} liveRun={researchRun} onRerunStarted={onRerunStarted} /> : <div className="px-6 pb-6"><IntakeOpenRunLink runId={researchRun?.id ?? null} /></div>`. Add a comment under the D-22-5 block: 23.6 / UI-SPEC UI-1 — the history replaces the single link for superadmins; the error state of the history still renders IntakeOpenRunLink so a failed list never removes the way into the run page.
    - Leave `onStartAutoResearch`, NextStepBanner props and every other block unchanged (D-23.6-03 keeps the decomposed Start path).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run && node scripts/i18n-audit.mjs && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All four commands exit 0. After `npm run build`, run `git status --porcelain frontend/src/routeTree.gen.ts`; if it shows a line-ending-only change, `git checkout -- frontend/src/routeTree.gen.ts` before committing (known CRLF trap).
    - `grep -c 'useActiveResearchRun(' 'frontend/src/routes/admin.pulse.intakes.$id.tsx'` returns 1 (still ONE stream) and that call's argument list contains `researchReopenKey` (`grep -A3 'useActiveResearchRun(' 'frontend/src/routes/admin.pulse.intakes.$id.tsx' | grep -c researchReopenKey` returns 1).
    - `grep -c '<ResearchRunHistory' 'frontend/src/routes/admin.pulse.intakes.$id.tsx'` returns 1 and `grep -c 'isSuperadmin' 'frontend/src/routes/admin.pulse.intakes.$id.tsx'` returns at least 2.
    - `git diff --name-only c91edd1 -- frontend/src/routes/intake.* frontend/src/routes/results.* frontend/src/components/ui frontend/src/components/intake/NextStepBanner.tsx` prints nothing (no client route, no shadcn, no banner change).
  </acceptance_criteria>
  <done>Superadmins see the history with Rerun and chosen-run on in_research/delivered/archived intakes; non-superadmins see the old link; one stream, reopened after a rerun; production build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator click -> paid trigger | a single mis-click must not spend ~$40 |
| browser -> nestor-api SSE | connection count per page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.6-16 | Denial of Service (spend) | Rerun button | mitigate | trigger fires only from AlertDialogAction of a dialog that states ~$40 and no limit; button disabled while in flight; backend 409 is the authority |
| T-23.6-17 | Denial of Service (resources) | SSE connections | mitigate | component opens no stream (grep gate = 0); page keeps exactly one useActiveResearchRun call; list refetch only on live status/id change, no timers |
| T-23.6-18 | Information Disclosure | client surfaces | mitigate | no client route, mail or report wiring; diff gate on intake.*/results.* routes |
| T-23.6-19 | Spoofing (role) | isSuperadmin branch | accept | presentation only; both new endpoints are superadmin_gate'd server-side (plan 02), a non-superadmin gets 404 regardless of UI |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npx vitest run && node scripts/i18n-audit.mjs && npm run build` green.
- Browser walkthrough is owed on dev (plan 05) — gates prove keys and types, not that the copy and layout are right.
</verification>

<success_criteria>
- RR-01 UI: Rerun visible/disabled/confirmed per spec, delivered intakes included.
- RR-02 UI: full history with per-run links, report and zip.
- RR-03 UI: one chosen badge, marked or default, movable by confirm.
</success_criteria>

<output>
Create `.planning/phases/23.6-rerun-deep-research-with-run-history-superadmin-rerun-button/23.6-04-SUMMARY.md` when done (`git add -f`).
</output>
