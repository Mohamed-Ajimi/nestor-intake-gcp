---
phase: 23.4-client-environment
plan: 05
type: execute
wave: 3
depends_on: ["23.4-03", "23.4-04"]
files_modified:
  - infra/BOOTSTRAP.md
  - infra/DEPLOY-RUNBOOK.md
  - .planning/STATE.md
  - .planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/deferred-items.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "A third environment can be stood up by following one numbered checklist, without reading 7,000 lines of per-phase history."
    - "Every manual step the runbook records as IaC drift appears in that checklist, with the Firebase authorized-domains step immediately after the first frontend deploy."
    - "The 23.4 deploy record names the digests, revisions, alembic heads and the proving run, and says plainly what was NOT verified."
    - "The open operator items that outlive this phase are written down where the next session will find them, not left in a transcript."
  artifacts:
    - path: "infra/BOOTSTRAP.md"
      provides: "the from-zero environment checklist"
      contains: "Authorized domains"
      min_lines: 150
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "the 23.4 record plus a promotion section pointing at promote.sh"
      contains: "23.4"
    - path: ".planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/deferred-items.md"
      provides: "the operator to-do list this phase creates or inherits"
  key_links:
    - from: "infra/BOOTSTRAP.md"
      to: "infra/promote.sh"
      via: "the checklist ends by handing over to the promotion path"
      pattern: "promote.sh"
    - from: "infra/BOOTSTRAP.md"
      to: "infra/env/client.tfvars"
      via: "the tfvars + backend-config invocation"
      pattern: "backend-config"
---

<objective>
Write down what was just done, so the next environment is a checklist and the next session
knows what is still owed.

Purpose: the stand-up steps for this system exist, but they are scattered across
`infra/DEPLOY-RUNBOOK.md` in per-phase order — § Step 1-5, § Phase 9, § Phase 10, § Phase 12,
§ Phase 13, § Phase 14, plus corrections dated 2026-09-07 — and several of them are recorded
as IaC drift, meaning Terraform does not apply them and only the prose knows. That is a
7,000-line file organised by when a thing was learned, which is the wrong axis for someone
standing up an environment.

Output: `infra/BOOTSTRAP.md` as a numbered from-zero checklist, a promotion section and a 23.4
record in the deploy runbook, an updated `.planning/STATE.md`, and a deferred-items file
carrying every open operator call.

This plan changes no code, deploys nothing and spends nothing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-CONTEXT.md
@.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-01-SUMMARY.md
@.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-02-SUMMARY.md
@.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-03-SUMMARY.md
@.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-04-SUMMARY.md

<interfaces>
<!-- Where the drift steps live in infra/DEPLOY-RUNBOOK.md. Read these ranges, not the file. -->

| section | lines | what the checklist must absorb |
|---|---|---|
| § Step 1-2 | 50-106 | secret containers + resource-scoped accessor; VALUES added via `--data-file=-` stdin, never echoed |
| § Step 4 | 126-196 | CPU always-allocated + min-instances, and the 2026-09-07 CORRECTION: `minScale=1` on nestor-api is load-bearing for the reconcile timer, and `--no-cpu-throttling` is load-bearing for the same reason — dropping either fails SILENTLY because `/readyz` stays 200 |
| § Phase 9 | 276-430 | uploads bucket + keyless signBlob IAM + the bucket CORS file |
| § Phase 10 | 432-520 | Resend secret + `NESTOR_ADMIN_EMAIL` + `APP_BASE_URL` |
| § Phase 12 Step 12.3-12.5 | 600-715 | frontend build substitutions; capture the run.app URL, never guess it; second pass (a) CORS + APP_BASE_URL (b) bucket CORS (c) **Firebase authorized domains, console, manual** |
| § Phase 13 | 716-960 | the six tribunal secrets, the asyncpg unix-socket DSN form, the tribunal-migrate job and its `cd /app/nestor_pulse_sdk` gotcha, `--set-cloudsql-instances` for jobs |
| § Phase 14 | 965-1180 | the dedicated `tribunal-run` SA, the invoker gate bound to the intake SA only, `TRIBUNAL_SERVICE_URL` on BOTH services as the OIDC audience |
| § Phase 17 Step 17.c | 1460-1495 | a Cloud Run JOB does not track its service's image — repin before execute |

Also from project memory, and not in any single section: migrations need a privilege bootstrap
the IaC does not cover (Phase 2); `google-auth >= 2.47` floor; `/healthz` 404s upstream while
`/readyz` 200s; the Anthropic secret-name drift this phase collapses (D-23.4-05).

The 2026-09-09 record at the END of `infra/DEPLOY-RUNBOOK.md` is the house shape for a deploy
record: a prose paragraph, a `| service | revision | image digest |` table, the build UUID and
how it was confirmed, a gates block with real numbers, a RED-evidence block, a post-deploy
read-back, a revert section, and a "found but not touched" list. Match it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: infra/BOOTSTRAP.md — stand up an environment from zero as a numbered checklist</name>
  <files>infra/BOOTSTRAP.md</files>
  <read_first>
The eight runbook ranges listed in the interfaces table above — read each range once, extract every manual step, and do not re-read
.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-01-SUMMARY.md and 23.4-03-SUMMARY.md (what was ACTUALLY done and in what order, including anything that had to be redone)
infra/env/README.md
infra/promote.sh (the handover point at the end of the checklist)
  </read_first>
  <action>
Write `infra/BOOTSTRAP.md`: one numbered checklist that takes a brand-new GCP project to a
working Nestor Pulse environment. Order it the way the work actually happens, which is the
order plans 23.4-01 and 23.4-03 executed — that ordering is now evidence, not a proposal, so
where the SUMMARYs show a step had to be redone, put it where it belonged and say why in one
line.

Non-negotiable placements:
* **Firebase authorized domains comes IMMEDIATELY after the first frontend deploy**, with both
  hostnames, host only, no wildcard. It is console-only; there is no gcloud or Terraform
  surface. Put the symptom next to it: `auth/unauthorized-domain`, which presents as a broken
  login rather than a missing allowlist entry.
* **The second pass is a numbered sub-list, not a paragraph** — CORS_ALLOWED_ORIGINS,
  APP_BASE_URL, uploads-bucket CORS, `TRIBUNAL_SERVICE_URL` on BOTH services — with the rule
  that the URLs are CAPTURED with `services describe`, never guessed, and that the two
  `TRIBUNAL_SERVICE_URL` values must be byte-identical because it is an OIDC audience.
* **Secret VALUES are a step the operator performs and the agent watches** (D-23.4-05): the
  stdin idiom, one container at a time, nothing echoed. List the exact container names.
* **The seed order is explicit**: `nestor-migrate` -> `tribunal-migrate` ->
  `nestor-seed-superadmin` -> create the space -> create users. Note that the canonical intake
  template needs NO seed (it ships in the image, D-CANON) and that a console-created Identity
  Platform user has no custom claims and is refused by the API, so users must come through the
  admin surface.
* **The load-bearing knobs get their own short section** with the reason each one matters and
  the fact that dropping it fails SILENTLY: `nestor-api` `minScale >= 1` and
  `--no-cpu-throttling` (the reconcile timer stops and `/readyz` keeps answering 200);
  `tribunal-worker` `cpu_idle = false` (it polls on its own schedule); `NESTOR_WORKER_STALE_MINUTES`
  strictly below `NESTOR_WORKER_RUN_TIMEOUT_MINUTES`.
* **A "things that are not what they look like" section**, short, each line a trap that has
  actually bitten: `gcloud builds submit | tail` reports the pipe's status so a FAILED build
  exits 0 — confirm with `builds describe <FULL UUID>`; `containers[0].image` is a mutable tag,
  read `status.imageDigest`; `echo n | gcloud` APPLIES the default because non-TTY stdin makes
  gcloud non-interactive; a Cloud Run job does not track its service's image; jobs take
  `--set-cloudsql-instances` and services take `--add-cloudsql-instances`; `/healthz` 404s
  upstream while `/readyz` 200s; `--update-env-vars` for hand-typed updates but
  `--set-env-vars` inside `deploy-worker.sh`, where all seven variables must live on one line.
* **Each step carries its own verification** — the command and the expected output. A step with
  no verification is not a step in this file.
* **The checklist ENDS by handing over to `infra/promote.sh`**: bootstrap is once per
  environment, everything after is a promotion.

Include a short opening section stating what the file is NOT: it is not a history. Point at
`infra/DEPLOY-RUNBOOK.md` for why any given step exists, and keep the reasons here to one line
each.

Add an explicit "still owed for any environment" block at the end, naming: no restore has ever
been rehearsed (a backup never restored is a hope, not a guarantee); the instance is ZONAL with
no HA; the retention/GDPR review is OUT of phase 23.4 and is owed BEFORE a client uploads
sensitive material; and `app_superadmin` rotation.
  </action>
  <acceptance_criteria>
1. `test -f infra/BOOTSTRAP.md && wc -l < infra/BOOTSTRAP.md` returns at least `150`.
2. `grep -n 'Authorized domains' infra/BOOTSTRAP.md` and `grep -n 'frontend' infra/BOOTSTRAP.md` — the FIRST authorized-domains line number is greater than the first frontend-deploy line number and within 40 lines of it. Record both numbers; a checklist that puts the allowlist before the URL exists is wrong in the same way the original runbook was.
3. Every one of these appears at least once: `--data-file=-`, `builds describe`, `status.imageDigest`, `--set-cloudsql-instances`, `TRIBUNAL_SERVICE_URL`, `/readyz`, `no-cpu-throttling`, `minScale`, `nestor-seed-superadmin`, `promote.sh`, `backend-config`. Assert with one grep per token and list the counts.
4. `grep -c 'GDPR\|retention' infra/BOOTSTRAP.md` returns at least `1` and `grep -c 'restore' infra/BOOTSTRAP.md` returns at least `1` — the two things that are owed and easiest to forget.
5. `grep -riE 'AIza|sk-ant|sk-proj|postgresql\+?[a-z]*://[a-z_]+:[^@$]' infra/BOOTSTRAP.md` returns nothing.
6. Numbered steps: `grep -cE '^ *[0-9]+\.' infra/BOOTSTRAP.md` returns at least `20`.
7. `grep -c 'echo n | gcloud' infra/BOOTSTRAP.md` returns at least `1` — the trap that patched a production database during what was meant to be a read-only probe is named.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'wc -l < infra/BOOTSTRAP.md; for t in "--data-file=-" "builds describe" "status.imageDigest" "TRIBUNAL_SERVICE_URL" "/readyz" "promote.sh" "backend-config"; do printf "%s: " "$t"; grep -c -- "$t" infra/BOOTSTRAP.md; done'</automated>
  </verify>
  <done>Standing up environment number three is reading one file top to bottom, and every step in it says how to tell whether it worked.</done>
</task>

<task type="auto">
  <name>Task 2: The 23.4 deploy record and the promotion section in infra/DEPLOY-RUNBOOK.md</name>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <read_first>
infra/DEPLOY-RUNBOOK.md the final 60 lines (the 2026-09-09 record — the house shape to match)
All four 23.4 SUMMARY files (every digest, revision, UUID, log line and read-back this record must carry)
infra/promote.sh and infra/queue-check.yaml (what the promotion section points at)
  </read_first>
  <action>
APPEND two sections to `infra/DEPLOY-RUNBOOK.md`. Do not restructure the file; it is organised
by when things were learned and every earlier section is a record someone may need.

**Section A — "Promotion (dev -> client), from 23.4 onward."** Short, and a pointer rather than
a copy: the mechanism is `infra/promote.sh`; the ordering rules it enforces; the idle gate and
what each of its exit codes means; the fact that the backend and both tribunal images are
promoted by DIGEST and the frontend is REBUILT per environment because Vite inlines `VITE_*` at
build time; the invocation form by hand; and the branch/tag model as ruled in 23.4-04. End with
the one thing a reader must not conclude: Terraform's `image_tag` variables are a RECORD, not
the deploy mechanism — `promote.sh` writes them back, and an `apply` run against stale tfvars
would revert the running image.

**Section B — the 23.4 record**, matching the 2026-09-09 shape:
* A title line naming what changed per service and whether each was a REBUILD, a PROMOTION or
  CONFIRM-ONLY, plus whether a migration ran and whether a run was executed.
* The prose paragraph: a second GCP project now exists as the client environment; the current
  project is dev.
* A `| service | revision | image digest |` table for `nestor-pulse-prod`, and a second line
  saying what changed in the DEV project (three images pushed to its Artifact Registry, and
  nothing else unless 23.4-04's dev path was run).
* The build UUIDs and the statement that each was confirmed by `builds describe`, never by exit
  code.
* Gates: the backend pytest count with its baseline, the frontend `tsc` / `vitest` file+test
  counts with their baselines, `i18n-audit` result, and `npm run build`. Take the numbers from
  the 23.4-02 SUMMARY; if any gate was not re-run on the merged tree, say so.
* The RED evidence block for both D-23.4-07 code fixes, quoted from the 23.4-02 SUMMARY —
  the backend cap failure and the frontend type error.
* The proving run (D-23.4-06): run id, terminal status, wall clock, the quoted `run_heartbeat
  rowcount=1` lines, the migration `Running upgrade` lines with both heads, and the cost
  recorded as a FLOOR with the reason it is a floor.
* Both alembic heads read from their own version tables in the CLIENT database, or labelled
  NOT OBTAINED with the evidence marked INFERENCE.
* **A revert section.** For the client environment the lever is a traffic switch back to the
  previous revision per service; name the previous revisions where they exist. Say plainly
  that the FIRST stand-up has no previous revision to fall back to, so the revert for a bad
  first release is a redeploy, not a rollback.
* **A "what was NOT verified" section**, written as flatly as the rest. Carry forward whatever
  the SUMMARYs recorded as NOT VERIFIED or operator-attested, and add the standing ones: zero
  browser/E2E test coverage exists in this repo, so every frontend claim is server-rendered
  markup plus a human looking; no backup has ever been restored; the instance is zonal.

Write the record from the SUMMARY files. Where a SUMMARY says a check was skipped or attested
rather than measured, the record says that too — a deploy record that rounds attestation up to
measurement is worse than no record.
  </action>
  <acceptance_criteria>
1. `grep -n '^## ' infra/DEPLOY-RUNBOOK.md | tail -3` shows the two new sections as the last two `##` headings, and the pre-existing 2026-09-09 record is still present (`grep -c '260909' infra/DEPLOY-RUNBOOK.md` is unchanged from its pre-edit value; record both).
2. `git diff --stat infra/DEPLOY-RUNBOOK.md` shows insertions and ZERO deletions — this is an append, not a rewrite.
3. The record carries a markdown table with a `image digest` column and at least four service rows: `grep -c 'sha256:' infra/DEPLOY-RUNBOOK.md` increases by at least `4` against its pre-edit count; state both counts.
4. `grep -c 'rowcount=1' infra/DEPLOY-RUNBOOK.md` increases by at least `1` — the proving run's heartbeat evidence is in the record, not only in a SUMMARY.
5. `grep -c 'Running upgrade' infra/DEPLOY-RUNBOOK.md` increases by at least `2` — both migration lines.
6. The new sections contain the strings `NOT VERIFIED`, `FLOOR` and `promote.sh`; one grep each, counts listed.
7. `grep -riE 'AIza|sk-ant|sk-proj' infra/DEPLOY-RUNBOOK.md` returns nothing.
8. Every digest, revision name, build UUID and run id in the record also appears in one of the four 23.4 SUMMARY files. Verify by extracting the `sha256:` values and revision names from the new section and grepping each in `.planning/phases/23.4-*/`; report any that does not match. A value present only in the record is a fabrication and must be removed.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'git diff --stat infra/DEPLOY-RUNBOOK.md; grep -c "rowcount=1" infra/DEPLOY-RUNBOOK.md; grep -c "NOT VERIFIED" infra/DEPLOY-RUNBOOK.md'</automated>
  </verify>
  <done>The client environment's provenance is in the same file as every other deploy in this project, at the same standard, including the parts that were not measured.</done>
</task>

<task type="auto">
  <name>Task 3: STATE.md position and the operator to-do list</name>
  <files>.planning/STATE.md, .planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/deferred-items.md</files>
  <read_first>
.planning/STATE.md lines 1-70 (the frontmatter and the Current Position block — note the warnings about hand-curated prose being mistaken for machine state)
.planning/phases/23.3-concurrent-research-execution-in-instance-concurrency-timeout-reconciler/deferred-items.md (the house shape for a deferred-items file)
All four 23.4 SUMMARY files
.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-CONTEXT.md (§ Scope fences — what was deliberately left out)
  </read_first>
  <action>
**`deferred-items.md`** — every open item this phase created or inherited, one per entry, each
with an id `DEF-23.4-NN`, what it is, why it was deferred, and what closing it would take.
At minimum:
* The OPEN ruling: may client users START paid runs, or operators only? Default in force today
  is operator-only, because the trigger verb is superadmin-gated (D-23.1-16). State the
  consequence of each answer in one line — a client-startable run is ~$25-45 of uncapped spend
  per press.
* Dev worker `min-instances` -> 0 to stop paying for idle capacity. The operator agreed in
  principle; it changes the DEV project, so it was fenced out of this phase. Note the
  consequence: at 0 the dev worker claims nothing, which is fine for dev and is NOT safe to
  copy to the client.
* The spend cap, still deferred by standing ruling (`NESTOR_TRIBUNAL_UNCAPPED=1`). Now applies
  to TWO environments.
* Retention / GDPR review — out of scope here, owed BEFORE the client uploads sensitive
  material. Carry the audit-trail deadline that has already passed.
* `app_superadmin` rotation, now owed in two projects.
* No restore has ever been rehearsed; both instances are zonal.
* Custom domain and Identity Platform email templates for the client environment (defaults are
  in use).
* Whatever 23.4-04 ruled as `triggers-later`, if that was the ruling.
* Zero browser/E2E test coverage — every frontend claim in this phase is markup plus a human.
* Anything the four SUMMARYs recorded as NOT VERIFIED.

**`.planning/STATE.md`** — update the Current Position block ONLY. Record: phase 23.4 complete,
how many plans, the client environment live with its four revisions and digests, the proving
run's id and outcome, and what the next action is. Keep the existing warnings intact.

⛔ **Do NOT run `gsd-sdk query state.advance-plan`.** It has corrupted this file before — on
2026-09-05 it flipped Status to complete on plan 1 of 11 and ERASED a deploy record, replacing
a substantive `last_activity` sentence with a bare date. Edit the file directly. If any tooling
rewrites the curated position into a bare counter, restore it and report rather than committing
the corruption.

Update `last_activity` in the frontmatter to a substantive sentence naming the client project,
the proving run and the date — not a bare date. That field is deploy provenance; a previous
tool run destroyed one and it had to be reconstructed.
  </action>
  <acceptance_criteria>
1. `test -f .planning/phases/23.4-*/deferred-items.md` and `grep -cE '^#+ *DEF-23\.4-[0-9]+|^\*\*DEF-23\.4-' .planning/phases/23.4-*/deferred-items.md` returns at least `8`.
2. The deferred file contains all of: `client users`, `min-instances`, `GDPR`, `app_superadmin`, `restore`, `zonal`, `E2E`. One grep each, counts listed.
3. `grep -c 'state.advance-plan' .planning/STATE.md` is unchanged from its pre-edit value — the warnings were not removed. Record both counts.
4. `python -c "import re,sys; s=open('.planning/STATE.md',encoding='utf-8').read(); m=re.match(r'^---\n(.*?)\n---\n', s, re.S); sys.exit(0 if m and 'last_activity' in m.group(1) else 1)"` exits 0, and the `last_activity` value is longer than 40 characters and names `nestor-pulse-prod`.
5. `grep -c '23.4' .planning/STATE.md` returns at least `2`, and the Current Position block names the proving run's id.
6. `git diff .planning/STATE.md` shows the Current Position block and the frontmatter changed and NOTHING else — specifically, `git diff .planning/STATE.md | grep -c '^-'` is small and every removed line is inside the position block or the frontmatter; list the removed lines in the SUMMARY.
7. `.planning/` is gitignored: both files must be force-added (`git add -f`). Confirm with `git status --porcelain -- .planning/phases/23.4-*/deferred-items.md` showing the file staged.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'ls .planning/phases/23.4-*/deferred-items.md && grep -c "DEF-23.4-" .planning/phases/23.4-*/deferred-items.md && grep -c "state.advance-plan" .planning/STATE.md'</automated>
  </verify>
  <done>The next session opens STATE.md and knows the client environment exists, what proved it, and the nine things still owed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SUMMARY files -> the deploy record | Documentation only; the risk is a false claim, not an exploit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.4-21 | Repudiation | a deploy record that rounds attestation up to measurement | mitigate | Task 2 criterion 8 requires every digest, revision, UUID and run id in the record to be traceable to a SUMMARY; a value present only in the record is removed as a fabrication. Criterion 6 requires a NOT VERIFIED section to exist. |
| T-23.4-22 | Information disclosure | a secret pasted into documentation | mitigate | Task 1 criterion 5 and Task 2 criterion 7 grep for key and DSN shapes across both documents. |
| T-23.4-23 | Tampering | tooling rewriting the curated STATE position | mitigate | Task 3 forbids `state.advance-plan` by name, requires the existing warnings to survive, and requires `last_activity` to stay a substantive sentence. Criterion 6 bounds the deletions. |
| T-23.4-SC | Tampering | npm/pip/cargo installs | mitigate | No package added; this plan touches four documents. |
</threat_model>

<verification>
- `infra/BOOTSTRAP.md` contains every manual step from the eight runbook ranges, with the
  authorized-domains step positioned after the first frontend deploy.
- The runbook edit is an append with zero deletions.
- Every number in the 23.4 record traces to a SUMMARY file.
- No code, config or infrastructure changes in this plan.
</verification>

<success_criteria>
1. `infra/BOOTSTRAP.md` stands an environment up from zero as a numbered checklist including
   every recorded drift step (ROADMAP criterion 6).
2. `infra/DEPLOY-RUNBOOK.md` carries the 23.4 record and a promotion section (ROADMAP
   criterion 6).
3. `.planning/STATE.md` names the client environment, the proving run and the next action, with
   its existing warnings intact.
4. Every open operator call is written down with an id, including the two rulings 23.4-01 and
   23.4-04 could not make and the ones this phase deliberately fenced out.
</success_criteria>

<output>
Create `.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-05-SUMMARY.md` when done.
</output>
