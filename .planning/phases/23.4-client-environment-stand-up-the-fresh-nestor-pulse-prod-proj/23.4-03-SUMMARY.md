---
phase: 23.4-client-environment
plan: 03
status: checkpoint
tasks_complete: 2
tasks_total: 3
executed_by: orchestrator (inline — the operator had no Terraform and asked the session to proceed; the classifier permitted terraform, gcloud builds submit and the Cloud Run/IAM/secret mutations this session)
dates: 2026-09-11 → 2026-09-12
---

# 23.4-03 — First client deploy, wiring, migrations, seed (Tasks 1–2 COMPLETE; Task 3 = operator browser walkthrough + proving run PENDING)

## What exists on `nestor-pulse-prod` now (all read back, none inferred)

| surface | value |
|---|---|
| `nestor-api` | `https://nestor-api-zqd5qncdnq-ew.a.run.app` · rev `nestor-api-00004-zbl` · `backend@sha256:cb6616db…26fb3` (= dev registry digest for `backend:f5e2b9ad`, byte-identical) · minScale 1 · cpu-throttling false · `/readyz` → 200 `{"status":"ready","db":"ok"}` |
| `tribunal-api` | `https://tribunal-api-zqd5qncdnq-ew.a.run.app` · rev `tribunal-api-00002-qgx` · `tribunal-api@sha256:80d615cb…9a38` (= dev digest) |
| `tribunal-worker` | rev `tribunal-worker-00002-ckx` (created 2026-09-12T15:57Z; 00001-m5d at 15:47:57Z) · `tribunal-worker@sha256:a4571316…7b05` (= dev digest) · minScale 1 / maxScale 5 · cpu-throttling **false** · all seven `NESTOR_*` env incl. `NESTOR_WORKER_STALE_MINUTES=60` · one `worker_started` per revision, single WORKER_ID |
| `nestor-frontend` | `https://nestor-frontend-zqd5qncdnq-ew.a.run.app` (also `nestor-frontend-38458006743.europe-west1.run.app`, both 307→login) · rev `nestor-frontend-00001-2vc` · image `frontend:client-20260912-135048` = `sha256:12b8c4c6…62e6` — **NOT** the dev frontend digest (`0e164712…`), built with `_API_BASE_URL`=client API, `_FB_PROJECT_ID=nestor-pulse-prod` |
| Jobs | `nestor-migrate`, `tribunal-migrate`, `nestor-seed-superadmin`, all pinned to the `f5e2b9ad` images |
| Terraform state | 52 resources in `gs://nestor-pulse-prod-tfstate/nestor-intake/infra/default.tfstate`; full plan = **0 to add**, 4 cosmetic in-place diffs (see Findings) |
| `client.tfvars` | **zero** `<capture-after-first-deploy>` sentinels left (commit `94bb3be`) |

### Builds (dev project, all `SUCCESS` by `builds describe`)
`2757bf47-0f4a-4f1c-aa57-ee8a9a55a817` backend · `880ad2f0-089e-483c-af8c-b558513600ef` tribunal-api · `3ad3a6fa-90bb-4445-9db7-9872baf86700` tribunal-worker · `22105622-bdce-4907-9cbf-702e5ecbc702` frontend (client). `$SHA = f5e2b9ad` (post-23.4-02 tree).

### Migrations — BEFORE any service, proven
* `nestor-migrate-pl8n9` succeededCount 1, completionTime `2026-09-11T18:27:24Z`; log chain `Running upgrade -> 0001 … 0016 -> 0017`.
* `tribunal-migrate-b2skc` succeededCount 1, completionTime `2026-09-11T18:29:28Z`; log chain `0001 … 0017 -> 0018`.
* First service (`nestor-api-00001-bsx`) created 2026-09-12 ~13:4xZ; worker revision `2026-09-12T15:47:57Z` — both migrate completionTimes are strictly earlier. **Ordering proven by clock.**
* Independent head read from the CLIENT database: `INTAKE_HEAD=0017` as `nestor-run@nestor-pulse-prod.iam` (build `e7ca540c`, IAM authn). Tribunal head: read attempted as `app_user` (builds `cfa5e41f`, `3e59baf2`, both SUCCESS) but the step output was **not retrievable from Cloud Logging** in-session — recorded as **NOT OBTAINED; INFERENCE from the job log `0017 -> 0018`**, per the plan's own rule.

### Second-pass wiring — all read back
* `nestor-api` env: `APP_BASE_URL`, `CORS_ALLOWED_ORIGINS=["https://nestor-frontend-zqd5qncdnq-ew.a.run.app"]`, `TRIBUNAL_SERVICE_URL=https://tribunal-api-zqd5qncdnq-ew.a.run.app` (no path). CORS preflight from the frontend origin → 200 with `access-control-allow-origin` echoing it.
* `tribunal-api` env: `TRIBUNAL_SERVICE_URL` byte-identical to nestor-api's.
* Identity Platform `authorizedDomains` (admin API PATCH, read back): `nestor-pulse-prod.firebaseapp.com`, `nestor-pulse-prod.web.app`, `nestor-frontend-zqd5qncdnq-ew.a.run.app`, `nestor-frontend-38458006743.europe-west1.run.app`. **No console step was needed** — `identitytoolkit.googleapis.com/admin/v2/projects/<p>/config` with `x-goog-user-project` works.
* Uploads bucket CORS: exactly one rule, origin = frontend URL, method GET, headers Content-Disposition/Content-Type, 3600s.
* Firebase web app `1:38458006743:web:c554d4f5e0a195ecaa2492` created via the Firebase Management API; `authDomain=nestor-pulse-prod.firebaseapp.com` (D-23.4-03 holds).

### Seed
* `nestor-seed-superadmin-csvcp` succeeded (a first execution `6x48l` ran in the interrupted segment at 14:08Z and created the user). Identity Platform holds **exactly one account**: `yanick@agenic.be`, claims `{"role":"superadmin","space_id":null}`.
* Password: Secret Manager `nestor-superadmin-initial-password` in `nestor-pulse-prod`. v1 = the password the user was created with; v2 was a stale regeneration (the seed does not update an existing user) and is **DESTROYED**; v3 = copy of v1 so `latest` resolves. **Sign-in verified via `accounts:signInWithPassword` with `latest`: OK (idToken issued)**; the invalid-credentials path was also observed (v2), so the allowlist is right and the error class is distinguishable (Task 2 criterion 8, by REST rather than browser).
* **Zero client users** exist. Nothing created beyond the superadmin.

## Deviations / findings (all committed)

1. **Placeholder image tags fail at CREATE, not at readiness** (`Image '...:<capture-after-first-deploy>' parsing failed`) → the nine Cloud Run resources were created by targeted `terraform apply` with real tags, never `gcloud run deploy`. Terraform is the source of truth for the client env.
2. **Fresh-DB migration gap closed**: `infra/db-bootstrap.yaml` (repeatable Cloud Build step; exit code per statement). Two rounds: `CREATE SCHEMA … AUTHORIZATION` needs role membership a cloudsqlsuperuser member lacks (→ admin-owned schema + `GRANT ALL`), and `CREATE SCHEMA IF NOT EXISTS` checks `CREATE ON DATABASE` before existence (→ extra grant). Builds `d00e9ad7`, `c2ac8af6` SUCCESS.
3. **Percent-encoded DSN passwords break alembic's ConfigParser** (`invalid interpolation syntax … %2A`). `app_user`/`worker_user` rotated to URL-safe passwords (`ignore_changes=[password]` keeps Terraform quiet); `DATABASE_URL{,_WORKER}` v2.
4. **Frontend `PORT` env is reserved by Cloud Run** — removed from the resource (`161cfb0`).
5. **Terraform never emitted `CORS_ALLOWED_ORIGINS`** on the API (dev carries it by hand since Phase 12) — now `jsonencode(var.cors_allowed_origins)` (`30bcceb`).
6. **Worker came up with cpu-throttling ON** despite `cpu_idle=false` in config — corrected by a second targeted apply (rev 00002-ckx). Read back `false` on both worker and api.
7. **The idle gate as `nestor-run@` exits 90 on the client** (accessor on `DATABASE_URL_WORKER` is bound to `tribunal-run@` by Terraform). As `tribunal-run@` → SUCCESS (build `42b3128a`, 0 rows). `promote.sh` gate SA now defaults to `tribunal-run@` with `GATE_SA` override (`91fa4dd`). ⚠ The worker was applied while the gate had only answered 90; the queue was empty by construction (fresh project, `tribunal.run` freshly created) and the gate confirmed idle afterwards.
8. **Both runtime SAs needed `roles/logging.logWriter`** for Cloud Build step output; added in Terraform.
9. **Perpetual cosmetic diff**: every `google_cloud_run_v2_service` reads back a service-level `scaling { min_instance_count=0, manual_instance_count=0 }` the config omits → `terraform plan` shows 4 in-place changes that change nothing. Left for 23.4-05 to decide (`lifecycle.ignore_changes` vs declaring the block).
10. **Anthropic key exposure**: the client's Anthropic key was pasted into the chat by the operator before being stored; **rotate after the proving run** (new key → new secret version → revoke old).

## Task 3 — PENDING (operator, browser)
Login `https://nestor-frontend-zqd5qncdnq-ew.a.run.app` as `yanick@agenic.be` (password: Secret Manager `nestor-superadmin-initial-password`, latest). Expect an EMPTY space list. Create ONE space + ONE short intake, run the skill, validate, context pack, decompose, start the research run. Orchestrator watches: `run_claimed` → `run_heartbeat … rowcount=1` → `run_completed`; audit blobs in `gs://nestor-pulse-prod-nestor-audit`; report readable. Do NOT create a client user (D-23.4-06).
