---
phase: 23.4-client-environment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/research_routes.py
  - backend/tests/test_research_routes.py
  - frontend/src/lib/research/triggerOutcome.ts
  - frontend/src/lib/research/triggerOutcome.test.ts
  - frontend/src/lib/api/research.ts
  - frontend/src/components/research/RunActions.tsx
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/locales/en/intake.json
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/en/admin.json
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/fr/admin.json
  - tribunal/infrastructure/cloud-run/deploy-worker.sh
  - tribunal/infrastructure/cloud-run/deploy-api.sh
  - tribunal/infrastructure/cloud-run/build-and-push.sh
  - tribunal/infrastructure/cloud-run/artifact-registry-create.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-23.4-07: a cancelled research run no longer consumes one of the three failure-recovery attempts — an intake with 2 failures and 1 cancellation can still be retried."
    - "D-23.4-07: an over-cap trigger tells the operator the run needs investigation instead of showing a success toast and doing nothing."
    - "D-23.4-07: `NESTOR_WORKER_STALE_MINUTES` is 60 on every future worker deploy."
    - "A tribunal deploy script run without an explicit project FAILS instead of landing in the dev project (ROADMAP success criterion 4)."
  artifacts:
    - path: "frontend/src/lib/research/triggerOutcome.ts"
      provides: "the pure classifier both trigger call sites route through"
      exports: ["classifyTriggerOutcome", "TriggerOutcome"]
    - path: "frontend/src/lib/research/triggerOutcome.test.ts"
      provides: "the RED-first unit test for the needs_investigation branch"
    - path: "backend/tests/test_research_routes.py"
      provides: "test_attempt_cap_ignores_cancelled — the RED test for the cap change"
      contains: "cancelled"
  key_links:
    - from: "frontend/src/components/research/RunActions.tsx::handleFreshAttempt"
      to: "classifyTriggerOutcome"
      via: "the 202 body is classified before any toast is chosen"
      pattern: "classifyTriggerOutcome"
    - from: "frontend/src/routes/admin.pulse.intakes.$id.tsx::onStartAutoResearch"
      to: "classifyTriggerOutcome"
      via: "same classifier, same three branches"
      pattern: "classifyTriggerOutcome"
    - from: "backend/app/api/research_routes.py::trigger_research"
      to: "the attempt count"
      via: "prior runs filtered to exclude status == 'cancelled' before the >= _MAX_ATTEMPTS comparison"
      pattern: "cancelled"
---

<objective>
Ship the three owed fixes of D-23.4-07 and make the tribunal deploy scripts refuse to guess a
project.

Purpose: the client environment must not start life carrying known defects. Two of them are
in the paid-research path and are silent: a `cancelled` run — which spent nothing and was
stopped on purpose — burns one of the three failure-recovery attempts, and an over-cap
trigger returns HTTP 202 with `{"research_run_id": null, "status": "needs_investigation"}`,
which `res.success` reads as SUCCESS, so the operator gets a green toast and nothing happens.
The third is a config stopgap whose condition has passed. The fourth item is the guard that
stops a client release from landing in the dev project by default.

Output: one backend behaviour change with a RED test, one frontend classifier with a RED test
and two rewired call sites, six locale files, and four hardened shell scripts. NO deploy, NO
provider spend, NO gcloud command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-CONTEXT.md
@backend/app/api/research_routes.py
@frontend/src/lib/api/research.ts
@frontend/src/components/research/RunActions.tsx

<interfaces>
<!-- Read from the repo at HEAD 791b550. Use these directly — no exploration needed. -->

BACKEND — `backend/app/api/research_routes.py`:

* `_MAX_ATTEMPTS = 3` at :162, with the D-04 docstring above it.
* `_RETRYABLE_RUN_STATUSES = {"failed", "cancelled", "needs_input"}` at :158 — `cancelled` is
  ALREADY a legal retry trigger, which is exactly why counting it toward the cap contradicts
  the rest of the handler.
* Inside `trigger_research`, the cap block reads:
  `run_repo = ResearchRunRepository(repo.session, identity)` ->
  `prior = run_repo.list_for_intake(intake_id)` -> `if len(prior) >= _MAX_ATTEMPTS:` ->
  `_log.warning(...)` -> `return {"research_run_id": None, "status": "needs_investigation",
  "attempts": len(prior)}` (a 202, because the route carries
  `status_code=status.HTTP_202_ACCEPTED`).
* `prior` is reused BELOW the cap block for the retry path: `latest = prior[0] if prior else
  None` with `list_for_intake` ordering newest-first. Whatever you filter for the cap must NOT
  change what `latest` sees — a cancelled latest run is a legal retry trigger.
* The handler's docstring has a dedicated paragraph on why the cap stays on the pre-block
  read; it must be updated, not left contradicting the code.

BACKEND TESTS — `backend/tests/test_research_routes.py`:

* Marked `pytest.mark.integration`; auto-skips without Docker/DATABASE_URL.
* Helpers available: `_seed_space`, `_seed_intake(engine, set_space, space, intake_id,
  status=...)`, `_seed_decomposition_and_questions`, `_seed_research_run(engine, set_space,
  space_id, intake_id, run_id, status, attempt=1)` (:244), `_patch_engines`,
  `_patch_superadmin_engine`, `_build_app`, `_count_runs`, `_read_intake_status`, `_cleanup`,
  fixtures `fake_tribunal_client` and `fake_resend`.
* `test_attempt_cap_3` (:520) seeds THREE `status="failed"` runs and asserts
  `needs_investigation`. It must stay green unchanged.
* The module docstring carries a `| test | proves |` table that new tests are added to.

FRONTEND — `frontend/src/lib/api/client.ts`:
`export type ApiResult<T> = { success: true; data: T } | { success: false; error: string;
code?: string }`.

FRONTEND — `frontend/src/lib/api/research.ts:337`:
`export function triggerResearch(intakeId: string): Promise<ApiResult<{ research_run_id:
string }>>`. The declared `research_run_id: string` is a LIE about the over-cap body, and
that lie is what makes the silent branch type-check today.

FRONTEND — the two call sites, both of which treat `success: true` as "started":
* `components/research/RunActions.tsx::handleFreshAttempt` (:176-196) — `if (!res.success)`
  -> `fail(...)`; else `toast.success(t("research.actions.retryOk"))`, then
  `const freshId = res.data?.research_run_id;` and `if (freshId && freshId !== run.id)`
  navigate, ELSE `onReload()`. Over-cap therefore lands in the else: green toast, reload,
  nothing changed.
* `routes/admin.pulse.intakes.$id.tsx::onStartAutoResearch` (:808-821) — `if (!res.success)`
  -> `toast.error(...)`; else `toast.success(t("intakeDetail.toast.researchStarted"))` and
  `await load()`. Same silent branch.

FRONTEND TESTS — `vitest.config.ts` sets `environment: "node"` and
`include: ["src/**/*.test.ts"]` — `.ts` ONLY, no `.tsx`, no jsdom. Pure-function tests are
the house shape (`src/lib/research/*.test.ts`). Run with `npm test` from `frontend/`.

LOCALES — `research.actions.*` lives in `intake.json` (en :470-475 and the nl/fr mirrors);
`intakeDetail.toast.*` lives in `admin.json`. `scripts/i18n-audit.mjs` CHECK A is a HARD
3-way key-parity gate across nl/fr/en per namespace, so a key added to one locale and not the
others fails the gate.

SHELL — `tribunal/infrastructure/cloud-run/`:
* `build-and-push.sh:19` and `artifact-registry-create.sh:14`:
  `PROJECT="${GOOGLE_CLOUD_PROJECT:-project-cb01b861-cb4a-438d-b9a}"` — the default that
  silently targets dev.
* `deploy-api.sh:30` and `deploy-worker.sh:44` already use
  `PROJECT="${GOOGLE_PROJECT:?export GOOGLE_PROJECT to the intake project id}"` — correct,
  but the variable NAME differs from the other two scripts, and `build-and-push.sh` calls
  `artifact-registry-create.sh` so they must agree.
* `deploy-worker.sh:281` carries the single atomic `--set-env-vars=` line with seven
  variables, `NESTOR_WORKER_STALE_MINUTES=90` among them, and a ~50-line comment block at
  :216-247 explaining the stopgap and naming its revert condition.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The attempt cap stops counting cancelled runs</name>
  <files>backend/tests/test_research_routes.py, backend/app/api/research_routes.py</files>
  <read_first>
backend/app/api/research_routes.py lines 140-200 and 330-400 (the cap block, `_RETRYABLE_RUN_STATUSES`, and the handler docstring's cap paragraph)
backend/tests/test_research_routes.py lines 1-60 (module docstring table), 240-260 (`_seed_research_run`), 515-560 (`test_attempt_cap_3`)
.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-CONTEXT.md (D-23.4-07)
  </read_first>
  <behavior>
    - An intake with 3 prior runs, ALL `failed`, still caps: 202 `needs_investigation`, no `create_run` call, no status flip. (`test_attempt_cap_3`, unchanged, must stay green.)
    - An intake with 2 `failed` runs and 1 `cancelled` run is NOT capped: 202 with a non-null `research_run_id`, `create_run` called exactly once, a 4th row inserted.
    - An intake with 3 `cancelled` runs and 0 failures is NOT capped.
    - The `attempts` figure reported in the `needs_investigation` body counts the SAME set the cap counted — it may not report a number the cap did not use.
    - The retry path is unchanged: when the intake is already `in_research` and the newest run is `cancelled`, the trigger is still allowed (`_RETRYABLE_RUN_STATUSES` membership is untouched).
  </behavior>
  <action>
RED FIRST. Write the tests before touching `research_routes.py`, run them, and record the
failure text in the SUMMARY. The expected RED for the new case is the `needs_investigation`
body arriving where a `research_run_id` was asserted.

Add to `backend/tests/test_research_routes.py`:
`test_attempt_cap_ignores_cancelled` — seed 2 `failed` + 1 `cancelled` prior run on a
`decomposed` intake, POST, assert 202, assert `body["research_run_id"] is not None`, assert
`body.get("status") != "needs_investigation"`, assert `fake_tribunal_client["create_run"]`
is truthy, assert `_count_runs(...) == 4` and `_read_intake_status(...) == "in_research"`.
`test_attempt_cap_all_cancelled_never_caps` — seed 3 `cancelled` runs, same assertions.
Add both rows to the module docstring's `| test | proves |` table.

THEN change `trigger_research`. Introduce a module constant next to `_MAX_ATTEMPTS`:
`_CAP_EXEMPT_RUN_STATUSES = {"cancelled"}`, with a comment stating the reason — a cancelled
run is a DELIBERATE stop, it is already a legal retry trigger via
`_RETRYABLE_RUN_STATUSES`, and counting it toward a cap whose stated purpose (D-04) is
"a runaway retrigger must not re-charge Tribunal" punishes the one action that PREVENTS
spend. Do not fold it into `_RETRYABLE_RUN_STATUSES`; the two sets answer different
questions and will drift.

In the handler, compute a separate `counted = [r for r in prior if r.status not in
_CAP_EXEMPT_RUN_STATUSES]` and compare `len(counted) >= _MAX_ATTEMPTS`. `prior` itself stays
untouched so `latest = prior[0]` below still sees the true newest run — a cancelled latest is
still the retry trigger it is today. The `_log.warning` and the returned `attempts` figure
both use `len(counted)`, and the log line must additionally state how many were exempt so the
number in the response can be reconciled from logs.

Update the docstring bullet that describes the cap and the dedicated paragraph about the
pre-block read: both must now say the cap counts NON-cancelled prior runs. Leave the
D-23.2-12 three-part idempotency paragraph alone — nothing here touches the CAS, the 0016
partial unique index, or the in-transaction `attempt` computation.
  </action>
  <acceptance_criteria>
1. RED, recorded before the source edit: `cd backend && python -m pytest tests/test_research_routes.py::test_attempt_cap_ignores_cancelled -x` FAILS, and the failure text is quoted in the SUMMARY. A skip is NOT a red — if the suite skips for want of Docker/DATABASE_URL, start the container and re-run; a skipped RED proves nothing.
2. GREEN after: `cd backend && python -m pytest tests/test_research_routes.py -x -q` reports 0 failed and 0 errors, with `test_attempt_cap_3`, `test_attempt_cap_ignores_cancelled` and `test_attempt_cap_all_cancelled_never_caps` all PASSED (not skipped).
3. `cd backend && python -m pytest tests -q` reports no NEW failures against the pre-change baseline, and the baseline count is recorded in the SUMMARY.
4. `grep -c '_CAP_EXEMPT_RUN_STATUSES' backend/app/api/research_routes.py` returns at least `3` (definition, filter, and the docstring or log reference).
5. `grep -n 'len(prior) >= _MAX_ATTEMPTS' backend/app/api/research_routes.py` returns nothing — the comparison no longer uses the unfiltered list.
6. `grep -n '"failed", "cancelled", "needs_input"' backend/app/api/research_routes.py` still matches — `_RETRYABLE_RUN_STATUSES` is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd backend && python -m pytest tests/test_research_routes.py -q 2>&1 | tail -5'</automated>
  </verify>
  <done>A cancelled run costs an intake nothing, three genuine failures still stop the spend, and both are pinned by tests that were red first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The frontend says "needs investigation" instead of a green toast and a no-op</name>
  <files>frontend/src/lib/research/triggerOutcome.ts, frontend/src/lib/research/triggerOutcome.test.ts, frontend/src/lib/api/research.ts, frontend/src/components/research/RunActions.tsx, frontend/src/routes/admin.pulse.intakes.$id.tsx, frontend/src/locales/en/intake.json, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/admin.json, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json</files>
  <read_first>
frontend/src/lib/api/research.ts lines 330-345 (`triggerResearch` and its declared response type)
frontend/src/lib/api/client.ts lines 20-40 (`ApiResult`)
frontend/src/components/research/RunActions.tsx lines 100-200 (`fail`, `handleResume`, `handleStop`, `handleFreshAttempt`)
frontend/src/routes/admin.pulse.intakes.$id.tsx lines 805-825 (`onStartAutoResearch`)
frontend/src/lib/research/feedRows.test.ts (the house shape for a pure-function vitest file)
frontend/src/locales/en/intake.json lines 468-476 (`research.actions`) and the `intakeDetail.toast` block in frontend/src/locales/en/admin.json
frontend/scripts/i18n-audit.mjs lines 1-40 (what CHECK A gates)
  </read_first>
  <behavior>
    - `classifyTriggerOutcome({ success: false, error: "…" })` -> `"error"`.
    - `classifyTriggerOutcome({ success: true, data: { research_run_id: "abc", status: undefined } })` -> `"started"`.
    - `classifyTriggerOutcome({ success: true, data: { research_run_id: null, status: "needs_investigation", attempts: 3 } })` -> `"needs_investigation"`.
    - `classifyTriggerOutcome({ success: true, data: { research_run_id: null } })` -> `"needs_investigation"` — a 202 with no run id is never reported as started, whatever the server called it. A future server-side label change must not resurrect the silent branch.
    - `classifyTriggerOutcome({ success: true, data: { research_run_id: "abc", status: "needs_investigation" } })` -> `"started"` — a real run id wins; the id is the thing the UI navigates to.
  </behavior>
  <action>
RED FIRST, in two independent ways, both recorded in the SUMMARY before any implementation.

RED (a) — the CONTRACT. `triggerResearch` is declared
`Promise<ApiResult<{ research_run_id: string }>>`, so the over-cap body is not even
representable and the silent branch type-checks. Write `triggerOutcome.test.ts` first; it
imports the widened response type from `@/lib/api/research` and constructs the over-cap
object. At HEAD `npx tsc --noEmit` FAILS on that construction (`null` is not assignable to
`string`, and `status` does not exist on the type). Quote that compiler error — it is the
defect stated in the type system, not a module-not-found artefact.

RED (b) — BEHAVIOUR. The same file's five cases fail to resolve `classifyTriggerOutcome`
until it exists. Record the vitest output.

THEN implement, in this order:

1. `frontend/src/lib/api/research.ts` — export
   `export type TriggerResearchResponse = { research_run_id: string | null; status?: string;
   attempts?: number }` and change `triggerResearch`'s return type to
   `Promise<ApiResult<TriggerResearchResponse>>`. Add a comment naming the backend truth: the
   over-cap path is a **202**, not a 4xx, so `res.success` is `true` and the caller MUST
   inspect the body. Leave `resumeResearch` and `cancelResearch` alone.

2. `frontend/src/lib/research/triggerOutcome.ts` — a pure module, no React, no i18n, no
   network: `export type TriggerOutcome = "started" | "needs_investigation" | "error";` and
   `export function classifyTriggerOutcome(res: ApiResult<TriggerResearchResponse>):
   TriggerOutcome`. Rule order, written as such in a comment: failure first; then a truthy
   `research_run_id` is `"started"`; then everything else is `"needs_investigation"`. The
   ABSENCE of a run id is the discriminator, NOT the `status` string — a body with a run id
   is a started run whatever it is labelled, and a body with none is not something to report
   as success. Note that this module exists because `vitest.config.ts` includes only
   `src/**/*.test.ts` in a node environment, so the branch must be testable without
   rendering a component.

3. `RunActions.tsx::handleFreshAttempt` — replace the `if (!res.success)` / else shape with a
   `switch` on `classifyTriggerOutcome(res)`. `"error"` keeps the existing
   `fail(res, "research.actions.retryError")`. `"needs_investigation"` calls
   `toast.warning(t("research.actions.needsInvestigation"))` and RETURNS without
   `onReload()` — a reload here is what made the no-op look like a refresh.
   `"started"` keeps today's success toast, the `freshId` navigate and the `onReload()`
   fallback. Delete the now-false comment at :178-180 ("an over-cap request comes back as a
   rejection, surfaced below as a toast") and replace it with what is actually true.

4. `admin.pulse.intakes.$id.tsx::onStartAutoResearch` — same three branches;
   `"needs_investigation"` calls `toast.warning(t("intakeDetail.toast.researchNeedsInvestigation"))`
   and skips `await load()`.

5. Locales, all three languages, both namespaces:
   `intake.json` -> `research.actions.needsInvestigation`;
   `admin.json` -> `intakeDetail.toast.researchNeedsInvestigation`.
   The copy must say what happened and what to do, in the register the surrounding keys use
   (see `cancelConfirm` / `parkedBody`): this intake has already used its research attempts,
   no new run was started and nothing was charged, and it needs a person to look at it. Do
   NOT quote a number of attempts in the string — the cap is a server constant and a hard-coded
   "3" in three languages is the class of defect phase 23 shipped. Write nl and fr as real
   translations, not English with a different key.
  </action>
  <acceptance_criteria>
1. RED (a) recorded: `cd frontend && npx tsc --noEmit` FAILS at HEAD with the test file present and `research.ts` unmodified, on the `research_run_id: null` construction. The exact compiler message is quoted in the SUMMARY.
2. RED (b) recorded: `cd frontend && npm test` FAILS on `triggerOutcome.test.ts` before `triggerOutcome.ts` exists. Quoted in the SUMMARY.
3. GREEN: `cd frontend && npx tsc --noEmit` exits 0.
4. GREEN: `cd frontend && npm test` passes with a file count of 17 (baseline 16, one new) and a test count strictly greater than the recorded 266 baseline; both numbers stated in the SUMMARY. A flat count is a FAIL, not a note.
5. `cd frontend && node scripts/i18n-audit.mjs` exits 0 (CHECK A/B/C clean). CHECK D advisories may not increase above the recorded 106.
6. `grep -c 'classifyTriggerOutcome' frontend/src/components/research/RunActions.tsx` returns at least `1` AND `grep -c 'classifyTriggerOutcome' frontend/src/routes/admin.pulse.intakes.\$id.tsx` returns at least `1` — both call sites route through it, not one.
7. `grep -n 'research_run_id: string }' frontend/src/lib/api/research.ts` returns nothing — the narrow type is gone.
8. `grep -c 'needsInvestigation' frontend/src/locales/en/intake.json frontend/src/locales/nl/intake.json frontend/src/locales/fr/intake.json` returns `1` for each file; same for `researchNeedsInvestigation` across the three `admin.json` files.
9. `grep -riE '"[^"]*\b3\b[^"]*(attempt|poging|tentative)' frontend/src/locales/*/intake.json` returns nothing in the new keys — no hard-coded cap number.
10. `cd frontend && npm run build` succeeds. If it rewrites `src/routeTree.gen.ts`, restore that file before commit (the known CRLF phantom diff) and say so in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd frontend && npx tsc --noEmit && npm test 2>&1 | tail -8 && node scripts/i18n-audit.mjs | tail -5'</automated>
  </verify>
  <done>An over-cap trigger produces a warning naming the real outcome, in three languages, on both surfaces that can fire it, and the response type can no longer hide the branch.</done>
</task>

<task type="auto">
  <name>Task 3: The deploy scripts stop guessing a project, and STALE goes back to 60</name>
  <files>tribunal/infrastructure/cloud-run/deploy-worker.sh, tribunal/infrastructure/cloud-run/deploy-api.sh, tribunal/infrastructure/cloud-run/build-and-push.sh, tribunal/infrastructure/cloud-run/artifact-registry-create.sh</files>
  <read_first>
tribunal/infrastructure/cloud-run/build-and-push.sh lines 1-40
tribunal/infrastructure/cloud-run/artifact-registry-create.sh (whole file, ~40 lines)
tribunal/infrastructure/cloud-run/deploy-worker.sh lines 40-110 and 190-300 (the env comment block and the single atomic --set-env-vars line)
tribunal/infrastructure/cloud-run/deploy-api.sh lines 25-60
.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-CONTEXT.md (D-23.4-07, § Deploy scripts)
  </read_first>
  <action>
**(a) One required project variable, no default, in all four scripts.** Replace
`PROJECT="${GOOGLE_CLOUD_PROJECT:-project-cb01b861-cb4a-438d-b9a}"` in `build-and-push.sh`
and `artifact-registry-create.sh` with the same `:?` form the two deploy scripts already use,
and unify on ONE variable name so `build-and-push.sh` calling
`artifact-registry-create.sh` cannot disagree with it. Accept `GOOGLE_PROJECT` as the
canonical name and `GOOGLE_CLOUD_PROJECT` as a back-compat fallback, but with NO literal
default:
`PROJECT="${GOOGLE_PROJECT:-${GOOGLE_CLOUD_PROJECT:?set GOOGLE_PROJECT to the target project id — there is no default, and there must not be: the default used to be the DEV project}}"`.
Also drop the dev-project default baked into `build-and-push.sh:23`'s `RUNTIME_SA` — it
derives from `${PROJECT}` already, so once `PROJECT` is required this follows, but the
`nestor-pulse-runtime@` local part is a THIRD service-account name that matches neither
`nestor-run` nor `tribunal-run`; leave the value alone and add a one-line comment marking it
as belonging to the retired standalone project layout, so nobody reads it as current.

Head each of the four scripts with a short block stating the rule: these scripts deploy
PAID infrastructure and there are two projects now; a script that guesses lands a client
release in dev, or a dev experiment in front of the client.

**(b) Optional but loud account pinning.** In all four scripts, read
`GCLOUD_ACCOUNT="${GCLOUD_ACCOUNT:-}"`, build an argument array
(`ACCOUNT_ARGS=(); [ -n "$GCLOUD_ACCOUNT" ] && ACCOUNT_ARGS=(--account="$GCLOUD_ACCOUNT")`)
and expand it on every `gcloud` invocation in the script. When it is EMPTY, print a warning
to stderr naming the recorded incident: `gcloud auth login` has silently switched both
account and project mid-deploy on this machine, there are four accounts on it, and deploy
scripts inherit ambient config without complaining. Do not make it required — that would
break the operator's existing invocations for no safety the warning does not already buy.

**(c) `NESTOR_WORKER_STALE_MINUTES` 90 -> 60 in `deploy-worker.sh`.** Change the value inside
the single `--set-env-vars=` line at :281 and NOWHERE else; the line is one atomic
replacement and the other six variables must survive byte-identical. Then rewrite the ~30-line
stopgap comment at :216-247: it currently says "REVERT TO 60 once production logs show
`run_heartbeat` with `rowcount=1`". That happened — the root cause was two worker module
copies producing two WORKER_IDs, so every fenced write matched zero rows; the single-identity
launcher shipped 2026-09-09 and `rowcount=1` was observed on run `6668b27e`. Rewrite the block
to record that history in the past tense and state the standing rule: with a working heartbeat
a live run never goes stale at any value, so 60 recovers a genuinely dead worker fastest, and
it must stay strictly below `NESTOR_WORKER_RUN_TIMEOUT_MINUTES=120`. Keep the sentence that
this value must match `infra/variables.tf` (plan 23.4-01 moved that one). Do NOT touch
`NESTOR_WORKER_RUN_CONCURRENCY`, `NESTOR_WORKER_RUN_TIMEOUT_MINUTES`,
`NESTOR_TRIBUNAL_UNCAPPED`, `NESTOR_OPENAI_DR_MODEL`, `NESTOR_ENV` or
`NESTOR_WORKER_POLL_INTERVAL`, and do not add a second `--set-env-vars` flag — a second flag
REPLACES the first.

Preserve the queue-empty warning block at the end of that comment verbatim; plan 23.4-04's
idle gate is the automation of exactly that paragraph and the two must not disagree.
  </action>
  <acceptance_criteria>
1. `grep -rn 'project-cb01b861-cb4a-438d-b9a' tribunal/infrastructure/cloud-run/*.sh` returns NOTHING. (ROADMAP success criterion 4.)
2. Fail-closed, proven by execution, not by reading: `cd tribunal/infrastructure/cloud-run && env -u GOOGLE_PROJECT -u GOOGLE_CLOUD_PROJECT bash -n artifact-registry-create.sh && env -u GOOGLE_PROJECT -u GOOGLE_CLOUD_PROJECT bash artifact-registry-create.sh; echo "exit=$?"` prints a non-zero exit and a message naming `GOOGLE_PROJECT`. Repeat for `build-and-push.sh`, `deploy-api.sh`, `deploy-worker.sh` — four non-zero exits, quoted in the SUMMARY. These scripts must die at the variable expansion, BEFORE any `gcloud` call.
3. `grep -v '^ *#' tribunal/infrastructure/cloud-run/deploy-worker.sh | grep -c 'NESTOR_WORKER_STALE_MINUTES=60'` returns `1`, and the same filtered grep for `NESTOR_WORKER_STALE_MINUTES=90` returns `0`. (Filtered, because the comment block legitimately discusses 90 in the past tense.)
4. The atomic env line still carries all seven names: `grep -o 'NESTOR_[A-Z_]*=' tribunal/infrastructure/cloud-run/deploy-worker.sh | sort -u | wc -l` is unchanged from the pre-edit count, and the SUMMARY lists the seven.
5. `bash -n` exits 0 on all four scripts.
6. `grep -c 'ACCOUNT_ARGS' tribunal/infrastructure/cloud-run/deploy-worker.sh` returns at least `2`, and `grep -c 'gcloud ' tribunal/infrastructure/cloud-run/deploy-worker.sh` equals the number of `"${ACCOUNT_ARGS[@]}"` expansions plus any gcloud mentioned only inside comments — state both counts in the SUMMARY rather than asserting equality blindly.
7. `git diff --stat tribunal/` shows exactly four files changed and no file outside `tribunal/infrastructure/cloud-run/`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd tribunal/infrastructure/cloud-run && for f in *.sh; do bash -n "$f" || echo "SYNTAX FAIL $f"; done; grep -rc "project-cb01b861" *.sh'</automated>
  </verify>
  <done>No tribunal deploy script can target a project nobody named, and the next worker deploy carries a 60-minute stale window with the reason written down in the past tense.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> nestor-api `/intakes/{id}/research` | The one verb that SPENDS (~$25-45/run, uncapped). Superadmin-gated. |
| operator shell -> Cloud Run | The deploy scripts; a wrong project here is a client-visible incident. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.4-06 | Denial of service (self-inflicted) | the attempt cap | mitigate | The cap still counts every non-cancelled prior run; only the deliberate-stop status is exempt, and `_CAP_EXEMPT_RUN_STATUSES` is a named set with tests on both sides of it. A user cannot cancel-and-retry indefinitely without a superadmin token — the verb is superadmin-only (D-23.1-16). |
| T-23.4-07 | Repudiation | the over-cap response | mitigate | The `attempts` figure and the log line are computed from the SAME filtered list, so a support conversation can reconcile the number the operator saw with the number in the logs. |
| T-23.4-08 | Tampering | a deploy script landing in the wrong project | mitigate | Task 3(a) makes the project required with no default; criterion 2 executes each script with the variables unset and requires a non-zero exit before any gcloud call. |
| T-23.4-09 | Information disclosure | new locale strings leaking internals | accept | The copy names an operational state ("needs a person to look at it"); it quotes no id, no cost, no count. Reviewed as part of criterion 9. |
| T-23.4-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new dependencies in this plan. `npm ci` restores the committed lockfile only; no `npm install`. No package legitimacy audit is required because no package is added. |
</threat_model>

<verification>
- Both RED expectations (backend cap, frontend type + behaviour) are recorded with their
  failure text BEFORE the corresponding implementation, in the SUMMARY.
- `frontend/` gates: `npx tsc --noEmit` exit 0, `npm test` file count 17 and test count above
  266, `node scripts/i18n-audit.mjs` exit 0, `npm run build` succeeds.
- `backend/` gate: `python -m pytest tests -q` with no new failures against the recorded
  baseline.
- Nothing outside the seventeen declared files is modified. No deploy, no gcloud, no spend.
</verification>

<success_criteria>
1. An intake whose prior runs include a cancellation can still be retried (D-23.4-07).
2. Both trigger surfaces report `needs_investigation` as a warning in nl, fr and en, and
   neither reloads as if something started (D-23.4-07).
3. `NESTOR_WORKER_STALE_MINUTES=60` on the deploy path, with the revert reason recorded
   (D-23.4-07).
4. All four tribunal deploy scripts exit non-zero with no project set (ROADMAP criterion 4).
</success_criteria>

<output>
Create `.planning/phases/23.4-client-environment-stand-up-the-fresh-nestor-pulse-prod-proj/23.4-02-SUMMARY.md` when done.
</output>
