---
phase: 23.4-client-environment
plan: 04
status: complete
subsystem: release-pipeline
tags: [D-23.4-04, idle-gate, digest-promotion, cloud-build, branch-master, triggers-later]
requires:
  - "tribunal/infrastructure/cloud-run/deploy-api.sh + deploy-worker.sh as hardened by 23.4-02 (GOOGLE_PROJECT / IMAGE_TAG / GCLOUD_ACCOUNT)"
  - "frontend/cloudbuild.yaml (_IMAGE,_API_BASE_URL,_FB_API_KEY,_FB_AUTH_DOMAIN,_FB_PROJECT_ID)"
  - "Cloud Run services nestor-api / nestor-frontend / tribunal-api / tribunal-worker and jobs nestor-migrate / tribunal-migrate"
provides:
  - "infra/queue-check.yaml — the fail-closed tribunal run-queue gate (exit 0/90/91/92/93)"
  - "infra/promote.sh — the ordered, digest-pinned promotion with a smoke check and a deploy record"
  - "cloudbuild.deploy-dev.yaml — build the three images, then promote the dev project to itself"
  - "cloudbuild.promote-client.yaml — the tag-invoked client promotion, with NO image build"
affects:
  - "every future deploy in both projects"
  - "the 2026-07-28 worker-restart class of incident, which becomes a mechanical refusal"
tech-stack:
  added: []
  patterns:
    - "the answer carried in the EXIT STATUS because nestor-run@ has no logWriter and the build's stdout is lost"
    - "DRY_RUN=1 makes ZERO outbound calls, so the whole ordering is reviewable offline by an agent whose classifier blocks the real commands"
    - "build confirmation by `builds describe <FULL UUID>`, never an exit code through a pipe"
key-files:
  created:
    - infra/queue-check.yaml
    - infra/promote.sh
    - cloudbuild.deploy-dev.yaml
    - cloudbuild.promote-client.yaml
  modified: []
decisions:
  - "D-23.4-04 branch ruling ANSWERED: option `branch-master`. master is the dev branch; the release TAG is the boundary that protects the client."
  - "D-23.4-04 trigger ruling ANSWERED: option `triggers-later`. Both configs are committed and documented for by-hand invocation; NO trigger and NO GitHub connection were created."
  - "promote.sh gained a step 1b the plan did not ask for: the promoted digest must EXIST in the target registry, or the promotion aborts before any migration with the copy command printed. Without it, 'promotes the same digest' was an assertion rather than a check."
  - "The idle gate's exit code is read out of the BUILD, not out of gcloud's exit status — `gcloud builds submit` returns 1 for any failed build and does not hand back the step's code. MEASURED: the code is in steps[0].exitCode and failureInfo.detail; statusDetail is EMPTY."
  - "PROVEN 2026-09-11: build a8db291a (inverted) FAILED with exit 93 and build 88afa4a6 (real) SUCCEEDED on an empty dev queue. Both logged `connected as worker_user`, so the anti-vacuity check operated and the idle answer is not a blind one."
metrics:
  duration: ~95 min
  completed: 2026-09-11
  tasks: 3
  commits: 7
---

# Phase 23.4 Plan 04: promotion pipeline + the idle gate — Summary

A release is now one script whose ordering rules are enforced instead of remembered: digests
resolved before anything moves, a fail-closed queue gate before the first migration and again
immediately before the worker, the worker last, and a smoke check that says out loud when it
could not run.

**The gate has been seen refusing.** Build `a8db291a-c3d2-4fe4-8d5b-b8b8a0f4d343`, run
inverted against the dev project, FAILED with step exit code **93**; the un-inverted build
`88afa4a6-46b0-4346-a287-7e8be75b2216` SUCCEEDED on a genuinely empty dev queue. Both are
quoted in full below. That was the plan's last open criterion and it is closed.

No deploy, no gcloud mutation by me, no provider spend. The two proving builds were submitted by
the orchestrator and run only `SELECT`s. Nothing outside the plan's four `files_modified` was
touched; STATE.md and ROADMAP.md are untouched by design.

## What was built

### Task 1 — `infra/queue-check.yaml`, the fail-closed idle gate

A single-step `--no-source` Cloud Build config, run as the EXISTING `nestor-run@` (which
already holds `secretAccessor` on `DATABASE_URL_WORKER`, so no IAM change and no
authorized-network change), that answers one question in its **exit status**:

| code | meaning |
|------|---------|
| 0  | no row in `tribunal.run` is `queued` or `running` — safe to deploy the worker |
| 90 | proxy/psql/secret could not answer — UNKNOWN, therefore NOT SAFE |
| 91 | `current_user` is not `worker_user` — the anti-vacuity check |
| 92 | `tribunal.run` absent from `information_schema.tables` |
| 93 | one or more claimable rows |

Exit 91 is the one that matters most and it runs *before* the queue query. A tenant-scoped role
matches no `worker_all` RLS policy, returns a falsely empty queue, and a gate that cannot see
the rows always passes — it would have waved through every deploy while looking green.

The answer is the code and not the stdout because `nestor-run@` has no `roles/logging.logWriter`:
the build goes SUCCESS and `gcloud builds log` returns an empty REMOTE BUILD OUTPUT. The rows are
still printed, for a human who has the logs.

`_INVERT=1` flips the predicate to `NOT IN ('queued','running')` so every completed run counts as
claimable and the gate must refuse. It is documented, in the file, as a self-test knob that must
never be set during a promotion.

### Task 2 — `infra/promote.sh`

Ten steps, in the order the two recorded incidents dictate:

1. resolve digests in the source registry (a tag is mutable; the digest is the release identity)
1b. **confirm the same digest exists in the target registry** — added beyond the plan, see Deviations
2. the idle gate, at the top, before anything is applied
3a/3b. `nestor-migrate` then `tribunal-migrate`, each **repinned** with `jobs update --image`
4. `nestor-api` by digest, then `status.imageDigest` read back and compared
5. the frontend **rebuilt**, not promoted (Vite inlines `VITE_*` at build time)
6. `tribunal-api` via `deploy-api.sh`
7. **the idle gate again**, immediately before the worker
8. `tribunal-worker` via `deploy-worker.sh`, LAST
9. smoke: `/readyz`, `/auth/login`, one authenticated intake read
10. the promoted tags written back into the tfvars file — the only file the script edits

Two rules here have already cost real money, so neither is prose any more. A worker deploy boots
the poll loop, which CLAIMS FIRST and SLEEPS LAST, so `--min-instances=0` is not protection and
an empty queue is the only protection (2026-07-28) — hence the gate at 2 and 7 and the worker
last. And `gcloud builds submit | tail` reports the PIPE's status, so a FAILED build exits 0 —
hence every build confirmed by `builds describe <FULL UUID>`.

`DRY_RUN=1` makes **no outbound call at all**, which is load-bearing rather than tidy: step 1's
real `artifacts docker images describe` fails on a tag that does not exist, so a dry run with a
placeholder tag would die before reaching the ordering the dry run exists to show.

### Task 3 — the two Cloud Build configs

`cloudbuild.deploy-dev.yaml` builds backend, tribunal-api and tribunal-worker at the resolved
tag and then calls `promote.sh` with `SOURCE_PROJECT` and `TARGET_PROJECT` **both** set to the
dev project. Promoting the project to itself means the dev deploy and the client release run the
same ordered code path — gate included — so the client's path is exercised on every dev deploy
rather than once per release.

`cloudbuild.promote-client.yaml` has exactly ONE step and it is not a docker build. That absence
is the D-23.4-04 guarantee and it is stated at the top of the file.

## Rulings recorded

| Ruling | Option id | Where it is written down |
|--------|-----------|--------------------------|
| Dev branch | **`branch-master`** | `cloudbuild.deploy-dev.yaml` header block "THE BRANCH: master" |
| Triggers | **`triggers-later`** | header blocks of BOTH configs, "THERE IS NO TRIGGER YET" |

`triggers-later` means: both configs are committed, the by-hand `gcloud builds submit` invocation
is documented in each header, and **no trigger and no GitHub host connection were created**.
Plan 23.4-05 must carry to the operator to-do list: create the 2nd-gen GitHub host connection
(console OAuth, region `europe-west1`) and then a branch trigger on `master` for
`cloudbuild.deploy-dev.yaml` and a **tag** trigger on `client-v*` for
`cloudbuild.promote-client.yaml` — after the promotion has run end to end at least once.

## Acceptance criteria — every one, run literally

### Task 1

| # | Criterion | Result |
|---|-----------|--------|
| 1 | `exit 93`/`91`/`92`/`90` counts | `2 / 2 / 1 / 10` — all four implemented, not only described. PASS |
| 2 | `grep -c worker_user` >= 2 | `6`. PASS |
| 3 | `grep -c DATABASE_URL_WORKER` >= 1; `grep -v '^ *#' \| grep -c app_user` == 0 | `2` and `0`. PASS |
| 4 | `grep -c _INVERT` >= 2 | `6`. PASS |
| 5 | extract the step body, `bash -n` | exit `0`. PASS — but see deviation 5: this lint passed on the version Cloud Build REJECTED |
| 6 | no DSN literal | grep exit `1` (no match). PASS |
| 7 | `yaml.safe_load` | exit `0`. PASS |

Criterion 5's extraction command, recorded as required (the script body doubles every shell `$`
as `$$` for Cloud Build, so the doubling is undone first):

```
python -c "import yaml;s=yaml.safe_load(open('infra/queue-check.yaml'))['steps'][0]['args'][-1];open('/tmp/qc.sh','w',newline='\n').write(s.replace('\$\$','\$'))"
bash -n /tmp/qc.sh          # exit 0
```

`shellcheck` is NOT installed on this machine (`command -v shellcheck` empty), so the optional
shellcheck pass in the repo rules could not be run; `bash -n` was run on every extracted body.

### Task 2

| # | Criterion | Result |
|---|-----------|--------|
| 1 | `bash -n infra/promote.sh` | exit `0`. PASS |
| 2 | fail-closed on missing input, by execution | PASS, quoted below |
| 3 | ordering proven mechanically | PASS on the stage sequence; the criterion's literal first-occurrence form is UNSATISFIABLE — see below |
| 3a | 3 `would resolve digest for` lines, under 5 s | `3`, and **0.098 s real**. PASS |
| 4 | `grep -c queue-check` >= 2 | `3`. PASS (see note) |
| 5 | `grep -c '@sha256'` >= 3 | `6`. PASS |
| 6 | no bare-tag service deploy | `0`. PASS |
| 7 | `grep -c 'builds describe'` >= 1 | `5`. PASS |
| 8 | no key shapes; every `FB_API_KEY` line a variable reference | PASS, listed below |
| 9 | `grep -c 'Running upgrade'` >= 1 | `4`. PASS |
| 10 | `grep -c SKIPPED` >= 1 | `2`. PASS |

**Criterion 2, quoted.** `env -i bash infra/promote.sh` cannot work verbatim on this machine —
`env -i` clears `PATH`, so `bash` itself is not found (`env: 'bash': No such file or directory`,
exit 127). Run with the absolute interpreter, which is the same test:

```
$ env -i /usr/bin/bash infra/promote.sh; echo "exit=$?"
ERROR: SOURCE_PROJECT is required and has no default.
       The project whose Artifact Registry holds the built images.
exit=2

$ env -i SOURCE_PROJECT=a TARGET_PROJECT=b /usr/bin/bash infra/promote.sh; echo "exit=$?"
ERROR: IMAGE_TAG is required and has no default.
       The tag the three promotable images carry in SOURCE_PROJECT. Resolved to digests immediately; nothing is deployed by tag.
exit=2
```

**Criterion 3 — the literal form cannot be satisfied, and this is a criterion defect, not an
implementation gap.** The dry run was executed exactly as specified and exits `0` with
`IMAGE_TAG=deadbeef`, a tag that exists in no registry. Its output piped through the criterion's
own grep gives:

```
4:would resolve digest for .../nestor/backend:deadbeef
5:would resolve digest for .../nestor/tribunal-api:deadbeef
6:would resolve digest for .../nestor/tribunal-worker:deadbeef
12:==> STEP 2/10  idle gate (infra/queue-check.yaml) — run before anything is applied
14:==> STEP 3a/10 migrations first — nestor-migrate, REPINNED to the promoted backend digest
19:==> STEP 3b/10 migrations — tribunal-migrate, repinned to the promoted tribunal api digest
23:==> STEP 4/10  nestor-api at the promoted backend digest
27:==> STEP 6/10  tribunal-api via tribunal/infrastructure/cloud-run/deploy-api.sh
29:==> STEP 7/10  idle gate again (infra/queue-check.yaml) — immediately before the worker
31:==> STEP 8/10  tribunal-worker via ... deploy-worker.sh — LAST
33:==> STEP 9/10  smoke — /readyz, the login page, and one authenticated intake read
```

Read as "first occurrence of each token must ascend", it fails on two tokens, and **no
implementation can pass it while also passing 3a**:

* `tribunal-api` and `tribunal-worker` first occur at lines 5-6 — inside the three
  `would resolve digest for` lines whose wording and content criterion 3a mandates. Any script
  that names the images it is resolving hits this.
* `tribunal-api` also occurs at line 20 (not shown above, a command echo) because the
  `tribunal-migrate` job is legitimately repinned to the **tribunal-api image**. That is correct
  behaviour, not an ordering violation.

A raw substring scan therefore cannot answer the ordering question. What does answer it is the
sequence of **stage announcements** — the step-1 resolve lines and the `==> STEP n/10` headers,
excluding command echoes. That check is mechanical, asserts strict ascent, and fails loudly if a
stage moves:

```
line    3  STEP 1   announces: would resolve digest for
line   12  STEP 2   announces: queue-check
line   14  STEP 3a  announces: nestor-migrate
line   19  STEP 3b  announces: tribunal-migrate
line   23  STEP 4   announces: nestor-api
line   27  STEP 6   announces: tribunal-api
line   29  STEP 7   announces: queue-check
line   31  STEP 8   announces: tribunal-worker
line   33  STEP 9   announces: readyz
PASS: the stages run in the required order   (exit 0)
```

(Line 3 is the STEP 1 header; lines 4-6 are its three resolve lines. The gate appearing twice, at
12 and 29, is steps 2 and 7.)

**Criterion 3a, the offline claim, measured:**

```
$ time bash infra/promote.sh   # DRY_RUN=1, IMAGE_TAG=deadbeef, SKIP_FRONTEND=1
real    0m0.098s
user    0m0.046s
sys     0m0.045s
exit=0
```

0.098 s against a 5 s budget, with a tag no registry holds — the script cannot have reached the
network.

**Criterion 4, note.** `grep -c 'queue-check'` returns `3`. The gate is invoked through one
shared `gate()` function called twice, so the literal count is a proxy rather than the proof; the
real evidence is the runtime output above, which shows the gate submitted at lines 12-13 (step 2)
and again at 29-30 (step 7). Both call sites now name `infra/queue-check.yaml` in their comments
so the grep is a true signal instead of a coincidence.

**Criterion 8, every `FB_API_KEY` line listed:**

```
53:#   API_BASE_URL, FB_API_KEY, FB_AUTH_DOMAIN, FB_PROJECT_ID          (comment)
59:# FB_API_KEY is read from the ENVIRONMENT only. It is never written   (comment)
113:  need FB_API_KEY "${FB_API_KEY:-}" "Read from the environment ONLY..."  (variable ref)
373:  # FB_API_KEY is expanded by the shell into the argument list...     (comment)
375:  show "... _FB_API_KEY=<from-env> ..."                              (placeholder, no value)
380:  --substitutions="..._FB_API_KEY=${FB_API_KEY}..."                  (variable ref)
```

`grep -riE 'AIza|sk-ant|sk-proj' infra/promote.sh` returns nothing (exit 1). The echoed form of
the frontend build command at line 375 carries `<from-env>` in place of the value, so the key
never reaches a log line.

### Task 3

| # | Criterion | Result |
|---|-----------|--------|
| 1 | both configs `yaml.safe_load` | exit `0`. PASS |
| 2 | no build step for the three promotable services | PASS, demonstrated below |
| 3 | `grep -c promote.sh` in both | `6` (deploy-dev) and `13` (promote-client). PASS |
| 4 | no key shape; `_FB_API_KEY` with no value | PASS, listed below |
| 5 | GATE-CAN-FAIL | **PASS** — build `a8db291a…`, exit code `93`, quoted below |
| 6 | un-inverted run recorded | **PASS** — build `88afa4a6…`, SUCCESS, zero rows, quoted below |
| 7 | both rulings recorded by option id | PASS — `branch-master`, `triggers-later` (table above) |

**Criterion 2, demonstrated rather than asserted.** Every match of
`(backend|tribunal-api|tribunal-worker)` in `cloudbuild.promote-client.yaml`, with its line —
there is exactly one, and it is a comment:

```
8:# three promotable services — the intake backend image, the tribunal api image
```

`grep -cE 'builds submit|docker'` returns `2`, both comments, listed:

```
7:# There is therefore exactly ONE step below, and it is not a docker build.
31:#   gcloud builds submit . \        (the by-hand invocation in the header)
```

And the parsed structure, which is the strongest form of the check — one step, on the cloud-sdk
image, and **no `images:` key at all** (so nothing is built or pushed):

```
$ python -c "import yaml;d=yaml.safe_load(open('cloudbuild.promote-client.yaml'));print([(s.get('id'),s.get('name')) for s in d['steps']]);print('images key present:', 'images' in d)"
[('promote', 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim')]
images key present: False
```

**Criterion 4:** `grep -riE 'AIza' cloudbuild.promote-client.yaml` returns nothing (exit 1).
`_FB_API_KEY` appears on three lines: the header's by-hand invocation (`<client firebase web
key>`, a placeholder), the substitution declaration `_FB_API_KEY: ''` carrying no value, and the
env mapping `FB_API_KEY=${_FB_API_KEY}`.

Beyond the plan's criteria, all six inline step scripts across both configs were extracted (with
the `$$` doubling undone) and `bash -n`'d: `resolve-tag`, `build-backend`, `build-tribunal-api`,
`build-tribunal-worker`, `promote-to-dev`, `promote` — all exit 0.

## Deviations from Plan

### 1. [Rule 2 — missing critical functionality] promote.sh step 1b: the promoted digest must exist in the target registry

- **Found during:** Task 2, writing step 1.
- **Issue:** The plan resolves digests in `SOURCE_PROJECT` and then deploys the tribunal services
  through `deploy-api.sh` / `deploy-worker.sh`, which build their image URL from
  `${TARGET_PROJECT}` **and address it by TAG**. When the two projects differ, that image is not
  in the target registry unless it was copied there, and "we promoted the same digest" would have
  been an unchecked claim — the failure would have surfaced as a Cloud Run pull error halfway
  through a release, after the migrations had already run.
- **Fix:** a step 1b that verifies each resolved digest is present in the target registry and,
  when it is not, aborts **before the first migration** with the exact
  `gcloud artifacts docker images copy` command printed. When `SOURCE == TARGET` (the dev
  self-promotion path) it is trivially satisfied. Belt and braces: the digest is also read back
  off the live revision after every deploy and a mismatch aborts.
- **Files modified:** `infra/promote.sh`
- **Commit:** `dc27c85`

### 2. [Rule 1 — bug] the idle gate's exit code is not gcloud's exit code

- **Found during:** Task 2, writing the gate wrapper.
- **Issue:** `gcloud builds submit` returns `1` for ANY failed build. It does not hand back the
  build step's exit status, so the plan's "map the code to Task 1's table" would have mapped
  every refusal to the same unreadable `1` — losing exactly the distinction (93 = runs in flight,
  91 = the gate is blind) the code scheme exists to make.
- **Fix:** on failure the wrapper recovers the build UUID from the submit output, reads
  `builds describe <UUID> --format='value(status,statusDetail)'`, and parses the `exited with
  status N` code out of the detail. If the UUID or the code cannot be read, it **refuses** —
  "the check errored" is never "the check passed".
- **Files modified:** `infra/promote.sh`
- **Commit:** `dc27c85`

### 3. [Rule 1 — bug] `resolve_digest` printed into a command substitution

- **Found during:** Task 2 self-review before the dry run.
- **Issue:** the first draft called `DIGEST=$(resolve_digest backend)`, which would have
  **captured** the `would resolve digest for ...` lines instead of printing them — criterion 3a
  would have counted `0`, and the dry run's whole purpose (an offline, readable plan) would have
  silently evaporated.
- **Fix:** the function sets `RESOLVED_DIGEST` instead of echoing.
- **Commit:** `dc27c85`

### 4. [Rule 1 — bug] `eval "$(python3 ...)"` reports success on empty output

- **Found during:** Task 1 self-review.
- **Issue:** in `queue-check.yaml`, `if ! eval "$(parser)"` cannot fail: `eval` of an empty string
  exits 0, so a dead parser would fall through with `PGUSER` unset. (It would have been caught one
  step later as a connection failure — exit 90, still fail-closed — but the reported cause would
  have been wrong.)
- **Fix:** capture into `PGENV`, check status and emptiness, then `eval`; the parser also raises
  when the DSN carries no user.
- **Commit:** `2e26a70`

### 5. [Rule 1 — bug, found by the FIRST REAL SUBMIT] Cloud Build substitutes inside COMMENTS

- **Found during:** the operator's first `gcloud builds submit` of `infra/queue-check.yaml`.
- **Symptom:** the submit was REJECTED before any step ran:
  `invalid value for 'build.substitutions': key in the template "NAME" is not a valid built-in
  substitution`.
- **Cause:** my own escaping NOTE at the top of the step body — the comment explaining that every
  shell dollar is doubled — contained a bare `$NAME` and a `${_NAME}` as *examples*. Cloud Build
  performs substitution on the whole step-body string, **comments included**; it does not know
  what a shell comment is. The note describing the rule violated the rule.
- **Fix:** reworded the note so no single dollar is followed by a name. Committed on master as
  `32b9982` before I resumed; not redone here.
- **Why criterion 5 could never have caught it:** `bash -n` lints the extracted body, and *a
  comment is legal bash*. The lint passed on a file Cloud Build refused outright. The gap is
  that the criterion tests the wrong consumer — bash sees the body second, Cloud Build sees it
  first.
- **What now covers it:** a scan that classifies every `$` in the step body and asserts that the
  only substitution-shaped tokens are the six declared `_PROJECT`-style keys. Run on the final
  file:

```
single-dollar tokens Cloud Build will try to substitute:
  body line   7  '$\\$'  key=None          <- '$' before a backslash: not a key, left alone
  body line   7  "$',"   key=None
  body line   7  "$')"   key=None
  body line  14  '${_PROJECT}'       key=_PROJECT
  body line  15  '${_REGION}'        key=_REGION
  body line  16  '${_INSTANCE}'      key=_INSTANCE
  body line  17  '${_SECRET}'        key=_SECRET
  body line  18  '${_INVERT}'        key=_INVERT
  body line  19  '${_PROXY_VERSION}' key=_PROXY_VERSION

PASS: every single-dollar token is one of the six declared substitutions   (exit 0)
```

  Only `$` followed by a name character is a key to Cloud Build; `$(`, `$'` and `$\` are left
  alone, which is why the extraction command on line 7 survives and why the build then ran.

### 6. [Rule 1 — a committed claim that was false] trap 1: the build output is NOT lost

- **Found during:** reading back the two proving builds.
- **Issue:** `infra/queue-check.yaml` asserted, on the 2026-07-28 runbook's authority, that
  `nestor-run@` has no `logWriter` so "the build's stdout is LOST" and `gcloud builds log`
  returns an EMPTY REMOTE BUILD OUTPUT. Both proving builds ran as `nestor-run@` and their logs
  came back **complete** — every `==>` line and all 20 rows. The premise has changed.
- **Also wrong:** the same block said the code appears in the build's `statusDetail`. It does
  not; `statusDetail` came back empty and the code lives in `steps[0].exitCode` /
  `failureInfo.detail`.
- **Fix:** the header now records both the old claim and what was measured, keeps the
  exit-code design (which is right *because* it does not depend on log readability), and gives
  the working `--format='value(steps[0].exitCode,failureInfo.detail)'` read-back.
- **Deliberately NOT changed:** the design. A comment was wrong; the mechanism was not.
- **Commit:** `e2b466d`

### 7. [Deliberate, documented] `_IMAGE_TAG` is not defaulted to the literal `$SHORT_SHA`

The plan specifies `_IMAGE_TAG` with default `$SHORT_SHA`. Cloud Build does **not** expand
substitutions inside substitution DEFAULTS, so that default would arrive as the ten-character
string `$SHORT_SHA` and every image in a by-hand build would be tagged with it. `deploy-dev` uses
`_IMAGE_TAG: ''` plus a `resolve-tag` step that falls back to the built-in `$SHORT_SHA` (which a
trigger supplies and a by-hand `builds submit` leaves empty) and **fails the build** when neither
exists.

### 8. [Deliberate, documented] the gate installs `postgresql-client` when the image lacks it

The threat model's T-23.4-SC line says the gate "uses the base image's psql". It does not have
one — **now measured, not assumed**: both proving builds logged `==> installing
postgresql-client`, so `cloud-sdk:slim` ships no postgres client. Had the file trusted the threat
model's wording, the gate would have exited 90 on every single invocation while looking like a
correctly wired safety check — a gate that always refuses is as useless as one that always
passes, and rather more likely to get switched off.

The step installs `postgresql-client` from the image's own distro **only when `psql` is absent**,
and treats a failed install as exit 90 (UNKNOWN, not safe). No repository dependency is added;
this is an ephemeral build-step package, and the `cloud-sql-proxy` version IS pinned (`v2.14.3`,
confirmed on both builds' substitutions) as T-23.4-SC requires.

### 9. [Verified beyond the plan] promote.sh step 10, exercised against the real `infra/env/client.tfvars`

No criterion covers the tfvars write-back, and it is the one place the script mutates a file, so
it was run against a copy of the real client tfvars that plan 23.4-01 landed while I was working:

```
53:image_tag          = "20260911-abc123"
54:frontend_image_tag = "20260911-abc123"
114:tribunal_image_tag = "20260911-abc123"
```

`diff --strip-trailing-cr` against the original shows **exactly those three lines** changed — the
anchored pattern for `image_tag` correctly does NOT also match `frontend_image_tag`, column
alignment survives, and no other variable is touched. Two behaviours are now written into the
script's step-10 comment rather than left to be discovered: the trailing
`# <capture-after-first-deploy>` note is replaced along with the value (it stops being true), and
`sed -i` writes LF, so running promote.sh from git-bash on this Windows checkout re-lines the
whole file. Inside Cloud Build, where it actually runs, everything is LF already.

## Threat Flags

None. No new network surface, no new auth path, no schema change. The gate's database access is
read-only `SELECT`s as an existing role via an existing secret binding (T-23.4-20, accepted).

## Criteria 5 and 6 — the gate has been seen refusing

Two builds, both `--no-source`, both as `nestor-run@` against the DEV project
`project-cb01b861-cb4a-438d-b9a`, both read-only (`SELECT`s only). Submitted by the orchestrator;
every figure below I read back myself with read-only `gcloud builds describe` / `builds log`.

### Criterion 5 — INVERTED (`_INVERT=1`): the gate MUST refuse

Build **`a8db291a-c3d2-4fe4-8d5b-b8b8a0f4d343`** — 2026-09-11 16:52:38Z → 16:53:28Z (50 s,
against a 180 s timeout). Substitutions as recorded on the build:
`_INSTANCE=nestor-pg;_INVERT=1;_PROJECT=project-cb01b861-cb4a-438d-b9a;_PROXY_VERSION=v2.14.3;_REGION=europe-west1;_SECRET=DATABASE_URL_WORKER`.

Submit output, verbatim:

```
BUILD FAILURE: Build step failure: build step 0 "gcr.io/google.com/cloudsdktool/cloud-sdk:slim" failed: step exited with non-zero status: 93
```

**The plan's criterion 5 names the wrong field, and this matters.** `statusDetail` is EMPTY on
this failure:

```
$ gcloud builds describe a8db291a-c3d2-4fe4-8d5b-b8b8a0f4d343 --format='value(status,statusDetail)'
FAILURE
```

A criterion that reads only `statusDetail` would therefore have recorded "FAILURE with no code" —
which the plan itself says is NOT acceptable, being indistinguishable from a broken config. The
code is in `steps[0].exitCode` and in `failureInfo.detail`:

```
$ gcloud builds describe a8db291a-c3d2-4fe4-8d5b-b8b8a0f4d343 \
    --format='value(steps[0].status,steps[0].exitCode,failureInfo.type,failureInfo.detail)'
FAILURE	93	USER_BUILD_STEP	Build step failure: build step 0 "gcr.io/google.com/cloudsdktool/cloud-sdk:slim" failed: step exited with non-zero status: 93
```

**Exit code 93. Criterion 5 is met** — on the corrected read path, which is now written into
`infra/queue-check.yaml`'s header so the next person does not repeat the dead end.

### Criterion 6 — UN-INVERTED: the real dev queue

Build **`88afa4a6-46b0-4346-a287-7e8be75b2216`** — 16:54:21Z → 16:54:59Z (38 s), `_INVERT=`
(empty), same service account and same pinned proxy `v2.14.3`.

```
$ gcloud builds describe 88afa4a6-46b0-4346-a287-7e8be75b2216 --format='value(status,steps[0].exitCode)'
SUCCESS
```

Exit 0. The log's final answer:

```
==> connected as worker_user (RLS worker_all policy - sees all tenants)
==> queue is idle: 0 rows queued or running. Safe to deploy the worker.
```

**The real answer for the dev project at 2026-09-11 ~16:55Z: the tribunal queue is empty.**

### What the two runs prove beyond the two criteria

The inverted run is not just "it can return non-zero". Its log shows the gate walked the entire
path and the **anti-vacuity check operated**:

```
==> queue-check: project=project-cb01b861-cb4a-438d-b9a instance=...:europe-west1:nestor-pg secret=DATABASE_URL_WORKER
==> installing postgresql-client
2026/09/11 16:53:12 [.../nestor-pg] Listening on 127.0.0.1:5432
2026/09/11 16:53:12 The proxy has started successfully and is ready for new connections!
==> connected as worker_user (RLS worker_all policy - sees all tenants)
==> _INVERT=1 - SELF-TEST MODE. Completed runs count as claimable;
    this run is EXPECTED to exit 93. Never set this during a promotion.
NOT SAFE - 20 claimable row(s) in tribunal.run:
d6bb3aae-33e7-49fd-aa35-57dc529e05b3 status=cancelled started=2026-07-28 08:22:57.784948+00 hb=2026-07-28 08:37:40.159925+00 reclaims=1
... 18 more ...
fa56778a-a18d-4538-b883-0cf7b076b81d status=completed_degraded started=2026-09-09 11:16:46.434969+00 hb=2026-09-09 12:33:29.946329+00 reclaims=0
A worker deploy would restart instances and kill these runs. Refusing.
ERROR: build step 0 "gcr.io/google.com/cloudsdktool/cloud-sdk:slim" failed: step exited with non-zero status: 93
```

Three things follow, and they are the ones that were worth buying:

1. **Exit 91 did not fire, in EITHER run.** Both logs carry `connected as worker_user`. The gate
   is demonstrably looking at the rows through the `worker_all` RLS policy, so the un-inverted
   SUCCESS is a REAL empty queue and not the falsely-empty answer a tenant-scoped role returns.
   A gate proven only from the failing side could still have been blind on the passing side.
2. **All 20 rows are terminal** (`cancelled`, `completed`, `completed_degraded`, `failed`) —
   consistent with the un-inverted run's zero. The two answers corroborate each other.
3. **The slim cloud-sdk image does NOT ship psql** — `==> installing postgresql-client` appears
   in the log. Deviation 6 below was the difference between a working gate and one that would
   have exited 90 on every single invocation while looking like a correctly wired safety check.

Codes 90, 91 and 92 remain unobserved (nothing broke), and `_PROJECT=nestor-pulse-prod` has never
been exercised — the client project has no database yet.

## What plan 23.4-05 must carry to the operator to-do list

1. **Create the two triggers** (ruling `triggers-later` deferred them): a 2nd-gen GitHub host
   connection in region `europe-west1`, then a **branch** trigger on `master` for
   `cloudbuild.deploy-dev.yaml` and a **tag** trigger on `client-v*` for
   `cloudbuild.promote-client.yaml` — after the promotion has run end to end at least once.
2. **Grant the build service accounts** what the two config headers list. Nothing in this plan
   granted any IAM.
3. **The gate has never run against `nestor-pulse-prod`.** It has no database yet. The first run
   there will exit 90 or 92 until 23.4-03's apply and migrations land — both are correct
   fail-closed answers, not faults.
4. **promote.sh has never executed a real promotion.** Everything proven here is either the
   offline dry run or the gate. The first live run should be the dev self-promotion path
   (`cloudbuild.deploy-dev.yaml`), watched, before any client release.

## Self-Check

Files claimed, verified on disk:

- FOUND: infra/queue-check.yaml
- FOUND: infra/promote.sh
- FOUND: cloudbuild.deploy-dev.yaml
- FOUND: cloudbuild.promote-client.yaml

Commits claimed, verified in `git log`:

- FOUND: 2e26a70 feat(23.4-04): fail-closed tribunal run-queue idle gate
- FOUND: dc27c85 feat(23.4-04): infra/promote.sh — ordered, digest-pinned promotion with a smoke check
- FOUND: ff012e6 feat(23.4-04): the two Cloud Build release configs (branch=master, triggers later)
- FOUND: 5d85178 docs(23.4-04): SUMMARY (PARTIAL) — promotion pipeline + idle gate
- FOUND: 32b9982 fix(23.4-04): queue-check.yaml — Cloud Build substitutes inside COMMENTS too (landed on master while I was paused; not redone)
- FOUND: e2b466d docs(23.4-04): correct two claims the first real gate runs disproved

Builds claimed, verified read-only with `gcloud builds describe`:

- FOUND: a8db291a-c3d2-4fe4-8d5b-b8b8a0f4d343 — FAILURE, `steps[0].exitCode` = 93
- FOUND: 88afa4a6-46b0-4346-a287-7e8be75b2216 — SUCCESS

The worktree was fast-forwarded to `origin/master` (`32b9982`) before this work; the merge was
clean and brought in plan 23.4-01's `infra/env/*` and `infra/*.tf`, which I did not touch. My
four files are the plan's `files_modified` exactly. STATE.md and ROADMAP.md are untouched.

## Self-Check: PASSED
