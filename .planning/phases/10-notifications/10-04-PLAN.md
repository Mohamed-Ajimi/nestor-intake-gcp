---
phase: 10-notifications
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
autonomous: false
requirements: [NOTIF-01, NOTIF-02]
files_modified:
  - frontend/src/lib/api/intakes.ts
  - frontend/src/lib/api/admin.ts
  - frontend/src/components/intake/RecipientPicker.tsx
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/components/admin/InviteUserDialog.tsx
  - frontend/src/routes/admin.users.tsx
  - frontend/public/agenic-logo.png

user_setup: []

must_haves:
  truths:
    - "Clicking a validation/reminder/results CTA opens a recipient picker listing the intake space's active members (loaded via listSpaceMembers → GET /intakes/{id}/members), all preselected; confirming sends and toasts success/failure."
    - "The picker's confirm CTA is disabled with an 'invite someone first' hint when the space has zero active members (D-07)."
    - "The InviteUserDialog success state and the member-list row both offer a 'send invitation mail' action calling the one invite-mail endpoint; the copy-link fallback stays (D-04/D-10)."
    - "The Agenic logo ships in frontend/public/ so mail templates can reference {app_base_url}/agenic-logo.png (D-15)."
  artifacts:
    - path: "frontend/src/components/intake/RecipientPicker.tsx"
      provides: "The one new UI element — member picker dialog"
      contains: "RecipientPicker"
    - path: "frontend/src/lib/api/intakes.ts"
      provides: "sendIntakeMail + listSpaceMembers seam functions"
      contains: "sendIntakeMail"
    - path: "frontend/src/lib/api/admin.ts"
      provides: "sendInviteMail seam function"
      contains: "sendInviteMail"
    - path: "frontend/public/agenic-logo.png"
      provides: "Mail logo static asset (D-15)"
  key_links:
    - from: "frontend/src/lib/api/intakes.ts listSpaceMembers"
      to: "GET /intakes/{id}/members (Plan 03)"
      via: "apiFetch to the members read endpoint"
      pattern: "/intakes/.*/members"
    - from: "frontend/src/routes/admin.pulse.intakes.$id.tsx onSendValidationMail"
      to: "sendIntakeMail via RecipientPicker"
      via: "picker → seam call → toast"
      pattern: "sendIntakeMail"
    - from: "frontend/src/components/admin/InviteUserDialog.tsx"
      to: "sendInviteMail"
      via: "success-state button"
      pattern: "sendInviteMail"
---

<objective>
Un-stub the three "komt in Phase 10" CTA handlers (validation / reminder / results) with the new `RecipientPicker` (fed by `listSpaceMembers`) and the `sendIntakeMail` seam; add the "send invitation mail" action to both the InviteUserDialog success state and the member-list rows via `sendInviteMail`; add the frontend API seam functions; and ship the Agenic logo static asset that the mail templates reference.

Purpose: This is the operator-facing surface of the phase — the recipient picker is the one genuinely new UI element; everything else swaps a toast stub for a real seam call behind buttons that are already wired.
Output: 2 seam-file additions, `RecipientPicker.tsx`, un-stubbed CTA handlers, invite-mail buttons, logo asset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-notifications/10-CONTEXT.md
@.planning/phases/10-notifications/10-RESEARCH.md
@.planning/phases/10-notifications/10-PATTERNS.md
@.planning/phases/10-notifications/10-03-SUMMARY.md

<interfaces>
<!-- Backend endpoints from Plan 03 the seam calls -->
GET  /intakes/{id}/members                                             → active members [{ id, email, name?: null }]  (Plan 03 Task 1 — the picker's list source)
POST /intakes/{id}/mail/validation | /mail/reminder | /mail/results    body: { recipients: string[] }  (membership ids)
POST /admin/users/{membership_id}/invite-mail

<!-- Frontend seam conventions to mirror (never fork client.ts) -->
From frontend/src/lib/api/intakes.ts:
  `import { apiFetch, type ApiResult } from "@/lib/api/client";`
  one thin function per backend route, returns ApiResult<T>, never throws
  e.g. `submitIntake(id) => apiFetch<Intake>(\`/intakes/${id}/submit\`, { method: "POST" })`
From frontend/src/lib/api/admin.ts:
  `inviteUser(input) => apiFetch<InviteResult>("/admin/users", { method: "POST", body: JSON.stringify({...}) })`
  `listUsers() => apiFetch<AdminUser[]>("/admin/users", { method: "GET" })`

<!-- CTA wiring already present -->
From frontend/src/components/intake/NextStepBanner.tsx:
  props onSendValidationMail / onSendValidationReminder / onSendResultsMail already wired to buttons
  BusyKey union includes "sendValidation" | "sendReminder" | "sendResults" (lines 33-38)
From frontend/src/routes/admin.pulse.intakes.$id.tsx:
  the 3 stubbed handlers at lines 571-577 and 621-623 ("komt in Phase 10" toasts)
  the working analog onGenerateContextPack at 579-597 (setBusyKey + seam call + toast pattern)
From frontend/src/components/admin/InviteUserDialog.tsx:
  success state at lines 112-144 (copyable actionLink + "Kopieer link" button at 134) — add the send button beside it
From frontend/src/routes/admin.users.tsx:
  users.map row rendering at 172 + deactivate/reactivate row actions — add a per-member "resend invite mail" action
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API seam functions + RecipientPicker component + logo asset</name>
  <files>frontend/src/lib/api/intakes.ts, frontend/src/lib/api/admin.ts, frontend/src/components/intake/RecipientPicker.tsx, frontend/public/agenic-logo.png</files>
  <read_first>
    - frontend/src/lib/api/intakes.ts (the apiFetch thin-function pattern; withActiveSpace usage)
    - frontend/src/lib/api/admin.ts (inviteUser/listUsers shapes; AdminUser type)
    - frontend/src/lib/api/client.ts (apiFetch + ApiResult — never fork)
    - frontend/src/components/admin/InviteUserDialog.tsx (the shadcn Dialog scaffold + state-reset-on-open + sonner toasts to mirror)
    - .planning/phases/10-notifications/10-03-SUMMARY.md (confirm the delivered shapes: `GET /intakes/{id}/members` returns active `{id, email}` rows; the send + invite-mail endpoints)
    - .planning/phases/10-notifications/10-PATTERNS.md (§ Frontend API seam, § RecipientPicker)
  </read_first>
  <action>
    In `frontend/src/lib/api/intakes.ts`, add:
    - `listSpaceMembers(intakeId: string): Promise<ApiResult<SpaceMember[]>>` calling `apiFetch(\`/intakes/${intakeId}/members\`, { method: "GET" })` — this hits the concrete `GET /intakes/{id}/members` endpoint delivered by Plan 03 Task 1 (active members of the intake's space, `{ id: string; email: string; name?: string | null }`). Define/export a `SpaceMember` type of that shape. Do NOT read 10-03-SUMMARY to "discover" the endpoint or surface a blocker — the endpoint is guaranteed by Plan 03 (this plan's `depends_on`).
    - `sendIntakeMail(intakeId: string, type: "validation" | "reminder" | "results", recipients: string[]): Promise<ApiResult<{...}>>` calling `apiFetch(\`/intakes/${intakeId}/mail/${type}\`, { method: "POST", body: JSON.stringify({ recipients }) })`.

    In `frontend/src/lib/api/admin.ts`, add `sendInviteMail(membershipId: string): Promise<ApiResult<{...}>>` calling `apiFetch(\`/admin/users/${membershipId}/invite-mail\`, { method: "POST" })`. All return the `ApiResult<T>` union; never throw.

    Create `frontend/src/components/intake/RecipientPicker.tsx` — a controlled shadcn `Dialog` (mirror InviteUserDialog's `open`/`onOpenChange` + `useEffect` reset-on-open) that on open calls `listSpaceMembers(intakeId)` to load the intake's active members (`{id, email, name?}`), renders each as a shadcn checkbox row (label = name ?? email), PRESELECTS all active members (D-07), and on confirm calls back with the selected membership ids. Show a loading state while the fetch is in flight and an inline error toast on fetch failure. When the resolved member list is empty, DISABLE the confirm CTA and show a Dutch "nodig eerst iemand uit" ("invite someone first") hint (D-07). Dutch copy, `font-mono uppercase` label style matching InviteUserDialog. NEVER offer a free-text address field (D-06).

    Add the Agenic logo to `frontend/public/agenic-logo.png` (D-15) — the mail templates reference `{app_base_url}/agenic-logo.png`. If the binary asset is not available in-repo, place a placeholder and surface a note that the real logo must be dropped in before UAT (do not block).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `intakes.ts` exports `listSpaceMembers(intakeId)` doing a GET to `/intakes/{id}/members` and returning `ApiResult<SpaceMember[]>`, and `sendIntakeMail(intakeId, type, recipients)` posting `{ recipients }` to `/intakes/{id}/mail/{type}`; both return `ApiResult`.
    - `admin.ts` exports `sendInviteMail(membershipId)` posting to `/admin/users/{id}/invite-mail`.
    - `RecipientPicker.tsx` loads members via `listSpaceMembers`, renders member checkboxes preselected, disables confirm on an empty list with a Dutch hint, and has no free-text address input.
    - `frontend/public/agenic-logo.png` exists.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Seam functions (incl. listSpaceMembers → GET /intakes/{id}/members) + RecipientPicker + logo asset exist and typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: Un-stub the 3 CTA handlers + wire the RecipientPicker</name>
  <files>frontend/src/routes/admin.pulse.intakes.$id.tsx</files>
  <read_first>
    - frontend/src/routes/admin.pulse.intakes.$id.tsx (the 3 stubs at 571-577/621-623; onGenerateContextPack at 579-597; the busy-key + intake state)
    - frontend/src/components/intake/NextStepBanner.tsx (BusyKey keys sendValidation/sendReminder/sendResults; the CTA props)
    - frontend/src/components/intake/RecipientPicker.tsx (from Task 1)
    - .planning/phases/10-notifications/10-PATTERNS.md (§ un-stub 3 handlers)
  </read_first>
  <action>
    In `frontend/src/routes/admin.pulse.intakes.$id.tsx`, replace the three `toast.message("... komt in Phase 10.")` stub handlers (`onSendValidationMail`, `onSendValidationReminder`, `onSendResultsMail`) with the working pattern from `onGenerateContextPack`: each opens the `RecipientPicker` (which self-loads the intake's active members via `listSpaceMembers(intake.id)`), and on confirm sets the matching busy key (`sendValidation`/`sendReminder`/`sendResults` — the exact keys NextStepBanner already reads), calls `sendIntakeMail(intake.id, <type>, selectedIds)`, and toasts success/failure (`res.success` → `toast.success`, else `toast.error(...res.error)`), clearing the busy key in `finally`. Mount the `RecipientPicker` in the route's render tree with controlled open state (track which of the three types is active so the confirm calls the right `sendIntakeMail` type). Keep the NextStepBanner CTA props unchanged (already wired).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - None of the three handlers contain a "komt in Phase 10" string.
    - Each handler opens the RecipientPicker and, on confirm, calls `sendIntakeMail` with the intake id, the correct type, and selected membership ids, then toasts.
    - The busy keys `sendValidation`/`sendReminder`/`sendResults` are set/cleared around the send.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The three CTAs send real mail through the picker with success/failure toasts.</done>
</task>

<task type="auto">
  <name>Task 3: Invitation-mail action in InviteUserDialog + member list</name>
  <files>frontend/src/components/admin/InviteUserDialog.tsx, frontend/src/routes/admin.users.tsx</files>
  <read_first>
    - frontend/src/components/admin/InviteUserDialog.tsx (success state at 112-144; the copy-link button at 134; the actionLink/props)
    - frontend/src/routes/admin.users.tsx (users.map rows at 172; deactivate/reactivate row actions to sit beside)
    - frontend/src/lib/api/admin.ts (sendInviteMail from Task 1)
    - .planning/phases/10-notifications/10-PATTERNS.md (§ InviteUserDialog send-mail button)
  </read_first>
  <action>
    In `frontend/src/components/admin/InviteUserDialog.tsx`, add a "Verstuur uitnodigingsmail" `Button` in the success state next to "Kopieer link" (line 134) that calls `sendInviteMail(<membershipId of the just-invited user>)` and toasts success/failure (D-10). Keep the copy-link fallback (D-04). The dialog needs the invited membership id — thread it from the invite response (InviteResult) or the membership row; if the invite response lacks a membership id, surface that to the orchestrator rather than guessing.

    In `frontend/src/routes/admin.users.tsx`, add a per-member "Herstuur uitnodiging" row action (beside deactivate/reactivate) that calls `sendInviteMail(u.id)` and toasts. One endpoint serves both surfaces (D-10). Update the outdated copy in InviteUserDialog's success state (lines 121-124/133 "e-mailverzending wordt later toegevoegd (Fase 10)" / "HANDMATIG BEZORGEN — nog geen e-mail") to reflect that the mail action now exists.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - InviteUserDialog success state has a send-invitation-mail button calling `sendInviteMail`; the copy-link fallback remains.
    - admin.users.tsx member rows have a resend-invite action calling `sendInviteMail(u.id)`.
    - The stale "komt later / Fase 10" copy is updated.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Invite mail is sendable from both the dialog and the member list via one endpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual/functional verification of the send surfaces</name>
  <files>frontend/src/components/intake/RecipientPicker.tsx, frontend/src/routes/admin.pulse.intakes.$id.tsx</files>
  <read_first>
    - frontend/src/components/intake/RecipientPicker.tsx
    - frontend/src/routes/admin.pulse.intakes.$id.tsx
    - .planning/phases/10-notifications/10-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <action>
    Pause after the wiring tasks for a human to run the frontend locally against the deployed backend and
    confirm the recipient-picker behavior (preselect, empty-space guard, toast) and both invite-mail
    surfaces. Real Resend delivery + Firebase click-through are proven in the phase-level live UAT, not here.
  </action>
  <what-built>
    The recipient picker, the three un-stubbed CTAs, and the two invite-mail surfaces — all wired to the
    Plan-03 endpoints. Real Resend delivery + Firebase click-through are proven in the phase-level live UAT
    (VALIDATION.md Manual-Only), not here.
  </what-built>
  <how-to-verify>
    1. Run the frontend locally (npm, localhost:8081 per MEMORY) against the deployed backend rev.
    2. Open an intake at the awaiting-validation phase; click "Verstuur validatie-link" — confirm the RecipientPicker opens with the space's active members preselected.
    3. Confirm the send CTA is disabled with an "invite someone first" hint for a space with zero active members.
    4. Confirm a send shows a success (or a clear failure) toast.
    5. Open the invite dialog success state and the member list — confirm both show a working "send/resend invitation mail" action alongside the copy-link fallback.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the picker preselects active members, the empty-space CTA is disabled with a hint, sends toast, and both invite-mail surfaces are present.
  </acceptance_criteria>
  <verify><human-check>Human confirms picker preselect + empty-guard + toast + both invite-mail surfaces.</human-check></verify>
  <done>The send surfaces are visually/functionally confirmed by a human.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker → send endpoint | admin selects membership ids; the browser never supplies a raw recipient address (D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Spoofing | free-text recipient in the picker (send-to-anyone) | mitigate | RecipientPicker offers only server-provided membership rows (from `listSpaceMembers` → `GET /intakes/{id}/members`) — no free-text address input (D-06); recipients are membership ids the backend re-validates. |
| T-10-12 | Info disclosure | action link surfaced/logged in the frontend | accept | The invite action link is displayed in the dialog for the copy-link fallback (existing Phase-5 behavior, D-04); it is a short-lived single-use Firebase code, not a data-bearer token (D-09). |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes.
- Grep: no "komt in Phase 10" string remains in admin.pulse.intakes.$id.tsx.
- Human checkpoint (Task 4) confirms picker behavior + invite-mail surfaces.
</verification>

<success_criteria>
- The three CTAs send real mail through a members-only recipient picker (fed by GET /intakes/{id}/members) with preselect + empty-guard.
- Invite mail is sendable/resendable from both the dialog and the member list.
- The logo asset ships for the mail templates.
</success_criteria>

<output>
Create `.planning/phases/10-notifications/10-04-SUMMARY.md` when done.
</output>
