---
phase: 10-notifications
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [NOTIF-01, NOTIF-02]
files_modified:
  - backend/pyproject.toml
  - backend/app/mail/__init__.py
  - backend/app/mail/resend.py
  - backend/app/mail/render.py
  - backend/app/mail/templates/_base.html.j2
  - backend/app/mail/templates/validation.html.j2
  - backend/app/mail/templates/results.html.j2
  - backend/app/mail/templates/admin_validated.html.j2
  - backend/app/mail/templates/invite.html.j2
  - backend/app/core/config.py
  - backend/tests/conftest.py
  - backend/tests/test_mail_render.py

user_setup: []

must_haves:
  truths:
    - "Every rendered non-invite mail body contains the intake id in its CTA and NO client_validation_token / client_results_token / bearer-token-shaped segment (NOTIF-01)."
    - "The invite mail body is the only template that carries an action link (D-09)."
    - "app.mail.resend.send() reads RESEND_API_KEY from os.environ at call time and is monkeypatchable as the single test seam."
    - "Jinja2 autoescapes interpolated project title / client name (email-context XSS guard)."
  artifacts:
    - path: "backend/app/mail/resend.py"
      provides: "Resend HTTPS POST transport; call-time secret; the fake seam"
      contains: "def send"
    - path: "backend/app/mail/render.py"
      provides: "Jinja2 Environment + per-type render functions"
      contains: "autoescape"
    - path: "backend/app/mail/templates/_base.html.j2"
      provides: "Shared inline-CSS layout (logo, card, tag, btn)"
    - path: "backend/tests/test_mail_render.py"
      provides: "NOTIF-01 token-absence + invite-only-link render assertions"
      contains: "def test_"
  key_links:
    - from: "backend/app/mail/render.py"
      to: "backend/app/mail/templates/*.html.j2"
      via: "FileSystemLoader over templates/"
      pattern: "FileSystemLoader"
    - from: "backend/app/mail/resend.py"
      to: "os.environ RESEND_API_KEY"
      via: "read at call time inside send()"
      pattern: "os.environ\\[.RESEND_API_KEY.\\]"
---

<objective>
Build the self-contained backend `app/mail/` module — the Resend transport, the Jinja2 render layer, the ported Dutch HTML templates — plus the two non-secret `Settings` fields (admin address, app base URL), the `jinja2` dependency, the `fake_resend` conftest fixture, and the Wave-0 render tests that lock NOTIF-01 (no token in any non-invite mail).

Purpose: This module is the contract every send endpoint (Plan 03) and the frontend (Plan 04/05) build on. Getting the token-absence invariant into a rendered-output test HERE means every downstream endpoint inherits a proven NOTIF-01 guard.
Output: `backend/app/mail/` (transport + render + 5 templates), 2 new `Settings` fields, `jinja2` dep, `fake_resend` fixture, `test_mail_render.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-notifications/10-CONTEXT.md
@.planning/phases/10-notifications/10-RESEARCH.md
@.planning/phases/10-notifications/10-PATTERNS.md

<interfaces>
<!-- The call-time-secret transport analog the executor MUST mirror -->
From backend/app/ai/clients.py (call-time secret discipline):
  key read INSIDE the function via `os.environ["ANTHROPIC_API_KEY"]` — never module-level, never in Settings, never logged.

Legacy parity source (port HTML/subjects, swap the URLs):
From docs/supabase-functions/send-pulse-mail.ts:
  - FROM = "Nestor Pulse <nestor@agenic.be>"  (line 12 — keep unchanged, D-13)
  - Resend POST: POST https://api.resend.com/emails, headers Authorization: Bearer <key> + Content-Type: application/json, body {from,to,subject,html} (lines 86-90)
  - styles() inline CSS block (lines 118-130): card border-top #BFEC40, .tag #FF2D87, .btn #141414
  - buildValidationHtml (lines 132-149) with isReminder greeting/intro branch (lines 133-136)
  - buildResultsHtml (lines 151-170)
  - buildAdminValidatedHtml (lines 172-183)
  - Dutch subjects (lines 61-77): validation "Even valideren — onderzoeksvragen voor {clientName}",
    reminder "Herinnering — onderzoeksvragen wachten op validatie ({clientName})",
    results "Onderzoeksresultaten klaar — {clientName}",
    admin_validated "[Nestor Pulse] Klant heeft gevalideerd — {clientName}"

Config field pattern to copy:
From backend/app/core/config.py:
  `storage_bucket: str | None = None` (env STORAGE_BUCKET; non-secret; read via get_settings()) — line 74
  Do NOT add resend_api_key here (it is a secret; read from os.environ in resend.send()).

Conftest fake-seam analog to copy:
From backend/tests/conftest.py:
  `fake_gcs` fixture (lines 676-755) — capture-only, monkeypatch the seam, `pytest.importorskip("app.storage.gcs")`; returns a calls dict.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify jinja2 package legitimacy before install</name>
  <files>backend/pyproject.toml</files>
  <read_first>
    - .planning/phases/10-notifications/10-RESEARCH.md (§ Package Legitimacy Audit — jinja2 tagged [ASSUMED], slopcheck unavailable at research time)
    - backend/pyproject.toml (confirm jinja2 is NOT already declared; httpx + firebase-admin ARE)
  </read_first>
  <action>
    Pause for a human legitimacy check on the one NEW backend dependency (`jinja2`) before any install,
    per the RESEARCH § Package Legitimacy Audit gate (slopcheck was unavailable at research time). Do NOT
    add jinja2 to pyproject.toml until this checkpoint is approved.
  </action>
  <what-built>
    No code yet — this is the legitimacy gate the RESEARCH audit mandates for the one NEW backend
    dependency (`jinja2`). httpx and firebase-admin are already declared and need no gate.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/Jinja2/ — confirm it is the Pallets project (source repo github.com/pallets/jinja), MIT-licensed, multi-year release history, very high download count.
    2. Confirm the current stable line is 3.x; note the exact latest 3.x version to pin as `jinja2>=3.1,<4`.
    3. Confirm there is no typo-squat concern (canonical name is `Jinja2` / import `jinja2`).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms `Jinja2` on PyPI is the Pallets project with a 3.x stable release and approves the `jinja2>=3.1,<4` pin, OR supplies the exact version to pin.
  </acceptance_criteria>
  <verify><human-check>Human approves the jinja2 pin (or supplies the exact version).</human-check></verify>
  <done>The jinja2 pin is approved by a human before any install proceeds.</done>
  <resume-signal>Type "approved" (optionally with an exact version) or describe the concern</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add jinja2 dependency + config fields + Resend transport + Jinja2 render + templates</name>
  <files>backend/pyproject.toml, backend/app/core/config.py, backend/app/mail/__init__.py, backend/app/mail/resend.py, backend/app/mail/render.py, backend/app/mail/templates/_base.html.j2, backend/app/mail/templates/validation.html.j2, backend/app/mail/templates/results.html.j2, backend/app/mail/templates/admin_validated.html.j2, backend/app/mail/templates/invite.html.j2</files>
  <read_first>
    - backend/pyproject.toml (the dependencies array — add jinja2 beside httpx/firebase-admin)
    - backend/app/ai/clients.py (the call-time-secret transport discipline to mirror in resend.py)
    - backend/app/core/config.py (Settings — add two non-secret fields after storage_bucket at line 74)
    - docs/supabase-functions/send-pulse-mail.ts (the FULL parity source: styles(), the 3 build*Html builders, subjects, Resend POST shape)
    - .planning/phases/10-notifications/10-RESEARCH.md (§ Pattern 1 Resend transport, § Pattern 2 Jinja2 render, § Pattern 3 CTA route selection)
    - .planning/phases/10-notifications/10-PATTERNS.md (§ resend.py, § render.py + templates — the exact URL swaps)
  </read_first>
  <action>
    Add `jinja2>=3.1,<4` (or the exact version approved in Task 1) to the dependencies array in `backend/pyproject.toml`.

    In `backend/app/core/config.py`, add two NON-secret fields to `Settings` after `storage_bucket` (line 74), mirroring its field+docstring style: `nestor_admin_email: str | None = None` (env `NESTOR_ADMIN_EMAIL`, D-08 — the single ops address `admin_validated` targets) and `app_base_url: str | None = None` (env `APP_BASE_URL`, the legacy `NESTOR_BASE_URL` analog — the origin for every mail CTA and the logo `<img>`). Do NOT add any `resend_api_key` field (it is a secret; read from os.environ in resend.send()).

    Create `backend/app/mail/__init__.py` (empty package marker).

    Create `backend/app/mail/resend.py` with a single `send(*, to: list[str], subject: str, html: str) -> str` function that mirrors `app/ai/clients.py`: read `os.environ["RESEND_API_KEY"]` INSIDE the function body (loud KeyError if absent; never module-level, never in Settings, never logged), POST to `https://api.resend.com/emails` via `httpx.post` (SYNC `httpx.Client`/`httpx.post`, not AsyncClient — handlers are sync-def/pg8000-threadpool) with `Authorization: Bearer <key>` + `Content-Type: application/json` and json body `{"from": FROM, "to": to, "subject": subject, "html": html}`, a 15s timeout, then `resp.raise_for_status()` and return `resp.json().get("id", "")`. Module constant `FROM = "Nestor Pulse <nestor@agenic.be>"` (D-13, unchanged). Include a module docstring naming this the ONE function the test suite monkeypatches (mirror fake_anthropic/fake_gcs).

    Create `backend/app/mail/render.py` with a module-level Jinja2 `Environment(loader=FileSystemLoader(<templates dir>), autoescape=select_autoescape(["html","j2"]))` and thin per-type render functions: `render_validation(*, first_name, project_title, cta_url, is_reminder) -> str`, `render_results(*, first_name, project_title, cta_url) -> str`, `render_admin_validated(*, client_name, project_title, cta_url) -> str`, `render_invite(*, cta_url) -> str`. Do NOT use FastAPI `Jinja2Templates`/`TemplateResponse` — this is pure string production.

    Create the templates under `backend/app/mail/templates/`. Port the legacy `styles()` inline CSS (card border-top #BFEC40, .tag #FF2D87, .btn #141414, .logo, .footer) into `_base.html.j2` as a shared layout the type templates extend/include; the logo `<img src>` MUST be `{{ app_base_url }}/agenic-logo.png` (D-15 — NOT the dying Supabase LOGO_URL). Port `buildValidationHtml` (with the isReminder greeting/intro branch) into `validation.html.j2`, `buildResultsHtml` into `results.html.j2`, `buildAdminValidatedHtml` into `admin_validated.html.j2`, and add `invite.html.j2` ("Kies je wachtwoord" set-password copy in Dutch with a "Stel je wachtwoord in" button). CTA swaps are MANDATORY (NOTIF-01): the validation CTA href is `{{ cta_url }}` where the caller passes `{app_base_url}/intake/{intake_id}` (NO `client_validation_token`); the results CTA is `{app_base_url}/intake/{intake_id}/results` (NO `client_results_token`); admin_validated CTA is `{app_base_url}/admin/pulse/intakes/{intake_id}`; invite CTA is the Firebase action link (the ONLY token any mail carries, D-09). Every interpolated value (`project_title`, `client_name`, `first_name`) goes through `{{ }}` so autoescape applies.
  </action>
  <verify>
    <automated>MISSING — proven by Task 3's test_mail_render.py in this same plan; author-by-construction (dev machine has no Python — suite runs in Cloud Build)</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` dependencies array contains a `jinja2` entry pinned `>=3.1,<4` (or the Task-1 version).
    - `backend/app/core/config.py` `Settings` has `nestor_admin_email` (env NESTOR_ADMIN_EMAIL) and `app_base_url` (env APP_BASE_URL), and has NO `resend_api_key` field.
    - `backend/app/mail/resend.py` `send()` reads `os.environ["RESEND_API_KEY"]` inside the function body; `RESEND_API_KEY` appears in no other line; `FROM` equals `"Nestor Pulse <nestor@agenic.be>"`; uses sync httpx (no `AsyncClient`).
    - `backend/app/mail/render.py` constructs a Jinja2 `Environment` with `autoescape` enabled and a `FileSystemLoader` over the templates dir; exports the four render_* functions.
    - Each of the 5 `.html.j2` templates exists; the logo `<img>` references `{{ app_base_url }}/agenic-logo.png` and no template references the legacy Supabase `inmsssedwdmgtnhaydmg.supabase.co` URL.
    - No non-invite template file contains the substrings `client_validation_token` or `client_results_token`.
  </acceptance_criteria>
  <done>Mail module, config fields, jinja2 dep, and 5 templates exist with the NOTIF-01 URL swaps applied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: fake_resend fixture + Wave-0 render tests (NOTIF-01 lock)</name>
  <files>backend/tests/conftest.py, backend/tests/test_mail_render.py</files>
  <read_first>
    - backend/tests/conftest.py (the fake_gcs fixture, lines 676-755 — copy its capture-only monkeypatch shape; importorskip)
    - backend/app/mail/render.py (the render functions under test — from Task 2)
    - backend/app/mail/resend.py (the send seam fake_resend patches)
    - .planning/phases/10-notifications/10-RESEARCH.md (§ Validation Architecture — Phase Requirements → Test Map; § Pitfall 3 token-in-CTA)
    - .planning/phases/10-notifications/10-VALIDATION.md (Per-Task Verification Map rows for NOTIF-01)
  </read_first>
  <behavior>
    - render_validation output contains the passed cta_url (an app route with the intake id) and does NOT contain "client_validation_token".
    - render_results output contains the results cta_url and does NOT contain "client_results_token".
    - render_admin_validated output contains "/admin/pulse/intakes/" and no token-shaped segment.
    - render_invite is the ONLY render that contains the action-link cta_url passed to it (invite-carries-link).
    - A rendered body with a project_title containing "<script>" has that value HTML-escaped (autoescape guard).
    - fake_resend captures {to, subject, html} per call and returns a deterministic fake message id; no test reaches real Resend.
  </behavior>
  <action>
    Add a `fake_resend` fixture to `backend/tests/conftest.py` mirroring `fake_gcs`: `pytest.importorskip("app.mail.resend")`, monkeypatch `app.mail.resend.send` with a capture-only fake that appends `{"to","subject","html"}` to a `calls` list and returns a fixed fake id (e.g. `"fake-resend-id"`); return the calls list/dict. Patch any package-level re-export too (raising=False) if `app.mail.__init__` re-exports `send`.

    Create `backend/tests/test_mail_render.py` asserting the six behaviors above by calling the render_* functions directly (no HTTP): pass a cta_url of the shape `https://app.example/intake/<uuid>` and assert the token substrings are absent; assert `render_invite` carries its action link; add `test_invite_carries_link`; add an autoescape test that passes `project_title="<script>x</script>"` and asserts the raw `<script>` is escaped in the output.
  </action>
  <verify>
    <automated>pytest backend/tests/test_mail_render.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_mail_render.py -x` passes in Cloud Build.
    - The suite includes `test_invite_carries_link` and an autoescape assertion.
    - `fake_resend` exists in conftest.py and is capture-only (records calls, returns a fake id, performs no network I/O).
  </acceptance_criteria>
  <done>Render tests green in Cloud Build; NOTIF-01 token-absence and invite-only-link locked at the render layer; fake_resend available for Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dynamic value → email HTML | project title / client name interpolated into a mail body (HTML-injection surface) |
| process env → Resend | RESEND_API_KEY held server-side; must never leak to logs or the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | render.py template interpolation | mitigate | Jinja2 `autoescape=True` escapes every `{{ var }}`; test asserts `<script>` in project_title is escaped. |
| T-10-02 | Information disclosure | resend.py RESEND_API_KEY | mitigate | Key read from `os.environ` at call time inside `send()`; never in Settings, never logged; asserted `RESEND_API_KEY` appears on no other line. |
| T-10-03 | Elevation / Info disclosure | non-invite templates carrying a bearer token (NOTIF-01 regression) | mitigate | Render tests assert no `client_validation_token`/`client_results_token` in non-invite bodies; invite is the only link-carrying template (D-09). |
| T-10-SC | Tampering | jinja2 install (new dependency) | mitigate | Task 1 blocking-human legitimacy checkpoint verifies jinja2 on PyPI (Pallets project) before install. |
</threat_model>

<verification>
- `pytest backend/tests/test_mail_render.py -x` green (Cloud Build).
- Grep: no non-invite `.html.j2` template contains `client_validation_token`/`client_results_token`; no template references the legacy Supabase logo URL.
- Grep: `RESEND_API_KEY` appears only inside `resend.py::send`.
</verification>

<success_criteria>
- `app/mail/` module renders all five mail types with NOTIF-01-compliant CTAs (intake-id routes, not tokens).
- `jinja2` dependency added behind an approved legitimacy checkpoint.
- Two non-secret Settings fields added; no secret field added.
- `fake_resend` fixture + render tests green — the NOTIF-01 invariant is test-locked for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/10-notifications/10-01-SUMMARY.md` when done.
</output>
