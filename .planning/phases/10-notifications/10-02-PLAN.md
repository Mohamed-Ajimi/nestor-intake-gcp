---
phase: 10-notifications
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [NOTIF-02]
files_modified:
  - infra/main.tf
  - infra/variables.tf
  - infra/DEPLOY-RUNBOOK.md

user_setup:
  - service: resend
    why: "Transactional email provider (D-13); RESEND_API_KEY is the only new secret this phase adds"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys (agenic.be sender domain is already verified)"
    dashboard_config: []

must_haves:
  truths:
    - "The Terraform config declares a RESEND_API_KEY Secret Manager secret + resource-scoped secretAccessor grant to the runtime SA + a Cloud Run secret_key_ref env, mirroring the anthropic_api_key block."
    - "NESTOR_ADMIN_EMAIL and APP_BASE_URL are declared as plain non-secret Cloud Run env vars."
    - "The deploy runbook records the manual gcloud steps to create the secret version, set the two env vars, and rebuild the image so jinja2 reaches Cloud Run (IaC-drift honest, Phase 8 D-07)."
  artifacts:
    - path: "infra/main.tf"
      provides: "resend_api_key secret + iam + env; NESTOR_ADMIN_EMAIL + APP_BASE_URL env"
      contains: "resend_api_key"
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "Phase 10 deploy step (secret version, env vars, image rebuild for jinja2)"
      contains: "RESEND_API_KEY"
  key_links:
    - from: "infra/main.tf runtime SA"
      to: "google_secret_manager_secret.resend_api_key"
      via: "secretmanager.secretAccessor iam_member"
      pattern: "resend_api_key"
---

<objective>
Land the infra + runbook changes this phase requires: the `RESEND_API_KEY` Secret Manager secret (drift-honest, value seeded out-of-band), its resource-scoped `secretAccessor` grant, and its Cloud Run `secret_key_ref` env; plus the two non-secret Cloud Run env vars `NESTOR_ADMIN_EMAIL` and `APP_BASE_URL`; and a DEPLOY-RUNBOOK step covering the manual secret-version + env-var + image-rebuild actions (so `jinja2` reaches the live image — the recurring deploy-gap).

Purpose: The mail module (Plan 01) and endpoints (Plan 03) are inert live until the secret and env vars exist on Cloud Run and the image is rebuilt with jinja2. Per Phase 8 D-07 the infra change lands in Terraform AND the runbook (IaC-drift is tracked, reconciled at Phase 12).
Output: `infra/main.tf` + `infra/variables.tf` additions, `infra/DEPLOY-RUNBOOK.md` Phase-10 section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-notifications/10-CONTEXT.md
@.planning/phases/10-notifications/10-RESEARCH.md
@.planning/phases/10-notifications/10-PATTERNS.md

<interfaces>
<!-- The exact analog block to copy for the new secret -->
From infra/main.tf:
  - google_secret_manager_secret.anthropic_api_key (lines 157-163) — secret resource, auto replication
  - google_secret_manager_secret_version.anthropic_api_key (lines 170-174) — count-guarded default-0 seed (value out-of-band per runbook)
  - google_secret_manager_secret_iam_member.runtime_anthropic_secret_accessor (lines 176-180) — resource-scoped secretAccessor to google_service_account.runtime.email
  - Cloud Run container env block: ANTHROPIC_API_KEY via value_source.secret_key_ref (lines 409-417); non-secret env like STORAGE_BUCKET (lines 386-389) and DB_NAME
  - Variable pattern: var.anthropic_api_key_secret_id + var.anthropic_api_key (the optional seed, default "")

Config field names the backend reads (from Plan 01 / config.py):
  - RESEND_API_KEY  (secret; read from os.environ at call time in resend.send())
  - NESTOR_ADMIN_EMAIL  (non-secret; Settings.nestor_admin_email, D-08)
  - APP_BASE_URL  (non-secret; Settings.app_base_url — the mail CTA + logo origin)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RESEND_API_KEY secret + IAM + env, and the two non-secret env vars</name>
  <files>infra/main.tf, infra/variables.tf</files>
  <read_first>
    - infra/main.tf (the anthropic_api_key secret/version/iam blocks at lines 157-180 and the Cloud Run env blocks at lines 386-425)
    - infra/variables.tf (the anthropic_api_key_secret_id + anthropic_api_key variable declarations to mirror)
    - .planning/phases/10-notifications/10-PATTERNS.md (§ infra/main.tf — the exact block-copy instruction)
  </read_first>
  <action>
    In `infra/variables.tf`, add `resend_api_key_secret_id` (string, default a stable id e.g. `"nestor-resend-api-key"`) and `resend_api_key` (string, default `""`, sensitive) mirroring the anthropic variable pair.

    In `infra/main.tf`, add a `google_secret_manager_secret.resend_api_key` (auto replication), a count-guarded `google_secret_manager_secret_version.resend_api_key` (`count = var.resend_api_key == "" ? 0 : 1`, drift-honest default 0 — the real key is added out-of-band per the runbook), and a `google_secret_manager_secret_iam_member.runtime_resend_secret_accessor` granting `roles/secretmanager.secretAccessor` on that secret to `serviceAccount:${google_service_account.runtime.email}` — a verbatim structural copy of the anthropic_api_key trio.

    In the Cloud Run service container env, add a `RESEND_API_KEY` env via `value_source.secret_key_ref { secret = google_secret_manager_secret.resend_api_key.secret_id, version = "latest" }` (mirror the ANTHROPIC_API_KEY env at lines 409-417). Add two PLAIN non-secret env vars beside STORAGE_BUCKET: `NESTOR_ADMIN_EMAIL` (value from a new `var.nestor_admin_email`, default `""`) and `APP_BASE_URL` (value from a new `var.app_base_url`, default `""`). Declare `nestor_admin_email` and `app_base_url` variables in variables.tf. Keep a comment noting these are IaC-drift-inert until the runbook `--update-env-vars` step sets them live (mirror the STORAGE_BUCKET drift comment).
  </action>
  <verify>
    <automated>grep -Eq 'google_secret_manager_secret[."]resend_api_key' infra/main.tf && grep -q 'runtime_resend_secret_accessor' infra/main.tf && grep -q 'RESEND_API_KEY' infra/main.tf && grep -q 'NESTOR_ADMIN_EMAIL' infra/main.tf && grep -q 'APP_BASE_URL' infra/main.tf && grep -q 'resend_api_key' infra/variables.tf && ! grep -RniE 're_[A-Za-z0-9]{8,}' infra/  (secret + iam + secret_key_ref env wiring present in Terraform; the two non-secret env vars present; no plaintext Resend key committed). Full terraform validate runs in the deploy runbook / CI — no local Terraform on the dev machine.</automated>
  </verify>
  <acceptance_criteria>
    - `infra/main.tf` contains `google_secret_manager_secret.resend_api_key`, a count-guarded `..._version.resend_api_key`, and `..._iam_member.runtime_resend_secret_accessor` scoped to the runtime SA.
    - The Cloud Run env block has a `RESEND_API_KEY` `secret_key_ref` (version `"latest"`) and plain `NESTOR_ADMIN_EMAIL` + `APP_BASE_URL` env vars.
    - `infra/variables.tf` declares `resend_api_key_secret_id`, `resend_api_key` (sensitive, default ""), `nestor_admin_email` (default ""), `app_base_url` (default "").
    - No plaintext RESEND_API_KEY value appears anywhere in `infra/*` (seed default is "").
  </acceptance_criteria>
  <done>The secret, grant, and three env vars are declared in Terraform mirroring the anthropic_api_key pattern.</done>
</task>

<task type="auto">
  <name>Task 2: DEPLOY-RUNBOOK Phase-10 deploy step</name>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md (existing structure — find the analogous ANTHROPIC/OPENAI secret + STORAGE_BUCKET env-var steps to mirror)
    - .planning/phases/10-notifications/10-RESEARCH.md (§ Runtime State Inventory — the RESEND_API_KEY secret, NESTOR_ADMIN_EMAIL/base-URL env, and the jinja2-image-rebuild pitfall)
  </read_first>
  <action>
    Add a Phase-10 section to `infra/DEPLOY-RUNBOOK.md` documenting the manual live steps (drift-honest, gcloud-native): (1) create the Resend secret version with the real key via `gcloud secrets versions add nestor-resend-api-key --data-file=-` (value pasted out-of-band, never committed); (2) set the two non-secret env vars on the live service via `gcloud run services update nestor-api --update-env-vars NESTOR_ADMIN_EMAIL=<ops address>,APP_BASE_URL=<deployed frontend origin>`; (3) map the RESEND_API_KEY secret to the service env via `--update-secrets RESEND_API_KEY=nestor-resend-api-key:latest` (if not applied by Terraform); (4) REBUILD the backend image via Cloud Build so `jinja2` is present in the running container (call out RESEARCH Pitfall 2 — green CI but a 500 `ModuleNotFoundError: jinja2` in UAT if skipped); (5) note the UAT gate: trigger each mail type (invite, validation, results, reminder, admin_validated) against the deployed rev and inspect the inbox. Reference that these are the IaC-drift items Phase 12 reconciles.
  </action>
  <verify>
    <automated>grep -q 'RESEND_API_KEY' infra/DEPLOY-RUNBOOK.md && grep -q 'NESTOR_ADMIN_EMAIL' infra/DEPLOY-RUNBOOK.md && grep -q 'APP_BASE_URL' infra/DEPLOY-RUNBOOK.md && grep -qi 'jinja2' infra/DEPLOY-RUNBOOK.md  (Phase-10 runbook section documents the RESEND_API_KEY secret, the two env vars, and the jinja2 image-rebuild step).</automated>
  </verify>
  <acceptance_criteria>
    - `infra/DEPLOY-RUNBOOK.md` contains a Phase-10 section referencing `RESEND_API_KEY`, `NESTOR_ADMIN_EMAIL`, `APP_BASE_URL`, and an explicit image-rebuild step calling out the jinja2 dependency.
    - The section states the RESEND_API_KEY value is added out-of-band (never committed).
    - The section lists the five mail-type UAT triggers.
  </acceptance_criteria>
  <done>Runbook documents the secret-version, env-var, image-rebuild, and UAT steps for Phase 10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Secret Manager → Cloud Run | RESEND_API_KEY injected as env via secret_key_ref; value never in IaC state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information disclosure | RESEND_API_KEY value in committed IaC/state | mitigate | Seed default `""` (count-0 version); real value added out-of-band per runbook; only the secret reference is in config. |
| T-10-05 | Elevation | over-broad secretAccessor grant | accept | Grant is resource-scoped to the single resend secret for the runtime SA only (mirrors anthropic pattern); no project-level secret access. |
</threat_model>

<verification>
- Grep `infra/main.tf` for `resend_api_key` (secret, version, iam), `RESEND_API_KEY`, `NESTOR_ADMIN_EMAIL`, `APP_BASE_URL`.
- Grep `infra/DEPLOY-RUNBOOK.md` for the Phase-10 tokens and the jinja2 rebuild step.
- Confirm no plaintext key value is committed.
</verification>

<success_criteria>
- Terraform declares the resend secret + grant + env and the two non-secret env vars, mirroring the established anthropic pattern.
- The runbook records the manual live steps including the jinja2 image rebuild and the five-mail UAT gate.
</success_criteria>

<output>
Create `.planning/phases/10-notifications/10-02-SUMMARY.md` when done.
</output>
