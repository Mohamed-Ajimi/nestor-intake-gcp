---
phase: 10-notifications
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
autonomous: true
requirements: [NOTIF-01, NOTIF-02]
files_modified:
  - backend/app/api/intake_routes.py
  - backend/app/api/admin_routes.py
  - backend/app/auth/admin_users.py
  - backend/app/db/admin_repo.py
  - backend/tests/test_mail_endpoints.py
  - backend/tests/test_mail_denial.py
  - backend/tests/test_intake_validate_mail.py

user_setup: []

must_haves:
  truths:
    - "An admin can GET /intakes/{id}/members and receive the intake space's ACTIVE membership rows ({id, email, name?}); a cross-space intake id returns 404 (existence-hidden, D-07). This is the read the RecipientPicker (Plan 04) lists from."
    - "An admin can POST /intakes/{id}/mail/validation|reminder|results with recipient membership ids; the server resolves ACTIVE-member emails, renders the token-free body, calls the faked Resend transport, and returns success."
    - "validation_link_sent_at / results_link_sent_at update ONLY on a successful (2xx) send, never on failure (D-16)."
    - "A cross-space intake send returns 404 (existence-hidden, D-07); no free-text recipient address is ever honored (D-06)."
    - "When submit_intake advances reviewed → validated_by_client, an admin_validated mail fires to Settings.nestor_admin_email; a mail failure NEVER fails the client's validate (D-03)."
    - "POST /admin/users/{membership_id}/invite-mail regenerates a fresh Firebase action link and sends the invite mail; the audit row never contains the link."
    - "The invite action link's continue URL is the /auth/action handler route (ActionCodeSettings, D-11)."
  artifacts:
    - path: "backend/app/api/intake_routes.py"
      provides: "GET /{id}/members read + 3 intake-scoped mail send endpoints + admin_validated auto-fire in submit_intake"
      contains: "mail/validation"
    - path: "backend/app/api/admin_routes.py"
      provides: "invite-mail endpoint"
      contains: "invite-mail"
    - path: "backend/app/auth/admin_users.py"
      provides: "generate_set_password_link with ActionCodeSettings continue URL"
      contains: "ActionCodeSettings"
    - path: "backend/tests/test_mail_endpoints.py"
      provides: "member-list read + send-endpoint contract + D-16 timestamp + no-free-address tests"
      contains: "def test_"
  key_links:
    - from: "backend/app/api/intake_routes.py GET /{id}/members"
      to: "OrganizationMembership (active rows for intake.space_id)"
      via: "repo.get(intake) 404-gate then active-member query via repo.session"
      pattern: "def .*members"
    - from: "frontend/src/lib/api/intakes.ts listSpaceMembers (Plan 04)"
      to: "GET /intakes/{id}/members"
      via: "apiFetch to the concrete read endpoint this plan creates"
      pattern: "/intakes/.*/members"
    - from: "backend/app/api/intake_routes.py send endpoints"
      to: "app.mail.resend.send"
      via: "render then send; timestamp on 2xx only"
      pattern: "resend\\.send"
    - from: "backend/app/api/intake_routes.py submit_intake"
      to: "app.mail.resend.send (admin_validated)"
      via: "guarded on new_status == validated_by_client, try/except"
      pattern: "validated_by_client"
    - from: "backend/app/api/admin_routes.py invite-mail"
      to: "admin_users.generate_set_password_link"
      via: "fresh link per send"
      pattern: "generate_set_password_link"
---

<objective>
Wire the backend send surface: the `GET /intakes/{id}/members` read endpoint that lists the intake space's active members (the concrete read the Plan-04 RecipientPicker lists from); three intake-scoped mail send endpoints (validation / reminder / results) with server-side active-member recipient resolution and the D-16 send-then-timestamp discipline; the automatic `admin_validated` operator mail fired inside `submit_intake` on the `reviewed → validated_by_client` edge (isolated so a mail failure never fails the client's validate); the `POST /admin/users/{membership_id}/invite-mail` endpoint; and the `ActionCodeSettings` continue-URL addition to `generate_set_password_link`. Full test coverage: member-list read (active-only, cross-space 404), endpoint contract, D-16 timestamp-on-success-only, no-free-address, cross-space 404 denial, and the admin_validated auto-fire / client-not-blocked behavior.

Purpose: These endpoints are the contract the frontend (Plan 04) calls. The member-list read feeds the RecipientPicker; the send endpoints prove recipients resolve only from active memberships, never leak cross-space, and stamp sent-at only on success. The NOTIF-01 render invariant is already locked by Plan 01.
Output: members read + send endpoints + admin_validated + invite-mail + ActionCodeSettings + 3 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-notifications/10-CONTEXT.md
@.planning/phases/10-notifications/10-RESEARCH.md
@.planning/phases/10-notifications/10-PATTERNS.md
@.planning/phases/10-notifications/10-01-SUMMARY.md

<interfaces>
<!-- The mail module from Plan 01 -->
From app.mail.resend: `send(*, to: list[str], subject: str, html: str) -> str`  (the faked seam)
From app.mail.render: `render_validation(*, first_name, project_title, cta_url, is_reminder)`,
  `render_results(*, first_name, project_title, cta_url)`,
  `render_admin_validated(*, client_name, project_title, cta_url)`,
  `render_invite(*, cta_url)`
Settings (get_settings()): `.nestor_admin_email` (D-08), `.app_base_url` (mail CTA + logo origin)

<!-- The intake router / repo / audit shape to mirror (from submit_intake, intake_routes.py) -->
From backend/app/api/intake_routes.py:
  - `intake_router = APIRouter(prefix="/intakes")`; handlers are sync `def`; NO auth dep on the router (mounted under protected_router)
  - `submit_intake(intake_id, repo: IntakeRepository = Depends(get_tenant_repo), identity: Identity = Depends(get_current_identity))` (line 680)
  - repo.get(intake_id) → None ⇒ `raise HTTPException(404, "Intake not found")` (existence-hidden, D-07)
  - `_SUBMIT_TRANSITIONS = {"draft":"submitted","reviewed":"validated_by_client"}` (line 649) — validated_by_client is the admin_validated trigger edge
  - `repo.patch(intake_id, **values)` returns rowcount; `repo.session` is the audit-write target
  - `audit.log(repo.session, actor_uid=identity.uid, event_type=..., target=str(intake_id), space_id=intake.space_id, metadata={...})` — metadata structured only, NEVER a link/token
  - imports: from app.db import audit; from app.db.session import get_tenant_repo; from app.auth.dependencies import get_current_identity

Intake fields available (backend/app/db/models/intake.py):
  intake.space_id, intake.client_name, intake.status,
  intake.validation_link_sent_at, intake.results_link_sent_at (nullable timestamps, already in schema — no migration)

Recipient source (backend/app/db/models/membership.py — a ROOT table, NOT RLS-scoped):
  OrganizationMembership.id, .organization_id (= space_id link), .email, .status ("active"/"deactivated"), .role
  (there is NO `name` column — the member-list read exposes {id, email}; a name is optional/None)

Invite endpoint analog (backend/app/api/admin_routes.py):
  `invite_user(body, repo: AdminRepo = Depends(get_admin_session), identity = Depends(get_current_identity))` (line 110)
  `admin_users.generate_set_password_link(email)` returns the fresh action link (line 161)
  `repo.get_membership(membership_id)` → membership | None (admin_repo.py:94)
  `audit.log(repo.session, actor_uid=..., event_type="user.invited", target=uid, space_id=space.id, metadata={...})` (line 165)

ActionCodeSettings target (backend/app/auth/admin_users.py:86):
  `generate_set_password_link(email)` currently returns `auth.generate_password_reset_link(email)` with NO ActionCodeSettings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /{id}/members read + intake-scoped send endpoints + recipient resolution + admin_validated auto-fire</name>
  <files>backend/app/api/intake_routes.py</files>
  <read_first>
    - backend/app/api/intake_routes.py (submit_intake at 680, _SUBMIT_TRANSITIONS at 649, router/imports at 38-72, the 404+audit-in-tx pattern)
    - backend/app/db/models/membership.py (OrganizationMembership — the active-member recipient source; id/organization_id/email/status; NO name column)
    - backend/app/db/repository.py (IntakeRepository.get/.patch/.session at 95-130 — the tenant seam)
    - app/mail/render.py + app/mail/resend.py (Plan 01 — the render + send functions)
    - .planning/phases/10-notifications/10-RESEARCH.md (§ Pattern 3 CTA route selection, § Pattern 4 admin_validated, § Pitfall 1 timestamp-after-send, § Pitfall 4 client-not-blocked)
    - .planning/phases/10-notifications/10-PATTERNS.md (§ intake_routes.py — discrete verbs, sent-at on 2xx, recipient resolution)
  </read_first>
  <behavior>
    - GET /intakes/{id}/members → returns the intake space's ACTIVE members as rows of {id, email} (name optional/None, no name column exists); a cross-space or unknown intake id → 404 (existence-hidden, D-07); deactivated members are never in the list.
    - POST /intakes/{id}/mail/validation with recipient membership ids → resolves those to ACTIVE-member emails in the intake's space, renders the token-free validation body (cta_url = {app_base_url}/intake/{id}), calls resend.send, and on success stamps validation_link_sent_at + audits event_type="mail.sent".
    - POST /intakes/{id}/mail/reminder → renders the isReminder validation body; sends; does NOT stamp any timestamp (legacy parity, no reminder column).
    - POST /intakes/{id}/mail/results → cta_url {app_base_url}/intake/{id}/results; on success stamps results_link_sent_at.
    - A non-2xx from resend.send → the endpoint returns a failure response and writes NO timestamp and NO mail.sent audit row (D-16 / Pitfall 1).
    - A recipient id that is not an active member of the intake's space is rejected (never emailed); a body-supplied raw `to`/email field is not honored (D-06).
    - Cross-space intake id → repo.get returns None → 404 (existence-hidden), for BOTH the members read and every send endpoint.
    - submit_intake, when it advances to validated_by_client, fires admin_validated to Settings.nestor_admin_email; if that send raises, the client's submit still returns 200 with the transitioned view.
  </behavior>
  <action>
    In `backend/app/api/intake_routes.py`, add a read endpoint `GET /{intake_id}/members` on `intake_router` (`def`, `repo: IntakeRepository = Depends(get_tenant_repo)`, `identity: Identity = Depends(get_current_identity)`): `repo.get(intake_id)` → None ⇒ 404 (existence-hidden, D-07); then query `OrganizationMembership` filtered `organization_id == intake.space_id AND status == "active"` via `repo.session` and return a Pydantic list of `{id, email}` rows (name is optional/None — the membership table has no name column). This is the concrete read the Plan-04 RecipientPicker lists from (`listSpaceMembers`); it reuses the same active-member query the send endpoints resolve against (share the private helper below).

    Add three discrete send endpoints on `intake_router` (mirroring the `/submit` verb style, RESEARCH Open Q1 → RESOLVED: discrete verbs): `POST /{intake_id}/mail/validation`, `/mail/reminder`, `/mail/results`, each `def` with `repo: IntakeRepository = Depends(get_tenant_repo)` and `identity: Identity = Depends(get_current_identity)` and a Pydantic body carrying ONLY `recipients: list[str]` (membership ids) — NO `to`/email/space_id field (D-06 / TENANT-02). Each handler: `repo.get(intake_id)` → None ⇒ 404; resolve the recipient emails server-side from `OrganizationMembership` filtered to `organization_id == intake.space_id AND status == "active" AND id IN recipients` (query via `repo.session`; reject any requested id that is not an active member of that space — do not silently drop-and-send-to-fewer without signalling; an empty resolved list ⇒ a 400/422, never a zero-recipient send); render via the matching `app.mail.render` function using `get_settings().app_base_url` to build the token-free `cta_url` (validation/reminder → `{base}/intake/{intake_id}`, results → `{base}/intake/{intake_id}/results`) and `intake.client_name` as project/client fields; call `app.mail.resend.send(to=<emails>, subject=<Dutch subject per type>, html=<rendered>)` FIRST; ONLY on success write the timestamp (`repo.patch(intake_id, validation_link_sent_at=<now>)` for validation, `results_link_sent_at` for results; reminder writes NO timestamp) and `audit.log(repo.session, ..., event_type="mail.sent", target=str(intake_id), space_id=intake.space_id, metadata={"type":..., "recipient_count":..., <NEVER a link/token>})`; on a `send()` exception return a JSON failure body (e.g. `{"success": False}`) with NO timestamp/audit write. Add a small private helper `_resolve_active_member_emails(session, space_id, recipient_ids) -> list[str]` (and reuse the same active-member query shape for the members read) to avoid duplicating the query across the read + three send handlers.

    In `submit_intake` (line 680), AFTER the existing status flip + `audit.log`, add: if `new_status == "validated_by_client"`, call a new private `_send_admin_validated(intake)` inside a `try/except Exception` that logs a warning on failure (silent-logged — the client must not see an operator-mail error, Pitfall 4). `_send_admin_validated` reads `get_settings().nestor_admin_email` (D-08; if unset, log and return — do not raise), renders `render_admin_validated(cta_url={base}/admin/pulse/intakes/{intake_id}, ...)`, and calls `resend.send`. Do NOT let this share a transaction that would roll back the status change.
  </action>
  <verify>
    <automated>pytest backend/tests/test_mail_endpoints.py backend/tests/test_intake_validate_mail.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `intake_routes.py` defines `GET /{intake_id}/members` returning active-member `{id, email}` rows, 404 on a cross-space/unknown intake id, deactivated members excluded.
    - `intake_routes.py` defines `POST /{intake_id}/mail/validation`, `/mail/reminder`, `/mail/results`, each with a body of `recipients: list[str]` and NO `to`/`email`/`space_id` field.
    - Recipient emails are resolved from `OrganizationMembership` filtered `organization_id == intake.space_id AND status == "active"`; a requested id not matching an active member of that space is rejected.
    - The timestamp write happens only after a successful `resend.send` (validation → validation_link_sent_at, results → results_link_sent_at, reminder → none).
    - `submit_intake` calls `_send_admin_validated` only when `new_status == "validated_by_client"`, wrapped in try/except so the handler still returns the transitioned `IntakeView`.
    - No `audit.log` metadata in these paths contains a link or token.
  </acceptance_criteria>
  <done>Members read + three send endpoints + admin_validated auto-fire implemented with active-member resolution and 2xx-only timestamping; tests referenced pass in Cloud Build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: invite-mail endpoint + ActionCodeSettings continue URL</name>
  <files>backend/app/api/admin_routes.py, backend/app/auth/admin_users.py, backend/app/db/admin_repo.py</files>
  <read_first>
    - backend/app/api/admin_routes.py (invite_user at 110-178 — the admin-scope + fresh-link + audit shape to mirror)
    - backend/app/db/admin_repo.py (get_membership at 94; the members read here is NOT needed — the intake-scoped GET /{id}/members in Task 1 is the picker's source)
    - backend/app/auth/admin_users.py (generate_set_password_link at 86 — add ActionCodeSettings; keep the mockable-seam `from firebase_admin import auth`)
    - app/mail/render.py + resend.py (render_invite + send)
    - .planning/phases/10-notifications/10-RESEARCH.md (§ Code Examples — server-side invite action link with continue URL; A6 verify ActionCodeSettings kwarg against firebase-admin>=7.4)
    - .planning/phases/10-notifications/10-PATTERNS.md (§ admin_routes.py invite-mail, § admin_users.py ActionCodeSettings)
  </read_first>
  <behavior>
    - POST /admin/users/{membership_id}/invite-mail → looks up the membership (404 if absent), regenerates a FRESH set-password action link, renders the invite body (the only mail carrying a link), sends via faked Resend, audits event_type="mail.sent" with NO link in metadata, returns success.
    - generate_set_password_link produces a link whose ActionCodeSettings.url is {app_base_url}/auth/action (continue URL to the branded handler).
    - A membership with no email → a clear error, not a send to None.
  </behavior>
  <action>
    In `backend/app/auth/admin_users.py`, modify `generate_set_password_link(email)` to build `auth.ActionCodeSettings(url=f"{app_base_url}/auth/action", handle_code_in_app=True)` (base URL from `get_settings().app_base_url`, NOT a literal — verify the exact `ActionCodeSettings` kwarg name against the installed `firebase-admin>=7.4`, RESEARCH A6) and pass it as `action_code_settings=acs` to `auth.generate_password_reset_link`. Preserve the module-level `from firebase_admin import auth` mockable seam (tests patch `app.auth.admin_users.auth.<call>`). If `app_base_url` is unset, fall back to the bare link (keep Phase-5 behavior) rather than raising.

    In `backend/app/api/admin_routes.py`, add `POST /admin/users/{membership_id}/invite-mail` (`def`, `repo: AdminRepo = Depends(get_admin_session)`, `identity = Depends(get_current_identity)`), mirroring `invite_user`: `repo.get_membership(membership_id)` → None ⇒ 404; if the membership has no email ⇒ 409/422 (do not send to None); regenerate `admin_users.generate_set_password_link(membership.email)` fresh per send (D-10); render `render_invite(cta_url=<action_link>)`; `resend.send(to=[membership.email], subject=<Dutch invite subject>, html=<rendered>)`; `audit.log(repo.session, actor_uid=identity.uid, event_type="mail.sent", target=<membership uid/id>, space_id=membership.organization_id, metadata={"type":"invite", <NEVER the link>})`; return a success view. One handler serves both the InviteUserDialog and the member-list resend (D-10).
  </action>
  <verify>
    <automated>pytest backend/tests/test_mail_endpoints.py -x -k "invite or action_code"</automated>
  </verify>
  <acceptance_criteria>
    - `admin_users.generate_set_password_link` constructs `ActionCodeSettings` with `url` derived from `get_settings().app_base_url` + `/auth/action` and passes it to `generate_password_reset_link`; the mockable `auth` seam is intact.
    - `admin_routes.py` defines `POST /admin/users/{membership_id}/invite-mail` behind `get_admin_session`; a missing membership → 404; a membership with no email → 4xx (no send-to-None).
    - The invite-mail audit row's metadata contains no action link.
  </acceptance_criteria>
  <done>Invite-mail endpoint sends a fresh-link invite; the action link's continue URL is the /auth/action handler; tests pass in Cloud Build.</done>
</task>

<task type="auto">
  <name>Task 3: Members-read + cross-space denial + no-free-address + D-16 timestamp tests</name>
  <files>backend/tests/test_mail_endpoints.py, backend/tests/test_mail_denial.py, backend/tests/test_intake_validate_mail.py</files>
  <read_first>
    - backend/tests/test_intake_cross_tenant.py (the two-space TestClient harness: dependency_overrides for get_current_identity, patched engine factories, two_spaces seeding — copy this scaffold)
    - backend/tests/conftest.py (fake_resend from Plan 01, two_spaces, set_space, superadmin_engine fixtures)
    - backend/app/api/intake_routes.py (the members read + send endpoints from Task 1)
    - .planning/phases/10-notifications/10-RESEARCH.md (§ Validation Architecture — Phase Requirements → Test Map, the exact test ids)
    - .planning/phases/10-notifications/10-VALIDATION.md (Per-Task Verification Map)
  </read_first>
  <action>
    Create `backend/tests/test_mail_denial.py` (`pytestmark = pytest.mark.integration`, guarded imports mirroring `test_intake_cross_tenant.py`): seed an intake in space_b, drive a send endpoint AND the `GET /{id}/members` read as a user-A Identity, assert EXACTLY 404 (existence-hidden, D-07) for both, and that `fake_resend` recorded ZERO calls (no cross-space mail left the building).

    Create `backend/tests/test_mail_endpoints.py` with contract tests over the faked Resend seam: `test_members_read_active_only` (GET /{id}/members returns the intake space's active members as {id, email} and EXCLUDES a deactivated member); `test_timestamp_on_success_only` (a successful send stamps the right column; a `send()` that raises leaves the column None and writes no mail.sent audit row); `test_no_free_address` (a body carrying an extra `to`/`email` field is ignored — recipients come only from resolved active memberships); a happy-path per type asserting `fake_resend` captured the resolved active-member emails and the rendered body; the invite-mail cases from Task 2 (`invite`, `action_code`). Use the two-space + membership seeding to create active + deactivated members and assert deactivated members are never in the members read NOR in the resolved `to`.

    Create `backend/tests/test_intake_validate_mail.py`: drive `submit_intake` across the `reviewed → validated_by_client` edge and assert `fake_resend` captured one admin_validated mail to the configured admin address; then set `fake_resend` to raise and assert the submit STILL returns 200 with status `validated_by_client` (client-not-blocked, Pitfall 4).
  </action>
  <verify>
    <automated>pytest backend/tests/test_mail_denial.py backend/tests/test_mail_endpoints.py backend/tests/test_intake_validate_mail.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_mail_denial.py` asserts a cross-space send AND a cross-space `GET /{id}/members` → 404 AND `fake_resend` has zero recorded calls.
    - `test_mail_endpoints.py::test_members_read_active_only` asserts the read returns active {id, email} rows and excludes a deactivated member.
    - `test_mail_endpoints.py::test_timestamp_on_success_only` asserts the timestamp is written on success and absent on a raised send.
    - `test_mail_endpoints.py::test_no_free_address` asserts a body-supplied address is not honored.
    - A test asserts deactivated members are excluded from resolved recipients.
    - `test_intake_validate_mail.py` asserts the admin_validated auto-fire AND that a mail failure does not fail the client's validate.
  </acceptance_criteria>
  <done>Members-read, denial, D-16, no-free-address, deactivated-exclusion, and admin_validated-non-blocking behaviors are test-locked and green in Cloud Build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → members read / send endpoint | admin-supplied intake id + recipient ids cross into the tenant-scoped API |
| intake space → membership read | recipient emails + the picker's member list resolved from a root table, must be pinned to the intake's own space |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation / Info disclosure | cross-space intake send / members read / intake enumeration | mitigate | Intake resolved via `get_tenant_repo`; cross-space → 404 existence-hidden (D-07) for both the members read and the send endpoints; `test_mail_denial.py` asserts 404 + zero sends. |
| T-10-07 | Spoofing / Info disclosure | send-to-arbitrary-address (open relay for phishing "log in" mails) | mitigate | Body carries only membership ids; emails resolved server-side from ACTIVE memberships of the intake's own space; `test_no_free_address` locks it (D-06). |
| T-10-08 | Info disclosure | action link / token in audit rows or logs | mitigate | `audit.log` metadata is structured fields only; invite-mail audit excludes the link (Phase 5 contract); admin_validated failure silent-logged without the CTA. |
| T-10-09 | Tampering | sent-at marked before the mail actually left | mitigate | D-16 send-then-stamp; `test_timestamp_on_success_only` asserts no stamp on a failed send. |
| T-10-10 | Denial of service | admin_validated failure crashes the client's validate | mitigate | admin_validated fired in try/except, does not share the status-change tx; `test_intake_validate_mail` asserts client-not-blocked (Pitfall 4). |
| T-10-13 | Info disclosure | members read leaks another space's member emails | mitigate | The read is `get_tenant_repo`-gated on the intake, filtered to `organization_id == intake.space_id`, active-only; `test_mail_denial` asserts cross-space 404 and `test_members_read_active_only` asserts scope + active-only. |
</threat_model>

<verification>
- `pytest backend/tests/test_mail_endpoints.py backend/tests/test_mail_denial.py backend/tests/test_intake_validate_mail.py -x` green in Cloud Build.
- Full backend suite green in Cloud Build (per-wave merge gate).
- Grep: `GET /{id}/members` returns active-member rows scoped to the intake's space; no `to`/`email`/`space_id` request-body field on the send endpoints; no link/token in send-path audit metadata.
</verification>

<success_criteria>
- The members read lists only the intake space's active members; cross-space → 404.
- Send endpoints resolve recipients only from active memberships of the intake's own space; cross-space → 404.
- Timestamps update only on a 2xx send.
- admin_validated auto-fires on validate without ever failing the client's request.
- Invite-mail endpoint sends a fresh-link invite whose continue URL is the /auth/action handler.
</success_criteria>

<output>
Create `.planning/phases/10-notifications/10-03-SUMMARY.md` when done.
</output>
