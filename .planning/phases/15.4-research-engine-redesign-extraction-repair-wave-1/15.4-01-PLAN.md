---
phase: 15.4-research-engine-redesign-extraction-repair-wave-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/__init__.py
  - tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/loader.py
  - tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/responses/
autonomous: false
requirements: []
user_setup:
  - service: google-cloud-storage
    why: "The four distiller audit blobs live in the run's audit bucket; only an authenticated gcloud can read them."
    dashboard_config:
      - task: "gcloud auth is already active for project-cb01b861-cb4a-438d-b9a (see .planning/STATE.md deploy record). If `gcloud storage ls` 403s, re-auth before starting."
        location: "Cloud Shell or local gcloud"

must_haves:
  truths:
    - "The four V-01 distiller responses are committed in-repo and readable by a test with no GCS call"
    - "Every committed fixture file has been scanned for a live credential and the scan result is recorded"
    - "The fixture's own manifest states the expected claim count per blob, so a later replay test cannot silently assert nothing"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/loader.py"
      provides: "DISTILLER_CALLS manifest + load_distiller_response(); provenance docstring naming the bucket and the redaction check"
      contains: "DISTILLER_CALLS"
    - path: "tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/responses/"
      provides: "exactly four .txt files — the raw model output of the four gemini-2.5-flash distiller calls"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/loader.py"
      to: "responses/*.txt"
      via: "Path(__file__).parent read, never a GCS client"
      pattern: "Path\\(__file__\\)"
---

<objective>
Commit the regression fixture this whole phase is measured against: the four gemini-2.5-flash
distiller responses from V-01 (`7dcf51d5`), pulled from the audit bucket, redaction-checked, and
committed as test data with a manifest of expected counts.

Purpose: two of these four blobs contain the 278 coffee claims the engine threw away. Without them
in the repo, every later plan's "the parser is fixed" is an assertion about synthetic strings.
With them, it is a replay against real, known-good model output.

Output: `tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/` — four response files, a loader, a
manifest. No production code changes; no test file is created here (plan 03 owns the replay test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-CONTEXT.md
@docs/tribunal-run-reports/run-20260728-7dcf51d5-DIAGNOSTICS.md

<interfaces>
<!-- The fixture convention this repo already has. Copy its shape; do not invent a second one. -->

`tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py` is the analog:
  - module docstring carries PROVENANCE (the `gs://` path, "the fixture never reads GCS")
  - module constants `RECORDED_GCS_RUN_ID`, `RECORDED_AUDIT_BUCKET` for lineage only
  - data read with `Path(__file__).parent / ...`, never a storage client

Audit blob JSON shape (`audit/gcs_blob.py::upload_audit_body`, line 133):
  {"run_id", "audit_id", "seq", "provider", "model", "request", "response"}
The model text is at `response.candidates[0].content.parts[0].text`
(stated in run-20260728-7dcf51d5-DIAGNOSTICS.md § Reproduction).

Key-based redaction (`_DEFAULT_REDACT_KEYS`, gcs_blob.py line 43) covers header/body KEYS only —
`authorization`, `x-api-key`, `api_key`, … It CANNOT catch a credential inside a URL query string.
That is exactly the SerpApi exposure .planning/STATE.md calls blocking, not advisory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pull the four distiller blobs and extract their response text</name>
  <read_first>
    - docs/tribunal-run-reports/run-20260728-7dcf51d5-DIAGNOSTICS.md (§ Diagnostic 1b table + § Reproduction)
    - .planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-CONTEXT.md (§ "The regression fixture is real")
    - tribunal/nestor_pulse_sdk/audit/gcs_blob.py (lines 30-150 — the blob key format and what redaction does and does not cover)
  </read_first>
  <action>
Work in the session scratchpad, NOT in the repo, until Task 3 signs off.

List the run's blobs:
  `gcloud storage ls "gs://project-cb01b861-cb4a-438d-b9a-nestor-audit/runs/7dcf51d5-1153-4374-b444-c25d17eeea01/" --project=project-cb01b861-cb4a-438d-b9a | grep gemini-2.5-flash`

The four distiller calls are the objects written 13:46:33Z–13:48:30Z, whose audit_id prefixes are
`e9a168b5`, `fe418029`, `af1995b6`, `7dcf4a14`. Download those four objects only.

From each blob read `response.candidates[0].content.parts[0].text` and write it, verbatim and
UTF-8, to a scratch file. Do NOT commit the `request` half: it carries the full research report,
which is the half that can embed a URL with a credential in a query parameter, and the parser under
test consumes only the response text.

Record, per blob, the count of non-empty lines in the extracted text. The expected values from the
DIAGNOSTICS table are: `e9a168b5` → 141, `fe418029` → 137, `af1995b6` → 43, `7dcf4a14` → 143. If any
count differs, STOP and report the discrepancy — do not adjust the manifest to match what you pulled.

Then run the redaction scan over the four extracted texts and record its output verbatim for Task 3:
case-insensitive search for `api_key`, `apikey`, `serpapi`, `x-goog-api-key`, `authorization`,
`secret`, `AIza`, and for any query parameter matching `[?&](key|token|api[_-]?key)=`.
  </action>
  <acceptance_criteria>
    - Four files exist in the scratchpad, one per audit_id prefix, each non-empty.
    - The per-file non-empty line counts are exactly 141 / 137 / 43 / 143. Any other value is a STOP,
      reported in the summary, not silently accepted.
    - `grep -icE '(api[_-]?key|apikey|serpapi|x-goog-api-key|authorization|secret|AIza)'` over the
      four files, and `grep -cE '[?&](key|token|api[_-]?key)='` over the four files, are both
      recorded VERBATIM in the plan summary — including when the answer is 0. A count that was never
      printed is not a check that was run.
    - Nothing has been written under `tribunal/` yet.
  </acceptance_criteria>
  <done>Four response texts are in the scratchpad with reconciled line counts and a recorded redaction scan.</done>
  <files>(scratchpad only — no repo path is written in this task)</files>
  <verify>
    <automated>wc -l "$SCRATCH"/*.txt && grep -icE '(api[_-]?key|apikey|serpapi|x-goog-api-key|authorization|secret|AIza)' "$SCRATCH"/*.txt</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator confirms the fixture carries no live credential</name>
  <what-built>Four extracted response texts in the scratchpad, plus the verbatim output of the credential scan from Task 1.</what-built>
  <how-to-verify>
    1. Read the scan output printed in Task 1. Every pattern's count is shown, including zeros.
    2. If any count is non-zero, open the offending line and decide: redact the value in place
       (replace with `[REDACTED]`) or drop that fixture. An unredacted blob freezes a live credential
       into a repo under 7-year retention — .planning/STATE.md calls this blocking, not advisory.
    3. Confirm that only the `response` half is being committed (no research-report request bodies).
  </how-to-verify>
  <resume-signal>Type "approved" to commit the fixture, or name the lines to redact first.</resume-signal>
  <files>(no repo path — this is a decision gate over the scratchpad output of Task 1)</files>
  <action>Present the verbatim scan output from Task 1 to the operator and wait. Do not create anything under `tribunal/` until the operator has answered. If the operator names lines to redact, replace those values with the literal string `[REDACTED]` in the scratch files and re-run the scan before asking again.</action>
  <verify>
    <human-check>The operator has read the scan output and answered "approved" or named the lines to redact.</human-check>
  </verify>
  <done>The operator has approved the fixture for commit, or the named values have been redacted and re-scanned.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the fixture, the loader and the expected-count manifest</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py (lines 1-45 — the provenance-docstring and no-GCS-at-test-time convention to copy)
    - tribunal/nestor_pulse_sdk/tests/fixtures/__init__.py
  </read_first>
  <action>
Create `tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/` with:

  - `__init__.py` (empty, matching the sibling fixture package)
  - `responses/e9a168b5-idx12-coffee.txt`
  - `responses/fe418029-idx16-coffee.txt`
  - `responses/af1995b6-idx08-chunk2.txt`
  - `responses/7dcf4a14-idx08-chunk1.txt`
  - `loader.py`

`loader.py` exports:
  - `RECORDED_RUN_ID = "7dcf51d5-1153-4374-b444-c25d17eeea01"` and
    `RECORDED_AUDIT_BUCKET = "gs://project-cb01b861-cb4a-438d-b9a-nestor-audit/runs/<RECORDED_RUN_ID>/"`
    — lineage only; the loader never constructs a storage client.
  - `DISTILLER_CALLS`: a tuple of four frozen dataclasses (or dicts) with fields
    `audit_prefix`, `filename`, `report_idx`, `separator` (`"<TAB>"` for the two coffee calls,
    `"\t"` for the other two), `non_empty_lines`, `expected_claims`.
    Values: e9a168b5 / idx 12 / `<TAB>` / 141 / 141 · fe418029 / idx 16 / `<TAB>` / 137 / 137 ·
    af1995b6 / idx 8 / tab / 43 / 43 · 7dcf4a14 / idx 8 / tab / 143 / 143.
  - `COFFEE_EXPECTED_CLAIMS = 278` with a comment stating it is 141 + 137 and is the number the
    delivered V-01 report was missing.
  - `load_distiller_response(audit_prefix: str) -> str` reading from `Path(__file__).parent`.
  - `load_all() -> list[tuple[str, str]]` returning (audit_prefix, text) for all four, and RAISING
    when the number of files found on disk is not exactly 4 — the `ls || true` silent-skip trap in
    fixture clothing: a loader that returns an empty list makes every downstream assertion vacuous.

The module docstring carries the provenance (bucket path, the 13:46:33Z–13:48:30Z window, the
DIAGNOSTICS reference), states that only the RESPONSE half is committed and why, and records the
date and outcome of the credential scan from Task 2.

Commit the fixture and the loader. Do not add anything to `cloudbuild.test-engine.yaml` — this plan
creates no test file, and plan 15.4-02 owns the next edit of that config.
  </action>
  <acceptance_criteria>
    - `python -c "from nestor_pulse_sdk.tests.fixtures.run_7dcf51d5.loader import load_all, DISTILLER_CALLS, COFFEE_EXPECTED_CLAIMS; rows=load_all(); print(len(rows), COFFEE_EXPECTED_CLAIMS, [len([l for l in t.splitlines() if l.strip()]) for _,t in rows])"`
      prints `4 278 [141, 137, 43, 143]` (order per `DISTILLER_CALLS`).
    - Deleting one response file and re-running the same command RAISES rather than returning 3 rows —
      demonstrate this in a scratch copy, not in the repo, and record the error text in the summary.
    - `sum(c.expected_claims for c in DISTILLER_CALLS if c.separator == "<TAB>") == COFFEE_EXPECTED_CLAIMS == 278`.
    - `grep -rn "google.cloud\|storage.Client" tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/`
      returns nothing — the fixture has no GCS dependency at test time.
    - `git status --porcelain` shows exactly the six new paths and nothing else.
  </acceptance_criteria>
  <done>The fixture is committed, loads from disk with no GCS call, and its manifest states 141/137/43/143 with a 278 coffee total.</done>
  <files>tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/__init__.py, tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/loader.py, tribunal/nestor_pulse_sdk/tests/fixtures/run_7dcf51d5/responses/*.txt</files>
  <verify>
    <automated>cd tribunal && python -c "from nestor_pulse_sdk.tests.fixtures.run_7dcf51d5.loader import load_all, COFFEE_EXPECTED_CLAIMS as C; r=load_all(); print(len(r), C, [len([l for l in t.splitlines() if l.strip()]) for _,t in r])"</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit bucket → repo | Model output recorded from a live run crosses into a 7-year-retained public-to-the-team artifact |
| model output → test assertions | The fixture text is untrusted model output; it is read as data, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.4-01 | Information disclosure | committed fixture text | mitigate | Only the `response` half is committed (the `request` half is where a URL-borne SerpApi key would ride); the seven-pattern scan in Task 1 plus the blocking operator checkpoint in Task 2 gate the commit |
| T-15.4-02 | Tampering | fixture loader | mitigate | `load_all()` raises on a file count other than 4, so a deleted or mistyped fixture cannot turn the replay proof into a vacuous pass |
| T-15.4-03 | Repudiation | provenance | mitigate | The loader docstring records the bucket URI, the call window and the date/outcome of the credential scan, so the fixture's origin is checkable without re-reading the bucket (which expires) |

No package-manager install in this plan; `tribunal/requirements.txt` is untouched.
</threat_model>

<verification>
- The fixture loads offline and reconciles to 141/137/43/143 and 278.
- The credential scan output is recorded verbatim in the summary, zeros included.
- No production source file and no gate config was modified.
</verification>

<success_criteria>
- Four real V-01 distiller responses are in the repo, redaction-checked and operator-signed-off.
- The manifest carries the numbers plan 15.4-03's replay proof will assert against.
</success_criteria>

<output>
Create `.planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-01-SUMMARY.md` when done
</output>
