---
phase: 15.4-research-engine-redesign-extraction-repair-wave-1
plan: 11
type: execute
wave: 5
depends_on: ["15.4-03", "15.4-04", "15.4-05", "15.4-06", "15.4-07", "15.4-08", "15.4-09", "15.4-10"]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - .planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-UAT.md
autonomous: false
requirements: []
superseded_by: "15.8-12"
superseded_on: "2026-08-04"
user_setup:
  - service: google-cloud-run
    why: "Wave 1 ships alone and is measured by ONE live run; only the operator can build, migrate, deploy and start it."
    dashboard_config:
      - task: "Confirm which ANTHROPIC secret the Tribunal services must bind. Nestor_Claude2 was not topped up as of 2026-07-27 and both services run on the TEMPORARY burner Nestor_Claude_Temp via TRIBUNAL_ANTHROPIC_SECRET. A redeploy without the override silently repoints at an empty key."
        location: "Secret Manager + deploy-worker.sh / deploy-api.sh"
      - task: "Confirm the queue is EMPTY before the worker is deployed — a deploy BOOTS the container and runs/worker.py CLAIMS FIRST, SLEEPS LAST, so --min-instances=0 does not protect you."
        location: "tribunal.run, read as worker_user"

must_haves:
  truths:
    - "The runbook has a Phase 15.4 section that an operator can execute without reading this plan"
    - "Alembic 0016 is proven applied by its literal upgrade log line, never by exit 0"
    - "The engine gate's collected-file count is read out of EXPECTED_FILES and reconciled, never recalled"
    - "ONE live run is executed after the deploy and compared against V-01 on named numbers"
    - "The delivered report is the artifact the run is judged from"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "§ Phase 15.4 — the ordered procedure, the gates, both read-backs, and the deploy record"
      contains: "Running upgrade 0015 -> 0016"
    - path: ".planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-UAT.md"
      provides: "the V-01 comparison table with its cells empty until the run fills them"
  key_links:
    - from: "infra/DEPLOY-RUNBOOK.md"
      to: "tribunal/cloudbuild.test-engine.yaml"
      via: "the gate's collected count, read out of EXPECTED_FILES at execution time"
      pattern: "EXPECTED_FILES"
---

> # ⛔ SUPERSEDED BY 15.8-12 — NOTHING IN THIS FILE IS EVER TO BE EXECUTED (2026-08-04)
>
> **This plan was RE-SCOPED into `infra/DEPLOY-RUNBOOK.md` § Phase 15.8 and
> `.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-UAT.md`
> by plan 15.8-12 on 2026-08-04.**
>
> - The **deploy** this file describes is executed by plan **15.8-14**.
> - The **measuring run** is executed by plan **15.8-15**.
> - ⛔ **Nothing here is to be executed, ever — INCLUDING its runbook-writing task.** That section now
>   exists, and a second author would produce a **second source of truth for one deploy**. The one
>   that gets read is whichever the reader found first.
>
> **THREE THINGS IN THIS FILE ARE WRONG IF READ ON THEIR FACE.** They are named here so a reader who
> skims the body below does not act on them:
>
> 1. **It targets Wave 1 ALONE.** The operator reversed that on **2026-07-29** — nothing is measured
>    until every wave is finished. All five waves (15.4 / 15.5 / 15.6 / 15.7 / 15.8) ship in ONE
>    deploy and ONE run measures them together.
> 2. **It names ONE migration (`0015 -> 0016`). The real deploy owes THREE:**
>    `Running upgrade 0015 -> 0016`, `Running upgrade 0016 -> 0017`, `Running upgrade 0017 -> 0018`.
>    **None of 0016 or 0017 has ever touched a database**, and an exit code is proof of none of them.
> 3. **Its V-01 comparison table covers Wave 1's extraction numbers only.** The real table carries
>    rows for all five waves, including the ones whose V-01 baseline is `n/a - feature did not exist`.
>
> **The text below this block is UNCHANGED and stays that way on purpose** — this project marks
> superseded text rather than deleting it, because a rewritten parked plan loses the evidence of what
> was originally intended and why it stopped.

> # ⛔ PARKED — DO NOT EXECUTE (operator decision, 2026-07-29)
>
> **The operator ruled: no deploy and no measurement until the WHOLE engine redesign is finished.**
> Waves 2-5 of `.planning/ENGINE-REDESIGN-SPEC.md` (claim attribution, dispatch redesign, the
> creative workshop loop, the discovery bracket, yield instrumentation) ship in the SAME deploy as
> Wave 1, and one live run measures all of it together.
>
> This plan's objective below still says "Ship Wave 1 alone and measure it". **That is superseded.**
> It was the spec's recommendation and the previous session's handoff; the operator has overridden
> it deliberately, having been told the trade-off — with several changes landing at once, an odd
> result cannot be attributed to a single change.
>
> **Do not run this plan when Wave 1's phase looks complete.** It is not the end of phase 15.4 any
> more; it is the end of the whole redesign. It will be re-scoped to cover every wave before it
> executes, and its V-01 comparison table will need rows for the later waves.
>
> Wave 1's code is committed and gate-verified (engine gate 1030 passed, 32 of 32). Nothing is live.

<objective>
Ship Wave 1 alone and measure it with one live run.

Purpose: everything downstream in the engine redesign is judged through the extraction funnel.
Shipping Wave 2 on top of an unmeasured Wave 1 would attribute the parser bug to the redesign. This
plan writes the procedure, executes it, and records what the run actually produced against V-01.

Output: a Phase 15.4 section in the deploy runbook, the deploy itself, and a filled comparison
table. Two blocking operator checkpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-CONTEXT.md
@.planning/ENGINE-REDESIGN-SPEC.md

<interfaces>
The section to copy the SHAPE of: `infra/DEPLOY-RUNBOOK.md` § "Phase 15.2 gap closure — Step 15.2.k"
(line ~2958). Read its ordering caution in full before writing anything: the as-written order caused
the 2026-07-28 incident, and the corrected order is `0→1→2→3→5→6→4→7→8→9→10` — the WORKER IS THE LAST
DEPLOYABLE.

What Phase 15.4 actually ships, and what it does NOT:
  - CHANGED: `tribunal/` only — `synthesis/steps.py`, `pipeline/tribunal/facts.py`,
    `pipeline/tribunal/research_division.py`, `pipeline/tribunal/own_researcher.py`,
    `citations/extractor.py`, new `citations/redirect_resolver.py`, `db/models/source.py`,
    alembic `0016`, `audit/cost_prices.json`, tests, `cloudbuild.test-engine.yaml`.
  - UNCHANGED: `backend/` and `frontend/` — ZERO files. So there is NO `nestor-migrate` line, NO
    `nestor-api` rebuild and NO frontend rebuild in this deploy. Say that explicitly in the runbook:
    an unstated omission reads as an oversight to the next operator.
  - Migration: TRIBUNAL line only, `0015 -> 0016`. The intake line stays at its own head.

Gate counts: `EXPECTED_FILES` in `tribunal/cloudbuild.test-engine.yaml` was 30 before this phase.
Plan 15.4-02 raised it to 31 and plan 15.4-03 to 32. DO NOT hard-code 32 in the runbook as a
remembered number — the runbook instruction must be "read `EXPECTED_FILES` out of the committed
config and require the `collecting: N of N` line to match it", which is how § 15.2.k words it.

Five gate-integrity traps this project has actually been bitten by, all of which the section must be
written to survive:
  - `ls || true` silent skip → reconcile against the printed `collecting:` count;
  - exit 0 as migration proof → require the literal `Running upgrade 0015 -> 0016` line;
  - a grep passing while behaviour is wrong → prefer running a test to grepping for a symbol;
  - a stale worktree deploying code that is not this phase → the on-disk artifact assertion;
  - a DB-bound test SKIPPING in a gate that provisions no Postgres → read the `-rs` skip summary,
    because a skip is not a pass.

V-01 baseline to compare against (`docs/tribunal-run-reports/run-20260728-7dcf51d5-V01-FINDINGS.md`
and the DIAGNOSTICS doc): 396 claims persisted, coffee claims 0, `gate_errors: 153`, $53.48 recorded
(a floor), ~65 minutes wall-clock, 225 unique grounding redirects.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 15.4 runbook section and the comparison table</name>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md (§ Phase 15.2 gap closure Step 15.2.k in full, ~line 2958 to the deploy record at its end — the ordering caution, the read-back discipline, the deploy-record template)
    - infra/DEPLOY-RUNBOOK.md (the Summary checklist at ~line 3637 — the new section needs its own checklist entry)
    - tribunal/cloudbuild.test-engine.yaml (the CURRENT committed EXPECTED_FILES value)
    - .planning/STATE.md (the standing cautions: the burner key, the queue, the two-SHA attribution lesson)
  </read_first>
  <action>
Add `## Phase 15.4 — Extraction repair (Wave 1): dual Tribunal REBUILD + 0016 migrate, then ONE live
run` to `infra/DEPLOY-RUNBOOK.md`, after § Step 15.2.k, as ONE ordered procedure with the worker
LAST:

  (0) PREFLIGHT, read-only. `git status --porcelain` EMPTY, HEAD SHA recorded, and a positive
      ON-DISK assertion for one artifact per plan: the `run_7dcf51d5` fixture directory,
      `_split_distiller_line` in `steps.py`, `build_cite_index` in `facts.py`,
      `citations/redirect_resolver.py`, `alembic/versions/0016_source_resolved_url.py`, the
      `openai/gpt-5.6-sol` decision in `cost_prices.json`, and
      `build_fact_list_retry_prompt` in `facts.py`. Cloud Build ships the tree you submit.
      Then the gates: the engine gate, whose printed `collecting: N of N expected files` MUST equal
      `EXPECTED_FILES` read out of the committed config at that moment (not a number from this
      document), plus `cloudbuild.test-gates.yaml` (it owns `test_claim_distiller.py`, which this
      phase edited). READ THE `-rs` SKIP SUMMARY: DB-bound files skip in the engine gate and a skip
      is not a pass. No backend gate and no frontend gate — `backend/` and `frontend/` are unchanged
      in this phase, and that omission is deliberate, not forgotten.
  (1) BUILD both Tribunal images (worker, api) at ONE `$SHA`. Build only; deploy is later.
  (2) QUEUE CONFIRMED EMPTY — `SELECT id,status FROM tribunal.run WHERE status IN ('queued','running')`,
      read as `worker_user`, never `app_user` (RLS returns a falsely empty queue to the wrong role).
      A `queued` row is a STOP: the worker deploy at step (4) would claim it on boot.
  (3) MIGRATE. Repin `tribunal-migrate` to the `$SHA` api image, confirm the repin took, execute.
      THE PROOF IS THE LITERAL LINE `Running upgrade 0015 -> 0016` IN THE JOB LOG. Exit 0 is not
      proof; this project has a recorded incident of a migration gate that never printed it.
      Additionally confirm `information_schema.columns` shows `source.resolved_url` and
      `source.resolution_status` as nullable text with no default, and that
      `tribunal.tribunal_alembic_version` reads `0016`. Migration BEFORE the service images.
  (4) DEPLOY `tribunal-api`, then `tribunal-worker` LAST, both at `IMAGE_TAG=$SHA`. State the
      ANTHROPIC secret decision inline: while `Nestor_Claude2` is not topped up, the worker deploy
      needs `TRIBUNAL_ANTHROPIC_SECRET=Nestor_Claude_Temp`, and a redeploy without the override
      silently repoints at an empty key.
  (5) READ-BACKS RECORDED VERBATIM: both revision names at 100% traffic, digest-pinned (`@sha256:`,
      never `:latest`), secret bindings BY NAME only (never a value), the tribunal alembic head from
      its own table, and the four new `NESTOR_REDIRECT_RESOLVE_*` knobs' effective values (absent =
      defaults, which is a valid read-back as long as it is stated).
  (6) THE DEPLOY RECORD at the end of the section: the `$SHA`(s), the revisions, the head with its
      literal upgrade line, and the change list by plan. Record MULTIPLE SHAs if the deploy spanned
      more than one build — as executed on 2026-07-28 it was two, and recording one would have made
      the attribution wrong.
  (7) FIRST-LIVE-RUN LOG CHECKLIST (what to grep for, and what each line means):
      the new `returned N non-empty line(s) but ZERO parsed as claims` WARNING (expect ABSENT — its
      presence means a new format deviation), the ZERO-claims warning (expect at most in-scope
      facets), `collect_provider_facts` honour lines, the fact-list retry attempt/outcome WARNINGs,
      and the redirect resolver's summary line with unique/resolved/unresolved counts.

Then create `.planning/phases/15.4-.../15.4-UAT.md` holding the V-01 comparison table with EMPTY
cells: claims persisted (V-01: 396), coffee-facet claims (V-01: 0), reports that fell through to the
distiller (V-01: 3 of 5 gemini), fact-list retries attempted / recovered (V-01: n/a), unique
grounding redirects and how many resolved (V-01: 225 / not attempted), `gate_errors` (V-01: 153),
`run.cost_pending` cleared y/n, total cost (V-01: $53.48, a floor), wall-clock (V-01: ~65 min).
Add one row per ROADMAP success criterion with a column for how it was observed.

Add the Phase 15.4 entry to the runbook's Summary checklist.
  </action>
  <acceptance_criteria>
    - `grep -n "Running upgrade 0015 -> 0016" infra/DEPLOY-RUNBOOK.md` returns at least one line, and the surrounding text states that exit 0 is not proof.
    - `grep -n "EXPECTED_FILES" infra/DEPLOY-RUNBOOK.md` shows the Phase 15.4 section instructing the reader to READ the number from the config; the section contains no hard-coded gate count presented as the expected value.
    - The section states in words that `backend/` and `frontend/` are unchanged and that there is therefore no nestor-migrate, no nestor-api rebuild and no frontend rebuild.
    - The worker is the LAST deployable in the written order, and the queue-empty step precedes it.
    - `15.4-UAT.md` exists with every V-01 baseline number filled in and every 15.4 cell empty.
  </acceptance_criteria>
  <done>An operator can execute this deploy from the runbook alone, and knows what number to compare against afterwards.</done>
  <files>infra/DEPLOY-RUNBOOK.md, .planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-UAT.md</files>
  <verify>
    <automated>grep -n "Running upgrade 0015 -> 0016" infra/DEPLOY-RUNBOOK.md && grep -n "EXPECTED_FILES" infra/DEPLOY-RUNBOOK.md</automated>
  </verify>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute the deploy</name>
  <what-built>The Phase 15.4 runbook section, and a green tree: engine gate at its asserted count, `cloudbuild.test-gates.yaml` green, alembic 0016 authored.</what-built>
  <how-to-verify>
    Execute § Phase 15.4 in `infra/DEPLOY-RUNBOOK.md`, steps (0) through (6), in the written order.
    Stop and report rather than improvising if any of these is true:
      - the engine gate's `collecting:` count does not equal `EXPECTED_FILES`;
      - the migrate job does not print `Running upgrade 0015 -> 0016`;
      - the queue is not empty when you reach step (2);
      - a revision resolves to `:latest` instead of an `@sha256:` digest.
    Fill in the deploy record at the end of the section before continuing.
  </how-to-verify>
  <resume-signal>Paste the deploy record: the SHA(s), both revision names, the literal upgrade line, and the gate's `collecting:` line.</resume-signal>
  <files>infra/DEPLOY-RUNBOOK.md (the deploy record at the end of § Phase 15.4)</files>
  <action>Hand the operator § Phase 15.4 of the runbook and wait. Do not run any build, migrate or deploy command on their behalf unless they ask. When they return, transcribe the SHA(s), both revision names, the literal `Running upgrade 0015 -> 0016` line and the gate's `collecting:` line into the deploy record — verbatim, not paraphrased.</action>
  <verify>
    <human-check>The deploy record is filled in with the SHA(s), both revisions, the literal upgrade line and the gate count.</human-check>
  </verify>
  <done>Both Tribunal services run this phase's code at a recorded SHA, with alembic at 0016 proven by its upgrade line.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ONE live run, judged from the delivered report</name>
  <what-built>Wave 1 deployed: the tolerant parser, the loud drop, the scoped ZERO warning, the fact-list retry, the cite-marker recovery, redirect resolution, the cost row and the restored run_event lines.</what-built>
  <how-to-verify>
    1. Start ONE live run on a FRESH test intake. Wave 1 ships alone and is measured before Wave 2
       starts — that is the whole reason for this sequencing.
    2. When it completes, fill `15.4-UAT.md` from the run: claims persisted, coffee-facet claims,
       reports that fell through to the distiller, retries attempted and recovered, redirects unique
       and resolved, `gate_errors`, `cost_pending`, total cost, wall-clock.
    3. READ THE DELIVERED REPORT — the `output` row with `format='markdown'` — not the claim table
       and not the logs. Four V-01 findings had to be withdrawn because they judged a working
       capability from an intermediate artifact. The question this run answers is whether the coffee
       section still tells the client the data "geeft geen volledig beeld".
    4. Check the log lines listed in runbook step (7). The `returned … but ZERO parsed as claims`
       WARNING appearing is not a failure of this phase — it is the phase working, and it names a
       deviation to investigate.
    5. Confirm citations in the report resolve, and that any unresolved redirect is still present as
       a citation rather than missing.
  </how-to-verify>
  <resume-signal>Type "accepted" with the filled comparison table, or list the defects to close before Wave 2 starts.</resume-signal>
  <files>.planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-UAT.md</files>
  <action>Wait for the operator's live run, then fill every cell of the comparison table in `15.4-UAT.md` from the run's own artifacts, and record the coffee section of the DELIVERED markdown report verbatim beside the V-01 sentence it replaces. Do not start the run yourself and do not start a second one; Wave 1 is measured by ONE run.</action>
  <verify>
    <human-check>The comparison table is filled, and the operator has answered "accepted" or listed the defects to close before Wave 2.</human-check>
  </verify>
  <done>One live run has been executed and judged from the delivered report, with the outcome recorded against the V-01 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build artifact → production Cloud Run | The tree submitted to Cloud Build becomes the running engine |
| migration job → live tenant data | DDL against the production Cloud SQL instance |
| deployed worker → the run queue | A worker claims work on boot, before it sleeps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.4-28 | Tampering | a stale worktree deploying code that is not this phase | mitigate | Step (0) requires an empty `git status --porcelain`, a recorded HEAD SHA, and a positive on-disk assertion for one artifact per plan before any build |
| T-15.4-29 | Denial of service / cost | the worker claiming a run on boot | mitigate | Queue-empty verification at step (2), read as `worker_user`; the worker is the LAST deployable in the written order — the corrected ordering that the 2026-07-28 incident produced |
| T-15.4-30 | Repudiation | the audit chain across the migration | mitigate | 0016 adds two nullable columns on `source`; none of `hash_chain._payload_for_row`'s eleven frozen fields lives there. `test_hash_chain_replay.py` runs in the engine gate at step (0), and the EU AI Act Art. 12 deadline of 2026-08-02 makes an unproven chain a hard stop |

Deferred debts this deploy does NOT close, restated so they are not assumed handled: rotating
`Nestor_Claude_Temp` (it transited a chat in plaintext on 2026-07-27 and is live on both Tribunal
services), the `serviceAccountTokenCreator` grant decision on `nestor-run@`, and 15.3 plan 09's two
operator checkpoints.

No package-manager install in this plan; `tribunal/requirements.txt` is untouched.
</threat_model>

<verification>
- The literal `Running upgrade 0015 -> 0016` line is in the deploy record.
- The engine gate's printed count equals `EXPECTED_FILES` read from the committed config.
- `15.4-UAT.md` is filled from the live run, with the coffee number and the delivered report's coffee section both recorded.
</verification>

<success_criteria>
- Wave 1 is live, measured against V-01 on named numbers, and the operator has accepted or listed what remains — the precondition Wave 2 waits on.
</success_criteria>

<output>
Create `.planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-11-SUMMARY.md` when done
</output>
