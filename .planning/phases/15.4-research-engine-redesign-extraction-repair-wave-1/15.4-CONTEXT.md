# Phase 15.4: Research Engine Redesign — Extraction Repair (Wave 1) - Context

**Gathered:** 2026-07-29
**Status:** Ready for planning
**Source:** PRD Express Path — `.planning/ENGINE-REDESIGN-SPEC.md` § 2 "Wave 1 — extraction repair"

<domain>
## Phase Boundary

**What happened.** V-01 (run `7dcf51d5`, 2026-07-28) produced a client report whose coffee section said
the Benelux data *"geeft geen volledig beeld"*. That statement was **false**. The distiller had returned
**278 well-formed coffee claims** and `_parse_distiller_response` threw away every one of them on a
string comparison: the model wrote the literal five-character string `<TAB>` instead of U+0009, because
**the prompt itself uses `<TAB>` as a placeholder describing the separator**. The only trace was a
`log.debug`, which production does not serve.

**What this phase delivers.** The engine stops silently discarding claims it successfully extracted, and
says so loudly when it does. Four things:

1. A separator-tolerant distiller parser + a prompt contract that cannot be copied back as data.
2. "Returned lines, kept zero claims" is a **WARNING**, and the ZERO-claims warning stops crying wolf.
3. One additive retry when a gemini fact list is unusable — covering **all three** observed deviations.
4. Gemini grounding redirects resolved to publisher URLs at ingest, so citations cannot rot.

Plus two smalls: the missing `gpt-5.6-sol` cost row and the dropped `run_event` rows.

**Ships alone.** Wave 1 is deployed and measured by ONE live run before Wave 2 starts. Everything
downstream in the redesign is judged through the extraction funnel — shipping the redesign on top of a
broken meter would attribute the parser bug to the redesign.

**Out of scope — explicitly.** Waves 2–5 of the spec (claim attribution, dispatch redesign, the creative
workshop loop, yield instrumentation). Anything in spec § 7 "What we are deliberately NOT doing".

</domain>

<decisions>
## Implementation Decisions

Everything below is **locked** — taken by the operator in session on 2026-07-29 and recorded in
`.planning/ENGINE-REDESIGN-SPEC.md`. Do not relitigate; if the code contradicts a decision, report it
rather than silently choosing.

### D-R1 — The `<TAB>` fix (spec § 1.1)

**File:** `tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py`

- **(a) Parser** — `_parse_distiller_response` (def at line 1260) currently does `if "\t" not in line:
  continue` then `line.split("\t", 2)`. Replace with a separator-tolerant split accepting, **in this
  priority order**: a real tab, `<TAB>`, `|||`, `|`, and a run of 2+ spaces. Priority order is
  load-bearing: a line containing both a real tab and a literal `<TAB>` MUST split on the tab. `|||`
  MUST be tried ahead of `|`. Returns `None` when no separator is present at all.
- **(b) Prompt** — `_build_distiller_prompt` (def at line 1185). **Stop describing the separator with a
  token the model can copy.** Decision taken: switch the contract to `FACET ||| CLAIM_TEXT ||| EVIDENCE`
  and keep accepting a tab for back-compat. (The rejected alternative was writing a real tab into the
  example — invisible in source, fragile under reformatting.)
- **(c) Make the drop loud** — in `_distill_unit` (line 1537), after `_parse_distiller_response` (called
  at line 1568): if the model returned text but zero claims parsed, `log.warning` with the provider, the
  non-empty line count, and the first line truncated to 200 chars. **This one line is the whole lesson
  of V-01.** WARNING is the lowest level production actually serves (D-V01-6).
- **(d) Scope the ZERO-claims warning** — line 1580 iterates every focus area in the mission brief and
  reports zero for any label absent from *this call's* output, in scope or not. On V-01 it cried wolf
  about `convenience` (never in the call) next to a real coffee zero. Restrict it to facets present in
  the call's **inputs** (`_angle` on the provider reports).

### D-R2 — Retry on an unusable fact list (spec § 1.2)

**Files:** `tribunal/nestor_pulse_sdk/pipeline/tribunal/facts.py` +
`synthesis/steps.py::collect_provider_facts` (line 1820; `needs_distiller_fallback` branch at line 2002).

Gemini's fact-list failures are **three distinct format deviations**, not one. A retry covering only
"block absent" fixes one of three:

| V-01 idx | Deviation | Symptom |
|---|---|---|
| 12, 16 | No `FACTS_START`/`FACTS_END` block at all | `gemini returned no FACTS_START/FACTS_END block` |
| 8 | Block present; every line prefixed with a literal `STATEMENT` column, shifting every field one place | `not one line in it parsed as a fact (4 line(s) ignored)` |
| 4 | Block well-formed; `SOURCE_URL` column holds the `[cite: N]` marker | ~20 × `rejecting non-http(s) SOURCE_URL '[cite: 25, 26]'` — facts survived, **sources did not** |

- **One** retry per report, at the `collect_provider_facts` level, on the `needs_distiller_fallback`
  branch, **before** falling through to `fallback_units`.
- Re-ask the **same provider** for **only the fact list**, over its own report text, using the same
  `build_fact_list_prompt_block` contract plus an explicit corrective naming the deviation observed.
- Parse the retry with the same `parse_fact_list`. On success, treat it exactly as a first-pass block.
- On failure, fall through to the distiller **exactly as today** — the retry is **additive, never a new
  failure mode**.
- Log the attempt and the outcome at **WARNING**.
- Add a **normalising pre-parse** in `parse_fact_list` for the idx-8 shape: when every non-empty line in
  the block starts with an identical leading column that is not a claim (`STATEMENT`, `FACT`, `CLAIM`),
  strip it before field assignment.
- For idx 4: do **not** reject the fact — reject only the URL (already the behaviour) — but **record the
  `[cite: N]` marker** so redirect resolution can use it.

### D-V01-11 — Resolve gemini redirects at ingest (spec § 1.3)

Every `vertexaisearch` grounding redirect resolves via a plain `302` to a publisher URL; **225/225
resolved** on V-01, and they **expire ~30 days after the run**.

- **Dedupe first** (V-01: 642 instances → 225 unique).
- `HEAD`, follow one hop, read `Location`. No API key; parallelises trivially.
- On failure **keep the redirect and mark it unresolved — never drop a citation**.
- Store the publisher URL **alongside** the redirect, not instead of it.
- This is the durable form of the one-off preservation already committed as
  `docs/tribunal-run-reports/run-20260728-7dcf51d5-CITATIONS.tsv`.

### Smalls in the same wave (spec § 1.4)

- **`gpt-5.6-sol` is absent from the cost table** → 5 NULL-cost calls per run and `run.cost_pending`
  never clears, so the recorded **$53.48 is a floor**. Add the model to the cost table.
- **`run_event` rows dropped** (D-V01-7): ~20 `KeyError`/`TypeError` in `emit_safe` for
  `stage='deep_research' kind='agent_done'` and `stage='own_research'`. Phase 15.3's feed is incomplete
  until this is fixed.
- **`own` researcher reports in English** in a Dutch run (D-V01-3) — 17 claims that can never match
  anything. **Not fixed here:** D-R5 drops `own` from the rotation in Wave 3. Note it, leave it.

### Claude's Discretion

- Plan/wave decomposition and file-level task ordering.
- Where the separator constant and split helper live (module-private in `steps.py` is fine).
- The redirect resolver's concurrency, timeout, and where resolution is invoked in the ingest path.
- Test file organisation, beyond the named files that MUST be updated.
- Whether the `[cite: N]` marker capture lands as a column, a side table, or metadata — as long as it
  survives to where redirect resolution can consume it.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The plan of record
- `.planning/ENGINE-REDESIGN-SPEC.md` — § 0 (where this came from + cost baseline), **§ 2 (Wave 1 — the
  scope of this phase, with code excerpts)**, § 7 (what we are deliberately NOT doing), § 8
  (verification + the fixture `gcloud` command), § 9 (environment knobs). §§ 3–6 are Waves 2–5 — read for
  direction only; **nothing in them is in scope**.

### Evidence
- `docs/tribunal-run-reports/run-20260728-7dcf51d5-DIAGNOSTICS.md` — the two root causes, settled from
  the audit bucket. Read this before doubting any decision above.
- `docs/tribunal-run-reports/run-20260728-7dcf51d5-V01-FINDINGS.md` — full run forensics, with
  corrections applied. **Note:** several findings in it were later withdrawn or corrected; the
  DIAGNOSTICS doc and the spec supersede it where they disagree.
- `docs/tribunal-run-reports/run-20260728-7dcf51d5-CITATIONS.tsv` — the 225 preserved citations.

### Code under change
- `tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py` — `_build_distiller_prompt` (1185),
  `_parse_distiller_response` (1260), `_distill_unit` (1537), ZERO-claims warning (1580),
  `collect_provider_facts` (1820), `needs_distiller_fallback` branch (2002).
- `tribunal/nestor_pulse_sdk/pipeline/tribunal/facts.py` — `build_fact_list_prompt_block` (194),
  `parse_fact_list` (573), `_extract_region` / the `FACTS_START` sentinels.
- Tests that **pin the distiller prompt byte-for-byte**: `tests/test_claim_distiller.py`,
  `tests/test_distiller_coverage.py`. The docstring at `steps.py` ~1203 says so explicitly.

### Standing engine rules
- `.planning/RESEARCH-ENGINE-DECISIONS.md` — D1–D15, R1–R7, C1.
- `.planning/phases/15.3-.../15.3-CONTEXT.md` — the `run_event` emitter contract (D-01…D-12) that the
  dropped-rows small must respect: never-raising, PII-scrubbing, batched.

</canonical_refs>

<specifics>
## Specific Ideas

### The regression fixture is real — use it

`gs://project-cb01b861-cb4a-438d-b9a-nestor-audit/runs/7dcf51d5-1153-4374-b444-c25d17eeea01/`

The four distiller calls are those written **13:46:33Z–13:48:30Z**:

| blob prefix | expectation |
|---|---|
| `e9a168b5…`, `fe418029…` | the two **coffee** calls → must yield **278** claims through the new parser |
| `af1995b6…`, `7dcf4a14…` | the two that already worked → must **still** yield **43** and **143** |

```bash
gcloud storage ls "gs://project-cb01b861-cb4a-438d-b9a-nestor-audit/runs/7dcf51d5-1153-4374-b444-c25d17eeea01/" \
  --project=project-cb01b861-cb4a-438d-b9a | grep gemini-2.5-flash
```

Pull the blobs down once, commit them as a **test fixture** (redacted per the standing audit-blob
redaction rule — the SerpApi key rides in a query parameter), and assert the counts. This is a
known-good regression test with real data; it is not optional.

### The prompt-pinning tests are the point of review, not a fixup

`test_claim_distiller.py` / `test_distiller_coverage.py` assert the prompt byte-for-byte. The `|||`
contract change breaks them by design. Update them **in the same commit** as the prompt change, and
treat the diff as the substantive edit it is.

### Judge the engine from the delivered report

`output` row, `format='markdown'` — not the claim table, not the logs. Four V-01 findings had to be
withdrawn or corrected because they assumed a missing capability that was in fact working.

### The audit bucket answers what the logs cannot

`gs://project-cb01b861-cb4a-438d-b9a-nestor-audit/runs/<run_id>/` holds the full request **and response**
of every LLM call. Both gating diagnostics were settled from it.

</specifics>

<deferred>
## Deferred Ideas

**Waves 2–5 of `ENGINE-REDESIGN-SPEC.md` — not in this phase, not in these plans:**

- **Wave 2** — claim attribution (D-R3): sub-question + `corroboration_key` on the claim row. A **hard
  prerequisite for Wave 3**, but it starts only after Wave 1 has shipped and been measured.
- **Wave 3** — dispatch redesign (D-R4, D-R5): ≤5 LLM-formed groups × all providers; `own` dropped.
- **Wave 4** — the creative workshop loop (D-R6, D-R9): generative evolve, reasoned judges, meta-review,
  10-round cap with saturation exit.
- **Wave 5** — yield instrumentation (D-R8).
- **Discovery bracket** (D-R7) — evidence-anchored questions the client did not ask.

**Explicitly not being done at all right now** (spec § 7): touching the verification stage (**it works —
do not touch it**), claim clustering for corroboration (goes LAST, expect modest gains: only 37/396
claims have any cross-provider partner at Jaccard 0.2), rewriting the report writer, two-wave research,
deleting rejected claims from research text, provider routing by measured competence.

**Open questions the spec parks as "worth answering during Wave 1"** — investigate and record; fix only
if the answer lands inside this phase's scope:

- Is **396 persisted vs 426 distilled vs 293 kept** reconcilable? A `rejected_claims` output (9,189
  chars) probably explains it; never verified.
- **`gate_errors: 153`** — a third of the selected set errored in the gates and nothing surfaced it.
  Never investigated.
- **175 of 396 claims carry `null` certainty** (the distiller path). Is a claim with no provider-stated
  certainty acceptable in a client report?

**Unrelated debts, not this phase:** the operator's no-engine-behaviour-change attestation (D-03 gate),
15.3 plan 09's two operator checkpoints, the D-L elapsed clock check, **rotating `Nestor_Claude_Temp`**
(transited a chat in plaintext 2026-07-27, still live on both Tribunal services), the
`serviceAccountTokenCreator` grant decision. Art. 12 audit-trail deadline: **2026-08-02**.

</deferred>

---

*Phase: 15.4-research-engine-redesign-extraction-repair-wave-1*
*Context gathered: 2026-07-29 via PRD Express Path over `.planning/ENGINE-REDESIGN-SPEC.md` § 2*
