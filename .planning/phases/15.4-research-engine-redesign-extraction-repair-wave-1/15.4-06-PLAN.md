---
phase: 15.4-research-engine-redesign-extraction-repair-wave-1
plan: 06
type: execute
wave: 2
depends_on: ["15.4-01"]
files_modified:
  - docs/tribunal-run-reports/run-20260728-7dcf51d5-OPEN-QUESTIONS.md
autonomous: false
requirements: []
user_setup:
  - service: google-cloud-storage
    why: "The audit bucket and the run's `output` rows are the only places these three questions can be answered."
    dashboard_config:
      - task: "Same gcloud auth as plan 15.4-01. Database reads use the credential-free tribunal.run recipe recorded in .planning/STATE.md / the deploy runbook — read as worker_user, never app_user, or RLS returns a falsely empty result."
        location: "Cloud Shell"

must_haves:
  truths:
    - "The 396 persisted / 426 distilled / 293 kept discrepancy is either reconciled with numbers or recorded as unreconcilable with the reason"
    - "gate_errors: 153 has a named cause or a named next step, and it is stated whether anything surfaced it to an operator"
    - "The 175 null-certainty claims have a stated disposition for a client report"
    - "Nothing outside Wave 1's scope was changed as a result"
  artifacts:
    - path: "docs/tribunal-run-reports/run-20260728-7dcf51d5-OPEN-QUESTIONS.md"
      provides: "the three answers with their evidence, and an explicit in-scope / out-of-scope call per finding"
      min_lines: 40
  key_links:
    - from: "docs/tribunal-run-reports/run-20260728-7dcf51d5-OPEN-QUESTIONS.md"
      to: "docs/tribunal-run-reports/run-20260728-7dcf51d5-DIAGNOSTICS.md"
      via: "same-run evidence chain, cross-referenced"
      pattern: "DIAGNOSTICS"
---

<objective>
Answer the three questions the spec parks as "worth answering during Wave 1", from the run's own
recorded artifacts, and write down what was found.

Purpose: each of the three is a number nobody has ever explained, and two of them are the shape of
the defect this phase exists to fix — a loss that no operator was told about. Answering them now,
while the V-01 artifacts are still readable, is cheap. Answering them after the next run is not.

Output: ONE document. This plan changes no code. If an answer implies a fix, the fix is proposed in
writing and only implemented here if it lands inside Wave 1's already-agreed scope — otherwise it is
recorded for the wave that owns it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-CONTEXT.md
@.planning/ENGINE-REDESIGN-SPEC.md
@docs/tribunal-run-reports/run-20260728-7dcf51d5-DIAGNOSTICS.md
@docs/tribunal-run-reports/run-20260728-7dcf51d5-V01-FINDINGS.md

<interfaces>
Run id: `7dcf51d5-1153-4374-b444-c25d17eeea01`.
Audit bucket: `gs://project-cb01b861-cb4a-438d-b9a-nestor-audit/runs/7dcf51d5-1153-4374-b444-c25d17eeea01/`
— full REQUEST and RESPONSE of every LLM call. Both gating diagnostics were settled from it.

The three questions, verbatim from 15.4-CONTEXT `<deferred>`:
  1. Is **396 persisted vs 426 distilled vs 293 kept** reconcilable? A `rejected_claims` output
     (9,189 chars) probably explains it; never verified.
  2. **`gate_errors: 153`** — a third of the selected set errored in the gates and nothing surfaced it.
  3. **175 of 396 claims carry `null` certainty** (the distiller path). Is a claim with no
     provider-stated certainty acceptable in a client report?

STANDING RULE, carried from three sessions and the cause of four withdrawn V-01 findings:
JUDGE THE ENGINE FROM THE DELIVERED REPORT — the `output` row with `format='markdown'` — not from
the claim table and not from the logs. Question 3 in particular is a question about the REPORT.

Known already, do not re-derive: `_normalise_fact_claim` (steps.py:1763) writes `certainty=None`
UNCONDITIONALLY on the `distiller_fallback` branch, as a security control (T-15.2-61), not as an
oversight. So "why are they null" has an answer in code; the open question is what the REPORT does
with them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the claim funnel and locate gate_errors: 153</name>
  <read_first>
    - docs/tribunal-run-reports/run-20260728-7dcf51d5-V01-FINDINGS.md (the funnel section — the source of the three numbers)
    - tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py (lines 2033-2110 — where distilled claims become persisted claims, and what is dropped between them)
    - tribunal/nestor_pulse_sdk/verification/report.py (how gate_errors is counted and where, if anywhere, it is surfaced)
  </read_first>
  <action>
Question 1 — the funnel. Pull the run's `rejected_claims` output (9,189 chars) and any sibling
`output` rows, plus the claim counts from the run's own recorded artifacts. Account for the deltas
426 → 396 and 396 → 293 with named mechanisms and integers that add up. Candidate mechanisms to test
against the data, not to assume: `_dedupe_claims` merges, `_normalise_fact_claim` returning None
(`unusable_claims`), `_NOT_FOUND_TOTAL_MAX` / `_MAX_FACTS` caps, and gate drops. If the numbers do
not reconcile, say so with the residual, and name what would be needed to close it.

Question 2 — `gate_errors: 153`. Find where the counter is incremented, what an "error" is at that
site, and whether ANY surface (log line at WARNING or above, verification report, feed, run status)
told a human about it. The answer to "did anything surface it" is the load-bearing half — this
phase's whole thesis is that a silent loss is the defect.

Record both with their evidence (blob ids, SQL, file:line), not with conclusions alone.
  </action>
  <acceptance_criteria>
    - The funnel section states three integers and the arithmetic between them, with each delta attributed to a named mechanism at a named file:line — or states the residual explicitly as unexplained.
    - The `gate_errors` section names the increment site as `file.py:line` and answers, in one sentence, whether an operator could have seen it — with the log level or surface named.
    - Every number in the document is traceable to a command, a blob id, or a file:line quoted in the document.
  </acceptance_criteria>
  <done>Questions 1 and 2 are answered from the run's own artifacts, with evidence.</done>
  <files>docs/tribunal-run-reports/run-20260728-7dcf51d5-OPEN-QUESTIONS.md</files>
  <verify>
    <automated>grep -nE "(396|426|293|gate_errors|153)" docs/tribunal-run-reports/run-20260728-7dcf51d5-OPEN-QUESTIONS.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Decide what a null-certainty claim does in the delivered report</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py (lines 1719-1785 — _normalise_fact_claim's unconditional NULLs and the T-15.2-61 reasoning)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/facts.py (lines 83-96 — CERTAINTY_VALUES and the G-11 fail-toward-more-checking default)
    - the run's delivered report (`output` row, `format='markdown'`) — read the actual prose, not the claim table
  </read_first>
  <action>
Establish what the DELIVERED report does with a claim whose `certainty` is NULL: is it hedged, is it
asserted flatly, is it indistinguishable from a `certain` claim? Quote the report.

Then state a disposition in one of exactly three forms, and no other:
  (a) acceptable as-is, with the reason;
  (b) needs a report-writer change — OUT OF SCOPE here (the report writer is on spec § 7's
      deliberately-not-doing list), recorded for the wave that owns it;
  (c) needs a change inside Wave 1's agreed scope — in which case name the exact file and function
      and STOP, reporting it to the operator rather than implementing it inside an investigation task.

Note in passing, without acting on it, that the `<TAB>` fix will change the mix: 278 recovered
distiller claims all arrive on the null-certainty path, so whatever this ratio is today, the next
run's will be larger. That is a consequence worth writing down before the run, not after.
  </action>
  <acceptance_criteria>
    - The document quotes at least one passage of the DELIVERED markdown report showing how a null-certainty claim reads to the client.
    - The disposition is exactly one of (a), (b), (c) and is labelled as such.
    - If (c), no code was changed and the plan summary says the operator was asked.
    - `git status --porcelain` shows exactly one changed path: the new document.
  </acceptance_criteria>
  <done>The null-certainty question has a written disposition grounded in the delivered report.</done>
  <files>docs/tribunal-run-reports/run-20260728-7dcf51d5-OPEN-QUESTIONS.md</files>
  <verify>
    <automated>git status --porcelain</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator reads the three answers and confirms nothing new enters this phase</name>
  <what-built>`docs/tribunal-run-reports/run-20260728-7dcf51d5-OPEN-QUESTIONS.md` — three answers with evidence and a scope call on each.</what-built>
  <how-to-verify>
    1. Read the funnel arithmetic: do the integers add up, or is the residual stated?
    2. Read the `gate_errors` answer: was a human ever told? If the answer is no, that is the same
       failure class as the `<TAB>` drop and deserves a decision now.
    3. Read the null-certainty disposition and confirm the (a)/(b)/(c) call.
    4. Confirm that nothing here has expanded Wave 1's scope. Anything that would is deferred in
       writing to the wave that owns it.
  </how-to-verify>
  <resume-signal>Type "approved", or name the item you want pulled into this phase.</resume-signal>
  <files>docs/tribunal-run-reports/run-20260728-7dcf51d5-OPEN-QUESTIONS.md</files>
  <action>Present the finished document to the operator and wait. Do not commit any code change arising from a finding without the operator's answer; if a finding calls for a change outside Wave 1's agreed scope, record it in the document for the wave that owns it rather than implementing it here.</action>
  <verify>
    <human-check>The operator has read the three answers and answered "approved" or named an item to pull into this phase.</human-check>
  </verify>
  <done>The operator has confirmed the answers and that no new scope entered Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit bucket / run DB → a committed document | Run content quoted into a repo document under 7-year retention |
| production database → read session | A read against live tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.4-15 | Information disclosure | quoted report prose and blob content in the committed document | mitigate | Quote only what the finding needs; run the same credential-pattern scan plan 15.4-01 defined over the finished document before committing, and record its output |
| T-15.4-16 | Tampering | production data during investigation | mitigate | READS ONLY — `SELECT` and `gcloud storage cat`; no `UPDATE`, no `DELETE`, no migration, no service redeploy. Stated in the plan summary |

No package-manager install in this plan; no source file is modified.
</threat_model>

<verification>
- Exactly one file changed, and it is a document.
- Every number in it is traceable to a command, blob id or file:line.
- The operator has signed off that no new scope entered Wave 1.
</verification>

<success_criteria>
- The three parked questions are answered or explicitly declared unanswerable, with evidence, before the next live run consumes the chance to answer them.
</success_criteria>

<output>
Create `.planning/phases/15.4-research-engine-redesign-extraction-repair-wave-1/15.4-06-SUMMARY.md` when done
</output>
