---
phase: 21-research-run-feed-completion-silent-post-research-stages-stu
plan: 08
type: execute
wave: 5
depends_on: [21-01, 21-02, 21-04, 21-07]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md
autonomous: false
requirements: [SC1, SC2, SC3, SC4, SC5, SC6, D-01, D-02]

must_haves:
  truths:
    - "The deploy surface is DERIVED from the phase's actual diff, not copied from a standing note"
    - "Both Cloud Build gates are green on the merged tree before anything is deployed"
    - "Every deployed revision is proven by its status.imageDigest, never by its mutable image tag"
    - "The operator has walked the run page on RECORDED data and signed off on all six success criteria"
    - "No research run is triggered by this plan"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "A Phase 21 section: the derived surface, the gates, the ordered deploy, and the read-back proofs"
      contains: "Phase 21"
    - path: ".planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md"
      provides: "The recorded-run walkthrough, one check per ROADMAP success criterion, with the operator's verdicts"
  key_links:
    - from: "infra/DEPLOY-RUNBOOK.md"
      to: "the phase diff"
      via: "a derivation step that lists changed paths and maps them to services"
      pattern: "git diff --name-only"
---

<objective>
Ship Phase 21 and prove it shipped, then hand the operator a run page worth spending forty-five
dollars in front of.

Purpose: D-01 is an OPERATOR decision — Phase 21 ships BEFORE the first measured run, so that one
run validates the three unexercised changes deployed at `20260806-175613` AND this feed at the
same time. Running first would spend it watching a view that reported nothing for eight of
thirteen stages.

Output: a Phase 21 runbook section, two green Cloud Build gates, a digest-proven deploy, and a
recorded-run UAT with the operator's sign-off against each of the six success criteria.

⛔ THIS PLAN DOES NOT TRIGGER A RESEARCH RUN. That is the next thing after this phase, not part of
it. Every check below is free and read-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONTINUE-HERE.md
@.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-CONTEXT.md

<measured_facts>
Live state as of 2026-08-10, from `.planning/CONTINUE-HERE.md`, re-verified that day.

1. THREE SERVICES ARE LIVE AT TAG `20260806-175613` AND NOT ONE RUN HAS EXECUTED ON THEM.
   `tribunal-api-20260806-175613-180706`, `tribunal-worker-20260806-175613-180925`,
   `nestor-api-00045-hdw`. The newest audit write anywhere in the bucket is still
   `2026-08-05T19:21:31Z` and the run prefix count is still 9. Everything in that deploy is
   unexercised — which is exactly why D-01 sequences this phase before the measuring run.

2. ⛔ THE ACCOUNT TRAP. The active gcloud account had silently switched to
   `mohamed.ajimi@agiliz.com` between 08-06 and 08-10. Four accounts exist on this machine.
   Nestor Pulse is `tools@dotto.be` / `project-cb01b861-cb4a-438d-b9a`. Verify BOTH, EVERY time.
   AND THE SECOND HALF OF THE TRAP: `--format='value(...)'` renders a PERMISSION ERROR AS AN
   EMPTY STRING. Three services and a whole bucket listing came back blank and read exactly like
   "the resources are gone". They were fine; the identity was wrong. When a `value()` read comes
   back empty, RE-RUN IT WITHOUT `--format` before believing it.

3. D-02 AND THE REASON IT EXISTS. 21-CONTEXT records the surface as "`tribunal-worker` plus
   `nestor-frontend`" and then says, in the same paragraph, RE-DERIVE IT FROM THE ACTUAL DIFF AND
   DO NOT TRUST THAT LINE. The reason is on the record: on 2026-08-06 a standing note said two
   services, the diff said three, and skipping `nestor-api` would have left the whole fix inert
   while reading as deployed. CONTINUE-HERE states the generalisation: "Which services a change
   touches is a MEASUREMENT WITH AN EXPIRY DATE, not a fact."

4. G-1, THE DIGEST TRAP. `containers[0].image` is a MUTABLE TAG. Read `status.imageDigest` off
   the revision. A revision whose image resolves to `:latest` is not pinned and is not proof.

5. THE FREE QUEUE CANARY. The always-on worker polls every 2s at `minScale=1`, so any claimable
   row would ALREADY have been claimed and would be writing audit blobs. Listing the audit bucket
   for the newest write answers "is anything running or claimable" read-only, in about thirty
   seconds, with no DB credential and no Cloud Build. Cheaper and stronger than the § 15.2.k queue
   recipe. ⚠ A `for f in $(gcloud storage ls ...)` loop silently produced 0-byte files for 3 of 4
   once — always `ls -la` after.

6. THE TWO GATES AND HOW TO READ THEM. `tribunal/cloudbuild.test-engine.yaml` (44 files) and
   `tribunal/cloudbuild.test-gates.yaml` (13 files). NEVER pipe `gcloud builds submit` through
   `tail` — that returns the PIPE's exit code and a FAILED build reports exit 0. Read the build
   TEXT: `gcloud builds describe BUILD_ID` and `gcloud builds log BUILD_ID`. SUCCESS and FAILURE
   are results; QUEUED, WORKING, EXPIRED and CANCELLED are not, and an EXPIRED build is visually
   identical to a QUEUED one.

7. FRONTEND: `package-lock.json` IS committed. Always `npm ci`, never `npm install`. CLAUDE.md's
   "no lockfile committed" line is stale and wrong; following it caused a Radix/React #185
   production break on 2026-07-21. `frontend/scripts/i18n-audit.mjs` CHECK B is a HARD gate.

8. MIGRATION EXPECTATION: NONE. This phase adds no column, no table and no event kind — the
   `run_event` table and `RUN_EVENT_KINDS` are untouched. But 21-CONTEXT's D-02 says CONFIRM
   RATHER THAN ASSUME, and this project has an unpaid-migration-proof habit. Derive the answer
   from the diff in Task 1 rather than from this paragraph.

9. `.planning/` is gitignored (`.gitignore:32`) — every planning artifact needs `git add -f`,
   including paths already tracked.
</measured_facts>

<interfaces>
The runbook's existing shape, for the new section to match: `infra/DEPLOY-RUNBOOK.md` carries one
`## Phase NN — ...` section per deploy, each with a numbered ordered procedure, `# expect ...`
comments beside every read-back, and an explicit CONFIRM-ONLY block listing the services the
deploy does NOT touch (see the Phase 15.8 section around line 3637 and its read-back block
around 4060-4080 — that is the template).

The build and deploy scripts: `tribunal/infrastructure/cloud-run/build-and-push.sh`,
`deploy-api.sh`, `deploy-worker.sh`. `--set-secrets` in the deploy SCRIPTS is CORRECT — they
compose the full set on purpose; the `--update-secrets` rule governs hand-typed
`gcloud run services update` only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the deploy surface from the diff, and write the runbook section</name>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md lines 3637-3700 and 4050-4090 (the Phase 15.8 section and its read-back block — the template to match)
    - infra/DEPLOY-RUNBOOK.md lines 2958-3060 (the Step 15.2.k combined deploy, for the multi-artifact ordering idiom)
    - .planning/CONTINUE-HERE.md (the standing cautions in full — the account trap, the gates, the canary, the digest rule)
    - tribunal/infrastructure/cloud-run/build-and-push.sh, deploy-api.sh, deploy-worker.sh
    - The seven SUMMARY files from this phase's plans 21-01 through 21-07
  </read_first>
  <action>
FIRST, DERIVE THE SURFACE. Do not write a word of the runbook until this is done.

Run `git diff --name-only <the commit before 21-01 landed>..HEAD` over the merged tree and produce
the list of every changed path. Then map each path to the artifact that ships it, using these
rules and stating the rule beside each mapping:
  - anything under `tribunal/nestor_pulse_sdk/` that the WORKER imports ships in the
    `tribunal-worker` image;
  - anything under `tribunal/nestor_pulse_sdk/` that the API's request path imports also ships in
    the `tribunal-api` image — CHECK, do not assume the split. `runs/stages.py` in particular is
    read by `RunMetrics.stages` on the API side, so a change there is an API change;
  - anything under `frontend/src/` ships in the `nestor-frontend` build;
  - anything under `backend/app/` ships in `nestor-api`;
  - `tests/`, `cloudbuild.*.yaml`, `.planning/` and `docs/` ship nothing.

WRITE THE DERIVED SURFACE DOWN AS A TABLE, path-group to service, with the count of changed files
in each group. If the derived surface differs from 21-CONTEXT's D-02 line ("`tribunal-worker` plus
`nestor-frontend`"), SAY SO EXPLICITLY IN THE RUNBOOK — that difference is the whole reason D-02
tells you to re-derive, and the 2026-08-06 near-miss is what happens when nobody writes it down.

ALSO DERIVE THE MIGRATION ANSWER: grep the diff for any path under an `alembic` or `versions`
directory and for any change to a model file. Record "no migration" as a DERIVED result with the
command that derived it, not as an assumption.

THEN write `## Phase 21 — Research run feed completion` into `infra/DEPLOY-RUNBOOK.md`, matching
the file's existing section shape. It contains, in order:

1. WHAT SHIPPED, in one short paragraph plus the derived surface table. Name the eight stages that
   gained bodies, the spinner and toggle fixes, the verification report wiring, the density
   ruling's outcome, and the stage-label fix. State plainly that this deploy changes OBSERVABILITY
   ONLY: no claim, verdict, cost or dispatch behaviour changed, and the `await set_stage(` count is
   unchanged from before the phase — cite the counts recorded in the 21-03, 21-05 and 21-06
   SUMMARYs.

2. PRE-FLIGHT, as numbered commands with `# expect` beside each:
   a. `gcloud config list --format="value(core.account,core.project)"` — expect
      `tools@dotto.be` and `project-cb01b861-cb4a-438d-b9a`. Beside it, the warning that an EMPTY
      result means a permission error, not an absent resource, and must be re-run without
      `--format` before it is believed.
   b. THE FREE QUEUE CANARY: list the audit bucket for the newest write and confirm nothing is
      running or claimable. Include the `ls -la`-after warning about 0-byte blobs.
   c. Both Cloud Build gates, submitted separately, each read via
      `gcloud builds describe` and `gcloud builds log` — with the explicit instruction never to
      pipe through `tail`, and the reminder that EXPIRED looks like QUEUED.
      State the expected engine-gate line: `collecting: 44 of 44 expected files`. State that the
      pass count must be READ AND RECORDED, not asserted from memory, and that a count which did
      not rise must be EXPLAINED WITH A MECHANISM rather than merely noted.
   d. `cd frontend && npm ci && npm run build && node scripts/i18n-audit.mjs && npx vitest run`.
      `npm ci`, NEVER `npm install`.

3. THE ORDERED DEPLOY, one numbered step per service in the derived surface, using the existing
   scripts. No migration step unless Task 1's derivation found one — and if it did, the proof is
   the literal `Running upgrade NNNN -> NNNN` line and never exit 0.

4. READ-BACK PROOFS, matching the Phase 15.8 block's shape:
   a. every new revision name at 100% traffic, DIGEST-PINNED — read `status.imageDigest`, never
      `containers[0].image`, and state G-1's reason beside it;
   b. a CONFIRM-ONLY block naming the services this deploy does NOT touch, with their existing
      revision names recorded;
   c. secret bindings by NAME only, never a value;
   d. the worker's plain env READ BACK, not asserted.

5. A closing note recording, for the next reader: that the ~$45 measuring run is the NEXT action
   and is deliberately NOT part of this deploy; and that per CONTINUE-HERE the next measured run
   is a NEW BASELINE, not a comparison against `368ff3a0`, because `260806-lvt` changed the
   report's shape and `260806-o96` changed which claims reach paid verification.

Do not run the deploy in this task. This task derives and documents.
  </action>
  <verify>
    <automated>grep -n "## Phase 21" infra/DEPLOY-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - The runbook contains a `## Phase 21` section: `grep -c "^## Phase 21" infra/DEPLOY-RUNBOOK.md` is 1.
    - The section contains a DERIVED surface table produced by an actual command, and the command appears in the section: `grep -n "git diff --name-only" infra/DEPLOY-RUNBOOK.md` matches inside it.
    - The section states whether the derived surface AGREES OR DISAGREES with 21-CONTEXT's D-02 line, in words. `grep -ci "D-02" infra/DEPLOY-RUNBOOK.md` is at least 1 within the Phase 21 section.
    - The migration answer is derived, not assumed: the section names the grep that produced it.
    - The digest rule is present and names the right field: `grep -n "status.imageDigest" infra/DEPLOY-RUNBOOK.md` matches inside the Phase 21 section, and `containers\[0\].image` appears there only as the thing NOT to read.
    - The account check names both the account and the project literally, and carries the empty-value-string warning.
    - The engine-gate expectation is the literal `collecting: 44 of 44 expected files`.
    - `npm ci` appears and `npm install` does not, within the Phase 21 section.
    - The section says explicitly that no research run is triggered by this deploy.
  </acceptance_criteria>
  <done>The deploy surface is derived from the real diff and written down with its derivation, and the runbook section is complete and matches the file's existing shape.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs the gates and the deploy</name>
  <files>none (Cloud Build + Cloud Run only)</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 21 (written in Task 1 — the ordered procedure being executed)
    - .planning/CONTINUE-HERE.md (the account trap, the empty-value-string trap, the free queue canary, the gate-reading traps, the digest rule)
    - tribunal/cloudbuild.test-engine.yaml and tribunal/cloudbuild.test-gates.yaml (the two gate configs and their expected file counts)
  </read_first>
  <action>
PAUSE. This is a checkpoint:human-action because a Cloud Build submit and a Cloud Run deploy need
the operator session — the agent gcloud is read-only and `builds submit` has been classifier-blocked
before. Do every read-only pre-flight the agent CAN do first and present the results, then hand the
operator the runbook procedure below and wait. Record their pasted build ids, statuses, revision
names and digests VERBATIM in 21-08-SUMMARY.md. Do not proceed to Task 3 until both gates are
reported green and every service in the derived surface table is reported deployed.
  </action>
  <what-built>
    Phase 21's full change set is merged: feed bodies for all eight previously silent stages, the
    spinner settle rule and the collapse-toggle gate, the verification report on the run page, the
    density ruling's outcome, and the stage-label fix. `infra/DEPLOY-RUNBOOK.md` now carries a
    `## Phase 21` section with a DERIVED deploy surface, both gate invocations, the ordered deploy
    and the read-back proofs.
  </what-built>
  <how-to-verify>
    This is a checkpoint:human-action because a Cloud Build submit and a Cloud Run deploy require
    the operator's session — the agent's gcloud is read-only and `builds submit` has been
    classifier-blocked before. Everything the agent CAN do read-only, it has already done.

    Follow `infra/DEPLOY-RUNBOOK.md` § Phase 21 exactly, in order.

    1. VERIFY THE ACCOUNT AND THE PROJECT FIRST. `tools@dotto.be` /
       `project-cb01b861-cb4a-438d-b9a`. It drifted once already. If a `value()` read comes back
       empty, re-run it without `--format` before believing the resources are gone.
    2. Run the free queue canary. Confirm nothing is running or claimable. `ls -la` after any
       bucket copy loop.
    3. Submit BOTH Cloud Build gates. Read each result with `gcloud builds describe` and
       `gcloud builds log` — never through `tail`. Record the engine gate's `collecting:` line and
       its pass/fail/skip counts, and the gates config's counts. A count that did not rise needs a
       MECHANISM, not a shrug.
    4. Only if both gates are green: run the ordered deploy for every service in the derived
       surface table. Do not deploy a service the table does not list, and do not skip one it does.
    5. Run every read-back proof. Digests from `status.imageDigest`. Record the new revision names.
    6. Do NOT trigger a research run.
  </how-to-verify>
  <verify>
    <human-check>Two Cloud Build ids with SUCCESS read from gcloud builds describe, the literal line collecting: 44 of 44 expected files, and every new revision name with an @sha256: digest read from status.imageDigest.</human-check>
  </verify>
  <resume-signal>Paste the two build ids with their statuses, the new revision names with their digests, and type "deployed" — or describe what went wrong.</resume-signal>
  <acceptance_criteria>
    - Both Cloud Build ids are recorded with a status of SUCCESS read from `gcloud builds describe`, not from a shell exit code.
    - The engine gate's line reads `collecting: 44 of 44 expected files`, recorded verbatim.
    - Every deployed revision's `status.imageDigest` is recorded as an `@sha256:` value; no revision resolves to `:latest`.
    - The set of deployed services equals the derived surface table from Task 1 — no extra, none missing. State the comparison explicitly.
    - The CONFIRM-ONLY services' existing revision names are recorded unchanged.
    - No run was triggered: the audit bucket's newest write timestamp after the deploy equals the one recorded in pre-flight.
  </acceptance_criteria>
  <done>Both gates green, every service in the derived surface deployed and digest-proven, nothing else touched, no run triggered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator UAT on recorded data — one check per success criterion</name>
  <files>.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 21 (the six success criteria, verbatim — the walkthrough is one check per criterion)
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-06-SUMMARY.md (the capstone test result, which is SC1 actual proof)
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-04-SUMMARY.md (the density ruling, so SC5 is judged against what was actually decided)
    - docs/design/prototypes/ResearchRunImproved.tsx (header comment — the six things the feed must preserve)
  </read_first>
  <action>
PAUSE. Present the walkthrough below and wait. Create 21-UAT.md with one section per success
criterion as the operator answers, recording PASS, FAIL or NOT-OBSERVABLE with what was actually
seen. NOT-OBSERVABLE is a legitimate verdict and must be used honestly for anything a pre-deploy
recorded run structurally cannot show. Record every FAIL verbatim and route it rather than
absorbing it. Trigger no research run.
  </action>
  <what-built>
    The dedicated run page at `/admin/pulse/runs/:runId`, deployed, with all six of the phase's
    success criteria in place.
  </what-built>
  <how-to-verify>
    FREE AND READ-ONLY. Walk an EXISTING recorded run — there are nine run prefixes in the audit
    bucket. Do not trigger a new run.

    Create `.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-UAT.md`
    with one section per criterion and record a verdict of PASS, FAIL or NOT-OBSERVABLE for each,
    with what was actually seen. NOT-OBSERVABLE is a legitimate verdict here and it must be used
    honestly: the recorded runs predate this phase's emit sites, so a run that finished before the
    deploy HAS NO ROWS for the eight stages and CANNOT show them. Marking that PASS would be the
    exact false green this project keeps catching.

    1. SC1 — all thirteen stages emit. On a PRE-DEPLOY recorded run this is NOT-OBSERVABLE by
       construction; say so. What IS observable now: the page renders without error, the stage
       dividers carry human labels, and the eight stages that used to render as a label with
       nothing under them still render a label — the difference will appear on the next run. The
       end-to-end proof for SC1 is the capstone test in plan 21-06, not this walkthrough.
    2. SC2 — no finished agent spins. On a TERMINAL recorded run, confirm there is not one
       animated spinner anywhere in the feed. This IS observable on recorded data and it is the
       single clearest check in this list.
    3. SC3 — the "Show more" toggle appears only where rows are hidden. Confirm no toggle sits
       above an empty phase, and that where a toggle IS shown, clicking it reveals rows.
    4. SC4 — the verification report is reachable from the run page. Open it. Confirm the funnel,
       verdicts and cost render and THE FEED IS STILL BELOW IT. If a failed or cancelled recorded
       run exists, confirm the toggle is offered there too.
    5. SC5 — density. Record whether the deep-research phase reads better or worse than it did on
       2026-08-10, and note that the density ruling from plan 21-04 may deliberately have been
       "no change to that file" — in which case the improvement expected here comes from the
       collapse toggle and from the other stages gaining bodies on the NEXT run.
    6. SC6 — no raw stage key on screen. Scan every divider on the page and confirm none is a
       snake_case string.

    Finally: confirm the clock does not restart when the page is closed and reopened (15.3 D-01/
    D-09 — this phase must not have regressed it), and confirm the intake detail card's embedded
    `ResearchRunProgress` still works, since it was deliberately NOT replaced.
  </how-to-verify>
  <verify>
    <human-check>21-UAT.md carries a PASS / FAIL / NOT-OBSERVABLE verdict for each of the six success criteria plus the clock-does-not-restart and embedded-card regression checks.</human-check>
  </verify>
  <resume-signal>Type "approved" with the six verdicts, or describe the failures.</resume-signal>
  <acceptance_criteria>
    - `21-UAT.md` exists with one section per success criterion and an explicit PASS / FAIL / NOT-OBSERVABLE verdict in each.
    - SC1 is recorded as NOT-OBSERVABLE on a pre-deploy recorded run, with the reason, and points at plan 21-06's capstone test as its actual proof. Any other verdict for SC1 on recorded data must be justified by naming a run that executed AFTER the deploy.
    - SC2's verdict is based on an actual terminal recorded run, and names the run id.
    - SC4's verdict confirms the feed remained rendered while the report was open.
    - The clock-does-not-restart check and the embedded-card check are both recorded.
    - Every FAIL is recorded VERBATIM as the operator described it, not paraphrased, and routed — either to a gap-closure plan in this phase or explicitly deferred with the operator's agreement.
  </acceptance_criteria>
  <done>All six criteria have honest verdicts recorded in `21-UAT.md`, the two regression checks pass, and any failure is routed rather than absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → GCP | `gcloud` with an identity that may not be the intended one. Four accounts exist on this machine. |
| built image → running revision | The tag is mutable; only the digest identifies what actually runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08-01 | Spoofing | wrong gcloud identity | mitigate | Pre-flight step (a) verifies BOTH account and project literally, and carries the measured warning that `--format='value(...)'` renders a permission error as an empty string. This exact trap cost real confusion on 2026-08-10. |
| T-21-08-02 | Tampering | a deployed revision that is not the built image | mitigate | Every read-back proof reads `status.imageDigest` and requires an `@sha256:` value. `containers[0].image` is named in the runbook only as the thing NOT to read (G-1). |
| T-21-08-03 | Repudiation | an inert deploy that reads as successful | mitigate | The deploy surface is DERIVED FROM THE DIFF in Task 1 and compared against the deployed set in Task 2's acceptance criteria. This is the 2026-08-06 near-miss control: a standing note said two services and the diff said three. |
| T-21-08-04 | Denial of service | an unintended paid run | mitigate | The free queue canary runs BEFORE the deploy and the newest-audit-write timestamp is compared AFTER it. `min-instances=0` does NOT stop a worker booting — the loop CLAIMS FIRST and SLEEPS LAST — so an empty queue is the only protection, and this plan triggers nothing. |
| T-21-08-05 | Tampering | a gate that goes green having run less than it claims | mitigate | The engine gate's `collecting: 44 of 44 expected files` line is recorded VERBATIM, results are read with `gcloud builds describe`/`log` and never through `tail`, and a pass count that did not rise requires a stated mechanism. |
| T-21-08-06 | Tampering | frontend dependency drift | mitigate | `npm ci` only. `npm install` is named in the runbook as forbidden; following the stale CLAUDE.md line caused a production break on 2026-07-21. |
| T-21-08-SC | Tampering | npm/pip/cargo installs | mitigate | The only install is `npm ci` from the COMMITTED `package-lock.json`, which resolves to already-pinned versions and introduces no new package. No `requirements.txt` change ships in this phase; Task 1's derivation confirms it from the diff. |
</threat_model>

<verification>
1. `grep -c "^## Phase 21" infra/DEPLOY-RUNBOOK.md` is 1, and the section carries a derived surface table with its derivation command.
2. Both Cloud Build ids recorded with SUCCESS from `gcloud builds describe`; engine gate line `collecting: 44 of 44 expected files` recorded verbatim.
3. Every deployed revision recorded with an `@sha256:` digest from `status.imageDigest`.
4. Deployed service set equals the derived surface table, stated as an explicit comparison.
5. `21-UAT.md` carries six honest verdicts plus the two regression checks.
6. Audit-bucket newest-write timestamp unchanged across the deploy — no run was triggered.
</verification>

<success_criteria>
- Phase 21 is live, and what is live was derived from the diff rather than copied from a note whose expiry date had passed.
- Every revision is digest-proven; no service was deployed that the diff did not require, and none was skipped that it did.
- The operator has walked the page and recorded an honest verdict for each of the six criteria, with NOT-OBSERVABLE used where recorded data genuinely cannot show a post-deploy behaviour.
- D-01 is satisfied: the feed is fixed BEFORE the ~$45 run, so one run validates both it and the three unexercised changes from `20260806-175613`.
</success_criteria>

<output>
Create `.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-08-SUMMARY.md` when done.
Record: the derived surface table and whether it matched D-02's line; both build ids, statuses and
counts; every new revision name with its digest; the CONFIRM-ONLY services; the six UAT verdicts;
and a short paragraph for `.planning/CONTINUE-HERE.md` replacing its "the deployed code has never
run" note with the new live state and the reminder that the next measured run is a NEW BASELINE.
Remember `git add -f` for every `.planning/` artifact.
</output>
