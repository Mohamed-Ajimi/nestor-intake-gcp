---
phase: 21-research-run-feed-completion-silent-post-research-stages-stu
plan: 06
type: execute
wave: 3
depends_on: [21-05]
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
  - tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py
autonomous: true
requirements: [SC1, D-03, D-04, D-05, D-06]

must_haves:
  truths:
    - "Adjudication reports how many claims survived, how many were dropped, and names the dropped ones"
    - "The coverage gate reports something — it is currently the emptiest of the eight, advancing its marker with no detail at all"
    - "A coverage re-entry, and a re-entry blocked by the skeptic circuit breaker, each say so as their own row"
    - "Conflict detection reports the contradictions it found and which side lost"
    - "Final synthesis reports what it is writing from, and reports again on the resume path where no stage closure ever ran"
    - "All thirteen declared tribunal stages now emit at least one event that is neither a divider nor a summary"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py"
      provides: "The adjudicate, coverage, conflict and synthesize emit helpers"
      exports: ["emit_adjudicate_dispatch", "emit_adjudicate_drop", "emit_adjudicate_done", "emit_coverage_dispatch", "emit_coverage_reentry", "emit_coverage_blocked", "emit_coverage_done", "emit_conflict_dispatch", "emit_conflict_finding", "emit_conflict_done", "emit_synthesize_dispatch", "emit_synthesize_scrubbed", "emit_synthesize_writing"]
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      provides: "The four remaining stages' emit call sites, including the module-level _write_final_report path"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py"
      via: "stage_events.emit_adjudicate_* / emit_coverage_* / emit_conflict_* / emit_synthesize_* calls"
      pattern: "stage_events\\.emit_(adjudicate|coverage|conflict|synthesize)"
---

<objective>
Close SC1: give the last four silent stages — `adjudicate`, `coverage`, `conflict` and
`synthesize` — bodies, so no declared tribunal stage renders as a label with nothing under it.

`coverage` is the emptiest of the eight and gets the most attention here. Its site at
`pipeline.py:3414` is `await set_stage(run_id, tenant_id, "coverage")` with NO detail argument at
all, so it reports neither rows nor a meaningful action count, and its automatic `summary` line
renders nearly empty because `state["items"]` stays 0 for a stage that never reported detail.
That last point is 21-CONTEXT's `<specifics>` hypothesis and this plan confirms or refutes it by
measurement rather than repeating it.

Output: four stages' helpers appended to `stage_events.py`, their call sites in `pipeline.py`
including the module-level `_write_final_report` path, and assertions in the already-registered
`tests/test_run_event_emit.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-CONTEXT.md
@.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-03-SUMMARY.md
@.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-05-SUMMARY.md

<measured_facts>
Read out of the tree at `30443e8` during planning.

1. THE SPINE AND SIX STAGES ARE ALREADY LANDED. `stage_events.py` carries `MAX_ROWS_PER_STAGE`,
   `RowBudget`, the clip helpers and the emitters for `verify` (21-03) and `distill` / `merge` /
   `gate` (21-05). This plan EXTENDS that module. Read both SUMMARYs for the exact public
   surface before writing a line.

2. NO CI CONFIG EDIT. `tests/test_run_event_emit.py` is already registered and `EXPECTED_FILES`
   is 44. This plan adds no test FILE, so neither moves.

3. `coverage` IS THE EMPTIEST STAGE. `pipeline.py:3414` is a bare marker advance with no `detail`
   argument. It is followed by `check_coverage(claims, adjudications, selected_only=True)` at
   3415 and a bounded re-entry loop at 3422-3459 whose blocked branch calls `_note_degradation`
   at 3445. NONE of that reaches the feed today.
   ⚠ THE COST TRAP AT 3407-3413 IS LOAD-BEARING AND MUST NOT BE DISTURBED: `selected_only=True`
   is passed EXPLICITLY, and without that intersection the recorded population's 738
   gate-dropped / SKIP_STABLE claims all read as uncovered and the loop fans out roughly 2,100
   Anthropic sessions. This plan adds rows around that call and changes NO argument to it.

4. `synthesize` HAS TWO CALL SITES IN DIFFERENT SCOPES. Line 3749 is inside `_run_staged`'s
   closure; line 4179 is inside the MODULE-LEVEL `_write_final_report`, which is ALSO the resume
   path's entry point (`run()` calls it directly from the synthesis cache). The comment at
   4168-4176 explains why that function is deliberately not re-plumbed through the closure.
   Both sites need a feed row, and the second one is the only reason a RESUMED run's synthesis is
   visible at all.

5. `adjudicate` has ONE site (3372-3378) with `survivors`, `dropped` and `claims` in scope.
   `conflict` has ONE site (3589-3596) with `group_reconciliations` in scope at emit time, and
   `conflicts`, `loser_idxs`, `conflict_losers` and `contested_notes` available AFTER the
   detector returns at 3597-3628.

6. `_factcheck_dropped_ids` at line 3552 is the snapshot that distinguishes a claim dropped by
   FACT-CHECK from one dropped as the weaker side of a CONFLICT, and `rejected_claims` at
   3710-3719 stamps `failed_factcheck` or `lost_conflict` on each. That distinction is exactly
   what an adjudicate row and a conflict row should each name, and it already exists — do not
   re-derive it.

7. `RUN_EVENT_KINDS` is closed (D-03). These four stages use `dispatch`, `plan`, `thinking`,
   `agent_fail` and `agent_done` only.
</measured_facts>

<interfaces>
The four stages' existing call sites in `pipeline.py`:

| stage | site(s) | what is in scope there |
|---|---|---|
| adjudicate | 3367-3378 | `adjudication_result`, `survivors`, `dropped`, `claims`, `SURVIVAL_RULE` |
| coverage | 3414 (bare marker), 3415-3459 | `coverage` dict with `pass` and `uncovered`, `reentry_count`, `MAX_REENTRY`, `reentry["blocked_reason"]`, `adjudications` |
| conflict | 3589-3596, then 3597-3628 | `group_reconciliations`, `contested_notes`, `survivors`, then `conflicts`, `loser_idxs`, `conflict_losers` |
| synthesize | 3748-3762 (in `_run_staged`), 4177-4179 (in module-level `_write_final_report`) | `survivors`, `dropped`, `cleaned_reports`; then `bundle`, `report_spec`, `anchor_ledger`, `numbered` |

The emitter contract, unchanged: one `emit_safe` per row, text and meta built inside
`build=lambda: (...)`, allowed meta keys are `run_events._META_FIELDS` only, no `divider` and no
`summary` from this module.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the adjudicate, coverage, conflict and synthesize helpers</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py (the whole file as 21-03 and 21-05 left it)
    - tribunal/nestor_pulse_sdk/runs/run_events.py lines 69-120 and 402-468 (kinds, meta whitelist, thunk contract)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 3364-3470 (adjudicate and the whole coverage block including the cost trap comment)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 3549-3630 (conflict detection and resolution)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 3700-3770 and 4143-4200 (the rejected-claims ledger and both synthesize sites)
    - tribunal/nestor_pulse_sdk/runs/stages.py lines 38-71 (the four stage keys and their declared labels)
  </read_first>
  <action>
Extend `tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py` with four more sections,
each opening with a comment naming its stage key and its declared label. Module constants:
`_STAGE_ADJUDICATE = "adjudicate"`, `_STAGE_COVERAGE = "coverage"`, `_STAGE_CONFLICT = "conflict"`,
`_STAGE_SYNTHESIZE = "synthesize"`. All four keys are already declared in `runs/stages.py:67-70`.

ADJUDICATE — label "Adjudication".
- `emit_adjudicate_dispatch(run_id, *, claims)` → `dispatch`, naming how many claims are being
  adjudicated and under which survival rule.
- `emit_adjudicate_drop(run_id, budget, *, claim)` → bounded `thinking` rows, one per DROPPED
  claim, naming the claim (clipped) and that it failed fact-check. This is the row that answers
  "what did the engine throw out and why" — the same question `rejected_claims` answers for the
  report, one stage earlier and on the page.
- `emit_adjudicate_done(run_id, *, survivors, dropped, claims)` → `thinking`, carrying the SAME
  sentence the closing `set_stage` at `pipeline.py:3372-3378` composes. Meta `items` = survivors.

COVERAGE — label "Coverage gate". THIS IS THE EMPTIEST STAGE AND GETS THE MOST CARE.
- `emit_coverage_dispatch(run_id, *, selected)` → `dispatch`, stating what the gate is about to
  check and over which population. The docstring must record WHY the population matters: the gate
  runs with `selected_only=True` deliberately, because without that intersection the recorded
  4cbb5311 population's 738 gate-dropped claims all read as uncovered and the re-entry loop fans
  out roughly 2,100 paid sessions. An operator reading this row is reading the cost trap's guard.
- `emit_coverage_done(run_id, *, passed, uncovered, reentries)` → `thinking`, stating whether the
  gate passed, how many high-stakes claims were uncovered, and how many re-entry passes ran.
  Meta `items` = uncovered count, `actions` = reentries.
- `emit_coverage_reentry(run_id, *, attempt, max_attempts, uncovered)` → `plan`. A re-entry is a
  routing decision that SPENDS MONEY, and it currently exists only as a `log.warning`.
- `emit_coverage_blocked(run_id, *, reason)` → `agent_fail`, carrying the `blocked_reason` the
  breaker produced verbatim. `pipeline.py:3440-3450` already names the loss for the operator
  through the run's one degradation accumulator; this puts the same words on the page.

CONFLICT — label "Conflict detection".
- `emit_conflict_dispatch(run_id, *, survivors, reconciliations)` → `dispatch`.
- `emit_conflict_finding(run_id, budget, *, conflict)` → bounded `thinking` rows, one per detected
  conflict. Takes the RAW conflict dict and reads `contested`, `loser`, `tension` and `note`
  inside the thunk. A contested conflict and a resolved one read differently: a resolved one names
  that a side was dropped, a contested one names the open disagreement the synthesiser must
  present. Do not collapse the two into one sentence.
- `emit_conflict_done(run_id, *, losers, contested, survivors)` → `thinking`.

SYNTHESIZE — label "Final synthesis".
- `emit_synthesize_dispatch(run_id, *, survivors)` → `dispatch`, carrying the same "writing report
  from N verified claims" sense the existing `set_stage` at 3749-3755 already states.
- `emit_synthesize_scrubbed(run_id, *, removed, reports)` → `thinking`, naming how many discredited
  claims were physically removed from the research before synthesis. This is SUBTRACTIVE
  VERIFICATION becoming visible: `pipeline.py:3743-3747` explains that every passage depending on
  a discredited claim is removed first, and nothing has ever shown that happening.
- `emit_synthesize_writing(run_id, *, ledger, numbered, resumed)` → `thinking`, emitted from
  `_write_final_report`. `resumed` is a boolean the caller supplies. Its docstring must state why
  this second row exists: `_write_final_report` is the RESUME path's entry point, so on a resumed
  run it is the ONLY synthesize row there will be — the closure that emits the first one never ran.

Every helper follows the shape 21-03 established: `run_id` positional first, everything else
keyword-only, every model-shaped read inside a `build=lambda`, no new kind, no new meta key, and
no `divider` or `summary`.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; "C:/Users/ajimimo/Desktop/MOELD/Nestor/.venv/Scripts/python.exe" -c "import re,pathlib; from nestor_pulse_sdk.pipeline.tribunal import stage_events as se; from nestor_pulse_sdk.runs.run_events import RUN_EVENT_KINDS; from nestor_pulse_sdk.runs.stages import stages_for; src=pathlib.Path(se.__file__).read_text(encoding='utf-8'); k=set(re.findall(r'kind=\"([a-z_]+)\"',src)); assert k &lt;= set(RUN_EVENT_KINDS), k; assert not (k &amp; {'divider','summary'}), k; assert src.count('emit_safe')==src.count('build=lambda'); keys={s['key'] for s in stages_for('tribunal')}; assert {se._STAGE_ADJUDICATE, se._STAGE_COVERAGE, se._STAGE_CONFLICT, se._STAGE_SYNTHESIZE} &lt;= keys; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The one-liner in `<verify>` prints `ok` and exits 0: kinds whitelisted, no divider/summary, `emit_safe` count equals `build=lambda` count, and all four stage-key constants are declared members of `ENGINE_STAGES["tribunal"]`.
    - The thirteen helpers exist and are importable — a one-liner asserting `all(hasattr(stage_events, n) for n in [...the thirteen names...])` exits 0.
    - `emit_conflict_finding` renders a CONTESTED conflict differently from a RESOLVED one: drive both against a recorder and assert the two recorded texts differ, and that neither is a substring of the other.
    - `emit_coverage_blocked` carries the reason verbatim: drive it with a distinctive reason string and assert that exact string appears in the recorded text.
    - Every recorded meta key is in `run_events._META_FIELDS`: drive all thirteen helpers against a recorder and assert set containment, not a grep.
  </acceptance_criteria>
  <done>Thirteen helpers exist, follow the landed contract, and their kind, meta, thunk, key-declaration and text-distinctness properties are proven by executed assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the four stages, including the module-level resume path</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 3364-3470 (adjudicate + the whole coverage block; read the cost-trap comment at 3407-3413 before touching anything there)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 3549-3630 (conflict detection and resolution)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 3740-3770 (the first synthesize site)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 4143-4200 (`_write_final_report` and the second synthesize site; read the comment at 4168-4176 explaining why this function is module-level)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py (as extended in Task 1)
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-05-SUMMARY.md (the budget create/flush shape to copy)
  </read_first>
  <action>
Edit `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` only. The `stage_events` import
already exists; do not add a second one.

ADJUDICATE:
- Before `adjudicate_all(...)` at line 3367, call `stage_events.emit_adjudicate_dispatch` with
  `claims=len(claims)`, and create `_adjudicate_budget`.
- After `survivors` and `dropped` are bound at 3370-3371, loop `dropped` and call
  `stage_events.emit_adjudicate_drop(run_id, _adjudicate_budget, claim=_c)` for each.
- At the closing `set_stage` at 3372-3378, bind the inline sentence to a local `_adjudicate_row`,
  pass it to `set_stage`, and pass the same local to `stage_events.emit_adjudicate_done`.
- Call `_adjudicate_budget.flush("dropped claim")` immediately after.

COVERAGE:
- Immediately after the bare `await set_stage(run_id, tenant_id, "coverage")` at line 3414 and
  BEFORE `check_coverage(...)` at 3415, call `stage_events.emit_coverage_dispatch`.
  ⚠ DO NOT CHANGE THE `check_coverage` CALL. `selected_only=True` stays passed explicitly. The
  comment above it (3407-3413) records that removing the intersection fans out roughly 2,100 paid
  sessions and that the budget governor is inert and will not stop it.
- Inside the `while` loop at 3422-3459, immediately after `reentry_count += 1` at 3423, call
  `stage_events.emit_coverage_reentry` with `attempt=reentry_count`, `max_attempts=MAX_REENTRY`
  and `uncovered=len(coverage["uncovered"])`.
- In the `if reentry["blocked_reason"]:` branch at 3440-3450, after the existing
  `_note_degradation(...)`, call `stage_events.emit_coverage_blocked` with that same reason.
- After the loop exits and after the `if reentry_count > 0:` re-adjudication at 3461-3465, call
  `stage_events.emit_coverage_done` with `passed=coverage["pass"]`,
  `uncovered=len(coverage["uncovered"])` and `reentries=reentry_count`.
  ⚠ Emit the closing row BEFORE line 3467's section break, so it lands while `coverage` is still
  the stage being reported. A row emitted after the verify stage's closing `set_stage` at 3536
  would be grouped under `verify` by the frontend, whose grouping is derived from consecutive
  `stage` values (`RunFeed.tsx:106-114`).

CONFLICT:
- Before the opening `set_stage` at 3589, call `stage_events.emit_conflict_dispatch` with
  `survivors=len(survivors)` and `reconciliations=len(group_reconciliations)`, and create
  `_conflict_budget`.
- After `conflict_detector(...)` returns at 3600-3608, loop `conflicts` and call
  `stage_events.emit_conflict_finding(run_id, _conflict_budget, conflict=conflict)` for each.
  Place the loop AFTER the existing `for conflict in conflicts:` classification loop at 3611-3617
  rather than inside it, so the classification logic is not entangled with emission.
- After the `if loser_idxs:` block at 3619-3628, call `stage_events.emit_conflict_done` with
  `losers=len(conflict_losers) if loser_idxs else 0`, `contested=len(contested_notes)` and
  `survivors=len(survivors)`. Read `conflict_losers` defensively — it is bound only inside the
  `if loser_idxs:` branch.
- Call `_conflict_budget.flush("contradiction")` immediately after.

SYNTHESIZE, SITE 1 (in `_run_staged`):
- Before the `set_stage` at 3749, call `stage_events.emit_synthesize_dispatch` with
  `survivors=len(survivors)`.
- After `scrub_research(...)` returns at 3756-3762, call `stage_events.emit_synthesize_scrubbed`
  with `removed=len(dropped)` and `reports=len(cleaned_reports)`.

SYNTHESIZE, SITE 2 (in module-level `_write_final_report`):
- After `_load_citation_context(...)` returns at line 4181, call
  `stage_events.emit_synthesize_writing` with `ledger=len(anchor_ledger)`,
  `numbered=len(numbered)` and a `resumed` boolean.
- Derive `resumed` from information `_write_final_report` already has, WITHOUT adding a parameter
  to it if that is avoidable — for example from whether the bundle it was handed came from the
  synthesis cache. If it is NOT derivable without a signature change, add ONE keyword-only
  parameter with a default that preserves every existing call, and say in a comment that the
  default exists so the resume entry point and the tests that drive this function directly keep
  their current call shape. Do NOT thread anything else through this function: the comment at
  4168-4176 records that threading a logging object through a paid path is not a trade worth
  making, and the same judgement applies here.

CHANGE NOTHING ELSE. Do not alter any argument to `check_coverage`, `adjudicate_all`,
`conflict_detector`, `scrub_research`, `synthesize_report` or `_resolve_then_persist_claims`; do
not change the `while` loop's bounds; do not touch the shim at 1622-1663; do not add an
`open_run`. The four inline-to-local sentence rebindings keep the exact strings they replace.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; "C:/Users/ajimimo/Desktop/MOELD/Nestor/.venv/Scripts/python.exe" -m pytest nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py nestor_pulse_sdk/tests/test_run_event_emit.py nestor_pulse_sdk/tests/test_coverage_reentry.py nestor_pulse_sdk/tests/test_verification_buckets.py nestor_pulse_sdk/tests/test_report_sections.py nestor_pulse_sdk/tests/test_checkpoint_resume.py -q -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - The pytest command in `<verify>` exits 0 with zero failures. `test_coverage_reentry.py` is the behavioural regression surface for the coverage loop and the cost trap; `test_checkpoint_resume.py` covers the resume path this task touches.
    - `grep -c "stage_events\.emit_adjudicate" pipeline.py` is at least 3; `emit_coverage` at least 4; `emit_conflict` at least 3; `emit_synthesize` at least 3.
    - Each of `_adjudicate_budget` and `_conflict_budget` is created once and flushed once — `grep -c` on each name's assignment is 1 and on its `.flush(` is 1.
    - THE COST TRAP IS UNTOUCHED: `grep -n "check_coverage(claims, adjudications, selected_only=True)" pipeline.py` still matches, and `git diff -U0` shows no hunk that modifies that line.
    - The coverage closing row lands before the section break: the line number of the `emit_coverage_done` call is LESS than the line number of the `_observed_unchecked` assignment (base line 3485). Compare `grep -n` outputs.
    - `grep -c "await set_stage(" pipeline.py` equals the count on the base commit — capture the base with `git show HEAD:...| grep -c`. Observability added, behaviour not.
    - `grep -c "run_events.open_run" pipeline.py` is still exactly 1.
    - `git diff -U0` shows no hunk intersecting lines 530-570 or 1610-1690 of the base file.
    - If `_write_final_report` gained a parameter, it is keyword-only WITH a default, and every existing call site still compiles unchanged: `grep -n "_write_final_report(" pipeline.py` shows the same call sites as HEAD, and the pytest run above (which drives the resume path) is green.
  </acceptance_criteria>
  <done>All four remaining stages emit, the resume path emits its own synthesize row, the coverage cost trap is provably untouched, and the `set_stage` call count matches HEAD.</done>
</task>

<task type="auto">
  <name>Task 3: Prove ALL THIRTEEN declared stages have bodies</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py (the whole file including the Phase 21 section 21-03 and 21-05 built)
    - tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py (the harness, its full-stage-sequence assertion, and its `_DECLARED_STAGE_KEYS` set)
    - tribunal/nestor_pulse_sdk/runs/stages.py lines 38-71 (the declared thirteen)
    - tribunal/cloudbuild.test-engine.yaml lines 485-535 (confirm registration and `EXPECTED_FILES` 44 — do not change either)
  </read_first>
  <action>
Extend the Phase 21 section of `tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py`. Create
no new test file and do not edit `tribunal/cloudbuild.test-engine.yaml`.

THE CAPSTONE TEST, and the reason this task exists at the end of the engine waves:

`test_every_declared_tribunal_stage_emits_a_body` — run the stubbed harness once, collect the
recorded rows, and for EVERY key in `[s["key"] for s in stages_for("tribunal")]` assert there is
at least one recorded event on that stage whose `kind` is neither `divider` nor `summary`.
Derive the stage list FROM `stages_for("tribunal")` — never from a hardcoded list of thirteen
names, because the whole point of SC1 is that the set is the schema's and a stage added later
must fail this test until it emits.
Carry two guards:
  - a VACUITY GUARD: assert the recorded row set is non-empty and that `_stage_sequence(...)`
    reported at least twelve distinct stages, so a harness that stopped early fails loudly;
  - a NAMED FAILURE: on failure, the assertion message lists the stage keys that had no body, so
    the red build says which stage went silent rather than printing a bare False.
The failure message is load-bearing. This test is the one a future engine change will trip, and
its whole value is telling that future reader which stage they silenced.

Then add:

(a) `test_coverage_is_no_longer_the_emptiest_stage` — assert the `coverage` stage's body count is
    at least 2. Name it for the measurement in 21-CONTEXT: this stage advanced its marker with no
    detail at all, so it reported neither rows nor a meaningful action count.

(b) `test_the_coverage_stage_summary_is_no_longer_empty` — CONFIRM OR REFUTE 21-CONTEXT's
    `<specifics>` hypothesis that the silent stages' `summary` lines render nearly empty because
    `state["items"]` is 0 for a stage that never reported detail. Read the recorded `summary` row
    for `coverage` and assert on its `meta`. If the hypothesis holds, assert the `items` or
    `actions` value is now non-zero. If measurement shows the hypothesis was WRONG — the summary
    meta is driven by `_stage_log_items(detail)` off the `set_stage` detail argument, which this
    plan does not change — then say so: write the test to assert the ACTUAL behaviour and record
    the refutation in the SUMMARY. Do not assert the hypothesis into truth.

(c) `test_a_coverage_reentry_and_a_blocked_reentry_each_say_so` — drive the harness so a re-entry
    fires, and separately so the breaker blocks one, and assert a `plan` row and an `agent_fail`
    row respectively on `stage == "coverage"`. If the stubbed harness cannot produce a blocked
    re-entry, drive `emit_coverage_blocked` directly against a recorder and say in the test's
    docstring which half is end-to-end and which is unit-level — an honest split beats a silent
    skip.

(d) `test_a_resumed_run_still_reports_final_synthesis` — drive `_write_final_report` directly from
    a cached bundle, the way the resume path does, and assert a `synthesize` body row is recorded.
    This is the ONE synthesize row a resumed run gets, because the closure that emits the other
    never runs on that path.

(e) `test_conflict_rows_distinguish_contested_from_resolved` — assert the two shapes produce
    distinguishable text, end to end where the harness allows and unit-level otherwise.

(f) `test_no_emit_in_the_last_four_stages_can_fail_the_run` — degraded shapes for each of the four
    stages' helpers, with the NEGATIVE CONTROL this file already uses: assert the same
    construction genuinely raises when performed outside the emitter.

Every test is PURE — no Postgres, no provider key, no network.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; "C:/Users/ajimimo/Desktop/MOELD/Nestor/.venv/Scripts/python.exe" -m pytest nestor_pulse_sdk/tests/test_run_event_emit.py -v -rs -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - The pytest command in `<verify>` exits 0 with zero failures and at least 8 more passing tests than at the end of plan 21-05 — record both numbers in the SUMMARY.
    - The capstone test derives its stage list from `stages_for("tribunal")`: `grep -n "stages_for(\"tribunal\")" tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py` matches inside that test, and no hardcoded thirteen-name list appears in it.
    - The capstone test's assertion message names the offending stage keys — verify by temporarily reverting one stage's emit calls, running the test, and confirming the failure output contains that stage's key. Restore afterwards and record the observed failure text in the SUMMARY. THIS IS THE PROOF THE GATE BITES; a capstone test nobody has seen fail is a capstone test nobody has verified.
    - Test (b) records whether 21-CONTEXT's summary-meta hypothesis was CONFIRMED or REFUTED, and asserts the actual behaviour either way.
    - Test (f) contains a negative control asserting the degraded construction raises outside `emit_safe`.
    - `git diff --name-only` for this task lists exactly one path: `tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py`. `grep -n "EXPECTED_FILES=" tribunal/cloudbuild.test-engine.yaml` still reads 44.
    - The full registered engine gate passes locally. Extract the 44 paths FROM the config's `WANTED` block with a grep — never retype them, never trust a filename — and run pytest over them with `-m "not live"`. Expect the 6 pre-existing `PYTEST_CURRENT_TEST` 32767-char errors.
  </acceptance_criteria>
  <done>SC1 is proven by a schema-derived capstone test that has been SEEN to fail on a deliberately silenced stage, the coverage summary hypothesis is settled by measurement, and the full local gate is green at 44 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conflict-detector model output → feed row | `conflict["tension"]`, `conflict["note"]` and claim text are model-authored strings reaching a persisted, operator-visible row. |
| breaker state → feed row | `reentry["blocked_reason"]` is engine-authored but is rendered verbatim as evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06-01 | Denial of service | THE COVERAGE COST TRAP | mitigate | The single highest-consequence line in this plan's blast radius. `check_coverage(claims, adjudications, selected_only=True)` is NOT modified, and an acceptance criterion asserts both that the literal call still matches and that `git diff -U0` shows no hunk touching it. Without that intersection the loop fans out roughly 2,100 Anthropic sessions and the budget governor is inert and will not stop it. |
| T-21-06-02 | Denial of service | one row per dropped claim in adjudicate, one per conflict | mitigate | Both bounded by their own `RowBudget` at `MAX_ROWS_PER_STAGE`, with the elision stated as a visible row. |
| T-21-06-03 | Denial of service | a malformed conflict dict or a `conflict_losers` read outside its branch | mitigate | Every model-shaped read is inside a `build()` thunk, and `conflict_losers` is read defensively at the one site where it may be unbound. Task 3 test (f) proves the degradation with a negative control. |
| T-21-06-04 | Tampering | a signature change to `_write_final_report` | mitigate | If a parameter is added it is keyword-only WITH a default, so every existing caller — including the resume entry point and the tests that drive this function directly — keeps its call shape. `test_checkpoint_resume.py` runs in `<verify>`, and an acceptance criterion compares the call sites against HEAD. |
| T-21-06-05 | Information disclosure | conflict tension text and claim text in feed rows | mitigate | `run_events.emit` scrubs with `scrub_pii` and THEN clamps, inside the emitter. Every site goes through `emit_safe`, so there is no path around it. |
| T-21-06-SC | Tampering | npm/pip/cargo installs | mitigate | NOT APPLICABLE BY CONSTRUCTION: no package installed, no `requirements.txt` edited. `git diff --name-only` proves it. |
</threat_model>

<verification>
1. Task 1's kind/meta/thunk/key one-liner prints `ok`.
2. Task 2's six-file pytest run is green; the `check_coverage` line is byte-identical; `grep -c "await set_stage("` matches HEAD.
3. Task 3's capstone test passes AND has been observed to fail with a named stage key when one stage's emits are temporarily reverted.
4. The full 44-file engine gate is green locally, paths extracted from the config.
5. `grep -n "EXPECTED_FILES=" tribunal/cloudbuild.test-engine.yaml` still reads 44.
</verification>

<success_criteria>
- SC1 is CLOSED and MEASURED: every key in `stages_for("tribunal")` has at least one non-divider, non-summary event in a stubbed end-to-end run, asserted by a schema-derived test whose failure message names the silent stage.
- `coverage` — the emptiest of the eight — now reports its population, its re-entries and its blocks.
- A resumed run reports its final synthesis, which it never did before.
- The coverage cost trap and the `set_stage` call count are both provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-06-SUMMARY.md` when done.
Record: the base and post-change pass counts; the base and post-change `await set_stage(` counts;
the VERBATIM failure output observed when one stage's emits were temporarily reverted, which is
the proof the capstone gate bites; and whether 21-CONTEXT's summary-meta hypothesis was confirmed
or refuted.
</output>
