---
phase: 21-research-run-feed-completion-silent-post-research-stages-stu
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
  - tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py
autonomous: true
requirements: [SC1, D-03, D-04, D-05, D-06]

must_haves:
  truths:
    - "The Skeptic verification phase renders a dispatch header naming how many clusters are being checked"
    - "Each checked cluster produces a start row and a matching finish row in the feed"
    - "A cluster whose skeptic session crashed, timed out or was skipped on budget says so, and says which cluster"
    - "Individual claim verdicts — refuted and superseded above all — appear as feed rows"
    - "The per-item rows are bounded, and an elision is stated as a visible row rather than truncating silently"
    - "No emit site can fail a run: text and meta are built inside the emitter's thunk"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py"
      provides: "The shared row budget, the elision row, and the verify stage's emit helpers"
      exports: ["MAX_ROWS_PER_STAGE", "RowBudget", "emit_verify_dispatch", "emit_verify_batch_dispatch", "emit_verify_group_run", "emit_verify_group_done", "emit_verify_group_failed", "emit_verify_verdicts", "emit_verify_closing"]
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      provides: "The verify stage's emit call sites, alongside the existing set_stage writes"
      contains: "stage_events"
    - path: "tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py"
      provides: "End-to-end assertions that the verify stage emits a non-empty body through the stubbed harness"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py"
      via: "module import, mirroring the run_events module-form import at pipeline.py:124"
      pattern: "from nestor_pulse_sdk.pipeline.tribunal import stage_events"
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py"
      to: "tribunal/nestor_pulse_sdk/runs/run_events.py"
      via: "run_events.emit_safe with build= thunks"
      pattern: "run_events\\.emit_safe"
---

<objective>
Give the `verify` stage a body. It is the stage the operator named twice, it is where the run's
money and meaning are, and it currently emits ZERO run events — so the run page shows "Skeptic
verification" as a label with nothing under it.

This plan also creates the SHARED SPINE the other seven silent stages copy: the row budget, the
visible elision row, and the house shape for a stage emitter module. Plans 21-05 and 21-06 EXTEND
`stage_events.py`; they do not re-invent it.

Purpose: SC1 and D-04 — no phase may render as a label with nothing under it, and `verify` in
particular must show the individual claim verdicts.

Output: `stage_events.py` with the spine and the verify helpers, the verify emit call sites in
`pipeline.py`, and end-to-end assertions in the already-registered `tests/test_run_event_emit.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-CONTEXT.md

<measured_facts>
Read out of the tree at `30443e8` during planning.

1. NO CI CONFIG EDIT IS NEEDED BY THIS PLAN. `tribunal/cloudbuild.test-engine.yaml` already
   names `nestor_pulse_sdk/tests/test_run_event_emit.py` in its `WANTED` list, and
   `EXPECTED_FILES` is 44. This plan adds NO new test FILE, so the count stays 44 and that
   config is NOT edited. If a new test file ever becomes necessary, its path and
   `EXPECTED_FILES` move in ONE edit — see the config's own header — but the correct move here
   is to extend the already-registered file.

2. Dividers and per-stage summaries are ALREADY AUTOMATIC. `_stage_event_boundary`
   (`pipeline.py:536-565`) emits a `summary` for the outgoing stage and a `divider` for the
   incoming one at every real transition, driven from `_stage_log_transition`
   (`pipeline.py:568-602`). `verify` therefore ALREADY has a label and a summary line. DO NOT
   hand-emit either — you would double them (D-06).

3. `RUN_EVENT_KINDS` (`runs/run_events.py:69-82`) is a CLOSED 12-kind vocabulary whose own
   comment states it is "one contract in two languages" with the frontend union. This plan adds
   NO kind (D-03). Everything below uses `dispatch`, `agent_run`, `agent_done`, `agent_fail`,
   `thinking` and `plan`, all of which the frontend already renders.

4. `open_run` is called exactly ONCE, at `pipeline.py:1680`. This plan adds no second call.

5. `set_stage` is deliberately SHADOWED inside `_run_staged` (`pipeline.py:1622-1663`) and the
   import under a different name is load-bearing — a past tidy-up of it killed the engine on
   every run. Read that comment before touching anything near it. This plan changes NOTHING in
   that region.

6. The eight silent stages already COMPOSE excellent human-readable strings — they just send
   them to `stage_detail` (which the old embedded card renders) rather than to `run_event`
   (which the run page renders). `_verify_closing_item(funnel)` at `pipeline.py:1054-1100` is
   the clearest example. Where such a string already exists, EMIT THE SAME STRING; do not write
   a second one that will drift.

7. The feed's total budget: `run_events._MAX_QUEUE` is 5000, and the frontend backfill caps at
   `PAGE_LIMIT` 500 x `MAX_PAGES` 10 = 5000
   (`frontend/src/lib/research/useRunEvents.ts:32-43`), after which the page renders a
   `truncated` notice in words. A per-stage bound of 25 across eight stages adds at most 200
   rows, safely inside both.

8. The verdict vocabulary is `support` / `refute` / `insufficient` / `superseded`, carried per
   claim index in `verdicts_by_index` (`group_skeptic.py:14-20, 169-192`). READ THAT FILE rather
   than trusting this line — the enum is exactly what the feed rows will name.

9. The stubbed end-to-end harness in `tests/test_engine_e2e_stubbed.py` DRIVES THE REAL PIPELINE
   through all thirteen stages plus `done` (its own assertion at roughly lines 1790-1801 pins
   that sequence). It exports `_ScriptedProvidersAudited`, `_engine_run`, `_no_db_sessionmaker`
   and `_stage_sequence`, and `test_run_event_emit.py` already imports all four. That harness is
   how this plan proves the verify stage emits a body, without a database, a provider key or a
   network call.
</measured_facts>

<interfaces>
The emitter contract every site must obey, from `runs/run_events.py:402-468`:

```python
run_events.emit_safe(
    run_id,
    stage="verify",
    kind="agent_done",
    build=lambda: ("text built HERE", {"items": 3}),
)
```

A CALLER'S ARGUMENTS ARE EVALUATED BEFORE THE CALLEE IS ENTERED. Composing the text above the
thunk moves the f-string OUTSIDE the emitter's try and re-creates the entire defect while looking
correct. There is one `try/except` and it wraps BOTH `build()` and the `emit(...)`.

The allowed `meta` keys, from `run_events._META_FIELDS` (lines 110-113) — anything else is
DROPPED with a warning:
`sub`, `is_live`, `worked`, `actions`, `items`, `cost`, `audit_id`, `provider`, `model`, `angle`,
`attempt`, `max`, `wait_s`.

The reviewed template to imitate, `pipeline/tribunal/workshop.py:495-638`: a module-level
`_EVENT_STAGE` constant, one `_emit_*` function per feed row, and — for any row whose text needs
a walk over model-shaped data — a separate `_*_event(...)` composer whose docstring reads "CALLED
ONLY FROM INSIDE A build() THUNK" (see `_orientation_done_event` at 534-553).

The verify stage's existing call sites in `pipeline.py`:

| line | what is in scope there |
|---|---|
| 3143-3145 | `queue` (gate-selected, corroboration-ordered groups), `total_passes`, `done_passes` |
| 3147-3158 | `_verify_detail(0)` — the grouped branch's opening `set_stage` |
| 3160-3178 | `_one_group_pass(group, sources)` — one thorough session per group |
| 3183-3248 | `_flush_groups()` — `zip(owners, results)`; `res is None` is the crash/timeout path; `vbi = res.get("verdicts_by_index", {})`; `recon`; the WR-10 no-filter member loop at 3224 |
| 3253-3276 | the queue loop, the `budget_exceeded` skip, `_extract_sources_for_group` |
| 3287-3362 | the per-claim A/B branch: `selected_claims`, `n_selected`, `_flush_batch()` |
| 3516-3539 | `verification_funnel` built, `_stage_log_counts`, and the closing `set_stage` carrying `_verify_closing_item(verification_funnel)` |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stage_events.py — the shared spine plus the verify helpers</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/runs/run_events.py (the whole file; the `emit_safe` contract at 402-468 and `_META_FIELDS` at 110-113 are binding)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop.py lines 495-638 (THE TEMPLATE D-04 says to imitate)
    - tribunal/nestor_pulse_sdk/runs/stage_feed.py lines 440-470 (the overflow-as-a-visible-row rule D-05 follows)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/group_skeptic.py lines 1-30 and 160-195 (the `verdicts_by_index` shape and the verdict enum)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 240-260 and 718-740 (the `NESTOR_TRIBUNAL_*` env-override idiom to copy)
  </read_first>
  <action>
Create `tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py`.

The module docstring states, in this order: that this module holds the feed emitters for the
eight stages Phase 15.3 left silent; that it adds NO event kind (D-03) because `RUN_EVENT_KINDS`
is a closed two-language contract; that dividers and summaries are already automatic via
`_stage_event_boundary` and must never be hand-emitted here (D-06); and that every function
below calls `run_events.emit_safe` with its text and meta built INSIDE the thunk, never composed
above it, for the control-flow reason `run_events.py:402-468` sets out at length.

Import `run_events` in MODULE FORM (`from nestor_pulse_sdk.runs import run_events`), matching
`pipeline.py:124` and for the reason stated there: the call-site gate counts qualified calls to
the bare emit entry point, and a from-import would slip past it.

THE SPINE — reused by plans 21-05 and 21-06, so get it right here.

1. `MAX_ROWS_PER_STAGE = int(os.environ.get("NESTOR_TRIBUNAL_FEED_ROWS_PER_STAGE", "25"))`,
   with a docstring giving D-05's arithmetic: the emitter queue ceiling is 5000
   (`run_events._MAX_QUEUE`) and the page backfill caps at the same 5000, so eight stages at 25
   rows each add at most 200 and cannot approach either bound. A run distills hundreds of claims;
   one row per claim across four stages would be thousands.

2. `class RowBudget` — one per stage per run. Constructor takes `run_id`, `stage`, and an
   optional `limit` defaulting to `MAX_ROWS_PER_STAGE`. It carries `used` and `elided` counters
   and exposes:
   - `take() -> bool` — returns True and increments `used` while `used` is below `limit`;
     otherwise increments `elided` and returns False. Never raises.
   - `flush(noun: str) -> None` — when `elided` is non-zero, emits ONE `thinking` row through
     `run_events.emit_safe` reading like "N more <noun> not shown — the feed shows the first
     <limit>". It then zeroes `elided`, so a second call is a no-op.
   `flush`'s docstring states D-05's rule in substance: the elision is STATED AS A VISIBLE ROW
   rather than truncated silently, following the house rule `StageFeed` already applies to its
   own overflow. A budget that silently swallowed rows would be the exact failure the
   `run_event` table exists to end.

3. A defensive one-line clip for claim and entity text used inside row bodies. Reuse
   `truncate_task_prompt` from `nestor_pulse_sdk.runs.stage_feed` if its semantics fit —
   `workshop.py` already imports it for exactly this; otherwise write a local one and say in a
   comment why the shared one did not fit. Name the two widths as module constants rather than
   inline integers: 110 characters for a claim, 60 for an entity or attribute label.

THE VERIFY HELPERS. `_STAGE_VERIFY = "verify"` — the key already declared in
`ENGINE_STAGES["tribunal"]` (`runs/stages.py:66`, label "Skeptic verification"). Do not invent it.

- `emit_verify_dispatch(run_id, *, groups_selected, groups_total, multi, claims_selected, claims_total)`
  → kind `dispatch`. ONE header for the whole stage, naming the work and its counts: how many
  clusters of how many are being checked, how many hold more than one stream's version of the
  same fact, and how many of how many claims the gates selected. One header for the STAGE, not
  one per group — `test_run_event_emit.py`'s header already records that one-header-per-item is
  a different design rather than a bug.

- `emit_verify_batch_dispatch(run_id, *, selected, total)` → kind `dispatch`, the per-claim A/B
  branch's equivalent header. The two branches are mutually exclusive at runtime.

- `emit_verify_group_run(run_id, budget, *, group)` → kind `agent_run`, guarded by
  `budget.take()`. Takes the RAW group dict and reads `entity`, `attribute`, `claims` and
  `stakes` off it INSIDE the thunk, so a restored or degraded group missing a key costs the row
  and never the run. Meta carries `items` = member count.
  DO NOT set `is_live`. Measured during planning: `is_live` is written at exactly one production
  site as a literal True, so it is a constant and not a liveness signal, and the frontend now
  derives liveness from position and run state (plan 21-01).

- `emit_verify_group_done(run_id, budget, *, group, verdicts)` → kind `agent_done`, guarded by
  `budget.take()`. `verdicts` is the raw `verdicts_by_index` mapping. The tally — how many
  support / refute / insufficient / superseded — is computed INSIDE the build thunk by a
  separate composer `_verify_group_done_event(group, verdicts)` whose docstring reads "CALLED
  ONLY FROM INSIDE A build() THUNK": walking a model-shaped mapping is exactly the argument
  construction that must stay inside the emitter's try. Meta carries `items` = verdict count.

- `emit_verify_group_failed(run_id, budget, *, group, reason)` → kind `agent_fail`, guarded by
  `budget.take()`. The text must say WHY — "the word failed on its own is the exact defect phase
  15.3 exists to remove" (`test_run_event_emit.py` header, behaviour 3). Two distinct reasons
  are passed by the caller: a crashed-or-timed-out session, and a cluster skipped because the
  budget cap was reached.

- `emit_verify_batch_done(run_id, budget, *, verified, selected)` → kind `agent_done`, guarded
  by `budget.take()`, for the per-claim branch's flush.

- `emit_verify_verdicts(run_id, budget, *, group, verdicts)` → bounded `thinking` rows, ONE PER
  NOTEWORTHY VERDICT. Noteworthy means a verdict whose value is `refute` or `superseded` — read
  those literals off `group_skeptic.py` rather than trusting this sentence. A `support` verdict
  is the expected outcome and does not earn a row; the tally on the group's `agent_done` already
  carries it. Each row names the claim (clipped) and the verdict, and for a superseded verdict
  appends its `superseded_note` when present. Every read of the verdict mapping and of the claim
  list happens inside a thunk.
  THIS IS THE ROW THE OPERATOR ASKED FOR TWICE. If scope is ever cut in this plan, it is the last
  thing cut.

- `emit_verify_closing(run_id, *, text)` → kind `thinking`, carrying the SAME string
  `_verify_closing_item(verification_funnel)["name"]` already builds. Its docstring says why the
  string is passed in rather than rebuilt: `_verify_closing_item` is the ONE place the
  degradation sentence is composed (G-10 requires degradation stated in words, not with an
  icon), and a second composer would drift from it.

Every helper takes `run_id` positionally first and everything else keyword-only, matching
`workshop.py`. Every helper returns `None` and cannot raise: the emit is inside `emit_safe`, and
the only work outside it is the `budget.take()` integer comparison.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; "C:/Users/ajimimo/Desktop/MOELD/Nestor/.venv/Scripts/python.exe" -c "from nestor_pulse_sdk.pipeline.tribunal import stage_events as se; from nestor_pulse_sdk.runs.run_events import RUN_EVENT_KINDS, _META_FIELDS; import re, pathlib; src = pathlib.Path(se.__file__).read_text(encoding='utf-8'); kinds = set(re.findall(r'kind=\"([a-z_]+)\"', src)); assert kinds and kinds &lt;= set(RUN_EVENT_KINDS), kinds; assert not (kinds &amp; {'divider','summary'}), kinds; assert src.count('emit_safe') == src.count('build=lambda'), (src.count('emit_safe'), src.count('build=lambda')); b = se.RowBudget('r','verify',limit=2); assert [b.take() for _ in range(4)] == [True,True,False,False]; assert b.elided == 2; print('spine ok', sorted(kinds))"</automated>
  </verify>
  <acceptance_criteria>
    - The one-liner in `<verify>` prints `spine ok` followed by the kind list, and exits 0. It asserts four things a comment cannot satisfy: every `kind=` literal in the file is a member of `RUN_EVENT_KINDS`; none of them is `divider` or `summary`; the count of `emit_safe` calls equals the count of `build=lambda` arguments (so no text is composed above a thunk); and `RowBudget.take()` returns True exactly `limit` times while `elided` counts the refusals.
    - `grep -nE "run_events\.emit\(" tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py` exits non-zero — the bare emit entry point is never called.
    - The env override works: running the same import with `NESTOR_TRIBUNAL_FEED_ROWS_PER_STAGE=3` set in the subprocess environment yields `stage_events.MAX_ROWS_PER_STAGE == 3`, and with it unset yields 25.
    - Every `meta` key the module passes is in `run_events._META_FIELDS`: prove it by running the module's helpers against a recorder (monkeypatch `run_events._writer`) and asserting the recorded rows' meta keys are a subset, NOT by grepping for key names.
    - `grep -c "CALLED ONLY FROM INSIDE" tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py` is at least 1 — the composer that walks `verdicts_by_index` is marked.
  </acceptance_criteria>
  <done>`stage_events.py` exists; the row budget, the kind whitelist and the thunk discipline are all proven by executed assertions rather than by inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the verify stage's emit sites into pipeline.py</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 100-130 (the import block and the module-form rule for run_events)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 452-620 (the feed's spine, `_stage_event_boundary`, `_stage_log_transition`)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 1610-1690 (the SHADOWED `set_stage` shim and its load-bearing import comment — read, do not touch)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 3100-3370 (the grouped and per-claim verify branches)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 3460-3548 (the funnel, `_stage_log_counts`, the closing set_stage)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 1050-1100 (`_verify_closing_item`)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/stage_events.py (written in Task 1)
  </read_first>
  <action>
Edit `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` only.

(a) Add `from nestor_pulse_sdk.pipeline.tribunal import stage_events` to the import block,
adjacent to the existing `from nestor_pulse_sdk.runs import run_events` at line 124, with a
one-line comment saying it holds the emitters for the stages 15.3 left silent and that it is
imported in module form for the same call-site-gate reason stated three lines above it.

(b) Create ONE `RowBudget` for the verify stage, bound to a local named `_verify_budget`,
immediately before the `if _GROUP_VERIFY:` branch at line 3124. One budget spans BOTH branches,
so the bound is a property of the stage rather than of whichever branch ran.

(c) GROUPED BRANCH:
   - After `total_passes = len(queue)` (line 3144) and before `await _verify_detail(0)`
     (line 3158), call `stage_events.emit_verify_dispatch` with `groups_selected=total_passes`,
     `groups_total=n_groups`, `multi=multi`, `claims_selected=gate_funnel["selected_verify"]`,
     `claims_total=len(claims)`. All five names are already in scope — `_verify_detail` at
     3151-3153 already renders three of them.
   - In the queue loop (lines 3253-3276): after `owners.append(group)` at line 3262, call
     `stage_events.emit_verify_group_run(run_id, _verify_budget, group=group)`. Pass the raw
     dict; every field read happens inside the helper's thunk.
   - In the `budget_exceeded` skip at lines 3254-3258, immediately after the existing
     `_book_unchecked(group["claims"], "budget cap reached")`, call
     `stage_events.emit_verify_group_failed` with a reason naming the budget cap. An operator
     must not have to infer that a cluster was skipped rather than checked.
   - In `_flush_groups`: on the `res is None` branch (lines 3190-3194), after `_book_unchecked`,
     call `emit_verify_group_failed` with the reason that the skeptic session crashed or timed
     out. On the success path, after `vbi` is bound at line 3195, call `emit_verify_group_done`
     with `grp` and `vbi`, then `emit_verify_verdicts` with `grp` and `vbi`.
     PLACE BOTH OUTSIDE THE `for i, c in enumerate(grp["claims"])` LOOP AT LINE 3224. That loop
     carries the WR-10 "DO NOT FILTER THIS LOOP" comment; a call inside it invites a later reader
     to add a condition next to it, which is precisely the defect that comment exists to prevent.
     Emit once per group, after the member loop closes.

(d) PER-CLAIM BRANCH (lines 3287-3362): immediately before the opening `set_stage` at line 3301,
call `stage_events.emit_verify_batch_dispatch` with `selected=n_selected` and
`total=len(claims)`. Inside `_flush_batch` (lines 3312-3331), after the `zip(owners, results)`
loop and after `_verified_count` is incremented, call `stage_events.emit_verify_batch_done` with
`verified=_verified_count` and `selected=n_selected`.

(e) CLOSING: at line 3536 the closing `set_stage` currently passes
`_verify_closing_item(verification_funnel)` inline. Bind that item to a local ONCE, pass its
`"name"` to `stage_events.emit_verify_closing(run_id, text=...)`, and pass the SAME object to
`set_stage`. Binding once is the point: the feed row and the stage detail must be the same
sentence, so the run page and the intake card cannot report different degradation.
Then call `_verify_budget.flush("cluster")` immediately after, so any elision row lands inside
the verify stage and before the next stage's divider.

(f) CHANGE NOTHING ELSE. Specifically: do not alter any `set_stage` call's arguments beyond the
one rebinding in (e); do not add a second `open_run`; do not touch the shim at 1622-1663; do not
touch `_stage_event_boundary`; and do not change which groups are queued, which sessions run,
what is booked unchecked, or what the funnel says. This phase adds OBSERVABILITY — a diff that
alters a claim, a verdict, a cost or a dispatch is out of scope even if it would improve the feed.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; "C:/Users/ajimimo/Desktop/MOELD/Nestor/.venv/Scripts/python.exe" -m pytest nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py nestor_pulse_sdk/tests/test_stage_logging.py nestor_pulse_sdk/tests/test_run_event_emit.py nestor_pulse_sdk/tests/test_verification_buckets.py nestor_pulse_sdk/tests/test_coverage_reentry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The pytest command in `<verify>` exits 0 with zero failures. These five files are the regression surface for the verify stage and the stubbed run; a change to what the pipeline DECIDES turns one of them red.
    - `grep -n "from nestor_pulse_sdk.pipeline.tribunal import stage_events" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` matches exactly once.
    - `grep -c "stage_events\.emit_verify" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` is at least 7.
    - The verify budget is created once and flushed once: `grep -c "_verify_budget = " pipeline.py` is 1 and `grep -c "_verify_budget.flush(" pipeline.py` is 1.
    - No second `open_run`: `grep -c "run_events.open_run" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` is still exactly 1.
    - The shim region is untouched: `git diff -U0 tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` shows no hunk whose header line range intersects 1610-1690.
    - `_stage_event_boundary` is untouched: the same `git diff -U0` shows no hunk intersecting 530-570.
    - `_verify_closing_item` is called exactly once in the file after the edit: `grep -c "_verify_closing_item(" pipeline.py` is 2 (its `def` and its single call site), proving the sentence is bound once and shared rather than composed twice.
    - The set_stage call count is unchanged: `grep -c "await set_stage(" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns the same number as on the base commit — capture the base number with `git show HEAD:tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py | grep -c "await set_stage("` and compare. This is the strongest single proof that observability was added and behaviour was not.
  </acceptance_criteria>
  <done>The verify stage emits a dispatch header, per-cluster lifecycle rows, per-verdict rows and a closing line; the five regression files are green; and the set_stage call count is provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the verify stage has a body, end to end</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py (the whole file — its harness `_Recorder`, its import block at roughly lines 80-105, and its header's three named behaviours)
    - tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py (the exported helpers `_ScriptedProvidersAudited`, `_engine_run`, `_no_db_sessionmaker`, `_stage_sequence`, and its own full-stage-sequence assertion)
    - tribunal/cloudbuild.test-engine.yaml lines 485-535 (confirm `test_run_event_emit.py` is in `WANTED` and that `EXPECTED_FILES` is 44 — this task must NOT change either)
  </read_first>
  <action>
Extend `tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py`. Do NOT create a new test file
and do NOT edit `tribunal/cloudbuild.test-engine.yaml`.

Add a section header comment naming Phase 21 and stating what this section proves: that the
stages Phase 15.3 left silent now emit a BODY, where "body" means at least one event that is
neither a `divider` nor a `summary` — the exact definition `RunFeed.tsx`'s `body` filter uses,
so the assertion measures what the operator sees rather than what the engine intended.

Add these tests, driving the stubbed harness with the real `run_events` module (install a
recorder on `run_events._writer`, exactly as the existing tests in this file do; do NOT
monkeypatch `emit` or `emit_safe`, which would prove nothing about argument construction):

(a) `test_verify_stage_is_no_longer_a_label_with_nothing_under_it` — run `_engine_run`, collect
    the recorded rows, filter to `stage == "verify"`, and assert that the rows whose `kind` is
    neither `divider` nor `summary` number at least 2. Include a VACUITY GUARD first: assert the
    run reported the `verify` stage at all via `_stage_sequence`, so a harness that stopped early
    fails loudly rather than passing on an empty filter.

(b) `test_verify_emits_exactly_one_dispatch_header` — one `dispatch` row on `stage == "verify"`,
    not one per cluster. Assert equality with 1, not `>= 1`.

(c) `test_a_verify_cluster_row_pairs_with_a_finish_row` — count `agent_run` rows on
    `stage == "verify"` and assert the count of `agent_done` plus `agent_fail` rows on the same
    stage is at least that count. This is the engine-side half of the positional pairing that
    `frontend/src/lib/research/feedRows.ts::settledSeqs` relies on; if the engine ever emits a
    start with no finish, the spinner defect returns and this test names it.

(d) `test_a_failed_verify_cluster_says_why` — drive the harness so at least one group session
    returns `None` (the crash/timeout path at `pipeline.py:3190`), and assert the resulting
    `agent_fail` text contains more than the word "failed": assert its length is above 30
    characters AND that it names either a timeout or a crash. Include a NEGATIVE CONTROL in the
    same test: assert that the bare string "failed" alone would NOT satisfy the same predicate,
    so the assertion is proving content rather than presence.

(e) `test_the_verify_row_budget_states_its_elision_as_a_row` — set
    `NESTOR_TRIBUNAL_FEED_ROWS_PER_STAGE` low (via monkeypatch of
    `stage_events.MAX_ROWS_PER_STAGE`, or by constructing a `RowBudget` with an explicit small
    `limit` and driving the helpers directly), emit more rows than the limit, and assert that
    exactly one extra `thinking` row exists carrying the elided count. Assert the count is the
    real number of refused rows, not a fixed string.

(f) `test_no_verify_emit_can_fail_the_run` — hand the verify helpers a group dict missing
    `claims` and a `verdicts_by_index` that is not a mapping, and assert (i) nothing raises,
    (ii) the run completes, and (iii) — the NEGATIVE CONTROL, matching the pattern this file
    already uses for its tests (h) and (i) — that performing the same construction OUTSIDE the
    emitter genuinely raises, so a green result cannot mean "the input was harmless after all".

Every test in this section must be PURE: no Postgres, no provider key, no network. The two
`run_events` operations that touch the database are module-level test seams; replace them, do not
reach past them.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; "C:/Users/ajimimo/Desktop/MOELD/Nestor/.venv/Scripts/python.exe" -m pytest nestor_pulse_sdk/tests/test_run_event_emit.py -v -rs -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest nestor_pulse_sdk/tests/test_run_event_emit.py -v -rs -m "not live"` exits 0 with zero failures, and its summary shows at least 6 MORE passing tests than on the base commit — capture the base count with `git stash` or by running the same command on `git show HEAD:` first, and record both numbers in the SUMMARY.
    - Test (a) contains a vacuity guard asserting `verify` appears in `_stage_sequence(...)` BEFORE it filters rows, so an empty filter can never read as a pass.
    - Test (d) contains a negative control asserting the bare string `"failed"` fails the same predicate the real text passes.
    - Test (f) contains a negative control asserting the degraded construction raises when performed outside `emit_safe`.
    - `git diff --name-only` for this task lists exactly one path: `tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py`. In particular `tribunal/cloudbuild.test-engine.yaml` is NOT listed and `grep -n "EXPECTED_FILES=" tribunal/cloudbuild.test-engine.yaml` still shows 44.
    - The whole registered engine gate still collects and passes locally: `python -m pytest $(the 44 WANTED paths) -q -m "not live"` exits 0. Extract the 44 paths FROM the config with a grep over the `WANTED` block — never retype them from memory, and never trust a filename.
  </acceptance_criteria>
  <done>The verify stage's body is proven end to end through the stubbed harness with vacuity guards and negative controls, the CI file list and count are untouched at 44, and the full local gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skeptic model output → feed row | `verdicts_by_index`, `entity`, `attribute`, `superseded_note` and claim text are all model-influenced strings crossing into a persisted, operator-visible row. |
| engine process → `run_event` table | A tenant-scoped write; `open_run` bound the tenant once at `pipeline.py:1680`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Denial of service | new emit sites in the paid verify loop | mitigate | Every per-item emit is guarded by `RowBudget.take()`, bounded by `MAX_ROWS_PER_STAGE` (25 by default). `emit` is synchronous, touches no database and appends to a bounded deque; the DB write is a separate drain task the caller never awaits. |
| T-21-03-02 | Denial of service | a malformed group or verdict mapping | mitigate | Every read of model-shaped data happens inside a `build()` thunk, so a `KeyError` or `TypeError` costs the ROW and never the run (D-06). Task 3 test (f) proves it with a negative control. |
| T-21-03-03 | Information disclosure | claim and evidence text in feed rows | mitigate | `run_events.emit` scrubs with `pipeline/tribunal/pii.py::scrub_pii` and THEN clamps — the order is load-bearing and already enforced inside the emitter (`run_events.py:345-351`). This plan adds no path around it, because every site goes through `emit_safe`. |
| T-21-03-04 | Tampering | tenant binding | mitigate | No new `open_run` call. An acceptance criterion asserts `run_events.open_run` still appears exactly once in `pipeline.py`. A tenant-less write is dropped by the emitter by construction. |
| T-21-03-05 | Repudiation | silent row loss | mitigate | The elision is a VISIBLE ROW (D-05), asserted by Task 3 test (e). Queue-ceiling drops are already counted and logged by the emitter. |
| T-21-03-SC | Tampering | npm/pip/cargo installs | mitigate | NOT APPLICABLE BY CONSTRUCTION: this plan installs no package. `stage_events.py` imports only `os`, `typing` and existing first-party modules. No `requirements.txt` edit; `git diff --name-only` proves it. |
</threat_model>

<verification>
1. Task 1's spine one-liner prints `spine ok` — kinds whitelisted, no divider/summary, thunk count matched, budget arithmetic correct.
2. Task 2's five-file pytest run is green, and `grep -c "await set_stage("` is unchanged from HEAD.
3. Task 3's `test_run_event_emit.py` run is green with at least six new passing tests.
4. The full 44-file engine gate runs green locally (paths extracted from `cloudbuild.test-engine.yaml`, never retyped). Expect the 6 pre-existing `PYTEST_CURRENT_TEST` 32767-char errors — they are present at every commit and are not caused by this plan.
5. `grep -n "EXPECTED_FILES=" tribunal/cloudbuild.test-engine.yaml` still reads 44.
</verification>

<success_criteria>
- The `verify` stage renders a dispatch header, per-cluster start and finish rows, per-verdict rows for refutations and supersessions, and a closing degradation sentence identical to the one `stage_detail` carries.
- Every one of those properties is proven by an executed assertion with a vacuity guard, not by a grep and not by inspection.
- Zero new event kinds, zero new meta keys, zero new test files, zero CI config edits, `EXPECTED_FILES` still 44.
- `await set_stage(` count unchanged: observability added, behaviour untouched.
</success_criteria>

<output>
Create `.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-03-SUMMARY.md` when done.
Record: the base and post-change pass counts for `test_run_event_emit.py`; the base and
post-change `await set_stage(` counts; the exact 44 paths extracted from the CI config and the
full-gate result; and the `stage_events.py` public surface, because plans 21-05 and 21-06 build
directly on it.
</output>
