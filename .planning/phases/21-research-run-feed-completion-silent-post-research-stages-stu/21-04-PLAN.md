---
phase: 21-research-run-feed-completion-silent-post-research-stages-stu
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-DENSITY-AUDIT.md
  - tribunal/nestor_pulse_sdk/audit/audited_llm_client.py
  - tribunal/nestor_pulse_sdk/tests/test_own_researcher.py
autonomous: false
requirements: [SC5, D-12, D-13, D-14]

must_haves:
  truths:
    - "A per-site keep/cut verdict for every deep_research feed row in audited_llm_client.py exists as a reviewable document before any line is changed"
    - "The operator has ruled on that document before the first line is edited"
    - "No feed line is deleted — a line that leaves the feed is demoted to a log, never removed"
    - "Every CI-pinned substring either survives verbatim or its pinning test moves in the same edit"
  artifacts:
    - path: ".planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-DENSITY-AUDIT.md"
      provides: "The measured per-site table, the CI pin inventory, the volume arithmetic and three costed options"
      contains: "audited_llm_client.py"
    - path: "tribunal/nestor_pulse_sdk/audit/audited_llm_client.py"
      provides: "The density target — 8 thinking sites and 5 agent_fail sites on stage deep_research"
  key_links:
    - from: ".planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-DENSITY-AUDIT.md"
      to: "tribunal/nestor_pulse_sdk/tests/test_own_researcher.py"
      via: "the CI-pinned substring inventory"
      pattern: "test_own_researcher"
---

<objective>
Answer the operator's fourth UAT complaint — "the whole thing too verbose with rubbish
information" — the way the operator asked for it: DIAGNOSE FIRST (D-12), produce the per-site
verdict as a reviewable artifact, get a ruling, and only then change a line.

Purpose: D-12 is an OPERATOR decision and it is explicit. The operator declined an immediate
aggressive trim and asked for a specific list of what is noise versus signal first. A commit that
folds the diagnosis silently into the code is not what was asked for.

Output: `21-DENSITY-AUDIT.md`, a blocking ruling, and whatever edit the ruling authorises.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-CONTEXT.md

<measured_facts>
READ THIS SECTION BEFORE WRITING A WORD OF THE AUDIT. The planner measured the density target
during planning and the measurement CONTRADICTS the premise 21-CONTEXT.md states for it. The
audit's job is to present that contradiction to the operator, not to resolve it silently.

21-CONTEXT.md's premise, in its "The measurement this phase is built on" section: "13 of the 48
sites live in `audit/audited_llm_client.py` and every one of them hardcodes
`stage="deep_research"`. Eight are `kind="thinking"`. ... most of its lines are exception-path
commentary addressed to an engineer rather than to a watcher."

What the code actually says at `30443e8`. All eight `thinking` sites, read in full:

| line | provider | what the line is about | D-13 class |
|---|---|---|---|
| 1408 | google | a recorded job id was refused, so the angle is dispatched fresh and PAID FOR AGAIN | money AND guard |
| 1436 | google | REJOINED an in-flight job, nothing charged twice; states the poll interval | money + long-silence |
| 1495 | google | "Waiting on Google ... a long silence here is the normal shape of this call" | long-silence |
| 1594 | google | the heartbeat: minutes elapsed, poll N of M, "THIS IS A WAIT, NOT A STALL" | long-silence |
| 1710 | openai | the same job-id refusal, PAID FOR AGAIN | money AND guard |
| 1824 | openai | REJOINED an in-flight response, nothing charged twice | money |
| 1866 | openai | "Waiting on OpenAI ... a long silence here is the normal shape of this call" | long-silence |
| 1942 | openai | the heartbeat, same wording contract as google's | long-silence |

EIGHT OF EIGHT fall in D-13's two KEEP classes. Zero are parser-defect explanations. Zero are
about the engine's own defensive machinery ALONE — the two guard-refusal lines (1408, 1710) are
guard commentary AND a money statement in the same sentence, a case D-13's rule as written does
not resolve. The five `agent_fail` sites (1569, 1622, 1911, 1925, 1969) are exception-path, but
`agent_fail` is a legitimate kind and a failure is exactly what a watcher wants to see.

FIVE OF THE EIGHT ARE PINNED BY A REGISTERED CI TEST.
`tribunal/nestor_pulse_sdk/tests/test_own_researcher.py` is in `cloudbuild.test-engine.yaml`'s
`WANTED` list. It asserts, on the recorded emit texts, around lines 1290-1400:
`text.startswith("Waiting on Google")`; `text.startswith("Still waiting")`;
`"5 min elapsed" in text`; `"poll 10 of 70" in text`;
`all("NOT A STALL" in text for text in heartbeats)` — carrying the comment "the wording is the
deliverable — this run was misread as a stall once"; `"Rejoined" in text`;
`"job interaction_in_flight_42" in text`; `"charged twice" in text`; `"refused" in text`;
`"paid for again" in text`; and a cardinality bound `2 <= len(thinking) <= 8` for a 31-poll wait.

Any rewrite that breaks one of those substrings turns the engine gate RED. That is not a reason
to avoid the rewrite — it is the reason the test path and the source path move in ONE edit, and
the reason the audit must list the pins so the operator rules with them in view.

The volume arithmetic nobody has done. `_POLL_EVENT_STRIDE`
(`audited_llm_client.py:107-113`) already caps a 31-poll wait at a handful of heartbeats; the CI
test pins the range at 2 to 8. But those lines fire PER ANGLE, and run `368ff3a0` dispatched 19
sub-questions. So the deep-research phase's thinking volume is on the order of (one waiting line
plus a handful of heartbeats) times the angle count — roughly a hundred near-identical rows
saying the same true and useful thing about different angles.

That reframes the diagnosis. The problem may not be that the CONTENT is engineer-facing —
measurement says it is not. It may be that a correct, operator-facing line is MULTIPLIED by the
fan-out. The fix for "wrong content" and the fix for "right content, wrong cardinality" are
completely different edits with different risk.

D-14 stands and is unaffected: cutting a line from the feed means demoting it to `log.warning`,
never deleting it. Note that lines 1400-1405 and 1430-1435 ALREADY carry a matching log call
beside their emit, so for those two sites the demotion is already paid for.
</measured_facts>

<interfaces>
The five `agent_fail` sites on `stage="deep_research"` in the same file, listed so the audit
covers the whole surface rather than only the `thinking` half:
`audited_llm_client.py` lines 1569, 1622, 1911, 1925, 1969.

The inventory command that produced the table above, so the audit re-derives rather than trusts:
`grep -n "run_events.emit_safe" -A 4 tribunal/nestor_pulse_sdk/audit/audited_llm_client.py`

The run-count source for the volume arithmetic:
`docs/tribunal-run-reports/run-20260805-368ff3a0-DISPATCH.md` records all dispatched
sub-questions for the only measured run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the density audit as a reviewable document</name>
  <files>.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-DENSITY-AUDIT.md</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/audit/audited_llm_client.py lines 100-120 (`_POLL_EVENT_STRIDE` and its rationale)
    - tribunal/nestor_pulse_sdk/audit/audited_llm_client.py lines 1390-1650 (the Gemini deep-research path — 4 thinking sites, 2 agent_fail sites)
    - tribunal/nestor_pulse_sdk/audit/audited_llm_client.py lines 1695-1980 (the OpenAI deep-research path — 4 thinking sites, 3 agent_fail sites)
    - tribunal/nestor_pulse_sdk/tests/test_own_researcher.py lines 1280-1400 (EVERY pinned substring — read them, do not trust the table above)
    - tribunal/cloudbuild.test-engine.yaml lines 485-535 (confirm `test_own_researcher.py` is registered and `EXPECTED_FILES` is 44)
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-CONTEXT.md (D-12, D-13, D-14 and the noise premise the measurement contradicts)
    - docs/tribunal-run-reports/run-20260805-368ff3a0-DISPATCH.md (the dispatched sub-question count for the volume arithmetic)
  </read_first>
  <action>
Create `.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-DENSITY-AUDIT.md`.
`.planning/` is gitignored, so committing it needs `git add -f`.

Five sections, in this order.

1. WHAT WAS MEASURED, AND HOW. Name the commit, name the grep that produced the site inventory,
and give the raw counts: how many `emit_safe` sites in the file, how many per kind, how many
carry `stage="deep_research"`. RE-DERIVE these from the code. If your count differs from the
planner's table above, YOUR NUMBER WINS and the document says so — a measured integer in a
comment rots, a dated measurement in a document does not.

2. THE PER-SITE TABLE. One row per `thinking` site AND one row per `agent_fail` site, with
columns: line, provider, one-clause summary of what the line says, D-13 class (long-silence /
money / guard commentary / parser-defect / other), CI-pinned substrings or "none", and a
recommended verdict of KEEP-AS-IS, REWRITE-SHORTER, DEMOTE-TO-LOG or NEEDS-RULING.
Sites 1408 and 1710 are recorded as NEEDS-RULING with the reason stated: D-13 says CUT
guard-refusal commentary and KEEP anything about money, and each of those two sentences is BOTH.
Resolving that by fiat is exactly what D-12 forbids.

3. THE CONTRADICTION, STATED PLAINLY. A short section headed so a skim cannot miss it:
21-CONTEXT.md's premise is that most of this file's lines are engineer-facing exception-path
commentary; the measurement says eight of eight `thinking` lines are money or long-silence, which
are D-13's two KEEP classes; therefore under D-13 as written, the correct trim of this file's
`thinking` CONTENT is zero cuts. One sentence, unsoftened.

4. THE VOLUME FINDING. Show the arithmetic: `_POLL_EVENT_STRIDE`'s bound per angle, times the
angle count from run `368ff3a0`. Give the resulting order-of-magnitude row count for the
deep-research phase. Then state the consequence: if the complaint is volume rather than content,
the lever is CARDINALITY — a stage-level wait line instead of a per-angle one — which is a
different edit from shortening prose, with a different test impact.

5. THE THREE OPTIONS, COSTED. Each written as a decision the operator can take, with its
mechanism, what breaks, and what it does not fix.
   Option A, SHORTEN IN PLACE: rewrite all eight `thinking` lines as short phrases per D-13's
   "the row is a 13px monospace line in a feed, not a log entry", preserving every CI-pinned
   substring verbatim. No test edit. Lowest risk. Does NOT reduce row COUNT at all.
   Option B, COLLAPSE THE CARDINALITY: emit the waiting line and the heartbeat ONCE for the
   deep-research stage rather than once per angle. Turns roughly a hundred rows into a handful.
   Requires an engine change to where the emit lives plus a matching edit to
   `test_own_researcher.py` in the same commit. State what is LOST — per-angle attribution, so an
   operator can no longer see which specific angle is slow — and what is GAINED — the phase
   becomes readable at a glance.
   Option C, NO CHANGE TO THIS FILE: all eight lines are KEEP-class and five are CI-pinned as
   "the wording is the deliverable". The verbosity may be RELATIVE — this was the only stage
   saying anything while eight said nothing. Once 21-03, 21-05 and 21-06 land, deep_research
   stops being the only thing on the page, and the collapse toggle fixed in 21-01 starts actually
   collapsing it. Under C, SC5 is satisfied by the audit itself: the diagnosis is that no cut is
   warranted, recorded with its reasons.
   Recommend ONE, in one sentence, with the reason. Do NOT implement it.

The document ends with a heading "## Operator ruling" and an empty line beneath it, for Task 2.

Change NO source file in this task.
  </action>
  <verify>
    <automated>ls -l .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-DENSITY-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `21-DENSITY-AUDIT.md` exists and is non-empty.
    - The per-site table names all 13 `deep_research` emit sites by line number: `grep -c "^| 1" 21-DENSITY-AUDIT.md` returns at least 13.
    - The document quotes at least 6 of the CI-pinned substrings verbatim, each attributed to `test_own_researcher.py`: `grep -c "test_own_researcher" 21-DENSITY-AUDIT.md` is at least 1 and `grep -c "NOT A STALL" 21-DENSITY-AUDIT.md` is at least 1.
    - Section 3 exists and states the contradiction: `grep -ci "contradict" 21-DENSITY-AUDIT.md` is at least 1.
    - Section 5 names three lettered options and one recommendation: `grep -c "Option A\|Option B\|Option C" 21-DENSITY-AUDIT.md` is at least 3.
    - The ruling heading exists and is empty: `grep -n "## Operator ruling" 21-DENSITY-AUDIT.md` matches, and the file has no ruling text under it yet.
    - `git status --porcelain` shows NO modification to any file under `tribunal/` — the diagnosis precedes the edit.
  </acceptance_criteria>
  <done>The audit exists, re-derives its own numbers, lists every CI pin, states the contradiction, does the volume arithmetic, and offers three costed options with a recommendation and an empty ruling slot.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Operator rules on the density verdict</name>
  <files>.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-DENSITY-AUDIT.md (the ruling section only)</files>
  <read_first>
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-DENSITY-AUDIT.md (the whole document written in Task 1 — this is what is being ruled on)
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-CONTEXT.md (D-12, D-13, D-14 verbatim)
    - tribunal/nestor_pulse_sdk/tests/test_own_researcher.py lines 1280-1400 (the five pinned assertions, so the cost of option B is read rather than described)
  </read_first>
  <action>
PAUSE. Present the decision below with its four options and wait. Make no source change in this
task. When the operator answers, write their selection VERBATIM, with the date, under the
`## Operator ruling` heading in 21-DENSITY-AUDIT.md, and record the classification they gave for
lines 1408 and 1710 explicitly rather than inferring it. That ruling is Task 3 entire scope.
  </action>
  <decision>
    Which of the three options in `21-DENSITY-AUDIT.md` is applied to
    `audited_llm_client.py`'s deep-research feed rows — and, separately, how the two guard-refusal
    lines at 1408 and 1710 are classified, since D-13's rule marks them both CUT and KEEP.
  </decision>
  <context>
    D-12 is the operator's own decision: diagnose before trimming, and produce the per-site
    verdict as a reviewable artifact rather than folding it into a commit. This is that review.

    The reason a ruling is genuinely needed rather than assumed: the measurement contradicts the
    premise the trim was proposed on. Eight of eight `thinking` lines are money or long-silence —
    D-13's two KEEP classes — and five of them are pinned by a registered CI test whose own
    comment says "the wording is the deliverable — this run was misread as a stall once". Under
    D-13 as written, the correct content trim is zero. What the measurement DOES support is a
    cardinality problem: a correct line multiplied by 19 angles.

    Nothing in the other seven plans of this phase depends on this ruling. If the answer is
    Option C, the phase loses nothing.
  </context>
  <options>
    <option id="option-a">
      <name>Shorten in place</name>
      <pros>Lowest risk; no test edit; honours D-13's "short phrase, not a paragraph" instruction; every CI-pinned substring survives verbatim.</pros>
      <cons>Reduces the number of CHARACTERS, not the number of ROWS. If the complaint was volume, this does not fix it.</cons>
    </option>
    <option id="option-b">
      <name>Collapse the cardinality</name>
      <pros>Turns roughly a hundred near-identical wait rows into a handful; directly addresses the measured volume driver.</pros>
      <cons>Loses per-angle attribution — the operator can no longer see WHICH angle is slow. Requires a real engine change plus a matching edit to `test_own_researcher.py`'s pinned assertions in the same commit, and that test's cardinality bound (2 to 8 thinking rows for a 31-poll wait) has to be re-derived rather than deleted.</cons>
    </option>
    <option id="option-c">
      <name>No change to this file</name>
      <pros>Every line is KEEP-class by D-13's own rule; five are pinned as deliverable wording. The verbosity may have been RELATIVE — deep_research was the only stage speaking. Plans 21-03/05/06 give the other eight stages bodies and 21-01 makes the collapse toggle actually collapse, both of which change the reading without touching a pinned string.</pros>
      <cons>If the operator re-runs and still finds the feed too noisy, this costs one round trip. It is also the option most likely to look like inaction, which is why the audit records the reasoning.</cons>
    </option>
    <option id="option-d">
      <name>A + C: shorten the two unpinned guard-refusal lines only</name>
      <pros>Resolves the NEEDS-RULING pair (1408, 1710) by keeping the money clause and dropping the guard mechanism to the log that already sits beside each of them (D-14 is pre-paid at both sites). Leaves every pinned line alone.</pros>
      <cons>Smallest possible change; does not address volume at all.</cons>
    </option>
  </options>
  <verify>
    <human-check>The operator has selected one of option-a / option-b / option-c / option-d and classified lines 1408 and 1710, and the selection is written under ## Operator ruling in 21-DENSITY-AUDIT.md with its date.</human-check>
  </verify>
  <resume-signal>Select: option-a, option-b, option-c, or option-d — and say how lines 1408 and 1710 are classified.</resume-signal>
  <acceptance_criteria>
    - The operator's selection is written verbatim under `## Operator ruling` in `21-DENSITY-AUDIT.md`, dated, before Task 3 begins.
    - The classification of lines 1408 and 1710 is recorded explicitly, not inferred.
    - If the ruling is option-c, Task 3 makes NO source edit and the plan closes with the audit as its deliverable.
  </acceptance_criteria>
  <done>The ruling is recorded in the audit document and Task 3's scope is fixed by it.</done>
</task>

<task type="auto">
  <name>Task 3: Apply exactly what was ruled, and nothing else</name>
  <files>tribunal/nestor_pulse_sdk/audit/audited_llm_client.py, tribunal/nestor_pulse_sdk/tests/test_own_researcher.py (only if the ruling changes a pinned substring)</files>
  <read_first>
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-DENSITY-AUDIT.md (the ruling recorded in Task 2 — this is the scope)
    - tribunal/nestor_pulse_sdk/audit/audited_llm_client.py (the sites named by the ruling, in full, before editing any of them)
    - tribunal/nestor_pulse_sdk/tests/test_own_researcher.py lines 1280-1400 (the pinned assertions)
    - tribunal/nestor_pulse_sdk/runs/run_events.py lines 402-468 (the emit_safe contract — every rewritten site keeps its text inside the thunk)
  </read_first>
  <action>
Apply the ruling from Task 2 and nothing beyond it.

Rules that bind whichever option was chosen:

- D-14: a line that leaves the feed is DEMOTED to `log.warning`, never deleted. Every one of
  these lines documents a real condition someone once needed to see; the feed is the wrong
  reader, the log is the right one. Where a matching log call already exists beside the emit
  (lines 1400-1405 and 1430-1435), the demotion is already paid for and no new log call is
  needed — say so in the commit rather than adding a duplicate.
- THE THUNK RULE SURVIVES EVERY REWRITE. A shorter string is still built inside
  `build=lambda: (...)`. Do not hoist a rewritten f-string above the `emit_safe` call while
  "tidying" it — that is the exact defect `run_events.py:402-468` exists to prevent, and it
  looks correct.
- THE PIN RULE: if a rewrite changes a substring that `test_own_researcher.py` asserts, the
  source edit and the test edit land in ONE commit. Never loosen an assertion to make a rewrite
  pass — re-derive it against the new wording, and keep the cardinality bound as a bound rather
  than deleting it.
- NO NEW EVENT KIND and no new meta key (D-03). No `stage` value changes.
- Do not touch any of the five `agent_fail` sites unless the ruling names them.

If the ruling is option-c, this task makes NO source edit. Record that as the outcome rather than
finding something to change — a plan that closes with a documented decision not to act is a
completed plan.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; "C:/Users/ajimimo/Desktop/MOELD/Nestor/.venv/Scripts/python.exe" -m pytest nestor_pulse_sdk/tests/test_own_researcher.py nestor_pulse_sdk/tests/test_provider_resume.py nestor_pulse_sdk/tests/test_deep_research_adapters.py -q -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - The pytest command in `<verify>` exits 0 with zero failures.
    - The diff touches ONLY the files the ruling names. `git diff --name-only` is compared against the ruling text in `21-DENSITY-AUDIT.md` and any extra path is removed before the task closes.
    - The thunk discipline is intact: in `audited_llm_client.py`, the count of `run_events.emit_safe` occurrences equals the count of `build=lambda` occurrences. Prove with two greps and compare the numbers.
    - No new kind was introduced: every `kind=` literal in `audited_llm_client.py` is a member of `RUN_EVENT_KINDS`. Prove with an import-and-assert one-liner over the file source, not a grep for names.
    - If any assertion in `test_own_researcher.py` changed, the same commit contains both the source change and the test change — verify with `git show --stat HEAD` listing both paths.
    - If the ruling was option-c, `git diff --name-only tribunal/` returns nothing, and the SUMMARY records the decision not to act along with its reason.
  </acceptance_criteria>
  <done>The ruled change is applied (or deliberately not applied), the three provider-path test files are green, and no pinned assertion was loosened to make a rewrite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider response → feed row | `interaction.get("status")`, `interaction.get("error")` and job ids are provider-controlled strings reaching a persisted, operator-visible row. |
| job id → stored row | `safe_job_id` refuses a malformed id BEFORE it reaches the URL builder or a stored row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Tampering | a rewritten feed line that embeds a provider-supplied job id | mitigate | `test_own_researcher.py` already asserts that a poisoned id and the substring `..` never appear in any recorded row text (T-15.3-32). That test runs unchanged in `<verify>`; a rewrite that started interpolating a raw id turns it red. |
| T-21-04-02 | Information disclosure | shorter lines dropping a scrub-relevant boundary | accept | `run_events.emit` scrubs with `scrub_pii` and THEN clamps, inside the emitter (`run_events.py:345-351`). A shorter string cannot bypass it, and this plan adds no path around `emit_safe`. |
| T-21-04-03 | Repudiation | a line deleted rather than demoted | mitigate | D-14 is a hard rule in Task 3's action, and the acceptance criteria bind the diff to the ruling. Two of the candidate sites already carry their log call, so the demotion costs nothing there. |
| T-21-04-04 | Denial of service | option-b's cardinality change reaching the paid poll loop | mitigate | Under option-b the emit MOVES rather than multiplies, and `_POLL_EVENT_STRIDE` continues to bound heartbeats. The pinned cardinality assertion in `test_own_researcher.py` is re-derived rather than deleted, so the bound survives the change. |
| T-21-04-SC | Tampering | npm/pip/cargo installs | mitigate | NOT APPLICABLE BY CONSTRUCTION: this plan installs no package and edits no `requirements.txt`. `git diff --name-only` proves it. |
</threat_model>

<verification>
1. `21-DENSITY-AUDIT.md` exists, re-derives its own counts, lists every CI pin, and carries a dated operator ruling.
2. `python -m pytest nestor_pulse_sdk/tests/test_own_researcher.py nestor_pulse_sdk/tests/test_provider_resume.py nestor_pulse_sdk/tests/test_deep_research_adapters.py -q -m "not live"` is green.
3. `emit_safe` count equals `build=lambda` count in `audited_llm_client.py`.
4. `grep -n "EXPECTED_FILES=" tribunal/cloudbuild.test-engine.yaml` still reads 44 — this plan adds no test file.
5. The diff matches the ruling exactly.
</verification>

<success_criteria>
- SC5 is satisfied by a DIAGNOSIS the operator ruled on, which is what D-12 asked for — whether or not that ruling results in a code change.
- The measurement that contradicts 21-CONTEXT.md's noise premise is recorded where the next reader will find it, rather than being quietly acted on or quietly ignored.
- No CI-pinned assertion was loosened, and no feed line was deleted rather than demoted.
</success_criteria>

<output>
Create `.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-04-SUMMARY.md` when done.
Record: the re-derived site counts (and whether they matched the planner's table), the operator's
verbatim ruling, the exact diff, and — if the ruling was option-c — the decision not to act with
its reason, so a later reader does not re-open it as an oversight.
Remember `git add -f` for both `.planning/` documents.
</output>
