---
phase: 21-research-run-feed-completion-silent-post-research-stages-stu
plan: 07
type: execute
wave: 4
depends_on: [21-06]
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
  - tribunal/nestor_pulse_sdk/runs/stages.py
  - tribunal/nestor_pulse_sdk/tests/test_stage_schema.py
autonomous: false
requirements: [SC6, D-15]

must_haves:
  truths:
    - "No raw snake_case stage key can reach the operator's screen as a phase divider"
    - "The stale 'fourteen stages' claim in pipeline.py's label docstring is corrected to the counted number"
    - "A stage key that is deliberately kept out of the ordered schema must still carry a human label, and a test enforces that"
    - "The WR-03 defect class cannot recur a third time undetected"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      provides: "`_stage_event_label` resolving every key the pipeline can write, not only schema members"
      contains: "_stage_event_label"
    - path: "tribunal/nestor_pulse_sdk/tests/test_stage_schema.py"
      provides: "The strengthened WR-03 guard — every set_stage key resolves to a human label on the render path"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/tests/test_stage_schema.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      via: "the existing source-reading `_SET_STAGE_RE` extraction, extended to assert label resolution"
      pattern: "_stage_event_label"
---

<objective>
Close SC6: no raw stage key reaches the operator's screen, and the WR-03 defect class — a stage
key written by the pipeline that the render path cannot name — cannot recur undetected.

⚠ THIS PLAN CARRIES A CORRECTION TO D-15. The premise D-15 was written on is contradicted by the
code, and getting this wrong introduces a NEW operator-visible defect in a plan meant to remove
one. Read `<measured_facts>` in full before writing anything, and do not start Task 2 before the
Task 1 checkpoint is answered.

Output: a corrected label resolver, a corrected docstring, and a guard that is strictly stronger
than the one that already exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-CONTEXT.md

<measured_facts>
Read out of the tree at `30443e8` during planning. Every one of these is checkable in under a
minute and every one of them changes what the right fix is.

**FACT 1 — THE RECURRENCE-GUARD TEST D-15 ASKS FOR ALREADY EXISTS.**
`tribunal/nestor_pulse_sdk/tests/test_stage_schema.py:175-203` is
`test_every_set_stage_key_in_the_pipeline_is_declared`. It reads `pipeline.py` AS TEXT with
`_SET_STAGE_RE`, extracts every `set_stage` stage key, and asserts each is declared in
`ENGINE_STAGES["tribunal"]`. It already carries a vacuity guard (`len(found) >= 8`) and a
positive control (`{"gate", "verify", "synthesize"} <= found`). It is REGISTERED — it is in
`tribunal/cloudbuild.test-gates.yaml`'s 13-file `WANTED` list.
So D-15's "the planner should add one; it is the only thing here that prevents a fourth" is
already satisfied by an existing, running test. The question is not whether to add a guard. It is
why the existing guard did not fire.

**FACT 2 — `report_spec` WAS NOT UNDETECTED. IT WAS FOUND, CONSIDERED, AND DELIBERATELY EXCLUDED,
WITH A WRITTEN RATIONALE.**
`test_stage_schema.py:37-48` defines `_NON_SCHEMA_MARKERS = {"done", "report_spec"}`. The comment
beside `report_spec` reads, in substance: an interactive PAUSE, not a pipeline stage; the run
parks as `needs_report_spec` until the user supplies a report shape; declaring it "would put a
phantom row in the ordered checklist of every NON-interactive run, so it stays out of the
schema"; and it is listed explicitly "so the exception is reviewable, not silently absorbed".
That decision was made by plan 15.1-13.
So 21-CONTEXT's framing — "It recurred, undetected, on a key added later" — is measurably wrong on
both halves: it was detected, and `report_spec` is not a key added later.

**FACT 3 — THE PHANTOM-ROW COST IS REAL, AND DECLARING `report_spec` WOULD PAY IT.**
`RunMetrics.stages` is built from `stages_for(run.engine)` (`runs/api.py`, around line 846) and
the intake card renders that as an ordered checklist. Adding `report_spec` puts a step in EVERY
run's checklist that will never fire for a seam run. It would also break the exact-list assertion
at `test_stage_schema.py:116-130`.

**FACT 4 — `report_spec` CAN ONLY BE WRITTEN ON THE INTERACTIVE PATH, WHICH NEVER FIRES FOR SEAM
RUNS.** `pipeline.py:3955` sits inside `if interactive_report:` (line 3948). `interactive_report`
comes from the `[INTERACTIVE_REPORT]` brief sentinel (`pipeline.py:3985-3987`), and Phase 16's
D-01/D-01b state that the interactive pause gates NEVER fire for seam runs — the validated intake
IS the brief and the report spec is auto-derived. So the operator has almost certainly never seen
a `report_spec` divider on screen, and 21-CONTEXT's "it sits squarely inside the operator's
complaint" is an inference rather than an observation. The DEFECT IS STILL REAL — the fallback at
`_stage_event_label` would print the raw key if that path ever ran — but it is a latent defect,
not the thing the operator saw.

**FACT 5 — THE STALE DOCSTRING IS REAL AND IS WORTH FIXING.** `pipeline.py:483` claims
`ENGINE_STAGES["tribunal"]` "has carried a label for all fourteen stages since Phase 15". Count
the list at `runs/stages.py:39-71`: there are THIRTEEN. `test_run_event_emit.py`'s own header
repeats the same "fourteen" claim. Both are wrong.

**FACT 6 — THE PROJECT ALREADY HAS A RULE FOR EXACTLY THIS SHAPE.** `.planning/CONTINUE-HERE.md`
§ "Three generalisations worth more than the specifics": *"When a truncated identifier reaches a
reader, resolve it on the READ path instead of widening the identifier. Second time this has paid
off (G-10, then G-5). Widening an identity key renames every stored value."* A raw key reaching a
reader is the same shape, and the read path here is `_stage_event_label`.

**WHAT THE ROADMAP ACTUALLY REQUIRES.** SC6's first clause is the OUTCOME — "No raw stage key ever
reaches the operator's screen" — and the rest is a proposed mechanism. The read-path fix achieves
the outcome without the phantom row and without reversing a recorded decision. That is the
planner's recommendation, and Task 1 puts it to the operator rather than acting on it.
</measured_facts>

<interfaces>
The read path, `pipeline.py:480-493`:

```python
def _stage_event_label(stage_key: Any) -> str:
    """The stage's HUMAN LABEL, falling back to the raw key. ..."""
    key = str(stage_key)
    for entry in stages_for("tribunal"):
        if entry.get("key") == key:
            return str(entry.get("label") or key)
    return key
```

Its ONE caller is `_stage_event_boundary` at `pipeline.py:560-565`, which emits the `divider`
event whose `text` the frontend renders verbatim as the uppercase phase label
(`RunFeed.tsx:297-306` — that component looks up no stage vocabulary and needs none).

The existing guard, `test_stage_schema.py:33-48 and 175-203`: `_SET_STAGE_RE`,
`_NON_SCHEMA_MARKERS`, and `test_every_set_stage_key_in_the_pipeline_is_declared`.

`test_stage_schema.py` is registered in `tribunal/cloudbuild.test-gates.yaml` (13 files). This
plan adds NO test file, so that config's list and count do not move — and neither does
`cloudbuild.test-engine.yaml`'s 44.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Operator rules — declare the marker, or label it on the read path</name>
  <files>none (decision only)</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/test_stage_schema.py lines 33-48 and 100-203 (the existing guard, the _NON_SCHEMA_MARKERS allowlist and the 15.1-13 rationale comment — the recorded decision this choice may reverse)
    - tribunal/nestor_pulse_sdk/runs/stages.py lines 38-71 (count the thirteen)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 480-493 and 3944-3990 (the label resolver and the one report_spec write, inside the interactive branch)
    - .planning/CONTINUE-HERE.md (the READ-PATH generalisation under "Three generalisations worth more than the specifics")
  </read_first>
  <action>
PAUSE. Present the decision below with its two options and wait. Make no source change in this
task. State the correction to D-15 plainly when presenting: the recurrence-guard test already
exists and is registered, `report_spec` was deliberately excluded from it by plan 15.1-13 with a
written rationale, and it can only be written on the interactive branch that never fires for seam
runs. When the operator answers, record the selection VERBATIM with its date in 21-07-SUMMARY.md
before Task 2 begins. That selection is Task 2 entire scope.
  </action>
  <decision>
    How `report_spec` (and `done`, and any future non-schema marker) stops rendering as a raw
    snake_case key: by DECLARING it in `ENGINE_STAGES["tribunal"]` as D-15 proposes, or by giving
    the READ PATH a label for it while leaving the ordered schema at thirteen.
  </decision>
  <context>
    D-15 is marked [ASSISTANT — CORRECTABLE] in 21-CONTEXT.md, and the planner's measurement
    contradicts three of its premises. Stated plainly so the choice is informed:

    - The recurrence-guard test D-15 asks for ALREADY EXISTS and is already registered in CI
      (`test_stage_schema.py:175-203`).
    - `report_spec` was NOT missed by it. It is in that test's `_NON_SCHEMA_MARKERS` allowlist,
      put there deliberately by plan 15.1-13 with a written reason: declaring it "would put a
      phantom row in the ordered checklist of every NON-interactive run".
    - `report_spec` is written only inside `if interactive_report:`, and Phase 16's D-01/D-01b
      say that gate never fires for seam runs — so this is a LATENT defect, not the raw key the
      operator saw during UAT. The operator's "rubbish information" complaint is answered by the
      other seven plans, not by this one.

    Whichever way this goes, both options fix the same latent defect and both strengthen the
    guard. The difference is whether a step that never fires appears in every run's checklist.
  </context>
  <options>
    <option id="option-declare">
      <name>Declare report_spec in ENGINE_STAGES, as D-15 proposes</name>
      <pros>Literally matches SC6's proposed mechanism and D-15's text. One entry, one label. The existing guard then covers it with no allowlist entry at all.</pros>
      <cons>Reverses a recorded 15.1-13 decision without new evidence. Puts a phantom "Report shaping" step in the ordered checklist of every non-interactive run — which is every seam run — so it introduces a new operator-visible oddity while removing a latent one. Also breaks the exact-list assertion at `test_stage_schema.py:116-130`, which must then be updated.</cons>
    </option>
    <option id="option-read-path">
      <name>Label it on the read path; leave the ordered schema at thirteen (PLANNER'S RECOMMENDATION)</name>
      <pros>Achieves SC6's actual outcome — no raw key on screen — with no phantom checklist row and no reversal of a recorded decision. Follows the project's own standing generalisation: "when a truncated identifier reaches a reader, resolve it on the READ path instead of widening the identifier" (CONTINUE-HERE.md), which has already paid off twice (G-10, G-5). Lets the guard be made STRICTLY STRONGER: every marker on the allowlist must now prove it has a human label, so the allowlist can never again grow into a raw-key leak.</pros>
      <cons>Two places now hold stage labels — the schema and a small marker map — and a future reader must be told why. The docstring and the test are where that is written down.</cons>
    </option>
  </options>
  <verify>
    <human-check>The operator has selected option-declare or option-read-path and the selection is recorded verbatim with its date in 21-07-SUMMARY.md.</human-check>
  </verify>
  <resume-signal>Select: option-declare or option-read-path.</resume-signal>
  <acceptance_criteria>
    - The selection is recorded verbatim, with its date, in `21-07-SUMMARY.md` before Task 2 begins.
    - If option-declare is chosen, the SUMMARY also records the acknowledgement that a phantom checklist row is being accepted, so the next reader does not file it as a regression.
  </acceptance_criteria>
  <done>The operator has selected an option and it is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Make every stage key the pipeline can write resolve to a human label</name>
  <files>tribunal/nestor_pulse_sdk/runs/stages.py, tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py, tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py (header comment only)</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 452-570 (`_stage_event_label`, its docstring, and its one caller `_stage_event_boundary`)
    - tribunal/nestor_pulse_sdk/runs/stages.py lines 33-91 (`ENGINE_STAGES`, the WR-03 comments already in it, `stages_for`)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 3944-3990 (the `if interactive_report:` branch, the one `report_spec` write, and the `[INTERACTIVE_REPORT]` sentinel)
    - tribunal/nestor_pulse_sdk/tests/test_stage_schema.py (the whole file — especially `_NON_SCHEMA_MARKERS` at 37-48 and the exact-list assertion at 116-130)
    - .planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-07-SUMMARY.md (the ruling from Task 1 — this is the scope)
  </read_first>
  <action>
Apply the ruling from Task 1 and nothing beyond it.

IF THE RULING IS option-read-path:
- In `runs/stages.py`, add a module-level mapping — name it `NON_SCHEMA_STAGE_LABELS` — from the
  keys the pipeline writes that are deliberately NOT ordered schema entries to their human labels:
  `done` and `report_spec`. Give `report_spec` a label that reads as what it is to a watcher
  (something in the register of "Report shaping", matched to the existing labels' style — they are
  sentence-case noun phrases like "Claim distillation" and "Skeptic verification").
  Its docstring states the rule in one paragraph: these keys are written to `run.current_stage`
  but are NOT ordered checklist steps, because a step that never fires for a non-interactive run
  would be a phantom row in every seam run's checklist (the 15.1-13 reasoning, preserved). They
  still need a label, because the divider on the run page renders whatever the label resolver
  returns, and a raw snake_case key on an operator's screen is the WR-03 defect wearing a
  different hat.
- In `pipeline.py`, `_stage_event_label` consults `ENGINE_STAGES["tribunal"]` FIRST and
  `NON_SCHEMA_STAGE_LABELS` SECOND, and only then falls back to the raw key. Keep the raw-key
  fallback — a key from a newer engine build that neither source knows must still render
  something rather than an empty line, exactly as the current docstring says.

IF THE RULING IS option-declare:
- Add `{"key": "report_spec", "label": ...}` to `ENGINE_STAGES["tribunal"]` in the position the
  pipeline actually writes it. ⚠ VERIFY THAT POSITION AGAINST THE CODE rather than trusting
  21-CONTEXT's static call-site map: the map reads 3589 `conflict` → 3749 `synthesize` → 3955
  `report_spec` → 4179 `synthesize`, but 3955 is inside `if interactive_report:` and RETURNS
  immediately (`pipeline.py:3960`), so it is a TERMINAL pause on a branch, not a step between two
  synthesize writes. Place it where a reader of the ordered checklist would expect a pause, and
  say in the entry's comment why that position was chosen.
- Remove `report_spec` from `_NON_SCHEMA_MARKERS` in `test_stage_schema.py` and update the
  exact-list assertion at 116-130.
- Record in the SUMMARY that the phantom checklist row is an accepted cost.

IN BOTH CASES:
- Correct the stale docstring at `pipeline.py:483`. It claims fourteen stages;
  `ENGINE_STAGES["tribunal"]` carries thirteen (or fourteen, if option-declare was taken — COUNT
  THE LIST, do not compute it from this sentence). Replace the number with the counted one and add
  a clause naming where a non-schema marker's label comes from, so the next reader does not have
  to discover the second source by accident.
- Correct the same "fourteen" claim in `tribunal/nestor_pulse_sdk/tests/test_run_event_emit.py`'s
  header comment if it is still there. It is a comment fix in a file another plan in this phase
  also edits — make it a single, isolated edit.
- Change NOTHING about which key the pipeline writes, when it writes it, or the
  `if interactive_report:` branch itself.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; "C:/Users/ajimimo/Desktop/MOELD/Nestor/.venv/Scripts/python.exe" -m pytest nestor_pulse_sdk/tests/test_stage_schema.py nestor_pulse_sdk/tests/test_run_event_emit.py nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py -q -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - The pytest command in `<verify>` exits 0 with zero failures.
    - EVERY key the pipeline can write resolves to something that is not the raw key. Prove it with an executed assertion, not a grep: import `_stage_event_label` and `_SET_STAGE_RE`'s extraction over `pipeline.py`, and assert that for every extracted key K, `_stage_event_label(K) != K`. Record the extracted key set in the SUMMARY.
    - `_stage_event_label("report_spec")` returns a label containing a space and at least one uppercase letter — a snake_case key can satisfy neither.
    - The docstring number is the counted number: assert `str(len(stages_for("tribunal")))` appears in `_stage_event_label.__doc__`, or that the word "fourteen" does not appear in it when the count is thirteen. Use `inspect.getdoc`, not a grep over the file, so a comment elsewhere cannot satisfy it.
    - `grep -ci "fourteen" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns 0 unless the count is genuinely fourteen.
    - The pipeline's behaviour is unchanged: `grep -c "await set_stage(" pipeline.py` equals the count on the base commit, and `git diff -U0` shows no hunk intersecting lines 3944-3990 other than comment-only changes.
    - Neither CI config moved: `grep -n "EXPECTED_FILES=" tribunal/cloudbuild.test-engine.yaml` still reads 44, and `tribunal/cloudbuild.test-gates.yaml`'s own expected count is unchanged. Read both numbers out of the files rather than asserting from memory.
  </acceptance_criteria>
  <done>Every stage key the pipeline can write resolves to a human label, proven by an executed assertion over the keys extracted from the source, and the stale count is corrected in both places.</done>
</task>

<task type="auto">
  <name>Task 3: Make the WR-03 guard strictly stronger than the one that missed this</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_stage_schema.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/test_stage_schema.py (the whole file — the module docstring explains why the existing guard was written and what it was for)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 452-570 (the label resolver as Task 2 left it)
    - tribunal/cloudbuild.test-gates.yaml (confirm `test_stage_schema.py` is registered and note the expected count — this task must not change either)
  </read_first>
  <action>
Extend `tribunal/nestor_pulse_sdk/tests/test_stage_schema.py`. Create no new test file and edit
neither CI config.

The existing `test_every_set_stage_key_in_the_pipeline_is_declared` stays exactly as it is — it
is a correct test and it did its job. What it does NOT cover is the gap this plan found: a key on
the `_NON_SCHEMA_MARKERS` allowlist is exempt from declaration and is therefore exempt from
having a LABEL, so the allowlist is a hole through which a raw key reaches the screen.

Add these tests.

(a) `test_every_pipeline_stage_key_resolves_to_a_human_label` — THE STRENGTHENED GUARD, and the
    one that would have caught this. Extract every `set_stage` key from `pipeline.py` with the
    existing `_SET_STAGE_RE`, then for each one assert `_stage_event_label(key) != key`. Carry the
    same vacuity guard the existing test carries (at least 8 keys extracted) and a positive
    control naming `report_spec` explicitly, so the test is proving coverage rather than iterating
    over nothing.
    Its docstring states, in the same register as the file's existing ones, what it protects and
    why it is not redundant with the test above it: declaration and LABEL RESOLUTION are two
    different properties, `_NON_SCHEMA_MARKERS` exempts a key from the first and nothing checked
    the second, and `_stage_event_label`'s raw-key fallback is what turns that gap into a
    snake_case string on the operator's screen.

(b) `test_the_non_schema_allowlist_cannot_grow_into_a_raw_key_leak` — assert that every key in
    `_NON_SCHEMA_MARKERS` resolves through `_stage_event_label` to a label that is not the key
    itself. This is the test that makes ADDING to the allowlist safe: a future engineer who
    exempts a new key from declaration is now forced to give it a label in the same edit.

(c) A NEGATIVE CONTROL, in the spirit of the negative controls this suite already uses elsewhere:
    prove the new assertion BITES. Feed the label resolver a key that is in neither source (an
    invented one) and assert it comes back as the raw key — demonstrating that the raw-key
    fallback is still there and that tests (a) and (b) are therefore asserting something a
    regression could break, rather than being true by construction.

(d) Update `_NON_SCHEMA_MARKERS`'s comment for `report_spec` to reflect the ruling from Task 1.
    If option-read-path was taken, the comment now says: still deliberately out of the ordered
    schema for the 15.1-13 phantom-row reason, AND now required by test (b) to carry a label — the
    exception is reviewable in both dimensions rather than one. If option-declare was taken, the
    entry is gone and the comment goes with it.

Do NOT weaken, reword or delete `test_every_set_stage_key_in_the_pipeline_is_declared`, its
vacuity guard, or its positive control.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; "C:/Users/ajimimo/Desktop/MOELD/Nestor/.venv/Scripts/python.exe" -m pytest nestor_pulse_sdk/tests/test_stage_schema.py -v -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - The pytest command in `<verify>` exits 0 with zero failures and at least 3 more passing tests than on the base commit — record both numbers.
    - THE NEW GUARD HAS BEEN SEEN TO FAIL. Temporarily remove `report_spec`'s label (or, under option-declare, its schema entry), run `test_stage_schema.py`, and confirm test (a) goes RED and names `report_spec` in its assertion message. Restore, re-run green, and record the VERBATIM failure text in the SUMMARY. A guard nobody has watched fail is a guard nobody has verified — this project has shipped two undeclared keys and one of them was found by an operator reading a screen.
    - Test (a) contains a vacuity guard of at least 8 extracted keys and a positive control naming `report_spec`.
    - Test (b) iterates `_NON_SCHEMA_MARKERS` itself rather than a hardcoded copy of its contents — `grep -n "_NON_SCHEMA_MARKERS" test_stage_schema.py` shows it referenced inside that test.
    - `test_every_set_stage_key_in_the_pipeline_is_declared` is byte-identical to HEAD: `git diff` shows no hunk inside its body.
    - `git diff --name-only` for this task lists exactly one path: `tribunal/nestor_pulse_sdk/tests/test_stage_schema.py`. Neither CI config is listed.
    - Both gate configs still run: `cloudbuild.test-engine.yaml` reads 44, and `cloudbuild.test-gates.yaml`'s expected count is unchanged from HEAD. Read both out of the files.
  </acceptance_criteria>
  <done>The allowlist can no longer hide a raw key, the new guard has been observed to fail on a deliberately broken label, and the original WR-03 test is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine stage key → operator screen | `_stage_event_label` is the ONE read path between an internal identifier and the `divider` text the run page renders verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07-01 | Information disclosure | a raw internal key rendered to an operator | mitigate | The read path now resolves both schema keys and declared non-schema markers, and test (a) asserts every key extracted from `pipeline.py` resolves to something other than itself. The raw-key fallback is deliberately KEPT for an unknown key from a newer build — a rolling deploy makes that the normal state of the world — and test (c) proves the fallback still exists. |
| T-21-07-02 | Tampering | a future exemption re-opening the hole | mitigate | Test (b) iterates `_NON_SCHEMA_MARKERS` itself, so adding a key to the allowlist without a label turns the build red in the same edit. |
| T-21-07-03 | Repudiation | reversing a recorded 15.1-13 decision silently | mitigate | Task 1 is a BLOCKING operator decision, and the SUMMARY records the ruling verbatim with its date. Under option-declare the accepted phantom-row cost is recorded too, so the next reader does not file it as a regression. |
| T-21-07-04 | Denial of service | none introduced | accept | This plan adds no emit site, no loop and no I/O. `_stage_event_label` gains one dictionary lookup on a path that runs once per stage transition. |
| T-21-07-SC | Tampering | npm/pip/cargo installs | mitigate | NOT APPLICABLE BY CONSTRUCTION: no package installed, no `requirements.txt` edited. `git diff --name-only` proves it. |
</threat_model>

<verification>
1. The Task 1 ruling is recorded verbatim and dated in `21-07-SUMMARY.md`.
2. `python -m pytest nestor_pulse_sdk/tests/test_stage_schema.py nestor_pulse_sdk/tests/test_run_event_emit.py nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py -q -m "not live"` is green.
3. The executed assertion over every key extracted from `pipeline.py` shows none resolves to itself.
4. The new guard has been OBSERVED to fail on a deliberately removed label, and the failure text is recorded.
5. `grep -ci "fourteen" pipeline.py` returns 0 (unless the count is genuinely fourteen).
6. Both CI configs' expected counts are unchanged, read out of the files.
</verification>

<success_criteria>
- SC6 is CLOSED on its OUTCOME clause: no stage key the pipeline can write renders as a raw snake_case string, proven by an executed assertion over the keys extracted from the source.
- SC6's guard clause is satisfied by a guard STRICTLY STRONGER than the one that already existed: declaration is still asserted, and label resolution — the property the allowlist exempted — now is too.
- The correction to D-15's premises is recorded where the next reader finds it, so nobody re-opens "why was `report_spec` never declared" as an oversight.
</success_criteria>

<output>
Create `.planning/phases/21-research-run-feed-completion-silent-post-research-stages-stu/21-07-SUMMARY.md` when done.
Record: the operator's verbatim ruling with its date; the full set of stage keys extracted from
`pipeline.py` and the label each resolves to; the VERBATIM failure text observed when a label was
temporarily removed; the counted number of tribunal stages after the change; and — because this
is the third time this project has had to reason about WR-03 — one short paragraph for
`.planning/CONTINUE-HERE.md` stating what the guard now covers and what it still does not.
</output>
