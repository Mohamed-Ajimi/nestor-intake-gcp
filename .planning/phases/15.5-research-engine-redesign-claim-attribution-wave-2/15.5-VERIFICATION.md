---
phase: 15.5-research-engine-redesign-claim-attribution-wave-2
verified: 2026-07-29T00:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 15.5: Research Engine Redesign — Claim Attribution (Wave 2) Verification Report

**Phase Goal:** A claim knows which sub-question it answers. Stamp the sub-question and a
`corroboration_key` on the claim row, plus `as_of`. Hard prerequisite for phase 15.6.
**Verified:** 2026-07-29
**Status:** passed
**Re-verification:** No — initial verification

This verification does not trust SUMMARY.md claims. Every truth below was checked against the
merged code on `master` (`54c5388`) directly — by reading the source, by AST-parsing and
executing lifted functions with the bundled stdlib Python 3.14.4
(`C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`), and by diffing against
the pre-phase base commits. No claim below rests on a SUMMARY assertion alone.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The full attribution path is wired end-to-end with matching key names at every hop | VERIFIED | `_angle()` (`research_division.py:836-861`, byte-unchanged, confirmed via `ast.unparse` of the enclosing `divide()`) stamps `sub_question`/`corroboration_key` on the angle → `run_angles` reads them with `angle.get(...) or None` (`:1441-1442`) → single `_enriched` dict literal carries `_sub_question`/`_corroboration_key` (`:1544-1549`, one assignment in the whole file) → `collect_provider_facts` reads `result.get("_sub_question")`/`result.get("_corroboration_key")` at all 4 call sites (`steps.py:2409-2410`, `2434-2437`, `2497-2500`, `2569-2572`; the 4th, `distiller_fallback` at `2622-2638`, passes explicit `None`) → `_normalise_fact_claim` writes `out["sub_question"]`/`out["corroboration_key"]`/`out["as_of"]` (`:2023-2037`) → `_FACT_CLAIM_KEYS` guarantees all 12 keys (`:1847-1851`) → `persist_tribunal_claims` passes `claim.get("sub_question")`/`.get("corroboration_key")`/`.get("as_of")` (`extractor.py:1010-1012`, AST-verified: exactly one `_insert_claim` call, all three kwargs use `.get(`) → `_insert_claim` clamps/coerces and binds them (`:358-474`, AST-verified kwonly tail `['sub_question','corroboration_key','as_of']`, all default `None`, exactly one `bindparams()` call, `as_of` explicitly typed `Date`) → `claim` ORM has the 3 nullable columns (`db/models/claim.py:58,72,84`) → alembic `0017` adds exactly those 3 columns (`upgrade()`/`downgrade()` read in full). |
| 2 | No deploy, no image build, no migration applied anywhere in this phase | VERIFIED | `git diff e5e7d6f^ HEAD` (the full phase span, all 3 plans + D-W2-4) contains zero occurrences of `gcloud run deploy`, `docker build`, or `gcloud builds submit`. Every SUMMARY and the 0017 docstring itself states the migration proof (`Running upgrade 0016 -> 0017`) is OWED, never applied. |
| 3 | The distiller prompt and `_parse_distiller_response` are byte-unchanged | VERIFIED | `ast.unparse()` comparison of `_build_distiller_prompt`, `_parse_distiller_response`, `_split_distiller_line`, `claim_distiller` between the pre-phase base (`e5e7d6f^`) and current `steps.py`: all four **IDENTICAL**. |
| 4 | No behaviour change beyond recording — the new columns have no reader in the pipeline | VERIFIED | `grep -rln "sub_question\|corroboration_key"` across the whole SDK (excluding `tests/`) returns only the writer chain (`claim.py`, `0017_*.py`, `claim_attribution.py`, `steps.py`, `extractor.py`) plus `research_division.py`/`pipeline.py`, and in both of those the hits are the **pre-existing angle-dispatch `corroboration_key` field** (readers at `research_division.py:684,714,1313` and `pipeline.py:3920`'s `_group_size`) — confirmed unrelated to the new claim columns because `pipeline.py` shows **zero diff** across the entire phase (`git diff e5e7d6f^ HEAD -- pipeline.py` empty). `resolved_facet`/`parent_index` have no production caller (grep confirms only `claim_attribution.py` and the test file reference them). |
| 5 | The verification stage is untouched | VERIFIED | `git diff e5e7d6f^ HEAD --stat` lists exactly 10 files under `nestor_pulse_sdk/` (+ `cloudbuild.test-engine.yaml`); none is under `verification/`, `citations/skeptic*`, or any adjudication module. |
| 6 | `_dedupe_claims`'s executable body is unchanged | VERIFIED | Read in full: only the docstring grew a bullet; the executable statements (`seen[norm]=c`, the `found_by`/`provider_quality_by_url`/`source_urls`/`certainty` branches) are unaltered. Independently proved by AST-lifting `_dedupe_claims` out of the committed source and **executing** it: two claims with different `sub_question`/`corroboration_key` merge first-wins; a `None` first occurrence survives a later real value; a distiller-shaped claim (no attribution keys at all) gains **no** new key after the merge. All 4 driven assertions passed. |
| 7 | `corroboration_key` is NULL, never `""`, where dispatch leaves it empty, and the 4 dispatch readers are unaffected | VERIFIED | `research_division.py:1441-1442` uses `angle.get(...) or None` (not `or ''`), with the deviation from the feed-line `or ''` idiom explicitly commented. `_normalise_fact_claim` coerces empty/non-string to `None` (`steps.py:2023-2030`). `_insert_claim`'s `_clamp_attribution` (driven directly: `''`, `'   '`, non-string all → `None`) binds it as `None`. `research_division.py:684,714,1313` and `pipeline.py:3920` are byte-unchanged (the enclosing `divide()` function is `ast.unparse`-IDENTICAL to the pre-phase base; `pipeline.py` has zero diff). |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `db/models/claim.py` | 3 nullable columns, `Date` imported | VERIFIED | Lines 58, 72, 84: `sub_question`/`corroboration_key: Text, nullable=True`; `as_of: Date, nullable=True`. `Date` imported line 15, `date` line 13. |
| `alembic/versions/0017_claim_attribution.py` | revision 0017 on 0016, additive only, deploy-proof stated as owed | VERIFIED | `revision="0017"`, `down_revision="0016"`; `upgrade()`/`downgrade()` each exactly 3 `op.` calls; literal `Running upgrade 0016 -> 0017` present unwrapped on its own line (`line 116`, confirmed with `grep ... | cat -A`, ends `$` — no wrap); docstring states the proof is OWED, never claims it ran. Single head confirmed: `0017` is the only file in `alembic/versions/` declaring `down_revision = "0016"`. |
| `pipeline/synthesis/claim_attribution.py` | `extract_as_of`, `parent_index`, `resolved_facet` | VERIFIED | All three exported (`__all__`), stdlib-only imports (`re`, `datetime`, `logging`, `typing`). `extract_as_of` driven directly against 16 hand-built cases (incl. the full D-W2-4 table: `2021`, `maart 2021`→`2021-03-01`, `2021-03`→`2021-03-01`, `03-2021`→`2021-03-01`, `2021/03`→`2021-03-01`, `4 maart 2021`, `03/04/2021`→`None`) — all 16 correct. The committed `_ACCEPTED`/`_REJECTED` lists in the test file were AST-lifted and driven against the real function: 20+20 cases, 0 failures, and no stale `maart 2021 → 2021-01-01` pinning remains. |
| `citations/extractor.py` | `_insert_claim` writes the 3 columns, typed/clamped/absent-means-NULL | VERIFIED | AST-verified kwonly signature tail, exactly one `bindparams()` call with `as_of` typed `Date`. `_clamp_attribution`/`_coerce_as_of` driven directly: empty/whitespace/non-string → `None`; over-cap truncated (with WARNING); `datetime` → `.date()`; string `as_of` → `None`. Legacy call site (`extract_and_persist_citations`, line 810) confirmed passing only `{tenant_id, run_id, claim_text, facet}` — unchanged. |
| `tests/test_claim_attribution.py` | migration chain, ORM/DDL agreement, `as_of` grammar, write-path proof | VERIFIED | Present, contains `_FakeSession` (line 723), the write-path tests (`test_the_claim_insert_names_all_three_new_columns`, `test_the_tenant_context_is_set_before_the_first_claim_insert`), and the full `_ACCEPTED`/`_REJECTED` grammar tables — independently executed, see above. |
| `cloudbuild.test-engine.yaml` | `EXPECTED_FILES=33`, WANTED list matches, gate submittable | VERIFIED | `EXPECTED_FILES=33` present; WANTED list has exactly 33 unique `nestor_pulse_sdk/tests/*.py` paths and **all 33 exist on disk**; exactly 2 apostrophes after `steps:`; script region 5150 chars (< 10,000 cap). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `research_division.py` | `steps.py` | `_enriched` underscore keys read by `collect_provider_facts` | WIRED | `result.get("_sub_question")`/`result.get("_corroboration_key")` at `steps.py:2409-2410`, fed to all 4 `_normalise_fact_claim` call sites. |
| `steps.py` | `claim_attribution.py` | `extract_as_of` over the EVIDENCE cell | WIRED | `steps.py:2037`: `out["as_of"] = extract_as_of(out["evidence"])`, called exactly once inside `_normalise_fact_claim`. |
| `alembic/0017` | `0016_source_resolved_url.py` | `down_revision` | WIRED | `down_revision: Union[str, None] = "0016"`; sole child of 0016 in the whole `versions/` directory. |
| `extractor.py` (`_insert_claim`) | `claim` table columns | `INSERT INTO claim (...)` column + VALUES lists | WIRED | Both lists name `sub_question`, `corroboration_key`, `as_of`; parameter dict supplies all three (`extractor.py:446-472`). |
| `extractor.py` (`persist_tribunal_claims`) | `_insert_claim` | keyword args off the claim dict | WIRED | AST-verified: single call site, 3 kwargs each `claim.get(...)`, no re-derivation. |

### Data-Flow Trace (Level 4)

Not applicable in the strict sense — this phase deliberately adds **write-only** columns with no
report or dispatch-decision reader (invariant 3). The relevant "does data actually flow" question
is answered by driving `extract_as_of` and `_dedupe_claims` directly against realistic inputs
(above), which confirms real values — not stubs — reach the INSERT statement.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `extract_as_of` grammar matches D-W2-4's worked table exactly | driven via `runpy.run_path` + assertions on 16 cases | all 16 correct | PASS |
| Committed `_ACCEPTED`/`_REJECTED` test lists hold against the real function | AST-lifted and executed | 20/20 accept, 20/20 reject | PASS |
| `_dedupe_claims` first-wins (D-W2-3) | AST-lifted, executed against 4 scenarios | all 4 correct (first-wins on both fields, `None`-first survives, distiller-shape gains no key) | PASS |
| `_clamp_attribution`/`_coerce_as_of` boundary coercion | AST-lifted, executed against 11 cases | all correct incl. truncation and `datetime→date` narrowing | PASS |
| `_insert_claim`/`persist_tribunal_claims` signatures and call-site wiring | AST parsed and asserted | matches plan's own verify commands exactly | PASS |
| Engine gate config integrity (apostrophe count, char cap, file count) | driven via script | 2 apostrophes, 5150 chars, 33/33 files on disk | PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files apply to this phase (tribunal engine test files are
collected via `gcloud builds submit`, not shell probes). Skipped — not applicable to this phase's
verification surface, per the phase's own stated policy: no live engine run is possible or
claimed in wave 2.

### Requirements Coverage

`ROADMAP.md` § Phase 15.5 states `**Requirements**: none`. No requirement IDs to cross-reference.

### Anti-Patterns Found

None. Scanned all 7 production/config files touched by this phase for `TBD`/`FIXME`/`XXX`/`TODO`/
`HACK`/placeholder language — zero hits in any of `claim.py`, `0017_claim_attribution.py`,
`claim_attribution.py`, `research_division.py`, `extractor.py`, `cloudbuild.test-engine.yaml`.
`steps.py` has several "placeholder" hits, but all are pre-existing Wave-1 prose describing the
`<TAB>` separator-placeholder parsing mechanism, unrelated to this phase's additions.

### Human Verification Required

None. Every must-have in this phase is a schema/code-threading change with no UI, no live-run
behavior, and no external service to observe — everything checkable was checked by direct
execution or AST assertion on this machine, and the two items this phase cannot resolve (the
alembic proof line, the pytest collection run) are explicitly deferred to phase 15.8 by design,
not left uncertain.

### Gaps Summary

No gaps. This phase's three plans plus the D-W2-4 follow-up commit deliver exactly what the
CONTEXT and ROADMAP specify: the three nullable columns exist in the ORM and in an additive,
single-head alembic revision; every value threaded through the pipeline uses matching key names at
every hop, verified by direct execution rather than by trusting the SUMMARYs; the scope fences
(no deploy, distiller byte-identity, no behaviour change, verification stage untouched, `_dedupe_claims`
body untouched, NULL-not-empty-string) all hold under independent inspection; the D-W2-4 month-precision
fix is correctly implemented and matches its own worked table exactly; the length-cap handoff between
plans 02 and 03 landed exactly once, at the DB boundary; and every genuinely pending item (the alembic
`Running upgrade 0016 -> 0017` proof, both Cloud Build pytest runs) is stated as PENDING/OWED in the
SUMMARYs and in `STATE.md`'s rollup — never claimed as done.

One item worth flagging for the record, not as a gap: `steps.py:2569-2572` (the D-R2 corrective-re-ask
call site) is a **fourth** `_normalise_fact_claim` call site beyond the three named in the CONTEXT's
canonical references (`:2336`, `:2397`, `:2511` in the pre-phase line numbering). Plan 15.5-02's
executor found this itself during implementation (documented as Deviation 1 in its SUMMARY) and
threaded the same angle values through it with a comment explaining why. Verified directly: all 4
call sites pass `sub_question=sub_question, corroboration_key=corroboration_key` (or explicit `None`
on the distiller-fallback branch) consistently. This is a correct completion of the phase's intent,
not a deviation from it — flagging it here only because the task brief named 4 call sites and the
CONTEXT's own reference section named 3.

---

*Verified: 2026-07-29*
*Verifier: Claude (gsd-verifier)*
