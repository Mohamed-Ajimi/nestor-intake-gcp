---
phase: 15.5-research-engine-redesign-claim-attribution-wave-2
plan: 02
type: execute
wave: 2
depends_on: ["15.5-01"]
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py
  - tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py
  - tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py
  - tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py
  - tribunal/nestor_pulse_sdk/tests/test_factlist_fallback.py
  - tribunal/nestor_pulse_sdk/tests/test_claim_distiller.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "An angle's `sub_question` and `corroboration_key` survive from dispatch onto the enriched result dict, and a restored checkpoint angle stays byte-identical in every other respect."
    - "A claim produced from a provider fact list carries the sub-question it was dispatched under and the corroboration key of its group; a claim with no group carries NULL, never the empty string."
    - "A claim carries its own `as_of` date where the EVIDENCE cell stated an unambiguous one."
    - "A cross-stream merge keeps the FIRST occurrence's sub_question and corroboration_key, and the `claim_distiller` path through `_dedupe_claims` is byte-identical to before."
    - "A winner's parent label IS the facet already stamped — invariant 2 is a no-op in this wave, asserted rather than assumed."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py"
      provides: "_sub_question / _corroboration_key on _enriched"
      contains: "_corroboration_key"
    - path: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py"
      provides: "the three keys on every claim dict"
      contains: "sub_question"
    - path: "tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py"
      provides: "resolved_facet + parent_index — the seam phase 15.6 consumes"
      exports: ["extract_as_of", "resolved_facet", "parent_index"]
  key_links:
    - from: "tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py"
      via: "_enriched underscore keys read by collect_provider_facts"
      pattern: "_sub_question"
    - from: "tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py"
      to: "tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py"
      via: "extract_as_of over the EVIDENCE cell"
      pattern: "extract_as_of"
---

<objective>
Thread the two values that already exist at dispatch onto the claim dict, and attach the date.

Every value this plan persists is **already present in memory** and simply stops short of the claim
row. This should read as plumbing, not invention.

Purpose: `facet` is today the parent client question, inherited from the angle. That inheritance is
correct only while one angle maps to exactly one client question. Phase 15.6 makes an LLM group
winners into <=5 groups sent to all providers, and the moment a group spans two client questions a
claim from it has no single parent. This plan records the sub-question so 15.6 has a parent to
resolve against.

Output: two new keys on `_enriched`, three new keys guaranteed on every claim dict, the `as_of`
call site, the `resolved_facet` seam, and tests in three already-registered test files.

**NO BEHAVIOUR CHANGE BEYOND RECORDING.** Corroboration still uses today's key. No dispatch
decision, no merge outcome, no report sentence changes. A diff that alters which claims reach paid
verification is OUT OF CONTRACT — that is the one variable the 15.8 measuring run must hold still.

**NO DEPLOY.** Waves 1-4 land in git only. Do not run `gcloud run deploy`, do not build an image,
do not apply a migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.5-research-engine-redesign-claim-attribution-wave-2/15.5-CONTEXT.md
@.planning/CONTINUE-HERE.md

Worktree hygiene, before any edit:
- Your worktree may be cut from a STALE ref. In phase 15.1, 8 of 10 executor worktrees were 62-110
  commits behind. Check before you start.
- **Per-dependency assertions — plan 15.5-01 must already be merged into your base.** Assert all
  three before editing anything, and STOP if any fails:
  1. `tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py` exists and defines
     `extract_as_of`.
  2. `tribunal/nestor_pulse_sdk/db/models/claim.py` contains `sub_question`, `corroboration_key`
     and `as_of`.
  3. `tribunal/nestor_pulse_sdk/alembic/versions/0017_claim_attribution.py` exists.
- Do NOT edit `tribunal/cloudbuild.test-engine.yaml`. Plan 15.5-01 owns it and has already bumped
  `EXPECTED_FILES` to 33. Every test file this plan touches is ALREADY registered — two in the
  engine gate, one in the gates build (see task acceptance criteria for which is which). If you
  find yourself wanting to add a new test file, you have gone outside this plan.
- `.planning/` is gitignored — `git add -f`.
- Do NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md`.

<interfaces>
The exact code you are threading through. Verified line numbers, current as of this planning pass.

**`research_division.py:836-861` — `_angle()`** already builds every angle dict with
`"sub_question": w["text"]` and `"corroboration_key": key`, alongside `focus_area`, `provider`,
`stakes`, `rank`, `langs`, `language` and `corroboration`. Nothing needs to be added there.

**`research_division.py:863-873` — the dispatch.** The top-`k` winners are dealt to every stream
with `key = f"w{int(w['rank']):02d}"`; the remainder is dealt round-robin with the EMPTY STRING as
its key. So only the top 3 of ~15 winners have a corroboration key today.

**`research_division.py:1411-1412`** — `fa = angle.get("focus_area", "")` and
`stakes = angle.get("stakes", "med")`, the values later folded into `_enriched`.

**`research_division.py:1514-1516` — `_enriched`** is
`{**result, "_angle": fa, "_stakes": stakes, "_d8_prompted": prompted}`. The comment at `:1525` is
explicit that this is ONE object on purpose: the checkpointed value and the returned value are the
same object so *"a restored angle is byte-identical to a freshly researched one."*

**`research_division.py:1338` and `:1440`** read angles as
`angle.get('sub_question') or angle.get('focus_area') or ''` — because the focus-area division path
produces angles with **no `sub_question` key at all**. Both are human-readable feed lines. Use
`.get()`, never a subscript.

**`checkpoints.py:92 — angles_digest`** derives its fingerprint from each angle's `query` ALONE.
Adding keys to the RESULT dict cannot move it, so no checkpoint is invalidated and no paid angle is
re-bought.

**`synthesis/steps.py:2312`** — `facet = str(result.get("_angle") or "") or "general"`, inside
`collect_provider_facts`. This is the precedent for reading an underscore key off the result dict.

**`synthesis/steps.py:1810-1813` — `_FACT_CLAIM_KEYS`**, currently the nine keys
`text, facet, evidence, found_by, source_urls, certainty, provider_quality, source_domain,
quality_tier_hint`.

**`synthesis/steps.py:1895-1959` — `_normalise_fact_claim(claim, *, provider, facet, fact_source)`**
is the ONE funnel both claim paths pass through. It ends with a `for key in _FACT_CLAIM_KEYS:
out.setdefault(key, None)` loop that GUARANTEES the key set.

**Its three call sites in `collect_provider_facts`:**
- `:2336` — the forced-tool (own-researcher) pre-parsed branch; `result` IS an angle result.
- `:2397` — the fact_list branch; `result` IS an angle result.
- `:2511` — the `distiller_fallback` branch. `claim_distiller` builds its units as
  `(provider_name, chunk_text)` (`steps.py:1624-1630`), so the angle is GONE before distillation and
  no per-claim dispatch attribution exists on this branch. That is a fact about the code, not a gap
  to close.

**`synthesis/steps.py:1416-1525` — `_dedupe_claims`**, the cross-stream merge point. It keeps the
first occurrence (`seen[norm] = c`) and mutates only named fields on `kept`: `found_by` unions,
`provider_quality_by_url` is first-to-introduce-owns-it, `source_urls` union, `certainty` takes the
cautious value. Every branch is guarded on the key being present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry the two dispatch values onto _enriched, and add the resolved_facet seam</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py, tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py, tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` lines 830-880 (`_angle` and the dispatch) and lines 1281-1530 (`_one_angle`, the restore path, the defensive reads, and `_enriched`)
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/checkpoints.py` lines 87-116 — `angles_digest`, to confirm for yourself that it reads `query` only
    - `tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py` lines 200-300 — the `_winner(text, rank, parent, langs)` / `_wbrief(*labels)` helpers and `test_d6_focus_area_is_the_parent_label_never_the_winner_text`, which is half of the invariant you are completing
    - `tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py` — plan 15.5-01's module, which you are extending
  </read_first>
  <action>
**(a) `research_division.py`.** Next to `fa` and `stakes` at `:1411-1412`, read the two values off
the angle with the same tolerant `.get()` the feed lines use:

- `sub_q = angle.get("sub_question") or None`
- `corr_key = angle.get("corroboration_key") or None`

Then add them to the `_enriched` literal at `:1514` as `"_sub_question": sub_q` and
`"_corroboration_key": corr_key`, in the same underscore-prefixed style as `_angle` / `_stakes` /
`_d8_prompted`. **Add them to the SAME dict literal.** Do not build a second dict and do not
enrich after `_record_result`: the comment at `:1525` states that the checkpointed value and the
returned value are deliberately the same object, so a restored angle is byte-identical to a fresh
one.

TWO DELIBERATE DEVIATIONS from the feed-line idiom, and both must be stated in a comment beside the
new lines:

1. `or None`, NOT `or ''`. The feed lines end in `or ''` because an empty label in a log line is
   fine. These two values become DATABASE COLUMNS, and D-W2-2 is explicit: an absent key is written
   as NULL, never as the empty string. "No key recorded" and "recorded as the empty key" are
   different facts and the corroboration queries must be able to tell them apart — the same rule
   `_insert_claim`'s docstring already records for `found_by`.
2. **No `or angle.get("focus_area")` fallback for `sub_question`.** The feed lines fall back to
   `focus_area` so a human sees a label instead of a blank. Doing that here would write the PARENT
   question into a column whose whole purpose is to be distinguishable from the parent, and `facet`
   already carries `focus_area`. The focus-area division path (`winners` falsy) produces angles with
   no `sub_question` key at all; those claims get NULL, correctly.

Do not change `_angle()`. Do not change the dispatch loops at `:863-873`. Do not populate the empty
corroboration key: it is READ for dispatch decisions at `research_division.py:684`, `:714`, `:1313`
and `pipeline/tribunal/pipeline.py:3920` (`_group_size`), and changing the empty string to a real
key would silently change reassignment behaviour and group sizes.

**(b) `claim_attribution.py` — the seam 15.6 consumes.** Add two pure functions to plan 15.5-01's
module, keeping it stdlib-only:

- `def parent_index(winners) -> dict[str, str]` — builds `{winner_text: parent_label}` from a
  workshop winners list. Tolerant: a non-dict entry, a missing `text` or a missing `parent` is
  skipped, never raised on.
- `def resolved_facet(claim: dict, parents: Mapping[str, str]) -> str | None` — returns the parent
  of the claim's `sub_question` when that sub-question is in the index, otherwise the claim's own
  `facet`. `.get()` throughout; never raises.

Both must carry a docstring saying, in words: **these are deliberately NOT CALLED by the pipeline in
this wave.** In wave 2, invariant 2 is a NO-OP BY CONSTRUCTION — a winner's parent label IS the
`focus_area` already stamped onto the claim as `facet`, so `resolved_facet` resolves to today's
value for every claim. Wiring it in would change nothing and would add a call path for zero gain,
which invariant 3 forbids. It exists so phase 15.6 has a named seam, and its consumer today is the
test in part (c).

**(c) Tests in `test_research_division_assignment.py`** (already in the ENGINE gate — do not touch
the gate config). Add, using the existing `_winner` / `_wbrief` helpers:

- the two `_enriched` keys are present and correct for a top-3 corroboration angle (key `w01`) and
  are `None` for a remainder angle whose dispatch key is the empty string. Assert `is None`, not
  falsiness — the empty string is falsy too and this test exists to tell them apart;
- adding the keys does not move `angles_digest` for the same angle list;
- **the invariant-2 no-op, asserted rather than assumed:** drive real `rd.divide(brief,
  winners=...)` with winners whose `parent` values are known labels, build `parent_index` from those
  same winners, and assert that for every angle `parents[angle["sub_question"]] == angle["focus_area"]`.
  Name it so the intent survives, e.g.
  `test_invariant_2_is_a_no_op_in_wave_2_the_winners_parent_is_already_the_facet`, and put a comment
  on it saying this is exactly what phase 15.6 breaks and therefore exactly what 15.6 must re-prove.
  Include the orphan-parent case the existing suite already exercises (a winner whose parent is not
  a known label falls back to `labels[0]`), and assert the resolver degrades to `claim["facet"]`
  there rather than inventing a parent.
  </action>
  <verify>
    <automated>C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -m py_compile tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py</automated>
  </verify>
  <acceptance_criteria>
    - `research_division.py` contains `"_sub_question"` and `"_corroboration_key"` inside the SAME dict literal as `"_angle"`, `"_stakes"` and `"_d8_prompted"` — verify by reading the `_enriched` assignment, not by grepping the file (a grep passes on a comment)
    - `research_division.py` contains exactly one assignment to `_enriched` in the whole file
    - The new reads use `angle.get(...) or None` and NOT `or ""`, and the `sub_question` read has NO `focus_area` fallback
    - `_angle()` and the dispatch loops at lines ~863-873 are byte-unchanged (`git diff` shows no hunk in that range)
    - `claim_attribution.py` exports `parent_index` and `resolved_facet`, both pure, both never raising, and its imports are still only `re`, `datetime`, `logging` plus typing
    - `resolved_facet` and `parent_index` are referenced by the test file and by NO production module (`grep -rn "resolved_facet" tribunal/nestor_pulse_sdk --include=*.py` names only `claim_attribution.py` and the test file)
    - `test_research_division_assignment.py` contains a test whose name contains `invariant_2` and which calls `rd.divide(..., winners=...)`
    - The remainder-angle test asserts `is None`, not merely falsiness
    - `py_compile` exits 0 for all three files
    - Test execution status: **PENDING-CLOUDBUILD** — this box has no pytest. Record it as PENDING; the resolving build is `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml`
  </acceptance_criteria>
  <done>The dispatch's two values ride on the one enriched object, absent means None, no checkpoint moved, and invariant 2's no-op is proven against real `divide()` output instead of assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Guarantee the three keys on every claim dict in collect_provider_facts</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py, tribunal/nestor_pulse_sdk/tests/test_factlist_fallback.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py` lines 1795-1960 (`_FACT_CLAIM_KEYS` and `_normalise_fact_claim` with its T-15.2-61 docstring) and lines 2300-2540 (`collect_provider_facts` — all three `_normalise_fact_claim` call sites and the final `_dedupe_claims`)
    - `tribunal/nestor_pulse_sdk/tests/test_factlist_fallback.py` lines 250-300 — the `_entry(provider, *, report, angle, prompted, **extra)` helper (its `**extra` is how you put the new underscore keys on a result dict) and the `_collect` wrapper
    - `tribunal/nestor_pulse_sdk/pipeline/synthesis/claim_attribution.py` — `extract_as_of`, which you are calling
  </read_first>
  <action>
**(a) `_FACT_CLAIM_KEYS`.** Extend the tuple from nine to TWELVE keys by appending
`"sub_question"`, `"corroboration_key"`, `"as_of"`. Update its comment: it currently reads *"The
nine keys 15.2-04 guarantees on every fact dict, in its order"* — it is no longer nine and no longer
only 15.2-04's, and a stale count in the constant that exists to guarantee a key set is precisely
the class of quietly-wrong number this phase family was opened to repair. Say what the three new
ones are and that phase 15.5 added them.

**(b) `_normalise_fact_claim`.** Add two keyword-only parameters, both defaulted to `None`, in the
`certainty` / `found_by` register so existing call sites stay valid:
`sub_question: str | None = None`, `corroboration_key: str | None = None`. Write them onto `out`
before the `setdefault` loop, coercing an empty or non-string value to `None` (absent means NULL —
never the empty string).

Then set `out["as_of"]` by calling `extract_as_of` on the SAME evidence string the function already
computes for `out["evidence"]`. Import `extract_as_of` at module scope from
`nestor_pulse_sdk.pipeline.synthesis.claim_attribution` — it is stdlib-pure, so unlike the
`redirect_resolver` import it adds no heavy dependency to this module's import graph.

`as_of` is set on BOTH branches, including `distiller_fallback`. Add a comment saying why this does
not contradict T-15.2-61, which hard-writes `certainty` / `provider_quality` / `source_domain` /
`quality_tier_hint` to `None` on that branch as a SECURITY CONTROL: those four are free-text
provider claims about their own confidence, and a web page saying "certainty: certain" is an
indirect prompt injection aimed at a queryable column. `as_of` is decision D-W2-1's single, bounded
exception — it passes through a strict grammar that rejects every ambiguous form, yields a
`datetime.date` or `None` and can express nothing else. The residual risk is a provider stating a
well-formed but untrue date, which is the same trust level as the claim text itself.

**(c) The three call sites in `collect_provider_facts`.** Beside the existing
`facet = str(result.get("_angle") or "") or "general"` at `:2312`, read the two new underscore keys
off the same result dict with `.get()` — a run restored from a checkpoint written before this
change has neither key, so a subscript would crash a resumed run. Pass them at:

- the forced-tool pre-parsed branch (`:2336`) — the angle values;
- the fact_list branch (`:2397`) — the angle values;
- the `distiller_fallback` branch (`:2511`) — **explicitly `None` for both, with a comment saying
  why**: `claim_distiller` builds its units as `(provider_name, chunk_text)` at `:1624-1630`, so the
  angle is gone before distillation and there is no per-claim dispatch attribution to carry. Passing
  the loop variable of some other report would be a fabricated attribution. This is a fact about the
  code, not a gap; it is why the columns are nullable.

**(d) `_dedupe_claims` — ADD NO BRANCH.** D-W2-3 is first-wins for both fields, and first-wins is
what this function ALREADY does: it keeps `seen[norm] = c` and mutates only the named fields it
unions or takes the cautious value of. Writing a new branch to implement first-wins would be
writing code to preserve behaviour that needs no code, and every line added to that function is a
line that can perturb the `claim_distiller` path. Add ONLY a sentence to its docstring, in the same
per-field register as the existing four bullets, recording that `sub_question`, `corroboration_key`
and `as_of` are first-wins BY CONSTRUCTION — the first occurrence's dict survives whole — and
recording the two accepted consequences: a claim merged across two corroboration groups is silently
attributed to the first (the cross-key-merge warning was explicitly declined for this wave), and a
first occurrence carrying `None` keeps its `None` even when a later duplicate carries a real value.
Note in the same sentence why that second consequence is bounded: `collect_provider_facts` calls
`_dedupe_claims(d8_claims + fallback_claims)` with the attributed fact-list claims FIRST, so the
unattributed distiller paraphrase is the duplicate, not the survivor.

**(e) Tests in `test_factlist_fallback.py`** (already in the ENGINE gate — do not touch the gate
config). Using `_entry(..., _sub_question="...", _corroboration_key="w01")` via its `**extra`:

- a fact_list claim carries the dispatched sub-question and key;
- an entry with NO `_sub_question` / `_corroboration_key` keys at all (the pre-15.5 checkpoint
  shape) produces claims with `sub_question is None` and `corroboration_key is None` and does not
  raise — assert `is None`;
- an entry whose `_corroboration_key` is the empty string produces `corroboration_key is None`
  (D-W2-2: absent means NULL, never the empty string);
- a distiller-fallback claim has `sub_question is None` and `corroboration_key is None`, named so it
  reads as the recorded, intended consequence it is;
- every claim dict has all twelve `_FACT_CLAIM_KEYS`, asserted against the constant itself rather
  than a hand-copied list;
- a claim whose evidence states `2021-03-04` carries `as_of == date(2021, 3, 4)`, and one whose
  evidence states `03/04/2021` carries `as_of is None`.
  </action>
  <verify>
    <automated>C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -m py_compile tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py tribunal/nestor_pulse_sdk/tests/test_factlist_fallback.py && C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -c "import ast; t=ast.parse(open('tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py',encoding='utf-8').read()); fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='_normalise_fact_claim'][0]; kw=[a.arg for a in fn.args.kwonlyargs]; assert 'sub_question' in kw and 'corroboration_key' in kw, kw; src=ast.unparse(fn); assert 'extract_as_of' in src; d=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='_dedupe_claims'][0]; print('normalise kwonly', kw); print('dedupe body stmts', len(d.body))"</automated>
  </verify>
  <acceptance_criteria>
    - `_FACT_CLAIM_KEYS` has exactly twelve entries ending `"sub_question", "corroboration_key", "as_of"` and its comment no longer says "nine"
    - `_normalise_fact_claim` has keyword-only params `sub_question` and `corroboration_key`, both defaulting to `None` (asserted by the AST check in the verify command)
    - `_normalise_fact_claim` calls `extract_as_of` exactly once, on the evidence string
    - `steps.py` imports `extract_as_of` at module scope from `nestor_pulse_sdk.pipeline.synthesis.claim_attribution`
    - All three `_normalise_fact_claim` call sites pass the two new kwargs; the `distiller_fallback` site passes literal `None` for both and carries the comment explaining that the angle is gone by construction
    - **`_dedupe_claims`'s executable body is UNCHANGED.** Prove it: `git diff` on the function shows only docstring lines. `ast.unparse` of `_dedupe_claims` before and after the change is identical apart from the docstring constant
    - `test_factlist_fallback.py` asserts `is None` (not falsiness) for every absent-attribution case
    - `test_factlist_fallback.py` asserts the key set against `_FACT_CLAIM_KEYS` itself, not a hand-copied list
    - `_build_distiller_prompt` and `_parse_distiller_response` are byte-unchanged (`git diff` shows no hunk in `steps.py` lines ~1136-1413 other than in `_dedupe_claims`'s docstring)
    - `py_compile` exits 0; the AST check prints the kwonly names
    - Test execution status: **PENDING-CLOUDBUILD** (engine gate)
  </acceptance_criteria>
  <done>Every claim dict leaving `collect_provider_facts` carries all three attribution keys with honest values, the distiller path's structural blindness is recorded rather than papered over, and `_dedupe_claims` gained a sentence and not a branch.</done>
</task>

<task type="auto">
  <name>Task 3: Pin first-wins and the byte-identical distiller path in test_claim_distiller.py</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_claim_distiller.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/test_claim_distiller.py` lines 500-600 — `class TestDedupeMerges`, the `_claim(...)` helper, `MIXED_CLAIMS`, and `_old_dedupe` (the verbatim pre-15.1 implementation the suite still compares against)
    - `tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py` lines 1416-1525 — `_dedupe_claims` as you left it in task 2
  </read_first>
  <action>
Add tests to `TestDedupeMerges` (or a sibling class in the same file) pinning D-W2-3. **This file is
collected by `tribunal/cloudbuild.test-gates.yaml`, NOT the engine gate** — a full proof of this
phase runs BOTH builds. It is already registered in that config; do not edit it.

Assert:

- two duplicate claims with DIFFERENT `sub_question` values merge to one claim carrying the FIRST
  one's value, and likewise for `corroboration_key`. Name the test so the accepted information loss
  is visible in the name — a claim merged across two corroboration groups is silently attributed to
  the first, and the warning about it was explicitly DECLINED for this wave;
- a first occurrence with `sub_question: None` and a later duplicate with a real value merges to
  `None`. This is first-wins working as specified, not a bug. Put the mitigation in the test's
  docstring: `collect_provider_facts` orders `d8_claims + fallback_claims`, so the attributed claim
  is the survivor in the real pipeline;
- **the claim_distiller path is byte-identical.** Claims in that path carry no `sub_question`, no
  `corroboration_key` and no `as_of` key at all — `_dedupe_claims` is called on raw distiller claims
  at `steps.py:1765` before `_normalise_fact_claim` has ever run. Assert that a list of
  distiller-shaped claims (`text` / `facet` / `evidence` / `found_by` only) comes out of
  `_dedupe_claims` with NO new key invented on any survivor, and that the existing
  `test_dedupe_merges_preserving_count` comparison against `_old_dedupe` still holds — the surviving
  count, order and texts must be unchanged.

Do not modify `_old_dedupe`, `MIXED_CLAIMS` or any existing test. If an existing test goes red, the
production change in task 2 was not additive and must be fixed there, not accommodated here.
  </action>
  <verify>
    <automated>C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -m py_compile tribunal/nestor_pulse_sdk/tests/test_claim_distiller.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_claim_distiller.py` contains a test asserting first-wins for `sub_question` across a merge and one asserting it for `corroboration_key`
    - A test asserts that a `None` first occurrence survives a later real value, with the ordering mitigation stated in its docstring
    - A test asserts distiller-shaped claims gain NO new keys through `_dedupe_claims`
    - `_old_dedupe`, `MIXED_CLAIMS` and every pre-existing test in the file are byte-unchanged (`git diff` shows additions only)
    - `py_compile` exits 0
    - Test execution status: **PENDING-CLOUDBUILD**, and the resolving build for THIS file is `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml` — the engine gate does not collect it. Say which build in the SUMMARY
  </acceptance_criteria>
  <done>First-wins is pinned by name, the accepted information loss is named in a test rather than discovered later, and the distiller path's byte-identity has an assertion instead of an assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch assignment -> claim dict | engine-authored values; must never be readable from model output |
| provider prose (EVIDENCE) -> `as_of` | the single sanctioned model-derived column, bounded by D-W2-1's grammar |
| pre-15.5 checkpoint -> resumed run | a restored result dict lacks the new keys entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.5-05 | Spoofing | `corroboration_key` / `sub_question` | mitigate | stamped in Python from the angle dict; never read from report text. Same rule `_parse_distiller_response` applies to `provider` and `enforce_scope_guard` applies to `parent`. A model-supplied key would let model text choose its own corroboration partner |
| T-15.5-06 | Tampering | `_dedupe_claims` | mitigate | NO executable change; docstring only. Byte-identity asserted by `ast.unparse` comparison and by the surviving `_old_dedupe` count/order test |
| T-15.5-07 | Denial of Service | resumed run on a pre-15.5 checkpoint | mitigate | every read of a new key uses `.get()`, never a subscript; a test drives an entry with neither key present |
| T-15.5-08 | Tampering | `_enriched` object identity | mitigate | keys added to the ONE dict literal; `angles_digest` reads `query` only, asserted by test, so no paid angle is re-bought on resume |
| T-15.5-09 | Repudiation | distiller-fallback attribution | accept | the angle is gone by construction (units are `(provider, chunk)`), so those claims carry NULL. Recorded in a comment and a named test rather than fabricated |
| T-15.5-SC | Tampering | package installs | n/a | no package installed, no `requirements.txt` change; nothing for the legitimacy gate to audit |
</threat_model>

<verification>
1. `py_compile` clean on all six files (bundled gcloud interpreter, 3.14.4).
2. The AST check in task 2 prints `_normalise_fact_claim`'s keyword-only names — a source assertion,
   not a grep, because a grep passes on a comment that merely mentions a column name.
3. `git diff` proves the three no-change claims: `_angle()` and the dispatch loops,
   `_build_distiller_prompt` / `_parse_distiller_response`, and `_dedupe_claims`'s executable body.
4. **NOT verified here:** every pytest assertion. Record PENDING-CLOUDBUILD. TWO builds resolve this
   plan — `cloudbuild.test-engine.yaml` for tasks 1 and 2, `cloudbuild.test-gates.yaml` for task 3.
   Reconcile the engine gate's printed `collecting: 33 of 33 expected files`; wave 1's numbers to
   beat are engine 1030 passed / 32 of 32 files and gates 182 passed.
5. **NOT verified here and must not be claimed:** that any of this ran against a live engine. This
   wave writes columns no report reads yet; judging it from a delivered report is impossible and
   inventing a report change to make the wave feel finished is out of contract.
</verification>

<success_criteria>
- `_enriched` carries `_sub_question` and `_corroboration_key`, in the one object, absent as `None`.
- Every claim dict leaving `collect_provider_facts` has twelve guaranteed keys including the three
  new ones.
- `as_of` is populated from the EVIDENCE cell through the bounded grammar, on both claim paths.
- `_dedupe_claims`'s executable body is unchanged and proven so.
- Invariant 2's no-op is asserted against real `divide()` output.
- No dispatch decision, merge outcome or report sentence changed. No deploy.
</success_criteria>

<output>
Create `.planning/phases/15.5-research-engine-redesign-claim-attribution-wave-2/15.5-02-SUMMARY.md`
when done (`git add -f`). Name which Cloud Build config resolves which task's PENDING items.
</output>
