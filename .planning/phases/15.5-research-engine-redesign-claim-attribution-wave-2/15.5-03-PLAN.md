---
phase: 15.5-research-engine-redesign-claim-attribution-wave-2
plan: 03
type: execute
wave: 2
depends_on: ["15.5-01"]
files_modified:
  - tribunal/nestor_pulse_sdk/citations/extractor.py
  - tribunal/nestor_pulse_sdk/tests/test_claim_attribution.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "The claim INSERT names the three new columns and binds them explicitly typed, so no driver has to infer a date type inside a paid run."
    - "An absent sub-question or corroboration key reaches the database as NULL, never as the empty string."
    - "An over-long or non-string attribution value is clamped at the boundary rather than written unbounded."
    - "Every pre-existing `_insert_claim` call site stays valid unchanged."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/citations/extractor.py"
      provides: "_insert_claim writes the three columns; persist_tribunal_claims supplies them"
      contains: "corroboration_key"
    - path: "tribunal/nestor_pulse_sdk/tests/test_claim_attribution.py"
      provides: "fake-session proof of the INSERT shape and the NULL/clamp rules"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/citations/extractor.py"
      to: "claim table columns added by alembic 0017"
      via: "INSERT INTO claim column list"
      pattern: "INSERT INTO claim"
---

<objective>
Write the three values to the row, at the one boundary that is allowed to decide what reaches the
database.

`_insert_claim` is the template for this whole wave. Its docstring already states the three binding
rules for additive nullable columns — explicit `bindparam` typing, absent-means-NULL, and clamping
at the boundary because *"this function is the last thing between untrusted model wording and the
database."* This plan copies that shape three more times.

Output: three keyword-only parameters on `_insert_claim`, the call site in `persist_tribunal_claims`
that supplies them, and pure fake-session tests in the phase's own test file.

**NO DEPLOY, and no database.** Waves 1-4 land in git only; ONE deploy and ONE measuring run happen
at the end of phase 15.8. The alembic proof (`Running upgrade 0016 -> 0017`) is OWED THERE and must
not be claimed here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.5-research-engine-redesign-claim-attribution-wave-2/15.5-CONTEXT.md
@.planning/CONTINUE-HERE.md

Worktree hygiene, before any edit:
- Your worktree may be cut from a STALE ref. In phase 15.1, 8 of 10 executor worktrees were 62-110
  commits behind. Check before you start.
- **Per-dependency assertions — plan 15.5-01 must already be merged into your base.** Assert both
  before editing, and STOP if either fails:
  1. `tribunal/nestor_pulse_sdk/tests/test_claim_attribution.py` exists (you are EXTENDING plan
     15.5-01's file, not creating it).
  2. `tribunal/nestor_pulse_sdk/db/models/claim.py` contains `sub_question`, `corroboration_key`
     and `as_of`.
- Do NOT edit `tribunal/cloudbuild.test-engine.yaml`. Plan 15.5-01 already registered
  `test_claim_attribution.py` and set `EXPECTED_FILES=33`. You add tests to a file that is already
  collected, so the count does not move.
- **You are running in parallel with plan 15.5-02.** Do NOT touch
  `pipeline/synthesis/steps.py`, `pipeline/tribunal/research_division.py` or
  `pipeline/synthesis/claim_attribution.py` — 15.5-02 owns all three. Your contract with it is the
  claim dict's key names (`sub_question`, `corroboration_key`, `as_of`) and nothing else.
- `.planning/` is gitignored — `git add -f`.
- Do NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md`.

<interfaces>
The exact shape to copy — it is in the file you are editing.

**`citations/extractor.py:279-358` — `_insert_claim`.** Signature today:
`async def _insert_claim(session, *, tenant_id, run_id, claim_text, facet, position=None,
certainty=None, found_by=None) -> uuid.UUID`. `certainty` and `found_by` are keyword-only and
default to `None` **so every pre-existing call site stays valid unchanged** — that property is
stated in its docstring and is the property you must preserve.

Its three binding rules, verbatim from that docstring:
- `found_by` is bound through an EXPLICIT `sqlalchemy.bindparam(..., type_=postgresql.ARRAY(Text))`
  rather than left for the driver to infer, because pg8000 and asyncpg disagree about that
  inference and the failure mode is a runtime error inside a PAID run rather than a test failure.
- An ABSENT provenance is bound as `None`, never as `[]`, so the column reads NULL — *"no
  provenance recorded" and "recorded as found by nobody" are different facts and the corroboration
  queries must be able to tell them apart.*
- `certainty` is clamped HERE as well as at parse time, because *"this function is the last thing
  between untrusted model wording and the database."*

**`extractor.py:67, 76-77`** — the bound constants: `_CERTAINTY_VALUES = ("certain", "single")`,
`_MAX_FOUND_BY = 16`, `_FOUND_BY_MAX_CHARS = 64`.

**`extractor.py:104` — `_clamp_enum(value, allowed, *, field)`** — the existing clamp helper.

**`extractor.py:840-867` — the persistence loop** in `persist_tribunal_claims`:
`facet = claim.get("facet") or claim.get("focus_area") or ""`, then `_insert_claim(...,
certainty=claim.get("certainty"), found_by=claim.get("found_by"))`. `certainty` and `found_by`
arriving here off the claim dict is the standing PROOF that a claim dict survives unrebuilt from
`collect_provider_facts` through the gate, skeptic and adjudication stages to this line.

**`extractor.py:694` — the OTHER `_insert_claim` call site**, in the legacy
`extract_and_persist_citations` coarse path. It passes only `claim_text` and `facet` and MUST keep
working with no edit.

**`extractor.py:1026` — the dropped-claims loop.** Dropped claims get NO `claim` row (only verdicts
with `claim_id = NULL`), so there is nothing to attribute there.

**`tests/test_verdict_write_path.py`** — the fake-session pattern to copy into
`test_claim_attribution.py`: a hand-written `_FakeSession` that records every statement and opens no
connection, with needles `_CLAIM_INSERT = "INSERT INTO claim ("` (the trailing paren keeps claim
rows distinct from `claim_source` join rows) and `_TENANT_CONTEXT = "set_config"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give _insert_claim the three columns, typed and clamped</name>
  <files>tribunal/nestor_pulse_sdk/citations/extractor.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/citations/extractor.py` lines 50-120 (the bound constants and `_clamp_enum`) and lines 279-360 (`_insert_claim` in full, docstring included — it is the template for this entire wave)
    - `tribunal/nestor_pulse_sdk/db/models/claim.py` — the column types plan 15.5-01 declared, so the bind types match the DDL
  </read_first>
  <action>
Add three keyword-only parameters to `_insert_claim`, each defaulting to `None`, placed after
`found_by`: `sub_question: Optional[str] = None`, `corroboration_key: Optional[str] = None`,
`as_of: Optional[date] = None`. Import `date` from `datetime` at module scope if it is not already
imported. Defaults are not cosmetic — they are what keeps the legacy call site at `:694` valid with
no edit.

Add two module-level cap constants beside `_MAX_FOUND_BY` / `_FOUND_BY_MAX_CHARS`, in the same
naming register: `_SUB_QUESTION_MAX_CHARS = 500` and `_CORROBORATION_KEY_MAX_CHARS = 32`. A
corroboration key is `wNN` today, three characters; a sub-question is one workshop winner's text.
Both caps are generous on purpose — they are a BUG BOUND, not an injection control, because unlike
`certainty` these values are caller-supplied and never model-supplied. Say exactly that in a
comment, mirroring the sentence `_insert_claim`'s docstring already carries for `found_by`, and say
the other half too: the column must not be a place a bug can write unbounded data.

Coercion rules, applied in the function body before the INSERT:
- `sub_question` / `corroboration_key`: a non-string, an empty string, or a whitespace-only string
  becomes `None`. A string longer than its cap is truncated to the cap and the truncation is logged
  at WARNING with the field name and both lengths — a silent truncation is the class of loss this
  phase family exists to end. **`None`, never the empty string** — D-W2-2 and the `found_by`
  precedent: "no key recorded" and "recorded as the empty key" are different facts, and roughly 12
  of 15 winners have no key today because `research_division.py:867-874` deals the remainder with
  the empty string.
- `as_of`: accept a `datetime.date`. A `datetime.datetime` is a `date` subclass — convert it with
  `.date()` rather than storing it, so the column can never receive a time. Anything else, including
  a string, becomes `None` with a WARNING. The parsing already happened upstream in
  `extract_as_of`; this boundary only refuses what it cannot vouch for.

Extend the INSERT statement's column list and VALUES list with `sub_question`, `corroboration_key`,
`as_of`, and extend the existing `.bindparams(...)` call — do not add a second one — with
`sqlalchemy.bindparam("as_of", type_=sqlalchemy.Date)`. The explicit type is the same rule
`found_by` follows and for the same reason: a driver inferring a type inside the final persistence
transaction of a ~$50 run fails as a runtime error, not as a test failure. `sub_question` and
`corroboration_key` are plain text and need no explicit bindparam.

Extend the docstring: name the three new columns, say they are phase 15.5 / D-R3 and ADDITIVE, and
state that all three default to `None` so every pre-existing call site stays valid unchanged — the
same sentence it already carries for `certainty` / `found_by`.

Do NOT change `_insert_claim`'s existing parameters, the `certainty` clamp, the `found_by` cap, or
the legacy call site at `:694`. Do NOT add anything to the dropped-claims loop at `:1026` — dropped
claims get no `claim` row.
  </action>
  <verify>
    <automated>C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -m py_compile tribunal/nestor_pulse_sdk/citations/extractor.py && C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -c "import ast; t=ast.parse(open('tribunal/nestor_pulse_sdk/citations/extractor.py',encoding='utf-8').read()); fn=[n for n in ast.walk(t) if isinstance(n,ast.AsyncFunctionDef) and n.name=='_insert_claim'][0]; kw=[a.arg for a in fn.args.kwonlyargs]; d=fn.args.kw_defaults; assert kw[-3:]==['sub_question','corroboration_key','as_of'], kw; assert all(x is not None and getattr(x,'value',False) is None for x in d[-3:]), 'the three new kwargs must default to None'; src=ast.unparse(fn); assert 'sub_question' in src and 'corroboration_key' in src and 'as_of' in src; assert src.count('bindparams')==1, 'exactly one bindparams call'; print('insert_claim kwonly', kw)"</automated>
  </verify>
  <acceptance_criteria>
    - `_insert_claim`'s keyword-only parameters end with `sub_question`, `corroboration_key`, `as_of`, all three defaulting to `None` (asserted by the AST check, not by grep)
    - `extractor.py` defines `_SUB_QUESTION_MAX_CHARS = 500` and `_CORROBORATION_KEY_MAX_CHARS = 32`
    - The INSERT statement text names `sub_question`, `corroboration_key` and `as_of` in both the column list and the VALUES list
    - There is exactly ONE `.bindparams(` call in `_insert_claim`, and it includes a bindparam for `as_of` typed `Date`
    - An empty-string `corroboration_key` is bound as `None` — verify in the function body, not the docstring
    - A `datetime.datetime` passed as `as_of` is converted with `.date()`; a string is rejected to `None` with a WARNING
    - Truncation of `sub_question` logs at WARNING with the field name and both lengths
    - The legacy call site at `extractor.py:694` is byte-unchanged (`git diff` shows no hunk there)
    - `py_compile` exits 0; the AST check prints the keyword-only names
  </acceptance_criteria>
  <done>The last thing between untrusted model wording and the database now knows about three more columns and treats each one the way it already treats `found_by` and `certainty`.</done>
</task>

<task type="auto">
  <name>Task 2: Supply the three values from the claim dict in persist_tribunal_claims</name>
  <files>tribunal/nestor_pulse_sdk/citations/extractor.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/citations/extractor.py` lines 723-830 (the `persist_tribunal_claims` docstring and its Args block, including the `resolved_urls` entry which is the register for documenting an additive parameter) and lines 836-900 (the persistence loop you are editing)
  </read_first>
  <action>
In the per-claim loop at `:840-867`, pass the three values through from the claim dict to
`_insert_claim` using `.get()`:
`sub_question=claim.get("sub_question")`, `corroboration_key=claim.get("corroboration_key")`,
`as_of=claim.get("as_of")`.

`.get()`, never a subscript: this function is also reached by the recorded-fixture loader and by
tests that build claim dicts by hand, and a claim dict that predates phase 15.5 has none of the
three keys. It must keep working and write NULLs.

Do NOT re-derive, re-parse or default any of the three here. A second place that decides what a
claim's attribution is, is a second place to get it wrong — the same reasoning
`_insert_research_gap`'s docstring records for `tenant_id`. `sub_question` and `corroboration_key`
were stamped in Python from the dispatch assignment in `collect_provider_facts`; `as_of` came
through `extract_as_of`'s grammar. Clamping and NULL-coercion belong to `_insert_claim`, where task
1 put them.

Extend the comment block above the `_insert_claim` call (the one that currently explains D-13's
`certainty` / `found_by`) with a paragraph in the same voice covering the three new values: where
they come from, that `corroboration_key` is NULL for roughly 12 of 15 winners today because only
the top-3 winners get a key and that this is correct and fills up in phase 15.6, and that the
`claim_distiller` fallback path carries no dispatch attribution at all by construction so those
claims are permanently NULL on the first two.

Do not add a new parameter to `persist_tribunal_claims` — the values ride on the claim dicts it
already receives, exactly as `certainty` and `found_by` do.
  </action>
  <verify>
    <automated>C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -c "import ast; t=ast.parse(open('tribunal/nestor_pulse_sdk/citations/extractor.py',encoding='utf-8').read()); fn=[n for n in ast.walk(t) if isinstance(n,ast.AsyncFunctionDef) and n.name=='persist_tribunal_claims'][0]; calls=[n for n in ast.walk(fn) if isinstance(n,ast.Call) and getattr(n.func,'id','')=='_insert_claim']; assert len(calls)==1, len(calls); kws={k.arg for k in calls[0].keywords}; assert {'sub_question','corroboration_key','as_of'} <= kws, kws; sigs=[ast.unparse(k.value) for k in calls[0].keywords if k.arg in ('sub_question','corroboration_key','as_of')]; assert all('.get(' in s for s in sigs), sigs; print('persist passes', sorted(kws))"</automated>
  </verify>
  <acceptance_criteria>
    - The single `_insert_claim` call inside `persist_tribunal_claims` passes `sub_question`, `corroboration_key` and `as_of`, each as a `claim.get(...)` expression (asserted by the AST check)
    - No `or ""` or other default is applied at this call site — absent stays absent and `_insert_claim` decides
    - `persist_tribunal_claims`'s signature is unchanged (no new parameter)
    - The comment above the call names the ~12-of-15 NULL expectation and the distiller path's structural absence
    - The dropped-claims loop at `:1026` is byte-unchanged
  </acceptance_criteria>
  <done>The three values reach the row from the claim dict the merge produced, with exactly one place deciding what a claim's attribution is.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the write path with a fake session, in the phase's own test file</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_claim_attribution.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/test_verdict_write_path.py` lines 1-140 — the `_FakeSession` / `_FakeResult` harness to copy, its `_CLAIM_INSERT = "INSERT INTO claim ("` needle, and how it calls `persist_tribunal_claims` with no database
    - `tribunal/nestor_pulse_sdk/tests/test_claim_attribution.py` — plan 15.5-01's file, which you are appending a section to; match its section-heading style and its "what this can and cannot prove" convention
    - `tribunal/nestor_pulse_sdk/citations/extractor.py` — `_insert_claim` and the persistence loop as you left them
  </read_first>
  <action>
Append a section 5 to `test_claim_attribution.py`, headed the way plan 15.5-01 headed its four, and
saying what it can and cannot prove. Copy the fake-session harness from `test_verdict_write_path.py`
into this file rather than importing it across test modules — that is what
`test_source_resolution.py` did for the same reason, and a shared fixture between two gate files is
a coupling neither config expresses.

The section must stay PURE: no Postgres, no network, no provider key, no mocking library. It drives
`persist_tribunal_claims` with a fake session that records statements and parameter dicts.

Assert:
- the recorded `INSERT INTO claim (` statement text names `sub_question`, `corroboration_key` and
  `as_of`, and the recorded parameter dict carries all three keys;
- a claim dict carrying `{"sub_question": "...", "corroboration_key": "w01", "as_of":
  date(2021, 3, 4)}` binds those three values through unchanged;
- a claim dict with NONE of the three keys (the pre-15.5 shape) binds all three as `None` and does
  not raise;
- `corroboration_key: ""` binds as `None`. Assert `is None`, not falsiness — the empty string is
  falsy too, and telling those two apart is the entire point of D-W2-2;
- a `sub_question` longer than `_SUB_QUESTION_MAX_CHARS` binds truncated to exactly that length;
- `as_of: "2021-03-04"` (a string that slipped through) binds as `None` rather than reaching the
  database as text;
- `as_of: datetime(2021, 3, 4, 13, 46)` binds as `date(2021, 3, 4)` — a time can never reach a Date
  column;
- **the ordering assertion:** `set_config` (the tenant context) is recorded BEFORE the first
  `INSERT INTO claim (`. `claim` is RLS-protected and `_insert_claim` runs under
  `set_tenant_context`; the recorded call order is the pure way to prove the write is governed by
  the policy. `test_verdict_write_path.py` has the precedent
  (`test_tenant_context_is_set_before_any_verdict_insert`).

Write, in the section heading comment, what this section CANNOT prove and why no attempt is made:
it cannot prove RLS actually denies a cross-tenant write, because that needs a real Postgres AND a
non-superuser role — a DB test run as superuser voids every RLS assertion and passes VACUOUSLY,
which is worse than no test. The enforcement itself is covered BY CONSTRUCTION: a PostgreSQL
row-level POLICY is a table-level object evaluated against row values, so a newly added column is
governed by `claim`'s existing policies without any new DDL, which is the reasoning migrations 0013
and 0016 both record.
  </action>
  <verify>
    <automated>C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -m py_compile tribunal/nestor_pulse_sdk/tests/test_claim_attribution.py && C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -c "s=open('tribunal/nestor_pulse_sdk/tests/test_claim_attribution.py',encoding='utf-8').read(); assert 'INSERT INTO claim (' in s; assert 'set_config' in s; assert 'is None' in s; assert 'asyncio' in s or 'async def' in s; print('write-path section present')"</automated>
  </verify>
  <acceptance_criteria>
    - `test_claim_attribution.py` contains a fake session class that records statements and opens no connection, and imports `persist_tribunal_claims` from `nestor_pulse_sdk.citations.extractor`
    - A test asserts the recorded claim INSERT text contains all three new column names
    - A test asserts `corroboration_key: ""` binds as `None`, written as `is None`
    - A test asserts a pre-15.5 claim dict (none of the three keys) binds three `None`s and raises nothing
    - A test asserts `set_config` is recorded before the first `INSERT INTO claim (`
    - A test asserts a `datetime` `as_of` is bound as a `date`, and a string `as_of` is bound as `None`
    - The section heading comment states what it cannot prove and names both the superuser-vacuity trap and the table-level-POLICY reason
    - The file still reads no `DATABASE_URL` and opens no session
    - `py_compile` exits 0
    - Test execution status: **PENDING-CLOUDBUILD** — this box has no pytest. The resolving build is `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml`; reconcile the printed `collecting: 33 of 33 expected files`
  </acceptance_criteria>
  <done>The write path is proven without a database: the columns are in the statement, absent is NULL, the clamps bite, and the tenant context is set before the row is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claim dict -> `_insert_claim` -> database | the last thing between untrusted model wording and a persisted, queryable column |
| tenant context -> RLS policy | the claim INSERT must run inside `set_tenant_context` or the policy does not govern it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.5-10 | Tampering | `as_of` bind type | mitigate | explicit `bindparam("as_of", type_=Date)`; a `datetime` is converted with `.date()`; a non-date becomes `None` with a WARNING. Driver type inference inside a paid transaction is a runtime error, not a test failure |
| T-15.5-11 | Denial of Service | unbounded attribution text | mitigate | `_SUB_QUESTION_MAX_CHARS` / `_CORROBORATION_KEY_MAX_CHARS` clamped at the boundary, truncation logged at WARNING. A bug bound, not an injection control — these values are caller-supplied |
| T-15.5-12 | Repudiation | empty string vs NULL | mitigate | empty and whitespace-only coerced to `None`, asserted with `is None`. "No key recorded" and "recorded as the empty key" must remain distinguishable to the corroboration queries |
| T-15.5-13 | Elevation of privilege | claim INSERT outside tenant context | mitigate | `persist_tribunal_claims` already calls `set_tenant_context` first; the recorded call ORDER is asserted by a pure test. RLS enforcement of the new columns is covered BY CONSTRUCTION (table-level POLICY) |
| T-15.5-14 | Information disclosure | a vacuous RLS test | accept | no DB-bound RLS test is written. A superuser DSN voids every RLS assertion and passes silently; the trap is named in the test file so nobody adds one later thinking it was an oversight |
| T-15.5-SC | Tampering | package installs | n/a | no package installed, no `requirements.txt` change |
</threat_model>

<verification>
1. `py_compile` clean on both files.
2. Two AST checks (tasks 1 and 2) assert the signature and the call site from the parsed source, not
   from a grep — a grep-based gate is blind to work done in an argument list and passes on a comment
   that merely mentions a column name.
3. `git diff` proves the two no-change claims: the legacy call site at `extractor.py:694` and the
   dropped-claims loop at `:1026`.
4. **NOT verified here:** the pytest run. Record PENDING-CLOUDBUILD; the resolving build is
   `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml` and the number to
   reconcile is `collecting: 33 of 33 expected files`.
5. **NOT verified here and must not be claimed:** that alembic 0017 ran, or that any of these
   columns exist in a live database. The proof is the literal `Running upgrade 0016 -> 0017` line,
   OWED AT PHASE 15.8 alongside 0016's still-unpaid `Running upgrade 0015 -> 0016`. Exit code 0 is
   not proof.
</verification>

<success_criteria>
- The claim INSERT names and binds all three columns, with `as_of` explicitly typed `Date`.
- Absent, empty and whitespace-only attribution values reach the database as NULL.
- Over-long values are truncated loudly; non-date `as_of` values are refused.
- Every pre-existing `_insert_claim` call site still compiles unchanged.
- The tenant context is provably set before the first claim INSERT.
- No deploy, no image build, no migration applied.
</success_criteria>

<output>
Create `.planning/phases/15.5-research-engine-redesign-claim-attribution-wave-2/15.5-03-SUMMARY.md`
when done (`git add -f`). State PENDING items as PENDING; do not resolve them by assertion.
</output>
