---
phase: 15.8-research-engine-redesign-yield-instrumentation-deploy-one-me
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/cloudbuild.test-gates.yaml
autonomous: true
requirements: [GATE-DEBT-GATES-01]

must_haves:
  truths:
    - "A mistyped or absent path in cloudbuild.test-gates.yaml FAILS the build and the failure output NAMES that path"
    - "An empty collection FAILS instead of exiting 0 with a reassuring sentence"
    - "The gate still collects exactly the same 13 verification-gate files and still filters -m \"not live\""
    - "The single-quoted bash -c block contains ZERO apostrophes and the whole build-step arg stays under the 10,000-char Cloud Build cap"
    - "A reader of this config learns why BOTH the || true and the count assertion are load-bearing, and why 15.8 does NOT apply D-W5-5s single-owner deviation to this file"
  artifacts:
    - path: "tribunal/cloudbuild.test-gates.yaml"
      provides: "the completeness assertion this config has never had, plus the failure block that names absent paths"
      contains: "EXPECTED_FILES=13"
    - path: "tribunal/cloudbuild.test-gates.yaml"
      provides: "ONE list read TWICE — ls collects from it, the reconciliation loop names from it"
      contains: "WANTED="
  key_links:
    - from: "WANTED"
      to: "FILES"
      via: "ls over the variable, guarded by || true so an absent file is a SKIP not a collection error"
      pattern: "ls \\$\\$WANTED"
    - from: "COLLECTED"
      to: "EXPECTED_FILES"
      via: "the -ne comparison that converts that skip into a RED build"
      pattern: "COLLECTED.*-ne.*EXPECTED_FILES"
    - from: "the failure block"
      to: "WANTED"
      via: "for f in $$WANTED; do [ -f \"$$f\" ] || echo — the loop that NAMES the missing path instead of printing a bare number"
      pattern: "\\[ -f "
---

<objective>
`tribunal/cloudbuild.test-gates.yaml` has **NO `EXPECTED_FILES` assertion at all** — verified: zero
occurrences. Its file list is built with `ls … 2>/dev/null || true`, so a single mistyped path is a
**silent skip with no red build**: the gate goes green having run 12 of 13 while reporting nothing
about the 13th. That is the exact defect class `cloudbuild.test-engine.yaml` was hardened against by
plan 15.2-26 — still live, one file over.

This plan ports that hardening. It also deletes the config's `if [ -z "$FILES" ] … exit 0` early
return: an empty collection is the WORST case of the false green, and today it is the one case this
config explicitly rewards with a zero exit and the sentence *"no 15.1 gate test files present yet —
nothing to run"*.

Purpose: this phase ends in **ONE deploy and ONE live run of a five-wave redesign**. Every plan in it
is proved by a Cloud Build gate. A gate that can go green having run less than it claims is not a
gate, and this is the last one in the repo without the assertion.

Output: `cloudbuild.test-gates.yaml` with `WANTED` / `EXPECTED_FILES=13` / a named-absent-path failure
block, its narrative commentary moved OUT of the 10,000-char arg into YAML comments, and a local
mutation proof that the assertion BITES.

**Traceability:** this plan carries no REQ-ID. `GATE-DEBT-GATES-01` is the carried gate-integrity debt
recorded verbatim in two places: `.planning/STATE.md` § Gates (15.6) — *"NOTE it has no `EXPECTED_FILES`
assertion at all — a mistyped path there is a silent skip with no red build. … Worth closing in 15.8"*
— and `15.8-CONTEXT.md` § Gate hygiene this phase should close, item 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_preflight gate="blocking">
The worktree stale-base trap is **15/15** in this repo, most recently 2/2 in phase 15.7 at 595–601
commits behind. `git rev-list --count BASE..HEAD` returning 0 reads **GREEN while stale**, because the
stale ref is usually the merge-base. Assert the MERGE-BASE, not the count:

    git merge-base HEAD <PHASE_BASE_SHA>

Phase base at plan-writing time: `2b03d0f` (master, 2026-08-04). If the orchestrator cuts from a later
master that is fine — the SHA is not the guard. **These two sentinels are:**

- POSITIVE: `grep -c test_verdict_write_path.py tribunal/cloudbuild.test-gates.yaml` returns **1**.
  That path is the newest of the 13 and its presence means you have the post-15.1 config.
- NEGATIVE: `grep -c EXPECTED_FILES tribunal/cloudbuild.test-gates.yaml` returns **0**.
  If it returns anything else, someone has already done this work or you are on the wrong file —
  **STOP and report** rather than editing on top of it.

Also sweep `git worktree list` at session start: a stale worktree from a halted run has sat on disk in
this repo before.
</worktree_preflight>

<proving_things_on_this_machine>
**There is no pytest, no Python 3.11 and no Docker on this box, and Cloud Build cannot be run as part
of this plan.** A stdlib-only Python 3.14 ships with gcloud at
`C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`. `bash` is available (Git Bash).

That is enough to prove the thing that matters. The assertion is **shell logic over the real file
list**, so Task 2 extracts it FROM the config, substitutes Cloud Build`s `$$` back to `$`, and runs it
against the real tree — once clean, once over a deliberately mutated copy of the YAML. What that
cannot prove is that Cloud Build itself still parses and runs the config. That proof is **OWED** and
named exactly in `<owed_proofs>` below.

⛔ Do **not** reach for the `ast`-lift harness here — there is no Python module in scope. And the
standing lesson applies to this plan too: **the harness lies before the code does.** Task 2`s
extraction must FAIL LOUD if it finds no block to extract, and its non-vacuity is the mutant run: a
harness that passes the clean case and does not bite the mutant has proved nothing.
</proving_things_on_this_machine>

<context>
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-CONTEXT.md
@tribunal/cloudbuild.test-gates.yaml
@tribunal/cloudbuild.test-engine.yaml
@CLAUDE.md

<reference_implementation>
`tribunal/cloudbuild.test-engine.yaml` is the reference. **Read it. Do not edit one byte of it.**

⛔ **D-W5-5: `cloudbuild.test-engine.yaml` has exactly ONE owner this phase — plan 15.8-13, in wave 4.**
That decision deliberately REVERSES the convention written in that file`s own comment block (which
instructs, four times, that each plan adds its path and its `EXPECTED_FILES` bump in one edit), because
eight wave-1 plans bumping one line is a guaranteed conflict that can silently resolve to a wrong
number. 15.8-13 amends that comment block. **You are a reader of that file and nothing more.**
The deviation applies to the ENGINE config only — this plan`s file keeps the original one-edit rule,
and Task 1 must say so in the YAML comments so the next reader does not generalise the deviation.

The named symbols to port, all present in the engine config`s `pytest-engine` step:

    WANTED=            a shell variable holding the explicit path list, one path per continuation line
    EXPECTED_FILES=    the pinned count, changed together with the list and never separately
    FILES=             the ls result, still guarded by  2>/dev/null || true
    COLLECTED=         echo $$FILES | wc -w
    the -ne guard      if [ "$$COLLECTED" -ne "$$EXPECTED_FILES" ] ... exit 1
    the naming loop    for f in $$WANTED; do [ -f "$$f" ] || echo ...  — inside the failure block
    the success line   echo "collecting: $$COLLECTED of $$EXPECTED_FILES expected files"

**Why the list lives in a variable rather than inline in the `ls`:** so the failure can NAME the path
that went missing instead of printing a bare number at a tired operator. One list, read twice.

**Both halves are load-bearing and removing either one looks reasonable:**
- the `|| true` prevents a COLLECTION ERROR — under `set -e` a bare `FILES=$(ls missing-file)` aborts
  the script, and an aborted script tells you nothing about the other 12 files;
- the count assertion is what converts the silent SKIP that `|| true` permits into a RED build.
</reference_implementation>

<current_state>
Established by grep at `2b03d0f`, so the executor does not re-derive it:

- The config names **13** paths, and **all 13 exist on disk** (verified one by one, 2026-08-04). The
  `|| true` window this config was written around is therefore **CLOSED** — every name resolves.
- The 13: `test_gate_replay.py`, `test_gate_selector.py`, `test_gate_failure_modes.py`,
  `test_gate_calibration.py`, `test_superseded_verdict.py`, `test_verification_buckets.py`,
  `test_fail_loud.py`, `test_tribunal_grouping.py`, `test_claim_distiller.py`,
  `test_tribunal_adjudicate.py`, `test_verdict_publication.py`, `test_stage_schema.py`,
  `test_verdict_write_path.py` — all under `nestor_pulse_sdk/tests/`.
- Last known green: build **`2eae97e6`** = **187 passed, 2 deselected** (STATE.md § Gates).
  The 2 deselected are the `live`-marked calibration tests that `-m "not live"` removes.
- The inline comment block inside the script (the paragraph about the LAST THREE entries arriving with
  15.1 gap-closure plans 15.1-12/13/14) is **STALE** — all three landed long ago.
- **One stale claim in the record, so you do not trust it:** `15.1-01-SUMMARY.md` line 213 says *"New
  test files are picked up automatically by `cloudbuild.test-gates.yaml` once they [exist]"*. That is
  **FALSE** — the list is explicit, never a glob, deliberately so, and this plan makes the falsehood
  loud instead of silent. Do not edit that summary; it is a historical document owned by another phase.

**The flat-count question, answered here so 15.8-13 does not have to guess.** 15.8 adds **no test file
to this config** (every new test file this phase writes belongs to the engine gate), so `EXPECTED_FILES`
stays **13** and the expected result is **187 passed, 2 deselected — unchanged**.

**That flatness is NOT insulation, and the difference matters.** 9 of these 13 files import
`pipeline.tribunal.pipeline`, `research_division.divide` or the tribunal grouping modules — files that
**15.8-02 and 15.8-09 edit**. So this gate is a live regression check on wave-1 and wave-2 work. If it
goes RED at 15.8-13, that is a real signal about those edits, **not** a config problem to be tuned away.
</current_state>

<tasks>

<task type="auto">
  <name>Task 1: Port the EXPECTED_FILES assertion into cloudbuild.test-gates.yaml and delete the empty-collection early exit</name>
  <files>tribunal/cloudbuild.test-gates.yaml</files>
  <read_first>
    - tribunal/cloudbuild.test-gates.yaml — the whole file, all 76 lines
    - tribunal/cloudbuild.test-engine.yaml lines 20–47 — the BOTH-HALVES-LOAD-BEARING paragraph and the ownership paragraph, the exact prose to model
    - tribunal/cloudbuild.test-engine.yaml lines 76–91 — the RELOCATED-2026-07-29 block explaining WHY commentary lives in YAML comments and not inside the arg
    - tribunal/cloudbuild.test-engine.yaml lines 315–411 — the `pytest-engine` step, the implementation being ported
    - .planning/phases/15.8-*/15.8-CONTEXT.md — decision D-W5-5 in full, and § Gate hygiene this phase should close
  </read_first>
  <action>
    Rewrite `tribunal/cloudbuild.test-gates.yaml` in two parts. Keep `steps:`, the step `id`
    (`pytest-gates`), `name`, `entrypoint`, the `docker run` invocation, `options` and `timeout`
    exactly as they are. Keep `pip install --no-cache-dir -q -r requirements.txt` and
    `python -m pytest $$FILES -v -rs --tb=short -m "not live"` **byte-identical** — this plan adds no
    package and changes no pytest argument.

    PART A — THE SCRIPT.

    Replace the inline `FILES=$$(ls path1 path2 … || true)` construction with the engine config`s
    two-reads-of-one-list shape, using the same identifiers named in `<reference_implementation>`:
    a `WANTED` variable holding the same 13 paths one per continuation line in the same order;
    `EXPECTED_FILES=13`; `FILES` from `ls $$WANTED 2>/dev/null || true`; `COLLECTED` from
    `echo $$FILES | wc -w`; and an `if [ "$$COLLECTED" -ne "$$EXPECTED_FILES" ]` block that prints a
    banner, iterates `WANTED` printing every path for which `[ -f "$$f" ]` is false under the heading
    ABSENT OR MISTYPED, then prints the full expected list, then `exit 1`.

    **DELETE the `if [ -z "$$FILES" ] … exit 0` early return and its sentence "no 15.1 gate test files
    present yet — nothing to run".** An empty collection is the worst case of the false green and must
    now fail through the same assertion as every other shortfall. Leave a one-line comment saying so,
    the way the engine config does.

    The banner must read **GATES GATE ASSERTION FAILED**, not ENGINE — an operator reading a build log
    at 3am must be able to tell which of the two configs failed without opening either. Otherwise
    mirror the engine block`s wording: it is not a test failure and it is not a pass; the gate would
    have run LESS than it claims; a pytest summary printed after this point would describe only the
    files that happened to exist; fix the path, or add the file, or change BOTH the list and
    EXPECTED_FILES deliberately; do not delete this check.

    Add ONE short comment inside the script, immediately above `WANTED`, stating that the full rationale
    lives in the YAML comments above `steps:` and that there must be **no apostrophes anywhere below
    that line**. Nothing else. Every other word of commentary goes in Part B.

    ⛔ **NO APOSTROPHE MAY APPEAR ANYWHERE between `bash -c '` and its closing quote** — not in a
    string, not in a comment, and not in a comment quoting this rule. The whole block is ONE
    single-quoted `-c` argument; one apostrophe closes it and the remainder is reparsed by the outer
    shell. The symptom is bewildering — a bare `python: command not found` long AFTER pip succeeded —
    and it has already cost this project a red build (2026-07-27). Write "the gate would have run less
    than it claims", never "the gate wouldnt".

    PART B — THE YAML COMMENTS (above `steps:`, where apostrophes are safe because YAML comments are
    not part of the arg).

    KEEP, unchanged: the no-Postgres / no-provider-key rationale, the `not live` LOAD-BEARING
    paragraph, and the INVOCATION block.

    MOVE OUT of the script, and mark as HISTORY rather than deleting: the pre-registration paragraph
    about `test_verdict_publication.py` / `test_stage_schema.py` / `test_verdict_write_path.py` arriving
    with plans 15.1-12/13/14. Record that all three landed and the window is closed. This repo`s
    convention is supersede-in-place, never deletion.

    REWRITE the paragraph that currently justifies the `|| true` as a wave-ordering accommodation. State
    instead that the arrangement is now CLOSED (2026-08-04, plan 15.8-07): all 13 named files exist,
    verified one by one, so the config **asserts its own completeness** — `EXPECTED_FILES` pins how many
    paths the list names, the script counts what `ls` actually collected, and a mismatch FAILS THE BUILD
    naming the absent or mistyped path.

    ADD, as its own paragraph: **BOTH HALVES ARE LOAD-BEARING AND REMOVING EITHER ONE LOOKS
    REASONABLE.** Give both reasons in the engine config`s words, with this file`s numbers: drop the
    `|| true` and one missing file kills the script before pytest runs; drop the assertion and one
    mistyped path makes this gate go green having run **12 of 13** while reporting nothing about the
    13th.

    ADD an OWNERSHIP paragraph recording all four facts:
      * 15.8-07 owns this file in wave 1; 15.8-13 is READ-ONLY on it in wave 4 and reconciles the count;
      * this file keeps the ORIGINAL one-edit rule — a path and its `EXPECTED_FILES` bump are ONE change;
      * **D-W5-5`s single-owner deviation applies to `cloudbuild.test-engine.yaml` ONLY**, because eight
        wave-1 plans add test files to THAT gate and none add one here. Say this explicitly: a reader
        who sees the deviation recorded in the sibling file must not generalise it to this one;
      * phase 15.8 adds NO file to this list, so `EXPECTED_FILES` stays 13 and 187 passed / 2 deselected
        is the number to beat — **and a count that did not rise is not automatically a skip, but it must
        be EXPLAINED, not merely noted.** Write the explanation down: the 13 are a fixed set and this
        phase adds none, **but 9 of the 13 import `pipeline.py`, `research_division.divide` or the
        tribunal grouping modules, which 15.8-02 and 15.8-09 edit** — so a RED here is a real regression
        signal about those edits, not a config to be tuned.

    ADD a short HOW TO READ A RESULT paragraph: `gcloud builds submit … | tail` returns the **PIPE`s**
    exit code, so a FAILED build reports exit 0 to the shell — read the build TEXT via
    `gcloud builds describe`, never a shell exit status. An **EXPIRED** build is visually identical to
    QUEUED and is **not a result**.
  </action>
  <acceptance_criteria>
    - `grep -c "EXPECTED_FILES=13" tribunal/cloudbuild.test-gates.yaml` returns exactly 1
    - `grep -c "WANTED=" tribunal/cloudbuild.test-gates.yaml` returns exactly 1
    - the string `nothing to run` does NOT appear anywhere in the file, and no `exit 0` appears inside
      the `bash -c` block
    - the 13 paths named by `WANTED` are EXACTLY the 13 paths named by the previous version — no
      addition, no removal, no reordering that changes the set
    - `grep -c -- '-m "not live"' tribunal/cloudbuild.test-gates.yaml` returns 1 (15.1-01`s invariant,
      preserved)
    - `grep -ci postgres tribunal/cloudbuild.test-gates.yaml` returns 0 and `grep -c DATABASE_URL …`
      returns 0 (15.1-01`s two other invariants, preserved)
    - `git diff` shows the `pip install` line and the `python -m pytest` line UNCHANGED
    - the count of apostrophes strictly between `bash -c '` and its closing quote is **0**
    - the second element of `args` is under 10,000 characters
    - the YAML comments contain the phrase BOTH HALVES ARE LOAD-BEARING and an explicit sentence
      scoping D-W5-5 to `cloudbuild.test-engine.yaml` only
  </acceptance_criteria>
  <verify>
    <automated>cd tribunal &amp;&amp; test "$(grep -c 'EXPECTED_FILES=13' cloudbuild.test-gates.yaml)" = 1 &amp;&amp; test "$(grep -c 'WANTED=' cloudbuild.test-gates.yaml)" = 1 &amp;&amp; test "$(grep -c 'nothing to run' cloudbuild.test-gates.yaml)" = 0 &amp;&amp; test "$(grep -c -- '-m "not live"' cloudbuild.test-gates.yaml)" = 1 &amp;&amp; test "$(grep -ci postgres cloudbuild.test-gates.yaml)" = 0 &amp;&amp; test "$(grep -c DATABASE_URL cloudbuild.test-gates.yaml)" = 0 &amp;&amp; echo TASK1_STATIC_OK</automated>
  </verify>
  <done>
    `cloudbuild.test-gates.yaml` reconciles its collected count against `EXPECTED_FILES=13`, names every
    absent or mistyped path on failure, has no early exit for an empty collection, and carries its
    rationale in YAML comments outside the 10,000-char arg. The pytest invocation and the pip line are
    unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the assertion BITES — extract the block from the YAML and run it clean AND mutated</name>
  <files>tribunal/cloudbuild.test-gates.yaml (read only), scratchpad harness under C:/Users/ajimimo/AppData/Local/Temp/claude/.../scratchpad (NOT committed)</files>
  <read_first>
    - tribunal/cloudbuild.test-gates.yaml as edited by Task 1
    - the `<proving_things_on_this_machine>` block above — no pytest, no Docker, no Cloud Build here
  </read_first>
  <action>
    **A happy-path run proves nothing. The assertion`s entire value is the failure case**, so the proof
    is a matched pair: the real config, and a copy of the real config with one character wrong.

    Build the harness in the scratchpad — **do not commit any file**; `files_modified` for this plan is
    one YAML config and nothing else. Use one directory for everything and refer to it as `$SC`; the
    `<verify>` command sets `SC="${TMPDIR:-/tmp}/gates-proof"`, and `$TMPDIR` is not always set in Git
    Bash — if you place the harness elsewhere, update the `<verify>` command to match rather than
    leaving the two pointing at different directories.

    Step 1 — two subjects. Copy `tribunal/cloudbuild.test-gates.yaml` to the scratchpad as
    `gates-clean.yaml`. Make `gates-mutant.yaml` from the SAME copy by changing exactly one character
    in one path — `test_fail_loud.py` becomes `test_fail_lound.py`. That is the real defect class: not
    a deleted file, a mistyped name. **Mutate the YAML, not the extracted script**, so both runs go
    through the identical extraction path and the proof is about the config rather than about a
    hand-written copy of it.

    Step 2 — extraction, with the bundled interpreter at
    `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`. Write the extractor to
    `$SC/extract.py`, taking two positional arguments (input YAML, output `.sh`) — the `<verify>`
    command below invokes it at exactly that path with exactly that signature, so do not rename it or
    change its argument order. Read a YAML file; find the
    line ending in `bash -c` followed by a single quote; take every following line up to the line whose
    stripped content is exactly a single quote; replace every `$$` with `$` (Cloud Build`s escape — there
    is no legitimate `$$` in this script, so a global replace is safe); drop any line whose stripped form
    begins with `pip install` or `python -m pytest` (neither can run on this box); dedent; write the
    result as a `.sh`.

    **The extractor must FAIL LOUD** — a non-zero exit and a message — if it finds no opening line or no
    closing line. A harness that silently emits an empty script would make both runs pass for the same
    empty reason, and in this project the harness has lied before the code did four times in one day.

    Step 3 — run each extracted script with `bash`, with the working directory set to `tribunal/` (the
    paths in `WANTED` are relative to it, exactly as `-w /workspace` makes them in Cloud Build). Capture
    stdout and the exit status of each.

    Step 4 — assert the pair, and record the actual captured output in the SUMMARY, not a paraphrase:
      * CLEAN: exit **0**, stdout contains `collecting: 13 of 13 expected files`, and stdout lists all
        13 paths.
      * MUTANT: exit **1**, stdout contains `GATES GATE ASSERTION FAILED`, contains
        `nestor_pulse_sdk/tests/test_fail_lound.py` under the ABSENT OR MISTYPED heading, and reports
        `collected 12`. **If the mutant exits 0, the assertion is not wired and Task 1 is not done** —
        go back, do not record a pass.

    Step 5 — the static invariants over the REAL file (not the copies), each as an explicit check with
    its number recorded:
      * apostrophes strictly between `bash -c '` and the closing quote: **0**;
      * length of the second `args` element: **under 10,000** — print the actual number, because the
        engine config hit this cap at 13,473 and could not run at all;
      * `-m "not live"` present exactly once; `postgres` 0; `DATABASE_URL` 0;
      * the set of paths in `WANTED` equals the set of `nestor_pulse_sdk/tests/*.py` files that the
        PREVIOUS version of the config named — compare BOTH directions, so neither an addition nor a
        removal can hide;
      * every path in `WANTED` satisfies `[ -f ]` on the real tree (13 of 13).

    Do not use `grep -c` against the unfiltered file for any check whose token also appears in the
    comment prose — this config now discusses `EXPECTED_FILES` and `|| true` in its own comments, and a
    gate that counts its own header is self-invalidating. Filter comments (`grep -v "^ *#"`) or scope the
    check to the extracted script.
  </action>
  <acceptance_criteria>
    - the extractor exits non-zero when pointed at a file with no `bash -c` block (demonstrate it once
      against any other YAML in `tribunal/`, so the fail-loud path is shown to work rather than assumed)
    - CLEAN run: exit 0 AND stdout contains `collecting: 13 of 13 expected files`
    - MUTANT run: exit 1 AND stdout names `nestor_pulse_sdk/tests/test_fail_lound.py` AND reports 12
      collected against 13 expected
    - apostrophe count inside the single-quoted block is 0, and the arg length is printed and is < 10000
    - the 13-path set is proved unchanged in BOTH directions against the pre-Task-1 version
    - no file outside `tribunal/cloudbuild.test-gates.yaml` is committed
  </acceptance_criteria>
  <verify>
    <automated>cd tribunal &amp;&amp; SC="${TMPDIR:-/tmp}/gates-proof" &amp;&amp; mkdir -p "$SC" &amp;&amp; cp cloudbuild.test-gates.yaml "$SC/gates-clean.yaml" &amp;&amp; sed 's|test_fail_loud\.py|test_fail_lound.py|' cloudbuild.test-gates.yaml > "$SC/gates-mutant.yaml" &amp;&amp; PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" &amp;&amp; for v in clean mutant; do "$PY" "$SC/extract.py" "$SC/gates-$v.yaml" "$SC/gates-$v.sh" || exit 1; done &amp;&amp; bash "$SC/gates-clean.sh" > "$SC/clean.out" 2>&amp;1; CLEAN=$?; bash "$SC/gates-mutant.sh" > "$SC/mutant.out" 2>&amp;1; MUT=$?; test "$CLEAN" = 0 &amp;&amp; test "$MUT" = 1 &amp;&amp; grep -q "collecting: 13 of 13 expected files" "$SC/clean.out" &amp;&amp; grep -q "test_fail_lound.py" "$SC/mutant.out" &amp;&amp; grep -q "GATES GATE ASSERTION FAILED" "$SC/mutant.out" &amp;&amp; echo TASK2_MUTATION_PROOF_OK</automated>
  </verify>
  <done>
    The clean config collects 13 of 13 and exits 0; the one-character-mutated config exits 1, prints
    GATES GATE ASSERTION FAILED and names `test_fail_lound.py`. Both outputs are pasted into the SUMMARY
    verbatim. The block holds zero apostrophes and the arg is under 10,000 characters, both numbers
    recorded. Nothing but the YAML config is committed.
  </done>
</task>

</tasks>

<owed_proofs>
**Say plainly what this plan cannot pay, and exactly what settles it.**

Cloud Build cannot be run from this machine as part of this plan. Two proofs are therefore OWED, both
to **15.8-13 in wave 4**, which is the only plan in this phase that runs a gate:

1. **The happy path on Cloud Build.**
   `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"`
   Settled by the build LOG TEXT containing `collecting: 13 of 13 expected files` followed by
   **187 passed, 2 deselected**, and by the status read from
   `gcloud builds describe <BUILD_ID> --format="value(status)"` returning `SUCCESS`.
   **Never by a shell exit code** — `builds submit … | tail` returns the PIPE`s status, so a FAILED
   build reports exit 0. **An EXPIRED build is visually identical to QUEUED and is NOT a result.**
   If the passed count is not 187, that is a REGRESSION from wave-1/wave-2 edits to `pipeline.py` and
   `research_division.py` (9 of the 13 files import them) and must be investigated, not renormalised.

2. **The failure path on Cloud Build — one extra build, and it is cheap.**
   The config does not have to be committed to be submitted. 15.8-13 may run
   `gcloud builds submit tribunal --config=<scratchpad copy with one mistyped path>` and confirm the
   build goes RED with `GATES GATE ASSERTION FAILED` naming the mistyped path.
   **This is OPTIONAL, and its absence is acceptable**, because Task 2 already proves the assertion
   bites on the real config text and what this build would add is only that Cloud Build`s `$$`
   substitution and the docker layer preserve that behaviour — which proof 1 already exercises for the
   same script. Record which of the two was taken; do not leave it ambiguous.

**Package legitimacy:** this plan installs nothing. The `pip install --no-cache-dir -q -r
requirements.txt` line is preserved byte-identical and `requirements.txt` is untouched, so no package
legitimacy audit is triggered and no `[ASSUMED]`/`[SUS]` checkpoint is required.
</owed_proofs>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree → Cloud Build gate | this config decides what the project accepts as PROOF; no human re-reads its file list on any given build |
| Cloud Build log → operator judgement | the operator declares "green" from text this config itself prints, at the end of a five-wave redesign with one deploy and one paid run behind it |
| build container → the internet | `pip install` reaches PyPI; `-m "not live"` is what keeps provider egress out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.8-07-01 | Tampering | the `WANTED` list in `cloudbuild.test-gates.yaml` | mitigate | `EXPECTED_FILES=13` reconciled against `echo $$FILES \| wc -w`; a mismatch exits 1 and the `[ -f "$$f" ]` loop NAMES every absent path. Task 2 proves it bites with a one-character mutation |
| T-15.8-07-02 | Repudiation | the build status an operator records | mitigate | status read from `gcloud builds describe`, never a shell exit code (`builds submit \| tail` returns the PIPE`s status); EXPIRED is not a result. Written into the YAML comments so it survives this plan |
| T-15.8-07-03 | Denial of Service (self-inflicted) | the single-quoted `bash -c` argument | mitigate | zero-apostrophe assertion strictly inside the block, plus a printed arg length under the 10,000-char Cloud Build cap; all narrative moved to YAML comments outside the arg. Precedent: one red build 2026-07-27 (exit 127) and one un-runnable config at 13,473 chars |
| T-15.8-07-04 | Spoofing (a real LLM call presenting as a test) | the pytest invocation | mitigate | `-m "not live"` preserved byte-identical and asserted present exactly once; this config injects no provider key and no `DATABASE_URL`, both asserted 0 |
| T-15.8-07-05 | Elevation of Privilege | `docker run -v /workspace:/workspace` running pytest as root | accept | identical to every other gate config in this repo; no credentials, no service-account token and no DSN are passed into the container. Changing it is out of scope and would diverge the two gate configs |
| T-15.8-07-06 | Information Disclosure | Cloud Build logs | accept | this config carries no secret, no DSN and no key; `logging: CLOUD_LOGGING_ONLY` keeps output inside the project |
| T-15.8-07-SC | Tampering | npm/pip/cargo installs | mitigate | this plan adds NO package. The `pip install … -r requirements.txt` line is preserved byte-identical and `git diff` is asserted to show it unchanged; `requirements.txt` is not in `files_modified`. No `[ASSUMED]`/`[SUS]` package, so no legitimacy checkpoint |
</threat_model>

<verification>
1. `grep -c EXPECTED_FILES tribunal/cloudbuild.test-gates.yaml` was **0** before this plan and is
   **non-zero** after — the debt named in STATE.md is closed.
2. The mutation pair from Task 2 is recorded with **verbatim captured output**, not a paraphrase:
   clean exits 0 at 13 of 13; mutant exits 1 naming `test_fail_lound.py`.
3. `tribunal/cloudbuild.test-engine.yaml` is **byte-identical** to its pre-plan state —
   `git diff --stat` names exactly one file. (D-W5-5: that file has one owner this phase and it is not
   this plan.)
4. The 13-path set is unchanged in both directions; the pytest line and the pip line are unchanged.
5. Zero apostrophes inside the single-quoted block; arg length printed and under 10,000.
6. The Cloud Build run is recorded as OWED to 15.8-13 with the exact command and the exact log text
   that settles it — not claimed as done.
</verification>

<success_criteria>
- `tribunal/cloudbuild.test-gates.yaml` asserts its own completeness: one list, read twice, reconciled
  against a pinned count, failing RED and naming the path when they disagree.
- An empty collection fails; the reassuring `exit 0` is gone.
- A reader of the file learns why both guards are load-bearing, why this file keeps the one-edit rule,
  and why D-W5-5`s deviation is scoped to the sibling config only.
- The failure case is PROVED locally, not asserted; the Cloud Build case is OWED in writing.
- Exactly one file changed.
</success_criteria>

<output>
Create `.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-07-SUMMARY.md`
when done. **`.planning/` is gitignored — `git add -f` it.**

The SUMMARY must carry, verbatim: the clean run output, the mutant run output, the apostrophe count,
the measured arg length, and the two OWED Cloud Build proofs with their exact commands.
</output>
