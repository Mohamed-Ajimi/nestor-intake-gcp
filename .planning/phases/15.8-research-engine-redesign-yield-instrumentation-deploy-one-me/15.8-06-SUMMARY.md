---
phase: 15.8-research-engine-redesign-yield-instrumentation-deploy-one-me
plan: 06
subsystem: planning-decisions
status: COMPLETE
tags: [decision-record, workshop-loop, telemetry, D-W5-8, D-W5-9]
requires: []
provides:
  - "D-W5-8 — the catch_up_matches newcomer-median boundary, ruled 1a"
  - "D-W5-9 — the actions counter, ruled 2-remove"
  - "15.8-06-DECISION-RECORD.md — the basis both entries cite"
affects:
  - "15.8-10 (wave 2) — implements BOTH rulings; owns the one-line actions edit"
  - "15.8-15 — reads `actions` off the one measuring run under D-W5-9's definition"
  - "15.8-04 — unchanged; D-W5-7 cross-referenced from D-W5-9, not re-ruled"
  - "15.8-05 — deliberately UNTOUCHED; 2-persist declined so no schema amendment"
tech-stack:
  added: []
  patterns: []
key-files:
  created:
    - .planning/phases/15.8-.../15.8-06-DECISION-RECORD.md
  modified:
    - .planning/phases/15.8-.../15.8-CONTEXT.md
decisions:
  - "D-W5-8 — catch_up_matches newcomer median: option 1a, accept + document + warn; D-W4-3 accepted as honestly delivered; both committed assertions preserved"
  - "D-W5-9 — actions counter: option 2-remove, drop admission_resolver_calls; actions is a CALL COUNT, not a spend signal"
metrics:
  tasks_completed: 3
  tasks_total: 3
  duration: ~45m
  completed: 2026-08-04
---

# Phase 15.8 Plan 06: Rule the two NOT RULED items — Summary

Both items `15.7-VERIFICATION.md` left explicitly NOT RULED are now ruled, recorded as **D-W5-8**
(`1a`) and **D-W5-9** (`2-remove`), with the arithmetic that settles each in a decision record the
operator answered from — so 15.8-10 has exactly one authority to implement against and no judgement
call left to it.

## Tasks

| Task | Name | Status | Commit |
|---|---|---|---|
| 1 | Write the decision record | DONE | `db0f247` |
| — | Checkpoint-paused summary (worktree-loss protection) | DONE | `73c8a59` |
| 2 | Operator rules on both items | DONE | `483fec7` |
| 3 | Record rulings as D-W5-8 / D-W5-9 in 15.8-CONTEXT.md | DONE | `c5d0cd3` |

## The rulings

**Item 1 → `1a`.** Accept the boundary, document it, warn when it binds. **D-W4-3 is accepted as
honestly delivered** — verification's arithmetic stands: at the validated configuration the low median
is 6 and the schedule fires. 15.8-10 implements exactly two things: the boundary condition replacing
the unconditional phrasing in **both** docstrings (`catch_up_matches` and `_catch_up_pairs`), and a
`log.warning` at `_catch_up_pairs`' `median <= 0` guard so a no-op stops being silent. D-W5-8
explicitly **bars** 15.8-10 from touching `test_catch_up_matches_returns_the_low_median` or
`test_catch_up_matches_takes_the_low_side_of_an_even_field`.

**Item 2 → `2-remove`.** Drop `admission_resolver_calls` from the ten-term `calls` sum in
`run_workshop_stage_b`. D-W5-9 records the correction that decided it — **`actions` is a CALL COUNT,
not a spend signal**; `cost_usd` rides separately on the same `_stage_b_feed_finish` call and was never
contaminated — plus D-W5-14's invocation-semantics finding. It names **15.8-10 as the owner** of the
one-line edit in a file already in its `files_modified`, and cross-references **D-W5-7** (15.8-04's,
already ruled) as the same integer damaged from the opposite direction.

**`2-persist` was declined, so `15.8-05` was NOT amended and `D-W5-11` was NOT reopened.** 15.8-05 was
executing in a parallel worktree throughout and was never touched. Wave 1 proceeded undisturbed.

## Deviations from Plan

### 1. [Rule 1 — gate integrity] The plan's own Task 2 verify is vacuous-prone, and it actually fired

The plan's gate is `grep -A40 '^## RULING' $R | grep -Eq '2-(keep|remove|persist)'`. After the real
ruling was written, that grep returns **both `2-remove` and `2-persist`** — because the reasoning text
records that `2-persist` was *declined*. **A gate that matches the option the operator rejected is not
measuring the ruling.** Left unaddressed it would also have passed on the pre-ruling placeholder had
that placeholder listed the ids the natural instructional way ("answer with 1a, 1b or 1c").

Fix applied at Task 1 and proved at Task 2: every identifier lives on a `**Ruling:**` line and nowhere
else in the section, and the check is anchored on that label. Proved discriminating:

| id | anchored ``^**Ruling:** `id` `` |
|---|---|
| `1a` | **1** |
| `1b`, `1c` | 0 |
| `2-remove` | **1** |
| `2-keep`, `2-persist` | 0 |

### 2. [Rule 1] The `D-W5-7` cross-reference fell outside the acceptance criterion's window

`grep -A20 '^### D-W5-9 —' | grep -q 'D-W5-7'` returned **0** on first write — the full cross-reference
paragraph sits at the end of a long entry. Added a compact pointer immediately under the RULED line,
which satisfies the gate and is better for a reader anyway (the pointer is now the fourth line of the
entry, not the fortieth). Full paragraph retained at the end. Re-verified: **1**.

### 3. Blast radius of the declined option `1b` was over-stated; corrected in the record

A third test, `test_catch_up_matches_returns_a_real_int_and_never_a_bool`, feeds the **same**
`[12, 12, 11, 0, 0]` input as the flipping assertion but asserts only `isinstance(result, int)` and
`not isinstance(result, bool)` — never the value, so it survives either ruling. Also:
`test_catch_up_matches_takes_the_low_side_of_an_even_field` holds two assertions and only its second
(`[0, 1, 2, 3] == 1`) moves; its first (`[10, 12] == 10`) has no zeros. So `1b`'s cost was **two test
functions and two assertion lines**, not three. The record states this precisely so the declined option
was costed honestly rather than inflated.

### 4. Stale-base trap fired again — 16/16

The worktree was cut from `a3a0c96`, **601 commits behind** the required base `e4b71bd`. Caught only by
the mandatory `merge-base != BASE` assertion; `rev-list --count BASE..HEAD` would have read green.
Corrected with `git reset --hard` before any work. Sixteenth consecutive occurrence in this repo.

## D-W5-20 compliance (the contended-write-target discipline)

- Snapshot captured **immediately before writing**, at Task 3 execution time: **19** headings. The 19
  measured during the earlier checkpoint pass was deliberately **not** reused as a baseline — it was
  flagged as informational-only in the paused summary, and a fresh snapshot was taken regardless.
- Delta asserted at **exactly +2** (19 → 21). **No total is hardcoded in any check.**
- **All 19 pre-existing headings survive verbatim** (`grep -c -xF -f snapshot context` = 19), so
  nothing was deleted, edited, reordered or duplicated. This check is baseline-relative and therefore
  immune to a sibling appending in the meantime.
- **Survival greps anchored on the trailing em dash, and the anchor was demonstrated necessary rather
  than assumed:** `^### D-W5-1` prefix-matches **11** headings in this file (D-W5-1 plus
  D-W5-10…D-W5-18), while `^### D-W5-1 —` returns exactly **1**. `^### D-W5-8 —` and `^### D-W5-9 —`
  each return exactly 1.
- Both target IDs were proved **unclaimed (0)** before the write, em-dash-anchored.
- The snapshot file is deliberately **untracked** (`git ls-files --error-unmatch` exits 1).

## Scope discipline

- **No production file, no test file, no CI config touched.** `git status --porcelain tribunal/`
  returned empty at every task boundary, verified four times.
- **D-W5-5 respected:** `cloudbuild.test-engine.yaml` untouched — 15.8-13 owns it in wave 4.
- The superseded "STILL NOT RULED" block in D-W5-3 is **marked, not deleted**: both original bullets
  remain verbatim under a superseded banner pointing at D-W5-8 / D-W5-9.
- STATE.md and ROADMAP.md untouched — the orchestrator owns those.

## Verification of every source fact

Every figure in the record was re-verified by grep against the merged tree, located by symbol and never
by line number (all line numbers in `15.7-VERIFICATION.md` / `15.6-REVIEW.md` drifted on 2026-08-04).
All held: `catch_up_matches` returns `int(values[(len(values) - 1) // 2])` with newcomers' zeros
included; `_catch_up_pairs` opens `if median <= 0: return []`; `_TOURNAMENT_ROUNDS_MIN = 6`,
`_LOOP_MIN_ROUNDS = 4`, `_LOOP_MAX_ROUNDS = 10`, `_NEW_PER_ROUND = 6`, `_CATCH_UP_MAX_MATCHES = 120`;
the catch-up `log.info` sits inside `if catch_up:`; `_stage_b_feed_finish` takes `actions` and
`cost_usd` as separate arguments on `StageFeed.set_summary`; `resolver_calls = 1` precedes the
`await resolve_redirects(...)`; and `.get("counts")` has exactly one unrelated consumer, confirming the
inert-instrumentation trap that kept a cheaper `2-persist` off the table.

## Known Stubs

None. This plan produces two markdown documents and changes no code.

## Threat Flags

None. No network, credential, tenant data, service or migration is touched by this plan.

## Self-Check

- `15.8-06-DECISION-RECORD.md` — FOUND, 300 lines, tracked
- `15.8-CONTEXT.md` — FOUND, 21 decision headings, tracked
- `.15.8-06-headings.before` — present and correctly UNTRACKED
- Commits `db0f247`, `73c8a59`, `483fec7`, `c5d0cd3` — all FOUND
- `git status --porcelain tribunal/` — empty

## Self-Check: PASSED
