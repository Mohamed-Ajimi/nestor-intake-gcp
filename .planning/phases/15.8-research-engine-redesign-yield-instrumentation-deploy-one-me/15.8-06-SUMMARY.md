---
phase: 15.8-research-engine-redesign-yield-instrumentation-deploy-one-me
plan: 06
subsystem: planning-decisions
status: CHECKPOINT-PAUSED — NOT COMPLETE
tags: [decision-record, workshop-loop, telemetry, D-W5-8, D-W5-9]
requires: []
provides:
  - "15.8-06-DECISION-RECORD.md — both NOT RULED items stated for the operator"
affects:
  - "15.8-10 (wave 2) — implements both rulings once D-W5-8 / D-W5-9 are written"
  - "15.8-15 — reads `actions` off the one measuring run under D-W5-9's definition"
tech-stack:
  added: []
  patterns: []
key-files:
  created:
    - .planning/phases/15.8-.../15.8-06-DECISION-RECORD.md
  modified: []
decisions: []
metrics:
  tasks_completed: 1
  tasks_total: 3
  duration: ~25m
  completed: null
---

# Phase 15.8 Plan 06: Rule the two NOT RULED items — Summary

**STATUS: PAUSED AT THE TASK 2 DECISION CHECKPOINT. THIS PLAN IS NOT COMPLETE.**
Task 1 is done and committed. Tasks 2 and 3 remain. A continuation agent must resume after the
operator rules.

One-liner: the decision record for `catch_up_matches`' newcomer median and the `actions` counter is
written and verified against source, and execution stopped at the blocking operator decision.

## Tasks

| Task | Name | Status | Commit |
|---|---|---|---|
| 1 | Write the decision record | DONE | `db0f247` |
| 2 | Operator rules on both items | **BLOCKED — awaiting operator** | — |
| 3 | Record rulings as D-W5-8 / D-W5-9 in 15.8-CONTEXT.md | NOT STARTED | — |

## What Task 1 produced

`.planning/phases/15.8-*/15.8-06-DECISION-RECORD.md` — 290 lines, tracked via `git add -f`.

Every figure in the plan's `<interfaces>` block was re-verified by grep against the merged tree,
located by symbol and never by line number. All of them held:

- `catch_up_matches` returns `int(values[(len(values) - 1) // 2])` — the low median, newcomers' zeros
  included. `_catch_up_pairs` opens `if median <= 0: return []`.
- `_TOURNAMENT_ROUNDS_MIN = 6`, `_TOURNAMENT_ROUNDS_MAX = 10`, `_LOOP_MIN_ROUNDS = 4`,
  `_LOOP_MAX_ROUNDS = 10`, `workshop_evolve._NEW_PER_ROUND = 6`, `_CATCH_UP_MAX_MATCHES = 120`.
- `_CATCH_UP_MAX_MATCHES`' comment records the median growing to 18 after three loop rounds over a
  field of 30 — the boundary moves further away, not closer.
- `run_tournament`'s catch-up `log.info` sits inside `if catch_up:` — a no-op is genuinely silent.
- The ten-term `calls` sum with its CR-07 comment, and
  `_stage_b_feed_finish(feed, final, actions=calls, items_read=..., cost_usd=cost)` — `actions` and
  `cost_usd` are separate arguments on `StageFeed.set_summary`, confirming `actions` is a call count
  and not a spend signal.
- `workshop_admission.admit_invented_angles`: `resolver_calls = 1` is assigned BEFORE
  `await resolve_redirects(...)`, confirming D-W5-14's invocation-semantics reading.
- `.get("counts")` across `tribunal/nestor_pulse_sdk` returns exactly ONE hit, unrelated
  (`pipeline.py` stage-logging state) — the inert-instrumentation trap is real, so no cheap version
  of option `2-persist` was offered.

## Deviations from Plan

### Findings recorded, no rule broken

**1. [Rule 2 — gate integrity] The `## RULING` placeholder was written to keep Task 2's gate non-vacuous.**
Task 2's automated verify is `grep -A40 '^## RULING' $R | grep -Eq '\b(1a|1b|1c)\b'` plus the same for
`2-(keep|remove|persist)`. Had the empty placeholder listed the option ids as instructions (the
natural way to write it — *"answer with 1a, 1b or 1c"*), **that gate would have passed with no ruling
recorded at all.** The option identifiers are therefore listed only in the two Options sections, above
the heading, and the RULING slots read `_(awaiting operator)_`. Verified: both greps return empty on
the current file. This is the same vacuous-gate class as the `^### D-W5-1` prefix trap in D-W5-20.

**2. Blast radius of option `1b` is two test functions, not three — stated in the record.**
A third test, `test_catch_up_matches_returns_a_real_int_and_never_a_bool`, feeds the **same**
`[12, 12, 11, 0, 0]` input as the flipping assertion, but asserts only `isinstance(result, int)` and
`not isinstance(result, bool)` — never the value. It survives either ruling. Also noted:
`test_catch_up_matches_takes_the_low_side_of_an_even_field` holds two assertions and only its second
(`[0, 1, 2, 3] == 1`) flips; its first (`[10, 12] == 10`) has no zeros. The plan's count of two test
functions is correct; the record now states precisely which lines move, so a `1b` ruling is costed
honestly rather than over-stated.

**3. No production file, no test file, no CI config touched.** `git status --porcelain tribunal/`
returned empty before and after Task 1. D-W5-5 respected: `cloudbuild.test-engine.yaml` untouched.

### Stale-base trap fired again — 16/16

The worktree was cut from `a3a0c96`, **601 commits behind** the required base `e4b71bd`. Caught by the
mandatory `merge-base != BASE` assertion and corrected with `git reset --hard`. `rev-list --count`
would have read green. This is the sixteenth consecutive occurrence in this repo.

## Informational readings for the continuation agent — NOT a baseline

At this worktree's base (`e4b71bd`), `15.8-CONTEXT.md` carried **19** `^### D-W5-` headings, and both
`^### D-W5-8 —` and `^### D-W5-9 —` returned **0** (em-dash anchored, so `D-W5-1` cannot match
`D-W5-10/11/12`).

**⚠ DO NOT USE 19 AS TASK 3'S BASELINE.** Per D-W5-20 the file is a contended write target with at
least three writers, and this worktree is destroyed on return. Task 3 must capture its OWN snapshot
immediately before writing and assert the delta is exactly `+2` against that snapshot. No total from
this document may be hardcoded anywhere.

## Checkpoint

Task 2 is `checkpoint:decision`, `gate="blocking"`, and the plan is `autonomous: false`. The executor
stopped rather than selecting an option. The operator must reply with one id per item:
`1a` | `1b` | `1c`, and `2-keep` | `2-remove` | `2-persist`. Recommendations in the record are
Item 1 (a) and Item 2 (2); both are recommendations, not defaults.

## Self-Check

- `15.8-06-DECISION-RECORD.md` — FOUND, 290 lines, tracked (`git ls-files --error-unmatch` exits 0)
- Commit `db0f247` — FOUND
- `git status --porcelain tribunal/` — empty
- All 12 required tokens present in the record

## Self-Check: PASSED (for Task 1 only — Tasks 2 and 3 not executed)
