# 15.8 UAT — the ONE measuring run, compared against V-01

**Created:** 2026-08-04 by plan 15.8-12, **BEFORE the run**, deliberately.
**Filled by:** plan 15.8-15, after the deploy in `infra/DEPLOY-RUNBOOK.md` § Phase 15.8.

This table is written now so that **the questions are fixed before the answers are known.** Every
`15.8 observed` and every `Verdict` cell holds the literal token `TBD` at creation — not a blank. A
blank cell is indistinguishable from a forgotten one; a `TBD` is a claim that the number is **owed**,
and it lets 15.8-15 assert that none remain.

---

# ⛔ STOP PROCEDURE — READ THIS FIRST, BEFORE ANY OTHER SECTION

**Written 2026-08-05 by plan 15.8-15, BEFORE the run was triggered.** It is deliberately the first
thing in this file. This is not documentation — **it is the abort handle**, and at 3am nobody reads a
plan. Five points, in the order you need them. Every claim below was verified against the source file
named beside it; nothing here is recalled.

### 1. ONLY RESOLVING THE ROW STOPS A RUN.

**Only resolving the ROW stops a run** — `cancel_research`'s own docstring is the authority, and this
is the sentence every other point on this list depends on.

The **Stop** button on the **ACTIVE** run card →
`POST /intakes/{intake_id}/research/cancel` → the engine's `POST /runs/{run_id}/cancel`, which sets
`status='cancelled'`.

It is **idempotent**: cancelling an already-terminal run is a no-op that reports itself, returning
that run's own unchanged status. `202` carries `{research_run_id, status}`. **There is no `409` arm
and no attempt cap** — stopping a run is not an attempt at anything. A `404` means non-superadmin
caller, cross-tenant/missing intake, no run, or a run carrying no `tribunal_run_id`; existence is
always hidden, never `403`. A `502` is a seam or transport failure.

The intake row is **not** touched and stays `in_research` — that is correct and not a bug.
Resolving the run row to `cancelled` is by itself what makes `trigger_research`'s retry path
reachable again (`cancelled` is in `_RETRYABLE_RUN_STATUSES`; `running` is not, which is exactly why
a run stuck at `running` blocks its intake).

*Source: `backend/app/api/research_routes.py::cancel_research` (line 530) — its docstring is the
authority.*

### 2. PAUSING THE SERVICE IS **NOT** CANCELLING.

On **2026-07-27** the in-flight process kept going for **16 more minutes** after the service was
paused; terminating it required **deploying a new revision**; and the fresh worker that came up was
**seconds from RE-CLAIMING the run at full cost**.

`--min-instances=0` is **not** a protection either: `runs/worker.py`'s `while True:` loop **CLAIMS
FIRST and SLEEPS LAST** — `claim_one(session)` runs at the top of every iteration (`worker.py:665`)
and `await asyncio.sleep(POLL_INTERVAL_SECONDS)` is only reached on the **empty-queue** branch
(`worker.py:700`). **An empty queue is the only protection.**

*Source: `cancel_research` docstring (D-E) + `tribunal/nestor_pulse_sdk/runs/worker.py:661-701`.*

### 3. CANCELLATION IS **COOPERATIVE** — EXPECT A DELAY, NOT AN INSTANT HALT.

`runs/adapter.py` polls the cancel flag **at most once every `_CANCEL_CHECK_INTERVAL = 10.0`
seconds**, and only **between streamed events**. ADK cannot be force-killed mid-tool-call, so **cancel
latency is bounded by the gap between streamed events plus that 10-second window.** During a
deep-research call that gap can be long. A run that has not stopped after ten seconds is **not** a
failed cancel.

*Source: `tribunal/nestor_pulse_sdk/runs/adapter.py:132-144`.*

### 4. A KILLED PROCESS'S ROW GETS **RE-CLAIMED AND RE-EXECUTED AT FULL COST.**

| | |
|---|---|
| `NESTOR_WORKER_STALE_MINUTES` — ✅ **MEASURED LIVE at Q-PRE-3** | **`60`** — read from `tribunal-worker-00004-gnv`'s own env. This is **not** the code default quoted; it is the live value. |
| `NESTOR_WORKER_MAX_RECLAIMS` | **`2` — the CODE DEFAULT, and it genuinely applies.** ⚠ The variable is **ABSENT from the live worker env** (Q-PRE-3 read the whole set), so `worker.py:95`'s `int(os.environ.get("NESTOR_WORKER_MAX_RECLAIMS", "2"))` falls through to `2`. Labelled a default because it is one — nothing on the service sets it. |

*Both read by **agent-run `gcloud`, 2026-08-05T11:50Z**, session-verified as `tools@dotto.be` /
`project-cb01b861-cb4a-438d-b9a`. Not an operator read.*

**Say the consequence in words:** after that many minutes of **heartbeat silence**, the row is
re-claimed by a worker and the pipeline **runs again from the top, at full cost**, up to
`NESTOR_WORKER_MAX_RECLAIMS` times. Past that ceiling the reaper fails the run with a sentence
instead of starting it a further time.

**So killing the process is not stopping the run. CANCEL THE ROW, then RE-READ ITS STATUS to
confirm it is `cancelled`.**

*Source for the mechanism: `worker.py:80` (`STALE_RUN_MINUTES =
int(os.environ.get("NESTOR_WORKER_STALE_MINUTES", "60"))`) and `worker.py:95` (`MAX_RECLAIMS =
int(os.environ.get("NESTOR_WORKER_MAX_RECLAIMS", "2"))`).*

*⚠ **The two rows above are now DIFFERENT KINDS OF FACT and the table says which is which.**
`STALE_MINUTES = 60` is a **live read** of the deployed service (Q-PRE-3, 2026-08-05T11:50Z),
independently agreeing with what 15.8-14 recorded at deploy time. `MAX_RECLAIMS = 2` is a **code
default that applies because nothing sets the variable** — a fact about an absence, established by
reading the whole env rather than by grepping for the name and finding nothing. Do not collapse the
two into "both are 60/2 as expected".*

### 5. THE STOP BUTTON RENDERS **ONLY** ON THE **ACTIVE** CARD.

`RunActions.tsx` gates the whole Stop block on `{!isTerminal && …}` (line 220), where
`isTerminal = RESEARCH_TERMINAL.has(status) || status === "needs_input"` (line 103).

**A FALSE TERMINAL STATE THEREFORE REMOVES THE OPERATOR'S ABILITY TO STOP THE REAL RUN** — which is
exactly what the 2026-07-28 edge-401 defect did. **If the card goes terminal while the engine is
plainly still working, cancel via the API, not the UI.**

*Source: `frontend/src/components/research/RunActions.tsx:103, 220, 299`.*

---

## Run identity — filled by 15.8-15 at trigger time, NOT afterwards

⚠️ **Fill this block BEFORE the run finishes.** A run whose identity was captured after the fact is a
run whose evidence chain starts with a memory — V-01's own comparison document still reads
`TBD — not captured` for three of these fields.

> **`«OPERATOR: …»` is this file's fill marker and it is deliberately NOT the token `TBD`.** Task 1's
> automated gate counts `TBD` over the whole file, and 15.8-12's comparison table below uses `TBD`
> as its own "this number is OWED" marker. Two different meanings for one token is the two-authorities
> trap, so every slot **15.8-15** adds uses `«OPERATOR: …»`. **Every one must be replaced with a real
> value before this plan is complete.**
>
> ⚠️ **RECORDED DEFECT IN 15.8-15'S OWN TASK-1 GATE — do not "fix" it by deleting 15.8-12's markers.**
> Task 1's `<automated>` check is `test $(grep -c "TBD" "$P") -eq 0` over the **WHOLE FILE**, but
> 15.8-12 deliberately seeded **every** `15.8 observed` and `Verdict` cell with `TBD`, and those are
> filled by **Task 2 and Task 3**. So the check as written **cannot pass at Task 1 time** — it is the
> same whole-file-versus-section scoping defect 15.8-14 booked as its deviation #9. **Task 1's own
> acceptance criterion is correctly scoped** — *"the run identity block is filled with real values —
> no `TBD` in any row"* — and that is the form to verify: zero `TBD` **within the run-identity
> block**, which the `«OPERATOR: …»` convention makes true by construction. The whole-file form
> becomes satisfiable only after Task 3.

> # 🔴 THE RUN IS IN FLIGHT. This block was filled AT 18:42:26Z, WHILE IT RAN — not afterwards.
>
> **That is the whole point of Task 1(g), and it is the exact step V-01 failed**: V-01's comparison
> document still reads `TBD — not captured` for three of these fields, because its identity was
> reconstructed from memory after the fact. **An evidence chain that starts with a recollection is
> not an evidence chain.** Every value below was read from `tribunal.run` as `worker_user` via build
> **`4f814cda-5da2-4248-ae6b-d0bf081ab0f9`**, agent-run `gcloud`, 2026-08-05T18:42:26Z,
> session-verified `tools@dotto.be` / `project-cb01b861-cb4a-438d-b9a`.

| Field | Value |
|---|---|
| Tribunal `run_id` | ✅ **`368ff3a0-360c-4e91-9b9d-88a690ee7c8b`** |
| `research_run_id` (backend) | `«OPERATOR: paste — not obtainable from `tribunal.run`; it lives on the backend's research-run row»` |
| Intake id | `«OPERATOR: paste the backend intake UUID»` — the intake is **named `lukoil`**. ⚠ `tribunal.run.project_id` = `99d197ec-cb9e-4f3d-8029-a69accb1e840` is recorded below as its own field; **whether it IS the intake id was NOT established, so it is not written into this row.** |
| `tribunal.run.project_id` | ✅ **`99d197ec-cb9e-4f3d-8029-a69accb1e840`** (recorded verbatim; mapping to the intake not asserted) |
| Space / tenant id | ✅ **`5b84ea30-a3be-4047-a035-59a82e72c7b9`** |
| `created_at` (UTC) | ✅ **`2026-08-05 18:37:01.005504+00`** |
| Started (UTC) | ✅ **`2026-08-05 18:37:02.226988+00`** |
| Status at read time | 🔴 **`running`** (18:42:26Z) · `current_stage` = **`workshop`** |
| Completed (UTC) · terminal status | `«fill at completion»` — **expected ≈ 19:42Z if it tracks V-01's 65.1 min. That is an EXPECTATION, not a prediction to be recorded as a result.** |
| `worker_id` | ✅ `localhost-1-7f9ba04c` |
| `idempotency_key` | ✅ `f5fd7a8b-d7f4-572a-b6ca-e39d54f962e5` |
| `cost_usd_total` | **`NULL`** at 18:42Z — **not yet aggregated. This is NOT a $0 measurement**, and it must not be read as one (see the cost-floor discipline). |
| `comparison_id` | ✅ **`NULL` — a POSITIVE finding, see below** |
| `engine` | `tribunal` |
| Image `$SHA` | ✅ **`20260805-111647`** — ONE shared SHA, confirmed live at Q-PRE-1 (2026-08-05T11:50Z) |
| `tribunal-worker` revision | ✅ **`tribunal-worker-00004-gnv`** · digest `sha256:41c25cca…de89` — Q-PRE-1, EXACT MATCH |
| `tribunal-api` revision | ✅ **`tribunal-api-20260805-111647-115349`** · digest `sha256:50ebfa44…dfdf` — Q-PRE-1, EXACT MATCH |
| `nestor-api` revision | ✅ **`nestor-api-00044-8bz`** · digest `sha256:171f716fd46b62d28fe2997d5cf4e653027bdd15869a450fb9bbbf09a675ba4d` (CONFIRM-ONLY; prefix agreement only — see Q-PRE-1) |
| `nestor-frontend` revision | ✅ **`nestor-frontend-00028-q52`** · digest `sha256:541b2b52074c80e0925e097cabd7e6205488f62b47fe4efb636d3e827e943dea` (CONFIRM-ONLY; **new baseline**, no prior to compare) |
| TRIBUNAL alembic head | ⚠ **CARRIED, NOT RE-READ.** 15.8-14 proved `0018` on 2026-08-05 from `tribunal.tribunal_alembic_version`, pinned from both sides (a build asserting `0017` exited 81). **No read has been taken today.** ⭐ **It is now READABLE, cheaply:** Q-PRE-4 is paid and Q-PRE-2 proved `worker_user` reaches the DB through the same build recipe with printed output. `«optional: re-read before the trigger to close this»` |
| ANTHROPIC secret bound — `tribunal-api` | ✅ **`Nestor_Claude_Temp:latest`** — Q-PRE-3, measured 2026-08-05T11:52Z. NAME only; no value read. |
| ANTHROPIC secret bound — `tribunal-worker` | ✅ **`Nestor_Claude_Temp:latest`** — Q-PRE-3, measured 2026-08-05T11:50Z. NAME only; no value read. |
| SerpApi tier in force | `«OPERATOR: paste»` |
| Intake / brief domain | ✅ **LUKOIL BeNeLux — dynamic pricing, coffee, Germany-entry 2027.** Intake named **`lukoil`** (V-01's was `moetest`). **Fresh intake confirmed; the baseline is untouched.** ⚠ One bounded difference from V-01 — see § "The brief differs in ONE place" below. |
| `NESTOR_WORKER_STALE_MINUTES` · `NESTOR_OPENAI_DR_MODEL` · `NESTOR_TRIBUNAL_UNCAPPED` | ✅ `60` · `gpt-5.6-sol` · `1` — carried from Q-PRE-3, not re-derived |

### 🟢 HEALTH SIGNALS AT 18:42:26Z — all PASS, with the reasoning, not just the values

| Signal | Value | What it means |
|---|---|---|
| `started_at` − `created_at` | **1.22 s** | ✅ The worker claimed **essentially instantly.** **Expected and correct at `minScale = 1`** — this is the claims-first/sleeps-last loop behaving exactly as documented (`worker.py:665`), **not an anomaly**. A slow claim would have been the surprise. |
| `heartbeat_at` | `18:42:12.881918+00` — **~14 s stale at read time** | ✅ **Live and healthy.** The consequence, in the same breath: the row only becomes **re-claimable after 60 MINUTES** of heartbeat silence (`NESTOR_WORKER_STALE_MINUTES = 60`), up to `MAX_RECLAIMS = 2`. **14 seconds is four orders of magnitude inside that window.** |
| `reclaim_count` | **`0`** | ✅ **No re-execution has occurred.** This is the counter that would expose a silent second execution of paid pipeline. |
| `comparison_id` | **`NULL`** | ⭐ **A POSITIVE FINDING, not a missing value.** Spec § 8's `NESTOR_TRIBUNAL_WORKSHOP_TOURNAMENT=false` A/B control was **deliberately NOT used** — the NULL is the *evidence* that **no second paid run is being spent to compare the engine with itself.** D-W5-1's cross-run tables are what answer that question for free. |

### ✅ THE PRE-FLIGHT HELD — the direct answer to threat T-15.8-15-02

**Q-PRE-2 was RE-RUN at 18:37:55Z**, immediately after the trigger — build **`90c1b554`** — returning
**exit 93** with **`running|1`** and **`total runs visible: 12`**.

⛔ **DO NOT READ THAT EXIT 93 AS A FAILURE.** It is the gate **correctly reporting a live run**. The
arithmetic is what makes it conclusive: the 12:14Z baseline was **11 rows visible, 0 claimable**; this
read is **12 rows, 1 claimable**. **Exactly one row was added, and it is THIS run.**

**So no stale or leftover row was claimed at trigger time** — which is precisely the denial-of-wallet
scenario T-15.8-15-02 names, and the one that cost run `d6bb3aae` ~15 minutes of unattended paid
pipeline on 2026-07-28. **It also doubles as a THIRD independent proof that this gate can return
non-zero**, after the `_INVERT=1` run (`d5a3a251` → 93) and the 15.8-14 inverted run.

### ⚠ THE BRIEF DIFFERS FROM V-01 IN ONE PLACE — recorded NOW, before any result exists to rationalise it

**This is written while the run is in flight deliberately.** A known difference recorded after the
numbers arrive is indistinguishable from an excuse constructed to fit them.

| Element | V-01 | This run | Assessment |
|---|---|---|---|
| **The three research questions** | — | — | ✅ **VERBATIM IDENTICAL**, confirmed by direct comparison of both `brief` reads. Q1 retains the literal **`en/of`**, so **D-V01-5's facet-split condition (286/102/8/1) is reproduced deliberately.** |
| Q3's trailing punctuation | `margegroei ?` (space before `?`) | `margegroei?` | Cosmetic. **Recorded as observed; no known behavioural consequence.** Not claimed to be harmless — claimed to be *unexamined and small*. |
| **The DECISION block** | *"Duitsland lanceren in 2027, of eerst marktpositie Nederland consolideren/versterken met nieuwe shop-concepten, dynamic pricing en/of koffie-differentiatie."* | *"Investeert LUKOIL primair in geografische uitbreiding naar Duitsland (2027-launch), of in margegroei via operational excellence in Benelux (dynamic pricing, shop-herpositionering, premium koffie)?"* | ⚠ **REWORDED — same decision in substance, different words.** Both well under `_DECISION_MAX_CHARS = 400`. |

⛔ **WHY THE REWORDED DECISION MATTERS, AND FOR EXACTLY ONE ROW.** The decision block feeds
`_gate_decision_context` — **it is literally what the gate judges "load-bearing" against.** So this is
booked as a **known, bounded difference between the two runs**, and:

**A `not_load_bearing` delta must NEVER be attributed solely to the engine.** V-01 dropped 112 claims
that way. If this run's figure moves, the reworded decision is a **live candidate cause alongside the
five waves** — which is the attribution limit doing its job at row level rather than as a slogan.
It also interacts with **G-5**: the reworded text is a different length, so it feeds the 1200-char
cap measurement differently.

⚠ **Same diacritic caveat as the V-01 read, and it now has a second data point:** the log again
rendered `koffiestrategie?n`. **V-01's read showed the identical `?`**, so this is **consistently a
log-encoding artifact of this channel** and says nothing about what is stored. ⛔ **Do not record it
as data corruption in either run — and do not claim the stored value is definitely correct either.
It is simply UNVERIFIED through this channel.**

---

## PRE-FLIGHT — the ABORT gate (Task 1). Nothing here spends money.

**Composed 2026-08-05 by plan 15.8-15, before the trigger.** Its whole job is to make the spend safe
and legible. **Run these IN ORDER: every one of them is an ABORT gate and none of them costs
anything, so the cheapest ABORT comes first.**

> # ✅ ALL FIVE PRE-FLIGHT GATES PASS — 2026-08-05
>
> | Gate | Status | Evidence |
> |---|---|---|
> | **Q-PRE-0** identity + project | ✅ PASS | active `tools@dotto.be` / `project-cb01b861-cb4a-438d-b9a`; no `auth login` run |
> | **Q-PRE-1** revisions + digests | ✅ PASS | 4/4 at 100% traffic; **both Tribunal digests EXACT** vs 15.8-14 |
> | **Q-PRE-2** empty queue | ✅ PASS | build `bf0dcf3b`, `claimable rows: 0`; inverted arm `d5a3a251` → exit 93 |
> | **Q-PRE-3** worker env | ✅ PASS | `NESTOR_TRIBUNAL_WORKSHOP_*` count **0** — the validated code defaults will run |
> | **Q-PRE-4** yield read path | ✅ **PAID + PROVEN** | `logging.logWriter` granted 2026-08-05; build `bf0dcf3b` printed real output |
>
> **THE ABORT GATE IS CLEAR. Nothing below blocks the spend.** What remains in Task 1 is
> operator-only: **(f)** a fresh intake in a clean space on the V-01 brief domain, and **(g)** the
> trigger plus the run-identity block. **The STOP PROCEDURE at the top of this file is written and
> the run is stoppable before it is startable** — which was the precondition for all of this.

> ⛔ **ABORT ON ANY MISS. Do not proceed to the trigger and do not "note it and continue".** A stale
> or wrong-source deploy produces a confidently green run of the **wrong code**, and the stale-base
> trap is 23/23 in this repo. *(Kept in place: it is the standing rule, and it was satisfied rather
> than waived — every one of the five gates above was actually run.)*

> ⚠️ **PASTE RESULTS, NEVER CONNECTION STRINGS, ENV VALUES OR SECRET VALUES** (T-15.8-15-06).
> Secrets are recorded **by NAME**. The `DATABASE_URL_WORKER` password is parsed inside the build
> step and never echoed.

### Q-PRE-0 — ⚠ VERIFY ACCOUNT **AND** PROJECT. FIRST, AND AGAIN BEFORE EVERY COMMAND.

**Four gcloud accounts exist on this machine.** During the 15.8-14 deploy, `gcloud` demanded
reauthentication mid-run and the login that cleared it **authenticated a different account and
switched the project**; the project was corrected, a live call succeeded, and minutes later the state
had **silently reverted** because the browser login finalised asynchronously and overwrote the fix
(15.8-14 FINDING-E). `builds submit`, `run deploy`, `run jobs execute` and every deploy script
inherit the **ambient** account and project, and **none of them complains about a wrong one.**

```bash
gcloud config list account project
```

**Nestor Pulse is `tools@dotto.be` / project `project-cb01b861-cb4a-438d-b9a`.** Anything else is an
ABORT until corrected. Then export it once, and pass `--project` **explicitly on every command
below** — never rely on the ambient value:

```bash
export GOOGLE_PROJECT=project-cb01b861-cb4a-438d-b9a
export REGION=europe-west1
```

#### ✅ Q-PRE-0 — **PASS**, measured 2026-08-05T11:50:32Z

> ⚠️ **PROVENANCE, AND IT IS PART OF THE EVIDENCE: this was run by the AGENT's own authenticated
> `gcloud` session, NOT by the operator.** The plan's `user_setup` premise — *"the agent has no
> gcloud session"* — **is FALSE**, exactly as 15.8-14 found and corrected in its deviation #2. It is
> corrected here in place rather than worked around. ⛔ **The correction changes WHO CAN RUN a
> read-only command and NOTHING about WHO MAY CLEAR A GATE:** Q-PRE-2's queue check and Q-PRE-4's
> grant remain operator-owned, and no measured value below was supplied by anything but a command
> whose output is quoted.

| Read-back | Value |
|---|---|
| `gcloud auth list` | **four accounts present; ACTIVE = `tools@dotto.be`** |
| `account` | ✅ **`tools@dotto.be`** |
| `project` | ✅ **`project-cb01b861-cb4a-438d-b9a`** |
| `compute/region` · `compute/zone` | `europe-west1` · `europe-west1-b` |
| Was `gcloud auth login` run? | **NO** — so the FINDING-E asynchronous-revert trap **was not engaged**. This matters: the hazard is not "a wrong account exists", it is "a login finalising in the background overwrites a correction minutes later". No login, no window. |
| Verified by | **agent-run `gcloud`, 2026-08-05T11:50:32Z** |

---

### (a) PRE-CONDITIONS — READ, NOT RE-DERIVED

Read from `15.8-PRECONDITIONS.md` (15.8-08's dated record) on **2026-08-05** by plan 15.8-15.
**BOTH pre-conditions are SETTLED, with dates.** They are not re-derived here.

| Pre-condition | State as recorded | Date | Who |
|---|---|---|---|
| 1 — `openai/gpt-5.6-sol` cost row | ✅ **SETTLED** — ruling `published-rates`, applied in `audit/cost_prices.json` (commit `3597e87`). **Not** a null-rate entry, so the fabricated-`$0.00` failure mode is closed. | 2026-08-04 | operator, in session |
| 2 — audit-blob credential scan | ✅ **SETTLED** — redaction PASS. Clean over all 415 V-01 blobs; SCAN 1 a true zero against a positive control of **1,724 query-string URLs**; all 58 SCAN 2 hits decomposed to false positives. Rotation trigger did **not** fire. | 2026-08-05 | **the OPERATOR's own run** (the agent's earlier run was corroboration only, and an agent-written pass token was reverted as an overreach — commit `7c76636`) |

⚠️ **One thing that record does NOT claim, and this run must not read into it:** V-01 is clean
because **the exposure path was never triggered in that run**, not because it was proven safe
(FINDING 2 there: 0 of 415 blobs carry a non-empty `error`/`exception`/`traceback` field, so
`write_failure`'s path never fired). `_scrub_urls_in_value` (commit `5c04421`) is what protects
**this** run's ~400 new blobs — which is why Q-PRE-1's digest match is the mitigation for
T-15.8-15-01, not the scan.

**`Nestor_Claude_Temp`: CLOSED-BY-DECISION (operator, 2026-08-03) — DEFERRED TO GO-LIVE.** It appears
here **once**, and nowhere in this file as a gap. Do not rotate it early and do not re-raise it.

*Measured 2026-08-05T11:50Z (agent-run `gcloud`), recorded inside this one note rather than opening a
second mention:* `ANTHROPIC_API_KEY` on `tribunal-worker-00004-gnv` binds **`Nestor_Claude_Temp:latest`**,
so the accepted-risk key is the one this run will use — as decided. **Two de-risking facts that
matter for a 60+ minute paid run:** the secret has **no `expireTime`** and **exactly one enabled
version** (created `2026-07-27T07:39:17Z`), so **it cannot silently expire or roll mid-run**. And
**`Nestor_Claude` and `Nestor_Claude2` both still exist in the project**, so the eventual go-live
rewiring target is present and the deferral costs nothing to unwind. **None of this reopens the
decision** — it records that the accepted risk is bounded in the way the decision assumed.
*Both Tribunal services were confirmed to bind it (Q-PRE-3 Finding 4), so the deferral is uniform.*

> ⚠️ **HOW "APPEARS ONCE" IS TO BE READ — ruled 2026-08-05 and endorsed by the team lead, recorded
> so it is not re-litigated.** Task 1's acceptance criterion says *"`Nestor_Claude_Temp` appears
> once, marked CLOSED-BY-DECISION, and nowhere as a gap."* **It means RAISED AS AN ITEM once — not
> STRING-OCCURS-ONCE.** The name necessarily also appears in the Q-PRE-3 env read-back and the
> run-identity block, **because the same plan requires recording which ANTHROPIC secret each Tribunal
> service binds.** Those are configuration evidence, not a second raising of the deferred item.
>
> **This criterion is mis-scoped in exactly the way Task 1's whole-file `TBD` check is** — a
> file-wide string property standing in for a property of one section — and it is booked here for
> the same reason. ⛔ **Do NOT mangle the record to satisfy a literal grep count.** The substantive
> rule, which IS satisfied, is that it appears **nowhere as a gap**. (Task 1's `<automated>` check
> greps `-qi "CLOSED-BY-DECISION"` for *presence*, so the automation itself is unaffected; it is the
> criterion's prose that would mislead a literal reader.)

#### The cost-floor reading discipline — copied forward so Task 2 does not rediscover it

**State it once and NEVER present a floor as a total.**

- `compute()` returns **`None`** for a **missing key** and **`Decimal("0")`** for a key **present
  with null rates**. Different things — and `Decimal("0")` is **falsy**, so a truthiness check cannot
  tell them apart.
- **The openai branch of `end_call` NEVER sets `run.cost_pending`** (`dr_cost_pending` is set at
  exactly one site, inside the `handle.provider == "google"` branch). `cost_pending` is **not** the
  protection here.
- `SUM(cost_usd)` **skips NULLs**.

**THE RUN TOTAL IS A FLOOR FOR THREE MECHANICALLY DISTINCT REASONS** — they compound, and they are
not one issue restated:

1. **NULL `cost_usd` rows** silently skipped by `SUM` (V-01's `$53.48` is itself a floor for exactly
   this reason and must be compared as one);
2. **WR-02** — a failed or timed-out angle emits **no `assignment_yield` row at all**, so `SUM`
   misses it with **no column to explain the gap**;
3. **the 272K long-context meter** — above 272,000 input tokens `gpt-5.6-sol` re-bills the entire
   request at 2× input / 1.5× output, and `_rate()` has no notion of a tier, so the flat rates
   **under-report by 2×/1.5× above the boundary**. Operator ruled 2026-08-04: **record it, do NOT fix
   it.**

**THE QUERY THAT MAKES REASON 1 VISIBLE — use it LITERALLY, beside every cost total:**

```sql
SELECT count(*) FROM audit_log WHERE run_id = :id AND cost_usd IS NULL;
```

**A cost total recorded without this count is not a cost measurement.**

**⚠ OBLIGATION HANDED TO THIS PLAN BY 15.8-08 — do not lose it:** measure the **actual
`prompt_tokens`** on the openai deep-research calls against the **272,000** boundary and report
**whether it bound**. If it did, the figure is corrected with real measured data — not guessed.

| Measurement | Value |
|---|---|
| openai deep-research calls, max `prompt_tokens` observed | `«OPERATOR: paste — from Q-COST-3's per-model rows / audit_log»` |
| Did the 272K long-context meter bind? | `«OPERATOR: paste YES/NO»` |

---

### Q-PRE-1 — THE FOUR LIVE REVISIONS + IMAGE DIGESTS. **ABORT ON ANY MISMATCH.**

15.8-14 gated the image build on **tree-level** sentinels. **This gate is on the DEPLOYED
ARTICLE** — it gates the SPEND. Run all four:

> ⛔ **THE TAG-VERSUS-DIGEST TRAP — found while running this, 2026-08-05, and the block below is the
> CORRECTED form.** `spec.template.spec.containers[0].image` returns the **TAG**
> (`…/tribunal-api:20260805-111647`). **A tag is MUTABLE and comparing tags is NOT a digest
> comparison** — a tag can be re-pointed at a different image while every string in the record still
> matches, which is precisely the wrong-source-deploy spoofing this gate exists to catch
> (T-15.8-15-03). **The digest must come from the REVISION, via `status.imageDigest`.** An earlier
> draft of this block read the service-level image as if it were the digest; that would have produced
> a confident, meaningless match.

```bash
for SVC in tribunal-worker tribunal-api nestor-api nestor-frontend; do
  echo "=== $SVC ==="
  REV=$(gcloud run services describe "$SVC" \
    --region "$REGION" --project "$GOOGLE_PROJECT" \
    --format='value(status.latestReadyRevisionName)')
  echo "revision: $REV"
  gcloud run services describe "$SVC" \
    --region "$REGION" --project "$GOOGLE_PROJECT" \
    --format='value(status.traffic)'
  # the TAG — mutable, records WHAT WAS ASKED FOR, and is NOT the comparison
  gcloud run services describe "$SVC" \
    --region "$REGION" --project "$GOOGLE_PROJECT" \
    --format='value(spec.template.spec.containers[0].image)'
  # THE DIGEST — immutable, records WHAT IS ACTUALLY RUNNING. This is the comparison.
  gcloud run revisions describe "$REV" \
    --region "$REGION" --project "$GOOGLE_PROJECT" \
    --format='value(status.imageDigest)'
done
```

Also confirm **nothing has been deployed since 15.8-14** — the revision creation timestamps must be
the ones 15.8-14 recorded, not newer:

```bash
gcloud run revisions list --service tribunal-worker \
  --region "$REGION" --project "$GOOGLE_PROJECT" \
  --format='table(name,creationTimestamp,status.conditions[0].status)' --limit 5
gcloud run revisions list --service tribunal-api \
  --region "$REGION" --project "$GOOGLE_PROJECT" \
  --format='table(name,creationTimestamp,status.creationTimestamp)' --limit 5
```

#### THE BASELINE — quoted from 15.8-14's DEPLOY RECORD (`infra/DEPLOY-RUNBOOK.md` § Step 15.8.j)

**Compare character by character against this table, not against memory.**

| Service | Expected revision | Traffic | Expected image |
|---|---|---|---|
| `tribunal-worker` | `tribunal-worker-00004-gnv` | **100%** | `…/nestor/tribunal-worker@sha256:41c25cca733c44b5acb092e5020e771254a9396014ef5a1f5beb1f29ef00de89` |
| `tribunal-api` | `tribunal-api-20260805-111647-115349` | **100%** | `…/nestor/tribunal-api@sha256:50ebfa445942f229d2fd3117a221b08a27a1159ee145bfd34a6dc420f3b2dfdf` |
| `nestor-api` | `nestor-api-00044-8bz` — **CONFIRM-ONLY**, not rebuilt | **100%** | ⚠ **no full digest is on record** (see below) |
| `nestor-frontend` | `nestor-frontend-00028-q52` — **CONFIRM-ONLY**, not rebuilt | **100%** | ⚠ **no digest is on record at all** (see below) |

Shared image `$SHA`: **`20260805-111647`** — ONE SHA, there is no second. Neither Tribunal image
resolves to `:latest`; both are digest-pinned, and **`:latest` on either is an ABORT.**

> ⛔ **ANY SINGLE-CHARACTER DIGEST DIFFERENCE ON EITHER TRIBUNAL SERVICE IS AN ABORT — not a
> discrepancy to explain.** T-15.8-15-03: a stale or wrong-source deploy produces a confidently green
> run of code that is not the redesign, and this run is the only measurement the redesign gets.

> ⚠️ **THE TWO CONFIRM-ONLY SERVICES CANNOT BE DIGEST-COMPARED, AND THAT IS STATED RATHER THAN
> PAPERED OVER.** 15.8-14 did not rebuild them and recorded **no digest** for either; the only digest
> anywhere in the runbook for `nestor-api` is the 2026-07-28 § 15.2 record's **truncated**
> `sha256:171f716f…`, which cannot support a character-by-character comparison, and `nestor-frontend`
> has none at all. **For these two the assertable baseline is the REVISION NAME plus 100% traffic
> plus "no newer revision exists".** Record the digests observed now as a **new baseline for the next
> deploy** — they are **not** an abort trigger this time. Do not manufacture agreement with a
> truncated string.
>
> ✅ **DONE 2026-08-05T11:50Z — both digests are now on record**, in the Q-PRE-1 result table below.
> `nestor-api`'s full digest resolves the truncation for every future deploy; `nestor-frontend` has a
> baseline for the first time. **Neither was available to compare against TODAY**, which is why this
> paragraph stands rather than being deleted.

#### ✅ Q-PRE-1 — **PASS.** All four revisions at 100% traffic; BOTH Tribunal digests EXACT.

**Measured by agent-run `gcloud`, 2026-08-05T11:50Z**, session-verified as `tools@dotto.be` /
`project-cb01b861-cb4a-438d-b9a`. Digests read from `status.imageDigest` on the **revision**, not
from the service-level tag.

| Service | Revision (live) | Traffic | Digest (live) | vs 15.8-14 |
|---|---|---|---|---|
| `tribunal-worker` | `tribunal-worker-00004-gnv` | 100% `latestRevision` | `sha256:41c25cca733c44b5acb092e5020e771254a9396014ef5a1f5beb1f29ef00de89` | ✅ **EXACT MATCH** |
| `tribunal-api` | `tribunal-api-20260805-111647-115349` | 100% `latestRevision` | `sha256:50ebfa445942f229d2fd3117a221b08a27a1159ee145bfd34a6dc420f3b2dfdf` | ✅ **EXACT MATCH** |
| `nestor-api` | `nestor-api-00044-8bz` | 100% `latestRevision` | `sha256:171f716fd46b62d28fe2997d5cf4e653027bdd15869a450fb9bbbf09a675ba4d` | prefix `171f716f…` agrees with the **truncated** § 15.2 record; **the FULL digest is on record for the first time** |
| `nestor-frontend` | `nestor-frontend-00028-q52` | 100% `latestRevision` | `sha256:541b2b52074c80e0925e097cabd7e6205488f62b47fe4efb636d3e827e943dea` | **no prior baseline existed** — this RECORDS one |

**Nothing deployed since 15.8-14** — newest revision per service, all agreeing with the deploy record:

| Service | Newest revision | Created |
|---|---|---|
| `tribunal-worker` | `tribunal-worker-00004-gnv` | `2026-08-05T11:02:25.641113Z` |
| `tribunal-api` | `tribunal-api-20260805-111647-115349` | `2026-08-05T09:53:57.069990Z` |
| `nestor-api` | `nestor-api-00044-8bz` | `2026-07-28T11:28:23.529659Z` |
| `nestor-frontend` | `nestor-frontend-00028-q52` | `2026-07-28T08:25:25.748274Z` |

⚠️ **The two CONFIRM-ONLY rows are a WEAKER check than the two Tribunal rows, and the difference is
recorded rather than smoothed over.** `nestor-api` is a **prefix** agreement against a truncated
8-hex-character record — that is 32 bits, which is evidence but is not the character-by-character
comparison the Tribunal rows got. `nestor-frontend` had **no digest baseline at all** until this
read, so its row proves only *"this is what is running now"*, not *"this is what was deployed then"*.
**Neither is an ABORT this time** (15.8-14 did not rebuild them and the revision names and timestamps
match), and **both now give the next deploy a real baseline.** Booked in `## Gaps found`.

---

### Q-PRE-2 — THE QUEUE MUST BE **EMPTY** — ✅ **PROVEN EMPTY, 2026-08-05T12:14:04Z**

> # ⛔ THE WORKER IS RUNNING RIGHT NOW — `minScale = 1`, POLLING EVERY 2.0s
>
> **Measured at Q-PRE-3, 2026-08-05T11:50Z:** `tribunal-worker-00004-gnv` carries `minScale = 1`
> (15.8-14's unpause, never reverted) and `NESTOR_WORKER_POLL_INTERVAL = 2.0`.
>
> **This is not a theoretical concern and it is not the usual `--min-instances=0` argument.** There
> is no scaled-to-zero worker that has to boot before it can claim anything: **an instance is up and
> asking the queue for work every two seconds.** The loop **CLAIMS FIRST and SLEEPS LAST**
> (`worker.py:665` / `:700`), so anything that reaches `queued` is claimed within ~2s and executed at
> full cost.
>
> **AN EMPTY QUEUE IS THE ONLY PROTECTION THIS RUN HAS.** Q-PRE-2 is the gate that establishes it,
> and it has **NOT been run** (see the status note below).

**STATUS: ✅ RUN AND PASSED**, by a check **proven able to fail**. Because Q-PRE-4 was paid first,
this answer is **read from printed output**, not inferred from an exit status — the first time in
this project that has been true for this query.

```sql
SELECT status, count(*) FROM run WHERE status IN ('queued','running') GROUP BY status;
```

**Expect ZERO ROWS.** Run as **`worker_user`** — `run` carries the `worker_all` policy, so this needs
no tenant binding, and `app_user` without a bound `app.tenant_id` returns zero rows that are
**indistinguishable from an empty queue** (the most expensive false negative in this project).

⛔ **If any run is `queued` or `running`, resolve that ROW first.** The worker boots on deploy and on
health-check and **claims before it sleeps** — that is how run `d6bb3aae` re-executed ~15 minutes of
paid pipeline unattended on 2026-07-28. `--min-instances=0` is **not** a protection (stop procedure
point 2).

#### How to reach the database at all — the recipe that works, and the two traps

Both obvious paths are dead ends: `nestor-pg` has an **EMPTY** authorized-networks list, and the
Phase-14 lockdown rejects a plain invoker token on `tribunal-api`
(`{"error":"invalid internal caller token"}`). What works, changing no networking:

```bash
gcloud builds submit --no-source --config=<query>.yaml \
  --project="$GOOGLE_PROJECT" \
  --service-account=projects/$GOOGLE_PROJECT/serviceAccounts/nestor-run@$GOOGLE_PROJECT.iam.gserviceaccount.com
```

The step downloads `cloud-sql-proxy`, points it at
`project-cb01b861-cb4a-438d-b9a:europe-west1:nestor-pg`, and runs `psql` against `127.0.0.1:5432`.

- **`--no-source` is REQUIRED.** `nestor-run@` cannot read the Cloud Build source bucket; a sourced
  build dies with `storage.objects.get denied` before the step ever runs.
- **Connect as `worker_user`, NEVER `app_user`.** The password is inside `DATABASE_URL_WORKER` —
  **parse it, never echo it.**
- ⚠ **Until Q-PRE-4 is paid, this channel carries a BOOLEAN IN THE EXIT STATUS, not numbers**
  (`nestor-run@` holds no `roles/logging.logWriter`, so build stdout is LOST and the build goes
  SUCCESS with an empty `REMOTE BUILD OUTPUT`). **Do not read a green build as an empty queue.** Fold
  the anti-vacuity arms into the exit status — e.g. `exit 91` if `SELECT count(*) FROM tribunal.run`
  is 0 (RLS hiding everything), `exit 92` if the known-cancelled control row `d6bb3aae-33e7-49fd-aa35-57dc529e05b3`
  is not visible as `cancelled`, `exit 93` if anything is claimable — **and prove the gate can fail**
  by re-running once with the claimable arm inverted.
- ⚠ **`gcloud builds submit … | tail` returns the PIPE's exit code, so a FAILED build reports exit
  0.** Read the build text via `gcloud builds describe <id>`. Never a shell status. And an **EXPIRED**
  build is visually identical to QUEUED and is **not a result**.

#### ✅ Q-PRE-2 RESULT — build `bf0dcf3b-95a3-4da2-bdbd-89b3dfcbfa65`, SUCCESS / exit 0, 2026-08-05T12:14:04Z

**Verbatim printed output, including the zeros:**

```
resolved database name: 'nestor'
=== connected as worker_user (password not shown) ===
total runs visible: 11
control row d6bb3aae status: 'cancelled'
=== Q-PRE-2: SELECT status, count(*) FROM run WHERE status IN ('queued','running') GROUP BY status ===
=== (no rows above means an empty queue) ===
claimable rows: 0
PASS: queue is EMPTY, and arms 91 and 92 passed first.
```

**Inverted reachability proof:** build **`d5a3a251-45f5-4223-b564-7ca0ec245a14`** with `_INVERT=1`
→ **exit 93**, as required.

> ⭐ **WHY THE INVERTED RUN IS WHAT MAKES THE ZERO MEAN ANYTHING.** Arm 93 is reachable **only after
> arms 91 and 92 pass** — the script exits earlier otherwise. So **exit 93 independently re-proves
> two things the empty result could not prove on its own**: that `tribunal.run` is visible to
> `worker_user` at all (**11 rows** — RLS is not silently hiding the table), and that control row
> `d6bb3aae` reads **`cancelled`** as expected. **A check that cannot fail is not a check**, and a
> zero from a query that returns zero for the wrong reason is the most expensive false negative in
> this project.

| Read-back | Value |
|---|---|
| Q-PRE-2 output, verbatim including zeros | ✅ above — **`claimable rows: 0`**, no rows in `('queued','running')` |
| Build id | ✅ `bf0dcf3b-95a3-4da2-bdbd-89b3dfcbfa65` — SUCCESS, exit 0 |
| Inverted-arm reachability proof | ✅ `d5a3a251-45f5-4223-b564-7ca0ec245a14`, `_INVERT=1` → **exit 93** |
| Non-vacuity | ✅ 11 runs visible; control row `d6bb3aae` = `cancelled` |
| Measured by | **agent-run `gcloud`, 2026-08-05T12:12–12:16Z**, session-verified |

⚠️ **The empty queue is a POINT-IN-TIME fact, and `minScale = 1` means the worker is still polling.**
Nothing may be enqueued between this read and the trigger. If any time passes, or anything is
touched, **re-run this build before triggering** — it is free.

---

### Q-PRE-3 — WORKER ENV READ-BACK

```bash
gcloud run services describe tribunal-worker \
  --region "$REGION" --project "$GOOGLE_PROJECT" \
  --format='value(spec.template.spec.containers[0].env)' \
  | tr ',' '\n' | grep -E 'NESTOR_WORKER|ANTHROPIC|SERP'
```

Record `NESTOR_WORKER_STALE_MINUTES` **verbatim** (it feeds stop-procedure point 4),
`NESTOR_WORKER_MAX_RECLAIMS`, and **which secret `ANTHROPIC_API_KEY` binds — BY NAME. Never a
value.**

⭐ **Also assert the POSITIVE finding 15.8-14 recorded: every `NESTOR_TRIBUNAL_WORKSHOP_*` is
ABSENT — grep count `0`.** The Wave-4 validated configuration **is the code defaults** in
`workshop_loop.py`. **If any one of them is set, this run is measuring a configuration nobody
validated** — treat that as an ABORT, not a note.

#### ✅ Q-PRE-3 — **PASS on the abort criterion**, measured 2026-08-05T11:50Z, with three findings

**Measured by agent-run `gcloud`** against `tribunal-worker-00004-gnv`. Full env, verbatim —
**secret NAMES only, no value was read, echoed or recorded:**

```
NESTOR_ENV                = prod
NESTOR_WORKER_POLL_INTERVAL = 2.0
NESTOR_WORKER_STALE_MINUTES = 60
NESTOR_TRIBUNAL_UNCAPPED  = 1
NESTOR_OPENAI_DR_MODEL    = gpt-5.6-sol
DATABASE_URL      -> SECRET DATABASE_URL_WORKER:latest
AUDIT_GCS_BUCKET  -> SECRET AUDIT_GCS_BUCKET:latest
ANTHROPIC_API_KEY -> SECRET Nestor_Claude_Temp:latest
GOOGLE_API_KEY    -> SECRET Nestor_Gemini:latest
OPENAI_API_KEY    -> SECRET Nestor_OpenAI:latest
SERPAPI_API_KEY   -> SECRET Nestor_SERP:latest
minScale = 1, maxScale = 5
```

| Read-back | Value |
|---|---|
| `NESTOR_WORKER_STALE_MINUTES` | ✅ **`60`** — measured live |
| `NESTOR_WORKER_MAX_RECLAIMS` | **ABSENT from the env** → the code default **`2`** applies (`worker.py:95`) |
| `ANTHROPIC_API_KEY` ← secret NAME (`tribunal-worker`) | **`Nestor_Claude_Temp:latest`** |
| `ANTHROPIC_API_KEY` ← secret NAME (`tribunal-api`) | ✅ **`Nestor_Claude_Temp:latest`** — measured 2026-08-05T11:52Z by a second, separate read (see below) |
| `SERPAPI_API_KEY` ← secret NAME | **`Nestor_SERP:latest`** |
| `NESTOR_TRIBUNAL_WORKSHOP_*` count (**must be 0**) | ✅ **`0`** — **ABORT CRITERION PASSES** |
| Full plain-env set, verbatim | above |

**FINDING 1 — the tournament A/B control is unset, and that is the POSITIVE result.**
`NESTOR_TRIBUNAL_WORKSHOP_*` count is **0**, so the Wave-4 **validated configuration — the code
defaults in `workshop_loop.py`** (`_FLOOR_PER_QUESTION` 5, `_CROSS_CUTTING_SLOTS` 2,
`_LOOP_MIN_ROUNDS` 4, `_LOOP_MAX_ROUNDS` 10) — is what will run. Had one been set, this run would be
measuring a configuration nobody validated. Spec § 8's
`NESTOR_TRIBUNAL_WORKSHOP_TOURNAMENT=false` control is therefore deliberately **not** in force.

**FINDING 2 — `NESTOR_WORKER_STALE_MINUTES = 60` is now MEASURED, not inferred.** Stop-procedure
point 4 above carries the real value. `NESTOR_WORKER_MAX_RECLAIMS` is a different kind of fact: it is
**absent**, so the code default genuinely applies — established by reading the whole env, not by a
grep that found nothing.

**FINDING 3 — the settled cost row applies to the model actually configured.**
`NESTOR_OPENAI_DR_MODEL = gpt-5.6-sol` is exactly the model whose rate pre-condition 1 settled as
`published-rates` on 2026-08-04 (commit `3597e87`). **The tie-up is worth stating because the
pre-condition would have been vacuous otherwise** — a settled rate for a model the service does not
use protects nothing. It also means the **272K long-context meter is live for this run**, so the
`prompt_tokens` measurement 15.8-08 handed to this plan is owed against real traffic.

**FINDING 4 — BOTH Tribunal services bind the SAME Anthropic secret. Measured, not carried.**
A second read at **2026-08-05T11:52Z** (agent-run `gcloud`, same session-verified identity) closed
the one row that was still a carried record:

```
gcloud run services describe tribunal-api --region europe-west1 --project <P> \
  --format='value(spec.template.spec.containers[0].env)'

{'name': 'ANTHROPIC_API_KEY', 'valueFrom': {'secretKeyRef': {'key': 'latest', 'name': 'Nestor_Claude_Temp'}}}
```

So `tribunal-worker` **and** `tribunal-api` both bind **`Nestor_Claude_Temp:latest`**.
**The consistency is the point:** there is **no split-brain between the two services on the Anthropic
key**, so the closed-by-decision item is **uniform rather than partial** — the accepted risk covers
one key on both services, not two keys with one of them unexamined. A split would have meant the
2026-08-03 deferral silently applied to only half the engine.

> ⛔ **`minScale = 1` — THE WORKER IS LIVE AND POLLING EVERY `2.0` SECONDS RIGHT NOW.** 15.8-14's
> unpause set it and it has not been reverted. **This makes Q-PRE-2 urgent rather than ceremonial:
> an empty queue is the ONLY protection, and there is no idle-worker grace period to fall back on.**
> See the banner at Q-PRE-2 above.

---

### Q-PRE-4 — ⛔ THE YIELD-TABLE READ PATH. **SETTLE IT BEFORE THE TRIGGER, NOT AFTER.**

> # ✅ STATUS: **PAID AND PROVEN — 2026-08-05.** THE GATE IS CLEAR.
>
> **The grant was run by the OPERATOR personally at ~2026-08-05T12:05Z.** Read-back in the returned
> policy confirms `nestor-run@` now sits in the `roles/logging.logWriter` binding alongside
> `1055853212188@cloudbuild` and `nestor-pulse-runtime@`.
>
> ⏳ **GRANT DATE FOR REVOCATION: 2026-08-05.** T-15.8-15-07 accepted this as reversible *on the
> condition that the grant and its date are recorded so it can be revoked after the run.* **That is
> this line. Revoke post-run.**
>
> **The proof is VISIBLE OUTPUT, not a green build** — build
> **`bf0dcf3b-95a3-4da2-bdbd-89b3dfcbfa65`** printed real query results into `gcloud builds log`.
> Before the grant, stdout was lost and the same build would have gone SUCCESS with an empty
> `REMOTE BUILD OUTPUT`. **That difference IS the gate.**
>
> **The working invocation, verbatim:**
>
> ```bash
> gcloud builds submit --no-source --config=queue-check.yaml \
>   --project=project-cb01b861-cb4a-438d-b9a \
>   --service-account=projects/project-cb01b861-cb4a-438d-b9a/serviceAccounts/nestor-run@project-cb01b861-cb4a-438d-b9a.iam.gserviceaccount.com
> ```
>
> ⚠ **The config must carry `options.logging: CLOUD_LOGGING_ONLY`, NOT `NONE`.** 15.8-14 used
> `NONE`, which is precisely why every one of its answers had to ride in an exit status and be pinned
> by **paired** runs. **The grant alone is not sufficient — a `NONE` config makes it look ineffective
> when it is merely unused.**

> ⚠️ **CORRECTION TO A RUNBOOK PREMISE — found while paying this gate, 2026-08-05.**
> The runbook says `nestor-run@` *"already holds `secretAccessor` on `DATABASE_URL_WORKER`"*. **That
> is TRUE, but only at SECRET SCOPE.** `nestor-run@` is **NOT** in the project-level
> `roles/secretmanager.secretAccessor` binding; the access is granted on the secret itself, verified
> by `gcloud secrets get-iam-policy DATABASE_URL_WORKER` → members `nestor-run@` **and**
> `tribunal-run@` on `roles/secretmanager.secretAccessor`.
>
> ⛔ **A PROJECT-LEVEL CHECK THEREFORE READS AS A FALSE NEGATIVE** and would make the whole recipe
> look broken when it works. Anyone verifying this must query the **secret's** IAM policy, not the
> project's. Booked as a runbook-premise correction, **not repaired in the runbook** — record, do not
> repair.

**15.8-14 said the same thing in writing:** *"`roles/logging.logWriter` was NOT granted. That grant
is 15.8-15's assigned blocking pre-flight gate (Q-PRE-4) and this plan does not own it."*

`assignment_yield` and `workshop_round_yield` have **no read endpoint, no seam verb and no UI
surface** — nothing in 15.8-05/09/10 adds one (D-W5-18). So the only credential-free DB path returns
**a boolean in an exit status, not a table of numbers**, and **Wave 5's own § 8 verification
criterion becomes unreadable after ~$45 has been spent.** That is the fourth instance in this project
of data produced and unreadable.

**1. Grant** (one reversible project-level IAM grant; no networking change, no new secret access —
T-15.8-15-07, disposition **accept**):

```bash
gcloud projects add-iam-policy-binding "$GOOGLE_PROJECT" \
  --member="serviceAccount:nestor-run@$GOOGLE_PROJECT.iam.gserviceaccount.com" \
  --role="roles/logging.logWriter"
```

**2. Verify by read-back** — do not trust the grant command's own output:

```bash
gcloud projects get-iam-policy "$GOOGLE_PROJECT" \
  --flatten="bindings[].members" \
  --filter="bindings.members:nestor-run@$GOOGLE_PROJECT.iam.gserviceaccount.com" \
  --format="table(bindings.role)"
```

**3. PROVE IT with a trivial `SELECT 1` build whose OUTPUT IS ACTUALLY VISIBLE IN THE BUILD LOG.**
A green build is **not** the proof — the proof is **reading the number `1` in
`gcloud builds log <id>`**. Note that the queue-check config sets `options.logging: NONE`; the proof
build must **not**, or the grant will look ineffective when it is merely unused.

**Record the working invocation VERBATIM below.** Discovering after the run that the numbers cannot
be read is an unrecoverable failure of this phase's own verification criterion.

| Read-back | Value |
|---|---|
| Grant made? · date (so it can be revoked after the run) | ✅ **YES — `roles/logging.logWriter` on `nestor-run@`, granted 2026-08-05 (~12:05Z) BY THE OPERATOR.** ⏳ **REVOKE AFTER THE RUN.** |
| `get-iam-policy` read-back showing `roles/logging.logWriter` | ✅ `nestor-run@` present in the binding, alongside `1055853212188@cloudbuild` and `nestor-pulse-runtime@` |
| Proof build id | ✅ **`bf0dcf3b-95a3-4da2-bdbd-89b3dfcbfa65`** — SUCCESS, 2026-08-05T12:14:04Z |
| **The VISIBLE OUTPUT** — the actual criterion | ✅ **real query results printed into `gcloud builds log`** (quoted in full at Q-PRE-2 below). Before the grant, stdout was lost. |
| The working invocation, verbatim | ✅ recorded above, with `options.logging: CLOUD_LOGGING_ONLY` |
| Measured by | operator ran the grant; **agent-run `gcloud` 2026-08-05T12:12–12:16Z** for everything after it |

> ⭐ **WHAT THIS UNLOCKS, AND IT IS THE WHOLE REASON THE GATE EXISTED:** `assignment_yield` and
> `workshop_round_yield` still have **no endpoint, no seam verb and no UI** (D-W5-18 is unchanged) —
> but the Cloud Build path now returns **a table of numbers instead of a boolean in an exit status.**
> **Wave 5's own § 8 criterion is READABLE after the money is spent.** Task 2's entire measurement
> query set depends on this and would have been unfillable without it.

---

### (f) THE SUBJECT — a FRESH intake in a CLEAN space

Reproduce the V-01 brief domain: **LUKOIL BeNeLux — dynamic pricing, coffee, Germany-entry 2027.**

**The domain is not decoration.** The coffee questions are what make Wave 1's 278-claim recovery
observable, and the three client questions are what make the winner arithmetic
(`5 × 3 + 2 = 17`) comparable to V-01's.

⛔ **NOT the smoke tenant `1464b60d`** — it is on the cleanup backlog.

#### ✅ V-01'S ACTUAL BRIEF — READ FROM THE DATABASE, so the new intake reproduces the real shape

**Read from `tribunal.run.brief` for run `7dcf51d5-1153-4374-b444-c25d17eeea01`** via the same Cloud
Build recipe — build **`ce96f123-6862-4daa-a90d-800a719cdad2`**, 2026-08-05T17:52Z, agent-run,
session-verified identity. **This replaces "reproduce the domain" with the real text**, so the new
intake is not built from a reconstruction of a description.

⭐ **V-01 RAN ON AN INTAKE NAMED `moetest`** — its `brief` opens `Deep research for moetest.`
**It was already a test intake.** Worth stating plainly, because it reframes the rule: **the
fresh-intake requirement is not about avoiding a real client — it is about not contaminating the
baseline.**

**THREE client questions — confirmed from the row, and this is what makes `5 × 3 + 2 = 17`
comparable:**

| # | Topic | Stakes (`stage_detail.intake`) |
|---|---|---|
| Q1 | dynamic pricing / fuel retailers Europe | **high** |
| Q2 | koffiestrategieën, Benelux petroliers | **med** |
| Q3 | supermarktformule / convenience-concepten | **high** |

**Decision block, 155 chars:** *"Duitsland lanceren in 2027, of eerst marktpositie Nederland
consolideren/versterken met nieuwe shop-concepten, dynamic pricing en/of koffie-differentiatie."*

> ⚠️ **PROVENANCE CAVEAT THAT MUST RIDE WITH THIS TEXT WHEREVER IT IS QUOTED.** The build log
> **mangled non-ASCII** — it returned `koffiestrategie?n`. The Dutch diacritics above were
> **restored by reading the words**. **The question text is VERBATIM; the DIACRITICS ARE A
> RECONSTRUCTION.** Do not present the restored characters as a transcript. If exact bytes ever
> matter, re-read the column through a UTF-8-safe channel.

⚠️ **Q1 CONTAINS THE LITERAL `en/of`** — "brandstof en/of shopproducten" — the string implicated in
**D-V01-5's facet split (286 / 102 / 8 / 1)**. **It is being reproduced AS-IS, deliberately.**
Rephrasing it to read better would make the new run's facet distribution accidentally divergent and
destroy the comparison. **Copy it, do not improve it.**

#### Two confirmations from the same read, captured because they cost nothing

**1. The `<baseline>` block is independently re-confirmed FROM THE LIVE ROW**, not from the findings
document. `verification_summary` on V-01 reads:

```json
{"both": 0, "kept": 293, "checked": 293, "dropped": 133, "distilled": 426,
 "gate_errors": 153, "not_falsifiable": 21, "not_load_bearing": 112,
 "verification_degraded": false, …}
```

**Every figure matches.** That matters more than it looks: this file's standing instruction is that
V-01's numbers are quoted from named documents, and three of that document's own claims had to be
withdrawn. **Now the load-bearing ones have a second, independent source — the database row itself.**

**2. `tribunal.run` carries `cost_usd_total`, `comparison_id`, `cost_pending`, `heartbeat_at`,
`reclaim_count`.** Directly relevant to Task 2's required reconciliation of
`SUM(assignment_yield.cost_usd)` against `run.cost_usd_total` — **the column exists, so that
arithmetic is takeable.**

#### The new intake — to be confirmed by the operator

| Confirmation | Value |
|---|---|
| Space is FRESH and CLEAN (not `1464b60d`) | `«OPERATOR: paste space id + confirm»` |
| Intake is FRESH | `«OPERATOR: paste intake id»` |
| Three client questions, quoted — reproducing the shape above, `en/of` included | `«OPERATOR: paste»` |
| Each question's LENGTH in chars (cap is 400; V-01's Q3 cleared by only 48) | `«OPERATOR: paste all three»` |
| Decision block + its length | `«OPERATOR: paste»` |

---

### (h) ⛔ DO NOT FIX ANYTHING

If a defect surfaces while the run is in flight, **RECORD it in `## Gaps found` and let the run
finish.** `gate_errors: 153` in particular is deferred **because fixing it changes which claims reach
paid verification** — the one variable this run exists to hold still (T-15.8-15-05).

**Repairing anything mid-run destroys the only measurement the redesign will ever get.**

---

## Three standing instructions — read these before filling anything

**1. JUDGE THE ENGINE FROM THE DELIVERED REPORT.** That is the `output` row with
`format='markdown'` — **not the claim table, and not the logs.** Three of V-01's own findings-doc
claims had to be withdrawn because they judged a working stage from an intermediate artifact. Where a
row below asks for a sentence, quote the sentence **from the delivered report**.

**2. THE ATTRIBUTION LIMIT — stated once, here, and not re-argued in the write-up.** Five waves land
in one deploy, so an unexpected result **cannot be attributed to a single change.** The operator
ruled this on 2026-07-29 having been told the trade-off. When a number surprises, the honest write-up
names the **candidate** waves; it does not pick one and it does not re-open the ruling.

**3. EVERY V-01 FIGURE HERE IS QUOTED FROM A NAMED DOCUMENT**, and every row carries the document it
came from. **A figure without a source is not a baseline.** Sources are
`docs/tribunal-run-reports/run-20260728-7dcf51d5-` + `V01-FINDINGS.md` / `DIAGNOSTICS.md` /
`OPEN-QUESTIONS.md`, for run `7dcf51d5-1153-4374-b444-c25d17eeea01` (2026-07-28).

> ⛔ **DO NOT TICK `ENGINE-REDESIGN-SPEC.md` § 8's STRUCK-THROUGH WAVE 2 AND WAVE 3 ROWS.**
> Wave 2's mixed-group test **cannot be run** — D-W3-5 made mandate groups strict, so the mixed group
> the test needs cannot be constructed. Wave 3 issues **9–15** calls, not 15 — D-W3-1 made 5 groups a
> hard **ceiling, not a target**. **Both waves shipped correctly; only the checklist is wrong.**
> Ticking those rows would record a phantom pass.

> ⚠️ **Where a feature did not exist in V-01, the baseline cell reads
> `n/a - feature did not exist`, NEVER `0`.** A zero would read as a measurement, and it would be a
> measurement of something that could not have happened.

---

## Wave 1 — extraction repair (phase 15.4)

*What this wave was supposed to change: the separator-tolerant distiller split, a LOUD drop warning,
the fact-list retry, cite-marker recovery, grounding-redirect resolution (alembic 0016), the
`gpt-5.6-sol` cost row.*

| Measure | V-01 observed | V-01 source | 15.8 observed | Verdict |
|---|---|---|---|---|
| **Wave 1** — coffee-facet claims persisted | **0** — and 278 well-formed coffee claims were parsed away on a literal `<TAB>`, across two calls (`e9a168b5`, 141 lines; `fe418029`, 137 lines) that each kept **zero** | DIAGNOSTICS § 1b | TBD | TBD |
| **Wave 1** — total claims persisted | **396** (426 distilled → 293 gate-kept → 133 dropped; residual 0) | V01-FINDINGS "What the run produced" + OPEN-QUESTIONS Q1 | TBD | TBD |
| **Wave 1** — the delivered report's coffee section, **quoted** | it told the client the Benelux data *"geeft geen volledig beeld"* — while the engine had 278 coffee claims it had silently dropped | DIAGNOSTICS § 1b | TBD | TBD |
| **Wave 1** — distiller calls that fell through to the fallback path | `n/a - feature did not exist` (no tolerant split, so no fallback existed to count) | DIAGNOSTICS § 1b | TBD | TBD |
| **Wave 1** — fact-list retries attempted vs recovered | `n/a - feature did not exist` | V01-FINDINGS (the retry is Wave 1's addition) | TBD | TBD |
| **Wave 1** — the ZERO-parsed-claims WARNING | `n/a - feature did not exist` — **278 lines, 278 silent drops, and the only log line for it did not exist** | DIAGNOSTICS § 1b | TBD | TBD |
| **Wave 1** — unique grounding redirects · how many resolved | **225** unique · **resolution not attempted** (225/225 were *resolvable* on 2026-07-28; 0016 is what makes the resolved column exist at all) | V01-FINDINGS (citations) + `0016`'s own docstring | TBD | TBD |
| **Wave 1** — claims with **no** source | **84** of 396 | V01-FINDINGS "What the run produced" | TBD | TBD |
| **Wave 1** — `found_by` split | gemini 198 · claude 107 · openai 74 · own 17 | V01-FINDINGS "What the run produced" | TBD | TBD |

> ⚠️ **The `own` stream is GONE by design.** Wave 3 removed it from the rotation (D-R5, spec § 9), so
> a 15.8 `found_by` with no `own` column is **correct, not a regression**. The denominator for
> provider coverage is **3 of 3**, not "≥3 of 4". This also resolves `15.2-UAT.md`'s permanently
> pending `own_research` stage expectation — mark it **N/A — removed by design**.

---

## Waves 2–3 — claim attribution and dispatch (phases 15.5, 15.6)

*What these waves were supposed to change: `sub_question` / `corroboration_key` / `as_of` on the claim
row (alembic 0017); dispatch by topic — at most 5 groups × 3 providers, `own` dropped — plus the
discovery bracket.*

**These two waves share rows because neither is readable alone.** `0017`'s own docstring says the key
would be NULL for ~12 of 15 winners on a wave-2-only run and *"fills up in phase 15.6 when every
group goes to every provider"*.

| Measure | V-01 observed | V-01 source | 15.8 observed | Verdict |
|---|---|---|---|---|
| **Wave 2** — angles carrying a non-empty `corroboration_key`, over total angles | 12 of 19 angles carried `corroboration: true` with keys `w01`/`w02`/`w03`; ranks 4–10 went to one provider each | V01-FINDINGS "Dispatch" | TBD | TBD |
| **Wave 3** — `assignment_yield.group_id` non-empty on **every** row | `n/a - feature did not exist` | (the table is Wave 5's) | TBD | TBD |
| **Wave 2** — claim-level `corroboration_key` NULLs, reconciled against the distiller-fallback count | `n/a - feature did not exist` (the column did not exist) | 0017 docstring | TBD | TBD |
| **Wave 2** — `verification_summary.both` | **0** — and record **why**: 396 claims produced **396 distinct exact-string keys**, so corroboration was **arithmetically impossible**, not merely absent | V01-FINDINGS D-V01-1 | TBD | TBD |
| **Wave 3** — groups issued · calls issued | 19 angles; deep research issued as gemini 5 async · openai 5 async · claude 5 inline · own 4 angles → 15 SerpApi searches (2 own angles failed) | V01-FINDINGS "Dispatch" | TBD | TBD |
| **Wave 3** — discovery-bracket questions the client did not ask: **count AND one quoted** | `n/a - feature did not exist` | (the bracket is Wave 3's) | TBD | TBD |
| **Wave 3 / D7** — every dispatched winner reaches the angle-query builder with **non-empty `langs`**, including coverage-repaired winners | `n/a - feature did not exist` | 15.7-VERIFICATION (the D7 gap) | TBD | TBD |

> ⛔ **MEASURE CORROBORATION AT THE ANGLE LEVEL, NOT THE CLAIM LEVEL.** *"`corroboration_key` on every
> angle"* is the real criterion. At the **claim** level it **FAILS BY DESIGN**: the distiller-fallback
> path cannot carry attribution — `claim_distiller` builds units as `(provider_name, chunk_text)`, so
> the angle is gone before distillation and those claims are **permanently NULL**. **A naive
> "zero NULLs" check would report a design decision as a regression.** Reconcile claim-level NULLs
> against the distiller-fallback count instead.

> ⚠️ **The expected call band is 9–15**, from ≤5 groups (`_D6_MAX_GROUPS` = 5) × 3 providers. **A
> number outside that band in EITHER direction is the finding** — too few means groups collapsed, too
> many means the ceiling did not bind.

> ⚠️ **There are THREE locales in this tree, not four.** `frontend/src/locales` holds `en`, `fr`, `nl`
> and `workshop_rank._LANGS_MAX` is **3**. There is no four-language contract to tick. And in the
> default per-question mode the coverage-repair path **may not be exercised at all** — that is an
> honest **"not exercised"**, **NOT a pass.**

---

## Wave 4 — the creative workshop loop (phase 15.7)

*What this wave was supposed to change: an evolving candidate population with a KEEP/WEAK/KILL
critique, a catch-up match schedule for newcomers, and an exit rule — replacing the straight line.*

| Measure | V-01 observed | V-01 source | 15.8 observed | Verdict |
|---|---|---|---|---|
| **Wave 4** — loop rounds actually run | `n/a - feature did not exist` — **pass condition is `>= 4`**, because `_LOOP_MIN_ROUNDS` = 4 now floors it after the round-1 vacuous-exit defect. **A run reporting exactly 1 round means the floor did not take.** `_LOOP_MAX_ROUNDS` = 10 is a **ceiling, not a target** | `workshop_loop.py` config block | TBD | TBD |
| **Wave 4** — winner count and shape | **10** winners over 3 client questions (Q1 4 · Q2 3 · Q3 3). The shipped configuration is `_FLOOR_PER_QUESTION` = 5 per client question plus `_CROSS_CUTTING_SLOTS` = 2, **so a three-question brief expects 17** | V01-FINDINGS "Tournament" + `workshop_loop.py` | TBD | TBD |
| **Wave 4** — winners flagged `WEAK` in the final set | **9 of 10** (only rank 1 was `KEEP`) — ⚠️ **see the caveat below; treat as INDICATIVE, not settled** | V01-FINDINGS "Tournament" | TBD | TBD |
| **Wave 4** — candidates killed | **0** (17 candidates ranked, 0 killed) — the critic annotated and nothing acted on it | V01-FINDINGS "Tournament" | TBD | TBD |
| **Wave 4** — new entrants into the top N at each round, read from `workshop_round_yield` | `n/a - feature did not exist` | (the loop and the table are both new) | TBD | TBD |
| **Wave 4** — Elo separation at the bottom of the winner set | ranks 8, 9 and 10 tied at Elo **exactly 1200.0** with 2 wins each — they won their places by tie order | V01-FINDINGS "Tournament" | TBD | TBD |
| **Wave 4** — the catch-up schedule fired (the `median <= 0` warning is **ABSENT** in the run log) | `n/a - feature did not exist` | D-W5-8 | TBD | TBD |

> ⚠️ **CAVEAT ON THE `WEAK` BASELINE — this V-01 figure is DISPUTED and the row must carry it.** The
> evidence base for *"9 of 10 winners WEAK"* was a **truncated critique field**:
> `_CANDIDATE_PROMPT_CHARS = 240` cut 17 of 18 candidates mid-word, so the critic was judging
> half-sentences. **Treat 9-of-10 as an indicative baseline, not a settled measurement.** A lower WEAK
> count this run is **partly the truncation fix, not prefer-KEEP alone** — record the number, do not
> attribute it to one rule.

> ⚠️ **"WEAK winners per round" is NOT cross-run queryable after this phase** (D-W5-17). `winners`,
> `weak_winners`, `barred`, `lookups` and `calls` have no home in `workshop_round_yield`'s frozen
> column set; they survive **per-run only**, in `loop_rounds`. That is a conscious acceptance of a
> real gap in D-R8's stated purpose, recorded here so it is not discovered while reading the table.

---

## Wave 5 — the instrumentation itself (this phase)

*What this wave was supposed to change: `assignment_yield` + `workshop_round_yield` (alembic 0018),
so that the loop's cost can be judged across runs rather than argued.*

| Measure | V-01 observed | V-01 source | 15.8 observed | Verdict |
|---|---|---|---|---|
| **Wave 5** — `assignment_yield` row count, **reconciled against the angles actually dispatched** | `n/a - feature did not exist` | (0018 is this phase's) | TBD | TBD |
| **Wave 5** — all three `parent_kind` values each observed at least once: `client_question`, `cross_cutting`, `discovery_rider` | `n/a - feature did not exist` | D-W5-2 | TBD | TBD |
| **Wave 5** — `workshop_round_yield` row count (expect one per round) | `n/a - feature did not exist` | D-W5-1 | TBD | TBD |
| **Wave 5** — `new_entrants_top_n` present and **non-null on every round row** | `n/a - feature did not exist` | D-W5-11 | TBD | TBD |
| **Wave 5** — `keep_count` + `weak_count` + `kill_count` == `candidates_in` on every round row | `n/a - feature did not exist` | D-W5-17 | TBD | TBD |
| **Wave 5** — `SUM(assignment_yield.cost_usd)` vs `run.cost_usd_total`: **the gap, named** | `n/a - feature did not exist` | review WR-02 | TBD | TBD |
| **Wave 5** — `fact_list_parsed` per provider (**TRUE / FALSE, not NULL**, on any angle that returned prose) | `n/a - feature did not exist` | review CR-01, restored | TBD | TBD |
| **Wave 5** — `resolvable_sources` per provider, **fallback rows excluded** (they are NULL by design) | `n/a - feature did not exist` | review CR-01, restored | TBD | TBD |

> ⛔ **A ROW-COUNT SHORTFALL MEANS PAID-BUT-UNRECORDED ANGLES (review WR-02).**
> `research_division._one_angle` returns a result **only on the success path**, so a timed-out or
> errored angle contributes **no row at all** — not a NULL cell, no row — and it may well have been
> billed, since a deep-research call can run thirty minutes and then time out. So
> `SUM(assignment_yield.cost_usd)` is a **LOWER BOUND on research spend, never the total**, and the
> row count is the only thing that can reveal the omission. ⚠ **This is a SECOND, INDEPENDENT
> mechanism from the `gpt-5.6-sol` NULL-cost floor** (open at 15.8-08, where a null rate makes
> `_rate()` return `Decimal("0")`). **Both push the same number down for different reasons. Do not
> present a floor as a total** — the V-01 `$53.48` baseline carries the same warning, and comparing a
> floor to a total is meaningless in either direction. No column was added: D-W5-1 froze the set.

> ⚠ **SUPERSEDED 2026-08-04 — RESTORED, NOT NULL. ALL ELEVEN COLUMNS CARRY A MEASUREMENT.** The
> operator authorised the `synthesis/steps.py` change the old note deferred; it was made in commit
> `54a1544` (engine gate `collecting: 43 of 43 · 1777 passed · 0 failed · 13 skipped`, build
> `409ecddc`). `collect_provider_facts` now measures both PER ASSIGNMENT inside its report loop,
> before `_dedupe_claims` unions the answer away, and carries them out as a bool and an int.
>
> **`fact_list_parsed`** is the real per-report parse outcome: `TRUE` = this assignment's own D8
> block parsed; `FALSE` = it fell back to the distiller; `NULL` = nothing was read.
> **`resolvable_sources`** counts DISTINCT non-empty URLs this assignment's OWN pre-merge claims
> cited; `0` is a measurement.
>
> ⛔ **`FALSE` + `NULL` IS THE CORRECT FALLBACK ROW, NOT A DEFECT.** A fallen-back angle's claims
> come from the ONE shared distiller call that discards the angle, so its sources cannot be
> attributed back — a `0` would falsely say it cited nothing.
> ⚠ **EXCLUDE FALLBACK ROWS FROM ANY `resolvable_sources` COMPARISON**, or a stream that fell back
> reads as a stream that cited nothing. **A `NULL` `fact_list_parsed` on an angle that plainly
> returned prose IS worth recording as a defect.**
>
> ~~Both were derived from the **post-merge** claim list, and `_dedupe_claims` merges
> `found_by` and **unions `source_urls`** across providers — so every corroborating provider's row
> reported the union of everyone's URLs and the first provider's parse outcome. The error was worst
> **exactly when corroboration works**, which is the point of the redesign, so the columns were bound
> to `None` before the run rather than shipped as plausible numbers.~~

> ⛔ **A PARTIAL `assignment_yield` TABLE IS THE INERT-INSTRUMENTATION FAILURE THE WHOLE SCHEMA
> DECISION WAS MADE TO AVOID. An EMPTY one is a PHASE FAILURE, not a note.** The tables exist instead
> of `run_events` precisely because that feed's `meta` allowlist drops unknown keys **silently**.

> ⛔ **`new_entrants_top_n` is the single most important counter in D-R8.** Spec § 6: *"That last
> counter is the loop's entire justification. If round 7+ never produces a new entrant across several
> runs, drop the cap and keep the money. If it does, raise it."* **A NULL column here means the loop's
> justification is still unmeasured after the run that was supposed to measure it.**

> ⛔ **THE YIELD TABLES HAVE NO READ SURFACE (D-W5-18).** No endpoint, no seam verb, no UI — and the
> only credential-free DB path lacks `roles/logging.logWriter`, so it returns a **boolean in an exit
> status, not a table of numbers.** 15.8-15 carries this as a **blocking pre-flight gate (Q-PRE-4)**:
> the grant is made and proven with a visible `SELECT 1` build **before the trigger**. If that gate
> was not paid, **every Wave 5 row above is unfillable and the money is already spent.**

---

## Run-level rows — applying to all five waves

| Measure | V-01 observed | V-01 source | 15.8 observed | Verdict |
|---|---|---|---|---|
| Run status | **`completed_degraded`** — **benign**: 18 candidates clustered to 17; `verification_degraded` was `false` | V01-FINDINGS "Run identity" | TBD | TBD |
| Wall-clock | 13:13:45Z → 14:18:52Z = **65.1 min** | V01-FINDINGS header table | TBD | TBD |
| Calls issued | **415** calls / 2,859,702 tokens / 266 LLM-minutes | V01-FINDINGS header table | TBD | TBD |
| **Total cost — ⚠️ BOTH SIDES ARE A FLOOR** | **$53.48** — a **FLOOR**: 5 openai calls carry NULL `cost_usd` and `run.cost_pending` is still `true`. `claude-sonnet-4-6` alone was $51.89 (97%) | V01-FINDINGS cost section | TBD (**also a FLOOR** — same mechanism) | TBD |
| **NULL-cost `audit_log` rows — the row that makes the floor visible** | **5** (openai, `gpt-5.6-sol`, `Unknown LLM model cost … writing NULL cost_usd`) | V01-FINDINGS cost section | TBD | TBD |
| `run.cost_pending` cleared? | **no** — still `true` | V01-FINDINGS cost section | TBD | TBD |
| `gate_errors` | **153**, all in gate 1; 110 were the model saying KEEP in shorthand, **43 were explicit DROPs sent to paid skeptics anyway (~$6–8)** | OPEN-QUESTIONS Q2 | TBD | TBD |
| **Gate decision-context length vs the `1200` cap — DOES IT BIND?** | ⚠️ **NOT MEASURED in V-01** — and this is **not** the same as `n/a - feature did not exist`: nobody established whether the cap was present at that revision, so the cell reads *unmeasured*, not *absent*. Do not convert one into the other. | (new measurement, added 2026-08-05) | `«AGENT/OPERATOR: paste the reconstructed length in CHARACTERS, beside 1200»` | `«BINDS / DOES NOT BIND»` |
| Gate decision-context — the reconstructed string itself | ⚠️ NOT MEASURED in V-01 | (as above) | `«AGENT/OPERATOR: paste `deep_research_prompt` + `"Focus areas: " + " · ".join(labels)`»` | `«observation only»` |
| Operator-input truncation — **any of the three client questions or the decision block cut?** | Q1/Q2/Q3 measure **288 / 309 / 352** chars against `_QUESTION_MAX_CHARS = 400`; decision block **155** against `_DECISION_MAX_CHARS = 400`. **NOTHING WAS TRUNCATED**, but Q3 clears by only **48 characters**. | `brief_input.py:166`/`:385` + `tribunal.run.brief`, read 2026-08-05 | `«AGENT/OPERATOR: paste the three question lengths + decision length for THIS run»` | `«INTACT / TRUNCATED»` |

> ### ⚠️ THE GATE JUDGES MATERIALITY FROM AT MOST 1200 CHARACTERS — measure whether that bound BINDS
>
> **Operator decision, 2026-08-05: RECORD THE CAPS, DO NOT CHANGE THEM.** Nothing is being uncapped
> and there is no rebuild. This is the same rule that defers `gate_errors: 153` — **changing a
> truncation cap changes which claims reach paid verification**, the one variable this run exists to
> hold still.
>
> **Read from source 2026-08-05, not recalled:**
>
> | Site | Fact |
> |---|---|
> | `pipeline.py:686-691` | `_GATE_DECISION_CONTEXT_CHARS = int(os.environ.get("NESTOR_TRIBUNAL_GATE_BRIEF_CHARS", "1200"))` |
> | `pipeline.py:717-740` | `_gate_decision_context(mission_brief)` returns `deep_research_prompt` + `"Focus areas: " + " · ".join(labels)`, then slices `[:1200]` |
> | `pipeline.py:2852` | the call site — `decision_context=_gate_decision_context(mission_brief)` |
> | `gates.py:82` / `:212` | `_CONTEXT_MAX_CHARS = 2000`, truncating a second time. **1200 is the tighter of the two and ALWAYS WINS** — `pipeline.py`'s own comment says so. |
>
> **Why it earns a row rather than a footnote:** the gate is where **133 of 426 claims died** in V-01
> (112 `not_load_bearing` + 21 `not_falsifiable`), with **`gate_errors: 153`** on top — and its
> entire job is judging materiality **relative to the decision**, from at most 1200 characters.
>
> **The documented reason for the bound is real and is quoted rather than editorialised:** *"a long
> brief must never crowd the claims out of the gate's own output budget"* (the gate runs on a
> 4096-token budget). **That is a sound reason. The question is not whether the cap is justified, it
> is whether it BINDS.**
>
> **THE MEASUREMENT:** reconstruct the string — `deep_research_prompt` plus the joined focus-area
> labels — and record its **length in characters** beside `1200`.
>
> - **`>= 1200`** → the gate judged load-bearing against a **TRUNCATED decision**, which becomes a
>   strong, evidenced candidate cause for the next phase to investigate against `gate_errors`.
> - **`< 1200`** → the cap **did not bind on this run**, and the `gate_errors` cause lies elsewhere.
>   ⛔ **THAT IS AN EQUALLY VALUABLE RESULT. Record it as a measurement that closed a hypothesis —
>   NOT as "nothing found".** A cheap check that eliminates a suspect is exactly what the next phase
>   needs; writing it up as a null result is how a real finding gets lost.
>
> ⚠️ **ORIENTATION ESTIMATE ONLY — NOT A RESULT, AND MUST NOT BE USED TO PRE-FILL THE ROW.** V-01's
> `deep_research_prompt` was ~270 chars, so with 3–4 focus labels the string likely lands around
> **~700** and does **not** bind. **This is an expectation, not evidence.** If the measured value
> comes back near 700, that CONFIRMS the estimate; if it comes back at 1200, the estimate was wrong
> and the measurement wins. Pre-filling from it would be exactly the fabrication this plan exists to
> prevent.
>
> **The two caps that touch OPERATOR INPUT were also checked, and CLEARED — recorded so the next
> reader knows they were examined rather than ignored:** `_QUESTION_MAX_CHARS = 400`
> (`brief_input.py:166`, applied at `:385`) is **deliberately NOT env-tunable**, and V-01's three
> questions measure **288 / 309 / 352** — all three pass intact, **Q3 with only 48 characters of
> headroom.** `_DECISION_MAX_CHARS = 400` against a **155**-char decision block. **No operator input
> is being truncated on this run.** ⚠ But 48 characters is thin: if the new intake's Q3 is phrased
> even slightly longer than V-01's, it will be silently cut — **measure the three lengths on the new
> intake before triggering.**
| Null-certainty claims | **175 of 396 (~44%)**, projected ~67% after the `<TAB>` fix | OPEN-QUESTIONS Q3 | TBD | TBD |

> ⛔ **THE COST TRAP, AND IT IS THE REASON THIS TABLE HAS TWO COST ROWS.** The run total is
> `COALESCE((SELECT SUM(cost_usd) FROM audit_log WHERE run_id = :id), 0)` — and **`SUM` skips NULL
> rows.** `run.cost_pending` is set only via `dr_cost_pending`, which the openai branch never sets. So
> **the NEXT run's total is a floor by the same mechanism.** Comparing $53.48 to a new total without
> counting the NULL-cost rows on **both** sides is **comparing a floor to a floor and calling it
> agreement.**
>
> Count them on both sides with the query 15.8-08 records in `15.8-PRECONDITIONS.md`:
>
> ```sql
> SELECT count(*) FROM audit_log WHERE run_id = :id AND cost_usd IS NULL;
> ```

> ⚠️ **`gate_errors` is DEFERRED ON PURPOSE — it is not a target.** Fixing it changes which claims
> reach paid verification, the one variable the measuring run must hold still. **A change in this
> number is UNEXPECTED and worth investigating**, not progress.

> ⚠️ **`actions` is not comparable across this deploy.** D-W5-9 removed `admission_resolver_calls`
> from the sum, so after 15.8 `actions` means exactly **"calls that went to a model"**. Say so in the
> write-up rather than presenting the drop as an efficiency gain. It was never a dollar figure —
> dollars ride separately as `cost_usd`.

---

## THE MEASUREMENT QUERY SET (Task 2) — run them all from here

**Composed 2026-08-05 by plan 15.8-15, before the run.** They live in one place so the operator runs
one session, not eleven scavenger hunts. Every query goes through the **same** `--no-source` Cloud
Build recipe as Q-PRE-2 (`worker_user`, `cloud-sql-proxy`, `--project` explicit) — and **they are
only READABLE AS NUMBERS if Q-PRE-4 was paid.**

⚠️ **Paste RESULTS. Never connection strings, never env values, never claim text carrying PII —
counts, not values** (T-15.8-15-06).

### Q-RPT — the delivered report. **THE ONLY THING THE ENGINE IS JUDGED FROM.**

- **Q-RPT-1 (preferred, credential-free).** As superadmin in the live app, open the intake and use
  the **raw-output download** (Phase 17). `build_bundle_zip` packages the report markdown, the
  scrubbed `cleaned_reports` and the sources. **This is the delivered artefact.**
- **Q-RPT-2 (fallback, DB).** RLS-scoped — bind `app.tenant_id` first, exactly as Q-YIELD-0 does.

```sql
SELECT length(body), body FROM output
 WHERE run_id = :run_id AND format = 'markdown'
 ORDER BY created_at DESC LIMIT 1;
```

⛔ **NOT the `claim` table and NOT the logs.** Three of V-01's own findings-doc claims were wrong
because they judged the verification stage from intermediate artifacts. **The verification stage
works — do not touch it.**

### Q-YIELD-0 — THE TENANT BINDING. **ONE TRANSACTION.**

```sql
BEGIN;
  SELECT tenant_id FROM run WHERE id = :run_id;          -- visible via worker_all
  SET LOCAL app.tenant_id = '<that uuid>';
  -- Q-YIELD-1, Q-YIELD-2 and Q-LOOP-1 run HERE, inside this same transaction
COMMIT;
```

> ⛔ **AN UNBOUND QUERY *RAISES*; IT DOES NOT RETURN ZERO ROWS. DO NOT READ AN RLS ERROR AS AN EMPTY
> TABLE** (T-15.8-15-08). Mirroring 0015, these two tables get a tenant-isolation policy and **no**
> `worker_all` policy, so an unbound read fails loudly with `unrecognized configuration parameter`.
> **An error here means the binding was wrong — it says nothing at all about whether the
> instrumentation fired.** The arithmetic check that a silent zero could never pass is Task 2's
> row-count reconciliation against the angles dispatched.

### Q-YIELD-1 — `assignment_yield`

```sql
SELECT provider, group_id, parent_kind, client_question, stakes,
       fact_list_parsed, retry_used, claims_kept,
       claims_surviving_verification, resolvable_sources,
       cost_usd, duration_s
  FROM assignment_yield WHERE run_id = :run_id
 ORDER BY group_id, provider;

SELECT parent_kind, count(*) FROM assignment_yield
 WHERE run_id = :run_id GROUP BY parent_kind;

SELECT count(*), COALESCE(SUM(cost_usd),0) FROM assignment_yield WHERE run_id = :run_id;
```

### Q-YIELD-2 — `workshop_round_yield`. **READ `new_entrants_top_n` FIRST.**

```sql
SELECT round_no, new_entrants_top_n, candidates_in, new_candidates,
       keep_count, weak_count, kill_count, barred_drops, round_cost_usd
  FROM workshop_round_yield WHERE run_id = :run_id
 ORDER BY round_no;
```

Spec § 6 calls `new_entrants_top_n` **"the loop's entire justification"**. Record the **per-round
values as a list**, not a summary.

### Q-COST — three queries, and the second is NOT optional

```sql
-- Q-COST-1  the total
SELECT COALESCE(SUM(cost_usd),0) FROM audit_log WHERE run_id = :run_id;

-- Q-COST-2  THE FLOOR MARKER — MANDATORY BESIDE Q-COST-1
SELECT count(*) FROM audit_log WHERE run_id = :run_id AND cost_usd IS NULL;

-- Q-COST-3  concentration (V-01: claude-sonnet-4-6 alone was 97%)
SELECT provider, model, count(*), SUM(cost_usd) FROM audit_log
 WHERE run_id = :run_id GROUP BY provider, model ORDER BY 4 DESC NULLS LAST;
```

**A cost total recorded without Q-COST-2 is not a cost measurement** (T-15.8-15-04). The full
reading discipline — and the three distinct mechanisms that make the total a floor — is in
§ PRE-FLIGHT (a) above; do not re-derive it.

### Q-CLAIM — corroboration and claims

```sql
-- Q-CLAIM-1
SELECT count(*) AS claims,
       count(corroboration_key) AS with_key,
       count(*) - count(corroboration_key) AS null_key
  FROM claim WHERE run_id = :run_id;

-- Q-CLAIM-2  distinct keys — THE V-01 KILLER. A key with count > 1 IS a merge; V-01 had none.
SELECT corroboration_key, count(*) FROM claim
 WHERE run_id = :run_id GROUP BY corroboration_key ORDER BY 2 DESC;

-- Q-CLAIM-3  coffee. V-01's value is 0.
SELECT count(*) FROM claim
 WHERE run_id = :run_id AND (text ILIKE '%koffie%' OR text ILIKE '%coffee%');
```

### Q-LOOP-1 — the loop's narrative, in the operator's own feed

```sql
SELECT seq, stage, kind, text FROM run_event WHERE run_id = :run_id ORDER BY seq;
```

Same tenant binding as Q-YIELD-0. Every workshop note is persisted under D-W4-11 and renders as
`Question workshop — …`. **This is also readable in the live run page without any DB access**, which
makes it the cheapest of the eleven.

---

## Findings

*Numbered, each naming the artifact it was read from. Empty until the run.*

1. TBD

---

## Gaps found

*The single consolidated list Task 2 and Task 3 both write into, and the list
`/gsd-verify-phase 15.8` and `/gsd-plan-phase --gaps` read. **NOTHING ON IT GETS FIXED IN THIS
PHASE.*** Each item names what was observed and where.

**G-1 — THE TAG-VERSUS-DIGEST TRAP in the deploy-verification path.**
*Observed:* while running Q-PRE-1, 2026-08-05T11:50Z.
`gcloud run services describe --format='value(spec.template.spec.containers[0].image)'` returns the
**TAG** (`…/tribunal-api:20260805-111647`), not a digest. **A tag is mutable**: it can be re-pointed
at a different image while every string in the record still matches, so a tag-to-tag comparison reads
as a successful digest verification and is not one. The immutable digest lives on the **revision**,
at `gcloud run revisions describe <rev> --format='value(status.imageDigest)'`.
*Why it matters:* T-15.8-15-03 — the whole point of Q-PRE-1 is catching a wrong-source deploy, and
this is the exact shape where a green check proves nothing. `infra/DEPLOY-RUNBOOK.md`'s digest-pin
proofs (lines ~2311, ~2688) should be audited for the same substitution.
*Disposition:* the Q-PRE-1 block in this file is corrected. **The runbook was NOT edited — RECORD,
DO NOT REPAIR.**

**G-2 — `nestor-frontend` had NO image-digest baseline at all before 2026-08-05.**
*Observed:* Q-PRE-1, same session.
`nestor-api`'s only recorded digest was the § 15.2 record's **truncated** `sha256:171f716f…` (8 hex
chars ≈ 32 bits), and **`nestor-frontend` had none**. So for both CONFIRM-ONLY services the
character-by-character comparison this gate demands **was not possible today** — the assertable
baseline was the revision name, the 100% traffic split and the absence of a newer revision.
*Why it matters:* a service can be CONFIRM-ONLY on one deploy and still be the thing that changed. A
record that omits digests for the services it did not rebuild cannot detect that later.
*Disposition:* both full digests are now recorded in Q-PRE-1 and in the run-identity block, giving
the next deploy a real baseline. **No runbook change made here.**

**G-3 — the runbook's queue-check config was MATERIALISED for the first time today, and is still not committed.**
*Observed:* paying Q-PRE-2/Q-PRE-4, 2026-08-05T12:12–12:16Z.
`infra/DEPLOY-RUNBOOK.md`'s own "four instructions that cannot be executed as written" list, item #2,
says the § 15.2.k queue recipe is **prose only — described three times across the file and two plans,
materialised nowhere.** It was written to `scratchpad/queue-check.yaml` to run this gate, and **it is
NOT committed to the repo**: this plan's file fence excludes `tribunal/*.yaml`, and RECORD-DO-NOT-REPAIR
applies. 15.8-14 hit the identical wall and booked it identically (its deviation #7).
*Why it matters:* **that is now TWICE the same config has been written from scratch and thrown away.**
The runbook itself names the fix — *"commit the `--no-source` config"* — so this is a known, named,
unclaimed piece of work, not a discovery.
*Disposition:* candidate for the next phase. **Nothing committed here.**

**G-4 — the first attempt failed on URL PARSING, not on the queue — and under `logging: NONE` that would have been invisible.**
*Observed:* build `c4307d4c-e09e-450d-88fb-36d68377a36f`, **exit 2**.
`DATABASE_URL_WORKER` has the Cloud SQL unix-socket form
`…://user:pw@/<db>?host=/cloudsql/<proj>:<region>:<inst>`. Taking *"everything after the last `/`"*
therefore yields the **Cloud SQL connection name**, not the database. **The correct parse strips the
query string FIRST.** The real database name is **`nestor`**. The symptom was
`FATAL: database "…:europe-west1:nestor-pg" does not exist`.
*Why it matters, and it is the reason this is booked rather than shrugged off:* **the error was
legible only because Q-PRE-4 had already been paid.** Under 15.8-14's `options.logging: NONE`, the
exact same bug surfaces as a **bare non-zero exit — indistinguishable from an assertion arm firing.**
A parse bug would have been read as a queue finding. That is the boolean-channel failure mode
D-W5-18 describes, caught in the act.
*Disposition:* recorded. The corrected parse lives in the uncommitted scratchpad config (see G-3).

**G-5 — the gate judges materiality from at most 1200 characters of decision context. NEXT-PHASE CANDIDATE, NOT A DEFECT.**
*Observed:* source read 2026-08-05 — `pipeline.py:686-691`, `:717-740`, `:2852`; `gates.py:82`, `:212`.
`_GATE_DECISION_CONTEXT_CHARS` defaults to **1200** (env `NESTOR_TRIBUNAL_GATE_BRIEF_CHARS`), and it
is the **tighter** of two truncations — `gates.py`'s `_CONTEXT_MAX_CHARS = 2000` never binds first.
The gate is where **133 of 426 claims died** in V-01, with `gate_errors: 153` on top.
*Why it is NOT booked as a defect:* **the bound has a real, documented reason** — *"a long brief must
never crowd the claims out of the gate's own output budget"* (4096-token gate budget). A cap with a
sound rationale is not a bug; **the open question is whether it BINDS in practice**, which nobody has
measured.
*Disposition:* **operator decision 2026-08-05 — RECORD, DO NOT CHANGE.** No uncapping, no rebuild —
changing a truncation cap changes which claims reach paid verification, the one variable this run
holds still (the same reasoning that defers `gate_errors: 153`). A measurable row was added to the
run-level comparison table instead. **A `< 1200` result closes the hypothesis and is a real finding,
not a null one.**
*Also cleared, and recorded so it is not re-checked:* the two caps touching operator input —
`_QUESTION_MAX_CHARS = 400` (not env-tunable) and `_DECISION_MAX_CHARS = 400` — **truncate nothing**
on V-01's brief (288 / 309 / 352 and 155). ⚠ **Q3's 48-character headroom is thin**, so the new
intake's question lengths are worth measuring before the trigger.

*Further items are added by Task 2 (from the run) and Task 3 (from the browser UAT).*

---

## Operator sign-off

| | |
|---|---|
| Date | TBD |
| Verdict (accepted / accepted-with-defects / rejected) | TBD |
| Defects to close before the next run | TBD |
| Signed | TBD |
