---
phase: 15.8-research-engine-redesign-yield-instrumentation-deploy-one-me
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
  - tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py
  - tribunal/nestor_pulse_sdk/tests/test_pipeline_dispatch_clause.py
autonomous: true
requirements: [WR-04, WR-06]

must_haves:
  truths:
    - "WR-06: a group that lost copies to the trim stops the feed saying 'every one of those N group(s) went to all 3 research streams'"
    - "WR-06: the 3-of-3 case still says it — the clause is corrected, not deleted"
    - "WR-06: the comment beside the arithmetic describes what the code now does, so the next reader cannot believe the wrong one"
    - "WR-04: `_bound_groups_to_winners` returns instead of raising on a non-iterable `groups`, a non-iterable `members`, and a non-set `allowed_texts`"
    - "WR-04: the fix is REACHABLE — the identical `list(groups or [])` at its only production call site no longer raises first"
    - "WR-04: the winners bound still FAILS CLOSED — a broken `allowed_texts` drops members, it never passes unbounded members through to paid research"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      provides: "the uniform-dispatch decision as a named pure helper that counts COPIES per corroboration key"
      contains: "_dispatch_was_uniform"
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py"
      provides: "a `_bound_groups_to_winners` whose body matches its 'PURE, never raises' docstring"
      contains: "_bound_groups_to_winners"
    - path: "tribunal/nestor_pulse_sdk/tests/test_pipeline_dispatch_clause.py"
      provides: "the negative arm WR-06 exists for — a trimmed group must not produce the strong wording"
      contains: "_dispatch_was_uniform"
  key_links:
    - from: "pipeline._run_staged's `_corroboration_clause`"
      to: "pipeline._dispatch_was_uniform"
      via: "the feed header's uniform-dispatch decision, taken in exactly one place"
      pattern: "_dispatch_was_uniform\\("
    - from: "research_division._divide_from_winners"
      to: "research_division._bound_groups_to_winners"
      via: "the only production call, which must survive the same input the helper now survives"
      pattern: "_bound_groups_to_winners\\("
---

<objective>
Two carried defects from 15.6's review, in the two files this plan owns exclusively in wave 1.

**WR-06 is the reason this plan is not optional.** `_uniform_dispatch` counts DISTINCT
`corroboration_key`s rather than COPIES, so a group that went out on three streams and came back on
one still contributes exactly one key — and the operator's own feed says *"every one of those N
group(s) went to all 3 research streams"* when one of them went to one. That sentence is written into
the run record. Plan 15.8-15 reads it, and there is no second run to correct it. The comment beside
the arithmetic already states the opposite of what the arithmetic does — it says a trim "keeps the
OLD, weaker wording rather than overclaiming" — so intent and implementation disagree in the source,
and both get fixed here or the next reader believes the comment.

**WR-04** is the smaller half: `_bound_groups_to_winners` is documented "PURE, never raises" and its
body has no `try`. `list(groups or [])` raises on any non-iterable, between the paid workshop and the
paid angles. CR-01's `_text_key` removed the unhashable-member path as a side effect; the raise itself
was never closed.

Purpose: the measuring run must not report something untrue about itself, and a data-shape defect must
not kill a run that has already paid for its workshop.
Output: one named pure helper that decides uniform dispatch by copies, a bound that survives hostile
input while still failing CLOSED, and a red-before/green-after proof of each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_preflight gate="blocking">
The worktree stale-base trap is **15/15 in this repo** — most recently 2/2 in phase 15.7, at 595-601
commits behind. `git rev-list --count BASE..HEAD` reads **0 (green) while stale**, because the stale
ref is usually the merge-base. That count proves nothing. Assert the MERGE-BASE:

    git merge-base HEAD 00ad132

Anything other than `00ad132` means the tree is stale — `git reset --hard 00ad132` before touching a
file. Also run `git worktree list` at session start: a stale worktree from a halted run has sat on
disk in this repo before.

Positive-presence sentinels that MUST be found at this base (absence = wrong tree, STOP and report):

    grep -c "_text_key" tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py     # >= 5
    grep -c "_uniform_dispatch" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py       # 2
    grep -c "workshop_notes" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py          # 8
</worktree_preflight>

<proving_things_on_this_machine>
**No pytest, no Python 3.11, no Docker, no `python3`.** A stdlib-only Python 3.14 ships with gcloud at
`C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`. A real
`import nestor_pulse_sdk...` FAILS here (`ModuleNotFoundError: sqlalchemy`) — verified at this base.

So every test this plan writes is **OWED to Cloud Build at plan 15.8-13**, which is the phase gate.
Say so plainly in the SUMMARY. Locally you prove BEHAVIOUR only, by `ast`-lifting the real function
source out of the file and `exec`ing it.

⛔ **The `ast`-lift harness SUPPLIES MODULE GLOBALS and therefore MANUFACTURES any name the module
forgot to import.** It hid a missing `DISCOVERY_PARENT` used at four sites through nine plans,
`py_compile`, and "38 lifted tests green". It can never prove a name resolves. Two rules follow, both
mandatory here:

1. When you lift `_bound_groups_to_winners`, lift the **real source text** of every helper it calls —
   `_text_key`, `_is_discovery_member`, `_member_parents`, `_member_rank` — rather than stubbing them.
   Supply only `logging` (real), a real `log = logging.getLogger(...)`, and `_D6_MAX_WINNERS` as an
   int.
2. Run the **static undefined-global check** over both edited production files and record its output:
   walk the module with `ast`, collect every `Name` in `Load` context that is not bound by an import,
   a module-level assignment, a `def`/`class`, a function parameter, a comprehension target or
   `builtins`, and assert the set is EMPTY. This is what catches what the lift manufactures. A
   non-vacuity arm is required: delete one real import line in a scratch copy and confirm the check
   reports that name.

`PYTHONIOENCODING=utf-8`, or accented text crashes the console. Never `importlib.reload` a lifted
module — it silently un-mutates your mutant.
</proving_things_on_this_machine>

<context>
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-CONTEXT.md
@tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py
@tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py

<interfaces>
Located by SYMBOL at `8a6c59f` / `00ad132`. Line numbers below are a reading aid ONLY — every line
number in `15.6-REVIEW.md` has drifted (three commits on 2026-08-04 edited `pipeline.py` among
others). Re-locate by grep before editing.

**`research_division.py`**

  `_D6_MAX_WINNERS`, `_D6_STREAMS = ("gemini", "openai", "claude")`, `_D6_MIN_CORROBORATION = 2`,
  `_MAX_ANGLES = 28` — the constants that bound this file. `_D6_STREAMS` is documented as the ONE
  place the stream count lives.

  `_text_key(value) -> str`            (~:1072) — PURE, never raises. The join key, `" ".join(str(v or "").split())`.
  `_member_rank(member) -> int`        (~:1056) — PURE, never raises. Missing/sub-1 sorts last.
  `_member_parents(member) -> list`    (~:1030) — PURE, already try/except-wrapped end to end.
  `_is_discovery_member(member)`       — the discovery exemption from the winners bound.
  `_bound_groups_to_winners(groups: Any, allowed_texts: set, labels: list[str]) -> list[dict]`
                                       (~:1093) — **WR-04's subject.** Docstring: "PURE, never
                                       raises." Body has no `try`. `labels` is unread (IN-01, not in
                                       scope).
  `_divide_from_winners(mission_brief, winners, groups, trim_out)` (~:1227) — the ONLY production
                                       caller. Its own step 2 does the IDENTICAL `list(groups or [])`
                                       at ~:1310, **before** the helper is reached, and then:
                                         `resolved = _bound_groups_to_winners(resolved, {_text_key(w["text"]) for w in ordered}, labels)`
                                         `if not resolved: ... return divide(mission_brief)`   # focus-area fallback
  `divide(mission_brief, *, winners=None, groups=None, trim_out=None)` (~:1542) — `if winners:
                                       return _divide_from_winners(...)`. This is the entry a test
                                       uses to prove the fix is REACHABLE.
  `_trim_ladder(angles, trim_out)`     (~:892) — what sheds copies. P2 removes corroboration copies
                                       ABOVE `_D6_MIN_CORROBORATION` (so 3 -> 2, the REACHABLE case:
                                       D-W3-2's fallback at ten client questions is 30 angles against
                                       a cap of 28). P1 goes BELOW the floor (3 -> 1). The WR-06 fix
                                       must catch BOTH; only P1 is "shed 2 of 3".

**`pipeline.py`**

  Module-scope import, ~:88-98 — `_D6_STREAMS` is imported from `research_division` **for its LENGTH
  only**, with a comment saying so. It is a real module-level name; do not add a second source for it.

  `TribunalPipeline._run_staged` — the async method holding the whole feed-header block:
    `groups = list(workshop_result.get("groups") or [])`                       (~:1777)
    `angles = divide(mission_brief, winners=winners, groups=groups, trim_out=_trims)`  (~:2088)
    `_corroborated = len({a.get("corroboration_key") for a in angles if a.get("corroboration")} - {None, ""})`  (~:2140)
    `_mixed_groups`, `_riders_by_group` / `_rider_questions` — the other two Wave 3 clauses (~:2163-2182)
    `_uniform_dispatch = bool(groups) and _corroborated == len(groups)`        (~:2189)  <-- **WR-06**
    `_corroboration_clause = f", and every one of those {len(groups)} group(s) went to all {len(_D6_STREAMS)} research streams" if _uniform_dispatch else (f", {_corroborated} of them checked by several streams" if _corroborated else "")`  (~:2190)
    `_division_items = [{...}]` then `_stage_log_counts(..., corroborated=int(_corroborated))`  (~:2244)

  `_angle_copies(angles, angle) -> int` (~:4194) — module-level, PURE, already counts copies sharing
  one `corroboration_key`. The new helper belongs immediately BEFORE it: same family, same file
  region, and far from the `run_angles` call site (~:2360) that plan 15.8-09 inherits.

**The comment that contradicts the code** (~:2183-2188), verbatim, so you can see what has to change:
  "3. UNIFORM DISPATCH — the headline of D-R4/D-W3-1. When the number of corroboration keys equals the
   number of groups stage B decided, every single group went to every stream and the operator can be
   told so plainly. Any other arithmetic (a trim removed a copy, or `groups` was empty because a
   pre-Wave-3 checkpoint was restored and dispatch fell back) keeps the OLD, weaker wording rather
   than overclaiming."
  The parenthesis names the exact case the code does not handle.

**Existing coverage you must not break**

  `tests/test_engine_e2e_stubbed.py` asserts the POSITIVE case —
  `assert f"went to all {n_streams} research streams" in name` (~:2440), on a 2-group / 3-stream
  stubbed run with no trim. It must stay green: the clause is corrected, not deleted.
  ⚠ **DO NOT EDIT `test_engine_e2e_stubbed.py`.** It is a shared e2e module and this plan does not own
  it; parallel executors in isolated worktrees cannot see each other's edits to it.

  `tests/test_research_division_assignment.py` — owned exclusively by this plan in wave 1. Three
  existing bound tests at ~:646-721, including
  `for empty in (None, [], [None, 3], [{"members": None}], [{"members": [None]}])`. Every one of those
  is ITERABLE, which is precisely why the existing suite does not catch WR-04.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-04 — make `_bound_groups_to_winners` match its "never raises" docstring, and make the fix reachable</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py, tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py lines 1030-1091 — `_member_parents`, `_member_rank`, `_text_key`; note all three are already defensive and none of them raises
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py lines 1093-1224 — the whole of `_bound_groups_to_winners`: the docstring's "PURE, never raises", the two `list(... or [])` sites, the `key in allowed_texts` membership test, the three drop counters and their three distinct warnings
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py lines 1300-1345 — `_divide_from_winners` step 2: the identical `list(groups or [])`, the call, and the `if not resolved: return divide(mission_brief)` fallback
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py lines 1542-1605 — `divide()`'s two paths, so the reachability test drives the real entry point
    - tribunal/nestor_pulse_sdk/tests/test_research_division_assignment.py lines 600-721 — the three existing bound tests, especially the all-iterable tuple at :718
    - .planning/phases/15.8-*/15.8-CONTEXT.md — D-W5-3's WR-04 bullet and the line-numbers-have-drifted warning
  </read_first>
  <behavior>
    - `_bound_groups_to_winners(17, {"x"}, ["Q1"])` returns `[]` instead of raising `TypeError`; same for a float, a bare `object()`, and a `bool`
    - `_bound_groups_to_winners([{"members": 17}], {"x"}, ["Q1"])` returns `[]` instead of raising
    - `_bound_groups_to_winners([group], None, ["Q1"])` returns `[]` instead of raising — a broken bound drops members, it does not pass them
    - a `list` (not a `set`) supplied as `allowed_texts` still works, because membership is what is asked of it
    - every case the current code already handles is unchanged: `None`, `[]`, `[None, 3]`, `[{"members": None}]`, `[{"members": [None]}]`, a dict, a str, the whitespace-insensitive join, the verbatim pass-through of an untouched group, and the three separately-counted drop causes
    - `divide(brief, winners=<real winners>, groups=17)` returns a non-empty angle list instead of raising — the helper's fix is reachable through the production entry point
    - the bound still BITES: a member whose collapsed text matches no collapsed winner is still dropped, and a group that loses every member is still dropped whole
  </behavior>
  <action>
    Locate `_bound_groups_to_winners` by grep, never by line.

    **The design decision, settled here so the executor does not have to make it.** 15.6's review
    offered "wrap the body in `try/except Exception` returning `[]`, or downgrade the docstring".
    Take NEITHER as written. Returning `[]` for one hostile group would discard every healthy group
    with it and send the run down `_divide_from_winners`' focus-area fallback, throwing away a paid
    workshop over a data-shape defect. Downgrading the docstring leaves the raise in production
    between the paid workshop and the paid angles. Do this instead, in three layers:

    1. **Tolerant iteration, minimal-change.** Replace both `list(... or [])` sites — the one over
       `groups` and the one over `group.get("members")` — with a tolerant read that attempts
       `list(value or [])` and returns an empty list on `TypeError`. Introduce ONE tiny module-level
       pure helper next to `_text_key` for this, named `_as_list`, documented as PURE and
       never-raising. Attempting `list()` first and only catching the failure is deliberate: it
       preserves EXACTLY today's behaviour for every input that works today, including a dict
       (iterates keys, then fails the `isinstance(group, dict)` test) and a str (iterates chars, same)
       — so this closes the raise and changes nothing else.
    2. **Fail CLOSED on a broken bound.** Guard the membership test so a non-container
       `allowed_texts` cannot raise. Build a local set once at the top of the function from
       `allowed_texts` via `_as_list`-style tolerance, and test membership against that local. A
       broken bound therefore yields an EMPTY allowed set, every mandate member is dropped, the group
       is dropped whole, and `_divide_from_winners` falls back — the conservative direction. State in
       a comment WHY that direction: this bound is the only real spend control the engine has left
       (T-15.2-61, the budget governor being inert under `NESTOR_TRIBUNAL_UNCAPPED=1`), so a bound
       that fails OPEN would buy paid research for winners it was built to exclude.
    3. **An outer backstop.** Wrap the per-group loop body in `try/except Exception` and `continue`,
       counting the skip in a NEW counter named apart from the existing two — the file's own
       discipline is that `dropped_unusable` is counted apart from `dropped_members` precisely so a
       warning cannot blame the spend control for a data defect. Emit its own `log.warning` naming it
       as a malformed group record and NOT the winners bound. Return the accumulated `out`, never a
       bare `[]`.

    Rewrite the docstring's "PURE, never raises" paragraph so the claim is now BACKED and says how:
    hostile input is skipped and counted, not raised on; a broken bound fails closed; and the phase's
    whole degradation contract is read off these docstrings, so the claim and the code must not
    diverge again. Do not delete any of the existing docstring's four load-bearing paragraphs (the
    whitespace-insensitive join, the comparison site being the right place, the bound still biting,
    the discovery exemption) — they are the record of CR-01 and T-15.2-61.

    **Then make the fix reachable.** `_divide_from_winners`' step 2 does the identical
    `list(groups or [])` BEFORE the helper is ever called, so with `groups=17` the caller raises first
    and a helper-only fix would be unreachable in production. Apply `_as_list` there too, and add one
    sentence of comment saying that this expression and the helper's are the same expression and were
    fixed in the same commit for that reason. Change nothing else in `_divide_from_winners`.

    **Do not** touch `run_angles`, `_group_angle`, `_trim_ladder`, the `labels` parameter (IN-01 is
    not in this plan's scope), or any constant.

    **Tests** go in `test_research_division_assignment.py`, which this plan owns exclusively this
    wave. Extend the existing `test_the_bound_stays_pure_and_does_not_raise_on_an_unhashable_member_text`
    tuple at ~:718 with the NON-ITERABLE shapes, and add two new tests: one asserting the fail-closed
    direction for a broken `allowed_texts` (and that a `list` bound still works), and one driving the
    real `divide(brief, winners=..., groups=17)` to prove reachability. Each test's docstring must
    name what breaks in production if it fires — the file's existing convention.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" &amp;&amp; PYTHONIOENCODING=utf-8 "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/research_division.py nestor_pulse_sdk/tests/test_research_division_assignment.py</automated>
  </verify>
  <acceptance_criteria>
    - `ast`-lifting the REAL source of `_bound_groups_to_winners` plus the REAL source of `_text_key`,
      `_is_discovery_member`, `_member_parents`, `_member_rank`: every shape in `<behavior>` returns
      rather than raising, and every previously-passing shape returns the same value it does today
    - **RED-BEFORE / GREEN-AFTER, proved by mutating the committed SOURCE TEXT**, recorded as a
      two-column table in the SUMMARY: restore `list(groups or [])` in a scratch copy and confirm the
      non-iterable cases RAISE; restore the raw `key in allowed_texts` and confirm the `None` bound
      RAISES. A test that does not go red on the reverted source is not proving the fix
    - the fail-closed arm is asserted explicitly: a `None` bound yields `[]` and NOT a pass-through of
      the members. A test that only asserts "did not raise" would pass on a fail-OPEN implementation
    - `grep -v '^ *#' nestor_pulse_sdk/pipeline/tribunal/research_division.py | grep -c 'list(groups or \[\])'`
      returns 0 — comments excluded, so the historical prose in the docstrings cannot self-invalidate
      the gate
    - `grep -c "_as_list" nestor_pulse_sdk/pipeline/tribunal/research_division.py` is at least 4 (the
      def, the two sites inside the helper, and the call-site fix)
    - the docstring still contains the phrases "never raises", "whitespace-insensitive" and
      "T-15.2-61", and now also states HOW the claim is backed
    - the static undefined-global check over `research_division.py` reports an EMPTY set, and its
      non-vacuity arm (one import deleted in a scratch copy) reports that name
    - no constant, no `run_angles` code, and no `labels` handling was changed
  </acceptance_criteria>
  <done>The bound survives every hostile shape, still fails closed, and the caller no longer raises before it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-06 — the uniform-dispatch clause counts COPIES, and the comment stops contradicting it</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py, tribunal/nestor_pulse_sdk/tests/test_pipeline_dispatch_clause.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 84-100 — the `research_division` import block and the comment explaining that `_D6_STREAMS` is imported for its LENGTH only
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 2138-2216 — `_corroborated`, the question-source clause, all three Wave 3 clauses, `_uniform_dispatch`, `_corroboration_clause` and the `_division_items` header this plan must not otherwise disturb
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 2240-2250 — `_stage_log_counts(..., corroborated=int(_corroborated))`, the other consumer of `_corroborated`
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py lines 4194-4206 — `_angle_copies`, the module-level pure helper the new one sits beside
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py lines 892-1020 — `_trim_ladder`: which rung sheds which copy, `_D6_MIN_CORROBORATION = 2`, `_MAX_ANGLES = 28`
    - tribunal/nestor_pulse_sdk/tests/test_engine_e2e_stubbed.py lines 2424-2450 — the existing POSITIVE assertion that must stay green. READ IT; DO NOT EDIT IT
    - .planning/phases/15.8-*/15.8-CONTEXT.md — D-W5-3's WR-06 bullet ("This one directly falsifies the measurement's own report")
  </read_first>
  <behavior>
    - 2 groups x 3 copies each -> uniform is True (the case the stubbed e2e run already asserts)
    - 2 groups, one of which kept only 1 copy -> uniform is False. This is the "shed 2 of 3" case WR-06 is named for
    - 2 groups, one of which kept 2 copies -> uniform is False. This is the REACHABLE case: `_trim_ladder`'s P2 rung stops at `_D6_MIN_CORROBORATION` = 2, and D-W3-2's fallback at ten client questions is 30 angles against a cap of 28
    - `groups` empty -> uniform is False, whatever the angles say (a restored pre-Wave-3 checkpoint)
    - angles empty -> uniform is False
    - a group present in `groups` that produced NO surviving angle -> uniform is False (fewer keys than groups)
    - an angle with `corroboration` falsy, or with an empty/missing `corroboration_key`, contributes nothing
    - a non-list `groups` or `angles` -> uniform is False, not a raise
    - the weaker fallback wording is unchanged: when uniform is False and some keys exist, the clause still reads "N of them checked by several streams"
  </behavior>
  <action>
    Locate the block by grepping for `_uniform_dispatch`, never by line.

    Add ONE module-level pure helper to `pipeline.py`, immediately BEFORE `_angle_copies` — same
    family, same file region, and deliberately far from the `run_angles` call site that plan 15.8-09
    inherits in wave 2:

    `_dispatch_was_uniform(angles: Any, groups: Any, streams: Optional[int] = None) -> bool`

    It counts copies PER corroboration key — build a `dict[str, int]` over the angles, skipping any
    angle whose `corroboration` is falsy and any whose `corroboration_key` stringifies to empty — and
    returns True only when ALL THREE hold: `groups` is non-empty; the number of keys equals the number
    of groups; and EVERY key's count equals the expected stream count. `streams` defaults to
    `len(_D6_STREAMS)` when None, so a test can assert the arithmetic without depending on the module
    global — `_D6_STREAMS` is already a real module-scope import here and must not gain a second
    source. The helper is PURE and must not raise on a non-list `angles` or `groups`; reuse the same
    tolerant-iteration discipline Task 1 applies in `research_division.py`, written locally here
    rather than imported across the seam (an exact-set/shared-symbol import across a seam two plans
    both touch is what turned phase 15.5's merged tree red).

    Replace `_uniform_dispatch = bool(groups) and _corroborated == len(groups)` with a call to the
    helper. Leave `_corroborated` exactly as it is: it still feeds the weaker fallback wording and
    `_stage_log_counts(..., corroborated=...)`, and this plan changes neither.

    **Rewrite the comment at the clause so the code and the comment cannot disagree.** The current
    text says a trim "keeps the OLD, weaker wording rather than overclaiming" — which is the intent,
    and is exactly what the code did not do. The new text must say what the code now does: UNIFORM
    means every group kept every copy, counting KEYS cannot see a trim that shed one of three, and
    counting COPIES is what makes the sentence true. Record WHY it matters in one clause: this
    sentence is written into the run's own record and the 15.8 measuring run reads it, once.

    Also state, in that comment, the boundary this fix deliberately does NOT move: `len(groups)` is
    stage B's PRE-dispatch group count, so a group that `_bound_groups_to_winners` dropped whole
    already makes the count disagree and already yields the weaker wording. That is the conservative
    direction and is left alone.

    **Tests** go in a NEW module, `tribunal/nestor_pulse_sdk/tests/test_pipeline_dispatch_clause.py`.
    A new module, not an addition to `test_engine_e2e_stubbed.py`: that file is shared, this plan does
    not own it, and parallel executors in isolated worktrees cannot see each other's edits to it.
    Import the pipeline module as `from nestor_pulse_sdk.pipeline.tribunal import pipeline as _pipeline_mod`
    — the pattern `test_engine_e2e_stubbed.py` already uses — and drive `_dispatch_was_uniform`
    directly with hand-built angle dicts. Cover every row of `<behavior>`. The module docstring must
    say what breaks in production if these fire: the operator's feed, and the 15.8 measuring run's own
    report, assert something untrue about the run, with no second run to correct it.

    ⚠ **This adds exactly ONE new test FILE.** It is NOT in `tribunal/cloudbuild.test-engine.yaml`'s
    explicit list, so it does not run until plan 15.8-13 adds it. **DO NOT TOUCH
    `tribunal/cloudbuild.test-engine.yaml`** — D-W5-5 gives it a single owner this phase, 15.8-13 in
    wave 4, because eight wave-1 plans each bumping one `EXPECTED_FILES` line is a guaranteed conflict
    that can silently resolve to a wrong number. Record the new filename prominently in the SUMMARY so
    15.8-13 can reconcile `EXPECTED_FILES` = 36 + (files this phase added).

    Emit no run event from this plan. If a later reader is tempted: `RUN_EVENT_KINDS` is a CLOSED
    twelve-value tuple and an out-of-vocabulary `kind` is DROPPED SILENTLY at `emit`.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" &amp;&amp; PYTHONIOENCODING=utf-8 "$PY" -m py_compile nestor_pulse_sdk/pipeline/tribunal/pipeline.py nestor_pulse_sdk/tests/test_pipeline_dispatch_clause.py</automated>
  </verify>
  <acceptance_criteria>
    - `ast`-lifting the REAL source of `_dispatch_was_uniform` and passing `streams=3` explicitly:
      every row of `<behavior>` returns the stated boolean
    - **RED-BEFORE / GREEN-AFTER, proved by mutating the committed SOURCE TEXT**, recorded as a
      two-column table in the SUMMARY: restore `_corroborated == len(groups)` arithmetic in a scratch
      copy and confirm the 3/1 case AND the 3/2 case both come back True (i.e. the tests go RED). The
      3/2 case is the one that is actually reachable in production; if only the 3/1 case is proved,
      the proof is incomplete
    - `grep -v '^ *#' nestor_pulse_sdk/pipeline/tribunal/pipeline.py | grep -c '_corroborated == len(groups)'`
      returns 0 — comments excluded so the rewritten prose cannot self-invalidate the gate
    - `grep -c "_dispatch_was_uniform" nestor_pulse_sdk/pipeline/tribunal/pipeline.py` is at least 2
      (the def and the call), and at least 1 in the new test module
    - the rewritten comment contains the word "copies" and no longer asserts anything the code does not
      do; a reader who trusts only the comment reaches the same conclusion as one who reads only the code
    - `_corroborated` is still computed and still passed to `_stage_log_counts` — this plan does not
      change the logged count or the weaker wording
    - `git diff --stat` shows `test_engine_e2e_stubbed.py` UNCHANGED and
      `tribunal/cloudbuild.test-engine.yaml` UNCHANGED
    - the static undefined-global check over `pipeline.py` reports an EMPTY set, confirming
      `_D6_STREAMS` genuinely resolves at module scope and was not manufactured by the lift
    - `git diff` on `pipeline.py` touches only the clause block and the region beside `_angle_copies`
      — nothing near the `run_angles` call site, and nothing in the `workshop_notes` /
      `run_events.emit_safe` loop that D-W4-11 landed on 2026-08-04
  </acceptance_criteria>
  <done>A trimmed group no longer produces the strong wording, the untrimmed case still does, and the comment says what the code does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stage B / grouping LLM output -> `groups` records | model-shaped data crossing into `divide()`, then into `_bound_groups_to_winners` |
| `_bound_groups_to_winners` -> paid third-party research | the winners bound is the last spend control before three providers are billed |
| engine -> operator feed / run record | the `research_division` stage detail string, which the 15.8 measuring run reads as evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.8-02-01 | Denial of service | `_bound_groups_to_winners` + `_divide_from_winners` step 2 | mitigate | a malformed `groups` record currently raises `TypeError` out of `divide()` AFTER the workshop is paid for and BEFORE any angle is bought, killing the run and its spend. Both `list(... or [])` sites become tolerant, the per-group loop gains a backstop, and the tests drive non-iterable `groups`, non-iterable `members` and a non-container bound (Task 1) |
| T-15.8-02-02 | Elevation of privilege | the `_D6_MAX_WINNERS` bound, on a broken `allowed_texts` | mitigate | the bound is the only real spend control left (T-15.2-61 — the budget governor is inert under `NESTOR_TRIBUNAL_UNCAPPED=1`). It must fail CLOSED: a bound that cannot be read drops members rather than passing them, so no winner the bound excluded can buy paid research. Asserted explicitly, because a test that only checks "did not raise" passes on a fail-open implementation |
| T-15.8-02-03 | Repudiation | the `research_division` feed header's uniform-dispatch clause | mitigate | the run's own record currently asserts "every one of those N group(s) went to all 3 research streams" when a trim shed copies. `_dispatch_was_uniform` counts copies per key and the adjacent comment is rewritten to match, so the record cannot claim a corroboration level the run did not buy. This is the threat the plan exists for: 15.8-15 reads that sentence, once |
| T-15.8-02-04 | Information disclosure | both changes | accept | no new text is rendered into any prompt, feed row, report or audit blob. The feed clause becomes weaker (or unchanged), never wider |
| T-15.8-02-05 | Tampering | the `ast`-lift harness used for local proof | mitigate | the lift SUPPLIES module globals and MANUFACTURES any name a module forgot to import — it cannot prove name resolution and has produced phantom passes in this repo. Mitigated by lifting real helper source rather than stubbing, by the static undefined-global check with its own non-vacuity arm, and by the fact that the real gate is OWED to Cloud Build at 15.8-13 |
| T-15.8-02-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NOTHING and adds no third-party import to any file it touches. `Optional` and `Any` are already imported in both modules |
</threat_model>

<verification>
- Both production files and both test files byte-compile on the bundled interpreter.
- The static undefined-global check reports an empty set for `research_division.py` and `pipeline.py`, and its non-vacuity arm reports a deliberately deleted import.
- Every `<behavior>` row of both tasks is demonstrated on `ast`-lifted REAL source.
- Both red-before/green-after mutation tables are recorded in the SUMMARY, including the 3-of-2 trim case as well as 3-of-1.
- `git diff --stat` shows exactly four files: the two production files and the two test files.
- `tribunal/cloudbuild.test-engine.yaml`, `tests/test_engine_e2e_stubbed.py`, `question_grouping.py`, `workshop_rank.py`, `workshop_register.py` and `workshop.py` are all UNCHANGED — each belongs to another wave-1 plan.
</verification>

<success_criteria>
- `_bound_groups_to_winners` returns on every hostile shape in Task 1's `<behavior>`, still fails closed, and still drops over-the-bound members; its docstring says how the claim is backed.
- The identical `list(groups or [])` at the helper's only production call site is fixed in the same commit, so the fix is reachable.
- `_dispatch_was_uniform` decides uniform dispatch by COPIES; the strong wording survives the 3-of-3 case and disappears for both 3-of-2 and 3-of-1.
- The comment beside that arithmetic describes the code, not its opposite.
- `_corroborated`, the weaker wording, and the logged `corroborated=` count are unchanged.
- No deploy, no migration, no live run, no `gcloud builds submit`. The engine gate is OWED to 15.8-13.
- `tribunal/cloudbuild.test-engine.yaml` NOT touched — D-W5-5 gives it one owner this phase, 15.8-13.
</success_criteria>

<output>
Create `.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-02-SUMMARY.md`
when done. `.planning/` is gitignored — `git add -f`.

The SUMMARY MUST record, prominently and by name, three things later plans depend on:

1. **`tribunal/nestor_pulse_sdk/tests/test_pipeline_dispatch_clause.py` is a NEW test FILE** and is
   NOT yet in `cloudbuild.test-engine.yaml`'s explicit list. Plan **15.8-13** owns that config and
   must add the path and reconcile `EXPECTED_FILES` (baseline 36, `7c89be5c` = 1538 passed / 0 failed
   / 13 skipped). Until then these tests DO NOT RUN.
2. **The two red-before/green-after mutation tables**, in full, as the only local evidence that exists
   — no pytest ran on this machine.
3. **What plan 15.8-09 inherits:** the exact symbol names and file regions this plan added or changed
   in `pipeline.py` and `research_division.py`, so the wave-2 writer can add the per-assignment yield
   record at the `run_angles` call site without re-reading the whole of either file.
</output>
