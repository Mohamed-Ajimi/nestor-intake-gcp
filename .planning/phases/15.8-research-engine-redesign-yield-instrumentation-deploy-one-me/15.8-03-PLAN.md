---
phase: 15.8-research-engine-redesign-yield-instrumentation-deploy-one-me
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py
  - tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py
autonomous: true
requirements: ["CR-01", "D7", "D-W5-3"]

must_haves:
  truths:
    - "A group member whose text differs from its winner's text ONLY in whitespace is
       re-stamped with the FRESH rank by `_restamp_groups`, not left holding a stale one."
    - "A discovery group member whose text differs from its dispatched question's text ONLY
       in whitespace is re-stamped with the fresh discovery rank by `_stamp_discovery_ranks`."
    - "Neither normalisation MERGES two winners the tournament ranked apart — a member whose
       collapsed text matches no collapsed winner still gets no rank from the join."
    - "A winner produced by `enforce_group_coverage`'s repair rungs arrives in
       `run_workshop_stage_b`'s returned `winners` with a NON-EMPTY `langs`, so 15.2-13 still
       builds its angle-query language sentence for that question (D7)."
    - "Deleting the post-coverage langs sweep turns the end-to-end D7 test RED; reverting either
       `_text_key` join turns its own test RED."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py"
      provides: "module-local `_text_key`, both CR-01 comparison sites joined through it, `_sweep_langs`, and its second call site below `enforce_group_coverage`"
      contains: "def _text_key"
    - path: "tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py"
      provides: "five new tests: two CR-01 rank tests, one no-merge test, one _text_key parity test, one end-to-end D7 coverage-repair test"
      contains: "_sweep_langs"
  key_links:
    - from: "workshop_rank._restamp_groups"
      to: "workshop_rank._text_key"
      via: "both sides of the rank_by_text join"
      pattern: "rank_by_text\\[_text_key"
    - from: "workshop_rank._stamp_discovery_ranks"
      to: "workshop_rank._text_key"
      via: "both sides of the numbered join"
      pattern: "numbered\\[_text_key"
    - from: "workshop_rank.run_workshop_stage_b"
      to: "workshop_rank._sweep_langs"
      via: "a second call immediately after enforce_group_coverage returns"
      pattern: "_sweep_langs\\(final"
    - from: "tests/test_workshop_scope_guard.py"
      to: "research_division._text_key"
      via: "a parity assertion over a shared corpus, so the two copies cannot drift"
      pattern: "research_division._text_key"
---

<objective>
Close the last two CR-01 stale-rank normalisation sites in `workshop_rank.py`, and stop D7 `langs`
being silently dropped for a winner produced by `enforce_group_coverage`'s repair rungs.

Purpose: `rank` is what `research_division._stakes_for_rank` derives stakes and stream treatment
from, so a stale rank silently mis-prices the ONE measuring run. And `langs` is what plan 15.2-13
gates its angle-query language sentence on, so an empty one drops D7 for that question while every
other assertion in the phase reads green. Both are on the measurement path, and there is no second
run to correct either.

Output: three symbol-local edits and one new helper in `workshop_rank.py`, plus five tests in
`tests/test_workshop_scope_guard.py`. NO new test file — this plan contributes **0** to 15.8-13's
`EXPECTED_FILES` (it stays 36 as far as this plan is concerned).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-CONTEXT.md
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-PLAN-OUTLINE.md
@CLAUDE.md

Read only these two source files. Everything else below is quoted from them.

@tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py
@tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from the tree at 2b03d0f (code at 8a6c59f). -->
<!-- LOCATE EVERY ONE BY SYMBOL. Line numbers in 15.7-VERIFICATION.md and 15.6-REVIEW.md for -->
<!-- this file are WRONG — 8eff424 / 868775b / 9f5a120 edited it on 2026-08-04. -->

The CR-01 reference implementation, in a DIFFERENT module:
  `research_division._text_key(value: Any) -> str`
    body: `" ".join(str(value or "").split())` inside a bare `try/except` returning `""`.
    Its docstring states the rule: EXACTLY the collapse `_normalise_winners` applies, and
    deliberately nothing more — no case folding, no truncation, no punctuation stripping,
    because anything more would start MERGING winners the tournament ranked apart. Returning
    a `str` is also what makes a list- or dict-valued `text` hashable instead of raising.
  Applied at `research_division._bound_groups_to_winners` and at the `_trim_ladder` call site,
  on BOTH SIDES OF THE JOIN, AT THE COMPARISON SITE — never pushed back into a producer.

The two comparison sites this plan fixes, both in `workshop_rank.py`:
  `_restamp_groups(groups, winners)` — builds `rank_by_text: dict[str, int]` keyed by
    `str(winner.get("text") or "")`, then looks up `str(member.get("text") or "")`.
  `_stamp_discovery_ranks(groups, discovery, *, base)` — builds `numbered: dict[str, int]`
    keyed by `str(question.get("text") or "")`, then looks up `str(member.get("text") or "")`.
  Both use `first-wins` on key collision (`if key and key not in <map>`). PRESERVE THAT.

The D7 helper and the sweep already in the file:
  `_normalise_langs(raw: Any, *, run_language: str = "") -> list[str]` — pure, never raises,
    documented NEVER EMPTY. Falls through model answer -> run language -> `_DEFAULT_LANGS` -> `["en"]`.
  The existing sweep lives in `run_workshop_stage_b`, immediately AFTER the
    `enforce_scope_guard(...)` call that produces `final, notes, injected`, as a bare
    `for winner in final: winner["langs"] = _normalise_langs(winner.get("langs"), run_language=run_language)`
    under the comment "A promoted or injected winner never went through the evolve step".

The two repair rungs that yield an empty `langs`, both in `enforce_group_coverage`:
  promotion rung: `entry.setdefault("langs", [])` on the promoted candidate.
  injection rung: `_verbatim_winner(label, texts)` returns a 12-key dict with `"langs": []`.
  Neither rung takes a `run_language`, and `enforce_group_coverage` has no such parameter.

The call ordering inside `run_workshop_stage_b` (unchanged by this plan):
  `enforce_scope_guard` -> the existing langs sweep -> `allocate_discovery` -> GAP A ->
  `question_grouping.group_winners` -> `attach_discovery_riders` -> the `d1` group ->
  `enforce_group_coverage` (returns `groups, final, cov_notes, cov_injected`) ->
  the `coverage_shed` block -> `_stamp_discovery_ranks(groups, dispatched_discovery, base=len(final))`
  -> `_stage_b_result(winners=final, ...)`.

Test-side helpers ALREADY in `tests/test_workshop_scope_guard.py` — use them, do not rebuild:
  `win(index, parent, ...)`, `rider(text, parent, rank=...)`, `mandate_group(*(label, count))`
  (built through `question_grouping.build_groups`, never by hand), `cross_cutting_group(text)`,
  `stage_a(labels, candidate_parents, *, conflicts=...)`, `working_fake(evolve_for, groups=...)`,
  `group_reply(*member_lists)`, `stage_b(audited, source, **kwargs)` (awaits
  `run_workshop_stage_b`), `grouping_mode(mode)` (context manager pinning
  `question_grouping._GROUPING_MODE`), `GROUP_PROMPT_MARKER`, `parent_union(winners)`.
  Existing models to copy: `test_a_dropped_client_question_is_put_back_at_the_top_ranked_first`
  and `test_discovery_ranks_below_every_winner_after_the_repair_grew_the_list` in SECTION 3.
</interfaces>

<constraints>
**These are not advice. Each one has already cost this project a plan or a run.**

1. **LOCATE BY SYMBOL, NEVER BY LINE.** `workshop_rank.py` was edited on 2026-08-04 by two of the
   three ruling commits. Every line number in `15.7-VERIFICATION.md` and `15.6-REVIEW.md` for this
   file is stale.

2. **DO NOT touch `tribunal/cloudbuild.test-engine.yaml`** (D-W5-5). It has exactly ONE owner this
   phase: 15.8-13, wave 4. This plan adds no test FILE, so it has nothing to declare there anyway —
   say so in the SUMMARY so 15.8-13 can reconcile its count against a stated zero rather than guess.

3. **DO NOT touch the three `register.get("drops")` bare-length reads** in this file (they feed
   `dropped_as_reproposal` and `round_metrics`). **D-W5-6 rules them to 15.8-10, explicitly NOT to
   this plan**, because the cause-filtered counter they will call (`workshop_register.count_drops`)
   is a symbol 15.8-04 is creating in a parallel worktree in this same wave. Calling a sibling's
   unmerged symbol is the exact shape that produced 15.5's cross-plan regression.

4. **`workshop_rank.py` is INHERITED BY 15.8-10 in wave 2** (the `round_metrics` call site and
   `_stage_b_feed_finish`). Keep every edit symbol-local. Do not reorder functions, do not reflow
   unrelated blocks, do not "tidy" adjacent code. Different regions of the same 5,000-line file is
   NOT a reason to parallelise — that is why you are serialised, and a large diff destroys that.

5. **NO module-level import of `research_division` into `workshop_rank.py`.** This file already
   carries a function-local `workshop_admission` / `workshop_evolve` / `workshop_register` import
   with a comment stating that a module-level import there is a CYCLE. Read that comment before
   adding any import. `_text_key` is defined LOCALLY in this module instead (see Task 1), following
   the precedent the file already documents for `_is_discovery_member` ("duplicated rather than
   imported"), with the drift risk closed by a test rather than by a comment.

6. **DO NOT reuse `_flatten` as the join key.** It is right there and it is the wrong tool: it also
   replaces `|`, strips `\r`, and TRUNCATES to a cap. Truncation would merge two distinct winners
   sharing a long prefix — the precise failure `research_division._text_key`'s docstring says the
   rule must not have. `_flatten` is a prompt-injection control; `_text_key` is an identity key.

7. **No pytest, no Python 3.11, no Docker on this machine.** Bundled stdlib-only interpreter at
   `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe` (3.14). Write real tests
   anyway — the pytest run is OWED to Cloud Build at 15.8-13.
   ⛔ **Do NOT use the `ast`-lift harness.** It SUPPLIES module globals and therefore MANUFACTURES
   any name a module forgot to import; it hid a missing import through nine plans. **It is not
   needed here:** `workshop_rank` imports cleanly on the bundled 3.14 interpreter with **no stubs at
   all** (verified during planning — its whole chain is stdlib). Import it for real.

8. **Worktree stale-base trap is 15/15 in this repo.** Before the first edit, assert
   `git merge-base HEAD <BASE> == <BASE>`. `git rev-list --count BASE..HEAD == 0` reads GREEN while
   stale, so it does not count as a check.

9. `.planning/` is gitignored — any planning artifact needs `git add -f`.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 ×2 — a local `_text_key`, applied to BOTH SIDES at both comparison sites</name>

  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/research_division.py` — the `_text_key` definition
      and the `_bound_groups_to_winners` docstring paragraph beginning "THE JOIN IS
      WHITESPACE-INSENSITIVE, ON BOTH SIDES" and the one beginning "THE COMPARISON SITE IS THE RIGHT
      PLACE FOR THAT RULE". Those two paragraphs are the rule you are transplanting.
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py` — `_is_discovery_member` (for
      the file's own documented duplicate-rather-than-import precedent), `_flatten` (so you can see
      why it is NOT the key), `_restamp_groups`, `_stamp_discovery_ranks`, `_rank_of`.
    - `tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py` — SECTION 3, specifically
      `test_a_dropped_client_question_is_put_back_at_the_top_ranked_first` and
      `test_discovery_ranks_below_every_winner_after_the_repair_grew_the_list`.
  </read_first>

  <behavior>
    - Test 1 (`_restamp_groups`): a mandate group whose member `text` differs from its winner's
      `text` only by an interior newline / double space / trailing space gets the winner's FRESH
      rank stamped onto it. Under the committed code it keeps its stale rank.
    - Test 2 (`_stamp_discovery_ranks`): a discovery group member whose `text` differs from the
      dispatched discovery question's `text` only in whitespace gets the fresh discovery rank.
      Under the committed code it keeps its stale rank.
    - Test 3 (the other half of CR-01's contract — the bound must still bite): a member whose
      collapsed text matches NO collapsed winner receives NO rank from the join and keeps whatever
      it carried. Whitespace-insensitivity must not become "admits strangers".
    - Test 4 (anti-drift): `workshop_rank._text_key` and `research_division._text_key` return the
      SAME value for every entry of a shared corpus that includes `None`, `""`, a list, a dict, an
      interior newline, an interior tab, a double space, and leading/trailing space.
    - Edge: a member whose `text` is a list or a dict does not raise out of the join (the `str`
      return is what makes it hashable) — assert it is simply unmatched.
  </behavior>

  <action>
    Add a module-local `_text_key(value: Any) -> str` to `workshop_rank.py`, in the same helper block
    that already holds `_is_discovery_member` and `_verbatim_winner`, immediately before
    `_restamp_groups`' block. Body is exactly `research_division._text_key`'s: return
    `" ".join(str(value or "").split())` inside a bare `try/except Exception` that returns `""`, with
    the same `noqa: BLE001` comment style this file uses for untrusted-input readers.

    Its docstring must say four things, because the next reader will otherwise re-derive all four:
    that this is the SAME RULE as `research_division._text_key` and that the two are held in step by
    a named parity test (name the test) rather than by hope; that it is deliberately a LOCAL copy
    because a module-level import of `research_division` into this module risks the cycle this file
    already documents at its function-local import; that it is deliberately NOT `_flatten` (truncation
    would merge distinct winners, and `_flatten` is a prompt-injection control, not an identity key);
    and that it is deliberately nothing MORE than the whitespace collapse — no case folding, no
    punctuation stripping — because more would merge winners the tournament ranked apart.

    Then apply it AT THE TWO COMPARISON SITES, on BOTH SIDES of each join. This is CR-01's whole
    method: fixing the comparison site fixes every producer at once, which is why CR-01's own fix
    held. Do not push a collapse back into `build_groups`, `_verbatim_winner`, or any other producer —
    `question_grouping.build_groups` copies member text VERBATIM by documented design and several
    producers feed these groups.

    Site 1 — `_restamp_groups`: key `rank_by_text` with `_text_key(winner.get("text"))` and look up
    with `_text_key(member.get("text"))`. Keep the existing `if key and key not in rank_by_text`
    first-wins guard and the falsy-key skip exactly as they are; an empty key must still match
    nothing.

    Site 2 — `_stamp_discovery_ranks`: key `numbered` with `_text_key(question.get("text"))` and look
    up with `_text_key(member.get("text"))`. Same first-wins guard, same falsy-key skip.

    Extend each function's existing docstring by amending the sentence that currently describes the
    join as an exact-string match over engine-copied strings — that sentence is now WRONG and leaving
    it is the two-authorities trap that produced D-DEF-01's inverted rationale comments. State
    instead that both sides normalise identically BY CONSTRUCTION rather than by two producers
    happening to agree, and that the failure this closes is a STALE `rank` (not a dropped question),
    which mis-prices the run through `research_division._stakes_for_rank`.

    Record honestly, in the `_text_key` docstring, what 15.6's review actually classified this as: a
    HAZARD, not a proven live trigger. Today both sides of both joins happen to be copies of one
    dict's own string, so they agree by accident of construction; nothing enforced it, and CR-01 is
    the proof that exactly that assumption already failed once one module over. Do not overstate it
    as a live defect and do not understate it as tidying.

    Write the five tests into `tests/test_workshop_scope_guard.py`, at the end of SECTION 3, using
    the file's existing helpers (`win`, `rider`, `mandate_group`, `question_grouping.build_groups`).
    Build the whitespace divergence DELIBERATELY and say so in each test's docstring — the member's
    text is the winner's text with an interior newline inserted, which is exactly what a client
    question typed into a form textarea looks like once one side of a join has passed through a
    collapsing producer. For Test 4, import `research_division` in the test module (a test module has
    no cycle constraint) and assert element-wise equality over the corpus.

    Add NO new test file. Add NO new import to `workshop_rank.py`.
  </action>

  <acceptance_criteria>
    - `grep -c "def _text_key" tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py` returns 1.
    - `grep -n "rank_by_text\[_text_key" tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py`
      matches, and `grep -n "rank_by_text.get(_text_key" …` matches — BOTH sides, not one.
    - `grep -n "numbered\[_text_key" …` and `grep -n "numbered.get(_text_key" …` both match.
    - `grep -c "^from nestor_pulse_sdk.pipeline.tribunal.research_division" tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py`
      returns 0 (no module-level import added).
    - `grep -n "_flatten" ` inside `_restamp_groups` and `_stamp_discovery_ranks` returns nothing —
      `_flatten` was not used as a key.
    - Five new test functions exist in `tests/test_workshop_scope_guard.py`, and one of them
      references `research_division._text_key`.
    - `git diff --stat` shows changes to exactly two files.
  </acceptance_criteria>

  <files>
    tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py,
    tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py
  </files>

  <verify>
    <automated>OWED TO CLOUD BUILD (15.8-13): pytest nestor_pulse_sdk/tests/test_workshop_scope_guard.py -x -q</automated>
    Locally executable now (real import, no stubs, no ast-lift):
    `cd tribunal && C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe -c "import sys; sys.path.insert(0,'.'); from nestor_pulse_sdk.pipeline.tribunal import workshop_rank as wr; print(wr._text_key(' a\nb  c '))"`
    must print `a b c`. Task 3 turns this into the full red/green battery.
  </verify>

  <done>
    Both comparison sites join through `_text_key` on both sides; a whitespace-divergent member is
    re-stamped with the fresh rank at each site; a non-matching member is still unmatched; the two
    `_text_key` copies are pinned equal by a test; both docstrings state the new rule and no longer
    describe the join as exact-string.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D7 — `langs` re-normalised AFTER the coverage repair</name>

  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py` — `_normalise_langs`; the
      existing sweep below `enforce_scope_guard` inside `run_workshop_stage_b`; the loop-header
      comment block that states the division of labour between `evolve_generative` and
      `evolve_winners` and ends "The `_normalise_langs` sweep below the scope guard is the belt to
      that pair of braces"; `_verbatim_winner`; `enforce_group_coverage`'s promotion rung
      (`entry.setdefault("langs", [])`) and its call site in `run_workshop_stage_b`;
      `run_workshop_stage_b`'s own docstring sentence promising `langs` is "never empty".
    - `tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py` — `grouping_mode`,
      `working_fake`, `stage_a`, `stage_b`, and `test_a_rider_costs_no_group_and_no_extra_call`
      (SECTION 4) as the model for an end-to-end topic-mode test.
  </read_first>

  <behavior>
    - Test 5 (end-to-end, the one that proves PLACEMENT): `run_workshop_stage_b` driven in `topic`
      grouping mode with a client question dropped from the returned groups returns
      `result["winners"]` in which EVERY entry has a non-empty `langs`, including the
      coverage-repaired one. Assert the repair really fired (a scope note naming the label, or the
      repaired winner at rank 1 with `scope_injected is True`) — a test that passes because no repair
      happened proves nothing, and that is the exact reason the two EXISTING D7 tests miss this.
    - Test 6 (pure, cheap, proves the HELPER): the output of `enforce_group_coverage` on a dropped
      label, passed through `_sweep_langs`, has a non-empty `langs` on the repaired winner via BOTH
      rungs — once with `all_ranked` supplying a promotable candidate (the `setdefault` rung) and
      once with none (the `_verbatim_winner` rung). Assert the run language actually lands (e.g.
      `run_language="Dutch"` yields `["nl"]`), not merely that the list is truthy.
  </behavior>

  <action>
    **Why a plain "the model dropped Q2" script does NOT reach the guard — read this before writing
    Test 5, it will save you an afternoon.** `question_grouping.validate_groups` is TOTAL: every
    winner the grouping model leaves out is placed deterministically by `_place_orphan`, and
    `clamp_groups` only MERGES and SPLITS, never drops a member. So scripting the grouping LLM to
    omit a question does not produce an uncovered label — `enforce_group_coverage`'s own docstring
    says as much where it calls rung 3 "unreachable while the partition is total". To drive the
    repair end-to-end you must make the GROUPS THEMSELVES come back short: enter
    `grouping_mode(question_grouping._GROUPING_MODE_TOPIC)` and monkeypatch
    `question_grouping.group_winners` with an async stub returning `(groups, notes, reasons)` where
    `groups` covers only some of the labels — built through `question_grouping.build_groups` so the
    record under test is the one production stamps, never a hand-rolled dict. `run_workshop_stage_b`
    calls it as a module attribute, so patching the module attribute works. Restore it in a
    `finally` or via `monkeypatch`. State this reasoning in the test's docstring: it is precisely the
    "an LLM deciding grouping is an LLM that can drop a question" scenario the guard exists for, and
    the next reader must not "simplify" the test back into something that never reaches the guard.

    Add `_sweep_langs(winners: Sequence[dict[str, Any]], *, run_language: str) -> None` next to
    `_normalise_langs`. It stamps `winner["langs"] = _normalise_langs(winner.get("langs"),
    run_language=run_language)` over every dict entry, in place, skipping non-dicts, and never
    raises. This exists so there is ONE rule with TWO call sites rather than two hand-maintained
    copies of one loop — this repo has paid for the second shape twice this phase already.

    Replace the existing inline sweep below `enforce_scope_guard` with a `_sweep_langs(final,
    run_language=run_language)` call, preserving its current comment verbatim. **Do not move that
    sweep down.** It must stay where it is: `final` is what `group_winners` renders into the grouping
    prompt, so a `langs` normalised only at the end would leave the grouping call reading raw values,
    and the loop-header comment block names that first sweep as the belt to `evolve_winners`' braces.

    Then add the SECOND call, `_sweep_langs(final, run_language=run_language)`, in
    `run_workshop_stage_b` immediately after the `enforce_group_coverage(...)` call returns and after
    the `coverage_shed` block — before `_stamp_discovery_ranks`. Comment it with why it exists in one
    short paragraph: both of `enforce_group_coverage`'s repair rungs yield an empty `langs`
    (`_verbatim_winner` sets `"langs": []`; the promotion rung `setdefault`s `[]`), the guard has no
    `run_language` to normalise with, and 15.2-13 builds its angle-query language sentence only when
    `langs` is non-empty — so without this a coverage-repaired winner ships D7-less while every other
    assertion reads green. Name the two rungs; a reader who cannot find them will delete the call.

    Do NOT add a `run_language` parameter to `enforce_group_coverage` and do NOT normalise inside its
    rungs. Its four-element return contract is pinned by existing tests, it is documented as PURE and
    NEVER-RAISING, and both rungs are also reached from the unconditional post-condition rescue —
    normalising in three places inside the guard is three authorities where the caller needs one.

    Record in the same comment what this defect is and is not, so nobody re-litigates it: it is
    **pre-existing at the phase base `67fce9f`** — Wave 4 did not introduce it — it is unreachable on
    the default per-question grouping mode and reachable on `topic`, and it is fixed anyway because
    `run_workshop_stage_b`'s own docstring promises `langs` is "never empty" and plan 15.7-09 asserts
    that invariant unconditionally. A promise the code does not keep is the defect, regardless of
    which mode is configured today.

    Write Tests 5 and 6 into `tests/test_workshop_scope_guard.py` — Test 5 at the end of SECTION 4
    (the end-to-end section), Test 6 at the end of SECTION 3 (the pure section). Note in Test 5's
    docstring that both EXISTING D7 tests drive `enforce_scope_guard`'s injection, which PRECEDES the
    first sweep, so neither reaches this path — that sentence is why this test may not be folded into
    them later.

    Add NO new test file.
  </action>

  <acceptance_criteria>
    - `grep -c "def _sweep_langs" tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py` returns 1.
    - `grep -c "_sweep_langs(final" tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py` returns 2
      (one below `enforce_scope_guard`, one below `enforce_group_coverage`).
    - The second call site appears AFTER the `enforce_group_coverage(` call and BEFORE the
      `_stamp_discovery_ranks(` call: verify by comparing the three
      `grep -n` line numbers in that order.
    - `grep -n "def enforce_group_coverage" -A 12 …` shows the signature UNCHANGED (no
      `run_language` parameter added).
    - `enforce_group_coverage` still returns a 4-tuple (the existing idempotency and never-raises
      tests in SECTION 3 are untouched).
    - Two new test functions exist and at least one enters
      `grouping_mode(question_grouping._GROUPING_MODE_TOPIC)`.
  </acceptance_criteria>

  <files>
    tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_rank.py,
    tribunal/nestor_pulse_sdk/tests/test_workshop_scope_guard.py
  </files>

  <verify>
    <automated>OWED TO CLOUD BUILD (15.8-13): pytest nestor_pulse_sdk/tests/test_workshop_scope_guard.py -x -q</automated>
    Locally executable now: the Task 3 battery drives `enforce_group_coverage` + `_sweep_langs`
    directly on the real imported module and asserts `langs == ["nl"]` for `run_language="Dutch"`
    on both rungs.
  </verify>

  <done>
    `_sweep_langs` exists with two call sites in the documented order; a coverage-repaired winner
    carries a non-empty `langs` end-to-end in topic mode; `enforce_group_coverage`'s signature and
    return contract are unchanged; the comment names both rungs, the pre-existing-at-`67fce9f`
    provenance, and the per-question/topic reachability split.
  </done>
</task>

<task type="auto">
  <name>Task 3: The red-before / green-after battery, proved by mutating the committed source text</name>

  <read_first>
    - The two files this plan modified (already in context from Tasks 1 and 2).
    - `.planning/phases/15.8-.../15.8-CONTEXT.md` § "Gate hygiene this phase should close" and
      § "Standing cautions" — for the wording of what is OWED to Cloud Build.
  </read_first>

  <action>
    Write a single throwaway driver under the scratchpad directory (NOT in the repo, NOT committed)
    that imports the REAL `nestor_pulse_sdk.pipeline.tribunal.workshop_rank` on the bundled
    interpreter with `sys.path` pointed at `tribunal/`. Verified during planning: that import needs
    **no stubs at all** — the whole chain is stdlib. ⛔ Do not use the `ast`-lift recipe; it supplies
    module globals and would manufacture any name the module forgot to import.

    For the end-to-end D7 case only, the driver additionally needs the test module's fakes. Insert a
    minimal `pytest` shim into `sys.modules` BEFORE importing them — `mark` returning a pass-through
    decorator for any attribute, `fixture` as identity, `approx` and `raises` present but unused —
    then import `nestor_pulse_sdk.tests.workshop_fakes` (stdlib-only) and the helpers it needs, and
    drive the coroutine with `asyncio.run`. That is a stub of an UNCALLED non-stdlib leaf, which is
    the one stubbing this project still permits. If the shim turns out to need more surface than
    that, STOP and record the end-to-end case as OWED to Cloud Build rather than growing the shim —
    a harness that grows to fit is how four phantom results were produced in one day.

    Run the battery in this exact order and record the result of every row in a table in the SUMMARY:

      Row 1 — GREEN AFTER, `_restamp_groups`: a whitespace-divergent member gets the fresh rank.
      Row 2 — RED BEFORE, `_restamp_groups`: revert BOTH `_text_key` calls at that site to the
              committed `str(... or "")` form IN THE SOURCE FILE, re-run, confirm the assertion
              fails, then restore. Also do the ONE-SIDED revert (winner side only) and confirm it
              is still red — a one-sided fix is the CR-01 defect itself and must not read green.
      Row 3 — GREEN AFTER / RED BEFORE, `_stamp_discovery_ranks`: identical treatment of `numbered`.
      Row 4 — the no-merge assertion: a member matching no collapsed winner is still unmatched,
              both before and after (this row is green in both states BY DESIGN — state that, it is
              the control that proves the fix did not become permissive).
      Row 5 — `_text_key` parity with `research_division._text_key` over the corpus.
      Row 6 — GREEN AFTER, D7 pure: `enforce_group_coverage` + `_sweep_langs` yields `["nl"]` for
              `run_language="Dutch"` on the promotion rung and on the verbatim rung.
      Row 7 — RED BEFORE, D7: DELETE the second `_sweep_langs(final, ...)` call from the source,
              re-run the end-to-end case, confirm the repaired winner comes back with `langs == []`,
              then restore.

    Every "RED BEFORE" row must be produced by mutating the COMMITTED SOURCE TEXT and re-running —
    never by reasoning about what would happen, and never by editing the test. After each mutation,
    restore with `git checkout --` on the file and re-run the green row to prove the tree is back.
    Finish with `git diff` showing only the intended Task 1 + Task 2 changes.

    Then state plainly in the SUMMARY, in this order:
      - **`EXPECTED_FILES` contribution from this plan: 0.** No test FILE was added; all five/seven
        new tests live in `tests/test_workshop_scope_guard.py`, which is ALREADY named in
        `tribunal/cloudbuild.test-engine.yaml`'s `WANTED` list. 15.8-13 must reconcile against a
        stated zero, not infer it.
      - The exact number of new test functions added, so 15.8-13 can attribute its passed-count delta
        rather than merely note it. A count that did not rise must be EXPLAINED, not tolerated.
      - That **no pytest run exists on this machine** and the full suite run is OWED to Cloud Build
        at 15.8-13. Do not present the local battery as a suite pass.
      - That this plan touched NEITHER `cloudbuild.test-engine.yaml` (D-W5-5) NOR the three
        `register.get("drops")` reads (D-W5-6), and that `workshop_rank.py` is handed to 15.8-10 in
        wave 2 with only symbol-local changes.
  </action>

  <acceptance_criteria>
    - The SUMMARY contains a 7-row table with a before/after state per row and no row left blank.
    - Every RED row names the exact source-text mutation that produced it and confirms the restore.
    - Row 2 records BOTH the two-sided revert and the one-sided revert as red.
    - Row 4 is recorded as green-in-both-states with the reason stated.
    - `git status` is clean of scratch files; the driver lives only in the scratchpad.
    - `git diff --stat` at the end lists exactly two files.
    - The four SUMMARY statements above are present verbatim in substance.
  </acceptance_criteria>

  <files>
    (no repo files modified — proof only; the SUMMARY is the artifact)
  </files>

  <verify>
    <automated>cd tribunal && C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe "$SCRATCH/verify_15_8_03.py"</automated>
    Exit 0 with all seven rows printed and their expected states.
  </verify>

  <done>
    All seven rows recorded with real before/after evidence from source-text mutation; the tree is
    restored and `git diff` shows only the two intended files; the four handoff statements for
    15.8-13 and 15.8-10 are in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grouping LLM → group `members[].text` | Model-influenced text becomes one side of a rank join key |
| client form textarea → `_verbatim_winner` `text` | Client-authored text, truncated but never collapsed, becomes the other side |
| workshop candidate pool → `discovery` question `text` | Model-invented text keys the discovery rank join |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.8-03-01 | Tampering | `_restamp_groups` / `_stamp_discovery_ranks` join | mitigate | Both sides normalise through one `_text_key` at the comparison site, so a stale `rank` — and therefore wrong stakes via `research_division._stakes_for_rank` — cannot arise from a whitespace divergence. Pinned by Tests 1–3. |
| T-15.8-03-02 | Tampering | `_text_key` collapse scope | accept | A crafted text whose whitespace-collapsed form collides with another winner's could steal its rank. Accepted: the collapse is whitespace-only — no case folding, no truncation, no punctuation stripping — which is `research_division._text_key`'s documented bound. Anything wider would merge winners the tournament ranked apart, a strictly worse failure. Test 3 pins that the join still refuses non-matches. |
| T-15.8-03-03 | Denial of service | member `text` of a hostile type | mitigate | `_text_key` coerces to `str` inside `try/except`, so a list-, dict- or hostile-`__str__`-valued `text` yields `""` instead of raising `TypeError: unhashable type` out of the middle of the workshop stage. Asserted in Task 1's edge case. |
| T-15.8-03-04 | Information disclosure | D7 `langs` dropped on the repair path | mitigate | `_sweep_langs` after `enforce_group_coverage` guarantees a non-empty `langs`, so 15.2-13 still emits the angle-query language sentence and the repaired client question is searched in the right languages rather than silently English-only. Pinned by Tests 5–6. |
| T-15.8-03-05 | Elevation of privilege | none in scope | accept | This plan adds no auth path, no endpoint, no tenant-scoped query, and no new I/O. Both edited functions are pure and in-process. |
| T-15.8-03-SC | Tampering | supply chain | n/a | **No `npm` / `pip` / `cargo` install in this plan.** No dependency is added to `requirements.txt` or any lockfile, so the Package Legitimacy Gate does not apply and no `[ASSUMED]`/`[SUS]` checkpoint is owed. Stated explicitly so a reader does not read its absence as an omission. |

**ASVS L1, block on high:** no HIGH-severity threat in this register. T-15.8-03-01 and -04 are the
two that reach the measuring run's own numbers; both are mitigated in code and pinned by a
red-before/green-after test. Nothing here blocks.
</threat_model>

<verification>
1. `git merge-base HEAD <BASE> == <BASE>` asserted before the first edit (the 15/15 stale-base trap;
   `rev-list --count BASE..HEAD == 0` does not count as a check).
2. `git diff --stat` lists exactly two files: `workshop_rank.py` and `test_workshop_scope_guard.py`.
3. `git diff tribunal/cloudbuild.test-engine.yaml` is EMPTY (D-W5-5).
4. `git diff` contains no change to any `register.get("drops")` read (D-W5-6 — 15.8-10 owns those).
5. `grep -c "_sweep_langs(final" workshop_rank.py` == 2, and the second call sits between the
   `enforce_group_coverage(` call and the `_stamp_discovery_ranks(` call by line order.
6. `_text_key` is defined once in `workshop_rank.py` and used on BOTH sides of BOTH joins (four
   call sites total inside the two functions).
7. No module-level `research_division` import was added to `workshop_rank.py`.
8. `workshop_rank` still imports cleanly on the bundled interpreter: real import, no stubs, no
   ast-lift.
9. The seven-row red/green table is in the SUMMARY with every RED row naming its source mutation.
10. The SUMMARY states `EXPECTED_FILES` contribution = 0 and the exact new-test-function count.
</verification>

<success_criteria>
- A group member diverging from its winner only in whitespace is re-stamped with the FRESH rank at
  both comparison sites, and a member matching no winner is still unmatched.
- `workshop_rank._text_key` and `research_division._text_key` are pinned equal by a test, so the two
  copies cannot drift.
- A winner produced by either of `enforce_group_coverage`'s repair rungs reaches
  `_stage_b_result` with a non-empty `langs`, proved end-to-end in `topic` mode through a coverage
  repair that is asserted to have actually fired.
- Deleting the second `_sweep_langs` call, or reverting either `_text_key` join on either side,
  turns the corresponding test RED — proved by mutating the committed source text, not by argument.
- `enforce_group_coverage`'s signature and four-element return contract are unchanged; every edit is
  symbol-local, so 15.8-10 inherits `workshop_rank.py` in wave 2 without a merge hazard.
- No test file added, no gate config touched, no sibling plan's unmerged symbol called.
</success_criteria>

<output>
Create
`.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-03-SUMMARY.md`
when done. `.planning/` is gitignored — commit it with `git add -f`.

The SUMMARY must carry, in addition to the standard template:
- the seven-row red/green table with the exact source mutation behind every RED row;
- **`EXPECTED_FILES` contribution: 0** (no test FILE added; `test_workshop_scope_guard.py` is already
  in the engine gate's `WANTED` list) and the exact count of new test functions, so 15.8-13 can
  attribute its passed-count delta instead of noting it;
- the statement that no pytest run exists on this machine and the suite run is OWED to Cloud Build
  at 15.8-13;
- the handoff line for 15.8-10: `workshop_rank.py` changed only at `_text_key` (new),
  `_restamp_groups`, `_stamp_discovery_ranks`, `_sweep_langs` (new) and two call sites inside
  `run_workshop_stage_b`; the three `register.get("drops")` reads were deliberately NOT touched
  (D-W5-6).
</output>
