---
phase: 15.8-research-engine-redesign-yield-instrumentation-deploy-one-me
plan: 14
type: execute
wave: 5
depends_on:
  - 15.8-08
  - 15.8-13
files_modified:
  - infra/DEPLOY-RUNBOOK.md
autonomous: false
# This phase maps to no REQ-ID (see 15.8-PLAN-OUTLINE.md header). Plans trace to decision
# IDs. D-W5-1 is the decision that created the third unpaid migration this plan must prove;
# DEPLOY-15.8 is the carried deploy debt named in 15.8-CONTEXT.md § The blocking pre-conditions.
requirements: [D-W5-1, DEPLOY-15.8]

user_setup:
  - service: gcp
    why: "Every step of this plan is an operator action. The agent has no gcloud credentials, cannot submit a Cloud Build, cannot execute a Cloud Run Job and cannot read Cloud SQL. Nothing here may be marked proven from a command the agent did not run."
    dashboard_config:
      - task: "Submit the two Tribunal image builds, execute the repinned tribunal-migrate Job, deploy tribunal-api then tribunal-worker via the retargeted scripts, and paste the VERBATIM outputs back — including the three literal `Running upgrade` lines."
        location: "Cloud Shell or a local shell authenticated as tools@dotto.be against the Nestor Pulse project"
      - task: "Run the credential-free queue check (--no-source Cloud Build + cloud-sql-proxy + psql as worker_user) BEFORE the worker is deployed, and again immediately before the unpause."
        location: "Cloud Build, service-account nestor-run@ (runbook § 15.2.k step 2)"
      - task: "Re-verify the audit hash chain on DEPLOYED data after migration 0018, through the superadmin Re-verify affordance on V-01's run page."
        location: "Admin UI, /admin/pulse/runs/7dcf51d5-1153-4374-b444-c25d17eeea01"

must_haves:
  truths:
    - "THE TREE THAT WAS BUILT IS THE TREE THAT CARRIES ALL TEN BUILD PLANS. Every sentinel is a POSITIVE presence assertion, checked BEFORE the first cent of build spend, and any single miss ABORTS the deploy rather than deferring the question."
    - "All THREE tribunal upgrades are proven by their LITERAL log lines — `Running upgrade 0015 -> 0016`, `0016 -> 0017`, `0017 -> 0018` — quoted verbatim in the record. An exit code, a green Job and a head read are each individually insufficient and are never accepted as the proof."
    - "The queue was proven EMPTY by a check that cannot return a false empty, immediately before the worker was deployed AND again immediately before the unpause; the worker was the LAST deployable of the whole procedure."
    - "The audit hash chain is green POST-migration against DEPLOYED data, not only against a gate's throwaway Postgres (EU AI Act Art. 12)."
    - "Both Tribunal services mount ANTHROPIC_API_KEY from `Nestor_Claude_Temp` AFTER the deploy, confirmed by secret NAME, never by a value — a redeploy without the override silently repoints at an unfunded key."
    - "The measuring run executes the VALIDATED Wave-4 config, and the record states how that is known: the config lives in CODE DEFAULTS and the worker carries no `NESTOR_TRIBUNAL_WORKSHOP_*` env at all."
    - "All FOUR services are accounted for in the record — two redeployed at the shared SHA, two recorded CONFIRM-ONLY with their existing revision names and the diff evidence that says why no rebuild was warranted."
    - "Every value in the record came from a command whose output is quoted. There is no placeholder, no `<fill me>`, and no field carried from a previous phase's record."
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "the § 15.8 DEPLOY RECORD, filled with real values: the shared SHA, all four services, the three literal upgrade lines, both alembic heads from their own version tables, the queue proof and the chain proof"
      contains: "Running upgrade 0017 -> 0018"
  key_links:
    - from: "infra/DEPLOY-RUNBOOK.md § 15.8 DEPLOY RECORD"
      to: "the built image tag"
      via: "one shared $SHA recorded once and referenced by both Tribunal revisions"
      pattern: "tribunal-worker:.*[0-9]{8}-[0-9]{6}"
    - from: "tribunal-migrate Job"
      to: "tribunal-api:$SHA"
      via: "the repin, CONFIRMED by describe before execution — an unpinned Job is a silent no-op that exits 0 having applied nothing"
      pattern: "jobs describe tribunal-migrate"
    - from: "the three literal upgrade lines"
      to: "tribunal.tribunal_alembic_version"
      via: "two independent proofs of the same fact; neither substitutes for the other"
      pattern: "tribunal_alembic_version"
---

<objective>
**THE ONE DEPLOY.** The first deploy since 2026-07-27, and it ships **five waves at once**: phases
15.4, 15.5, 15.6, 15.7 and the build half of 15.8 are all in git and **none has ever run**.

Operator ruling, 2026-07-29, **not to be re-argued**: *"I don't want to measure anything unless we
finish all changes."* The trade-off was stated and accepted — with five waves landing together, an
odd result in the measuring run cannot be attributed to a single change.

Purpose: 15.8-15 is the ONE measuring run this redesign will ever get. Everything that makes that run
trustworthy is settled here, before a cent is spent: that the tree being built is the tree that
carries the work, that the schema the run writes into actually exists, that no worker claims anything
before we intend it to, and that the audit chain the run appends to is intact.

Output: two Tribunal images at one shared SHA, three paid alembic upgrades proven by their literal
log lines, `tribunal-api` and then `tribunal-worker` — **the worker LAST** — and the § 15.8 DEPLOY
RECORD filled with values that were read back rather than assumed.

**THE HEADLINE, AND IT IS NOT NEGOTIABLE.** Runbook § 15.2.k: **the worker deploys LAST, after the
queue is proven empty.** The as-written ordering caused the 2026-07-28 incident. The mechanism is
easy to talk past, so it is stated in full here rather than cross-referenced: **`--min-instances=0`
does NOT stop a worker booting.** A deploy starts a container to health-check it, and `runs/worker.py`'s
`while True:` **claims FIRST and sleeps LAST** — `claim_one()` runs at the top of the very first
iteration, before `asyncio.sleep(POLL_INTERVAL_SECONDS)` is ever reached. So no env lever helps:
`NESTOR_WORKER_POLL_INTERVAL` buys nothing and `NESTOR_WORKER_STALE_MINUTES` guards only the stale-
`running` reclaim arm — a `queued` row is claimable **at any age**. **An empty queue is the ONLY
protection**, which is why this plan proves it twice and never assumes it once.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-CONTEXT.md
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-PLAN-OUTLINE.md
@CLAUDE.md

Read these three, and read them before touching anything:

@infra/DEPLOY-RUNBOOK.md
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-PRECONDITIONS.md
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-13-SUMMARY.md

In the runbook, the two sections that matter are **§ 15.2.k** (the corrected ordering and the
2026-07-28 incident record) and **§ 15.8** (written by 15.8-12 — the procedure). This plan fills the
RECORD at the end of § 15.8. **Keep that boundary clean: 15.8-12 owns the procedure text, this plan
owns the record.** If the procedure is wrong, say so in the summary and record the deviation in the
record; do not silently rewrite 15.8-12's section.

<interfaces>
<!-- Established by direct read of the tree and of the live-deploy history at a2ec05f, 2026-08-04. -->
<!-- Every number and file path below was checked, not recalled. Verify they still hold; do not re-derive. -->

## What is actually being deployed — the surface is SMALLER than the phase

`git log --oneline 31a7f71..HEAD -- backend/ frontend/` returns **NOTHING**. All 127 commits since
the last deployed backend commit are under `tribunal/`. Verified at `a2ec05f`.

`31a7f71` is SHA **B** of the 2026-07-28 deploy — *"a transient seam 401/403 must not finalize a run
as failed"* — and it touched exactly two files, both under `backend/`. It is the newest commit that
reached a live service.

**Therefore:**

| Deployable | This deploy | Why |
|---|---|---|
| `tribunal-worker` | **REBUILD + DEPLOY** | carries all five waves of engine code |
| `tribunal-api` | **REBUILD + DEPLOY** | serves the read surfaces over the changed schema |
| `tribunal-migrate` (Job) | **REPIN + EXECUTE** | three unpaid migrations |
| `nestor-api` | **CONFIRM-ONLY** | zero diff under `backend/` since the commit it runs |
| `nestor-frontend` | **CONFIRM-ONLY** | zero diff under `frontend/` since that same commit, and the 15.2.k record confirms the run page shipped at SHA A |
| `nestor-migrate` (Job) | **NOT EXECUTED** | no `backend/app/db/alembic` revision added; the INTAKE head stays `0013` |

⚠ **This is a DEVIATION from the four-service shape of the 15.2 deploy record, and it is deliberate.**
The one-`$SHA` property is preserved for what changed. Rebuilding two unchanged images would add two
Cloud Builds and two live-service rollouts for zero code delta, and the frontend build in particular
takes four hand-typed `--substitutions` (`_API_BASE_URL`, `_FB_API_KEY`, `_FB_AUTH_DOMAIN`,
`_FB_PROJECT_ID`) whose mistyping breaks the live frontend — a real risk bought for no benefit.
**All four services are still RECORDED**; two of them as confirmed-unchanged with their existing
revision names. Task 1 re-derives the diff rather than trusting this paragraph, and if it comes back
non-empty the rebuild is back on.

## The migration chain — checked in the files, not remembered

| File | `revision` | `down_revision` |
|---|---|---|
| `alembic/versions/0015_run_events.py` | `"0015"` | `"0014"` |
| `alembic/versions/0016_source_resolved_url.py` | `"0016"` | `"0015"` |
| `alembic/versions/0017_claim_attribution.py` | `"0017"` | `"0016"` |
| `alembic/versions/0018_yield_instrumentation.py` | `"0018"` | `"0017"` — created by 15.8-05 |

Live TRIBUNAL head is **`0015`** (the 2026-07-28 record). **Neither 0016 nor 0017 has ever touched a
database**; 0018 is new this phase. That is **three** upgrades in one Job execution and therefore
**three** literal lines. Live INTAKE head is **`0013`** and stays there.

## Where the deploy scripts already do the right thing — do NOT "fix" them

`tribunal/infrastructure/cloud-run/deploy-worker.sh` and `deploy-api.sh` compose the FULL secret set
and the FULL plain env in variables and pass `--set-secrets` / `--set-env-vars`.

⛔ **Do NOT change these to `--update-secrets`.** The *"use `--update-secrets`, never `--set-secrets`"*
rule in `15.2-UAT.md` governs **hand-typed `gcloud run services update` commands against a live
service**, where `--set-secrets` would drop every binding you did not restate. The scripts are the
opposite case: they restate everything on purpose, and that is exactly why a mapping omitted from
their composed list is a *deploy-time* bug rather than a silent live-service regression. Two rules,
two situations. Conflating them here would break the scripts.

`deploy-worker.sh`'s committed plain env is exactly:
`NESTOR_ENV=prod,NESTOR_WORKER_POLL_INTERVAL=2.0,NESTOR_WORKER_STALE_MINUTES=60,NESTOR_TRIBUNAL_UNCAPPED=1,NESTOR_OPENAI_DR_MODEL=gpt-5.6-sol`

`MIN_INSTANCES` defaults to **1**. `TRIBUNAL_ANTHROPIC_SECRET` defaults to the committed
`Nestor_Claude2`, which is **not funded**. Both need an override on every invocation in this plan.

## The validated Wave-4 config is the CODE DEFAULTS — this is what makes the measurement mean anything

`workshop_loop.py` reads its tunables at import with these defaults:

    _TOURNAMENT_ROUNDS_MIN = 6      _LOOP_MAX_ROUNDS  = 10
    _TOURNAMENT_ROUNDS_MAX = 10     _LOOP_MIN_ROUNDS  = 4
    _FLOOR_PER_QUESTION    = 5      _CROSS_CUTTING_SLOTS = 2

Those are the numbers the Wave-4 local harness validated (one global loop, a floor of 5 winners per
client question, 2 cross-cutting). The worker's committed env carries **no `NESTOR_TRIBUNAL_*`
tunable except `UNCAPPED`**, so the run executes on those defaults — and because `--set-env-vars`
replaces the whole plain env, that stays true across this deploy without anyone doing anything.

**Record this as a positive fact, not as an absence.** If a `NESTOR_TRIBUNAL_WORKSHOP_*` variable
ever appears on the live worker, 15.8-15 is measuring a config nobody validated.

## The chain proof on DEPLOYED data

`cloudbuild.test-critical.yaml` provisions its OWN throwaway Postgres. Re-running it after the
migration proves the chain LOGIC against the new DDL — that is what 15.2 recorded as *34 passed* —
but it says nothing about the rows in Cloud SQL.

The deployed-data path exists and is superadmin-only:
`POST /intakes/{intake_id}/research/{run_id}/verify-chain` on `nestor-api` (`reverify_chain` in
`backend/app/api/research_routes.py`), which proxies through
`tribunal_client.verify_chain` to `GET {tribunal_api}/api/audit/verify/{run_id}` and returns
`{"chain_status": ...}`. In the UI it is the **Re-verify** affordance on the run page. The intake id
is not recorded anywhere in the run reports — resolve it through the run page or the run→intake
locate verb rather than guessing one.

V-01's run id: **`7dcf51d5-1153-4374-b444-c25d17eeea01`**.

## The queue check, and why a green build is not an empty queue

Runbook § 15.2.k step 2 carries the working recipe: `--no-source` Cloud Build as `nestor-run@`,
cloud-sql-proxy to `$GOOGLE_PROJECT:$REGION:nestor-pg`, `psql` on `127.0.0.1:5432`. **`nestor-run@`
lacks `logging.logWriter`, so the build's stdout is LOST and `gcloud builds log` returns an empty
`REMOTE BUILD OUTPUT`.** A SUCCESS there is not an answer — the answer must be carried in the EXIT
STATUS. Connect as **`worker_user`**, never `app_user`: `app_user` is tenant-scoped and returns zero
rows without a bound `app.tenant_id`, which is indistinguishable from an empty queue and is the most
expensive false negative in this repo.

The control row for the vacuity check is now `d6bb3aae-33e7-49fd-aa35-57dc529e05b3`, status
**`cancelled`** since 2026-07-28 09:53:25Z.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: THE ABORT GATE — positive-presence sentinels, the two pre-conditions, and the real deploy surface</name>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/15.8-*/15.8-PRECONDITIONS.md (whole file — 15.8-08's dated record of BOTH blocking pre-conditions)
    - .planning/phases/15.8-*/15.8-13-SUMMARY.md (whole file — the gate build ids, the engine gate's passed count and its reconciled EXPECTED_FILES, the gates gate's count, and the batched revert-and-confirm-red result)
    - infra/DEPLOY-RUNBOOK.md § 15.8 (written by 15.8-12 — the procedure this plan executes) and § 15.2.k (the ORDERING CORRECTION block at its head, and step 4's warning in full)
    - .planning/phases/15.8-*/15.8-PLAN-OUTLINE.md § Shared-file conflicts (which plan owns which file, so a missing sentinel can be attributed to a plan rather than guessed at)
  </read_first>
  <action>
Run the whole gate BEFORE anything is submitted to Cloud Build. Nothing in this task spends money and
nothing in it needs credentials, which is precisely why it comes first.

**(a) The tree is clean and is not stale.** `git status --porcelain` must be EMPTY. Record
`git log --oneline -1`. The worktree stale-base trap is **15/15** in this repo, most recently 2/2 in
phase 15.7 at 595-601 commits behind, and `git rev-list --count BASE..HEAD` returns **0 — green —
while stale**, because the stale ref is usually the merge-base. Assert the MERGE-BASE against the
phase base, not the count. Also sweep `git worktree list`: a stale worktree from a halted run has sat
on disk in this repo before.

**(b) The POSITIVE-PRESENCE SENTINELS.** One named symbol per build plan, each introduced by that
plan, each asserted PRESENT. A stale tree submits a Cloud Build of the WRONG SOURCE and returns a
confidently green result, so this is the only thing standing between a wasted deploy and a measuring
run of code that was never merged. Attribute every miss to its owning plan by name and **ABORT** —
do not proceed and check later, and do not weaken a grep to make it pass.

The sentinels, by plan:

  * **15.8-01** — `_resolve_ceiling` in `pipeline/tribunal/question_grouping.py`
  * **15.8-02** — `_dispatch_was_uniform` in `pipeline/tribunal/pipeline.py`
  * **15.8-03** — `def _text_key` AND `_sweep_langs` in `pipeline/tribunal/workshop_rank.py`
  * **15.8-04** — `def count_drops` in `pipeline/tribunal/workshop_register.py` AND
    `DROP_CLUSTERED_ONTO_LIVE` in `pipeline/tribunal/workshop.py`
  * **15.8-05** — all four files exist: `alembic/versions/0018_yield_instrumentation.py`,
    `runs/yield_records.py`, `db/models/assignment_yield.py`, `db/models/workshop_round_yield.py`;
    and 0018's `down_revision` is `"0017"`
  * **15.8-07** — `EXPECTED_FILES` present in `tribunal/cloudbuild.test-gates.yaml`
  * **15.8-08** — `_scrub_urls_in_value` in `audit/gcs_blob.py`
  * **15.8-09** — `yield_records` referenced from `pipeline/tribunal/` AND `parent_kind` present there
  * **15.8-10** — `new_entrants_top_n` in `pipeline/tribunal/workshop_loop.py` AND `yield_records`
    referenced from `pipeline/tribunal/workshop_rank.py`
  * **Wave 3 (15.6), carried** — `max_riders` present in `attach_discovery_riders`'s signature in
    `question_grouping.py`, **and `max_size` absent from that signature**. Assert BOTH. The positive
    half is the reliable one; a bare absence check reads green on an empty file, which is why this
    repo's own gate rules forbid unpaired `== 0` gates.
  * **Wave 4 (15.7), carried** — `_LOOP_MIN_ROUNDS` in `workshop_loop.py`

**If a sentinel name disagrees with what a plan actually landed**, reconcile it against that plan's
own `must_haves.artifacts[].contains` field in its PLAN.md and its SUMMARY — and use the plan's name.
Do not delete the sentinel. A sentinel you could not resolve is an ABORT, not a footnote.

**(c) The two blocking pre-conditions, from `15.8-PRECONDITIONS.md`, not from memory.**

  * **The `gpt-5.6-sol` cost row** must be in one of exactly two recorded states: published rates, or
    a dated `no-published-rate` ruling. **Nulls are not a third state** — `_rate()` turns a null into
    `Decimal("0")` and produces a confident **$0.00**. If the record shows anything else, ABORT.
  * **The audit-blob redaction scan.** If it came back POSITIVE, **the SerpApi key must be rotated
    before this deploy.** It rides in a query PARAMETER and an unredacted body freezes a live
    credential into the audit bucket under 7-year retention. Rotation means a NEW VERSION on the
    existing `Nestor_SERP` secret; both deploy scripts bind `:latest`, so the new revisions pick it up
    with no script change. Record the rotation in the deploy record. **This is NOT covered by any
    deferral** — do not confuse it with the next bullet.
  * ⚠ **`Nestor_Claude_Temp` rotation is DEFERRED TO GO-LIVE** (operator, 2026-08-03). The key
    transited a chat in plaintext and is live on both Tribunal services; **that risk is deliberately
    accepted** for the pre-go-live period. **Do not rotate it, do not block on it, do not re-raise it
    as a gap.** It is a decision. It still gets its override on every deploy command in this plan.

**(d) The gate is already paid — read 15.8-13's record, do not re-run it.** Both Cloud Build configs
green on the merged tree, with their build IDs, their `collecting:` counts and the reconciled
`EXPECTED_FILES`. Baseline to beat: `7c89be5c` = **1538 passed / 0 failed / 13 skipped**,
`collecting: 36 of 36`; gates baseline `2eae97e6` = 187 passed, 2 deselected. If 15.8-13's summary
carries anything other than a green, reconciled result, ABORT — this plan does not re-litigate the
gate and does not substitute its own run for it.

**(e) Compute the real deploy surface rather than inheriting the 15.2 four-service shape.** Reference
commit `31a7f71` — SHA B of the 2026-07-28 deploy, the newest commit that reached a live service.
Assert the `backend/` and `frontend/` diffs against HEAD. If BOTH are empty, `nestor-api` and
`nestor-frontend` are CONFIRM-ONLY and no `nestor-migrate` execution is scheduled. If EITHER is
non-empty, that image is back in the build list at the shared SHA and this task says so explicitly
rather than letting the omission stand. Note the one residual assumption in the summary: the live
frontend image was built at SHA A (a tree at or before `31a7f71`), and the evidence that it carries
today's `frontend/` is `31a7f71` touching only `backend/` plus § 15.2.k's own record line confirming
the run page shipped at SHA A.

**(f) Write the PREFLIGHT block into `infra/DEPLOY-RUNBOOK.md`'s § 15.8 DEPLOY RECORD** — the HEAD
SHA, the sentinel results, the two pre-condition states with their dates, 15.8-13's build ids and
counts, and the computed deploy surface. Append to the record only; do not edit 15.8-12's procedure
text above it.
  </action>
  <acceptance_criteria>
    - `git status --porcelain` prints nothing, and `git log --oneline -1` is recorded verbatim in the record.
    - `git merge-base HEAD <PHASE_BASE_SHA>` equals `<PHASE_BASE_SHA>`. The bare `rev-list --count` is NOT accepted as the staleness proof and is not recorded as one.
    - Every sentinel above returns at least one match, each attributed to its owning plan, and the counts are recorded. ZERO misses. A miss is recorded as an ABORT with the plan named.
    - `grep -c "max_size" <the attach_discovery_riders signature lines>` is 0 AND `max_riders` is present in that same signature — both halves recorded.
    - `15.8-PRECONDITIONS.md` shows the cost row in exactly one of its two legal states, with a date, and the record quotes which.
    - The redaction scan result is quoted from `15.8-PRECONDITIONS.md`; if positive, the SerpApi rotation is scheduled BEFORE the build step and said so in the record.
    - `Nestor_Claude_Temp` appears in the record once, as CLOSED-BY-DECISION (2026-08-03), and nowhere as an open item or a gap.
    - 15.8-13's two build IDs, the engine gate's passed count and reconciled `EXPECTED_FILES`, and the gates gate's count are quoted verbatim from its SUMMARY.
    - `git diff --stat 31a7f71..HEAD -- backend/ frontend/` output is quoted verbatim — including when it is empty — and the record states the resulting deploy surface as a decision with its evidence.
    - No `gcloud` command has been run in this task. Nothing has been built. Nothing has been deployed.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp" && test -z "$(git status --porcelain)" && for s in "pipeline/tribunal/question_grouping.py:_resolve_ceiling" "pipeline/tribunal/pipeline.py:_dispatch_was_uniform" "pipeline/tribunal/workshop_rank.py:def _text_key" "pipeline/tribunal/workshop_rank.py:_sweep_langs" "pipeline/tribunal/workshop_register.py:def count_drops" "pipeline/tribunal/workshop.py:DROP_CLUSTERED_ONTO_LIVE" "pipeline/tribunal/workshop_loop.py:new_entrants_top_n" "pipeline/tribunal/workshop_loop.py:_LOOP_MIN_ROUNDS" "pipeline/tribunal/question_grouping.py:max_riders" "audit/gcs_blob.py:_scrub_urls_in_value"; do f="tribunal/nestor_pulse_sdk/${s%%:*}"; p="${s#*:}"; grep -q -- "$p" "$f" || { echo "ABORT sentinel MISSING: $p in $f"; exit 1; }; done && for f in alembic/versions/0018_yield_instrumentation.py runs/yield_records.py db/models/assignment_yield.py db/models/workshop_round_yield.py; do test -f "tribunal/nestor_pulse_sdk/$f" || { echo "ABORT file MISSING: $f (15.8-05)"; exit 1; }; done && grep -q 'down_revision.*"0017"' tribunal/nestor_pulse_sdk/alembic/versions/0018_yield_instrumentation.py && grep -q "EXPECTED_FILES" tribunal/cloudbuild.test-gates.yaml && grep -rq "yield_records" tribunal/nestor_pulse_sdk/pipeline/tribunal/ && test -f .planning/phases/15.8-*/15.8-PRECONDITIONS.md && echo "ABORT GATE CLEAR"</automated>
  </verify>
  <done>Every sentinel is present and attributed, both pre-conditions are settled with dates, the gate is recorded as already paid, the deploy surface is computed rather than inherited, and the PREFLIGHT block is in the record — with no build and no deploy having happened.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Build both images at ONE $SHA, prove the queue EMPTY, and pay the three alembic upgrades</name>
  <what-built>
Task 1 cleared the abort gate on the tree, the pre-conditions and the already-paid gate. Nothing has
been built and nothing has been deployed.

Ready for the operator: the exact build commands, the queue check that cannot lie, and the migration
step with the three literal strings that settle it. **No service is deployed in this task.** Build,
prove, migrate — in that order, for the reason § 15.2.k states twice: a service that ships ahead of
its own migration fails on a missing relation, in a loop, on every request.
  </what-built>
  <how-to-verify>
Run these as the operator against the Nestor Pulse project and paste the VERBATIM output back. Set
the session exports first exactly as § 15.2.k does (`GOOGLE_PROJECT`, `REGION`).

**1. Build the two Tribunal images at ONE shared `$SHA`. BUILD ONLY.**

    export SHA="$(date +%Y%m%d-%H%M%S)"
    gcloud builds submit tribunal --config=tribunal/cloudbuild.worker.yaml \
      --substitutions=_IMAGE=${REGION}-docker.pkg.dev/${GOOGLE_PROJECT}/nestor/tribunal-worker:${SHA} \
      --project="$GOOGLE_PROJECT"
    gcloud builds submit tribunal --config=tribunal/cloudbuild.api.yaml \
      --substitutions=_IMAGE=${REGION}-docker.pkg.dev/${GOOGLE_PROJECT}/nestor/tribunal-api:${SHA} \
      --project="$GOOGLE_PROJECT"

One SHA so a mixed-revision state is unreachable by accident and the revision list reads as one fact.
**Both rebuild:** the worker EXECUTES the engine, the api SERVES the read surfaces over the changed
schema.

⚠ **NEVER pipe a build submit into `tail`, `head` or anything else.** A pipeline returns the PIPE's
exit code, so a FAILED build reports **exit 0**. Read the build TEXT:

    gcloud builds describe <BUILD_ID> --project="$GOOGLE_PROJECT" --format='value(status,logUrl)'

⚠ **An EXPIRED build looks identical to QUEUED and is NOT a result.** One 15.7 build expired in the
queue and was nearly read as pending. `SUCCESS` is the only pass.

**2. Prove the queue is EMPTY.** Runbook § 15.2.k step 2 carries the working recipe — use it, do not
invent a new one. `--no-source` Cloud Build as `nestor-run@`, cloud-sql-proxy, `psql` as
**`worker_user`** (never `app_user` — it is tenant-scoped and returns zero rows unbound, which is
indistinguishable from an empty queue).

    SELECT id, status, started_at, heartbeat_at, reclaim_count
      FROM tribunal.run
     WHERE status IN ('queued', 'running')
     ORDER BY created_at;

**Expect ZERO rows.** `d6bb3aae` was cancelled 2026-07-28 09:53:25Z and V-01's `7dcf51d5` completed.
Any `queued` or `running` row is a **STOP**: identify it and decide deliberately, because the worker
deploy in Task 3 would claim it on boot.

⚠ **`nestor-run@` lacks `logging.logWriter`, so the build's stdout is LOST** and a green build proves
nothing. **Carry the answer in the EXIT STATUS**, with the anti-vacuity checks folded into the success
condition: `exit 91` if `SELECT count(*) FROM tribunal.run` is 0 (RLS hiding everything), `exit 92` if
the control row `d6bb3aae-33e7-49fd-aa35-57dc529e05b3` is not visible as `cancelled`, `exit 93` if
anything is claimable. **Then prove the gate can fail:** re-run once with the claimable assertion
inverted and confirm it exits 93. Paste both exit codes.

**3. REPIN `tribunal-migrate` to the `$SHA` api image, CONFIRM the repin, then execute.**

    gcloud run jobs update tribunal-migrate --region "$REGION" --project="$GOOGLE_PROJECT" \
      --image "${REGION}-docker.pkg.dev/${GOOGLE_PROJECT}/nestor/tribunal-api:${SHA}"
    gcloud run jobs describe tribunal-migrate --region "$REGION" --project="$GOOGLE_PROJECT" \
      --format='value(spec.template.spec.template.spec.containers[0].image)'   # must echo :${SHA}
    gcloud run jobs execute tribunal-migrate --region "$REGION" --project="$GOOGLE_PROJECT" --wait

**The repin is not optional.** An unpinned Job is a silent no-op: alembic connects, finds itself at
head on the stale revision set, exits 0, and logs no upgrade line at all. Confirm it before executing.

**4. THE THREE LITERAL LINES. This is the proof, and there is no substitute for it.**

    Running upgrade 0015 -> 0016
    Running upgrade 0016 -> 0017
    Running upgrade 0017 -> 0018

⛔ **`Container called exit(0)` is NOT proof. A green Job is NOT proof. A head read is NOT proof of
the path taken.** Neither `0016` nor `0017` has ever touched a database and `0018` is new this phase,
so all three must appear. **Seeing two and not the third means the Job ran on an image built before
15.8-05 — repin and re-execute.** This repo has been burned by exactly this: the backend gate has
never once printed one of these lines. Paste all three verbatim, with their surrounding alembic
INFO prefixes.

**5. Read the schema back — both lines, each from its OWN version table.** One does not imply the
other, and a head read on the wrong table proves nothing at all.

    SELECT version_num FROM tribunal.tribunal_alembic_version;   -- expect: 0018
    SELECT version_num FROM public.alembic_version;              -- expect: 0013 (UNCHANGED)

Then confirm the two new tables exist with RLS actually on — a new table that inherits nothing is a
cross-tenant leak, and 15.8-05's own must-have is USING **and** WITH CHECK (a USING-only policy lets
the read pass while the write fails):

    SELECT relname, relrowsecurity, relforcerowsecurity
      FROM pg_class WHERE relname IN ('assignment_yield','workshop_round_yield');
    SELECT tablename, policyname, qual IS NOT NULL AS has_using, with_check IS NOT NULL AS has_check
      FROM pg_policies WHERE tablename IN ('assignment_yield','workshop_round_yield');

**The INTAKE line is CONFIRM-ONLY.** No `backend/app/db/alembic` revision was added, so
`nestor-migrate` is **NOT executed** and `public.alembic_version` must still read `0013`. If it does
not, something outside this phase moved and that is a STOP.
  </how-to-verify>
  <resume-signal>Paste: both build IDs with their `gcloud builds describe` status, the queue check's exit code AND the inverted re-run's exit code, the repin `describe` output, all THREE `Running upgrade` lines verbatim, both `version_num` reads, and the two RLS reads. Then type `migrated` — or name what stopped you.</resume-signal>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <action>
Before the checkpoint: state plainly in the summary that every step here is an OPERATOR action, that
the agent has no gcloud credentials, and that **nothing in this task may be marked proven from a
command the agent did not run.** Do not pre-fill any value in the record.

After the operator answers: append the BUILD + QUEUE + MIGRATION block to § 15.8's DEPLOY RECORD with
the pasted values verbatim — the shared `$SHA`, both build IDs and their statuses, the queue result
with both exit codes, and the three upgrade lines quoted in full including their alembic prefixes.
Record the two version_num reads as **two separate fields**; a single "migrations fine" is not a
record of either.

If only two of the three upgrade lines appeared, do NOT record a partial success and do NOT proceed
to Task 3. Record the repin state, name the likely cause (a Job image predating 15.8-05), and stop.
  </action>
  <acceptance_criteria>
    - All three literal lines `Running upgrade 0015 -> 0016`, `0016 -> 0017`, `0017 -> 0018` appear verbatim in the record. Nothing else is accepted in their place.
    - Both build IDs appear with a status read from `gcloud builds describe`, and the record says explicitly that no build status was taken from a shell exit code or a piped command.
    - The queue check's exit code is recorded AND the inverted re-run's exit 93 is recorded — the proof that the gate can fail.
    - `tribunal.tribunal_alembic_version` = `0018` and `public.alembic_version` = `0013`, recorded as two separate fields with their own reads.
    - Both new tables show `relrowsecurity` AND `relforcerowsecurity` true, and each policy shows `has_using` AND `has_check` true.
    - The record states that `nestor-migrate` was NOT executed and why (no backend alembic revision this phase).
    - No Cloud Run service was deployed in this task — the record says so, and the revision names in Task 3 are all newer than this task's timestamp.
  </acceptance_criteria>
  <verify>
    <human-check>The operator has pasted all three `Running upgrade` lines verbatim, both build statuses read via `gcloud builds describe`, and both alembic head reads. No value in the record was supplied by the agent.</human-check>
    <automated>cd "C:/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp" && grep -q "Running upgrade 0015 -> 0016" infra/DEPLOY-RUNBOOK.md && grep -q "Running upgrade 0016 -> 0017" infra/DEPLOY-RUNBOOK.md && grep -q "Running upgrade 0017 -> 0018" infra/DEPLOY-RUNBOOK.md && echo "THREE PROOFS RECORDED"</automated>
  </verify>
  <done>Two images exist at one shared SHA with statuses read from build text, the queue was proven empty by a check that was itself proven able to fail, and all three tribunal upgrades are paid and proven by their literal lines — with no service yet deployed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy tribunal-api, prove the chain, then the worker LAST — and only then unpause</name>
  <what-built>
The schema is at `0018`, the queue was empty, and both images exist at `$SHA`. Nothing is deployed.

Ready for the operator: the two service deploys in the only safe order, the post-migration chain
proof, and the read-backs. **The worker is the LAST deployable of the entire procedure**, and the
unpause is a separate deliberate act after that.
  </what-built>
  <how-to-verify>
**Order mapping onto § 15.2.k, stated so nobody re-derives it under time pressure.** § 15.2.k's
corrected sequence is `0 → 1 → 2 → 3 → 5 → 6 → 4 → 7 → 8 → 9 → 10`. Here: **0/1/2/3 were Task 2**;
**5** is the api deploy below (`nestor-api` and the frontend are CONFIRM-ONLY, per Task 1); **6** —
resolve the stuck run — **has nothing to resolve**, so it is REPLACED by re-proving the queue empty,
which is the property step 6 existed to establish; **4** is the worker, LAST; **7** is the unpause;
**8/10** are the read-backs and the record.

**1. Deploy `tribunal-api` at the shared `$SHA`.**

    TRIBUNAL_ANTHROPIC_SECRET=Nestor_Claude_Temp IMAGE_TAG="$SHA" \
      tribunal/infrastructure/cloud-run/deploy-api.sh

⚠ **The `TRIBUNAL_ANTHROPIC_SECRET` override is NOT optional.** The committed default is
`Nestor_Claude2`, which is **not funded**. A deploy without the override silently repoints the
service at an empty key and the measuring run walls mid-flight. Use the retargeted script, never a
hand-rolled `gcloud run deploy` — the script preserves the Phase-14 lockdown (`tribunal-run` SA,
`--no-allow-unauthenticated`, invoker = `nestor-run` only).

⚠ **Do NOT "fix" the script to use `--update-secrets`.** It composes the full set on purpose. The
`--update-secrets` rule governs hand-typed `services update` commands against a live service, which
this is not.

Then confirm the tribunal-api URL is **UNCHANGED** — capture it WITHOUT a path (the OIDC audience is
the path-less `service_url`; Pitfall 4) and do **not** re-set the seam env from it:

    gcloud run services describe tribunal-api --region "$REGION" --project="$GOOGLE_PROJECT" \
      --format='value(status.url)'

`/readyz` with an identity token must return 200 (the service is `--no-allow-unauthenticated`).

**2. Prove the audit hash chain POST-migration. EU AI Act Art. 12 — a RED chain is a STOP.**

Two proofs, and neither substitutes for the other:

  * **The gate, re-run against the migrated DDL:**
    `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT"`.
    15.2 recorded **34 passed, 0 failed, 0 skipped** here. Read the status via
    `gcloud builds describe`, never a pipe.
  * **DEPLOYED data.** Open V-01's run page
    `/admin/pulse/runs/7dcf51d5-1153-4374-b444-c25d17eeea01` as a superadmin, resolve the intake
    through the page (the intake id is recorded nowhere — do not guess one), and use the **Re-verify**
    affordance. It calls `POST /intakes/{intake_id}/research/{run_id}/verify-chain` on `nestor-api`,
    which proxies to `GET /api/audit/verify/{run_id}` on `tribunal-api`. **Expect
    `{"chain_status": "verified"}`.** Migration 0018 creates new tables and alters no hashed column,
    so the chain must be untouched — if it is not, STOP; something changed a frozen field.

**3. RE-PROVE THE QUEUE EMPTY.** Same recipe as Task 2 step 2, run again, now. Minutes have passed
and the property that makes the next command safe is a *current* fact, not a remembered one.

**4. Deploy `tribunal-worker` — LAST — and ship it PAUSED.**

    MIN_INSTANCES=0 TRIBUNAL_ANTHROPIC_SECRET=Nestor_Claude_Temp IMAGE_TAG="$SHA" \
      tribunal/infrastructure/cloud-run/deploy-worker.sh

⛔ **`MIN_INSTANCES=0` does NOT make this safe and is not why it is here.** The deploy BOOTS a
container to health-check it and the loop **claims FIRST, sleeps LAST**. `min-instances=0` governs
*steady state* only. The empty queue from step 3 is the protection; `MIN_INSTANCES=0` merely keeps
the unpause a separate deliberate act. Observed twice on 2026-07-28: at 08:22:57Z the deploy claimed
`d6bb3aae` and burned ~15 minutes of paid pipeline; at 12:35Z the same boot happened harmlessly
because the queue had been proven empty first.

`MIN_INSTANCES` defaults to **1** — without the override this one command unpauses the worker and
re-arms reclaim in the same atomic instant.

**5. ONLY NOW unpause.**

    gcloud run services update tribunal-worker --region "$REGION" --project="$GOOGLE_PROJECT" \
      --min-instances=1

Watch one poll cycle and confirm it **claims NOTHING**. An idle worker is the correct observation.
**If it claims something, STOP and report** — the queue proof was wrong.

**6. Read back — verbatim, never "looks right".**

    # (a) the worker's plain env
    gcloud run services describe tribunal-worker --region "$REGION" --project="$GOOGLE_PROJECT" \
      --format='value(spec.template.spec.containers[0].env)' | tr ',' '\n' \
      | grep -E 'NESTOR_WORKER|NESTOR_TRIBUNAL|NESTOR_OPENAI_DR_MODEL|ABORTED'
    # expect: NESTOR_WORKER_STALE_MINUTES=60 · NESTOR_TRIBUNAL_UNCAPPED=1 ·
    #         NESTOR_OPENAI_DR_MODEL=gpt-5.6-sol · NO NESTOR_RUN_ABORTED_MARKER ·
    #         and NO NESTOR_TRIBUNAL_WORKSHOP_* of any kind

    # (b) secret bindings by NAME only, never a value
    for SVC in tribunal-worker tribunal-api; do
      gcloud run services describe "$SVC" --region="$REGION" --project="$GOOGLE_PROJECT" \
        --format='value(spec.template.spec.containers[0].env)' | tr ',' '\n' \
        | grep -E 'ANTHROPIC_API_KEY|SERPAPI_API_KEY'
    done
    # expect Nestor_Claude_Temp + Nestor_SERP on BOTH

    # (c) all four services' revisions and traffic
    for SVC in tribunal-worker tribunal-api nestor-api nestor-frontend; do
      gcloud run services describe "$SVC" --region="$REGION" --project="$GOOGLE_PROJECT" \
        --format='value(status.latestReadyRevisionName,status.traffic)'
    done
    # the two Tribunal services must be NEW revisions at 100% traffic;
    # nestor-api and nestor-frontend must be the SAME revisions as before this session

    # (d) digest pin — each new revision must resolve to an @sha256: digest, never :latest

**The `NESTOR_TRIBUNAL_WORKSHOP_*` absence in (a) is a POSITIVE finding, not a formality.** The
validated Wave-4 config — one global loop, a floor of 5 winners per client question, 2 cross-cutting,
10 max rounds — lives in the CODE DEFAULTS in `workshop_loop.py`. If any such variable is set on the
live worker, 15.8-15 measures a config nobody validated. Record the absence explicitly.
  </how-to-verify>
  <resume-signal>Paste: the api deploy output + the unchanged tribunal-api URL + the `/readyz` status, the `test-critical` build ID and its counts, the `{"chain_status": ...}` response, the SECOND queue check's exit code, the worker deploy output, the unpause confirmation with the claims-nothing observation, and read-backs (a) through (d). Then type `deployed` — or name what stopped you.</resume-signal>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <action>
Before the checkpoint: restate the ordering and its mechanism in the operator's own terms, in two
sentences, without cross-referencing a section they would have to go and read. The worker deploys
last because the deploy boots the container and the loop claims before it sleeps; the empty queue is
the only protection.

After the operator answers: append the DEPLOY block to § 15.8's DEPLOY RECORD. Record all four
services — the two new revisions with their image tags and digests, and the two CONFIRM-ONLY services
with their existing revision names and the Task 1 diff evidence that says why they were not rebuilt.

If ANY read-back disagrees with its expectation — a wrong secret name, a `NESTOR_TRIBUNAL_WORKSHOP_*`
variable present, traffic split rather than 100%, a `:latest` image, or a worker that claimed
something — record the disagreement as a finding and STOP. Do not reconcile it in prose and do not
proceed to 15.8-15 on a service whose read-back did not match.
  </action>
  <acceptance_criteria>
    - `tribunal-api` and `tribunal-worker` both show NEW revisions at the shared `$SHA` with 100% traffic and an `@sha256:` digest, recorded by name.
    - `nestor-api` and `nestor-frontend` show the SAME revision names as before this session, recorded as CONFIRM-ONLY with the diff evidence.
    - Both Tribunal services mount `ANTHROPIC_API_KEY` from `Nestor_Claude_Temp` and `SERPAPI_API_KEY` from `Nestor_SERP`, confirmed by NAME. No secret VALUE appears anywhere in the record.
    - The record contains the SECOND queue proof, timestamped after the api deploy and before the worker deploy, with its exit code.
    - The record shows the worker revision's creation time is AFTER the api revision's — the worker was demonstrably last.
    - The chain proof appears twice: the `test-critical` build ID with its pass/fail/skip counts, and the `{"chain_status": "verified"}` response from the deployed-data Re-verify.
    - `NESTOR_WORKER_STALE_MINUTES=60`, `NESTOR_OPENAI_DR_MODEL=gpt-5.6-sol` and `NESTOR_TRIBUNAL_UNCAPPED=1` are recorded present; `NESTOR_RUN_ABORTED_MARKER` and every `NESTOR_TRIBUNAL_WORKSHOP_*` are recorded ABSENT, with the sentence saying why the absence matters.
    - The unpause is recorded as a separate act with the claims-nothing observation.
  </acceptance_criteria>
  <verify>
    <human-check>The operator confirms the worker was deployed after the api and after the second queue proof, that it claimed nothing on unpause, and that every read-back matched. Every value in the record was pasted by the operator.</human-check>
    <automated>cd "C:/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp" && grep -q "Nestor_Claude_Temp" infra/DEPLOY-RUNBOOK.md && grep -q "chain_status" infra/DEPLOY-RUNBOOK.md && ! grep -qiE "sk-[A-Za-z0-9]{20}|AIza[A-Za-z0-9_-]{30}" infra/DEPLOY-RUNBOOK.md && echo "DEPLOY BLOCK RECORDED, NO CREDENTIAL VALUE PRESENT"</automated>
  </verify>
  <done>Both Tribunal services are live at one shared SHA with the worker demonstrably last, the chain is green on deployed data post-migration, and every read-back matched its expectation and is in the record.</done>
</task>

<task type="auto">
  <name>Task 4: Close the § 15.8 DEPLOY RECORD — complete, quoted, and honest about what it does not settle</name>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § 15.2.k step 10, "THE COMBINED DEPLOY RECORD" (the shape to follow — the field table, the separately-labelled change lists, and the operator-affirmed sentence)
    - .planning/phases/15.2-*/15.2-UAT.md § Deploy Record (the live SHA/revision shape: a service/revision/image/traffic table, the migration execution id with its literal line, and secret bindings by name)
    - infra/DEPLOY-RUNBOOK.md § 15.8 as it now stands, including the three blocks Tasks 1-3 appended
  </read_first>
  <action>
Consolidate the three appended blocks into ONE readable § 15.8 DEPLOY RECORD, following § 15.2.k
step 10's shape and 15.2-UAT's table form. This is not a formatting pass — a record a reader has to
reassemble from three appendices is the record that gets skipped at 3am.

It must carry:

  * **The field table** — date and who ran it; the ONE shared `$SHA`; a service/revision/image/traffic
    row for **all four** services, with the two CONFIRM-ONLY rows saying so and carrying the diff
    evidence; the migrate Job execution id.
  * **The three literal upgrade lines**, quoted in full with their alembic prefixes, and **both**
    alembic heads read from **their own** version tables — TRIBUNAL `0018`, INTAKE `0013` — as two
    separate fields.
  * **The two gate build IDs from 15.8-13** with their counts, and the `test-critical` re-run's
    counts. State plainly that this plan did not re-run the phase gate and did not need to.
  * **The queue state before the worker deploy** — both proofs, both exit codes, and the inverted
    re-run that showed the check can fail.
  * **The chain proof** — both halves, with the `{"chain_status": ...}` response quoted.
  * **The five-wave change list**, one row per wave (15.4 extraction repair, 15.5 claim attribution,
    15.6 dispatch + discovery bracket, 15.7 creative workshop loop, 15.8 carried defects + yield
    instrumentation), each marked landed with the image it rode in.
  * **THE ATTRIBUTION LIMIT, stated once, plainly, and not re-argued.** Five waves land together, so
    an unexpected result in the measuring run **cannot be attributed to a single change**. Operator
    ruling 2026-07-29: *"I don't want to measure anything unless we finish all changes."* This
    reverses `ENGINE-REDESIGN-SPEC.md` § 2's "ship Wave 1 alone", which still reads the opposite way
    on its face and carries the override in place. Write the limit as a recorded acceptance, not as a
    warning and not as a caveat to be re-litigated.
  * **What this deploy did NOT settle**, named so nothing reads as covered by accident: 15.8-15's
    measuring run is not run here; the frontend and `nestor-api` were not rebuilt and why; the
    deferred items in `15.8-CONTEXT.md` § Deferred Ideas were not absorbed; and
    **`Nestor_Claude_Temp` rotation is CLOSED-BY-DECISION (deferred to go-live, operator 2026-08-03)**
    — recorded once, as a decision, never as an open gap.

**Do not touch 15.8-12's procedure text.** If executing it revealed a step that is wrong or missing,
record the deviation in the record and say so in the summary — do not edit the procedure into
agreement with what you did. Two authorities disagreeing silently is the exact trap this phase has
been guarding against all the way through.

⛔ **No placeholder survives.** No `<fill me>`, no TBD, no field carried over from the 15.2 record.
Any value you could not obtain is recorded as NOT OBTAINED with the reason — the 15.2 record's own
`⚠️ NOT CARRIED INTO THE RECORD` line for the engine gate count is the honest precedent, and it is
better than a plausible number.

Commit the runbook normally — `infra/` is tracked. (`.planning/` is gitignored and needs `git add -f`,
but this plan writes no `.planning/` artifact other than its SUMMARY.)
  </action>
  <acceptance_criteria>
    - § 15.8 contains ONE consolidated DEPLOY RECORD, not three appended blocks.
    - All four services appear in one table with revision, image and traffic; the two CONFIRM-ONLY rows carry their diff evidence.
    - All three literal `Running upgrade` lines are present, and both alembic heads appear as two separate fields with their own version-table names.
    - The attribution limit appears exactly once, with the 2026-07-29 ruling quoted, framed as an accepted trade-off rather than a warning.
    - `Nestor_Claude_Temp` appears as CLOSED-BY-DECISION with the 2026-08-03 date and appears nowhere as an open item.
    - `grep -icE "TBD|<fill|FILL ME|placeholder|xxxx"` over the § 15.8 section returns 0.
    - No secret VALUE anywhere in the file — only secret NAMES.
    - `git log --oneline -1 -- infra/DEPLOY-RUNBOOK.md` returns this plan's commit.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp" && grep -q "Running upgrade 0017 -> 0018" infra/DEPLOY-RUNBOOK.md && grep -q "0018" infra/DEPLOY-RUNBOOK.md && grep -q "tribunal_alembic_version" infra/DEPLOY-RUNBOOK.md && ! grep -iqE "<fill|FILL ME|placeholder" infra/DEPLOY-RUNBOOK.md && git log --oneline -1 -- infra/DEPLOY-RUNBOOK.md && echo "RECORD CLOSED"</automated>
    <human-check>The operator can read § 15.8's record top to bottom and answer, without opening a plan: what SHA is live, which four services are at which revisions, that all three migrations were paid and proven, that the chain is green on deployed data, and that five waves landed together so a surprising result is not attributable to one change.</human-check>
  </verify>
  <done>The § 15.8 DEPLOY RECORD is one complete, quoted, placeholder-free record that 15.8-15 can start from and that a reader six weeks from now can use to answer "what was live" in ten seconds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git tree → Cloud Build → a running production image | **The headline boundary.** Cloud Build ships the tree you submit. A stale tree crosses here silently and returns a confidently green result. |
| Cloud Run Job → live Cloud SQL schema | Three unpaid DDL migrations against the database that also holds the hash-chained Art. 12 audit record |
| a deployed worker container → the run queue | A deploy BOOTS the container and the loop CLAIMS before it sleeps — deployment itself is the untrusted event, not the unpause |
| secret NAMES in a deploy script → a live service's key binding | A committed default silently repoints a service at an unfunded key |
| operator-pasted command output → the permanent deploy record | Values not read back become a record that disagrees with reality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.8-14-01 | Tampering | The submitted build source — a stale worktree deploys code that is not this phase and every downstream check reads green. **15/15 in this repo.** | mitigate | Task 1's positive-presence sentinel scan, one named symbol per build plan, run BEFORE any spend, ABORT on any single miss; plus `merge-base != BASE` rather than the `rev-list --count` that reads green while stale. |
| T-15.8-14-02 | Denial of Service | `tribunal-worker` deploy claiming a queued run on boot and executing ~$45 of pipeline unattended (**observed 2026-07-28 08:22:57Z**) | mitigate | Worker deploys LAST; queue proven empty twice, the second time immediately before the deploy; shipped `MIN_INSTANCES=0` so the unpause is a separate deliberate act; the claims-nothing observation after unpause. |
| T-15.8-14-03 | Repudiation | A migration recorded as applied that never ran — an unpinned Job exits 0 having applied nothing | mitigate | Repin BEFORE execute, repin CONFIRMED by `jobs describe`, and the proof is the THREE literal `Running upgrade` lines. Exit code, green Job and head read are each explicitly rejected as the proof. |
| T-15.8-14-04 | Tampering | The EU AI Act Art. 12 hash chain, in the database the migration touches | mitigate | `verify_chain` proven green POST-migration on DEPLOYED data via the superadmin Re-verify path, plus the `test-critical` gate re-run against the migrated DDL. A RED chain is a STOP with no sign-off. |
| T-15.8-14-05 | Information Disclosure | The SerpApi key riding in a URL QUERY PARAMETER into 7-year-retained audit blobs | mitigate | 15.8-08's redaction fix ships in this image. Task 1 gates on its scan result and **schedules a SerpApi rotation BEFORE the build if the scan was positive** — a new `Nestor_SERP` version, picked up by `:latest` with no script change. Explicitly NOT covered by the `Nestor_Claude_Temp` deferral. |
| T-15.8-14-06 | Spoofing | A service silently repointed at an unfunded ANTHROPIC key by a redeploy without the override | mitigate | `TRIBUNAL_ANTHROPIC_SECRET=Nestor_Claude_Temp` on BOTH deploy commands; read back by secret NAME on both services in Task 3 step 6(b). |
| T-15.8-14-07 | Elevation of Privilege | The Phase-14 lockdown (`tribunal-run` SA, `--no-allow-unauthenticated`, invoker = `nestor-run` only) being dropped by a hand-rolled deploy | mitigate | The retargeted scripts only; no hand-typed `gcloud run deploy` against a Tribunal service; the `--set-secrets` vs `--update-secrets` distinction stated so the scripts are not "corrected" into dropping bindings. |
| T-15.8-14-08 | Repudiation | A measuring run executed on a config nobody validated | mitigate | Task 3 records the ABSENCE of every `NESTOR_TRIBUNAL_WORKSHOP_*` on the live worker as a positive finding, with the code defaults that constitute the validated config named in the record. |
| T-15.8-14-09 | Information Disclosure | A secret VALUE landing in the committed deploy record | mitigate | Secret NAMES only, everywhere; an acceptance criterion greps the runbook for `sk-`/`AIza` credential shapes and fails on a match. |
| T-15.8-14-10 | Repudiation | A build read as SUCCESS when it FAILED (`… \| tail` returns the pipe's status) or as pending when it EXPIRED | mitigate | Every build status read from `gcloud builds describe` TEXT; no piped submit anywhere in the plan; EXPIRED named explicitly as not-a-result. |
| T-15.8-14-SC | Tampering | npm/pip/cargo installs | mitigate | **No package install in this plan.** `tribunal/requirements.txt` is untouched; the images build from the committed requirements exactly as the last deploy did. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required. |

**Accepted elsewhere, recorded here so it is not re-raised:** `Nestor_Claude_Temp` transited a chat in
plaintext on 2026-07-27 and is live on both Tribunal services. Rotation is **DEFERRED TO GO-LIVE** by
operator ruling (2026-08-03); the risk is deliberately accepted for the pre-go-live period. It is a
decision, not an oversight, and it is out of scope for this plan.
</threat_model>

<verification>
- Every sentinel present before the first build; zero misses; each attributed to its owning plan.
- Both pre-conditions read from `15.8-PRECONDITIONS.md` and settled, with the SerpApi rotation
  scheduled before the build if the scan was positive.
- Two images at ONE shared `$SHA`, both statuses read from `gcloud builds describe`.
- Queue proven empty TWICE, by a check proven able to fail (exit 93 on the inverted re-run).
- THREE literal `Running upgrade` lines; TRIBUNAL head `0018`; INTAKE head `0013` unchanged;
  `nestor-migrate` not executed.
- Both new tables ENABLE + FORCE RLS with USING **and** WITH CHECK policies.
- `verify_chain` green post-migration on DEPLOYED data, plus the `test-critical` re-run.
- `tribunal-api` deployed BEFORE `tribunal-worker`; the worker's revision timestamp proves it.
- Both Tribunal services on `Nestor_Claude_Temp` + `Nestor_SERP`, by NAME.
- No `NESTOR_TRIBUNAL_WORKSHOP_*` on the live worker.
- All four services recorded; two CONFIRM-ONLY with their diff evidence.
- § 15.8's record is consolidated, quoted and placeholder-free; 15.8-12's procedure text is unedited.
</verification>

<success_criteria>
- The five-wave redesign is LIVE on one shared SHA, with the worker demonstrably the last thing
  deployed and demonstrably claiming nothing until unpaused.
- All three unpaid migrations are paid and proven by their literal lines — never by an exit code.
- The Art. 12 audit chain is green after the migration, on deployed data.
- 15.8-15 can begin from the record alone: it knows what is live, at what SHA, on which config, and
  that the attribution limit was accepted rather than overlooked.
- Nothing in the record was written by the agent from a command the agent did not run.
</success_criteria>

<output>
Create `.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-14-SUMMARY.md` when done.

The summary MUST state:
- the shared `$SHA` and the four services' revisions, with the two CONFIRM-ONLY ones named as such;
- all three `Running upgrade` lines, verbatim;
- both alembic heads, from their own version tables;
- the two queue proofs with their exit codes, including the inverted re-run;
- both chain proofs;
- the deploy-surface DEVIATION from the four-service shape, with the `git diff` evidence that
  justified it — this is the one place a reviewer should look first;
- that every step was an OPERATOR action and that no value was supplied by the agent;
- any deviation from 15.8-12's § 15.8 procedure, and that the procedure text was NOT edited.

`.planning/` is gitignored — the summary needs `git add -f`.
</output>
</content>
</invoke>
