---
phase: 15.8-research-engine-redesign-yield-instrumentation-deploy-one-me
plan: 11
type: execute
wave: 3
depends_on: ["15.8-01", "15.8-02", "15.8-03", "15.8-04", "15.8-05", "15.8-09", "15.8-10"]
files_modified:
  - tribunal/nestor_pulse_sdk/tests/test_suite_hygiene.py
  - tribunal/nestor_pulse_sdk/tests/*
autonomous: true
# This phase maps to no REQ-ID (see 15.8-PLAN-OUTLINE.md header). Plans trace to
# decision IDs instead. This plan closes 15.6-REVIEW.md's last recorded scope
# exclusion ("the six tests/test_*.py files are a separate pass") and D-W5-3's
# final bullet ("Test-file code review was never run for 15.6").
requirements: [D-W5-3, D-W5-5a]

user_setup: []

must_haves:
  truths:
    - "Every one of 15.6's six never-reviewed test files has been judged, and every test file this phase created or edited has been judged, and the judgement is recorded per file — not as a single 'reviewed' tick."
    - "No test module in the engine suite defines the same helper, fixture, class or test function twice in one namespace. This is asserted by a committed scan, not by a reviewer's eye, and the scan is proved non-vacuous by a module-count floor."
    - "Every EXACT-SET or SUBSET assertion made over a production file this phase edits has been identified by name and either justified in place or relaxed — a sibling plan in an isolated worktree cannot see the assertion, so an unjustified one is a scheduled failure."
    - "No test in the reviewed set relies on the `ast`-LIFT (a harness that injects module globals into a parsed AST namespace) for NAME RESOLUTION. Behaviour-only use is permitted and must say so; a committed guard bans the injection pattern outright."
    - "Every production-code defect found during the review is RECORDED with the plan that owns the file, and NONE is fixed here."
    - "The SUMMARY hands 15.8-13 an explicit EXPECTED_FILES delta and the exact path(s) to register, plus any pre-existing test file that SHOULD be in a gate list and is not."
    - "Every fix this plan makes has a red-before/green-after proof over the exact pre-fix body; a review that changed a test without proving the change bites is indistinguishable from a review that broke a test."
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/tests/test_suite_hygiene.py"
      provides: "The committed, permanent scan: duplicate definitions, the ast-lift ban, and the non-vacuity floor"
      contains: "def test_no_test_module_defines_the_same_name_twice_in_one_namespace"
      min_lines: 120
    - path: ".planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-11-SUMMARY.md"
      provides: "The per-file findings ledger, the production-defect referrals, and the 15.8-13 handoff"
      contains: "EXPECTED_FILES"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/tests/test_suite_hygiene.py"
      to: "tribunal/nestor_pulse_sdk/tests/"
      via: "ast.parse over every test_*.py on disk, with an asserted module-count floor"
      pattern: "ast\\.parse"
    - from: "15.8-11-SUMMARY.md"
      to: "15.8-13"
      via: "EXPECTED_FILES delta + the path to register"
      pattern: "EXPECTED_FILES"
---

<objective>
Run the test-file code review that 15.6 explicitly deferred, over the six files it never read plus
every test file this phase created or edited, and leave behind a committed guard for the one defect
class that has manufactured false green four times in this repo.

**Purpose:** In this repo a test-file defect is not cosmetic — it manufactures false green, and every
downstream gate then reports a number it did not earn. Four precedents, all real, all from the last
two phases:

- **Three test helpers defined TWICE** in `test_workshop_critique.py`. Python keeps the last; the
  shadowed versions had been silently degrading fixtures on tests that still **PASSED**.
- **A test pinning an EXACT allowlist** over a file a sibling plan also edited (15.5-01's stdlib
  allowlist vs 15.5-02's imports). Three green verifications, a passed plan-check and three executors
  reporting success; only executing the merged tree found it.
- **A test asserting a dead constant.** `DROP_CLUSTERED_ONTO_LIVE`'s only writer is
  `test_workshop_critique.py`, which is how a production branch stayed dead while looking covered.
- **The `ast`-LIFT harness**, which SUPPLIES MODULE GLOBALS and therefore MANUFACTURES any name a
  module forgot to import. It hid a missing `DISCOVERY_PARENT` used at four sites through nine plans,
  `py_compile`, and "38 lifted tests green".

**Output:** a per-file findings ledger, a committed hygiene scan, fixes only where a real defect
exists (each with a red-before/green-after proof), and an explicit handoff to 15.8-13.

**This plan is a REVIEW, not a rewrite.** The six files total 10,714 lines. Rewriting them is out of
scope and reading them end to end would burn the context this plan needs for judgement. The method is
STATIC SCAN FIRST, TARGETED READ SECOND: the scan narrows 10,714 lines to a flag list, and only
flagged regions plus six named must-look sites are read in full.

**A review that finds nothing is a RESULT, not a failure** — provided the scan is proved non-vacuous.
Do not manufacture findings to justify the plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-CONTEXT.md
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-PLAN-OUTLINE.md
@CLAUDE.md

Read only the § Resolution section of `.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-REVIEW.md`
— its last line is the scope exclusion this plan closes:
*"Scope: production files only; the six `tests/test_*.py` files are a separate pass."*

## The review set, named

**15.6's six never-reviewed files** (derived from the 15.6 SUMMARYs; all six are registered in
`cloudbuild.test-engine.yaml`, so all six run today):

| File | Lines | Also edited by 15.8 |
|---|---|---|
| `tests/test_question_grouping.py` | 1585 | 15.8-01 |
| `tests/test_discovery_bracket.py` | 1463 | no plan |
| `tests/test_research_division_assignment.py` | 1929 | 15.8-02 |
| `tests/test_workshop_scope_guard.py` | 1638 | 15.8-03 |
| `tests/test_report_sections.py` | 958 | no plan |
| `tests/test_engine_e2e_stubbed.py` | 3141 | 15.8-02 |

**This phase's test files, as known at planning time.** 15.8-09 and 15.8-10 had not been written when
this plan was — **DISCOVER their test files from the merged tree, do not assume this list is
complete:**

- NEW: `tests/test_pipeline_dispatch_clause.py` (15.8-02), `tests/test_yield_schema.py` and
  `tests/test_yield_records.py` (15.8-05), plus whatever 15.8-09 / 15.8-10 added.
- EDITED: `tests/test_workshop_critique.py` (15.8-04) — the file that carried the duplicate-helper
  defect — plus the four 15.6 files marked above.

## Interfaces — the state of the tree at planning time

These were measured at `8a6c59f` with the bundled stdlib interpreter and are the baseline the
executor's own scan must reconcile against:

- **Duplicate definitions across all 85 test modules: `0`.** The 15.7 defect is FIXED. The scan's job
  is to prove it STAYS 0 across the files this phase adds — a scan that reports 0 having walked
  nothing is the failure mode, hence the module-count floor.
- **Test files on disk: 85. Registered in `cloudbuild.test-engine.yaml`: 36.** The engine gate runs a
  curated subset by design. 49 on-disk files are therefore NOT run by that gate. That is a
  reportable fact for 15.8-13, not a defect this plan fixes.
- `tests/conftest.py:167` defines an **`autouse`** fixture `_no_live_redirect_resolution` that sets
  `NESTOR_REDIRECT_RESOLVE_ENABLED=0` for **every test in the suite**.
- `tests/test_discovery_bracket.py:714` asserts `modules == {"logging", "os", "typing"}` — an
  EXACT-SET import allowlist over `discovery_bracket.py`. `:720` asserts an exact set over
  `discovery_bracket.__all__`.
- `tests/test_question_grouping.py:1562` asserts `roots <= {...}` over **its own** imports;
  `:1568` scans `question_grouping.py`'s imports as a DENYLIST.
- `tests/test_claim_attribution.py:194` walks `alembic/versions/` and asserts
  `children_of_0016 == ["0017_claim_attribution.py"]` plus revision-id uniqueness.
- `tests/test_tribunal_pipeline.py:562` walks `pipeline/tribunal/pipeline.py` by relative path as a
  DENYLIST (no exact set).

## Environment

**No pytest, no Python 3.11, no Docker on this machine.** Stdlib-only bundled interpreter:
`C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe` (3.14). Every test written or
edited here is **OWED to Cloud Build** and is paid by 15.8-13, not by this plan.

⛔ **Do NOT use the `ast`-LIFT** (the harness that injects module globals into a parsed namespace) to
prove any name resolves. It manufactures names. Static `ast` WALKING — parsing a module and inspecting
its tree without executing it — is a different technique, is safe, and is exactly the right tool for
the duplicate-definition class. Use it that way.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abort on a stale tree, then land the committed hygiene scan</name>

  <read_first>
    - `tribunal/nestor_pulse_sdk/tests/test_workshop_critique.py` — read only its module-level helper
      and fixture definitions (the region that carried the duplicate-definition defect), to see the
      shape the scan must catch.
    - `tribunal/nestor_pulse_sdk/tests/test_fail_loud.py` — its module docstring, as the house style
      for a test module that states WHY IT EXISTS before it asserts anything.
    - `tribunal/nestor_pulse_sdk/tests/conftest.py` in full (182 lines) — the autouse fixture at `:167`
      is a Task 2 must-look site and the scan must not trip over conftest's non-test shape.
  </read_first>

  <action>
    **PREFLIGHT — ABORT, do not proceed on a stale tree.** The worktree stale-base trap is 15/15 in
    this repo and `rev-list --count BASE..HEAD == 0` reads GREEN while stale, so only
    `merge-base != BASE` catches it. Reviewing a stale tree produces confident findings about code
    that no longer exists, which is worse than not reviewing at all. Assert POSITIVE PRESENCE of one
    symbol per upstream plan; every one of these is confirmed ABSENT at `8a6c59f`, so a hit proves the
    merge landed:

    - 15.8-01 → `_resolve_ceiling` in `pipeline/tribunal/question_grouping.py`
    - 15.8-02 → the file `tests/test_pipeline_dispatch_clause.py` exists
    - 15.8-03 → `_sweep_langs` in `pipeline/tribunal/workshop_rank.py` (note `_text_key` already
      exists in `research_division.py`, so it is NOT a valid sentinel for 15.8-03; `_text_key` in
      `workshop_rank.py` specifically IS)
    - 15.8-04 → `count_drops` in `pipeline/tribunal/workshop_register.py`
    - 15.8-05 → `PARENT_KINDS` in `runs/yield_records.py`
    - 15.8-09 → `yield_records` imported by `pipeline/tribunal/research_division.py` or
      `pipeline/tribunal/pipeline.py`
    - 15.8-10 → `new_entrants_top_n` anywhere under `pipeline/tribunal/`

    If ANY is absent, STOP and report which — do not review.

    **Then create `tribunal/nestor_pulse_sdk/tests/test_suite_hygiene.py`.** Its subject is the SUITE's
    own integrity; state that in a module docstring in the house style, naming the four precedents from
    this plan's objective, so the next reader knows why a test file is testing test files.

    It must contain exactly three test functions, all pure (no DB, no network, no imports of
    `nestor_pulse_sdk` production modules — it reads source text, it does not execute it):

    1. `test_the_scan_walked_the_whole_suite` — the NON-VACUITY control, and it comes first on purpose.
       Glob `test_*.py` in the tests directory, assert the count is `>= 80`, and assert every one
       parses. A scan that reports zero findings having walked zero files is the exact failure this
       repo has been burned by; a bad glob must go RED here, not silently green in the next function.

    2. `test_no_test_module_defines_the_same_name_twice_in_one_namespace` — parse every module with
       `ast.parse` and, per NAMESPACE, collect `FunctionDef` / `AsyncFunctionDef` / `ClassDef` names
       plus module-level `Assign` targets that are plain `Name`s. Report every name bound more than
       once in one namespace, with file and line numbers, and assert the list is EMPTY. Namespace
       rules, and they are the load-bearing detail:
       - descend into `Try`, `With`, `For` and `While` bodies as the SAME namespace (Python binds them
         there);
       - treat each branch of an `If` as a SIBLING namespace — do not compare across branches, or
         every legitimate `if/else` definition pair false-positives;
       - skip any `FunctionDef` carrying a `typing.overload` / `overload` decorator.
       State both exemptions as comments; an unstated exemption is how a scan quietly stops biting.
       **Expected result today: an empty list.** If it is not empty, that is a real finding and Task 2
       judges it.

    3. `test_no_test_module_injects_module_globals_into_a_parsed_namespace` — the `ast`-LIFT ban. Scan
       the SOURCE TEXT of every test module for the injection shapes: `types.ModuleType(`,
       `exec(compile(`, `.__dict__.update(`, and `exec(` with a second argument. Assert none appears.
       The docstring must state the distinction plainly: static `ast` WALKING is safe and is used by
       this very file; the LIFT is banned because it SUPPLIES the globals and therefore cannot
       distinguish a name a module imports from a name it forgot to import. Name the `DISCOVERY_PARENT`
       precedent. If a legitimate existing use is found, do NOT weaken the assertion silently — record
       it in the ledger and let Task 2 rule on it.

    Run the file's logic locally with the bundled interpreter and record the raw output; it is the flag
    list Task 2 works from. **This is a static scan of source text — no production module is imported
    and no lift is used anywhere in it.**

    EXPECTED_FILES contribution of this task: **+1**. Do not touch either Cloud Build config
    (D-W5-5); the number and the path go to 15.8-13 in the SUMMARY.
  </action>

  <acceptance_criteria>
    - All seven presence sentinels resolve; the command prints one line per plan and exits 0.
    - `git merge-base HEAD <BASE>` differs from `<BASE>`, proving the tree is not the stale ref.
    - `tests/test_suite_hygiene.py` exists, parses, and defines exactly the three named test functions.
    - Running its scan logic locally prints a module count `>= 80` and a duplicate-name list.
    - The `ast`-lift scan prints its hit list (expected: empty).
    - The file imports no `nestor_pulse_sdk` module and contains no `exec(`, no `types.ModuleType(`.
  </acceptance_criteria>

  <files>tribunal/nestor_pulse_sdk/tests/test_suite_hygiene.py</files>

  <verify>
    <automated>cd /c/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && T=tribunal/nestor_pulse_sdk && MISS=0 && grep -q "_resolve_ceiling" $T/pipeline/tribunal/question_grouping.py || { echo "STALE: 15.8-01 absent"; MISS=1; }; test -f $T/tests/test_pipeline_dispatch_clause.py || { echo "STALE: 15.8-02 absent"; MISS=1; }; grep -q "_sweep_langs" $T/pipeline/tribunal/workshop_rank.py || { echo "STALE: 15.8-03 absent"; MISS=1; }; grep -q "count_drops" $T/pipeline/tribunal/workshop_register.py || { echo "STALE: 15.8-04 absent"; MISS=1; }; grep -q "PARENT_KINDS" $T/runs/yield_records.py 2>/dev/null || { echo "STALE: 15.8-05 absent"; MISS=1; }; grep -qr "yield_records" $T/pipeline/tribunal/ || { echo "STALE: 15.8-09 absent"; MISS=1; }; grep -qr "new_entrants_top_n" $T/pipeline/tribunal/ || { echo "STALE: 15.8-10 absent"; MISS=1; }; test "$MISS" = 0 && echo PREFLIGHT_OK && test -f $T/tests/test_suite_hygiene.py && "$PY" -c "import ast,sys,pathlib; p=pathlib.Path('$T/tests/test_suite_hygiene.py'); t=ast.parse(p.read_text(encoding='utf-8')); names={n.name for n in t.body if isinstance(n,ast.FunctionDef)}; want={'test_the_scan_walked_the_whole_suite','test_no_test_module_defines_the_same_name_twice_in_one_namespace','test_no_test_module_injects_module_globals_into_a_parsed_namespace'}; sys.exit(0 if names>=want else 1)" && "$PY" -c "import pathlib,sys; s=pathlib.Path('$T/tests/test_suite_hygiene.py').read_text(encoding='utf-8'); bad=[x for x in ('types.ModuleType(','exec(compile(') if x in s]; print('SELF_LIFT_FREE' if not bad else bad); sys.exit(1 if bad else 0)" && echo TASK1_OK</automated>
  </verify>

  <done>The tree is proved current against all seven upstream plans, `test_suite_hygiene.py` is
  committed with its three functions, and its local run has produced a module count and a flag list
  that Task 2 consumes.</done>
</task>

<task type="auto">
  <name>Task 2: Judge every flag and the six named must-look sites</name>

  <read_first>
    - The Task 1 flag list (module count, duplicate names, lift hits).
    - `tribunal/nestor_pulse_sdk/tests/conftest.py:157-182` — the autouse fixture.
    - `tribunal/nestor_pulse_sdk/tests/test_discovery_bracket.py:695-740` — the exact-set import
      allowlist and the `__all__` exact set.
    - `tribunal/nestor_pulse_sdk/tests/test_question_grouping.py:1527-1600` — the `roots <= {...}`
      subset guard and the grouping-module import denylist.
    - `tribunal/nestor_pulse_sdk/tests/test_claim_attribution.py:160-240` — the alembic chain tests.
    - `tribunal/nestor_pulse_sdk/tests/test_tribunal_pipeline.py:555-595` — the `pipeline.py` scan.
    - The NEW test files this phase added, in full — they are the ones no reviewer has ever seen:
      `test_pipeline_dispatch_clause.py`, `test_yield_schema.py`, `test_yield_records.py`, plus
      whatever 15.8-09 and 15.8-10 added (discover by `git diff --name-only` against the phase base).
  </read_first>

  <action>
    Judge, do not skim. Every item below gets one of exactly three verdicts, recorded with its
    reasoning: **DEFECT** (fix in Task 3), **LANDMINE** (correct today, will break under a named future
    change — record, and harden only if the hardening costs nothing), or **OK-WITH-REASON** (state the
    reason; "looks fine" is not a reason).

    **A. Whatever Task 1's scan flagged.** Expect an empty duplicate list; if so, record that the scan
    walked N modules and found none, and that N is above the floor. Do not pad.

    **B. The six named must-look sites, each of which is a live instance of a class this repo has been
    burned by:**

    1. **`conftest.py::_no_live_redirect_resolution` — an AUTOUSE fixture that disables a production
       default for the ENTIRE suite.** This is the fixture-masks-the-behaviour-under-test class. It is
       documented and justified for its original purpose (no outbound HTTP from CI). The question this
       plan must answer, because nobody else will: **15.8-06 and 15.8-10 concern
       `admission_resolver_calls`, which counts an HTTP REDIRECT RESOLUTION.** Determine whether any
       test asserting on `admission_resolver_calls` runs under this fixture with resolution disabled,
       and therefore asserts over a counter that is structurally zero. If so it is a DEFECT and the fix
       is for the test to clear the variable explicitly, as the resolver's own tests already do.

    2. **`test_discovery_bracket.py`'s exact-set import allowlist and `__all__` exact set.** No 15.8
       plan edits `discovery_bracket.py`, so this is not a scheduled failure THIS phase — but it is the
       exact 15.5-01/15.5-02 shape and the 15.6 review only confirmed it "holds" for that phase.
       Verdict is almost certainly LANDMINE. Record it by name, with the condition that trips it (any
       future plan adding an import or an export to `discovery_bracket.py` in a parallel worktree).

    3. **`test_question_grouping.py::test_this_test_file_needs_no_database_and_no_network`'s
       `roots <= {...}`.** 15.8-01 edits this very file. Confirm 15.8-01's added imports are inside the
       allowed set; if 15.8-01 added one that is not, the merged tree is already red and that is a
       DEFECT to fix here. Note the assertion's own comment argues the exact/subset form is deliberate
       — respect that argument; the fix is to extend the set with a stated reason, never to relax the
       form.

    4. **`test_claim_attribution.py`'s alembic chain tests vs 15.8-05's new `0018`.** These walk the
       whole `alembic/versions/` directory. **No plan in this phase owns this file**, so if `0018`
       breaks them nobody else is looking. Verify specifically: `children_of_0016` still equals exactly
       `["0017_claim_attribution.py"]` (0018 is a child of 0017, so it should); every revision id is
       still unique; `len(downs) <= 1` holds for `0018`; and no assertion in the file states that 0017
       is the HEAD. Analysis at planning time says all four hold — CONFIRM against the merged tree
       rather than inheriting that claim.

    5. **`test_tribunal_pipeline.py`'s AST scan of `pipeline.py`**, which 15.8-02 and 15.8-09 both
       edit. It is a denylist, not an exact set, so it should be safe — confirm, and confirm the
       trailing `assert "persist_tribunal_claims" in source` is a raw text match that a comment could
       satisfy vacuously.

    6. **The new files nobody has reviewed:** `test_pipeline_dispatch_clause.py`, `test_yield_schema.py`,
       `test_yield_records.py`, and 15.8-09's and 15.8-10's. Read them in full. Apply every class in
       section C below, and one extra: `test_yield_schema.py` asserts a column set that must mirror
       migration `0018` — check it reads BOTH sides from source rather than restating one side as a
       literal on both, which would make it self-satisfying.

    **C. Hunt the CLASS, not the instance, across the whole review set:**

    - **Vacuous assertions.** `assert True`; `assert x is not None` where `x` was constructed from a
      literal in the same test; `== 0` or `>= 0` baselines that hold before the feature exists;
      `assert len(y) >= 0`. Any `grep -c` gate inside a test that counts its own header comments
      (the fix is `grep -v '^#' | grep -c`), and any bare `== 0` gate over an unfiltered file.
    - **The test is the only writer.** For each production constant asserted in the reviewed set,
      determine whether ANY production module writes it. `DROP_CLUSTERED_ONTO_LIVE` is the known
      instance and 15.8-04 is fixing the production side — verify 15.8-04's writer actually landed, and
      look for a second instance of the same shape.
    - **Exact-set / subset assertions over any production file this phase edits.** The seven are
      `question_grouping.py`, `research_division.py`, `pipeline.py`, `workshop_rank.py`,
      `workshop_loop.py`, `workshop.py`, `workshop_register.py`, plus `db/models/__init__.py` and
      `runs/yield_records.py`. Enumerate every `== {`, `<= {`, `== [` assertion that reads one of
      those files or a symbol exported from it.
    - **Fixtures that mask the behaviour under test**, beyond the autouse one — a fixture that stubs
      the very function whose behaviour the test claims to assert.
    - **`ast`-lift for NAME RESOLUTION.** Task 1's guard bans the injection pattern; here, judge any
      existing use: if a test uses static walking it is fine and should say so, and if any test relies
      on a lifted namespace to prove a name exists, that is a DEFECT.

    **D. Production defects: RECORD, DO NOT FIX.** You own no production file. For each, name the plan
    that owns the file per `15.8-PLAN-OUTLINE.md § Shared-file conflicts`, or state plainly that no
    plan in this phase owns it — an unowned production defect that goes unnamed is how D-DEF-01
    survived an entire phase.

    **E. Gate registration.** Compare the test files on disk against those registered in
    `cloudbuild.test-engine.yaml`. Report any file that SHOULD be registered and is not — in particular
    every test file this phase added. ⛔ **DO NOT EDIT `cloudbuild.test-engine.yaml`** (D-W5-5; sole
    owner is 15.8-13, wave 4). D-W5-5a: that single-owner deviation is engine-config-only, so
    `cloudbuild.test-gates.yaml` still lives under the one-edit rule and is 15.8-07's, not yours.
  </action>

  <acceptance_criteria>
    - A ledger exists with one row per reviewed file, each carrying a verdict and a reason.
    - All six must-look sites appear in the ledger by file and symbol.
    - The `admission_resolver_calls`-under-autouse question is answered YES or NO with the evidence,
      not left open.
    - Every production defect found is listed with its owning plan or an explicit "no plan owns this".
    - The on-disk-vs-registered comparison is recorded as two counts plus the diff list.
    - No edit was made to `tribunal/cloudbuild.test-engine.yaml`.
  </acceptance_criteria>

  <files>(read-only judgement pass; no file is written by this task)</files>

  <verify>
    <automated>cd /c/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp && git diff --name-only HEAD -- tribunal/cloudbuild.test-engine.yaml | grep -q . && { echo "VIOLATION: engine gate config was edited"; exit 1; } || echo ENGINE_CONFIG_UNTOUCHED && cd tribunal && ONDISK=$(ls nestor_pulse_sdk/tests/test_*.py | wc -l) && REG=$(grep -oE "tests/test_[a-z0-9_]+\.py" cloudbuild.test-engine.yaml | sort -u | wc -l) && echo "ondisk=$ONDISK registered=$REG" && comm -23 <(ls nestor_pulse_sdk/tests/test_*.py | sed 's|nestor_pulse_sdk/||' | sort) <(grep -oE "tests/test_[a-z0-9_]+\.py" cloudbuild.test-engine.yaml | sort -u) > /tmp/unregistered.txt && echo "UNREGISTERED_COUNT=$(wc -l < /tmp/unregistered.txt)" && echo TASK2_SCAN_OK</automated>
    <human-check>The ledger's verdicts are judgements and cannot be asserted by a command. The
    reviewer's own check: every row says WHY, and no row says "looks fine".</human-check>
  </verify>

  <done>Every file in the review set has a recorded verdict; the six must-look sites are each ruled;
  production defects are referred by owning plan; the registration diff is captured.</done>
</task>

<task type="auto">
  <name>Task 3: Fix only the real defects, prove each one bites, and hand off to 15.8-13</name>

  <read_first>
    - The Task 2 ledger — specifically every row verdicted DEFECT.
    - `.planning/phases/15.6-research-engine-redesign-dispatch-discovery-bracket-wave-3/15.6-REVIEW.md`
      § Resolution, the paragraph beginning "10 new tests, 8 of which fail on pre-fix source" — that is
      the house standard for a red-before/green-after proof, and mutant M7's near-miss is why the proof
      is by source-text mutation and not by argument.
  </read_first>

  <action>
    **Fix ONLY rows verdicted DEFECT.** A LANDMINE gets a comment stating the condition that trips it,
    at the assertion, and nothing more. An OK-WITH-REASON gets nothing. Do not rewrite a file because
    it could read better — this is a review plan, and six files at 10,714 lines is not a refactor
    budget.

    **Every fix carries its own red-before/green-after proof, by SOURCE-TEXT MUTATION.** For each fix:
    restore the exact pre-fix body as a mutant, run the affected test logic locally with the bundled
    interpreter, and record that it goes RED; then restore the fix and record GREEN. A fix whose test
    passes in both columns is not proving the fix — say so and either strengthen the test or record it
    honestly as a behaviour-PRESERVATION guard, which is a legitimate but different thing.

    **If the review found no test-file defect, this task delivers the ledger and the handoff and says
    so plainly.** Record the module count the scan walked and the classes it checked. Do not invent a
    fix to make the plan look productive; a clean review over a proved-non-vacuous scan is the result.

    **Write the SUMMARY, and make the 15.8-13 handoff unmissable.** It must state, as its own section:

    - **`EXPECTED_FILES` contribution: `+1`.** Exactly one new test file, `tests/test_suite_hygiene.py`,
      to be registered by 15.8-13 in `tribunal/cloudbuild.test-engine.yaml`. This plan did not and must
      not touch that config (D-W5-5).
    - The count of tests `test_suite_hygiene.py` contributes (3), so 15.8-13 can reconcile the passed
      count rather than merely watch it move. **A count that did not rise is not automatically a skip,
      but it must be EXPLAINED.**
    - The full list of test files that exist on disk and are NOT registered in the engine gate, split
      into "added by this phase — 15.8-13 should register these" and "pre-existing and deliberately
      out of the curated subset — reported, not proposed".
    - Every production defect found, with its owning plan, flagged for the per-wave code review.
    - The statement that no test in this plan was executed under pytest: **every test here is OWED to
      Cloud Build and is paid by 15.8-13.** The local proofs are stdlib source-text scans and
      mutations, which prove the assertion logic and NOT that pytest collects and passes it.
  </action>

  <acceptance_criteria>
    - Every file changed by this task is under `tribunal/nestor_pulse_sdk/tests/`; `git diff --name-only`
      shows nothing outside it.
    - Every DEFECT row in the ledger maps to exactly one commit-level change, and each has a recorded
      RED (mutant) and GREEN (fixed) local result.
    - No row verdicted LANDMINE or OK-WITH-REASON produced a behavioural change to any test.
    - The SUMMARY contains the literal string `EXPECTED_FILES` and states `+1` with the path
      `tribunal/nestor_pulse_sdk/tests/test_suite_hygiene.py`.
    - The SUMMARY lists the unregistered-test-file diff in the two named halves.
    - The SUMMARY states that pytest was not run and names 15.8-13 as the plan that owes it.
  </acceptance_criteria>

  <files>tribunal/nestor_pulse_sdk/tests/* (only files carrying a DEFECT verdict), .planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-11-SUMMARY.md</files>

  <verify>
    <automated>cd /c/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp && OUT=$(git diff --name-only HEAD | grep -v "^\.planning/" | grep -v "^tribunal/nestor_pulse_sdk/tests/" || true) && test -z "$OUT" && echo SCOPE_OK || { echo "OUT OF SCOPE: $OUT"; exit 1; } && S=.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-11-SUMMARY.md && grep -q "EXPECTED_FILES" $S && grep -q "test_suite_hygiene.py" $S && grep -q "15.8-13" $S && PY="/c/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe" && "$PY" -c "import ast,pathlib,sys,collections; d=pathlib.Path('tribunal/nestor_pulse_sdk/tests'); mods=sorted(d.glob('test_*.py')); assert len(mods)>=80, len(mods); dups=[]
def walk(body, scope, ml, fn):
    names=collections.defaultdict(list)
    for n in body:
        if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef,ast.ClassDef)):
            names[n.name].append(n.lineno); walk(n.body, scope+'.'+n.name, False, fn)
        elif ml and isinstance(n,ast.Assign):
            for t in n.targets:
                if isinstance(t,ast.Name): names[t.id].append(n.lineno)
    for k,v in names.items():
        if len(v)>1: dups.append((fn,scope,k,v))
for m in mods: walk(ast.parse(m.read_text(encoding='utf-8')).body,'<module>',True,m.name)
print('modules',len(mods),'dups',len(dups)); [print(x) for x in dups]; sys.exit(1 if dups else 0)" && echo TASK3_OK</automated>
  </verify>

  <done>Every DEFECT is fixed with a mutation proof, nothing outside `tests/` changed, and the SUMMARY
  hands 15.8-13 the EXPECTED_FILES delta, the path to register, the unregistered-file diff, and the
  statement that the pytest run is owed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → gate verdict | A green gate is the only evidence the engine is correct before a $45 unrepeatable run. A defective test crosses this boundary as **false assurance**. |
| parallel worktree → merged tree | Sibling plans cannot see each other's assertions. An exact-set assertion crosses this boundary as a **scheduled failure**. |
| test file → production constant | A test that is a constant's only writer crosses this boundary as **coverage that covers nothing**. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.8-11-01 | Repudiation | duplicate definitions in a test module | mitigate | `test_suite_hygiene.py::test_no_test_module_defines_the_same_name_twice_in_one_namespace`, asserted empty across ≥80 modules |
| T-15.8-11-02 | Tampering | `ast`-lift supplying module globals | mitigate | `test_no_test_module_injects_module_globals_into_a_parsed_namespace` bans `types.ModuleType(`, `exec(compile(`, `.__dict__.update(` in test modules |
| T-15.8-11-03 | Denial of Service | exact-set assertion over a file a sibling worktree edits | mitigate | Task 2 § C enumerates every `== {` / `<= {` / `== [` over the nine production files this phase edits; each is justified in place or extended with a stated reason |
| T-15.8-11-04 | Information Disclosure | a vacuous scan reporting zero findings over zero files | mitigate | `test_the_scan_walked_the_whole_suite` asserts a module-count floor of 80 BEFORE any finding count is reported |
| T-15.8-11-05 | Elevation of Privilege | this plan editing `cloudbuild.test-engine.yaml` and setting a wrong `EXPECTED_FILES` | mitigate | D-W5-5: sole owner is 15.8-13. Task 2 and Task 3 `<verify>` both assert `git diff` is empty for that path |
| T-15.8-11-06 | Spoofing | a review of a stale tree reporting confident findings about deleted code | mitigate | Task 1 preflight: seven positive-presence sentinels, each confirmed ABSENT at `8a6c59f`, plus `merge-base != BASE` |
| T-15.8-11-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no package. No `pip install`, no `npm install`, no new dependency. Nothing to audit. |

**Block on high:** T-15.8-11-06 is blocking — if any sentinel is absent, Task 1 STOPS and the review
does not run.
</threat_model>

<verification>
- The seven presence sentinels all resolved, proving the tree carries waves 1 and 2.
- `test_suite_hygiene.py` exists, defines its three functions, imports no production module, and its
  own source contains no lift pattern.
- The duplicate-definition scan walked ≥80 modules and returned an empty list.
- Every file in the review set has a ledger verdict with a reason.
- `git diff --name-only` shows no path outside `tribunal/nestor_pulse_sdk/tests/` and `.planning/`.
- `tribunal/cloudbuild.test-engine.yaml` is byte-identical to its pre-plan state.
- The SUMMARY states `EXPECTED_FILES +1`, the path, the test-count contribution, and the unregistered
  file diff.
</verification>

<success_criteria>
- 15.6-REVIEW.md's recorded scope exclusion is closed: all six files judged, per file, with reasons.
- Every test file this phase created or edited is judged, discovered from the merged tree rather than
  from this plan's list.
- The duplicate-definition class, the `ast`-lift class, and the exact-set class each have a recorded
  finding count over a scan proved non-vacuous.
- Each fix has a red-before/green-after source-text mutation result; zero fixes is an acceptable
  outcome if the ledger shows the classes were checked.
- Every production defect is referred to its owning plan by name, or explicitly recorded as unowned.
- 15.8-13 can register this plan's contribution without reading the plan.
</success_criteria>

<output>
Create `.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-11-SUMMARY.md`
when done. `.planning/` is gitignored — commit it with `git add -f`.
</output>
