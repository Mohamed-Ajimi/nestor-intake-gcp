---
phase: 15.8-research-engine-redesign-yield-instrumentation-deploy-one-me
plan: 13
type: execute
wave: 4
depends_on: ["15.8-07", "15.8-11", "15.8-12"]
files_modified:
  - tribunal/cloudbuild.test-engine.yaml
autonomous: true
# This phase maps to no REQ-ID (15.8-PLAN-OUTLINE.md § header). Plans trace to
# decision IDs instead. This one implements D-W5-4, D-W5-5, D-W5-5a and D-W5-12,
# and pays the carried `15.4-05` revert-and-confirm-red debt.
requirements: [D-W5-4, D-W5-5, D-W5-5a, D-W5-12, "15.4-05-revert-red"]

must_haves:
  truths:
    - "EXPECTED_FILES equals the number of paths WANTED names, and that number was derived from the REAL file list on the merged tree — not inferred, not incremented by memory"
    - "Every test FILE this phase added is either registered in WANTED or has a written, named reason it is not"
    - "The engine gate ran on the fully merged tree and its status was read from `gcloud builds describe` build TEXT, never from a shell exit code"
    - "The passed count is reconciled against 1538 + 10 (D-W5-4) + the per-plan stated new-test counts; any shortfall is EXPLAINED, not merely noted"
    - "A reader of the engine config learns that 15.8 REVERSED its own one-edit rule, why, and the exact condition (no gate runs before 15.8-13) that made the deviation safe"
    - "A reader learns the deviation is scoped to THIS config only — cloudbuild.test-gates.yaml keeps the original one-edit rule (D-W5-5a)"
    - "A reader learns that a FLAT gates count is a regression PASS over wave-1 and wave-2 edits, not insulation, and that a RED there is an engine signal and not a config to tune (D-W5-12)"
    - "The new tests this phase wrote go RED on a deliberately mutated tree, and the 15.4-05 helpers go RED when reverted"
    - "The single-quoted bash -c block still contains ZERO apostrophes and the build-step arg is still under the 10,000-char Cloud Build cap"
  artifacts:
    - path: "tribunal/cloudbuild.test-engine.yaml"
      provides: "the completeness assertion, set ONCE this phase from the real merged-tree file list"
      contains: "EXPECTED_FILES="
    - path: "tribunal/cloudbuild.test-engine.yaml"
      provides: "the amended ownership block recording the 15.8 deviation, its safety condition, and its scope fence"
      contains: "D-W5-5"
    - path: ".planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-13-SUMMARY.md"
      provides: "the file-count reconciliation table, the passed-count arithmetic, every BUILD_ID with its status, and the batched mutation table"
      contains: "BUILD_ID"
  key_links:
    - from: "WANTED"
      to: "FILES"
      via: "ls over the variable, guarded by || true so an absent file is a SKIP not a collection error"
      pattern: "ls \\$\\$WANTED"
    - from: "COLLECTED"
      to: "EXPECTED_FILES"
      via: "the -ne comparison that converts that skip into a RED build"
      pattern: "COLLECTED.*-ne.*EXPECTED_FILES"
    - from: "the amended comment block"
      to: "D-W5-5 / D-W5-5a / D-W5-12"
      via: "the decision IDs written into the YAML comments so the code and the comment cannot disagree"
      pattern: "D-W5-5a"
---

<objective>
This is **the phase gate**. Four waves of engine work, a new schema, two new writers and a test-file
review land here and are proved — or are not — by two Cloud Build runs and one deliberately mutated
tree.

Three jobs, in this order:

1. **Set `EXPECTED_FILES` ONCE**, from the real file list on the merged tree. Eight wave-1 plans were
   each barred from touching this config (D-W5-5) precisely so that this number is derived once, by
   one reader, against one tree — instead of eight plans each bumping one line and silently resolving
   to a wrong total.
2. **Amend the config's own comment block.** That block instructs the OPPOSITE of what this phase
   did, four times over. Leaving it in place while doing the opposite is a two-authorities trap — the
   exact shape that produced D-DEF-01's inverted rationale comments and 15.6's CR-01.
3. **Run both gates and pay the mutation debt.** Engine gate must beat `7c89be5c`
   (1538 passed / 0 failed / 13 skipped, `collecting: 36 of 36`). Gates gate must beat `2eae97e6`
   (187 passed, 2 deselected) with 15.8-07's new assertion live. And `15.4-05`'s
   revert-and-confirm-red proof — owed since 2026-07-29 — is paid here, batched.

Purpose: everything downstream of this plan spends money. 15.8-14 is the ONE deploy of a five-wave
redesign; 15.8-15 is the ONE measuring run at ~$45, and there is no second one. **A gate that can go
green having run less than it claims is the only failure in this phase that cannot be detected
afterwards** — the run would produce numbers, the numbers would look plausible, and nothing would
say which of 36-plus test files never executed.

Output: `tribunal/cloudbuild.test-engine.yaml` with a reconciled `EXPECTED_FILES` and an honest
ownership block; a SUMMARY carrying the file-count reconciliation, the passed-count arithmetic, every
`BUILD_ID` with the status read from `gcloud builds describe`, and the batched mutation table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<worktree_preflight gate="blocking">
**Assert the tree BEFORE reading the file list.** A stale tree yields a confidently wrong
`EXPECTED_FILES`, and submitting a Cloud Build of the WRONG SOURCE returns a confidently green
result. The worktree stale-base trap is **15/15** in this repo, most recently 2/2 in phase 15.7 at
595–601 commits behind. `git rev-list --count BASE..HEAD` returning 0 reads **GREEN while stale**,
because the stale ref is usually the merge-base itself.

1. `git worktree list` — sweep it. A stale worktree from a halted run has sat on disk in this repo
   before.
2. `git merge-base HEAD <the SHA the orchestrator says is the merged wave-3 tree>` — assert the
   merge-base **is not** an older base. The count is not the guard; the merge-base is.
3. **Positive-presence sentinels — every one must hold, and a single absence is an ABORT, not a
   note.** Each names a symbol introduced by a specific earlier commit or plan, so an old tree cannot
   fake them:

   - `grep -c "_LOOP_MIN_ROUNDS" tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_loop.py`
     is non-zero — commit `8eff424` (D-W4-9, 2026-08-04) is present.
   - `grep -A3 "def attach_discovery_riders" tribunal/nestor_pulse_sdk/pipeline/tribunal/question_grouping.py`
     shows the signature `(groups, riders, *, max_riders=None)` and contains **no** `max_size`
     parameter — commit `868775b` (D-W4-10) is present. This is the NEGATIVE half of the pair; keep
     it, but do not rely on it alone.
   - `ls tribunal/nestor_pulse_sdk/tests/test_yield_schema.py tribunal/nestor_pulse_sdk/tests/test_yield_records.py`
     both exist — 15.8-05 merged.
   - `ls tribunal/nestor_pulse_sdk/tests/test_pipeline_dispatch_clause.py` exists — 15.8-02 merged.
   - `grep -c "count_drops" tribunal/nestor_pulse_sdk/pipeline/tribunal/workshop_register.py`
     is non-zero — 15.8-04 merged (D-W5-6).
   - `ls tribunal/nestor_pulse_sdk/alembic/versions/0018_yield_instrumentation.py` exists — 15.8-05's
     migration merged.
   - `grep -c "EXPECTED_FILES" tribunal/cloudbuild.test-gates.yaml` is **non-zero** — 15.8-07 merged.
     (Before 15.8-07 this was 0. If it is still 0, your dependency did not land and you must STOP.)

   **If the orchestrator's merged tree also contains 15.8-09 / 15.8-10 / 15.8-11, add one sentinel
   symbol from each of their SUMMARYs before proceeding.** Those plans are written in parallel with
   this one and their symbols are not knowable here; read them from the SUMMARY, not from this plan.

4. `git status --porcelain` is EMPTY before you start. You will deliberately dirty the tree in
   Task 3 and you must be able to tell your mutation from someone else's leftovers.
</worktree_preflight>

<proving_things_on_this_machine>
**There is no pytest, no Python 3.11 and no Docker on this box.** A stdlib-only Python 3.14 ships
with gcloud at `C:/Users/ajimimo/google-cloud-sdk/platform/bundledpython/python.exe`. `bash` is
available (Git Bash). Every test written by every plan in this phase was authored locally and is
**OWED to Cloud Build here** — this plan is where that debt is settled or is honestly declared
unsettled.

**Read the result, never the exit code.** Three traps, all of which have bitten this project:

- `gcloud builds submit … | tail` returns the **PIPE's** exit code, so a **FAILED build reports exit
  0**. Read the build TEXT. Take the status from
  `gcloud builds describe <BUILD_ID> --format="value(status)"` and the log body from
  `gcloud builds log <BUILD_ID>` (or the console URL the submit prints).
- An **EXPIRED** build is visually identical to **QUEUED** and **is not a result**. One 15.7 run
  expired in the queue and was nearly read as pending. `SUCCESS` and `FAILURE` are results;
  `QUEUED`, `WORKING`, `EXPIRED`, `CANCELLED` are not.
- **A count that did not rise must be EXPLAINED, not merely noted.** Write the explanation down in
  the SUMMARY, in words, with the mechanism.

**If `gcloud builds submit` cannot run in this environment** — no credentials, no `GOOGLE_PROJECT`,
a permission denial, anything — **do NOT mark Task 2 or Task 3 complete.** Record them as
`BLOCKED-OWED` in the SUMMARY with the exact commands and the exact output that would settle each
one, and hand them to the orchestrator. **Never write a build result you did not read.** This repo
has a documented history of gates that were green because they ran nothing; a fabricated build ID
would be strictly worse than an honest debt, because 15.8-14 spends money on the strength of this
plan's word.
</proving_things_on_this_machine>

<context>
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-CONTEXT.md
@.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-PLAN-OUTLINE.md
@tribunal/cloudbuild.test-engine.yaml
@tribunal/cloudbuild.test-gates.yaml
@CLAUDE.md

<current_state>
Established by grep at `a2ec05f`, so the executor does not re-derive it — but **re-verify the two
counts on YOUR tree**, because six or more plans merge between this writing and your execution.

- `tribunal/cloudbuild.test-engine.yaml` names **36** paths and pins `EXPECTED_FILES=36`.
- The literal string `nestor_pulse_sdk/tests/` occurs **exactly 36 times in the whole file**, all of
  them inside the `WANTED` list — **zero** occurrences outside the script. The YAML comments name
  test files by BARE FILENAME (`test_stale_reclaim.py`), never with that path prefix. **That is what
  makes a one-grep reconciliation possible, and Task 1 must preserve it.**
- The string `EXPECTED_FILES=<digits>` occurs exactly once. The comments discuss `EXPECTED_FILES` by
  name but never write it with an `=` and a number. **Task 1 must preserve that too.**
- The build-step arg (the `- |` block: `docker run …` plus the whole single-quoted inner script) is
  **~5,500 characters including YAML indentation**, against a Cloud Build cap of **10,000**. It was
  13,473 on 2026-07-29 and the build failed outright with `build step 0 arg 1 too long`; plan
  15.4-03's follow-up moved ~9,400 characters of commentary OUT into YAML comments, verbatim.
  YAML comments are not part of the arg, so the amendment in Task 1 costs the budget nothing —
  **provided every word of it goes above `steps:`.**
- Apostrophes strictly inside the `bash -c '…'` block: **0**. It has to stay 0.
- Baseline to beat, from `.planning/STATE.md` § Gates (15.7): engine build **`7c89be5c`** =
  **1538 passed / 0 failed / 13 skipped**, `collecting: 36 of 36 expected files`. Gates build
  **`2eae97e6`** = **187 passed, 2 deselected**.
- The 13 skips are the DB-bound listed files (`test_stale_reclaim.py`, `test_checkpoint_resume.py`
  and friends) skipping cleanly with no `DATABASE_URL`. **A skip in this gate is contract-compliant
  and is NOT a pass** — read the `-rs` summary, not the exit status.
</current_state>

<what_the_number_is_made_of>
**The formula, and it is the whole job:**

    EXPECTED_FILES = 36 + (test FILES this phase added AND registered in this list)

**36 is not a starting guess — it is a measured baseline** that `7c89be5c` ran against, and D-W5-4
pins the three ungated commits `8eff424` / `868775b` / `9f5a120` as adding **no new test FILE**, so
they contribute **0** to it.

**Known at plan-writing time (verify, do not trust):**

| Plan | New test FILE(s) | Contribution |
|---|---|---|
| 15.8-01 | none — 7 tests into the already-registered `test_question_grouping.py` | 0 |
| 15.8-02 | `test_pipeline_dispatch_clause.py` (NEW) | +1 |
| 15.8-03 | none — into `test_workshop_scope_guard.py` | 0 |
| 15.8-04 | none — into `test_workshop_critique.py` | 0 |
| 15.8-05 | `test_yield_schema.py`, `test_yield_records.py` (NEW) | +2 |
| 15.8-07 | none — it owns the OTHER config | 0 |
| 15.8-08 | none — into `test_cost_serpapi.py` | 0 |
| 15.8-09 / 15.8-10 / 15.8-11 | **UNKNOWN at plan-writing time** — those plans were authored in parallel with this one | read their SUMMARYs |

So the projection is **39 plus whatever 09/10/11 state.** Most plans this phase added **zero** new
test files: they extended modules already in the list. That is normal and is not a reason to doubt
the number.

**The reconciliation is a cross-check, not a source.** The real list comes from the tree:

    git diff --diff-filter=A --name-only <PHASE_CODE_BASE>..HEAD -- tribunal/nestor_pulse_sdk/tests/
    git diff --diff-filter=DR --name-only <PHASE_CODE_BASE>..HEAD -- tribunal/nestor_pulse_sdk/tests/

`PHASE_CODE_BASE` is **`8a6c59f`** (2026-08-04, the docs commit on top of `9f5a120`; the code tree is
identical to `9f5a120`). Deletions and RENAMES matter as much as additions — 15.8-11 reviews test
files and may rename one, and a renamed path that is still in `WANTED` turns the gate RED naming it,
which is the assertion working correctly and must be resolved by fixing the path, never by lowering
the count.

**⚠ A stated number that disagrees with the real file list is a FINDING, not a rounding error.**
Report it in the SUMMARY, name both numbers and both sources, and resolve it by reading the actual
files — not by averaging, not by preferring whichever is larger.

**Exclusions must be NAMED, not silent.** A file this phase added is excluded from `WANTED` only if
one of these is written down beside it:
- it needs a real Postgres and cannot skip cleanly → it belongs to `cloudbuild.test-critical.yaml`;
- it is already run by `cloudbuild.test-gates.yaml` → listing it twice only slows the gate;
- it errors at import or collection without a provider key → it does not meet this config's stated
  contract (pure, or `pytest.skip()`s cleanly when `DATABASE_URL` is unset, per the header).
An unnamed exclusion is the same silent skip this config exists to prevent.

**The passed-count arithmetic, stated so a shortfall cannot be shrugged off:**

    EXPECTED_PASSED = 1538
                    + 10                      (D-W5-4 — the three ungated commits, exactly 10)
                    + 7                       (15.8-01, stated in its plan)
                    + Σ(each other plan's stated new-test count, read from its SUMMARY)

**Skipped should stay at 13.** Every test file this phase adds is stated PURE by its own plan
(`test_yield_schema.py` parses the migration with `ast`; `test_yield_records.py` drives module-level
test seams). **A RISE in the skip count is a finding** — it means a new file is skipping silently,
which is the exact failure this gate exists to catch. Read the `-rs` summary and account for every
skip by name.
</what_the_number_is_made_of>

<tasks>

<task type="auto">
  <name>Task 1: Set EXPECTED_FILES ONCE from the real merged-tree file list, and amend the comment block to record the 15.8 deviation</name>
  <files>tribunal/cloudbuild.test-engine.yaml</files>
  <read_first>
    - tribunal/cloudbuild.test-engine.yaml — THE WHOLE FILE, all 416 lines. The comment block above `steps:` is the thing you are amending; read it before you touch the list.
    - .planning/phases/15.8-*/15.8-CONTEXT.md — D-W5-4, D-W5-5, D-W5-5a and D-W5-12, verbatim
    - .planning/phases/15.8-*/15.8-PLAN-OUTLINE.md — § Shared-file conflicts, the `cloudbuild.test-engine.yaml` row
    - every `.planning/phases/15.8-*/15.8-NN-SUMMARY.md` that exists on your tree — each one states its `EXPECTED_FILES` contribution and its new-test count; those stated numbers are the cross-check
    - .planning/STATE.md § Gates (15.7) — the `7c89be5c` and `2eae97e6` baselines
    - tribunal/cloudbuild.test-gates.yaml — READ ONLY. 15.8-07 owns it; you are a reader and a reconciler of its count, and you may not edit one byte of it.
  </read_first>
  <action>
    **PART A — DERIVE THE LIST.**

    Run the two `git diff --diff-filter` commands from `<what_the_number_is_made_of>` against
    `PHASE_CODE_BASE=8a6c59f` and build the real set of test files this phase ADDED, RENAMED or
    DELETED under `tribunal/nestor_pulse_sdk/tests/`.

    For each ADDED file, decide registration and write the decision down:
      * registered → append its path to `WANTED`, in the same `nestor_pulse_sdk/tests/NAME.py`
        relative form, one per continuation line, appended at the END of the list (do not reorder
        the existing 36 — a reordering makes the diff unreadable and proves nothing);
      * excluded → name which of the three stated exclusion reasons applies, in the YAML comments.

    For each RENAMED or DELETED file, fix or remove its path in `WANTED`. **A path that no longer
    resolves is a RED build naming it — that is the assertion working. Fix the path; never lower the
    count to make the red go away.**

    Then set `EXPECTED_FILES` to the number of paths `WANTED` now names, and **reconcile it three
    ways before you accept it**: (a) the count of paths you can see in the list; (b) `36 + additions
    − removals` from the git diff; (c) the sum of the per-plan stated contributions read out of the
    SUMMARYs. **All three must agree.** If any two disagree, STOP, read the actual files, and record
    the disagreement in the SUMMARY as a finding with both numbers and both sources named.

    **Two invariants that make this file reconcilable with one grep, and that you must preserve:**
      * the literal string `nestor_pulse_sdk/tests/` appears ONLY inside `WANTED` — write test files
        in comments by BARE FILENAME, exactly as the existing comments do;
      * the pattern `EXPECTED_FILES=<digits>` appears EXACTLY ONCE in the whole file — discuss the
        variable by name in comments, never with an `=` and a number.
    Say both invariants out loud in the comment block so the next editor does not break them by
    accident.

    **PART B — AMEND THE COMMENT BLOCK (above `steps:`, where apostrophes are safe).**

    Follow this file's own convention: **supersede in place, never delete.** The 15.2 / 15.3 / 15.4 /
    15.6 ownership paragraphs stay exactly as they are — they are the history of how the list was
    assembled. Add a new PHASE 15.8 paragraph after them, recording all six of these:

    1. **The deviation, stated as a reversal.** Phase 15.8 gives this file exactly ONE owner —
       plan 15.8-13, in wave 4 — and **that is the OPPOSITE of the rule this block states four times
       above**, most explicitly *"the list and the number are one change, and splitting them is how a
       gate goes green having run less than it claims."* Quote that sentence and say plainly that
       15.8 did not follow it. Do not soften it into a footnote: a reader who finds the old rule and
       the new behaviour without this paragraph is reading two authorities that disagree, which is
       the exact shape that produced D-DEF-01's inverted rationale comments and 15.6's CR-01.
    2. **Why.** Eight wave-1 plans would each have bumped the same single `EXPECTED_FILES` line — a
       guaranteed merge conflict, and worse, one that can silently resolve to a WRONG NUMBER. It is
       also the "exact-set allowlist over a file a sibling plan also edits" trap that cost 15.5 a
       cross-plan regression which three green verifications and a passed plan-check all missed.
    3. **The cost that was accepted.** Between wave 1 and wave 4 this repo contained test files that
       were NOT in this list, so they did not run. **The count assertion does not catch that** — the
       list matched its own count; the files were simply absent from it. Say this explicitly; it is
       the honest weakness of the arrangement.
    4. **The condition that makes it safe, written as a condition and not as a reassurance:**
       **NO GATE RUNS BEFORE 15.8-13.** This plan IS the phase gate. **If any future phase adopts
       single-owner ownership AND proposes running this gate mid-phase, the arrangement breaks and
       must be revisited before that build is trusted.**
    5. **D-W5-5a — the scope fence.** The single-owner deviation applies to **THIS config only**.
       `tribunal/cloudbuild.test-gates.yaml` keeps the ORIGINAL one-edit rule: a plan that adds a
       file there adds the path and the count together. A reader who generalises the deviation to
       every CI config reproduces the two-authorities trap in a second place. **Both files must say
       which rule they are under** — 15.8-07 wrote the matching sentence into the other file.
    6. **D-W5-12 — what a flat count on the OTHER gate means.** `.planning/STATE.md` explains 15.6's
       flat 187 as *"a fixed set of 13 files, none of which 15.6 touched."* **The second half does
       not transfer to 15.8.** Re-grep it yourself on your tree — the pattern is
       `pipeline\.tribunal\.|nestor_pulse_sdk\.pipeline` over the 13 paths that config names — and
       write the number YOU measured. D-W5-12 records **10 of 13**; 15.8-07's planner counted **9**
       with a narrower pattern and wrote 9 into the gates config. Record both, name yours as the
       measured one, and state the consequence in both directions: a flat 187 is a genuine
       **REGRESSION PASS** over wave-1 and wave-2 edits to `pipeline.tribunal.*`, and a **RED** there
       is a **real signal about those edits, NOT a config to tune**. **If your measured number
       differs from the 9 written in `cloudbuild.test-gates.yaml`, that is a FINDING for the
       SUMMARY — you may not edit that file to correct it (15.8-07 owns it).**

    Also add, in the same paragraph or beside it: the D-W5-4 arithmetic (baseline `7c89be5c` = 1538
    passed / 0 failed / 13 skipped at 36 of 36; the three commits `8eff424` / `868775b` / `9f5a120`
    added no test FILE, so they are +0 files and +10 passed), and the HOW TO READ A RESULT note —
    `gcloud builds submit … | tail` returns the PIPE's exit code so a FAILED build reports exit 0,
    read the build TEXT via `gcloud builds describe`, and an EXPIRED build is visually identical to
    QUEUED and is not a result.

    Update the narrative counts in the existing "30 paths / THAT 30 IS THE 15.2 + 15.3 BASELINE"
    comment only by APPENDING the 15.8 line to its running history. That comment already says every
    count in it except `EXPECTED_FILES` is narrative; keep it that way.

    **PART C — WHAT YOU MUST NOT TOUCH.**

    ⛔ **NO APOSTROPHE MAY APPEAR ANYWHERE between `bash -c '` and its closing quote** — not in a
    string, not in a comment, and not in a comment quoting this rule. The whole block is ONE
    single-quoted `-c` argument; one apostrophe closes it and the remainder is reparsed by the outer
    shell. The symptom is bewildering — a bare `python: command not found` long AFTER pip has
    succeeded — and it has already cost this project a red build (2026-07-27). Write "the gate would
    have run less than it claims", never "the gate wouldnt". **Every word of Part B goes in YAML
    comments above `steps:`, where apostrophes are safe and cost the arg budget nothing.**

    Keep byte-identical: `pip install --no-cache-dir -q -r requirements.txt` (this plan adds no
    package), `python -m pytest $$FILES -v -rs --tb=short -m "not live"` (the `not live` filter is
    what keeps a real, paid LLM call out of CI), the `|| true` on the `ls`, the `-ne` assertion, the
    `[ -f "$$f" ]` naming loop, the `docker run` invocation, `options` and `timeout`.

    **`|| true` and the count assertion are BOTH load-bearing and removing either one looks
    reasonable.** Drop the `|| true` and one missing file kills the script under `set -e` before
    pytest runs, telling you nothing about the other 38. Drop the assertion and one mistyped path
    makes this gate go green having run less than it claims. The file says so; do not tidy either
    away while you are in there.

    Report the resulting build-step arg length in the SUMMARY, in characters.
  </action>
  <acceptance_criteria>
    - `grep -c "nestor_pulse_sdk/tests/" tribunal/cloudbuild.test-engine.yaml` equals the numeric
      value of the single `EXPECTED_FILES=<digits>` occurrence
    - `grep -cE "EXPECTED_FILES=[0-9]+" tribunal/cloudbuild.test-engine.yaml` returns exactly 1
    - every path named in `WANTED` resolves to a file on disk (`[ -f ]` over the extracted list, zero
      absences)
    - the 36 pre-existing paths are all still present, in their original order, with no removals that
      are not explained by a git-diff-proved rename or deletion
    - apostrophes strictly between `bash -c '` and its closing quote: **0**
    - the build-step arg is under 10,000 characters (measure it; a conservative over-estimate is the
      right kind of error here)
    - `git diff` shows the `pip install` line, the `python -m pytest` line, the `ls $$WANTED … || true`
      line, the `-ne` guard and the `[ -f "$$f" ]` loop all UNCHANGED
    - the YAML comments contain the literal decision IDs `D-W5-4`, `D-W5-5`, `D-W5-5a` and `D-W5-12`,
      a sentence quoting the one-edit rule this phase reversed, and a sentence stating the condition
      `no gate runs before 15.8-13`
    - the YAML comments contain an explicit sentence scoping the deviation to this config only and
      naming `cloudbuild.test-gates.yaml` as keeping the original rule
    - `git diff --name-only` lists exactly ONE file: `tribunal/cloudbuild.test-engine.yaml`
  </acceptance_criteria>
  <verify>
    <automated>cd tribunal &amp;&amp; N=$(grep -c "nestor_pulse_sdk/tests/" cloudbuild.test-engine.yaml) &amp;&amp; test "$(grep -cE 'EXPECTED_FILES=[0-9]+' cloudbuild.test-engine.yaml)" = 1 &amp;&amp; E=$(grep -oE 'EXPECTED_FILES=[0-9]+' cloudbuild.test-engine.yaml | cut -d= -f2) &amp;&amp; test "$N" = "$E" &amp;&amp; MISS=0 &amp;&amp; for f in $(grep -oE 'nestor_pulse_sdk/tests/[A-Za-z0-9_]+\.py' cloudbuild.test-engine.yaml); do [ -f "$f" ] || { echo "ABSENT $f"; MISS=1; }; done &amp;&amp; test "$MISS" = 0 &amp;&amp; test "$(sed -n '/bash -c/,/^options:/p' cloudbuild.test-engine.yaml | tr -cd "'" | wc -c)" = 2 &amp;&amp; test "$(sed -n '/^      - |$/,/^options:/p' cloudbuild.test-engine.yaml | wc -c)" -lt 10000 &amp;&amp; test "$(grep -c 'D-W5-5a' cloudbuild.test-engine.yaml)" -ge 1 &amp;&amp; test "$(grep -c 'D-W5-12' cloudbuild.test-engine.yaml)" -ge 1 &amp;&amp; test "$(grep -c -- '-m "not live"' cloudbuild.test-engine.yaml)" = 1 &amp;&amp; echo "TASK1_STATIC_OK EXPECTED_FILES=$E"</automated>
  </verify>
  <done>
    `EXPECTED_FILES` equals the number of paths `WANTED` names, that number reconciles three ways
    against the merged tree and the per-plan stated contributions, every named path resolves, the
    comment block records the 15.8 deviation with its reasoning, its safety condition and its scope
    fence, and neither the apostrophe count nor the arg length moved off safe ground.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run BOTH gates on the merged tree, read every status from `gcloud builds describe`, and reconcile the counts</name>
  <files>tribunal/cloudbuild.test-engine.yaml (read only — Task 1 already edited it), .planning/phases/15.8-*/15.8-13-SUMMARY.md</files>
  <read_first>
    - tribunal/cloudbuild.test-engine.yaml as edited by Task 1
    - tribunal/cloudbuild.test-gates.yaml as edited by 15.8-07 — READ ONLY
    - .planning/STATE.md § Gates (15.7) — `7c89be5c` and `2eae97e6`
    - .planning/phases/15.8-*/15.8-07-PLAN.md § owed_proofs — the two proofs 15.8-07 owes to this plan
    - every `.planning/phases/15.8-*/15.8-NN-SUMMARY.md` — each plan's stated new-test count
  </read_first>
  <action>
    Submit both gates against the merged tree. The config does not need to be committed to be
    submitted; the SOURCE that is uploaded is the `tribunal` directory as it sits on disk, which is
    why the worktree preflight is a blocking gate on this plan.

        gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT"
        gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml  --project="$GOOGLE_PROJECT"

    **Capture the `BUILD_ID` of each.** Then, for each, read the outcome as TEXT — never as a shell
    status:

        gcloud builds describe <BUILD_ID> --format="value(status)"
        gcloud builds log <BUILD_ID>

    `SUCCESS` and `FAILURE` are results. `QUEUED`, `WORKING`, `EXPIRED` and `CANCELLED` are not — an
    EXPIRED build looks exactly like a QUEUED one and must be resubmitted, not interpreted.

    **ENGINE GATE — what settles it.** The log must contain
    `collecting: <N> of <N> expected files` where `<N>` is the `EXPECTED_FILES` Task 1 set, followed
    by a pytest summary. Then reconcile, in the SUMMARY, as a table:

      * **Files:** `<N>` vs `36 + additions − removals`. Equal, or a named finding.
      * **Passed:** observed vs `1538 + 10 + 7 + Σ(other plans' stated new-test counts)`. **If the
        observed count is LOWER than expected, that is a shortfall and it must be EXPLAINED with a
        mechanism, not noted.** Candidate mechanisms, in the order worth checking: a plan's SUMMARY
        overstated its test count; a test file collected but a class or module-level guard skipped
        part of it; a parametrised test collapsed. **Do not adjust the expectation to match the
        observation.**
      * **Failed:** must be **0**. Any failure is a real defect in the merged tree — read the
        traceback and report it. **Do not touch `cloudbuild.test-engine.yaml` to make a red go
        green;** a config change in response to a test failure is precisely the false-assurance
        move this plan exists to prevent.
      * **Skipped:** baseline 13. Account for every skip BY NAME from the `-rs` summary. **A rise is
        a finding** — a new file skipping silently is the failure mode this gate exists to catch.

    **GATES GATE — what settles it (D-W5-12).** The log must contain
    `collecting: 13 of 13 expected files` — 15.8-07's newly-ported assertion, running for the first
    time — followed by **187 passed, 2 deselected**. The 2 deselected are the `live`-marked
    calibration tests that `-m "not live"` removes.

    **A flat 187 here is a genuine REGRESSION PASS, not insulation, and you must write that
    explanation down** rather than merely noting the flatness. The mechanism: this config runs a
    fixed set of 13 files and 15.8 adds none — **but the majority of those 13 import
    `pipeline.tribunal.*` or `nestor_pulse_sdk.pipeline`, and 15.8-02, 15.8-03, 15.8-09 and 15.8-10
    all edit those modules.** So an unchanged 187 is evidence that four waves of engine edits broke
    nothing those files assert.

    **If the gates gate goes RED, that is a real signal about the engine edits. Find what wave 1 or
    wave 2 broke. Do not make the config green** — and note that you cannot edit it anyway; 15.8-07
    owns it and you are read-only on it.

    **Also settle 15.8-07's two owed proofs and say which you took.** Proof 1 is the happy path above.
    Proof 2 — submitting a scratchpad copy of the gates config with one mistyped path and confirming
    the build goes RED with `GATES GATE ASSERTION FAILED` naming that path — is explicitly OPTIONAL
    per 15.8-07's `<owed_proofs>`, because its Task 2 already proved the assertion bites on the real
    config text locally. **Record which of the two you took; do not leave it ambiguous.**

    Record in the SUMMARY, for every build: the `BUILD_ID`, the config, the status read from
    `describe`, the `collecting:` line verbatim, and the pytest summary line verbatim.
  </action>
  <acceptance_criteria>
    - a `BUILD_ID` is recorded for the engine gate and for the gates gate, each with a status read
      from `gcloud builds describe`, and each status is `SUCCESS` or `FAILURE` (never QUEUED /
      WORKING / EXPIRED / CANCELLED)
    - the engine log's `collecting:` line reads `<N> of <N>` where `<N>` is the value Task 1 set
    - the engine result is `0 failed`, and the passed count is reconciled arithmetically against
      1538 + 10 + the per-plan stated counts, with any shortfall explained by a named mechanism
    - the engine skip count is accounted for by name; a rise above 13 is recorded as a finding
    - the gates log reads `collecting: 13 of 13 expected files` and `187 passed, 2 deselected`, or
      any deviation is recorded as an engine-regression finding and NOT as a config problem
    - the SUMMARY states which of 15.8-07's two owed proofs was taken
    - `git diff --name-only` still lists only `tribunal/cloudbuild.test-engine.yaml` — this task
      edits no source
  </acceptance_criteria>
  <verify>
    <automated>OWED TO CLOUD BUILD — this is itself the build task. It is settled by two recorded BUILD_IDs whose `gcloud builds describe &lt;BUILD_ID&gt; --format="value(status)"` both return SUCCESS, plus the `collecting:` and pytest summary lines quoted verbatim in the SUMMARY. If `gcloud builds submit` cannot run in this environment, mark this task BLOCKED-OWED with those exact commands — do NOT mark it complete and do NOT record a result you did not read.</automated>
  </verify>
  <done>
    Both gates ran on the merged tree, both statuses were read from `gcloud builds describe`, the
    engine gate collected exactly the number of files the config names with 0 failures, the passed
    count is reconciled with every delta explained, and the gates gate's result is recorded as either
    a regression PASS with its mechanism written down or a named engine-regression finding.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pay the batched revert-and-confirm-red proof — the new tests must go RED on a deliberately mutated tree</name>
  <files>a scratch git branch or worktree (NOT committed, NOT merged), .planning/phases/15.8-*/15.8-13-SUMMARY.md</files>
  <read_first>
    - .planning/phases/15.4-*/15.4-05-SUMMARY.md — proof rows P3 and P4, both PENDING-CLOUDBUILD since 2026-07-29; P4 names the exact expected failure text
    - every `.planning/phases/15.8-*/15.8-NN-SUMMARY.md` mutation table — 15.8-01 Task 3 and 15.8-04 both authored one; each names the minimal source mutation and the test node IDs it must turn RED
    - .planning/phases/15.8-*/15.8-CONTEXT.md § Specifics item 5 — the debt this task pays
  </read_first>
  <action>
    A test that cannot fail is not a gate. This proof is owed from `15.4-05` and is paid here
    **batched — one mutated tree per group, not one per plan.**

    Work on a scratch branch or worktree cut from the merged HEAD. **Never commit a mutation and
    never merge one.** `git status --porcelain` must be EMPTY again before this task is done, and the
    SUMMARY must state that it is.

    **MUTANT A — this phase's own fixes.**

    Use **minimal source-text mutations**, one per fix, taken VERBATIM from each plan's own SUMMARY
    mutation table. **Do NOT restore whole production files to `8a6c59f`**: a wholesale restore
    removes symbols that wave-2 code and wave-2 tests import (`count_drops`, the yield emitter call
    sites), which produces COLLECTION ERRORS rather than clean test failures — and a red from a
    collection error does not prove that the test asserting the fix can fail.

    The mutations known at plan-writing time, to be confirmed against each SUMMARY:

      * 15.8-01 / WR-01 — move the `room = ceiling - len(work)` computation back BEFORE the merge pass
      * 15.8-01 / WR-05 — restore `ceiling = max(1, int(max_groups or 1))`
      * 15.8-02 / WR-04 — restore the raising `list(groups or [])` in `_bound_groups_to_winners`
      * 15.8-02 / WR-06 — restore `_uniform_dispatch` counting distinct `corroboration_key`s instead
        of copies
      * 15.8-03 / CR-01 ×2 — remove the `_text_key` join from `_restamp_groups`' `rank_by_text` and
        `_stamp_discovery_ranks`' `numbered` comparison sites
      * 15.8-03 / D7 — remove the `langs` re-normalisation that follows `enforce_group_coverage`
      * 15.8-04 — flip `entries[-limit:]` back to `entries[:limit]` in `barred_block`
      * 15.8-04 — remove the production `DROP_CLUSTERED_ONTO_LIVE` writer
      * 15.8-04 / D-W5-7 — restore `stats["calls"] = 0` in `cluster_candidates`' `except` path
      * 15.8-05 / D-W5-10 — make the emitter DROP the row on an out-of-vocabulary `parent_kind`
      * 15.8-09 / 15.8-10 — whatever their SUMMARY mutation tables name

    **Two rules on batching:**
      * every mutation must live in a DISTINCT symbol, and the test node IDs it is expected to turn
        RED must be DISJOINT from every other mutation's. If two mutations share an expected-red
        test, split them across two mutant builds — a shared red proves neither.
      * the mutant tree must still print `collecting: <N> of <N>`. **A mutation that changes the file
        list invalidates the whole proof.**

    Submit ONE build of the mutant tree with the engine config, capture the `BUILD_ID`, and read the
    result with `gcloud builds describe` and `gcloud builds log` as in Task 2. **The expected status
    is `FAILURE`.**

    **MUTANT B — the carried `15.4-05` debt.**

    First try `git revert --no-commit 6980fda` (fix(15.4-05): tolerant agent_done build lambdas). It
    touches `own_researcher.py` and `research_division.py`, and **`research_division.py` is edited by
    15.8-02 and 15.8-09 this phase, so the reverse patch may not apply.** If it conflicts, abandon
    the revert (`git revert --abort` / `git checkout -- .`) and use the targeted equivalent instead:
    make `_fact_count_label` (`research_division.py`) and `_skipped_label` (`own_researcher.py`)
    propagate their exception rather than returning `_UNKNOWN_FACTS` / `_UNKNOWN_SKIPPED` — the
    minimal change that restores the intolerant behaviour those helpers were written to fix. Say in
    the SUMMARY which of the two routes you took.

    Expected RED, per `15.4-05-SUMMARY.md` P4: the four
    `test_j_a_degraded_result_still_records_exactly_one_done_line[...]` parameters and
    `test_j_an_uncountable_fact_block_still_records_its_done_line`, each failing with
    **`recorded 0 done line(s), not 1`**. The healthy-case tests still pass and nothing else changes.
    Also confirm P3 is settled by Task 2's clean build: the two inverted tests
    (`test_a_result_missing_the_keys_the_done_line_reads_still_records_its_line` and
    `test_own_researcher.py::test_a_turn_whose_shape_makes_the_line_itself_raise_changes_nothing`)
    passed there. **If either fails on the clean tree, 15.4-05's inversion is wrong, not the fix** —
    report that, do not adjust the test.

    **THE TABLE — the deliverable.** In the SUMMARY, one row per mutation:

        | Mutation | Symbol / file | Expected RED node IDs | Observed | Verdict |

    **A mutation whose tests stayed GREEN is a FINDING: that test is vacuous and does not prove its
    fix.** Name it, name the plan that authored it, and report it to the orchestrator. Do not
    silently drop the row, and do not weaken the expectation to match what happened. Equally, a red
    that appears where none was expected is a finding — read the traceback before assuming it is
    collateral.

    Finish by destroying the scratch branch/worktree and asserting the working tree is clean.
  </action>
  <acceptance_criteria>
    - Mutant A: one `BUILD_ID`, status read from `gcloud builds describe`, status is `FAILURE`, and
      the `collecting: <N> of <N>` line matches the clean build exactly
    - Mutant B: one `BUILD_ID` with status `FAILURE`, and the log contains
      `recorded 0 done line(s), not 1` for the five node IDs named in `15.4-05-SUMMARY.md` P4
    - the mutation table has an OBSERVED result recorded for every row — no row left blank
    - every row whose tests stayed green is recorded as a FINDING naming the authoring plan
    - the SUMMARY states which route Mutant B took (`git revert` or the targeted helper mutation)
    - `git status --porcelain` is EMPTY and `git worktree list` shows no scratch worktree left behind
    - no mutation was committed to any branch that will be merged
  </acceptance_criteria>
  <verify>
    <automated>cd /c/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; echo TREE_CLEAN &amp;&amp; git worktree list</automated>
  </verify>
  <done>
    Two mutant builds ran, both came back `FAILURE` with statuses read from `gcloud builds describe`,
    every mutation row has an observed result, `15.4-05`'s P4 produced its exact recorded failure
    text, every vacuous test is named as a finding, and the working tree is clean with no scratch
    worktree left on disk.
  </done>
</task>

</tasks>

<owed_proofs>
**Say plainly what this plan cannot pay from this machine, and exactly what settles each.**

There is no pytest, no Python 3.11 and no Docker here. Task 1 is fully provable locally — it is text
and shell logic over the real file list. **Tasks 2 and 3 are Cloud Build results and nothing else
settles them.**

1. **The engine gate on the merged tree.**
   `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-engine.yaml --project="$GOOGLE_PROJECT"`
   Settled by: `gcloud builds describe <BUILD_ID> --format="value(status)"` returning `SUCCESS`, a
   log containing `collecting: <N> of <N> expected files`, and a pytest summary with **0 failed** and
   a passed count reconciled against 1538 + 10 + the per-plan stated counts.

2. **The gates gate.**
   `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"`
   Settled by: status `SUCCESS`, `collecting: 13 of 13 expected files`, **187 passed, 2 deselected**.
   This also pays 15.8-07's owed proof 1.

3. **The two mutant builds.** Same commands against the mutated trees; settled by status `FAILURE`
   with the expected node IDs red and the mutation table complete.

4. **15.8-07's owed proof 2** (a scratchpad copy of the gates config with one mistyped path, expected
   RED with `GATES GATE ASSERTION FAILED`) is **OPTIONAL** by that plan's own ruling. Record which
   was taken.

**Never write a build result you did not read, and never read a result from a shell exit code.** If
these cannot be run in this environment, they are `BLOCKED-OWED` and the orchestrator must be told —
15.8-14 deploys and 15.8-15 spends ~$45 on the strength of this plan's word.

**Package legitimacy:** this plan installs nothing. `requirements.txt` is untouched and the
`pip install --no-cache-dir -q -r requirements.txt` line is preserved byte-identical, so no package
legitimacy audit is triggered and no `[ASSUMED]` / `[SUS]` checkpoint is required.
</owed_proofs>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo tree → Cloud Build gate | this config decides what this project accepts as PROOF, and no human re-reads its file list on any given build |
| Cloud Build log → operator judgement | the operator declares "green" at the end of a five-wave redesign, from text this config itself prints, immediately before one deploy and one ~$45 run |
| this plan's SUMMARY → 15.8-14 / 15.8-15 | the deploy and the measuring run proceed on the strength of numbers recorded here; a fabricated or unread build result is unrecoverable downstream |
| comment block → the next phase's planner | prose in this file has been treated as authority by three prior phases; a comment that disagrees with the code has already caused defects here |
| build container → the internet | `pip install` reaches PyPI; `-m "not live"` is the only thing keeping paid provider egress out of CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.8-13-01 | Tampering | the `WANTED` list vs `EXPECTED_FILES` | mitigate | the number is DERIVED from the merged tree and reconciled three ways (list count, git-diff additions/removals, per-plan stated contributions); Task 1's verify re-derives it from the file with one grep and fails on any mismatch, and every path is proved to resolve with `[ -f ]` |
| T-15.8-13-02 | Repudiation | the build status an operator records | mitigate | status read only from `gcloud builds describe`; `builds submit \| tail` returns the PIPE's exit code so a FAILED build reports exit 0; EXPIRED is explicitly not a result; the rule is written into the YAML comments so it survives this plan |
| T-15.8-13-03 | Spoofing (a config presenting as a gate) | a green build that ran less than it claims | mitigate | the `-ne` assertion plus the `[ -f "$$f" ]` naming loop are preserved byte-identical; Task 3's mutant builds prove the tests can fail; a count that did not rise must be EXPLAINED with a mechanism, not noted |
| T-15.8-13-04 | Tampering (two authorities) | the comment block instructing the opposite of what the phase did | mitigate | Part B records the reversal, quotes the rule it broke, states the safety condition (`no gate runs before 15.8-13`) and fences the scope to this config only (D-W5-5a); acceptance criteria assert the decision IDs are literally present |
| T-15.8-13-05 | Denial of Service (self-inflicted) | the single-quoted `bash -c` argument | mitigate | zero-apostrophe assertion strictly inside the block and a measured arg length under the 10,000-char cap; all narrative goes into YAML comments outside the arg. Precedent: one red build 2026-07-27 (exit 127) and one un-runnable config at 13,473 chars |
| T-15.8-13-06 | Spoofing (a real LLM call presenting as a test) | the pytest invocation | mitigate | `-m "not live"` preserved byte-identical and asserted present exactly once; this config injects no provider key and no `DATABASE_URL` |
| T-15.8-13-07 | Tampering | a mutant tree merged by accident | mitigate | mutations live on a scratch branch or worktree, are never committed, and the task is not done until `git status --porcelain` is empty and `git worktree list` is clean |
| T-15.8-13-08 | Elevation of Privilege | `docker run -v /workspace:/workspace` running pytest as root | accept | identical to every other gate config in this repo; no credential, service-account token or DSN is passed into the container. Changing it is out of scope and would diverge the two gate configs |
| T-15.8-13-09 | Information Disclosure | Cloud Build logs | accept | this config carries no secret, no DSN and no key; `logging: CLOUD_LOGGING_ONLY` keeps output inside the project |
| T-15.8-13-SC | Tampering | npm/pip/cargo installs | mitigate | this plan adds NO package. The `pip install … -r requirements.txt` line is preserved byte-identical and asserted unchanged in `git diff`; `requirements.txt` is not in `files_modified`. No `[ASSUMED]` / `[SUS]` package, so no legitimacy checkpoint |
</threat_model>

<verification>
1. `EXPECTED_FILES` equals the count of `nestor_pulse_sdk/tests/` occurrences in
   `tribunal/cloudbuild.test-engine.yaml`, and every one of those paths resolves on disk.
2. That number reconciles with `36 + additions − removals` from
   `git diff --diff-filter=A|DR <PHASE_CODE_BASE>..HEAD -- tribunal/nestor_pulse_sdk/tests/`, and
   with the sum of the per-plan stated contributions. Any disagreement is recorded as a finding with
   both numbers and both sources named.
3. Apostrophes strictly inside the `bash -c '…'` block: 0. Build-step arg: under 10,000 characters,
   with the measured length stated.
4. The YAML comments contain `D-W5-4`, `D-W5-5`, `D-W5-5a` and `D-W5-12`, the quoted one-edit rule
   this phase reversed, the condition `no gate runs before 15.8-13`, and the sentence scoping the
   deviation away from `cloudbuild.test-gates.yaml`.
5. Engine gate: `BUILD_ID` recorded, status `SUCCESS` from `gcloud builds describe`,
   `collecting: <N> of <N>`, **0 failed**, passed count reconciled against 1538 + 10 + stated counts,
   skips accounted for by name.
6. Gates gate: `BUILD_ID` recorded, status `SUCCESS`, `collecting: 13 of 13 expected files`,
   **187 passed, 2 deselected** — with the flatness EXPLAINED as a regression pass over
   `pipeline.tribunal.*` edits, and the measured import count stated alongside D-W5-12's 10 and the
   gates config's 9.
7. Mutant A and Mutant B: both `FAILURE`, mutation table complete with an observed result on every
   row, `15.4-05` P4's exact text `recorded 0 done line(s), not 1` present, every vacuous test named.
8. `git diff --name-only` on the delivered branch lists exactly one file:
   `tribunal/cloudbuild.test-engine.yaml`. `git status --porcelain` is empty and no scratch worktree
   remains.
9. `15.8-13-SUMMARY.md` is force-added to git (`.planning/` is gitignored).
</verification>

<success_criteria>
- `EXPECTED_FILES` was set ONCE, from the real file list on the merged tree, and reconciles three
  ways.
- Every test file this phase added is registered or has a named exclusion reason.
- The comment block and the code no longer disagree: the 15.8 deviation, its reasoning, its safety
  condition and its scope fence are all written down, with the rule they reversed quoted.
- Both Cloud Build gates ran on the merged tree, with statuses read from `gcloud builds describe` and
  never from a shell exit code.
- The engine gate beats `7c89be5c` — 0 failed, and a passed count reconciled arithmetically rather
  than eyeballed.
- The gates gate's result is recorded either as an explained regression PASS or as a named
  engine-regression finding — never as a config tuned to green.
- `15.4-05`'s revert-and-confirm-red debt is paid, batched, with a complete mutation table.
- Nothing that could not be proved is claimed as proved.
</success_criteria>

<output>
Create
`.planning/phases/15.8-research-engine-redesign-yield-instrumentation-deploy-one-me/15.8-13-SUMMARY.md`
when done. `.planning/` is gitignored — it needs `git add -f`.

**The SUMMARY must carry, for 15.8-14 and 15.8-15 to read without opening this plan:**

- the final `EXPECTED_FILES` value and the three-way reconciliation table behind it;
- every test file this phase added, with registered / excluded and the reason;
- the passed-count arithmetic — baseline, +10, per-plan contributions, observed, and any shortfall
  with its mechanism;
- every `BUILD_ID` with its config, its status as read from `gcloud builds describe`, its
  `collecting:` line and its pytest summary line, quoted verbatim;
- the gates-gate flatness explanation and the import count YOU measured, beside D-W5-12's 10 and the
  9 written into `cloudbuild.test-gates.yaml` — flagged as a finding if they differ;
- the complete mutation table with an observed result on every row, and every vacuous test named
  with its authoring plan;
- which of 15.8-07's two owed proofs was taken;
- the measured build-step arg length;
- **an explicit statement of what remains OWED**, if anything, with the exact command that settles
  it — because 15.8-14 deploys on the strength of this document.
</output>
</content>
</invoke>
