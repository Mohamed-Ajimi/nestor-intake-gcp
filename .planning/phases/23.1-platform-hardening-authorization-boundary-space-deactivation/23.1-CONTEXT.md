# Phase 23.1 — CONTEXT (THE AUTHORITY)

Source: an external audit of 2026-09-03, re-verified against the tree at `047dcfe` by
reading the code. **This file records what was VERIFIED, what the audit got WRONG, and the
decisions that are now pinned.** Where this file and the audit disagree, this file wins.

Every claim below carries a `file:line` that was actually opened. Nothing here is inferred
from the audit's prose.

---

## 1. VERIFIED — the authorization boundary (the real hole, and it is WIDER than the audit said)

The audit named `/review` and the AI router. Reading every route in `intake_routes.py`
found the same defect on six more verbs. The full list of routes that take
`Depends(get_current_identity)` and NO role gate:

| Verb | Line | What a role=`user` can do today |
|---|---|---|
| `POST /intakes/{id}/review` | `intake_routes.py:1356` | advance their own intake `submitted -> reviewed` |
| `POST /intakes/{id}/deliver` | `intake_routes.py:1517` | mark their own intake `delivered` + mail the client |
| `POST /intakes/{id}/report/replace` | `intake_routes.py:1590` | repoint the delivered report |
| `POST /intakes/{id}/mail/validation` | `intake_routes.py:894` | send product-branded mail + stamp `validation_link_sent_at` |
| `POST /intakes/{id}/mail/reminder` | `intake_routes.py:914` | send product-branded mail |
| `POST /intakes/{id}/mail/results` | `intake_routes.py:932` | send product-branded mail + stamp `results_link_sent_at` |
| `POST /intakes/{id}/mail/intake` | `intake_routes.py:952` | send product-branded mail |
| `GET /intakes/{id}/members` | `intake_routes.py:856` | enumerate space member emails |
| the whole `ai_router` (7 routes) | `ai_routes.py:56` | fire unbounded paid Claude/OpenAI/Whisper work |

`ai_router` is mounted under `protected_router` (`main.py:158`) so it inherits
`get_current_identity` and nothing more. `ai_routes.py:80` confirms Identity-only.

### The gate already exists — do NOT invent a new one

`research_routes.py:153` defines `_superadmin_gate`. Its docstring pins the convention:
**existence-hidden 404, not 403**, declared BEFORE `get_tenant_repo` in the signature so it
resolves first (a null-space user must not get `get_tenant_repo`'s 403, which would leak
that the endpoint exists). A denial suite pins EXACTLY 404. `db/session.py:357`
(`get_admin_session`) is the other precedent — it uses 403, but that is the *admin* router
where existence is not secret.

### THE TRAP — the client surface that MUST STAY OPEN

Traced from every non-admin route (`intake.index.tsx`, `intake.$id.tsx`,
`intake.$id.results.tsx`, `intake.$id.report.tsx`) and from `IntakeForm.tsx:14-16`:

- `GET /intakes` — `intake.index.tsx:12`
- `GET /intakes/{id}` — `intake.$id.tsx:8`
- `GET /intakes/templates` — `intake.$id.tsx:10`
- `GET /intakes/{id}/answers` and `PATCH /intakes/{id}/answers` — `IntakeForm.tsx:14`
- `POST /intakes/{id}/submit` — `IntakeForm.tsx:15`
- **`GET /intakes/{id}/skill-runs` and `GET /intakes/{id}/skill-runs/{run_id}`** — `IntakeForm.tsx:16`
- `GET /intakes/{id}/report` + storage signed-url — `intake.$id.report.tsx:10-11`

**`IntakeForm` (the CLIENT form) reads the skill-run endpoints.** They render the client's
proposal tick shipped 2026-08-31. Gating them breaks a live client feature, and every
locale/type/lint gate stays green while it happens. This is the single most likely way this
phase does damage.

---

## 2. VERIFIED — space deactivation is cosmetic

`admin_routes.py:511` writes `organizations.status='deactivated'` and audits. A grep for any
read of that column in an auth path returns nothing. Members keep working claims.

Contrast the per-USER path, which is correct and proven: `admin_routes.py:246` does
`update_user(disabled=True)` + `revoke_refresh_tokens` + `set_membership_status(...)`, and
`dependencies.py:78` passes `check_revoked=True` so it bites on the next request.

---

## 3. VERIFIED — AI jobs have no dedup and context-pack has no precondition

- `ai_session.py:172` `create_running_skill_run` inserts a fresh `running` row on EVERY
  call. No active-run check, no idempotency key, no uniqueness constraint
  (`models/skill_run.py` has none).
- `ai/skills/context_pack.py:171` calls `patch(intake_id, status="decomposed", ...)`
  UNCONDITIONALLY. Any status becomes `decomposed`. Compare `/review`, which does have an
  allow-list (`_next_review_status`, 409 otherwise) — the pattern to copy is already here.

---

## 4. VERIFIED — tribunal run ownership

A fencing token DOES exist and the audit missed it: `execute.py:88` `_CONSUME_CLAIM_SQL`
gates on `worker_id = :wid AND started_at = :token` under a per-run advisory lock
(`execute.py:69`), making a claim dispatch at-most-once. What is missing:

- `worker.py:198` `_HEARTBEAT_SQL` gates on `status='running'` only — **no `worker_id`**. A
  displaced-but-alive worker keeps asserting liveness on a run it no longer owns.
- `worker.py:565` terminal write gates on `status='running'` only — no `worker_id`.
- Nothing re-checks the fence DURING execution, so a reclaimed run can be executed twice at
  full paid cost.

Mitigations already in place (why this is Medium, not Critical): `STALE_RUN_MINUTES=60`
(`deploy-worker.sh:183`) against a 30s heartbeat = 120 consecutive misses required;
`MAX_RECLAIMS=2`; and `REAP_SQL` (`worker.py:240`) fails a run that exhausts recoveries.

### Tribunal API non-idempotency (verified)

- **`GET /{run_id}/report-proposal`** (`runs/api.py:371`) — on a cache miss it calls
  `build_audited_client()` + `build_report_proposal(...)` and INSERTs an `Output`
  (`api.py:405-415`). A **billable, side-effecting GET**. Two concurrent reads = two paid
  calls.
- **`POST /{run_id}/answer`** (`runs/api.py:269`) — reads the run, checks
  `status != "needs_input"` -> 409, THEN writes. Two concurrent callers both pass the check
  and both queue a new run. `idempotency_key=uuid.uuid4()` at `api.py:374` is freshly random
  per row and protects nothing.

---

## 5. VERIFIED — hygiene

- `.gitignore` has no `*.tfstate`; `providers.tf:26` ships the `backend "gcs"` block
  commented out. **No state file exists in the tree and none was ever committed**
  (`git log --all --diff-filter=A` over `*.tfstate` is empty), so this is a latent gap, not
  a live exposure.
- `BattlecardMarkdown.tsx:74` uses `rehypeRaw` with no sanitizer. Reachable only from
  `admin.sales.projects.$id.tsx:1081` (legacy Supabase path, inert without `VITE_SUPABASE_*`).
- `cloudbuild.test.yaml:86` runs the integration-marked suite ONLY.
- `frontend/cloudbuild.yaml` has exactly one step (`id: build`) — no lint/tsc/vitest.
- `tribunal/infrastructure/cloud-run/DEPLOY.md:88` says `--allow-unauthenticated`;
  `deploy-worker.sh:175` says `--no-allow-unauthenticated`.
- Dead: `AdminResearchResultsPanel.tsx` (0 refs), `NestorBriefingPDF.tsx` (refs in comments
  only), two `StatusPill` impls (`intake/_status.tsx:29` + `admin.pulse.intakes.$id.tsx:184`),
  `skill_runs.started_at` (already documented dead at `intake_routes.py:177`).
- `runs/schemas.py:111,126` — the `brief` field has a min length and no max.

---

## 6. THE AUDIT WAS WRONG ABOUT THESE — do not "fix" them

- **`_ACTIVE_IDENTITY` (`run_task.py:542`) is NOT a tenant-scope bug.** The audit claimed
  finalization "applies the wrong tenant scope and silently updates zero rows". Every caller
  is superadmin-gated (`research_routes.py:193/389/532` all take `_superadmin_gate`), and
  `_patch_run` (`run_task.py:515`) documents that a superadmin has no own space so the repo
  `_scope` leaves the statement unchanged. Swapping one superadmin for another changes
  `actor_uid` only. **Scope: pass identity explicitly, do not re-architect.**
- **"a caller-selected bucket passed to an extractor"** — no such parameter exists anywhere
  in the tribunal schemas or run API. `audit/gcs_blob.py:354` resolves from the
  `AUDIT_GCS_BUCKET` env var. **No work item.**
- **Terraform state is not Critical** — see section 5.

---

## 7. PINNED DECISIONS

- **D-23.1-01** — Promote `_superadmin_gate` out of `research_routes.py` into a shared
  module (`app/auth/gates.py`). `research_routes.py` imports it. Behaviour identical
  (existence-hidden 404) so its existing denial suite stays green untouched. NO per-route
  role-comparison copies — one gate, one convention, one test surface.
- **D-23.1-02** — Apply the gate to exactly the nine verbs in section 1's table, and to
  `ai_router` at the ROUTER level (one dependency, not seven). Section 1's stay-open list is
  a hard non-goal — a test must pin each stay-open route as still reachable by role=`user`.
- **D-23.1-03** — Space deactivation CASCADES inside `deactivate_space`'s existing
  transaction, reusing the per-user machinery at `admin_routes.py:246`. Reactivate gets the
  inverse. **Rejected alternative:** an org-status lookup in `get_current_identity` —
  `dependencies.py` deliberately has no DB call (D-06) and already pays one `get_user`
  round-trip (D-04); a cascade costs zero per request and reuses a tested path.
- **D-23.1-04** — AI dedup is a DB invariant, not an app check: an alembic partial unique
  index on `skill_runs (intake_id, skill) WHERE status='running'`, with
  `create_running_skill_run` translating the IntegrityError into a 409. A pure app-level
  check races.
- **D-23.1-05** — `context_pack` uses a compare-and-swap on the `decomposed` transition
  mirroring `_next_review_status`. On a disallowed source status the SKILL RUN finalizes
  `failed` with a readable message — it must never corrupt the intake lifecycle, and it must
  never leave a `running` row behind.
- **D-23.1-06** — Tribunal: add the `worker_id` clause to `_HEARTBEAT_SQL` and to the
  terminal write. Keep the `status='running'` clause as well; both, not one.
- **D-23.1-07** — `GET /report-proposal` keeps its URL and its on-demand generation (the
  shaping panel depends on it) but wraps generation in the SAME per-run advisory lock
  `execute.py` uses, re-reading the cache UNDER the lock. **Rejected alternative:** moving
  generation to a POST — a breaking API change for a live UI that buys nothing the lock does
  not.
- **D-23.1-08** — `POST /answer` becomes a conditional UPDATE (compare-and-swap on the
  `needs_input` status), not read-then-write.
- **D-23.1-09** — CI widening lands LAST. Dropping the integration-only marker surfaces
  three currently-failing non-integration tests; they are fixed FIRST or the gate lands red.
  The three were reported, not reproduced here (no pytest in the only local interpreter,
  which is 3.11.9 while the backend needs 3.12+) — **reproduce via Cloud Build before
  fixing, do not trust the audit's list.**
- **D-23.1-10** — The `decomposed` scope ceiling: CLAUDE.md says the flow stops there;
  `main.py:152` mounts the research router and Tribunal is LIVE and deployed. **Reality
  wins — update CLAUDE.md.** Removing the research router is explicitly forbidden.

---

## 8. OPERATOR-ONLY (agent cannot do these)

1. Create the tfstate bucket and run `terraform init -migrate-state`.
2. Decide whether to rotate the `app_superadmin` password. `main.tf:108` generates it and it
   flows to Secret Manager; `terraform apply` ran at some point from an unknown machine, so
   that machine holds it in plaintext state. Rotation touches the Cloud SQL user AND the
   secret — operator call.
3. The deploy. `gcloud builds submit` is classifier-blocked for the agent; the operator runs
   it and polls by build id (their terminal times out at 2 min while the build continues —
   NEVER resubmit).
4. Pin the account and project on every gcloud command; the config drifts mid-session.

---

## 9. NON-GOALS

- No change to the research router's existence or mount.
- No re-architecture of `_ACTIVE_IDENTITY` beyond passing identity explicitly.
- No dependency-advisory remediation in this phase (36 reported, mostly build-chain; the
  prod image ships only the built output). Triage is its own phase.
- No Supabase work. The legacy project is never paused or deleted.
- No run is triggered. This phase must cost ZERO provider spend.
