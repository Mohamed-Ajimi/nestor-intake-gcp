---
phase: 23.1-platform-hardening-authorization-boundary-space-deactivation
plan: 14
type: execute
wave: 5
depends_on: [23.1-07, 23.1-13]
files_modified:
  - backend/tests/test_mail_render.py
  - backend/tests/test_research_runs_migration.py
  - cloudbuild.test.yaml
  - frontend/cloudbuild.yaml
  # DEF-23.1-02 (the deferred frontend lint cleanup) is appended here. Plan 08
  # created the file in wave 1; this plan is wave 5, so there is no contention.
  # `.planning/` is gitignored — `git add -f`.
  - .planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/deferred-items.md
autonomous: false
requirements: [SEC-01, COST-01, TENANT-02]

must_haves:
  truths:
    - "The whole backend test suite passes locally with zero failures — not just the integration-marked subset."
    - "cloudbuild.test.yaml runs every backend test, not only the integration-marked ones."
    - "The widened gate asserts how many test files it collected, so a mistyped path is a red build rather than a silent skip."
    - "frontend/cloudbuild.yaml runs tsc and vitest before it builds the image."
    - "The widened configs have actually been executed once in Cloud Build and observed green."
  artifacts:
    - path: "cloudbuild.test.yaml"
      provides: "Full backend suite as the merge gate, with a collection assertion"
      contains: "EXPECTED_FILES"
    - path: "frontend/cloudbuild.yaml"
      provides: "tsc + vitest steps ahead of the image build"
      contains: "tsc"
  key_links:
    - from: "cloudbuild.test.yaml"
      to: "backend/tests"
      via: "pytest with no marker filter plus a collected-file-count assertion"
      pattern: "EXPECTED_FILES"
---

<objective>
Close D-23.1-09, LAST, as the decision requires. `23.1-CONTEXT.md` § 5: `cloudbuild.test.yaml:86`
runs the integration-marked suite ONLY, and `frontend/cloudbuild.yaml` has exactly one step
(`id: build`) — no lint, no tsc, no vitest.

D-23.1-09: dropping the marker filter surfaces currently-failing non-integration tests; they
are fixed FIRST or the gate lands red. It also says the failures "were reported, not reproduced
here — reproduce via Cloud Build before fixing, do not trust the audit's list."

Purpose: CI that runs a third of the suite is not a gate, it is a habit.

Output: the remaining red non-integration tests fixed; `cloudbuild.test.yaml` widened with a
collection assertion; `frontend/cloudbuild.yaml` gaining tsc and vitest; and ONE operator-run
Cloud Build proving both.

## ⭐ THE AUDIT'S LIST WAS WRONG IN THREE WAYS. HERE IS THE REPRODUCTION.

D-23.1-09 told the planner to reproduce before fixing. Done, 2026-09-03 at `047dcfe`:

**(a) It is not reproducible only in Cloud Build.** D-23.1-09's parenthetical says "no pytest
in the only local interpreter, which is 3.11.9 while the backend needs 3.12+". There IS a
Python **3.12.6** on this machine at
`C:\Users\ajimimo\AppData\Local\Programs\Python\Python312\python.exe`, the Docker daemon
(29.6.2) is running, and `pgvector/pgvector:pg16` + `testcontainers/ryuk:0.8.1` are already
pulled. A venv from that interpreter with `pip install -e "backend[dev]"` runs the entire
suite, integration included, in ~106 seconds.

**(b) There are FOUR failures, not three.**

    cd backend && $PY -m pytest tests -q
    -> 4 failed, 450 passed, 1 skipped in 106.03s
       FAILED tests/test_ci_guard_raw_db.py::test_guard_passes_on_clean_app_tree
       FAILED tests/test_mail_render.py::test_invite_carries_link
       FAILED tests/test_research_runs_migration.py::test_carries_grants
       FAILED tests/test_research_runs_migration.py::test_0012_no_server_default_on_new_columns

Non-integration = 155 tests; integration = 299.

**(c) One of the four is an APPLICATION defect, and it is already fixed elsewhere.**
`test_guard_passes_on_clean_app_tree` was red because `app/research/run_task.py:247` called
`get_engine()` outside the `app/db/` seam — a real D-03 violation. **Plan 23.1-07 fixes it**
by moving the helper into the seam. This plan therefore owns THREE tests, and all three are
TEST defects with diagnosed root causes:

| Test | Root cause, diagnosed |
|---|---|
| `test_invite_carries_link` | The jinja environment has `autoescape` ON (`app/mail/render.py:36-38`, the T-10-01 XSS guard). The test's URL contains `&` (`?mode=resetPassword&oobCode=XYZ123`), which renders as `&amp;`, so the raw-string `in html` assertion can never match. The template DOES emit the link (`templates/nl/invite.html.j2:11` `href="{{ cta_url }}"`). **The product is correct; the assertion is wrong.** |
| `test_carries_grants` | The test builds `f"GRANT ... ON {SCHEMA}.{TABLE} TO app_superadmin"` from its OWN constants and looks for that INTERPOLATED string in the migration's RAW SOURCE TEXT — where it exists only as an f-string with `{SCHEMA}.{_NEW_TABLE}` placeholders (`0011_research_runs.py:243`). It could never have matched. |
| `test_0012_no_server_default_on_new_columns` | `assert "server_default" not in src` over the whole file — and 0012's own docstring contains the words "with NO ``server_default``". A grep matching prose ABOUT the thing instead of the thing. |

**Do not trust that this list is complete either.** Re-run the suite in YOUR worktree first
and record what you actually see.

## HOW THESE THREE GET FIXED: BEHAVIOUR, NOT BETTER GREPS

Two of the three fail because they assert on raw source text. Do not repair them with a
cleverer regex — a comment-stripping grep is one docstring away from the same failure. Replace
them with assertions against the DATABASE after `alembic upgrade head`, which the local
testcontainer makes cheap:

- `test_carries_grants` -> query `information_schema.role_table_grants` for `app_superadmin`
  on `nestor.research_runs`; assert SELECT / INSERT / UPDATE / DELETE are all present.
- `test_0012_no_server_default_on_new_columns` -> query `information_schema.columns` for the
  three 0012 columns and assert `column_default IS NULL` for each.

Both become `@pytest.mark.integration` at the TEST level (the file has no module-level
`pytestmark`). That is fine: the point of this plan is that CI stops filtering on the marker.

## WHAT THIS PLAN MUST NOT DO

- **Do NOT disable jinja `autoescape` to make `test_invite_carries_link` pass.** It is the
  T-10-01 XSS guard. The assertion changes, not the renderer.
- **Do NOT add a `lint` step to `frontend/cloudbuild.yaml`.** See Task 3's measurement — it
  would land red. Record it as deferred instead.
- **Do NOT delete a failing test.** All three are asking a legitimate question badly.
- **Do NOT weaken `cloudbuild.test.yaml`'s Postgres setup**, its out-of-band `app_superadmin`
  role creation, or its `DATABASE_URL`. You remove a marker filter and add an assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

<!-- THE AUTHORITY. § 5's CI findings and D-23.1-09. -->
@.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/23.1-CONTEXT.md
@.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/23.1-07-SUMMARY.md
@.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/23.1-13-SUMMARY.md

<interfaces>
Verified at `047dcfe`.

`cloudbuild.test.yaml` — three steps: `start-postgres` (a `pgvector/pgvector:pg16` container
named `nestor-test-pg` on the `cloudbuild` network), `wait-for-postgres` (`pg_isready` poll),
and `pytest-integration` (`python:3.12-slim`, `uv pip install --system -e '.[dev]'`, then
`python -m pytest tests -m integration -v`) with
`DATABASE_URL=postgresql+pg8000://test:test@nestor-test-pg:5432/test`. `timeout: "1200s"`,
`options.logging: CLOUD_LOGGING_ONLY`. Its header explains that `conftest.py` honours an
explicit `DATABASE_URL` and bypasses testcontainers, and that the container's `test` superuser
can `CREATE ROLE` so `conftest._ensure_app_superadmin` provisions `app_superadmin` out of band.
All of that stays.

`frontend/cloudbuild.yaml` — one `build` step (`gcr.io/cloud-builders/docker`) forwarding four
`VITE_*` substitutions as `--build-arg`, plus `images:` and `timeout: "1200s"`. Its header
explains WHY it is a config rather than `--tag` (build-args cannot be passed to `--tag`), and
warns there is DELIBERATELY no Supabase substitution (D-08/D-09) with a Dockerfile bundle guard.
⚠ Repo memory: `_FB_API_KEY` literals are classifier-blocked for the agent — recover
substitution values from a prior build rather than typing them, and never inline a key.

THE COLLECTION-ASSERTION PATTERN to copy: `tribunal/cloudbuild.test-engine.yaml:520-560`. It
pins `EXPECTED_FILES` and fails the build when the collected count differs, with a comment
explaining that an empty collection is the WORST false green and that the number and the list
move together in ONE edit. Reproduce the SPIRIT (assert the count, fail loudly, explain why in
the config) — you do not need its explicit file list, because this gate runs a whole directory.

`backend/app/mail/render.py:34-38` — the jinja `Environment` with
`autoescape=select_autoescape(["html", "j2"])`.
`backend/app/mail/templates/nl/invite.html.j2:11` — `href="{{ cta_url }}"`.
`backend/app/db/alembic/versions/0011_research_runs.py:243` — the f-string GRANT.
`backend/app/db/alembic/versions/0012_research_run_chain_bundle.py` — its docstring contains
the literal word "server_default".
</interfaces>

<environment>
⭐ The backend suite RUNS LOCALLY. Python 3.12.6, Docker 29.6.2, images already pulled.

MEASURED FRONTEND BASELINE AT `047dcfe`, from `frontend/`:
  - `npx tsc --noEmit` -> clean, exit 0.
  - `npm test` -> 9 files, 140 tests, all passing, ~8s.
  - `npm run lint` -> 29,334 problems on THIS machine, of which **29,235 are
    `prettier/prettier` "Delete ␍"** — `git config core.autocrlf` is `true` and
    `git ls-files --eol` reports `i/lf w/crlf`, so they are a Windows-checkout artifact. On an
    LF checkout the real figure is **61 errors + 38 warnings** (54
    `@typescript-eslint/no-explicit-any`, 4 `no-empty`, 2 `prefer-const`, 1 genuine
    `prettier/prettier`).

Set up once per worktree, from the repo root:
    python312 -m venv .venv312
    .venv312/Scripts/python -m pip install -q -e "backend[dev]"
</environment>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the three red non-integration tests</name>

  <files>
    backend/tests/test_mail_render.py
    backend/tests/test_research_runs_migration.py
  </files>

  <read_first>
    - `backend/tests/test_mail_render.py` around `test_invite_carries_link`, and the other tests
      in that file — if any asserts on an un-escaped string it is red for the same reason and
      has simply not been hit yet.
    - `backend/app/mail/render.py:25-60` and `backend/app/mail/templates/nl/invite.html.j2`.
    - `backend/tests/test_research_runs_migration.py` in full — `_source()`, `_source_0012()`,
      `SCHEMA`, `TABLE`, `_CHAIN_BUNDLE_COLUMNS`, and every test reading raw source text.
      Several are green today only by luck; note the fragile ones in the SUMMARY rather than
      fixing them all here.
    - `backend/tests/conftest.py:157-230` — `_run_migrations` and `_ensure_app_superadmin`, so
      your database assertions run against the same schema build CI uses.
  </read_first>

  <behavior>
    - `test_invite_carries_link` asserts the HTML-ESCAPED form of the action link is present
      (`&` rendered as `&amp;`), AND asserts the raw un-escaped form is ABSENT. The second half
      is what keeps the test meaningful: it now PROVES autoescape is on — the T-10-01 guard —
      instead of accidentally proving it is off.
    - A new sibling test asserts a hostile `cta_url` containing `"><script>` is escaped in the
      output. That is the property the original test was reaching for.
    - `test_carries_grants` becomes an integration test: after `alembic upgrade head`,
      `information_schema.role_table_grants` shows `app_superadmin` holding SELECT, INSERT,
      UPDATE and DELETE on `nestor.research_runs`. Assert each of the four separately, so a
      partial grant names which one is missing.
    - A companion assertion keeps the other half of the original test alive: the env-guarded
      runtime-SA grant. Assert it behaviourally if that role exists in the test database, and
      SKIP with an explicit message if it does not — a skip that says why is honest; a
      silently-passing assertion is not.
    - `test_0012_no_server_default_on_new_columns` becomes an integration test: each of the
      three 0012 columns has `column_default IS NULL` in `information_schema.columns`.
    - All three, plus the new sibling, pass locally.
  </behavior>

  <action>
    FIRST, run `cd backend && $PY -m pytest tests -q` in YOUR worktree and record the exact
    failure list in the SUMMARY. Plan 23.1-07 should have taken it to three. If you see a
    different set, the diagnosis above does not apply to your tree — investigate before
    changing anything, and say what you found.

    Fix `test_invite_carries_link` per `<behavior>`, with a comment naming the root cause:
    autoescape is ON by design (T-10-01), the URL contains `&`, and the original assertion
    could only have passed with the XSS guard off. Add the hostile-`cta_url` sibling.

    Rewrite the two migration tests as behavioural integration tests, each marked
    `@pytest.mark.integration` at the test level. DELETE the raw-source assertions they replace
    — do not keep both, because a text assertion that "also passes" is a trap waiting for the
    next docstring edit. Comment each with why it asserts against the built schema rather than
    the migration's source text (a source grep matches prose about the thing).

    Do not touch the other tests in `test_research_runs_migration.py`.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; $PY -m pytest tests -q</automated>
  </verify>

  <done>
    `cd backend && $PY -m pytest tests -q` reports **0 failed**. Record the full summary line
    (passed / skipped / duration) in the SUMMARY — it is the number the widened CI gate must
    reproduce.
    `grep -n 'autoescape' backend/app/mail/render.py` still shows it enabled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen the backend gate, with a collection assertion</name>

  <files>
    cloudbuild.test.yaml
  </files>

  <read_first>
    - `cloudbuild.test.yaml` in full, header comment included.
    - `tribunal/cloudbuild.test-engine.yaml:462-560` — the `EXPECTED_FILES` assertion and the
      comment block on why an unasserted collection is the worst false green.
  </read_first>

  <action>
    Change the pytest invocation from `python -m pytest tests -m integration -v` to
    `python -m pytest tests -v` (no marker filter).

    Add a collection assertion BEFORE the run: count the test FILES pytest collects
    (`python -m pytest tests --collect-only -q`, count distinct `tests/test_*.py` paths),
    compare against an `EXPECTED_FILES` constant, and fail the build loudly when they differ.
    Derive the number by RUNNING the count — do not hand-count from `ls`. Comment it: this
    number and the test directory move together; a shortfall means the gate ran LESS than it
    claims; there is deliberately no early exit for an empty collection, because empty is the
    worst case.

    Rewrite the config's header. Today it says the gate runs the integration-marked denial
    suite only, and names that as its purpose. It must now say: this gate runs the WHOLE
    backend suite; the cross-tenant denial suites remain its most important content and are
    named; the marker filter was removed in phase 23.1 per D-23.1-09; and the non-integration
    tests that were red at the time were fixed first, in this same plan. Keep every existing
    sentence about Postgres, `DATABASE_URL`, `_ensure_app_superadmin` and the Cloud Build
    trigger deferral.

    Check one hazard before finishing: `backend/tests/test_ci_guard_raw_db.py` shells out to
    `bash` to run `scripts/ci_no_raw_db_access.sh`, and the test step's image is
    `python:3.12-slim`. Confirm `bash` and the script's dependencies exist there — if they do
    not, this test passes locally and fails only in Cloud Build, which is the most likely way
    this widening lands red for a reason unrelated to the code. Either verify it
    (`docker run --rm python:3.12-slim bash -c 'echo ok'`) or add the necessary install to the
    step, and record which you did.

    Runtime: the suite is ~106s locally; `timeout: "1200s"` is unchanged and adequate. Say so
    rather than raising it speculatively.
  </action>

  <verify>
    <automated>bash -c 'grep -q -- "-m integration" cloudbuild.test.yaml && { echo STILL_FILTERED; exit 1; }; grep -q EXPECTED_FILES cloudbuild.test.yaml || { echo NO_COLLECTION_ASSERTION; exit 1; }; echo WIDENED_OK' &amp;&amp; docker run --rm python:3.12-slim bash -c 'echo bash_ok'</automated>
  </verify>

  <done>
    `cloudbuild.test.yaml` no longer contains `-m integration`, contains an `EXPECTED_FILES`
    assertion whose value was derived by running the count, and its header describes what it
    now does. The `bash`-in-`python:3.12-slim` check was performed and its result recorded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tsc and vitest to the frontend build, and defer lint with the numbers</name>

  <files>
    frontend/cloudbuild.yaml
  </files>

  <read_first>
    - `frontend/cloudbuild.yaml` in full, header included.
    - `frontend/package.json` — `test` is `vitest run`; there is no `typecheck` script, so the
      step calls `npx tsc --noEmit` directly.
    - `frontend/vitest.config.ts` — `environment: "node"`, `include: ["src/**/*.test.ts"]`.
  </read_first>

  <action>
    Add two steps BEFORE the existing `build` step, both on `node:22-slim` (the local toolchain
    is Node v22.14.0, so the CI image must not be older):

      - `id: install-and-typecheck` — `npm ci` then `npx tsc --noEmit`
      - `id: vitest` — `npm test`

    Use `npm ci`, never `npm install`: the lockfile IS committed in this repo and `npm install`
    would drift it. Whether the two steps share a `node_modules` depends on Cloud Build's
    `/workspace` persistence — if the second step cannot see the first's `node_modules`, run
    `npm ci` in both rather than guessing. A step that silently reinstalls is fine; a step that
    silently skips is not.

    Confirm the `build` step runs after them (Cloud Build steps are sequential by default —
    check there is no `waitFor` in this config that changes it).

    **DO NOT add a lint step.** Record the reason in the config header with the measured
    numbers, so the next reader does not re-derive them: `npm run lint` is RED at HEAD; on an
    LF checkout it reports 61 errors and 38 warnings (54 `@typescript-eslint/no-explicit-any`,
    4 `no-empty`, 2 `prefer-const`, 1 `prettier/prettier`); on a Windows working tree the
    figure balloons to ~29,300 because `core.autocrlf` is `true` and `prettier/prettier` flags
    every CRLF. Adding lint as a blocking gate would land it red, which D-23.1-09 forbids by
    the same logic that put this whole plan last. It needs its own cleanup phase.

    Add DEF-23.1-02 to `deferred-items.md` with the same numbers, naming the two distinct
    problems: the 61 real errors, and the CRLF divergence between the committed LF blobs and a
    Windows working tree (which also means `gcloud builds submit` from THIS machine uploads
    CRLF sources).

    Leave the `VITE_*` substitutions, the `images:` block, the Supabase-guard comment and the
    timeout untouched.
  </action>

  <verify>
    <automated>bash -c 'grep -q "tsc --noEmit" frontend/cloudbuild.yaml &amp;&amp; grep -q "npm ci" frontend/cloudbuild.yaml &amp;&amp; grep -qE "npm test|vitest" frontend/cloudbuild.yaml &amp;&amp; ! grep -q "run lint" frontend/cloudbuild.yaml &amp;&amp; echo FRONTEND_CI_OK' &amp;&amp; cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm test</automated>
  </verify>

  <done>
    `frontend/cloudbuild.yaml` has `npm ci` + `npx tsc --noEmit` + `npm test` steps ahead of
    `build`, and NO lint step. tsc is clean and `npm test` is 140 passed locally. The header
    carries the measured lint numbers and the reason lint is absent. `deferred-items.md`
    carries DEF-23.1-02.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator — execute both widened configs once</name>

  <what-built>
    Two Cloud Build configs were widened but NEVER EXECUTED. `gcloud builds submit` is
    classifier-blocked for the agent, so a config that has only ever been read is exactly the
    "green gate that says nothing" this project has shipped before. This checkpoint exists so
    the phase does not close on an unexercised gate.

    Changed:
      - `cloudbuild.test.yaml` — now runs the WHOLE backend suite (no `-m integration`) plus a
        collected-file-count assertion.
      - `frontend/cloudbuild.yaml` — now runs `npm ci`, `npx tsc --noEmit` and `npm test`
        before building the image.

    Locally at the end of this plan: `cd backend && $PY -m pytest tests -q` reports 0 failed
    (the exact summary line is in the SUMMARY); `cd frontend && npx tsc --noEmit` is clean and
    `npm test` is 140 passed.
  </what-built>

  <action>
    The agent does NOT run these commands — `gcloud builds submit` is classifier-blocked here.
    Present this checkpoint to the operator verbatim, wait, and then RECORD what comes back:
    both build ids, both statuses as read from `gcloud builds describe`, and the backend log's
    pytest summary line. Write those into the SUMMARY as observations, never as expectations.
    If either build FAILED, do not close the plan — diagnose from the reported log and fix the
    config, then re-present this checkpoint.
  </action>

  <how-to-verify>
    Pin the account and project on EVERY command — the gcloud config on this machine drifts
    mid-session and there are four accounts on it:

        gcloud config list account project
        # must be tools@dotto.be / project-cb01b861-cb4a-438d-b9a

    1. Run the widened backend gate from the repo root:

           gcloud builds submit . --config=cloudbuild.test.yaml \
             --account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a

       Your terminal times out around 2 minutes while the build KEEPS GOING. Note the build id
       and poll it — NEVER resubmit:

           gcloud builds describe BUILD_ID --account=tools@dotto.be \
             --project=project-cb01b861-cb4a-438d-b9a --format='value(status)'

       Read the STATUS from `builds describe`, never from a shell exit code:
       `builds submit | tail` returns the PIPE's status, so a FAILED build reports exit 0.

       EXPECTED: SUCCESS, and in the log a pytest summary with 0 failed and a passed count
       matching the local number recorded in this plan's SUMMARY. A different passed count is
       not automatically a failure, but it must be EXPLAINED, not merely noted. The
       collected-file-count assertion must print its OK line; if it fails, the gate ran less
       than it claims and that is a red build even if pytest was green.

    2. Run the frontend build. Recover the four `_*` substitution values from a PREVIOUS
       successful frontend build rather than typing them (never inline a Firebase key):

           gcloud builds list --limit=10 --account=tools@dotto.be \
             --project=project-cb01b861-cb4a-438d-b9a
           gcloud builds describe PREVIOUS_BUILD_ID --format='value(substitutions)' \
             --account=tools@dotto.be --project=project-cb01b861-cb4a-438d-b9a

       Then submit `gcloud builds submit frontend --config=frontend/cloudbuild.yaml
       --substitutions=<recovered>` with the same pins, and poll by id.

       EXPECTED: SUCCESS, with `install-and-typecheck` and `vitest` both green BEFORE `build`
       runs. If tsc or vitest fails in the container while passing locally, that is a real
       finding about the CI image — report it, do not delete the step.

       This machine's working tree has CRLF line endings and `gcloud builds submit` uploads the
       WORKING TREE. tsc and vitest do not care about line endings, so this should not bite —
       but if you see a bizarre parse error, check that first. It is also why no lint step was
       added (DEF-23.1-02).

    3. This is a TEST build only. It deploys nothing. No `gcloud run deploy` is part of this
       checkpoint.
  </how-to-verify>

  <resume-signal>
    Reply with the two build ids and their statuses, plus the backend pytest summary line from
    the log. Type "approved" to close, or describe what failed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge -> main branch | CI is the only automated thing between a change and a deploy |
| CI config -> what it actually runs | A config collecting fewer files than it claims is a false green |
| local working tree -> Cloud Build source | `builds submit` uploads the working tree, CRLF and all |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.1-60 | Tampering | 155 non-integration tests never running in CI, so a regression in them ships | mitigate | The marker filter is removed; the whole suite runs; the previously-red tests are fixed first |
| T-23.1-61 | Repudiation | a mistyped path or an empty collection reading as a green gate | mitigate | The `EXPECTED_FILES` collected-file assertion, derived by running the count, with no early exit for empty |
| T-23.1-62 | Tampering | frontend type or unit regressions reaching an image | mitigate | `tsc --noEmit` and `vitest` steps ahead of the image build |
| T-23.1-63 | Denial of Service (self-inflicted) | a lint step landing red and blocking every merge | mitigate | Lint deliberately NOT added; measured numbers and the CRLF cause recorded in the config header and DEF-23.1-02 |
| T-23.1-64 | Repudiation | closing the phase on a CI config that was never executed | mitigate | The blocking human-verify checkpoint; the phase does not close until two real build ids come back SUCCESS |
| T-23.1-65 | Tampering | `test_ci_guard_raw_db.py` failing in CI only because `python:3.12-slim` lacks `bash` | mitigate | Task 2 verifies `bash` in that image explicitly and records the result |
| T-23.1-SC | Tampering | npm/pip/cargo installs | mitigate | The configs run `npm ci` and `uv pip install -e '.[dev]'`, both resolving ONLY the repo's committed lockfile / pinned dependency set. This plan adds no new package to either manifest, so no `[ASSUMED]`/`[SUS]` entry arises and no legitimacy checkpoint is required. If you find yourself adding a dependency, stop |
</threat_model>

<verification>
1. `cd backend && $PY -m pytest tests -q` — **0 failed**. Record the full summary line.
2. `cd frontend && npx tsc --noEmit && npm test` — clean, 140 passed.
3. `grep -c -- "-m integration" cloudbuild.test.yaml` — 0.
4. `grep -c EXPECTED_FILES cloudbuild.test.yaml` — at least 1.
5. `grep -c "run lint" frontend/cloudbuild.yaml` — 0.
6. TWO real Cloud Build ids reported SUCCESS by `gcloud builds describe`, with the backend
   log's pytest summary showing 0 failed and the collection assertion printing its OK line.
7. `deferred-items.md` carries DEF-23.1-02 with the measured lint figures.
</verification>

<success_criteria>
- The whole backend suite is green locally with zero failures, and the three fixes are
  behavioural rather than better greps.
- `cloudbuild.test.yaml` runs every test and asserts how many files it collected.
- `frontend/cloudbuild.yaml` typechecks and unit-tests before it builds.
- No lint gate was added, and the reason is recorded with real numbers in two places.
- Both configs have been executed once in Cloud Build and observed SUCCESS, with ids recorded.
</success_criteria>

<output>
Create `.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/23.1-14-SUMMARY.md` when done.
</output>
