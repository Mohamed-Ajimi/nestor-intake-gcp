---
phase: 23.1-platform-hardening-authorization-boundary-space-deactivation
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - tribunal/infrastructure/cloud-run/DEPLOY.md
  - CLAUDE.md
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "A terraform state file created in this working tree cannot be committed by accident."
    - "DEPLOY.md's authentication claim matches what the deploy scripts actually pass."
    - "CLAUDE.md no longer tells a reader that the flow stops at decomposed while the research router is mounted and Tribunal is live."
    - "CLAUDE.md still forbids the thing that is actually forbidden — the legacy Supabase run-research edge function."
  artifacts:
    - path: ".gitignore"
      provides: "tfstate exclusion"
      contains: "tfstate"
  key_links:
    - from: "CLAUDE.md"
      to: "backend/app/main.py:152"
      via: "the corrected scope statement naming the mounted research router"
      pattern: "research"
---

<objective>
Three documentation and repo-hygiene corrections from `23.1-CONTEXT.md` § 5 and D-23.1-10.
None changes behaviour; all three are places where the repository states something that is not
true, and two of them are the kind of untruth that gets acted on.

Purpose: D-23.1-10 is the important one. CLAUDE.md tells every agent and every reader that the
flow stops at `decomposed`, while `main.py:152` mounts the research router and Tribunal is
LIVE and deployed. **Reality wins — update CLAUDE.md.** Removing the research router is
explicitly forbidden. An agent that believes the doc could "helpfully" unmount a live paid
subsystem, and every static gate would stay green while it did.

Output: `*.tfstate*` in `.gitignore`; DEPLOY.md's authentication line corrected against the
script; CLAUDE.md's scope ceiling rewritten to describe the system that exists.

## ⚠ PLANNING CORRECTION — CONTEXT NAMED THE WRONG PAIR OF FILES FOR THE DEPLOY.md DRIFT

`23.1-CONTEXT.md` § 5 says: "`tribunal/infrastructure/cloud-run/DEPLOY.md:88` says
`--allow-unauthenticated`; `deploy-worker.sh:175` says `--no-allow-unauthenticated`."

Verified at `047dcfe`, the drift is real but the counterpart is a different file. `DEPLOY.md:88`
sits inside **"### Step 3: Deploy the API service"** and describes `nestor-pulse-api`. Its
actual counterpart is **`deploy-api.sh:157`, which passes `--no-allow-unauthenticated`.**
`deploy-worker.sh:175` also passes `--no-allow-unauthenticated`, and DEPLOY.md's Step 4 (the
worker, `:108-118`) makes NO authentication claim at all — so it is not in conflict with
anything.

**The correction to make is DEPLOY.md:88 against `deploy-api.sh:157`.** Verify both lines
yourself before editing; if your tree differs from these line numbers, trust the tree and
record what you found.

## WHAT THIS PLAN MUST NOT DO

- **Do NOT remove or unmount the research router.** D-23.1-10 forbids it by name. This plan
  changes prose only. `backend/app/main.py` must not appear in any diff.
- **Do NOT create the tfstate bucket or run `terraform init -migrate-state`.** That is
  operator item 1 in CONTEXT § 8 and is collected by plan 23.1-15. This plan adds a gitignore
  line and nothing else in `infra/`.
- **Do NOT uncomment the `backend "gcs"` block in `infra/providers.tf:26`.** `terraform init`
  would fail against a not-yet-created bucket; the comment there says so.
- **Do NOT change any deploy script.** DEPLOY.md is wrong; the scripts are right.
- **Do NOT edit the `## Developer Profile` or `## Project Skills` sections of CLAUDE.md** —
  they are tool-managed and say so.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

<!-- THE AUTHORITY. § 5's hygiene list, § 8's operator-only list, D-23.1-10. -->
@.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/23.1-CONTEXT.md

<interfaces>
Verified at `047dcfe`.

`.gitignore` — has sections for deps/builds, python bytecode, env & secrets, os/editor, logs,
phase-deploy scratch pointers, and `.planning/`. There is NO terraform section. CONTEXT § 5
confirms **no state file exists in the tree and none was ever committed**
(`git log --all --diff-filter=A` over `*.tfstate` is empty), so this is a latent gap, not a
live exposure — say that in the SUMMARY rather than implying a leak was closed.

`infra/providers.tf:18-33` — the `backend "gcs"` block, shipped commented out, with the
`gsutil mb` / `versioning set on` runbook lines above it.

`tribunal/infrastructure/cloud-run/DEPLOY.md`:
  - `:16-21` the Services table (`nestor-pulse-api`, `nestor-pulse-worker`)
  - `:82-106` Step 3, the API service; `:88` carries the false claim
  - `:108-118` Step 4, the worker; makes no auth claim
`tribunal/infrastructure/cloud-run/deploy-api.sh:157` — `--no-allow-unauthenticated`
`tribunal/infrastructure/cloud-run/deploy-worker.sh:175` — `--no-allow-unauthenticated`

CLAUDE.md, the four places that carry the stale ceiling:
  - `:13` "…this flow stops at status `decomposed`."
  - `:16` the Core Value sentence, which ends at "a validated, decomposed context pack"
  - `:25` "**Scope ceiling**: Flow ends at `decomposed`. `run-research` must never be invoked
    from the new frontend/backend credentials."
  - `:281` "The re-platform scope ends at `decomposed`; `in_research` onward involves
    run-research (Tribunal, out of scope)"

  ⭐ `:25`'s SECOND sentence is still TRUE and must survive: `run-research` there is the LEGACY
  SUPABASE EDGE FUNCTION (see `:83-85`, where `SERPAPI_API_KEY` / `SEARCHAPI_API_KEY` /
  `APIFY_API_TOKEN` are all listed as `run-research` config). `backend/tests/test_no_run_research_route.py`
  and `backend/tests/test_scope_guard_run_research.py` both still enforce it. Do NOT delete
  that prohibition while fixing the ceiling around it — they are two different statements that
  happen to sit in one bullet.

`backend/app/main.py:152` — `protected_router.include_router(research_router)`, with the
comment describing the Phase-16 trigger verb and the SSE progress stream. This is the reality
CLAUDE.md must be reconciled to.

`.planning/STATE.md` — records Tribunal deployed and live (`tribunal-api-00023-bc6` /
`tribunal-worker-00009-fkm`, tag `20260901-134253`). Use it for the factual claim, and do not
invent a revision name.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: gitignore terraform state, and correct DEPLOY.md against the script</name>

  <files>
    .gitignore
    tribunal/infrastructure/cloud-run/DEPLOY.md
  </files>

  <read_first>
    - `.gitignore` in full — match its section-comment style exactly (a short lowercase
      heading, then patterns).
    - `infra/providers.tf:18-33` — so the gitignore comment can point at the real migration
      path rather than inventing one.
    - `tribunal/infrastructure/cloud-run/DEPLOY.md:82-118` — Steps 3 and 4.
    - `tribunal/infrastructure/cloud-run/deploy-api.sh:150-165` — the actual flag.
  </read_first>

  <action>
    Add a `# terraform state (NEVER commit — carries resource ids and can carry secrets)`
    section to `.gitignore` with `*.tfstate`, `*.tfstate.*` and `.terraform/`. Note in the
    section comment that state is intended to move to a GCS backend
    (`infra/providers.tf`'s commented `backend "gcs"` block) and that this ignore is the guard
    for the window before that happens — the bucket creation and
    `terraform init -migrate-state` are an operator step, not this plan's.

    Verify the rule actually bites rather than trusting it:
        touch infra/terraform.tfstate && git status --porcelain infra/ && rm infra/terraform.tfstate
    `git status` must show NOTHING for that path. Paste the observed output into the SUMMARY.
    A gitignore line that does not match — because of a leading slash, a directory prefix, or
    an earlier negation — reads exactly like one that does.

    Fix `DEPLOY.md:88` to say `--no-allow-unauthenticated`, matching `deploy-api.sh:157`, and
    rewrite the parenthetical so it is coherent: JWT protection is in FastAPI, and Cloud Run
    itself does NOT allow unauthenticated invocations. Read the surrounding sentence about
    health probes and make sure the corrected text does not leave a claim that only made sense
    under the old flag.

    While you are in that file, check Step 4 (`:108-118`) and the Services table (`:16-21`)
    for any other authentication claim, and reconcile anything you find against
    `deploy-worker.sh`. Report what you checked, including "nothing else was wrong" if that is
    the finding.
  </action>

  <verify>
    <automated>bash -c 'touch infra/terraform.tfstate; out=$(git status --porcelain infra/); rm -f infra/terraform.tfstate; test -z "$out" &amp;&amp; echo IGNORED_OK || { echo "NOT IGNORED: $out"; exit 1; }' &amp;&amp; grep -n "no-allow-unauthenticated" tribunal/infrastructure/cloud-run/DEPLOY.md</automated>
  </verify>

  <done>
    The touch/status/rm probe prints `IGNORED_OK` and its output is recorded in the SUMMARY.
    `grep -n "allow-unauthenticated" tribunal/infrastructure/cloud-run/DEPLOY.md` shows no
    remaining bare `--allow-unauthenticated` claim.
    `git diff --stat tribunal/infrastructure/cloud-run/deploy-api.sh tribunal/infrastructure/cloud-run/deploy-worker.sh`
    is empty — the docs were wrong, not the scripts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile CLAUDE.md's scope ceiling with the deployed system (D-23.1-10)</name>

  <files>
    CLAUDE.md
  </files>

  <read_first>
    - `CLAUDE.md:1-30` — the Project section, the Core Value line, and the Constraints list.
    - `CLAUDE.md:270-290` — the Architectural Constraints section containing `:281`.
    - `CLAUDE.md:80-90` — the `run-research` env-var lines that establish it as the LEGACY
      Supabase edge function.
    - `backend/app/main.py:130-160` — every router mount, and the research router's comment.
    - `backend/tests/test_no_run_research_route.py` and
      `backend/tests/test_scope_guard_run_research.py` — the two guards that keep the legacy
      prohibition true. Read them so your rewrite does not contradict a test.
    - `.planning/STATE.md` — for the deployed Tribunal revisions, if you cite them.
  </read_first>

  <action>
    Rewrite the four stale statements so they describe the system that exists, keeping every
    claim that is still true.

    At `:13` and `:16`: the pre-research intake flow (form -> AI skills -> validated context
    pack) is what THIS re-platform was scoped to, and it ends at `decomposed`; deep research
    (Tribunal) is a separate track that has since been built, deployed and is live. Say both,
    and say which is which — the original sentence was correct about the intake scope and only
    wrong in implying nothing exists past it.

    At `:25`, SPLIT the bullet into two, because it currently welds a stale claim to a live
    one:
      - the intake re-platform's own scope ended at `decomposed`; the research trigger, the
        SSE progress stream and the Tribunal seam were added later and `backend/app/main.py`
        mounts `research_router` under `protected_router`. **The research router must not be
        removed or unmounted (D-23.1-10).**
      - the LEGACY Supabase `run-research` edge function must never be invoked from the new
        frontend or backend credentials — still true, still guarded by
        `backend/tests/test_no_run_research_route.py` and
        `backend/tests/test_scope_guard_run_research.py`. Name both tests so the next reader
        can see it is enforced and not merely asserted.

    At `:281`: replace "out of scope" with a pointer to where the research surface actually
    lives (`backend/app/api/research_routes.py`, mounted at `main.py:152`) and to the Tribunal
    engine under `tribunal/nestor_pulse_sdk/`.

    Add ONE dated line recording the correction and its authority, e.g.
    "Corrected 2026-09-03 (phase 23.1, D-23.1-10): the `decomposed` ceiling described the
    intake re-platform only; Tribunal is live." — so a future reader can tell this is a
    deliberate reconciliation and not drift.

    Change nothing else in CLAUDE.md. In particular do not touch `## Developer Profile`,
    `## Project Skills`, or the `## GSD Workflow Enforcement` section.
  </action>

  <verify>
    <automated>bash -c 'grep -n "run-research" CLAUDE.md | grep -qi "legacy\|Supabase" &amp;&amp; grep -qi "must not be removed\|must not be unmounted" CLAUDE.md &amp;&amp; echo CLAUDE_MD_OK'</automated>
  </verify>

  <done>
    `grep -n "decomposed" CLAUDE.md` — every remaining occurrence is qualified as the INTAKE
    flow's ceiling, never as the system's. Read each hit; do not count them.
    The legacy `run-research` prohibition survives and now names the two guarding tests.
    CLAUDE.md contains an explicit "must not be removed / unmounted" sentence about the
    research router.
    `git diff --stat backend/app/main.py` is empty.
    `git diff CLAUDE.md` touches no line inside `## Developer Profile` or `## Project Skills`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> git history | A tfstate file committed once is in history forever |
| documentation -> agent behaviour | An agent reads CLAUDE.md as instruction; a false constraint there is an instruction to break something live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.1-34 | Information Disclosure | terraform state committed, carrying resource ids and possibly the `app_superadmin` password | mitigate | `*.tfstate*` + `.terraform/` ignored, with the rule PROVEN by a touch/status/rm probe rather than assumed |
| T-23.1-35 | Denial of Service (self-inflicted) | an agent unmounting the live research router because CLAUDE.md said the flow stops at `decomposed` | mitigate | D-23.1-10 reconciliation, with an explicit "must not be removed or unmounted" sentence |
| T-23.1-36 | Elevation of Privilege | the legacy Supabase `run-research` prohibition being deleted along with the stale ceiling | mitigate | The bullet is SPLIT rather than rewritten; the surviving half names its two guarding tests; the verify command greps for it |
| T-23.1-37 | Spoofing | DEPLOY.md's false `--allow-unauthenticated` claim being copied into a hand-typed deploy | mitigate | Corrected against `deploy-api.sh:157`; the scripts are asserted unchanged |
| T-23.1-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NOTHING; it edits three text files |
</threat_model>

<verification>
1. The touch/status/rm probe prints `IGNORED_OK`.
2. `grep -n "allow-unauthenticated" tribunal/infrastructure/cloud-run/DEPLOY.md` — no bare
   `--allow-unauthenticated` remains, and the file agrees with both deploy scripts.
3. `git diff --stat backend/app/main.py tribunal/infrastructure/cloud-run/deploy-api.sh
   tribunal/infrastructure/cloud-run/deploy-worker.sh infra/providers.tf` is EMPTY.
4. `cd backend && $PY -m pytest tests/test_no_run_research_route.py tests/test_scope_guard_run_research.py -v`
   — still green, proving the surviving prohibition is still enforced by code and not only by
   prose.
</verification>

<success_criteria>
- `*.tfstate`, `*.tfstate.*` and `.terraform/` are ignored, proven by an observed probe.
- DEPLOY.md's API authentication claim matches `deploy-api.sh`; no deploy script changed.
- CLAUDE.md describes a live Tribunal, forbids unmounting the research router, and keeps the
  legacy Supabase `run-research` prohibition with its two guarding tests named.
- No application code changed by this plan.
</success_criteria>

<output>
Create `.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/23.1-09-SUMMARY.md` when done.
</output>
