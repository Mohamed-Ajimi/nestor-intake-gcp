---
phase: 23.1-platform-hardening-authorization-boundary-space-deactivation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/admin_repo.py
  - backend/app/api/admin_routes.py
  - backend/tests/test_space_deactivation_cascade.py
  # Task 3 (D-23.1-13). `app/auth/session.py` is untouched by every other plan in
  # the phase — and it is NOT `app/db/session.py`, which plan 23.1-07 owns. Same
  # basename, different file, no conflict.
  - backend/app/auth/session.py
  - backend/tests/test_auth_session_membership_status.py
autonomous: true
requirements: [SEC-01, SEC-02]

must_haves:
  truths:
    - "Deactivating a space disables every one of its members in the IdP and revokes their refresh tokens, so an in-flight session stops working on the next request."
    - "Deactivating a space marks each cascaded membership with a status that is distinguishable from an individually deactivated one."
    - "The cascade's DB flip is not the only thing standing between a cascaded member and a working session: D-23.1-13 closes the login-sync no-list so a member whose IdP disable failed is still refused."
    - "Login-sync refuses a non-active membership on BOTH arms of _find_membership, and a deactivated member is indistinguishable from a user with no row at all."
    - "Reactivating a space restores ONLY the members the cascade took down — a member who was individually deactivated before the space was stays deactivated."
    - "Deactivating a space can never disable the acting superadmin, and can never drop the count of active superadmins to zero."
    - "Re-issuing deactivate on an already-deactivated space is safe and re-attempts any member the IdP call previously failed for."
  artifacts:
    - path: "backend/app/db/admin_repo.py"
      provides: "Space-scoped membership listing + the cascade status vocabulary"
      exports: ["AdminRepo.list_memberships_for_space", "AdminRepo.set_membership_status"]
    - path: "backend/tests/test_space_deactivation_cascade.py"
      provides: "Behavioural proof of the cascade, the selective inverse, and both refusal guards"
    - path: "backend/app/auth/session.py"
      provides: "Active-status predicate on both arms of _find_membership (D-23.1-13)"
      contains: "_STATUS_ACTIVE"
    - path: "backend/tests/test_auth_session_membership_status.py"
      provides: "Per-arm refusal proof, the no-row contract, and the partial-cascade denial"
  key_links:
    - from: "backend/app/api/admin_routes.py"
      to: "backend/app/auth/admin_users.py"
      via: "deactivate_user(provider_user_id) per member of the space"
      pattern: "admin_users\\.deactivate_user"
    - from: "backend/app/api/admin_routes.py"
      to: "backend/app/db/admin_repo.py"
      via: "list_memberships_for_space + set_membership_status inside the request tx"
      pattern: "list_memberships_for_space"
    - from: "backend/app/auth/session.py"
      to: "organization_memberships.status"
      via: "an == active predicate in EACH of the two select() statements"
      pattern: "OrganizationMembership\.status"
---

<objective>
Make space deactivation actually deactivate. `23.1-CONTEXT.md` § 2: `admin_routes.py:511`
writes `organizations.status='deactivated'` and audits, and a grep for any read of that column
in an auth path returns nothing — members keep working. This closes SEC-02 by reusing the
per-USER path at `admin_routes.py:246`, which is correct and proven, rather than adding an
org-status lookup to `get_current_identity` (D-23.1-03 rejects that: `dependencies.py`
deliberately has no DB call per D-06, and a cascade costs zero per request).

Purpose: a space marked "deactivated" that grants full access is worse than no feature — it
tells the operator the tenant is off while the tenant is on.

Output: a cascading `deactivate_space`, a selective `reactivate_space` inverse, two refusal
guards carried over from the per-user path, and a behavioural test suite.

## PLANNING RULING #1 — THE INVERSE MUST BE SELECTIVE, AND THAT NEEDS A THIRD STATUS VALUE

`23.1-CONTEXT.md` D-23.1-03 says "Reactivate gets the inverse" and stops there. A naive
inverse — set every membership in the space to `active` — silently un-fires a member who was
individually deactivated BEFORE the space went down. Deactivate-then-reactivate would become
an undocumented way to restore revoked access.

The fix needs no migration. `models/membership.py:43` declares the status set as app-level
`{"active","deactivated"}`, a plain `String` with NO PG enum and NO CHECK constraint, and
0006 confirms it. VERIFIED at `047dcfe`: the five membership-status reads in `backend/app/`
that carry LOGIC are all ALLOW-LISTS (`== "active"`) — `admin_repo.py:113`,
`admin_repo.py:166`, `admin_routes.py:236`, `intake_routes.py:748`, `me_routes.py:123`.
There is NOT ONE deny-list comparison (`!= "deactivated"`). So a third value is inert
everywhere it is not explicitly handled, which is the safe direction.

⚠ **CORRECTED 2026-09-03 — THAT SURVEY WAS INCOMPLETE, AND THE MISSING SITE IS THE DANGEROUS
ONE.** `23.1-CONTEXT.md` D-23.1-11 now records a SIXTH membership-status site:
`_find_membership` (`auth/session.py:92-113`), which is not a deny-list gap — it is a
**no-list**, no status predicate at all, which is strictly worse. Login-sync therefore mints
claims for a `space_deactivated` member, and this plan's cascade is what makes that
load-bearing: a partial IdP failure commits the DB flip while those members keep
authenticating. **D-23.1-13 closes it. Do not restate "five sites, no deny-list anywhere" as
if it were the whole picture** — say five allow-list sites plus one no-list site closed by
D-23.1-13. (`admin_routes.py:122` also reads `status`, but only to surface it in a view; it
carries no logic and is not a seventh site.)

**Ruling: the cascade writes `status="space_deactivated"`. The inverse restores EXACTLY the
rows carrying that value and leaves `"deactivated"` rows alone.**

## PLANNING RULING #2 — THE IdP IS NOT IN THE TRANSACTION, SO THE VERB MUST BE RETRIABLE

`admin_users.deactivate_user` (`auth/admin_users.py:115`) is a Firebase Admin SDK call:
`update_user(disabled=True)` + `revoke_refresh_tokens(uid)`. It cannot join the request's DB
transaction. A partial cascade — some members disabled, then a rollback — leaves the IdP and
the DB disagreeing, and a member the IdP call FAILED for keeps working while the UI says the
space is off.

**Ruling: DB first, then IdP, then report honestly.**
1. Inside the request transaction: flip the space status and every `active` membership in it
   to `space_deactivated`, and write the audit row. Commit.
2. Then call `admin_users.deactivate_user` for each affected `provider_user_id`, collecting
   failures instead of raising on the first one.
3. If every call succeeded -> 200 with the space view.
   If any failed -> **502** whose detail names how many members are still enabled in the IdP,
   WITHOUT listing emails or uids in the response body (T-06-09: no identifiers in an error
   body). The uids go to the audit `metadata` and the log, not to the browser.
4. Because step 1 is idempotent and step 2 is idempotent, **re-issuing
   `POST /spaces/{id}/deactivate` retries the failed IdP calls.** The handler must therefore
   collect members whose status is `active` OR already `space_deactivated`, so a retry still
   has someone to disable. State this in the docstring — it is the whole reason the verb is
   safe to press twice.

## WHAT THIS PLAN MUST NOT DO

- **Do NOT add an org-status read to `app/auth/dependencies.py`.** D-23.1-03 rejects it by
  name. `dependencies.py` has no DB call by design (D-06) and already pays one `get_user`
  round-trip (D-04).
- **Do NOT hard-delete anything.** `admin_repo.py`'s docstring records "NO delete method
  exists (D-10)". The cascade is status flips and IdP disables only.
- **Do NOT touch `update_space`.** Its docstring states `status` is NEVER routed through it so
  a benign PATCH can never soft-delete a space. That stays true.
- **Do NOT change `deactivate_user` / `reactivate_user` in `admin_users.py`.** Reuse them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

<!-- THE AUTHORITY. § 2 and D-23.1-03. -->
@.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/23.1-CONTEXT.md

<interfaces>
Extracted at planning time. Verified at `047dcfe`.

`backend/app/api/admin_routes.py:203-258` — THE PATTERN TO REUSE, the per-user deactivate:
  - refuses 409 `"Cannot deactivate yourself"` when `membership.provider_user_id == identity.uid`
  - refuses 409 `"Cannot deactivate the last active superadmin"` when the target is an active
    superadmin and `repo.count_active_superadmins() <= 1`
  - `admin_users.deactivate_user(membership.provider_user_id)` then
    `repo.set_membership_status(membership_id, "deactivated")` then `audit.log(repo.session, ...)`
  - `audit.log` takes `repo.session` so the audit row rides the SAME transaction (T-5-16).

`backend/app/api/admin_routes.py:264-292` — `reactivate_user`: `admin_users.reactivate_user`
(un-disable only; claims are NOT re-issued — login-sync is idempotent, A3) then
`set_membership_status(..., "active")` then a `user.reactivated` audit row.

`backend/app/api/admin_routes.py:505-542` — `deactivate_space` / `reactivate_space` as they
stand: one `repo.set_space_status(...)` + `audit.log(...)`, rowcount 0 -> 404.

`backend/app/db/admin_repo.py`:
  - `_STATUS_ACTIVE = "active"` (:48), `_STATUS_DEACTIVATED = "deactivated"` (:49)
  - `list_users()` (:91) returns EVERY membership cross-space — do NOT filter it in Python at
    the route; add a scoped method instead.
  - `set_membership_status(membership_id, status)` (:142) returns rowcount.
  - `count_active_superadmins()` (:155) counts `role == "superadmin" AND status == "active"`.
  - `set_space_status(space_id, status)` (:215) returns rowcount.
  - Every method runs on the `app_superadmin` engine (0003 bypass policy), so no `space_id`
    filter is applied automatically — your new method must filter explicitly.

`backend/app/auth/admin_users.py:115` `deactivate_user(uid)` = `update_user(disabled=True)` +
`revoke_refresh_tokens(uid)`. `:128` `reactivate_user(uid)` = `update_user(disabled=False)`.
`backend/app/auth/dependencies.py:78` passes `check_revoked=True`, which is what makes the
revoke bite on the next request.

`backend/app/db/models/membership.py:41-47` — `role` and `status` are plain `String` columns
with server defaults and NO enum/CHECK. `provider_user_id` is NULLABLE (a membership created
before the IdP account exists), so the cascade must skip `None` uids rather than crash.
</interfaces>

<environment>
⭐ PLANNING FINDING — the backend suite RUNS LOCALLY, contradicting `23.1-CONTEXT.md`
D-23.1-09's parenthetical. Verified 2026-09-03 at `047dcfe`: Python 3.12.6 at
`C:\Users\ajimimo\AppData\Local\Programs\Python\Python312\python.exe`, Docker daemon 29.6.2
running, `pgvector/pgvector:pg16` + `testcontainers/ryuk:0.8.1` already pulled.

MEASURED BASELINE AT HEAD: `cd backend && $PY -m pytest tests -q` = **4 failed, 450 passed,
1 skipped in ~106s** (the four are pre-existing; 23.1-07 and 23.1-14 fix them).
`tests/test_admin_routes.py` at HEAD passes.

Set up once per worktree, from the repo root:
    python312 -m venv .venv312
    .venv312/Scripts/python -m pip install -q -e "backend[dev]"
</environment>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The space-scoped membership accessor and the cascade status vocabulary</name>

  <files>
    backend/app/db/admin_repo.py
    backend/tests/test_space_deactivation_cascade.py
  </files>

  <read_first>
    - `backend/app/db/admin_repo.py:40-60` — the status constants and the module docstring's
      D-05/D-10 rules.
    - `backend/app/db/admin_repo.py:91-170` — `list_users`, `find_active_membership`,
      `set_membership_status`, `count_active_superadmins`. Match their shape exactly:
      `select(...)` + `.scalars().all()` for reads, `update(...)` returning `result.rowcount`
      for writes.
    - `backend/tests/test_admin_routes.py` — the house harness for admin-surface tests
      (superadmin engine, `get_admin_session` override, `fake_*` seams).
  </read_first>

  <behavior>
    - `_STATUS_SPACE_DEACTIVATED = "space_deactivated"` is defined beside the two existing
      status constants.
    - `list_memberships_for_space(organization_id)` returns every membership row of that
      space regardless of status, and returns `[]` for a space with none. It accepts a
      `str` or a `uuid.UUID` (use the existing `_as_uuid` helper) — the route passes the
      path parameter through as a string.
    - `list_memberships_for_space` NEVER returns a row belonging to another space. Assert
      this with two seeded spaces, not one.
    - `set_membership_status(mid, "space_deactivated")` persists that exact string and
      returns rowcount 1; the row is then NOT counted by `count_active_superadmins()` even
      when its role is `superadmin` — proving the allow-list reads treat the new value as
      inactive.
    - `find_active_membership(org, email)` returns `None` for a membership that carries
      `space_deactivated` — same allow-list proof from the other direction.
  </behavior>

  <action>
    Add `_STATUS_SPACE_DEACTIVATED = "space_deactivated"` next to `_STATUS_ACTIVE` and
    `_STATUS_DEACTIVATED`, with a comment stating (a) why a third value exists — so the
    reactivate inverse can restore ONLY what the cascade took down and never un-fire an
    individually deactivated member; (b) that no migration is needed because the column is a
    plain `String` with no enum or CHECK (`models/membership.py:43`, migration 0006); and
    (c) the load-bearing survey: every membership-status read in `backend/app/` is an
    allow-list `== "active"` (name the five sites), so the new value reads as inactive
    everywhere by construction — and a future deny-list comparison would silently break that,
    which is why the test below pins it.

    Add `list_memberships_for_space(self, organization_id)` to `AdminRepo`, directly beneath
    `find_active_membership`. A `select(OrganizationMembership).where(... == _as_uuid(...))`
    returning `.scalars().all()`. Its docstring must say it returns ALL statuses, and why:
    the deactivate retry path needs rows that are already `space_deactivated` so a failed IdP
    call can be re-attempted.

    Create `backend/tests/test_space_deactivation_cascade.py` with the repo-level half of the
    suite — one named test per `<behavior>` row. Use the same integration harness as
    `test_admin_routes.py` (`pytestmark = pytest.mark.integration`, module-level
    `importorskip` guards). The route-level tests land in Task 2 in this same file.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; $PY -m pytest tests/test_space_deactivation_cascade.py -v</automated>
  </verify>

  <done>
    Five named repo-level tests pass. `grep -rn 'status != "deactivated"' backend/app/
    --include='*.py'` returns nothing (the allow-list survey still holds).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cascade deactivate_space, invert it selectively in reactivate_space</name>

  <files>
    backend/app/api/admin_routes.py
    backend/tests/test_space_deactivation_cascade.py
  </files>

  <read_first>
    - `backend/app/api/admin_routes.py:203-292` — both per-user verbs, verbatim. The order of
      operations, the two 409 guards, and the `audit.log(repo.session, ...)` one-tx rule all
      come from here.
    - `backend/app/api/admin_routes.py:505-542` — the two space verbs being changed.
    - `backend/app/db/audit.py` — `log(...)`'s signature and which fields are safe to put in
      `metadata` (T-06-09: never a link or a token; this plan adds uids, which ARE
      appropriate for an audit row and are NOT appropriate for a response body).
  </read_first>

  <behavior>
    `POST /admin/spaces/{space_id}/deactivate`:
    - Unknown space -> 404 (`set_space_status` rowcount 0), unchanged.
    - A space containing the CALLING superadmin's own membership -> 409, message in the
      register of `"Cannot deactivate yourself"`, and NOTHING is written: the space stays
      `active`, no membership changes, no IdP call is made.
    - A space whose active superadmin members are the last active superadmins -> 409
      (`count_active_superadmins()` would drop to 0), nothing written, no IdP call.
    - The happy path: space status -> `deactivated`; every `active` membership in it ->
      `space_deactivated`; `admin_users.deactivate_user` called EXACTLY ONCE per member with
      a non-null `provider_user_id`; a `space.deactivated` audit row whose `metadata` records
      the number of members cascaded. Members of OTHER spaces are untouched — assert with two
      seeded spaces.
    - A membership whose `provider_user_id` is `None` is skipped by the IdP loop but STILL
      flipped to `space_deactivated`. No crash.
    - When one `deactivate_user` call raises: the DB flip is still committed, the remaining
      members are still attempted (no early return), and the response is 502 whose detail
      states a COUNT and contains no email and no uid.
    - Re-issuing deactivate on an already-deactivated space returns 200 and re-attempts the
      IdP call for its `space_deactivated` members (idempotent retry).

    `POST /admin/spaces/{space_id}/reactivate`:
    - Unknown space -> 404, unchanged.
    - Happy path: space -> `active`; every `space_deactivated` membership -> `active` with
      `admin_users.reactivate_user` called once each; a `space.reactivated` audit row.
    - **A membership carrying `deactivated` (individually fired BEFORE the space went down)
      is NOT touched: its status stays `deactivated` and `reactivate_user` is NOT called for
      it.** This is the single most important assertion in the plan — write it as its own
      named test with a docstring explaining the un-firing hazard.
  </behavior>

  <action>
    Rewrite `deactivate_space` to the order in PLANNING RULING #2. Structure it as:
    read the space (404 if missing) -> read `repo.list_memberships_for_space(space_id)` ->
    evaluate BOTH refusal guards against that list BEFORE any write -> flip memberships and
    the space and write the audit row on `repo.session` -> then loop the IdP calls collecting
    failures -> return the space view, or raise 502 with a count-only detail.

    The self-guard: refuse if any membership in the space has
    `provider_user_id == identity.uid`. The last-superadmin guard: count how many
    `role == "superadmin" AND status == "active"` rows the cascade would take down and refuse
    if that number is `>= repo.count_active_superadmins()`. Both guards must run against the
    list you already read — do not issue a second query and do not re-derive the count inside
    the loop.

    Rewrite `reactivate_space` as the selective inverse: only rows whose status is EXACTLY
    `_STATUS_SPACE_DEACTIVATED` are restored. Carry the same DB-then-IdP order and the same
    502-on-partial-failure shape.

    Both docstrings must state, in this repo's register: what the verb now does beyond the
    status flip; that the IdP is the enforcement and the DB column is bookkeeping (per
    CONTEXT § 2, nothing in an auth path reads `organizations.status`, and this plan does not
    add such a read); that the verb is safe to press twice and why; and, on reactivate, that
    `deactivated` rows are deliberately left alone.

    Add the route-level tests to `backend/tests/test_space_deactivation_cascade.py`, one
    named test per `<behavior>` row. Patch the `admin_users` seam the way
    `test_admin_routes.py` already patches it — recording calls in a list so "called EXACTLY
    ONCE per member" is asserted on the recorded call list, not on a boolean flag. For the
    502 case make the fake raise on the SECOND member specifically, so the test also proves
    the loop did not stop early.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; $PY -m pytest tests/test_space_deactivation_cascade.py tests/test_admin_routes.py -v</automated>
  </verify>

  <done>
    Every `<behavior>` row has its own named passing test, including the un-firing test.
    `tests/test_admin_routes.py` still passes with no edits to it
    (`git diff --stat backend/tests/test_admin_routes.py` is empty).
    `grep -n 'deactivate_user' backend/app/api/admin_routes.py` shows the call inside
    `deactivate_space` as well as in `deactivate_user`.
  </done>
</task>


<task type="auto" tdd="true">
  <name>Task 3: D-23.1-13 — the active-status predicate on both arms of _find_membership</name>

  <files>
    backend/app/auth/session.py
    backend/tests/test_auth_session_membership_status.py
  </files>

  <read_first>
    - `backend/app/auth/session.py:77-113` — `_find_membership`, both arms, and the docstring
      whose three rules (uid-first / CR-01 email gating / CR-02 `.first()`) all survive this
      change.
    - `backend/app/auth/session.py:116-170` — `sync_claims_from_membership`: the
      `ALREADY_SYNCED` short-circuit, the CR-01 email gating, the `_find_membership` call, and
      the `NO_MEMBERSHIP` return.
    - `backend/app/api/auth_routes.py:85-104` — the sole consumer, its `UserDisabledError` ->
      401 arm, and the WR-04 branch on the 3-state `SyncResult`.
    - `backend/tests/test_auth_session.py` — the pre-existing login-sync suite. READ it, do
      NOT edit it: it is not in `files_modified`, and its
      `test_unverified_email_does_not_match_seeded_row` is the CR-01 guard you must not break.
      Its harness (module-level `pytest.importorskip`, per-test `@pytest.mark.integration`,
      `_session_factory(engine)`, and `app.auth.session.auth.set_custom_user_claims` patched in
      EVERY case per threat T-03-02) is the pattern your new file clones.

    ⚠ LINE NUMBERS: verified at `29bc2f1`. The uid arm's `.where()` is at `:95`, the email
    arm's at `:108`, the `if row is not None: return row` early return at `:101`, and the CR-01
    `email_verified` gating at `:147`. If your tree differs, TRUST THE TREE and record what you
    found — do not hunt for a line number that no longer exists.
  </read_first>

  <behavior>
    Every case patches `app.auth.session.auth.set_custom_user_claims` and asserts on the
    RECORDED CALL LIST, not only on the returned `SyncResult`. "No claim was minted" is the
    property that matters, and minting is the harm; a `SyncResult` value alone does not prove
    it.

    (a) NO REGRESSION — an `active` membership still resolves through BOTH arms: matched by
        `provider_user_id`, and matched by `email` on a token that asserts `email_verified`.
        Two named tests, not one; a single test cannot tell you which arm you broke.
    (b) REFUSAL AT THE FUNCTION — `_find_membership` returns `None` for a `deactivated` row and
        for a `space_deactivated` row, on EACH arm. Assert against `_find_membership` directly,
        not only through the route: a route-level assertion would also pass if some unrelated
        layer happened to deny, and this is the function whose contract changed.
        `space_deactivated` needs its own case because it is the value Task 2's cascade writes,
        and a deny-list implementation would admit it.
    (c) THE ROUTE CONTRACT — `sync_claims_from_membership` on a token with NO baked-in role and
        a deactivated-only membership returns `SyncResult.NO_MEMBERSHIP`, and `auth_routes.py`
        maps that to 403.
    (d) THE NO-ROW CONTRACT, AS ITS OWN NAMED TEST — a verified user with genuinely NO
        membership row still returns `NO_MEMBERSHIP` -> 403. Do NOT let this be inferred from
        (c): (c) passing does not prove it, and this is the contract that must not break.
        Add one assertion comparing (c)'s and (d)'s results DIRECTLY for equality — the
        property is that a deactivated member and a non-member are INDISTINGUISHABLE to the
        caller, and two separate `is NO_MEMBERSHIP` assertions do not say that.
    (e) THE PARTIAL-CASCADE DENIAL — the case this decision exists for. Seed a
        `space_deactivated` membership and do NOT simulate any IdP-side disablement: an
        ordinary signature-valid, non-revoked token with no `role` claim, exactly what a member
        presents when Task 2's cascade committed its DB flip and the `deactivate_user` call for
        THAT member raised. The `/auth/session` handshake must DENY and mint nothing.
    (f) CR-01 STILL HOLDS — an ACTIVE membership matched only by an email whose token does NOT
        assert `email_verified` is still refused. Assert it against an ACTIVE row specifically,
        so the test cannot pass merely because the row was inactive.
    (g) THE FALL-THROUGH, PINNED — see the action below. One named test asserting whichever
        behaviour was chosen, with a docstring saying it is a deliberate decision.
  </behavior>

  <action>
    ## WHY THIS SITS IN PLAN 03

    D-23.1-13 was found while verifying D-23.1-11, which is this plan's Task 1 and Task 2, and
    it is the same story: Task 2's cascade is what makes a pre-existing hole load-bearing. It
    is here rather than in a plan of its own because `app/auth/session.py` is untouched by
    every other plan in the phase, and because it closes in wave 1 alongside the cascade it
    protects.

    ⚠ `app/auth/session.py` is NOT `app/db/session.py`. Plan 23.1-07 owns the latter. Same
    basename, different file, no conflict — do not confuse them.

    ## THE EXACT WINDOW — this is why (e) is the test that matters

    Traced and re-verified at `29bc2f1`:

    - `auth_routes.py:99` is the SOLE caller of `sync_claims_from_membership` (a `grep -rn`
      over `backend/app` returns that call plus the definition and docstrings, nothing else).
    - `session.py:141-142` short-circuits with **NO DB READ AT ALL** when the token already
      carries a `role` claim, returning `ALREADY_SYNCED`. So the missing filter cannot bite for
      an already-synced token.
    - `auth_routes.py:89` catches `auth.UserDisabledError` and returns **401 BEFORE login-sync
      runs**. So the missing filter cannot bite for a member whose IdP disable DID land.

    Those two exclusions leave exactly ONE window, and it is the partial-cascade failure: the
    DB says `space_deactivated`, the IdP disable/revoke did not land for that member, their
    still-valid ID token with no baked-in claims hits `/auth/session`, `_find_membership` hands
    back the row, and fresh `role` + `space_id` claims are minted for someone the operator just
    tried to cut off. A version of this task that only tests the happy path proves nothing
    about that window.

    ## THE SIXTH SITE

    Correct the survey wording wherever it appears: `_find_membership` is a SIXTH
    membership-status site, and it is not a deny-list gap like the other five — it is a
    **no-list**, no predicate at all, which is strictly worse.

    ## THE FIX

    Define `_STATUS_ACTIVE = "active"` at module level in `app/auth/session.py` and add
    `OrganizationMembership.status == _STATUS_ACTIVE` to BOTH `.where()` clauses — the uid arm
    at `:95` and the email fallback at `:108`. **One arm is not enough**: the email arm is the
    one an attacker who self-registers a known address would reach.

    Write it as an ALLOW-LIST (`== "active"`), never as `!= "deactivated"`. A deny-list would
    admit `space_deactivated`, which is precisely the value Task 2 writes, and would be the
    exact bug this task exists to close.

    Do NOT import the constant from `app/db/admin_repo.py:48`. The auth layer must not depend
    on the admin repository; duplicate the four characters and leave a comment pointing at
    `admin_repo.py:48` as the sibling definition and `models/membership.py:43` as the
    vocabulary's home.

    ## THE FALL-THROUGH — decide it here, do not discover it in execution

    The uid arm is an EARLY RETURN (`if row is not None: return row`, `:101`). Today a
    deactivated uid row short-circuits the email arm entirely. After your change that query
    returns `None`, so control FALLS THROUGH to the email arm — which now also filters on
    active. A user with a DEACTIVATED uid row AND a separate ACTIVE email row on a verified
    token would therefore be ADMITTED where today they are refused.

    **Recommended: keep the fall-through.** The uid arm now means "an ACTIVE row for this uid";
    when there is none, consulting a verified-email row is the same deterministic uid-first
    policy the docstring already describes, and an active email row represents access the user
    legitimately holds. Whichever you choose, write the choice into the code comment and pin it
    with test (g), so it is a decision on record rather than an accident of an early return.

    ## DOCSTRINGS

    Extend `_find_membership`'s docstring with a FOURTH numbered rule in the register of the
    existing three: only ACTIVE memberships match (D-23.1-13); a `deactivated` or
    `space_deactivated` row is invisible to login-sync and yields `SyncResult.NO_MEMBERSHIP`,
    indistinguishable from having no row at all; and this makes the DATABASE a backstop rather
    than trusting the IdP `disabled=True` + `revoke_refresh_tokens` round-trip alone — which
    matters because this plan's own cascade makes N IdP calls for N members and commits its DB
    flip even when some of those calls fail.

    ## THE admin_routes.py:236 INTERACTION — RULED HERE, NOT LEFT TO AN EXECUTOR

    `admin_routes.py:230-241` guards individual deactivation with
    `membership.role == "superadmin" and membership.status == "active" and
    repo.count_active_superadmins() <= 1`. Once Task 2's cascade marks a member
    `space_deactivated`, that guard no longer fires for them. **That is CORRECT and no change
    is needed.** The guard exists to stop the active-superadmin count reaching zero, and
    `count_active_superadmins()` counts `status == "active"` — a `space_deactivated` superadmin
    is ALREADY excluded from that count, so deactivating them further cannot drop it. Skipping
    the guard is arithmetically right, not an oversight.

    The practical consequence is worth stating plainly, because it is a real and desirable
    semantic rather than a bug: individually deactivating an already-cascade-deactivated member
    flips `space_deactivated` -> `deactivated`, converting a reversible space-level suspension
    into a permanent individual one that Task 2's selective inverse will NOT restore. That is
    "fire someone whose space is currently off", and it works. Put this paragraph in the code
    comment beside `_STATUS_ACTIVE` so the next reader does not file it as a defect.

    The self-deactivation guard at `:230` (`membership.provider_user_id == identity.uid`) is
    status-independent and is unaffected.

    ## WHAT TASK 3 MUST NOT DO

    - Do NOT change the `SyncResult` contract: three states, same meanings, same HTTP mapping.
      Do not add a fourth state, a distinct error message, or a log line at the route that
      would let a caller tell a deactivated member from a non-member.
    - Do NOT weaken the CR-01 `email_verified` gating at `:147`. This task ADDS a status test
      on top of it.
    - Do NOT change the CR-02 `.scalars().first()` shape on either arm. Narrowing the WHERE
      must not change the single-row resolution that keeps this handshake from raising
      `MultipleResultsFound` and turning an auth decision into a 500.
    - Do NOT add a DB call to `app/auth/dependencies.py` — D-06 keeps it DB-free, and
      D-23.1-03 rejects an org-status lookup there by name.
    - Do NOT edit `backend/tests/test_auth_session.py`; it is not in `files_modified`. Test (d)
      deliberately restates a property that file already covers, because this task's change is
      what could break it and the equivalence in (d) is not asserted there.

    ## ORDER OF WORK

    Write `backend/tests/test_auth_session_membership_status.py` FIRST and RUN it. The refusal
    cases in (b), (c) and (e) must FAIL at that point — login-sync currently mints claims for
    those rows — while (a), (d) and (f) pass. **Record that pre-edit failure count in the
    SUMMARY.** A refusal suite that is green before the predicate exists is testing nothing,
    and this repo has shipped that shape before.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; $PY -m pytest tests/test_auth_session_membership_status.py tests/test_auth_session.py tests/test_auth_dependency.py tests/test_me_routes.py -v</automated>
  </verify>

  <done>
    Every `<behavior>` row has its own named passing test, including (d) as a standalone test
    and its direct equality comparison with (c).
    The SUMMARY records the PRE-EDIT run — which tests failed and how many.
    `grep -n "OrganizationMembership.status" backend/app/auth/session.py` shows the predicate on
    BOTH selects — READ the two hits, do not count them; one arm patched and one missed is the
    exact half-fix this task exists to avoid.
    `grep -c '!= "deactivated"' backend/app/auth/session.py` returns 0 — no deny-list.
    `grep -n 'email_verified' backend/app/auth/session.py` still shows the CR-01 guard.
    `git diff --stat backend/tests/test_auth_session.py backend/app/auth/dependencies.py` is
    EMPTY; `admin_routes.py` may differ only from Task 2, never from this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> admin_router | A superadmin issues a tenant-wide revocation; a mistake here locks out a paying client or the operator themselves |
| backend -> Identity Platform | The enforcement half; a network failure here means the DB says "off" and the IdP says "on" |
| membership.status vocabulary -> every allow-list read | A new status value is only safe while every read is an allow-list |
| Identity Platform token -> login-sync | A signature-valid token from a NOT-disabled account is the input; the DB decides whether it earns a claim. This is the ONLY thing standing between a cascaded member and fresh claims when their IdP call did not land |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.1-07 | Elevation of Privilege | a member keeping access after their space is deactivated | mitigate | `deactivate_user` (disable + `revoke_refresh_tokens`) per member; `dependencies.py:78`'s `check_revoked=True` makes it bite next request |
| T-23.1-08 | Elevation of Privilege | reactivate silently restoring an individually fired member | mitigate | The `space_deactivated` third status; the inverse restores only that value; its own named test |
| T-23.1-09 | Denial of Service (self-inflicted) | the operator deactivating their own space and locking everyone out | mitigate | Both 409 guards evaluated BEFORE any write, carried from `admin_routes.py:230-241` |
| T-23.1-10 | Repudiation | a partial cascade with no record of who was actually disabled | mitigate | Failed uids go into the audit `metadata` and the log; the response carries a count only (T-06-09) |
| T-23.1-11 | Information Disclosure | the 502 body leaking member emails/uids | mitigate | Detail is a count; the test asserts no `@` and no uid substring appears in the response body |
| T-23.1-11a | Elevation of Privilege | login-sync minting `role` + `space_id` claims for a `deactivated` or `space_deactivated` membership (D-23.1-13) | mitigate | Task 3's `== "active"` predicate on BOTH arms; refusal tests assert ZERO recorded `set_custom_user_claims` calls |
| T-23.1-11b | Elevation of Privilege | the same via the email arm, which a self-registered known address reaches | mitigate | The predicate on the email arm tested independently; the `<done>` requires READING both hits, not counting them |
| T-23.1-11c | Elevation of Privilege | a member of a cascaded space still authenticating because their IdP call failed mid-cascade | mitigate | Task 3 test (e): a `space_deactivated` row with NO IdP disablement is refused by the DB alone. This is the row that makes the cascade's 502-on-partial-failure design safe |
| T-23.1-11d | Information Disclosure | a distinct response letting a caller tell "deactivated" from "no such member" | mitigate | Both return `SyncResult.NO_MEMBERSHIP`; test (d) compares the two results directly rather than asserting each separately |
| T-23.1-11e | Elevation of Privilege | the predicate written as a deny-list, admitting `space_deactivated` | mitigate | Allow-list mandated; `space_deactivated` has its own named case; `<done>` greps for the deny-list form and requires 0 |
| T-23.1-11f | Spoofing | the CR-01 `email_verified` guard weakened while adding the status test | mitigate | Test (f) re-asserts it against an ACTIVE row, so it cannot pass merely because the row was inactive |
| T-23.1-11g | Elevation of Privilege | the changed control flow admitting a user with a deactivated uid row and an active email row | mitigate | Ruled in Task 3's action and pinned by test (g) — a decision on record rather than an accident of an early return |
| T-23.1-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NOTHING; no package-manager command appears in any task |
</threat_model>

<verification>
1. `cd backend && $PY -m pytest tests/test_space_deactivation_cascade.py tests/test_admin_routes.py -v` — green.
2. `cd backend && $PY -m pytest tests -q` — the failure set is the SAME four names as the
   recorded baseline. A fifth is this plan's regression.
3. `grep -rn 'status != "deactivated"' backend/app/ --include='*.py'` returns nothing — the
   allow-list survey that makes the third status value safe still holds.
4. `grep -n 'dependencies' backend/app/api/admin_routes.py` shows no new import of
   `app.auth.dependencies` beyond `get_current_identity` — D-23.1-03's rejected alternative
   did not sneak in.
5. `cd backend && $PY -m pytest tests/test_auth_session_membership_status.py tests/test_auth_session.py -v`
   — green, with `test_auth_session.py` unedited (`git diff --stat` over it is empty).
6. `grep -c '!= "deactivated"' backend/app/auth/session.py` — 0.
7. The SUMMARY records Task 3's PRE-EDIT failure count.
</verification>

<success_criteria>
- Deactivating a space calls `admin_users.deactivate_user` once per member with a non-null
  `provider_user_id`, proved on a recorded call list.
- Reactivating a space restores only `space_deactivated` rows; a `deactivated` row is
  untouched and `reactivate_user` is not called for it.
- Both 409 guards refuse before any write and before any IdP call.
- A partial IdP failure yields 502 with a count-only detail and a committed DB flip, and a
  second press of the verb retries.
- Login-sync refuses a non-active membership on BOTH arms of `_find_membership`, proved at the
  function and through the route, with the partial-cascade case as its own named test.
- A deactivated member and a user with no row at all are indistinguishable, proved by comparing
  the two results directly.
- CR-01's `email_verified` guard and CR-02's `.first()` shape are intact.
</success_criteria>

<output>
Create `.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/23.1-03-SUMMARY.md` when done.
</output>
