---
phase: 23.1-platform-hardening-authorization-boundary-space-deactivation
verified: 2026-09-04T16:18:26Z
status: gaps_found
score: 11/12 must-haves verified
overrides_applied: 0
verified_at_commit: fb518c17a45787b4a1be0beb3722bed7979ecbe9
tree_state: clean (only untracked .claude/, excluded from all searches)
gaps:
  - truth: "Every operator-only verb is gated by ONE shared superadmin_gate (existence-hidden 404)"
    status: partial
    reason: >
      The eight intake verbs and all seven ai_router routes ARE gated and were
      independently re-measured. But POST /intakes/{intake_id}/research — the PAID
      Tribunal deep-research trigger, ~$45 per invocation with the cost governor
      UNCAPPED — takes Depends(get_current_identity) and NO role gate. Its only
      frontend caller is the admin route admin.pulse.intakes.$id.tsx, so it is an
      operator verb by the same evidence CONTEXT § 1 used for the other eight. The
      FREE sibling verbs on the same router (/research/cancel, /research/resume) ARE
      gated; the paid one is not. Root cause: CONTEXT § 1's survey says "reading every
      route in intake_routes.py" — research_routes.py was never surveyed for ungated
      verbs, and D-23.1-02 then froze the scope at "exactly the nine verbs in section
      1's table".
    artifacts:
      - path: "backend/app/api/research_routes.py:249-254"
        issue: "trigger_research declares Depends(get_current_identity) with no superadmin_gate and no in-body role check"
      - path: "backend/tests/test_client_surface_open.py:697"
        issue: "POST /intakes/{intake_id}/research is present in _PINNED_INTAKE_ROUTES, but that test only alarms on ADD/REMOVE — nothing in this phase asserts whether the route is gated or open"
    evidence:
      - "Independent include-aware route walk over the live app: 65 routes flattened, 24 gated. POST /intakes/{intake_id}/research resolves to ['get_tenant_repo','get_current_identity'] — superadmin_gate absent."
      - "Live probe, role=user, own-space intake at status=decomposed: HTTP 422 'Intake has no validated research questions', i.e. the request passed authorization and reached the handler body. A gated verb returns 404 before the body runs."
      - "grep over all 16 PLANs, 15 SUMMARYs and CONTEXT: zero occurrences of trigger_research. The phase discussed the 'paid research trigger' only as a threat D-23.1-05 must not re-arm, never as a verb needing a gate."
    missing:
      - "Add Depends(superadmin_gate) to trigger_research, declared BEFORE Depends(get_tenant_repo) per the gate's ordering contract."
      - "Add the three-arm test set the other eight verbs have (user-role 404, null-space 404, superadmin still works) plus a no-side-effect arm asserting zero new research_runs rows."
      - "Decide and record the disposition of the four remaining ungated /intakes verbs listed under WARNINGS: POST /intakes, GET /intakes/{id}/context-pack, GET /intakes/{id}/sources, GET /intakes/{id}/research/stream."
      - "Consider upgrading test_client_route_inventory_is_pinned from an add/remove alarm to a gated/open classification, so a new operator verb cannot land ungated."
human_verification:
  - test: "Deploy nestor-api + nestor-frontend, apply migrations 0014 and 0015, then run 23.1-UAT.md § 4a-4e against the live service"
    expected: "role=user gets 404 on POST /intakes/{id}/review and POST /intakes/{id}/skills/apply with no new skill_runs row; 200 on GET /intakes/{id}/skill-runs; superadmin still succeeds; a deactivated space's member cannot authenticate on the next request"
    why_human: "gcloud builds submit is classifier-blocked for the agent. Nothing in this phase has ever run in production — the live revisions are still nestor-api-00047-ghp / nestor-frontend-00035-zz2, both from 2026-08-31, and neither migration is applied."
  - test: "Run 23.1-UAT.md § 3.4 — migration 0014 against production data"
    expected: "The duplicate pre-flight resolves any pre-existing duplicate running skill_runs to failed, then CREATE UNIQUE INDEX succeeds"
    why_human: "0014 is the risky one (D-23.1-12). It has only ever been exercised against a seeded test database; the production duplicate population is unknown."
  - test: "Run both new CI configs once in Cloud Build (cloudbuild.test.yaml and frontend/cloudbuild.yaml)"
    expected: "Both go green; the backend collection assertion reports 66 on disk / 65 collected"
    why_human: "Plan 14's own openly-unmet criterion. Both configs are committed but have never executed in Cloud Build. Locally re-measured green here (599 passed / 2 skipped; tsc 0; vitest 154/10), which is not the same as the gate running."
  - test: "Append the three tribunal test filenames to tribunal/cloudbuild.test-critical.yaml:32 (DEF-23.1-14-03)"
    expected: "test_run_ownership_fence.py, test_run_api_idempotency.py and test_stale_reclaim.py run in a committed gate"
    why_human: "The file is outside every plan's files_modified and editing it changes what the operator's build runs. Confirmed still naming exactly four files at HEAD. The 37 tests were re-run here against a disposable postgres:15 (40 items, all pass, zero skips) — they are correct, they are just not guarded."
  - test: "§ 6 operator decisions — tfstate bucket + terraform init -migrate-state, and whether to rotate app_superadmin"
    expected: "A recorded decision either way"
    why_human: "Both require credentials and judgement the agent does not have."
---

# Phase 23.1: Platform Hardening — Verification Report

**Phase Goal (ROADMAP):** Close the verified authorization, cost-control and run-ownership
defects found by the 2026-09-03 audit, without breaking the live client surface.
**Verified:** 2026-09-04T16:18:26Z at `fb518c1`
**Status:** `gaps_found`
**Re-verification:** No — initial verification.

**Method.** Everything below was measured in this session against the working tree. No claim
was carried over from a SUMMARY, from `23.1-CONTEXT.md`, or from the orchestrator's brief.
Repo-wide searches exclude `.claude/`. `git status` was checked before and after every
measurement; the tree is clean apart from untracked `.claude/`.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ONE shared `superadmin_gate` exists, existence-hidden 404, and `research_routes.py` imports it rather than defining its own | VERIFIED | `backend/app/auth/gates.py:34` is the only `def superadmin_gate` / `def _superadmin_gate` in the tree. `research_routes.py:87` imports it aliased to `_superadmin_gate`, so its 12 pre-existing `Depends` sites are unchanged. Condition is an allow-list of one (`!= "superadmin"` → 404 "Intake not found"). |
| 2 | The eight intake operator verbs are gated | VERIFIED | Independent include-aware walk of the live app (65 routes): `review`, `deliver`, `report/replace`, `mail/validation`, `mail/reminder`, `mail/results`, `mail/intake`, `GET members` all resolve `superadmin_gate`. Ordering contract held on all eight — gate declared before `get_tenant_repo`. 24 route/method pairs gated overall. |
| 3 | `ai_router` is gated once, at router level, covering all seven AI routes | VERIFIED | `ai_routes.py:80-83` — `APIRouter(..., dependencies=[Depends(superadmin_gate)])`. Route walk confirms the gate resolves on all seven: `skills/apply`, `skills/context-pack`, `skills/extract-insights`, `skills/structure-answers`, `sources/{id}/transcribe`, `embeddings`, `search`. No per-handler copies (`test_no_ai_handler_declares_its_own_superadmin_gate`). |
| 4 | **Every** operator-only verb is gated | **FAILED** | `POST /intakes/{intake_id}/research` — the paid Tribunal trigger — is ungated. See Gaps Summary. |
| 5 | The ten client-reachable routes stay open to role=`user` and are pinned by test | VERIFIED | `backend/tests/test_client_surface_open.py` blob is `b37224a6` at HEAD, identical to plan 02's final commit `53fd785` — byte-unchanged as required. 12 tests re-run here: **12 passed**. Route walk independently shows all ten OPEN. |
| 6 | Deactivating a space actually revokes its members (SEC-02) | VERIFIED | `admin_routes.py:598-640`: targets = `active` + `space_deactivated`, flip to `space_deactivated`, `_apply_to_idp(uids, admin_users.deactivate_user)` which is `update_user(disabled=True)` + `revoke_refresh_tokens`, audit in the same tx, partial failure commits then 502. `reactivate_space` restores ONLY `space_deactivated` rows. `test_space_deactivation_cascade.py` (921 lines) green. |
| 7 | A partial cascade failure cannot leave a member authenticating (D-23.1-13) | VERIFIED | `auth/session.py:141` and `:174` — both arms of `_find_membership` carry `OrganizationMembership.status == _STATUS_ACTIVE`. Independent survey of every membership-status read in `backend/app/`: 9 sites, all allow-lists (`== "active"` / `== _STATUS_ACTIVE`) plus the two deliberate `space_deactivated` selectors. **Zero deny-lists** — `space_deactivated` cannot leak through anywhere. |
| 8 | A second concurrent AI skill run is refused by a DB invariant (COST-01) | VERIFIED | Alembic `0014` creates `uq_skill_runs_one_running_per_intake_skill`, UNIQUE on `(intake_id, skill) WHERE status='running'`, preceded by the D-23.1-12 duplicate pre-flight (with `NO FORCE RLS` lifted for exactly one statement). Mirrored in `models/skill_run.py:95`. `ai_session.py` raises `ActiveSkillRunExistsError`; `ai_routes.py:120-131` translates it to 409 in `_dispatch_skill_run`, which every skill route goes through. |
| 9 | Context-pack cannot flip an arbitrary intake to `decomposed` | VERIFIED | `_CONTEXT_PACK_TRANSITIONS = {"validated_by_client": "decomposed"}` (`context_pack.py:81`), applied through the new `IntakeRepository.patch_if` compare-and-swap (`repository.py:120`) — the precondition rides in the UPDATE's WHERE, ANDed onto `_scope` so TENANT-02 can only narrow. On refusal the run finalizes `failed` with a readable message in the same control flow — never left `running`, `applied_at` deliberately unset. |
| 10 | A displaced Tribunal worker can neither heartbeat nor finalize a run it lost | VERIFIED | Every `UPDATE run` in `worker.py` audited. Fenced with `AND worker_id = :wid`: heartbeat (`:213`), 3 park writes (`:347/:374/:455`), 2 terminal writes (`:643/:705`). Unfenced by design: `CLAIM_SQL` (`:138`) and `REAP_SQL` (`:265`, the cross-worker reaper). `:wid` binds to the module `WORKER_ID`, not `claimed["worker_id"]` (`:251`) — the distinction that makes the fence real. |
| 11 | The two non-idempotent paid Tribunal endpoints are safe under concurrency | VERIFIED | `GET /{run_id}/report-proposal`: `api.py:483` takes the same `ADVISORY_LOCK_SQL` the executor uses, BEFORE the first read that can lead to generation, then re-reads the cache under the lock (`:492`). `POST /{run_id}/answer`: `_RETIRE_FOR_ANSWER_SQL` (`api.py:66`) is a conditional `UPDATE ... WHERE status='needs_input' RETURNING id`; the cancel is the ticket and the child run is created only by the winner. `brief` bounded at `max_length=1_000_000` on both request models (`schemas.py:137,157`). Re-run here against a live DSN: **40 test items, all passed, zero skips.** |
| 12 | CI is no longer blind to the non-integration suite and the frontend gates | VERIFIED | `cloudbuild.test.yaml:189` runs `python -m pytest tests -v -rs` — the whole suite, filter removed — behind a collection assertion (`EXPECTED_DISK_FILES=66`, `EXPECTED_FILES=65`). Both numbers re-measured exactly here. `frontend/cloudbuild.yaml` gained `install-and-typecheck` (`npm ci` + `npx tsc --noEmit`) and `vitest` steps ahead of `build`; lint deliberately absent with the reason in the file header. |

**Score: 11/12 truths verified.**

---

### Requirements Coverage

| Requirement | Description (ROADMAP) | Status | Evidence |
|---|---|---|---|
| **SEC-01** | server-side role enforcement on every operator verb | **BLOCKED (partial)** | 15 of 16 operator verbs closed and independently re-measured (truths 2 + 3). `POST /intakes/{intake_id}/research` remains ungated — see Gaps Summary. STATE.md's "SEC-01 measured closed" is true of the eight intake verbs and the seven AI routes it names, and overbroad as a statement about SEC-01. |
| **SEC-02** | space deactivation revokes access | SATISFIED | Truths 6 + 7. Cascade + selective reactivate + the `_find_membership` DB backstop, all three test-covered and green. |
| **COST-01** | no duplicate paid work | SATISFIED | Truth 8 (DB invariant, not an app check) and truth 11 (both paid Tribunal endpoints serialised). Truth 9 removes the path that re-armed the paid trigger from an arbitrary status. Partially undercut by the SEC-01 gap: an ungated paid trigger is unbounded work of a different kind. |
| **TENANT-02** | no cross-tenant access — unchanged, must stay green | SATISFIED | Eight cross-tenant / RLS / scope test files (`test_intake_cross_tenant`, `test_ai_cross_tenant`, `test_research_cross_tenant`, `test_rls_isolation`, `test_storage_cross_tenant`, `test_ai_search_cross_tenant`, `test_tenant_repository`, `test_scope_guard_ai`) are **byte-unchanged** since `047dcfe` and re-run here: **72 passed**. `patch_if` ANDs its predicates onto `_scope`, which can only narrow. No regression. |

---

### Decision Coverage (D-23.1-01 … D-23.1-15)

| Decision | Status | Where it actually is in the code |
|---|---|---|
| D-23.1-01 shared gate | VERIFIED | `app/auth/gates.py:34`; sole definition in the tree; `research_routes.py:87` imports it |
| D-23.1-02 gate the nine verbs + router-level `ai_router` | VERIFIED **as scoped** | 8 × `Depends(superadmin_gate)` in `intake_routes.py`; `ai_routes.py:83` router-level. The decision's own scope ("exactly the nine verbs in section 1's table") is what leaves the gap in truth 4 |
| D-23.1-03 cascade inside the existing tx | VERIFIED | `admin_routes.py:598-640` + `645+` |
| D-23.1-04 DB invariant, not an app check | VERIFIED | alembic `0014` + `IntegrityError` → `ActiveSkillRunExistsError` → 409 |
| D-23.1-05 context-pack CAS, run finalizes `failed` | VERIFIED | `context_pack.py:81,220,251`; `repository.py:120` |
| D-23.1-06 `worker_id` on heartbeat AND terminal writes, keeping `status='running'` | VERIFIED | 6 fenced sites, both predicates present on each |
| D-23.1-07 advisory-locked proposal GET, same lock as `execute.py`, URL unchanged | VERIFIED | `api.py:32` imports `ADVISORY_LOCK_SQL` from `execute.py`; `:483` takes it; `:492` re-reads under it |
| D-23.1-08 `POST /answer` as compare-and-swap | VERIFIED | `api.py:66` `_RETIRE_FOR_ANSWER_SQL`, `:330` `_claim_for_retire` |
| D-23.1-09 red tests fixed FIRST, CI widening LAST | VERIFIED | Wave-5 commit order `7d86450` (tests) → `daa3935` (backend CI) → `4e3a549` (frontend CI). The D-03 guard was fixed by relocating `get_engine_for_pool_check` into `app/db/session.py`; `test_ci_guard_raw_db.py` and `backend/scripts/` are **byte-unchanged** — the exclusion list was not widened. `test_mail_render.py`'s fix asserts BOTH the escaped link present and the raw form absent, so it now proves autoescape is ON. |
| D-23.1-10 scope-ceiling reconciliation, Supabase ban kept | VERIFIED | `CLAUDE.md` carries the operator-approved wording verbatim, adds an explicit "do not unmount the research router" line naming the two enforcing tests, and keeps the legacy `run-research` prohibition. `main.py:152` (the cited mount line) is accurate. |
| D-23.1-11 reactivate restores only `space_deactivated` | VERIFIED | `admin_routes.py:675`; survey of all 9 status reads confirms allow-lists only |
| D-23.1-12 0014 resolves pre-existing duplicates first | VERIFIED | `0014:145-180` pre-flight UPDATE under lifted FORCE RLS, restored in `finally`; `downgrade()` honestly declines to reverse it |
| D-23.1-13 `_find_membership` filters on active in both arms | VERIFIED | `session.py:141` and `:174`. The verbatim `admin_routes.py:236` consequence text is at `session.py:76-79` as CONTEXT required |
| D-23.1-14 route walking must be include-aware | VERIFIED | `_flatten_routes` / `_resolved_dependency_calls` in `test_client_surface_open.py:711,775`, reused by the gate suites. Independently reproduced: a naive flat loop sees 0 `/intakes` routes; the recursive walk yields 65 |
| D-23.1-15 the tribunal fence has no committed CI gate | **RECORDED, OPEN** | `tribunal/cloudbuild.test-critical.yaml` is **byte-unchanged** and still names exactly four files at `:32`. Correctly handed to the operator rather than silently applied |

---

### Key Link Verification

| From | To | Via | Status | Detail |
|---|---|---|---|---|
| `intake_routes.py` (8 verbs) | `app/auth/gates.superadmin_gate` | `Depends(...)` before `get_tenant_repo` | WIRED | Resolved on the live app, not grepped |
| `ai_routes.ai_router` | `superadmin_gate` | `APIRouter(dependencies=[...])` | WIRED | Gate resolves on all 7 leaves |
| `research_routes.py` (12 sites) | `superadmin_gate` | import alias `_superadmin_gate` | WIRED | Denial suite `test_research_cross_tenant.py` unchanged and green |
| `research_routes.trigger_research` | `superadmin_gate` | — | **NOT_WIRED** | No gate, no in-body role check; live probe reached the handler body as role=`user` |
| `ai_session.create_running_skill_run` | alembic 0014 index | `IntegrityError` → `ActiveSkillRunExistsError` → 409 | WIRED | `_is_one_running_violation` narrows on the index name so a real defect is not hidden behind a 409 |
| `context_pack.py` | `IntakeRepository.patch_if` | conditional UPDATE | WIRED | Refusal path finalizes the run in the same tx |
| `worker._heartbeat_loop` | `_HEARTBEAT_SQL` | `{"wid": WORKER_ID}` | WIRED | Module constant, not the stale claim copy |
| `runs/api.get_report_proposal` | `execute.ADVISORY_LOCK_SQL` | cross-module import | WIRED | Same lock the executor holds |
| `admin_routes.deactivate_space` | `admin_users.deactivate_user` | `_apply_to_idp` | WIRED | `update_user(disabled=True)` + `revoke_refresh_tokens` |
| `cloudbuild.test.yaml` | whole backend suite | `pytest tests -v -rs` + collection assertion | WIRED | 66/65 re-measured exactly |
| `frontend/cloudbuild.yaml` | tsc + vitest | two steps before `build` | WIRED | Both re-run green locally |

---

### Behavioural Spot-Checks

| Behaviour | Command | Result | Status |
|---|---|---|---|
| Full backend suite | `python -m pytest -q` (backend, py3.12.6) | **599 passed, 2 skipped, 0 failed** in 90.19s | PASS — matches the claimed figure exactly |
| Client stay-open suite in isolation | `pytest tests/test_client_surface_open.py` | 12 passed | PASS |
| TENANT-02 regression set | 8 cross-tenant / RLS / scope files | 72 passed | PASS |
| Backend collection assertion inputs | `ls tests/test_*.py` / `pytest --collect-only` | 66 on disk, 65 collected | PASS — the CI assertion's constants are honest |
| Alembic single head | derived from `revision` / `down_revision` across all 15 files | single head `0015`; chain 0012→0013→0014→0015 | PASS |
| Frontend typecheck | `npx tsc --noEmit` | exit 0, no output | PASS |
| Frontend unit tests | `npm test` | **154 passed / 10 files**, exit 0 | PASS |
| i18n audit | `node scripts/i18n-audit.mjs` | `RESULT: PASS — A/B/C clean (104 CHECK D advisories)`, exit 0 | PASS |
| Tribunal fence + idempotency + reclaim, against a real DSN | disposable `postgres:15`, `DATABASE_URL=postgresql+asyncpg://…` | **40 items passed, 0 skipped** (37 test functions: 16+16+5) | PASS |
| Route gate audit (independent, include-aware) | own script over `app.main.app` | 65 routes, 24 gated; all 8 verbs + 7 AI routes gated; all 10 client routes open | PASS with one exception (truth 4) |
| Paid trigger reachable as role=`user` | live probe, own-space `decomposed` intake, driver neutralised | **HTTP 422** (business validation), not 404 | **FAIL — no role gate** |

Container cleanup verified; `git status` clean after every step.

---

### Non-Goals and CONTEXT § 6 Prohibitions

| Item | Status | Evidence |
|---|---|---|
| Research router still mounted and unmodified | HONOURED | `git diff 047dcfe..HEAD -- backend/app/main.py` is **empty**. `research_router` mounted at `main.py:152`. Route walk shows all 12 research routes live. |
| No `_ACTIVE_IDENTITY` re-architecture beyond explicit threading | HONOURED | The module slot and `identity_of()` are deleted; `identity: Identity` is now a required parameter threaded through the finalize writers. `test_research_run_task.py:1091` asserts neither name survives. No scope logic changed. |
| No phantom bucket-parameter work | HONOURED | Zero bucket-related lines in the tribunal schemas/API diff. `audit/gcs_blob.py` untouched. |
| No terraform-state overclaim | HONOURED | The only change is `.gitignore:39-40` (`*.tfstate`, `*.tfstate.*`). The bucket + `init -migrate-state` are correctly left as operator item § 6a. |
| No dependency additions | HONOURED | The only `package.json` change is the **removal** of `rehype-raw`. `backend/pyproject.toml` and the tribunal requirements are unchanged. |
| No Supabase work | HONOURED | No file under a Supabase path modified; the legacy project untouched. |
| **ZERO provider spend / no run triggered** | HONOURED — measured live | Newest object in `gs://project-cb01b861-cb4a-438d-b9a-nestor-audit/runs/**` is **2026-08-31T08:43:24Z**; newest in `gs://nestor-audit-prod/` is 2026-06-15. Both predate the phase (2026-09-03/04). 10 run prefixes, none new. The one probe I ran in this verification neutralised the driver and stopped at a 422 before any dispatch. |
| Deploy scripts' `--account` limitation | CONFIRMED | `--account` appears zero times in `deploy-api.sh` and `deploy-worker.sh`, as CONTEXT § 14.4 states. (Noted while checking: the active gcloud config had drifted to `tools@epicimpact.be` again — every read here pinned `--account=tools@dotto.be`.) |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | `TBD` / `FIXME` / `XXX` | — | **Zero** across all 56 non-doc files modified by this phase |
| — | — | `TODO` / `HACK` / `PLACEHOLDER` | — | **Zero** across the same 56 files |
| `backend/app/main.py` | 153-162 | Stale comment: "each handler depends on Identity ONLY" — `ai_router` now also carries a router-level `superadmin_gate` | INFO | No behavioural effect. Openly recorded as DEF-23.1-11-01, which says "Pick up in 23.1-15"; plan 15 did not, and `main.py` is unchanged. Still open. |

No stubs, no hollow props, no empty returns introduced. Every new file is substantive and wired.

---

## WARNINGS — four more ungated `/intakes` verbs, dispositions unrecorded

Found by the same route audit. None is as severe as the research trigger, but none was
classified by this phase either. All are own-space scoped, so TENANT-02 still holds; the
question is role, not tenant.

| Route | Only frontend caller | Assessment |
|---|---|---|
| `POST /intakes` | `admin.pulse.intakes.new.tsx:45` (`createIntake`) | Operator verb by caller. Creates in the caller's own space; no cost, no lifecycle jump. Low. |
| `GET /intakes/{id}/context-pack` | `ContextPackBlock.tsx` → admin route only (the client results page mentions it in a comment only) | Read-only, own-space. Low. |
| `GET /intakes/{id}/sources` | `AISkillsPanel.tsx` → admin route only | Read-only, own-space. Low. |
| `GET /intakes/{id}/research/stream` | `research.ts:572` SSE, admin run page | Read-only, own-space. Low. |

Checked and found **not** to be gaps: `PATCH /intakes/{id}` explicitly refuses lifecycle/status
fields (`intake_routes.py:533-543`) and has no frontend caller at all;
`POST /intakes/{id}/storage/uploads` and `DELETE /intakes/{id}/storage/objects` are the client
form's own upload path; the whole `/admin/*` router is superadmin-only via `get_admin_session`'s
403 (`db/session.py:374`), which CONTEXT deliberately does not unify with the 404 gate.

---

## Deferrals — assessed for honesty

All four are recorded plainly, in the file named for the purpose, and none is buried.

| Item | Recorded where | Honest? |
|---|---|---|
| DEF-23.1-01 rehype-raw badge loss | `deferred-items.md` + UAT appendix | **Yes, and self-corrected.** The entry contradicts its own plan in a block headed "Correction to the plan", stating the blast radius is two call paths, not one. Verified in the tree: `rehype-raw` appears in `frontend/src` only inside explanatory comments; both badge handlers deliberately left in place. |
| DEF-23.1-02 lint | `deferred-items.md` (plan 08 entry **left intact**) + a plan-14 ADDENDUM + UAT appendix | **Yes.** The wrong figure (61) is not rewritten — it is superseded in place by a measured table (6,540 LF / 28,924 CRLF, 99.1% prettier) with the method that produced it and an explanation of why the original was wrong. This is the right shape for a correction. |
| T-23.1-20 execution-time fence transferred | plan 05, its SUMMARY, CONTEXT § 10, UAT appendix | **Yes on substance, one filing quibble.** Stated in four places as "TRANSFERRED, not mitigated", and no acceptance criterion in the phase implies otherwise. It is the only accepted deferral **absent from `deferred-items.md`** — deliberately, because plan 05 was told that file belonged to plan 08 in the same wave. Defensible, but a reader who opens only `deferred-items.md` will not find it. |
| DEF-23.1-14-03 / § 14 — 37 of 37 tribunal tests gate nothing | `deferred-items.md` (entry + addendum), CONTEXT § 12 and § 14, UAT § 5.2 | **Yes, unusually so.** The addendum corrects its own mechanism twice, downgrades the headline from 32/37 to the worse 37/37, and explicitly records that the two documents disagree about whether plan 15 could fix it ("recorded here rather than resolved silently"). Independently confirmed: `cloudbuild.test-critical.yaml` still names four files; the three test files carry zero `@pytest.mark.live`, so the `-m "not live"` filter is indeed innocent. |

---

## Contradictions found — items that disagree with a source document

1. **The brief says sixteen SUMMARY files; there are fifteen.** `23.1-15-SUMMARY.md` **does not
   exist** — not in the directory and not in `git ls-files`. Plan 15's task 1 output is
   `23.1-UAT.md` itself, which is complete and high quality, but the completed task has no
   SUMMARY record. Every other plan (01-14, 16) has one.
2. **`CONTEXT § 1`'s survey scope is narrower than its conclusion.** It says "reading every
   route in `intake_routes.py` found the same defect on six more verbs" and then presents that
   table as *the* list of ungated operator verbs. `research_routes.py` was never surveyed the
   same way. That is precisely where the one gap in this phase lives.
3. **ROADMAP says "the nine client-reachable routes"; CONTEXT § 1 and the pin suite say ten.**
   The implementation covers ten, so the effect is benign — the ROADMAP sentence is simply
   miscounted.
4. **`STATE.md`'s "SEC-01 measured closed"** is accurate about the fifteen verbs it enumerates
   and overbroad as a claim about the requirement. (Not edited — STATE.md and ROADMAP.md are
   the orchestrator's.)
5. Everything else I sampled from CONTEXT held up under independent measurement, including the
   ones most likely to be wrong: the 65-route flatten, the `_IncludedRouter` behaviour, the
   owning-vs-including router dependency distinction, `--account` absent from both deploy
   scripts, `cloudbuild.test-critical.yaml`'s four-file list, and the zero-`live`-marker
   correction.

---

## Gaps Summary

**One gap. It is a real hole in the phase's headline claim, and it is the expensive one.**

`POST /intakes/{intake_id}/research` starts a Tribunal deep-research run. Per this project's
own cost records that is roughly **$45 of provider spend per invocation**, with the $25 governor
never armed (`UNCAPPED=1`), repeatable up to the 3-attempt cap per intake. It takes
`Depends(get_current_identity)` and nothing else.

The asymmetry is what makes this look like an oversight rather than a decision: on the very
same router, `POST /research/cancel` and `POST /research/resume` — both **free** — carry
`Depends(_superadmin_gate)`, and `resume_research`'s own docstring calls itself "free,
unlimited, superadmin-only". Every research *read* verb is gated. The one verb that spends
money is not.

Two independent measurements:

- **Structural.** An include-aware walk of the live `app.main.app` resolves this route's full
  dependency tree to `['get_tenant_repo', 'get_current_identity']`. `superadmin_gate` is absent,
  and there is no in-body role check — the only `identity.role == "superadmin"` in the handler
  is a space-scoping branch, not a denial.
- **Behavioural.** A probe with a fabricated role=`user` Identity, an own-space intake seeded at
  `status='decomposed'`, and the driver neutralised returned **HTTP 422** with a business
  validation message. A gated verb answers 404 from the dependency, before the handler body
  runs. Reaching a 422 proves the request cleared authorization. (No provider call was made and
  no run row survived; the probe file was deleted and the tree re-verified clean.)

The phase did not miss this through carelessness so much as through an inherited scope: the
audit surveyed `intake_routes.py`, CONTEXT § 1 recorded that survey as the list, and D-23.1-02
then pinned the work to "exactly the nine verbs in section 1's table". Every plan executed its
scope faithfully. The scope was one file too narrow. That is the classic
task-complete / goal-missed shape, and it is exactly what goal-backward verification is for.

**Not deferred to a later phase.** Phase 24 does touch `research_routes.py:258` and the
re-trigger affordance, and its goal is phrased "A superadmin can deliberately re-run…" — but it
names no role-gate success criterion, so deferring on that basis would be reading intent into a
tangential match. It is, however, the natural place to close this if the operator prefers not
to open a 23.2.

**Everything else in the phase holds.** Eleven of twelve truths verified against the tree and,
where runnable, against live execution. All four requirements are delivered except SEC-01's
completeness. Zero debt markers. Zero provider spend, confirmed from the audit bucket rather
than asserted. The client surface — the single most likely damage path — is provably intact:
the pin suite is byte-identical to plan 02's commit, all ten routes measured open, and the
TENANT-02 regression set is untouched and green.

**And nothing here has run in production.** Live revisions are still `nestor-api-00047-ghp`
and `nestor-frontend-00035-zz2`, both from 2026-08-31, i.e. the pre-phase build. Migrations
0014 and 0015 exist only in the tree. Neither new CI config has executed in Cloud Build. Local
green is evidence about the code; it is not evidence about the deployment, and this report does
not treat it as such.

---

_Verified: 2026-09-04T16:18:26Z at `fb518c1`_
_Verifier: Claude (gsd-verifier) — measured in-session; no SUMMARY claim accepted as evidence_

---

## RESOLUTION APPENDED 2026-09-04 (after this report was written)

The single gap this report found — **`POST /intakes/{intake_id}/research` ungated** — was closed
immediately, and a second instance of the same class was found and closed with it. The verdict
above (`gaps_found`) was accurate WHEN WRITTEN and is retained unedited; this section records
what happened next rather than rewriting history.

- **Plan 17** (`5d65f98`) gated `trigger_research` with `superadmin_gate` declared BEFORE
  `get_tenant_repo`. Measured RED first: a role=`user` in the intake's own space got **202**, the
  intake flipped to `in_research`, one `research_runs` row was inserted, and `run_poll_driver`
  was **dispatched** — the ~$45 run, authorized by a non-operator. Post-fix both caller shapes
  get exactly 404 with zero rows, no status flip and an empty driver recorder. The ordering
  property is now pinned by a mutation test that re-registers the real handler with the
  parameters swapped and observes the live 403 existence oracle.
- **Plan 18** (`45e6196`) gated `stream_research_run` — operator-approved after this report, once
  the stated reason for excluding it (that `EventSource` cannot send an Authorization header) was
  measured FALSE: the codebase contains no `EventSource` at all, and `research.ts:566-577` opens
  the stream with `fetch()` and a Bearer token. Its RED: a role=`user` received the complete
  operator frame — stage trace, cost, chain-guard state, event cursor.

**`research_router` is now 11 of 11 gated**, proved by walking the live resolved dependency graph
rather than by grep. Suite **608 passed / 2 skipped / 0 failed**.

Two things plan 18 added that outlast this phase:

1. **`test_every_research_route_is_gated`.** The pre-existing ordering guard counts routes that DO
   resolve the gate, so a twelfth ungated route would leave the count at 11 and pass — **the exact
   shape of miss that produced this gap.** The new test computes the ungated set from the live
   router and names every offender.
2. **A defect-pinning test was found and corrected.** `test_stream_null_space_403` had pinned the
   403 existence oracle as expected behaviour — asserting precisely what the fix removes. It was
   strengthened to assert the existence-hidden 404, matching its two siblings in the same file.

**Still true and unchanged by any of this: nothing has been observed running in production.** The
deploy, both migrations, the two Cloud Build runs and the live 404/200 observation remain
operator work.
