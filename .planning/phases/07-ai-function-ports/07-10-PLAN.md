---
phase: 07-ai-function-ports
plan: 10
type: execute
wave: 2
depends_on: ["07-09", "07-11"]
autonomous: true
gap_closure: true
requirements: [AI-02]
files_modified:
  - frontend/src/lib/api/contextPack.ts
  - frontend/src/lib/api/skillRuns.ts
  - frontend/src/components/intake/ContextPackBlock.tsx
  - frontend/src/components/intake/IntakeForm.tsx
  - frontend/src/routes/admin.pulse.intakes.$id.tsx

must_haves:
  truths:
    - "The generated context pack markdown renders in ContextPackBlock (latest + history) after a run succeeds"
    - "Context-pack generation shows a running/progress affordance and refreshes on completion, like apply-intake-skill"
    - "The client validation proposals loader consumes only the apply-intake-skill run, not a context-pack run"
    - "The admin review-consume effect only enters review mode for an apply-intake-skill run"
  artifacts:
    - path: "frontend/src/lib/api/contextPack.ts"
      provides: "getContextPack seam over the new GET /intakes/{id}/context-pack"
      contains: "context-pack"
    - path: "frontend/src/components/intake/ContextPackBlock.tsx"
      provides: "loadLatest/loadHistory wired to the real read; running progress affordance"
      contains: "getContextPack"
  key_links:
    - from: "ContextPackBlock.loadLatest"
      to: "GET /intakes/{id}/context-pack"
      via: "getContextPack(intakeId)"
      pattern: "getContextPack"
    - from: "IntakeForm proposals loader"
      to: "apply-intake-skill run discriminator"
      via: "runs.find(r => r.skill === 'apply-intake-skill' && r.status === 'succeeded')"
      pattern: "skill === \"apply-intake-skill\""
---

<objective>
Wire the frontend to the read surface and discriminator that 07-09 adds, and give context-pack
runs the same progress UX apply-intake-skill has. Three major/minor UAT gaps close here:

1. "Artifacts-read endpoint + Context Pack display" (major, frontend half) — `ContextPackBlock`
   `loadLatest`/`loadHistory` are stubbed (return null/[]). Point them at the new
   `GET /intakes/{id}/context-pack` so the pack markdown renders (Bekijk laatste / Download PDF /
   history list) after a run finishes.
2. "Context-pack run progress UX" (major) — the context-pack CTA dispatches 202 then goes
   silent. The admin route ALREADY sets `optimisticRunStartedAt` + `skillLoading` on
   onGenerateContextPack (07 inline fix), and `useActiveSkillRun`'s SSE/poll already watches the
   latest run regardless of skill type. The gap is that the running affordance
   (RunningClock / auto-refresh on terminal) is only rendered in the awaiting_skill_run banner
   phase, not awaiting_context_pack. Render the same progress affordance for the context-pack run
   and reload the pack on terminal.
3. "Run-list skill discriminator" (minor, consumer half) — with 07-09 projecting `skill`, fix
   the two consumers that assume the newest succeeded run is apply-intake-skill:
   `IntakeForm` (client validation proposals loader) and the admin review-consume effect.

Purpose: makes the pack visible + the context-pack run legible + the proposal/review consumers
correct now that multiple skills produce succeeded runs.
Output: contextPack.ts seam, real loadLatest/loadHistory, a context-pack progress affordance,
`skill` on the SkillRun frontend type + discriminated consumers.

DEPENDS ON 07-09 (GET /context-pack + `skill` on SkillRunView) and 07-11 (shares
admin.pulse.intakes.$id.tsx — run after it to avoid a merge conflict). The backend change from
07-09 must be DEPLOYED (Cloud Build image rebuild) before this is testable live; the frontend
code can be authored against the contract regardless.

ENVIRONMENT: frontend on local vite (npm, localhost:8081) against live Cloud Run. Conventions
(CLAUDE.md): Prettier printWidth 100, double quotes, trailing commas, `@/` imports, sonner
toasts, ApiResult return-no-throw, no shadcn ui/ edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-ai-function-ports/07-UAT.md
@.planning/phases/07-ai-function-ports/07-09-PLAN.md

<interfaces>
<!-- Contracts from 07-09 (backend) + the existing frontend seam. Build against these. -->

07-09 adds (backend):
  GET /intakes/{intake_id}/context-pack → { latest: ContextPackView | null, history: ContextPackView[] }
  ContextPackView = { id: string, text_content: string | null, created_at: string | null, notes: string | null }
  (empty read {latest: null, history: []} for no-pack / out-of-scope.)
  SkillRunView now carries `skill: string` (e.g. "apply-intake-skill", "context-pack",
  "structure-answers", "extract-insights", "generate-embeddings", "transcribe-audio").

Existing frontend SkillRun type (frontend/src/lib/api/skillRuns.ts):
  export type SkillRun = { id: string; status: string; applied_at: string|null; completed_at: string|null };
  → ADD `skill: string` to mirror the widened SkillRunView. latestPhaseInput maps only
  {status, applied_at} into PhaseSkillRunInput, so adding `skill` does NOT affect derivePhase.

ContextPackBlock (frontend/src/components/intake/ContextPackBlock.tsx):
  Pack type = { id, output: string|null, cost_estimate_usd: number|null, completed_at: string|null, model: string|null }
  loadLatest (~198) currently sets latestPack=null; loadHistory (~216) sets history=[].
  The modal + PDF export + "Bekijk laatste"/"Download PDF"/history UI already exist and key off
  `latestPack.output` (markdown) and `completed_at`. VISIBLE_STATUSES gate: validated_by_client,
  decomposed, in_research, delivered.
  NOTE: ContextPackView projects `text_content` (not `output`), `created_at` (not `completed_at`),
  and has no cost/model. Map text_content→output, created_at→completed_at, cost_estimate_usd→null,
  model→null when building the Pack (the pack markdown is what the UI renders; cost/model are
  cosmetic metadata already tolerant of null).

Admin route (frontend/src/routes/admin.pulse.intakes.$id.tsx):
  onGenerateContextPack (~575) already sets optimisticRunStartedAt + skillLoading (07 inline fix).
  bannerActiveRun (~228) + useActiveSkillRun (~226) already track the latest run's status via SSE/poll.
  The terminal-refresh effect (~653) already calls load()+loadSkillRuns() on a terminal status.
  NextStepBanner awaiting_context_pack case (~208) renders only the "Genereer Context Pack" button
  (busy state), NOT a RunningClock — that is the progress-UX gap.

IntakeForm proposals loader (frontend/src/components/intake/IntakeForm.tsx ~108-123):
  const runsRes = await listSkillRuns(payload.intake.id);
  const latest = runsRes.data.runs.find((r) => r.status === "succeeded") ?? runsRes.data.latest;
  if (!latest || latest.status !== "succeeded") return;
  const fullRes = await getSkillRunFull(payload.intake.id, latest.id);
  → the `find` must ALSO require r.skill === "apply-intake-skill" (proposals come only from that run).

Admin review-consume effect (admin.pulse.intakes.$id.tsx ~631): fires when activeRun flips to
succeeded and fetches output_parsed to enter review mode. shouldFetchFull (~274) gates on
phase === "awaiting_review". Guard the review entry so it only consumes an apply-intake-skill run
(the activeRun is the latest run; a context-pack succeeded run must NOT trigger review mode).
`useActiveSkillRun`'s ActiveSkillRun does not carry `skill` today — thread it through if needed,
or gate on phase (awaiting_review is only reachable off an apply run) — prefer the phase gate if
it already excludes context-pack runs, else add `skill` to the ActiveSkillRun mapping.

NextStepBanner (frontend/src/components/intake/NextStepBanner.tsx): RunningClock component
(~101) already exists and takes { triggeredAt }. The banner receives activeRun; reuse
RunningClock in the awaiting_context_pack case when activeRun?.status === "running".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: contextPack.ts seam + wire ContextPackBlock loadLatest/loadHistory; add skill to SkillRun</name>
  <read_first>
    - frontend/src/lib/api/skillRuns.ts (SkillRun type + apiFetch pattern to mirror)
    - frontend/src/lib/api/skills.ts (seam module style)
    - frontend/src/components/intake/ContextPackBlock.tsx (Pack type + loadLatest ~198 + loadHistory ~216 + the modal/PDF consumers)
    - 07-09-PLAN.md (ContextPackView contract)
  </read_first>
  <action>
    Create frontend/src/lib/api/contextPack.ts mirroring skillRuns.ts style (import { apiFetch,
    type ApiResult } from "@/lib/api/client"). Define:
      export type ContextPackView = { id: string; text_content: string | null; created_at: string | null; notes: string | null };
      export type ContextPackRead = { latest: ContextPackView | null; history: ContextPackView[] };
      export function getContextPack(intakeId: string): Promise<ApiResult<ContextPackRead>>
        → apiFetch<ContextPackRead>(`/intakes/${intakeId}/context-pack`, { method: "GET" }).

    In frontend/src/lib/api/skillRuns.ts, add `skill: string;` to the `SkillRun` type (after
    `status`). Do NOT change latestPhaseInput (it maps only {status, applied_at}).

    In ContextPackBlock.tsx:
    - loadLatest: call getContextPack(intakeId). On success map res.data.latest → Pack
      (id, output = text_content, completed_at = created_at, cost_estimate_usd = null,
      model = null) or null; setLatestPack(mapped); setLoadingPack(false). On failure
      (return-no-throw) setLatestPack(null) + setLoadingPack(false) (optionally set error).
    - loadHistory: call getContextPack(intakeId) and setHistory(res.data.history.map(→ Pack)).
    - Remove the `void intakeId; setLatestPack(null)` stub bodies and their "no read surface"
      comments. The existing modal/PDF/history UI keys off `latestPack.output` — the mapping
      keeps them working. Keep double quotes / printWidth 100 / `@/` imports.
  </action>
  <verify>
    <automated>[ "$(grep -c 'getContextPack' frontend/src/components/intake/ContextPackBlock.tsx)" -ge 2 ] && [ "$(grep -c 'skill: string' frontend/src/lib/api/skillRuns.ts)" -eq 1 ]</automated>
    <manual>No frontend test harness — assert visually on local vite (npm, localhost:8081) that the pack markdown renders in ContextPackBlock (Bekijk laatste / history) once the 07-09 backend image ships. Confirm the stub "no read surface" comment is gone (only the error-path setLatestPack(null) remains).</manual>
  </verify>
  <done>getContextPack seam exists; ContextPackBlock renders the real pack markdown + history; SkillRun carries `skill`.</done>
</task>

<task type="auto">
  <name>Task 2: Context-pack running-progress affordance + terminal reload of the pack</name>
  <read_first>
    - frontend/src/components/intake/NextStepBanner.tsx (awaiting_context_pack case ~208 + RunningClock ~101)
    - frontend/src/routes/admin.pulse.intakes.$id.tsx (onGenerateContextPack ~575, bannerActiveRun ~228, terminal-refresh effect ~653, ContextPackBlock mount ~1171)
  </read_first>
  <action>
    In NextStepBanner.tsx `awaiting_context_pack` case: when `activeRun?.status === "running"`,
    render `<RunningClock triggeredAt={activeRun.triggered_at} />` (and a "Context Pack wordt
    gegenereerd…" body) INSTEAD of the "Genereer Context Pack" button — mirroring the
    awaiting_skill_run case. When not running, keep the existing Genereer button. This reuses the
    Phase-8 progress component with no new machinery. Do NOT change the Props contract.

    In admin.pulse.intakes.$id.tsx: the terminal-refresh effect (~653) already reloads intake +
    skill runs on a terminal status; ensure the ContextPackBlock re-reads its pack when the
    context-pack run terminates. Options (pick the minimal one): pass a `refreshKey` /
    `reloadSignal` prop to ContextPackBlock derived from `activeRunStatus`+`activeRunId` so its
    load effect re-runs on terminal, OR have ContextPackBlock re-run loadLatest when
    intakeStatus flips to "decomposed" (it already depends on intakeStatus in the load effect —
    verify the effect deps include the terminal transition; the route's load() updates
    intake.status → decomposed, which re-renders ContextPackBlock with a new intakeStatus and
    its useEffect on [intakeStatus] re-runs loadLatest). Prefer the intakeStatus-driven reload
    since the effect dependency already exists; add an explicit re-read only if the status does
    not change (a re-generate on an already-decomposed intake). For the re-generate case, thread
    the terminal activeRunId as a prop so the load effect re-runs. Keep ApiResult no-throw.
  </action>
  <verify>
    <automated>[ "$(grep -c 'RunningClock' frontend/src/components/intake/NextStepBanner.tsx)" -ge 2 ]</automated>
    <manual>No frontend test harness — verify on local vite that the awaiting_context_pack banner shows a RunningClock while the run is running and the pack renders after terminal. Confirm the awaiting_context_pack case references activeRun?.status.</manual>
  </verify>
  <done>The awaiting_context_pack banner shows a RunningClock while the run is running, and the pack renders after the run terminates (via the decomposed status re-read or the terminal reload signal).</done>
</task>

<task type="auto">
  <name>Task 3: Discriminate apply-intake-skill in the proposals loader + review-consume effect</name>
  <read_first>
    - frontend/src/components/intake/IntakeForm.tsx (proposals loader ~108-123)
    - frontend/src/routes/admin.pulse.intakes.$id.tsx (review-consume effect ~631, shouldFetchFull ~274)
    - frontend/src/components/intake/SkillRunProgress.tsx (ActiveSkillRun type + toActiveSkillRun ~21 — thread `skill` if the review gate needs it)
  </read_first>
  <action>
    In IntakeForm.tsx proposals loader: change the run selection to require the apply-intake-skill
    skill AND succeeded status:
      const latest = runsRes.data.runs.find(
        (r) => r.skill === "apply-intake-skill" && r.status === "succeeded",
      );
      if (!latest) return;
    (drop the `?? runsRes.data.latest` fallback — with the discriminator present, a context-pack
    or structure-answers run must never be mistaken for the proposals source). The subsequent
    getSkillRunFull(intake.id, latest.id) stays.

    In admin.pulse.intakes.$id.tsx review-consume effect (~631): ensure review mode is entered
    ONLY for an apply-intake-skill run. If the phase gate (`phase === "awaiting_review"` on
    shouldFetchFull) already excludes context-pack runs (awaiting_review is reached only after an
    apply run, per derivePhase), document that and leave the gate. If a context-pack succeeded run
    could reach this effect, thread `skill` through: add `skill: string` to ActiveSkillRun,
    populate it in `toActiveSkillRun` (from the SkillRun `skill` field added in Task 1), and guard
    the effect with `if (activeRun.skill !== "apply-intake-skill") return;`. Prefer the explicit
    skill guard for safety since the latest run can now be a context-pack run.
  </action>
  <verify>
    <automated>[ "$(grep -c 'skill === "apply-intake-skill"' frontend/src/components/intake/IntakeForm.tsx)" -ge 1 ] && [ "$(grep -c 'r.status === "succeeded"' frontend/src/components/intake/IntakeForm.tsx)" -ge 1 ]</automated>
    <manual>No frontend test harness — the grep asserts both conditions are present in the find; verify on local vite that a context-pack succeeded run does not enter review mode or drive proposals.</manual>
  </verify>
  <done>The proposals loader and the review-consume effect act only on the apply-intake-skill run, never a context-pack/enrichment run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → GET /intakes/{id}/context-pack | reads the tenant-scoped pack markdown (server existence-hides cross-tenant per 07-09) |
| admin browser → run discriminator | selects which succeeded run drives proposals/review UX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-10-01 | Information Disclosure | getContextPack read | mitigate | The read authz lives server-side (07-09 ResearchArtifactRepository `_scope` + existence-hidden empty read). The frontend seam adds no bypass — it sends only intakeId in the path and renders whatever the scoped endpoint returns (empty for a stranger). |
| T-7-10-02 | Tampering | wrong-skill run drives proposals/review | mitigate | The discriminator (`skill === "apply-intake-skill"`) prevents a context-pack/structure-answers succeeded run from being mistaken for the proposals/review source — the exact ambiguity the UAT logged. Fixes a correctness/trust bug, not a privilege bug. |
| T-7-10-03 | Denial of Service | ContextPackBlock reload loop | mitigate | The reload is driven by an intakeStatus transition or a one-shot terminal signal (guarded like the existing refreshedRunRef pattern), not an unconditional poll — no busy loop. |
| T-7-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — reuses apiFetch, react-markdown, jsPDF, sonner already in the tree. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- The context pack markdown renders (Bekijk laatste / Download PDF / history) after a run succeeds.
- awaiting_context_pack shows a RunningClock while running and refreshes on terminal.
- Proposals loader + review effect act only on the apply-intake-skill run.
- No shadcn ui/ edits; Prettier conventions honored; ApiResult never thrown.
</verification>

<success_criteria>
- contextPack.ts + real loadLatest/loadHistory wired to GET /context-pack.
- RunningClock shown for the context-pack run; pack re-reads on terminal/decomposed.
- `skill` on the frontend SkillRun type; both consumers discriminated to apply-intake-skill.
</success_criteria>

<output>
Create `.planning/phases/07-ai-function-ports/07-10-SUMMARY.md` when done. Note that live
verification of the pack render requires the 07-09 backend change to be DEPLOYED (Cloud Build
rebuild); the frontend is authored against the contract and testable on local vite once the
image ships. Record which reload strategy was used for ContextPackBlock (status-driven vs
terminal signal) and whether `skill` was threaded onto ActiveSkillRun.
</output>
