---
phase: 23.2-authorization-depth-field-level-confidentiality-answer-lifec
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/storage/keys.py
  - backend/app/api/storage_routes.py
  - backend/tests/test_storage_delete.py
autonomous: true
requirements: [SEC-05]

must_haves:
  truths:
    - "A role=user cannot delete a delivered report, even though the API deliberately hands them that report's exact storage path."
    - "A role=user cannot delete a research artifact."
    - "A role=user can still delete the attachments and audio they uploaded — the live client file-remove flow is unaffected."
    - "A superadmin can still delete any category, including reports."
    - "A denied delete removes NOTHING: no GCS call, no intake_sources row deleted, all-or-nothing across the batch."
    - "A key whose third path segment is not a known category is refused, not silently allowed."
  artifacts:
    - path: "backend/app/storage/keys.py"
      provides: "category_of(key) — parses the third path segment of a server-authored key against CATEGORIES"
      exports: ["category_of"]
    - path: "backend/app/api/storage_routes.py"
      provides: "Category authorization in delete_objects, applied to every key before any seam call"
    - path: "backend/tests/test_storage_delete.py"
      provides: "The report/artifact denial cases plus the attachments/audio allow cases"
  key_links:
    - from: "backend/app/api/storage_routes.py"
      to: "backend/app/storage/keys.py"
      via: "category_of imported and called per key inside the pre-validation loop"
      pattern: "category_of"
---

<objective>
Close **D-23.2-08**. `23.2-CONTEXT.md` § 4 (F-03): `delete_objects`
(`storage_routes.py:273-312`) validates only that each key starts with
`{space_id}/{intake_id}/`. Reports are written to
`{space_id}/{intake_id}/reports/{uuid}-{name}.pdf` (`intake_routes.py:1468`) — **inside** that
prefix. `GET /intakes/{id}/report` hands the client that exact `storage_path` deliberately, for
the download flow. So a client can delete the operator's delivered report by replaying a path
the API just gave them.

No migration is needed: the key already encodes the category.
`build_object_key` (`storage/keys.py:90-104`) produces
`{space}/{intake}/{category}/{uuid4}-{sanitized_name}` and
`CATEGORIES = frozenset({"attachments", "audio", "artifacts", "reports"})` (`keys.py:35`).

Purpose: the client is handed operator-produced paths by design, and today possession of a path
is authorization to destroy the object behind it.

Output: `category_of()` on `storage/keys.py`; a per-key category authorization in
`delete_objects` that runs BEFORE any seam call; and the tests that pin both the denial and the
still-allowed client flow.

## THE RULE, EXACTLY (D-23.2-08)

| Caller | May delete |
|---|---|
| `role != "superadmin"` | `attachments` and `audio` ONLY — the two a client can upload |
| `role == "superadmin"` | any category |

A denied key is **404** (existence-hidden), the same code and the same message the existing
prefix mismatch already raises. Not 403 — a 403 here tells the caller "that object exists and
is not yours", which is the existence oracle D-07 spends the whole codebase avoiding.

A key whose third path segment is not in `CATEGORIES` is **also 404** for everyone. It cannot
have been authored by `build_object_key`, so it is either forged or from a shape the server no
longer produces; either way the safe answer is "not found".

## ⚠ NEVER SUBSTRING-MATCH THE CATEGORY

`23.2-CONTEXT.md` § 11 trap 2, and it bites here concretely. `sanitize_filename` keeps
`[A-Za-z0-9._-]`, so a client-chosen filename can legally contain the word `reports` — a key
like `{space}/{intake}/attachments/{uuid}-quarterly_reports.pdf` is a perfectly ordinary client
attachment. `"reports" in key` denies it. Conversely
`{space}/{intake}/reports/{uuid}-summary.pdf` must be denied, and a rule looking for
`"/reports/"` happens to work today but breaks the moment a category is renamed.

**Parse, then compare.** Split the key on `/`, require at least 4 segments, take index 2, and
test membership in `CATEGORIES`. The test suite must include the
`{uuid}-quarterly_reports.pdf` attachment case explicitly — it is the exact substring trap.

## THE ORDER IS LOAD-BEARING

The existing handler already validates EVERY key before deleting ANY (an all-or-nothing prefix
gate, so a forged key in a batch never reaches the seam). Put the category check in that SAME
pre-validation loop, immediately after the prefix assert on the same key. Do not add a second
loop, and do not check the category inside the deletion loop — that would delete the first N
objects and then refuse, which is exactly the partial-destruction the existing comment says the
pre-loop exists to prevent.

`tests/test_storage_cross_tenant.py` already asserts `_no_seam_calls(fake_gcs)` after a denied
delete. Your new denial tests must assert the same thing.

## WHAT THIS PLAN MUST NOT DO

- **Do NOT gate the route.** `DELETE /intakes/{intake_id}/storage/objects` stays reachable by
  `role=user` — it is in `_PINNED_INTAKE_ROUTES` and it backs the live client file-remove flow
  (`FieldRenderer.tsx:488`). This plan changes what the route ACCEPTS, never who may call it.
- **Do NOT touch the upload route's category handling.** `upload_file` (`storage_routes.py:141`)
  accepts all four categories from any caller. That is an adjacent observation, it is NOT in
  D-23.2-08, and CONTEXT § 9 makes scope creep the failure mode this project keeps paying for.
  Report it in the SUMMARY as an observation; do not fix it.
- **Do NOT touch `create_signed_url`.** Handing a client a signed READ url for their own
  report is the delivery flow working as designed (`intake_routes.py:1699` docstring), and
  `GET /intakes/{id}/storage/signed-url` is pinned open. Read is not delete.
- **Do NOT change the prefix assert or `_normalize_intake_id`.** You are adding a check, not
  replacing one.
- **Do NOT change `CATEGORIES`.** Four categories, unchanged.

## IF AN ACCEPTANCE CRITERION HERE IS FALSE

If any criterion in this plan is false, vacuous, or already green at HEAD, **report it with the
measured number** and do not contort the code to satisfy it. A criterion already green at HEAD
is decoration, not a gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

<!-- THE AUTHORITY. § 4 (F-03) and D-23.2-08. -->
@.planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/23.2-CONTEXT.md

<interfaces>
Verified at `922fd91`.

`backend/app/storage/keys.py:35` and `:90-104`:

```python
CATEGORIES = frozenset({"attachments", "audio", "artifacts", "reports"})

def build_object_key(space_id: str, intake_id: str, category: str, filename: str) -> str:
    if category not in CATEGORIES:
        raise ValueError(...)
    return f"{space_id}/{intake_id}/{category}/{uuid.uuid4()}-{sanitize_filename(filename)}"
```

`backend/app/api/storage_routes.py:273-312` — the whole handler you are editing:

```python
@storage_router.delete("/{intake_id}/storage/objects")
def delete_objects(
    intake_id: str,
    body: DeleteObjectsBody,
    identity: Identity = Depends(get_current_identity),
    repos: tuple = Depends(get_intake_and_source_repos),
) -> DeleteResult:
    intake_repo, source_repo = repos
    intake_id = _normalize_intake_id(intake_id)
    intake = intake_repo.get(intake_id)
    if intake is None:
        raise HTTPException(status.HTTP_404_NOT_FOUND, "Intake not found")
    prefix = f"{intake.space_id}/{intake_id}/"
    for key in body.paths:                      # <-- the pre-validation loop
        if not key.startswith(prefix):
            raise HTTPException(status.HTTP_404_NOT_FOUND, "Object not found")
    removed = 0
    for key in body.paths:                      # <-- the deletion loop
        gcs.delete_object(key)
        source_repo.delete_by_storage_path(intake_id, key)
        removed += 1
    return DeleteResult(removed=removed)
```

`identity` is ALREADY in the signature — no new `Depends` is needed, so CONTEXT § 11 trap 3
(signature order on gated routes) does not apply to this plan. Do not reorder the signature.

`backend/app/api/intake_routes.py:1468` — where a report key is built:
`build_object_key(str(intake.space_id), intake_id, "reports", body.file_name)`.

Frontend callers (for the blast radius, no frontend edit is required):
* `frontend/src/components/intake/FieldRenderer.tsx:470` — the CLIENT form uploads
  `isAudioField ? "audio" : "attachments"`, and `:488` calls `storage.removeFile` on those keys.
  These two categories are exactly the allow-list.
* `frontend/src/components/intake/FinalReportBlock.tsx:111` — uploads `"reports"`, rendered
  only from `admin.pulse.intakes.$id.tsx:1338` (superadmin).
* `frontend/src/routes/admin.pulse.intakes.$id.tsx:964` — `storage.removeFile` on answer-field
  file objects, as superadmin.
</interfaces>

<environment>
⭐ **The backend suite RUNS LOCALLY against a real Postgres.** Docker daemon **29.6.2** is up
and `pgvector/pgvector:pg16` testcontainers work. Older planning notes saying "no Docker on this
box" are stale.

    PY="C:/Users/ajimimo/AppData/Local/Temp/claude/C--Users-ajimimo-Desktop-MOELD-nestor-intake-gcp/1394013e-049c-41ee-93f5-4e994f8182a3/scratchpad/venv-be/Scripts/python.exe"

There is **no `requirements.txt`** — `pip install -e "backend[dev]"` from `backend/pyproject.toml`.

**MEASURED BASELINE at `922fd91`:** `cd backend && $PY -m pytest -q` →
**608 passed, 2 skipped, 0 failed**. Measure your own worktree baseline first and record it.

NO PROVIDER CALL. NO DEPLOY. `fake_gcs` is the seam double — no GCS traffic.
</environment>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: category_of() and the failing denial tests</name>

  <files>
    backend/app/storage/keys.py
    backend/tests/test_storage_delete.py
  </files>

  <read_first>
    - `backend/app/storage/keys.py` in full (104 lines) — `CATEGORIES`, `sanitize_filename`,
      `build_object_key` and their docstring register.
    - `backend/tests/test_storage_delete.py` in full — `_seed_intake_with_audio_object`
      (`:72-95`, seeds an `audio` key plus the matching `intake_sources` row) and
      `test_delete_cleans_ref` (`:123-160`), the harness you extend.
    - `backend/tests/test_storage_cross_tenant.py:240-340` — `_no_seam_calls(fake_gcs)`, the
      "nothing reached the seam" assertion your denial tests must reuse.
  </read_first>

  <behavior>
    `category_of(key)`:
    - `category_of("s/i/reports/uuid-x.pdf") == "reports"`.
    - `category_of("s/i/attachments/uuid-quarterly_reports.pdf") == "attachments"` — THE
      SUBSTRING TRAP. A rule that answers `"reports"` here is wrong.
    - `category_of("s/i/audio/uuid-gesprek.m4a") == "audio"`.
    - `category_of("s/i/artifacts/uuid-pack.md") == "artifacts"`.
    - `category_of("s/i/nonsense/uuid-x") is None` — third segment not in `CATEGORIES`.
    - `category_of("s/i/reports") is None` — only 3 segments, no filename.
    - `category_of("") is None`, `category_of("///") is None` — never raises.
    - ANTI-VACUITY: parametrize the four positive cases over `sorted(CATEGORIES)` and assert
      `checked == 4`, so a `category_of` that always returns `None` cannot pass by accident.

    Route behaviour (all as `role=user`, on an intake in their OWN space, seeded via the
    existing harness):
    - DELETE a `reports/` key -> EXACTLY **404**, `_no_seam_calls(fake_gcs)`, and the
      `intake_sources` row count for the intake is unchanged.
    - DELETE an `artifacts/` key -> EXACTLY **404**, same two side-effect assertions.
    - DELETE an `audio/` key -> **200** and the object AND its `intake_sources` row are gone
      (this is `test_delete_cleans_ref` — it must stay green unmodified).
    - DELETE an `attachments/` key whose FILENAME contains `reports` -> **200**. The substring
      trap, at the route level.
    - MIXED BATCH `[audio_key, reports_key]` -> EXACTLY **404** and `_no_seam_calls(fake_gcs)`:
      the audio object must still exist. This is the all-or-nothing criterion; a category check
      placed in the deletion loop deletes the audio object first and fails here.
    - DELETE a key with an unparseable third segment (`{space}/{intake}/x/y`) -> **404** for
      role=user AND for superadmin.
    - As **superadmin**: DELETE the same `reports/` key -> **200** and the object is gone.
  </behavior>

  <action>
    Write the tests FIRST. Run them and record which ones are RED at `922fd91` — the four route
    denial cases (`reports`, `artifacts`, mixed batch, unparseable) will return **200** today,
    because the prefix gate admits them. Paste the observed status codes into the SUMMARY.
    `category_of`'s tests will fail on import.

    Then add `category_of` to `storage/keys.py`, directly under `build_object_key` (it is that
    function's inverse and belongs beside it):

    - Split on `"/"`. Require `len(parts) >= 4` so a key with no filename segment is refused.
    - Take `parts[2]`; return it if it is in `CATEGORIES`, else `None`.
    - Never raise. Accept any `str`; a `None`/non-str input returns `None`.
    - Docstring it as the parse-not-substring rule, naming the
      `attachments/{uuid}-quarterly_reports.pdf` counter-example explicitly so the next reader
      cannot "simplify" it into an `in` check.

    Do not extend the superadmin path or the `role` vocabulary here — the route reads
    `identity.role` and that is all this plan needs.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; $PY -m pytest tests/test_storage_delete.py -v</automated>
  </verify>

  <done>
    `category_of`'s unit tests pass. The route tests still fail (the handler is untouched), and
    the SUMMARY records the observed 200s that prove F-03 is real in this tree.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authorize deletion by category in the pre-validation loop</name>

  <files>
    backend/app/api/storage_routes.py
    backend/tests/test_storage_delete.py
  </files>

  <read_first>
    - `backend/app/api/storage_routes.py:273-312` — the handler, in full, plus its docstring.
    - `backend/app/api/storage_routes.py:60-70` — the module's existing `from app.storage.keys
      import ...` line, which you extend with `category_of`.
  </read_first>

  <behavior>
    Every route case from Task 1 goes GREEN. `test_delete_cleans_ref` and every test in
    `tests/test_storage_cross_tenant.py` stay green UNMODIFIED — the cross-tenant 404s come
    from the prefix/ownership gates that run before yours, so your check must not change their
    codes or their `_no_seam_calls` result.
  </behavior>

  <action>
    Add a module-level constant next to `_MAX_BYTES`:

        _CLIENT_DELETABLE_CATEGORIES = frozenset({"attachments", "audio"})

    with a comment naming D-23.2-08 and the reason: these are the two a client can upload from
    `FieldRenderer.tsx:470`; `artifacts` and `reports` are operator-produced.

    In the EXISTING pre-validation loop, after the prefix assert on the same key:

        category = category_of(key)
        if category is None:
            raise HTTPException(404, "Object not found")
        if identity.role != "superadmin" and category not in _CLIENT_DELETABLE_CATEGORIES:
            raise HTTPException(404, "Object not found")

    Note the polarity: `identity.role != "superadmin"`, not `== "user"`. Deny-by-default — any
    role value that is not exactly the superadmin literal gets the restricted set.

    Reuse the SAME `HTTPException(status.HTTP_404_NOT_FOUND, "Object not found")` the prefix
    mismatch already raises, byte-for-byte. A different message here is a discriminator: a
    caller who can tell "wrong prefix" from "wrong category" apart learns that the object exists
    and is an operator artifact, which is the disclosure the 404 exists to prevent.

    Extend the handler docstring: state the category rule, that a denial is existence-hidden,
    that the check is in the pre-loop for all-or-nothing, and that the category is PARSED (never
    substring-matched) with the `quarterly_reports.pdf` counter-example.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; $PY -m pytest tests/test_storage_delete.py tests/test_storage_cross_tenant.py tests/test_client_surface_open.py -v &amp;&amp; $PY -m pytest -q</automated>
  </verify>

  <done>
    Every behaviour above is green. `tests/test_storage_cross_tenant.py` and
    `tests/test_client_surface_open.py` are green with ZERO edits — record that they were not
    modified (`git status` in the SUMMARY). Full suite at baseline + the new tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `DELETE /intakes/{id}/storage/objects` | An authenticated `role=user` supplies arbitrary object keys, and the API deliberately handed them operator-produced paths |
| API → GCS | `gcs.delete_object` is irreversible; nothing downstream can undo a wrong call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.2-02-01 | Denial of service (destruction) | `delete_objects` — a client replays the `storage_path` from `GET /intakes/{id}/report` and destroys the delivered report | mitigate | `category_of()` parse + `_CLIENT_DELETABLE_CATEGORIES` deny in the pre-validation loop; pinned by the `reports/` 404 test asserting `_no_seam_calls` |
| T-23.2-02-02 | Information disclosure | A 403 (or a distinct message) on a denied category tells the caller the object exists and is operator-owned | mitigate | Identical `HTTPException(404, "Object not found")` reused byte-for-byte from the prefix branch |
| T-23.2-02-03 | Denial of service (destruction) | Category checked inside the deletion loop -> the first keys in a mixed batch are destroyed before the refusal | mitigate | Check lives in the existing pre-validation loop; the mixed-batch test asserts the audio object survives |
| T-23.2-02-04 | Denial of service (availability) | A substring rule denies a legitimate client attachment named `quarterly_reports.pdf`, silently breaking the live file-remove flow with no test going red | mitigate | Parse the third segment; the `quarterly_reports.pdf` case is an explicit test at both the unit and the route level |
| T-23.2-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NOTHING; no dependency is added |
</threat_model>

<verification>
1. `cd backend && $PY -m pytest tests/test_storage_delete.py -v` — all green, including the
   substring-trap case and the mixed batch.
2. `cd backend && $PY -m pytest tests/test_storage_cross_tenant.py tests/test_client_surface_open.py -q`
   — green, with `git diff --stat` showing ZERO changes to either file.
3. `grep -n '"reports" in\|'"'"'reports'"'"' in' backend/app/api/storage_routes.py` returns
   **0 lines** — no substring match crept in.
4. `cd backend && $PY -m pytest -q` — baseline + the new tests.
</verification>

<success_criteria>
- `category_of` exists in `app/storage/keys.py`, parses the third segment, never raises.
- `delete_objects` denies `artifacts` and `reports` to a non-superadmin with an
  existence-hidden 404, in the pre-validation loop, all-or-nothing.
- The client attachment/audio remove flow is untouched (200, object and ref both gone).
- Superadmin can still delete every category.
- `tests/test_storage_cross_tenant.py` and `tests/test_client_surface_open.py` unmodified and
  green.
- The SUMMARY records the observed pre-fix 200s (proof F-03 was real here), and the
  `upload_file` category observation as an explicit NON-fix.
</success_criteria>

<output>
Create `.planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/23.2-02-SUMMARY.md` when done.
</output>
