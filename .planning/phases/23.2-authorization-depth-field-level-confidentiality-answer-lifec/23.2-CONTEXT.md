# Phase 23.2 — CONTEXT (THE AUTHORITY)

**Where this and any other document disagree, THIS FILE WINS.** That includes the source
audit, the plans, the plan summaries and the ROADMAP row.

Base commit: `922fd91` (= `origin/master`, master, tree clean apart from untracked `.claude/`).

---

## 0. What this phase is, and why it is not "more 23.1"

Phase 23.1 answered **"who may call which verb."** It gated 8 operator intake verbs, 7 AI
routes and 11-of-11 research routes, and it pinned ten client routes OPEN so the gate could
not creep.

This phase answers the question 23.1 never asked: **"given a caller who is legitimately
allowed to call this verb, what may they see and what may they change?"**

That is a different axis. Every finding below sits *inside* a route that 23.1 correctly
decided must stay open to `role=user`. None of them is a regression from 23.1 — all seven
predate it, and 23.1's scope never looked for them.

> **The recurring failure this project keeps paying for**: a boundary gets audited along
> ONE dimension, and the table produced by that pass is then treated as the whole map.
> 23.1's CONTEXT section 1 surveyed one file and hid the ~$45 ungated trigger. This
> CONTEXT's section 1 is therefore a MEASURED enumeration of every router, produced by
> reading each router's construction — not a hand-written list. See section 11 for how to
> extend it safely.

---

## 1. THE COMPLETE SURFACE — measured, not recalled

Six routers exist. `auth_router` is public; `protected_router` carries
`Depends(get_current_identity)` and every other router is mounted under it
(`backend/app/main.py:129-174`).

**These numbers are MEASURED**, by walking the live resolved dependency graph of
`app.main.app` with the `_flatten_routes` walker (script kept at
`<scratchpad>/gatewalk.py`). **65 routes total, of which 61 can carry a gate; 26 do.**

⚠ **Corrected by the plan-11 executor**: an earlier draft said "65 routes total" without
qualification. Four of the 65 are Starlette's own doc routes (`/openapi.json`, `/docs`,
`/docs/oauth2-redirect`, `/redoc`); they have no `dependant` and **cannot** carry a gate, so
quoting 65 as the gate-bearing population overstates it. The gate-bearing population is **61**.
Every other figure below (26 gated, `/intakes` 43 = 26 + 17, and every per-router split) was
measured correctly and is unchanged.

| Router | File | Prefix | Gated / total | Mechanism | Reachable by `role=user`? |
|---|---|---|---|---|---|
| `auth_router` | `api/auth_routes.py:51` | `/auth` | 0 / 1 | public by design | yes — pre-identity |
| `admin_router` | `api/admin_routes.py:84` | `/admin` | **0 / 13** | **`get_admin_session` — 403 before any session opens** (`db/session.py:372-375`) | NO |
| `intake_router` | `api/intake_routes.py:92` | `/intakes` | **8 / 22** | per-route `superadmin_gate` | partially — see below |
| `research_router` | `api/research_routes.py:126` | `/intakes` | **11 / 11** (23.1) | per-route `superadmin_gate` | NO |
| `ai_router` | `api/ai_routes.py:80-84` | `/intakes` | **7 / 7** | ONE router-level `Depends(superadmin_gate)` | NO |
| `storage_router` | `api/storage_routes.py:63` | `/intakes` | **0 / 3** | **none** | YES, all three |
| `me_router` | `api/me_routes.py:59` | (none) | 0 / 2 | self-scoped only | yes |
| FastAPI/app built-ins | — | — | 0 / 6 | `/docs`, `/openapi.json`, `/healthz`, `/readyz`, `/redoc` | public |

### ⚠ TWO CORRECTIONS TO AN EARLIER DRAFT OF THIS TABLE — read them, they are the point

1. **`intake_router` is 8 of 22 gated, NOT "17 per-route gates".** The 17 came from
   `grep -c "superadmin_gate" intake_routes.py`, which counts the import line, the
   docstrings and the comments as well as the actual `Depends(...)` bindings. That is a
   **grep artifact** — precisely the failure this CONTEXT warns about in section 11.1, made
   by the author of that warning. Eight is the number of routes a `role=user` is denied.
2. **`admin_router` carries ZERO `superadmin_gate` dependencies** — the first walk showed
   `0 gated / 13 total` and that looks alarming. It is not a hole: every admin route depends
   on `get_admin_session`, which raises **403 "Superadmin only"** before selecting an engine
   or opening a transaction (`db/session.py:372-375`, T-5-13). So admin routes are closed,
   by a *different* mechanism.

   Note the asymmetry, and do NOT "fix" it in this phase: `/admin/*` denies with **403**
   while the intake operator verbs deny with **404** (existence-hidden, D-07). That is
   defensible — the existence of `/admin/spaces` is not a secret, whereas the existence of a
   specific intake is. It is recorded here so a future audit does not read it as a defect.

`backend/tests/test_client_surface_open.py` holds `_PINNED_INTAKE_ROUTES`, a
machine-generated inventory of all **43** `/intakes` routes, plus a reusable
`_flatten_routes(routes, prefix, inherited)` walker that resolves `_IncludedRouter`
placeholders and returns include-level dependencies alongside each leaf. **Use that walker
to prove a gate. Never prove a gate with `grep`.**

### The client-reachable set this phase must reason about — all 17, measured

Every `/intakes` route a `role=user` can actually reach today:

| Route | Relevance to this phase |
|---|---|
| `GET /intakes` | unpaginated + Python-side filter — DEF-23.2-07 |
| `POST /intakes` | never sets `template_id` — feeds F-08 |
| `GET /intakes/templates` | canonical schema **including the admin-only section** — D-23.2-04 |
| `GET /intakes/{id}` | — |
| `PATCH /intakes/{id}` | `client_name` only; benign |
| `GET /intakes/{id}/answers` | **F-01 hop 1** — every answer row, unfiltered |
| `PATCH /intakes/{id}/answers` | **F-02** — ownership check only |
| `GET /intakes/{id}/context-pack` | **F-01 hop 3** — pack text laundered from all answers |
| `GET /intakes/{id}/report` | hands the client the report `storage_path` — feeds F-03 |
| `GET /intakes/{id}/skill-runs` | list view carries no `output_parsed`; safe |
| `GET /intakes/{id}/skill-runs/stream` | SSE; status only |
| `GET /intakes/{id}/skill-runs/{run_id}` | **F-01 hop 2** — raw `output_parsed` |
| `GET /intakes/{id}/sources` | — |
| `POST /intakes/{id}/storage/uploads` | client upload |
| `GET /intakes/{id}/storage/signed-url` | prefix-asserted read |
| `DELETE /intakes/{id}/storage/objects` | **F-03** — prefix-only authorization |
| `POST /intakes/{id}/submit` | lifecycle verb; transition map is allow-listed and correct |

**The 23.1 pin covers TEN of these seventeen.** The seven unpinned ones, **derived by
subtracting the pinned ten from the reachable seventeen** — do not copy this list, re-derive
it, because an earlier draft of this very paragraph said "seven" and then listed only six:

| Unpinned but open | Expected code |
|---|---|
| `POST /intakes` | **201** (`intake_routes.py:443`) |
| `PATCH /intakes/{id}` | 200 |
| `GET /intakes/{id}/context-pack` | 200 (scoped-empty by design, `:630`) |
| `GET /intakes/{id}/skill-runs/stream` | 200 — **SSE, see the warning below** |
| `GET /intakes/{id}/sources` | 200 (scoped-empty by design, `:664`) |
| `POST /intakes/{id}/storage/uploads` | **201** (`storage_routes.py:135-137`) |
| `DELETE /intakes/{id}/storage/objects` | 200 — **the one the earlier draft omitted** |

Widening the pin to all seventeen is a reasonable, cheap addition for the wave-5 plan — but
it is NOT licence to change any of their behaviour. Two rules for whoever does it:

* **`skill-runs/stream` is SSE.** `TestClient.get` on a streaming response can block; use
  `client.stream(...)` or whatever `tests/test_sse_stream.py` already established.
  **Sixteen honest pins plus one documented exclusion beats seventeen with a flaky test.**
* **A red pin means an earlier plan in this phase regressed something.** Stop and name it.
  Never change production code to make a new pin go green.

Already closed by 23.1 and therefore NOT a leak path: `GET /intakes/{id}/search`
(`ai_router`, router-level gate) and every `research_router` read, including
`GET /intakes/{id}/research/stream`. Note that `skill-runs/stream` (intake router, open) and
`research/stream` (research router, gated) are DIFFERENT routes — do not conflate them.

---

## 2. F-01 — Admin-only content reaches clients, through FOUR paths not one

`backend/app/data/pulse_intake_v1.json:881` carries `"admin_only": true` at **SECTION**
level on `strategic_perspective`, whose own description reads *"Visible only to admin, not
to the client and not in the handoff PDF."* The section holds exactly four fields:
`bias_radar`, `blind_spots_upstream`, `blind_spots_downstream`, `blind_spots_perspectief`.

Enforcement today is **client-side only**: `IntakeForm.tsx:164` (`if (s.admin_only) return
false`) and `intake.$id.results.tsx:163`.

### The propagation chain — the audit found hops 1 and 2, and stopped

| # | Hop | Evidence | Client-reachable? |
|---|---|---|---|
| 1 | `list_answers` returns every row | `intake_routes.py:558-564` | **yes** |
| 2 | `output_parsed` carries `bias_radar` + `blind_spots` verbatim | `ai/skills/apply.py:17` | **yes** via `:1234` |
| 3 | **Context-pack generation reads ALL answers into the LLM prompt** | `ai/skills/context_pack.py:150-159` then `_format_intake_markdown` | **yes** via `GET /context-pack` |
| 4 | Embeddings index `ResearchArtifact.text_content` (= the pack) | `ai/skills/embeddings.py:89-100` | no — `/search` is gated |
| 5 | The research brief carries `context_pack_text` into Tribunal | `research_routes.py`, `assemble_brief(..., context_pack_text=...)` | no — research reads gated |

**Filtering only `list_answers` leaves hop 3 wide open**, and hop 3 is the worst of them:
once admin-only prose is laundered through the LLM into pack text, no output filter can
reliably remove it.

**D-23.2-01 — filter the INPUT, not the prose.** The context-pack generator must drop
admin-only answers *before* building `user_message`. Output-side scrubbing of generated
prose is not acceptable and must not be attempted.

**D-23.2-02 — one derivation, one helper.** Add `admin_only_field_keys()` to
`app/intake_canonical.py`, deriving the key set from the canonical schema's section-level
`admin_only`. Every consumer imports it. Do NOT hand-write the four keys anywhere: a fifth
admin-only field added to the JSON later must close automatically.

**D-23.2-03 — the projection is by ROLE, not by route.** `role == "superadmin"` sees
everything (the AIReviewPanel depends on it). `role == "user"` gets the filtered
projection. Apply at: `list_answers`, `get_skill_run_full` (`output_parsed` subset),
`list_templates` (drop admin-only sections), and the context-pack input.

**D-23.2-04 — `list_templates` is in scope.** It hands every authenticated caller the
canonical schema including the admin-only section's labels and help text
(`intake_routes.py:482-500`). That is schema disclosure, not answer disclosure — lower
severity, but it tells a client exactly which keys to ask for, and both frontend consumers
already discard the section. Filter it for `role=user`.

---

## 3. F-02 — Answer writes ignore lifecycle, schema membership and field permission

`upsert_answers` (`intake_routes.py:568-598`) verifies intake ownership and **nothing
else**. No status check, no schema-membership check, no admin-only check, no type/constraint
validation. A client can write an unknown field to a `delivered` intake.

### THE TRAP THAT WILL BREAK PRODUCTION IF YOU MISS IT

The intended client policy is **not** "only `draft` is writable."

* `intake.$id.tsx:106` — `editable: intake.status === "draft"`
* `intake.$id.tsx:110-114` — `phase: (status === "reviewed" || status === "validated_by_client") ? "validation" : "intake"`
* `IntakeForm.tsx:501` — `disabled={!editable && !(isValidationPhase && f.type === "proposal_list")}`

So in **`reviewed` AND `validated_by_client`**, the form renders `proposal_list` fields as
ENABLED while everything else is read-only — the client's "keep Nestor's proposal" tick,
shipped 2026-08-31.

### ⚠ CORRECTED by the plan-09 executor — and the correction found a REAL BUG

An earlier draft said a naive draft-only server rule "silently kills a live feature". **That
is wrong at HEAD, because the frontend already kills it.** Measured:

* `saveCurrentSection` (`IntakeForm.tsx:209-210`) opens with `if (!editable) return true;`
  and `editable` is `status === "draft"` (`intake.$id.tsx:106`). In the validation phase the
  section PATCH is **skipped entirely** — and it returns `true`, i.e. reports success.
* `doSubmit` (`:303-308`) gates the transition on that return value, so it advances
  `reviewed → validated_by_client` having persisted nothing.
* `confirmedDiffKeys` is local `useState` and is never transmitted.
* The only other `saveAnswers` callers are `AIReviewPanel.tsx` (×2) and
  `admin.pulse.intakes.$id.tsx:951` — all superadmin admin paths.

**So: a client can tick "keep Nestor's proposal", press Akkoord, and the tick is discarded
while the status advances.** An enabled control whose value is never saved. That is a
user-visible data-loss defect that predates this phase and that nothing tests.

**The server-side exception in D-23.2-05 is still correct and stays** — it is the rule the
frontend fix will need, and writing the server to a draft-only rule would mean fixing the
frontend later requires reopening the backend. But it is **defence in depth, not a live-path
rescue**, and the phase must not claim it saved a working feature.

Recorded as **DEF-23.2-12** (frontend is fenced this phase, section 9.4): the one-line fix is
to let `saveCurrentSection` proceed for `proposal_list` fields during the validation phase.
The server will already accept exactly those writes.

**D-23.2-05 — the server-side write policy, exactly:**

| Intake status | Client (`role=user`) may write |
|---|---|
| `draft` | any field that is in the canonical schema AND not admin-only |
| `reviewed`, `validated_by_client` | **only** fields whose canonical `type` is `proposal_list` |
| `submitted`, `decomposed`, `in_research`, `delivered` | nothing — 409 |

Superadmin is unaffected — the AI-review apply path writes as superadmin
(`intake_routes.py:590-595`, a live-UAT regression fixed on 2026-07-13; do not re-break it).

**D-23.2-06 — rejection codes.** Unknown `field_key` gives 422. An admin-only `field_key`
from a `role=user` gives 404 on the field (existence-hidden, consistent with D-07). Wrong
lifecycle status gives 409. A batch is all-or-nothing: validate every item before writing
any.

**D-23.2-07 — value validation is IN scope but bounded.** Enforce the canonical field's
`type` and its `options` where the schema states them, plus upper bounds (value length,
item count, batch size). Do NOT invent constraints the schema does not carry.

**⚠ CORRECTED — `required` / `min_length` / `min_items` must NOT be enforced at write
time.** An earlier draft of this decision said "required/options". That is wrong and would
break the live client form. `IntakeForm.tsx:214-217` saves only the DIRTY fields and maps
each through `toAnswerInput` (`:26-30`), which sends `{value: null, value_json: null}` for
a null value. Clearing a required field, or saving a section before it is complete, is
NORMAL save-as-you-go. `validateField` (`:33-40`) enforces `required` in the browser at
SUBMIT time, which is where the server-side equivalent belongs too.

**So: the write path always accepts an empty value.** Minimum-constraint enforcement is a
follow-up whose home is `POST /intakes/{id}/submit`, not `PATCH .../answers`.

### The SECOND write-shape trap: `radio` + `allow_text`

`output_form`'s "Anders/Other" option carries `allow_text: true`, and
`FieldRenderer.tsx:302-306` stores that choice as an **object** —
`onChange({choice: opt.value, text: ...})` — which `toAnswerInput` routes to `value_json`,
not to `value`.

A naive `radio => value in options` rule therefore 422s a live client path, and **no
existing test catches it**. The type rule for `radio` must be dual: a plain string in
`value` that is one of `options`, OR an object in `value_json` of shape
`{choice, text}` whose `choice` is one of the options carrying `allow_text`. Both cases
need a test.

This is the same shape as the `proposal_list` trap above. There are **two**, not one — and
both were found only by reading the renderer, not the schema.

---

## 4. F-03 — Clients can delete operator deliverables

`delete_objects` (`storage_routes.py:273-312`) validates only that each key starts with
`{space_id}/{intake_id}/`. Reports are written to
`{space_id}/{intake_id}/reports/{uuid}-{name}.pdf` — **inside**
that prefix. `GET /intakes/{id}/report` hands the client that exact `storage_path`
deliberately, for the download flow (`:1699` docstring).

The fix needs no migration, because the key already encodes a category:
`build_object_key` (`storage/keys.py:90-104`) produces
`{space}/{intake}/{category}/{uuid}-{name}` and
`CATEGORIES = frozenset({"attachments", "audio", "artifacts", "reports"})` (`keys.py:35`).

**D-23.2-08 — deletion is authorized by CATEGORY, not by prefix.** A `role=user` may delete
only `attachments` and `audio` (the two a client can upload). `artifacts` and `reports` are
operator-produced and deny with 404 (existence-hidden). Superadmin may delete any category.
Parse the category as the third path segment and reject a key that does not parse — never
substring-match `"reports"` anywhere in the key.

**D-23.2-17 — the UPLOAD side has the same hole, and is fixed in the same plan.**
`upload_file` (`storage_routes.py:141-162`) takes `category` straight from the form and
validates it only against `CATEGORIES`, with no role check. A `role=user` can therefore
upload into `reports/` — writing objects into the operator's deliverable prefix, and (once
D-23.2-08 lands) creating objects they can no longer delete.

**⚠ CORRECTED, and it makes D-23.2-17 MORE important than argued above.** An earlier draft of
this section said `intake_routes.py:1468` is where a report key is built via
`build_object_key(...)`. That is false. Measured: `intake_routes.py` has **ZERO**
`build_object_key` call sites. `:1468` is `prefix = f"{intake.space_id}/{intake.id}/reports/"`
inside `_assert_report_key` (def `:1455`) — a prefix **guard**, not a key builder. The three real
call sites are `storage_routes.py:194`, `research_routes.py:750` and `research/run_task.py:617`.

Why that strengthens the case: the `reports/` key is authored by **the very `upload_file` this
decision gates**, and `_assert_report_key` then accepts *any* staged path under that prefix. So
before D-23.2-17, a `role=user` could place an object at a path the delivery guard would accept.

**The honest bound**, because severity should not be overstated: the verbs that consume such a
path — `POST /intakes/{id}/report/replace` (`:1595`) and `POST /intakes/{id}/deliver` (`:1665`) —
are **both superadmin-gated** (confirmed by dependency-graph walk). So an operator must still
reference the planted key; this is not a direct client-to-delivery path. It is a real integrity
risk (an operator can be induced to deliver a client-planted file) and it justifies the fix, but
it is not remote code delivery.

Restrict `category` by role: `role=user` may upload only `attachments` and `audio`.
**One shared client-writable constant in `storage/keys.py`, imported by BOTH routes**, so
the upload and delete rules cannot drift apart. An out-of-role category is **422**, not
404 — it is a form value, it matches the existing unknown-category 422 at `:162`, and the
category vocabulary is already spelled out in that route's own error message, so it is not
a secret.

---

## 5. F-04 + F-07 — Deactivation and reactivation are not fail-closed, and share ONE fix

Both cascades write the DB first and call the IdP second, committing the DB either way:

* deactivate — `admin_routes.py:598-640`
* reactivate — `admin_routes.py:675-706`

Consequences, both confirmed:

* **F-04**: `get_current_identity` (`auth/dependencies.py:78-107`) reads `role`/`space_id`
  purely from token claims with **zero DB reads**, and `login_sync`
  (`auth/session.py:205-207`) returns `ALREADY_SYNCED` before touching the DB whenever the
  token already carries a role. So when the IdP disable fails, the DB says
  `space_deactivated` while a valid token keeps working.
* **F-07**: reactivate flips rows to `active` *before* the IdP call and commits. A retry
  selects only `space_deactivated` rows, finds none, and returns 200 having done nothing —
  while the account stays disabled. Deactivate does not have this bug: its `targets`
  deliberately include already-`space_deactivated` rows for exactly this retry
  (`:598-601`).

**D-23.2-09 — invert the ordering on BOTH verbs: IdP first, DB commit second.** On partial
IdP failure the DB is not flipped for the failed members, so the DB never over-claims, the
502 is honest, and a retry re-attempts exactly the members that failed. A crash between the
IdP call and the DB commit leaves members denied with `active` rows — fail-CLOSED, the safe
direction. The audit row must still be written and committed (that was the reason for the
current ordering; preserve the intent, not the sequence).

**D-23.2-10 — `invite_user` and any individual reactivate must reject a deactivated target
space.** `invite_user` (`admin_routes.py:148-200`) checks the space EXISTS but never that it
is active, so today you can invite an active member into a deactivated space.

**D-23.2-11 — the per-request membership check is DEFERRED, deliberately.** Consulting
membership/space status on every request would close the residual token-vs-DB divergence in
general, not just on IdP failure. It is NOT in this phase: it changes the authorization
model from claims-based to session-based, touches every test fixture that builds an
`Identity` without a membership row, and deserves its own phase. **State the residual risk
plainly in the phase summary**: after D-23.2-09 the DB can no longer over-claim, but a token
minted before an *individual* deactivation still authenticates until it expires. That window
is bounded by ID-token TTL (about 1h) and `check_revoked=True` already closes it whenever
the IdP revoke succeeded.

---

## 6. F-05 — Concurrent research triggers duplicate a ~$45 run

`trigger_research` (`research_routes.py:260-410`) reads status and prior attempts on
`repo.session`, then opens a **separate** `tenant_session` that patches status
unconditionally and inserts a run row. `research_runs` carries three plain indexes and
**none is unique** (`db/models/research_runs.py:143-148`).

Two concurrent authorized requests therefore both read the same `prior`, both compute
`attempt = 1`, and both insert and dispatch. This is not an authorization bypass — 23.1's
gate holds and only a superadmin reaches it — but it is uncapped duplicate spend on a path
running with `NESTOR_TRIBUNAL_UNCAPPED=1`.

**D-23.2-12 — three changes, all required; any one alone is insufficient:**

1. **Migration 0016** — partial unique index on `research_runs (intake_id)
   WHERE status IN ('queued', 'running', 'needs_report_spec')`. Mirror 0014's shape
   exactly, including the FORCE-RLS bracket and the duplicate pre-flight resolution (see
   section 11).

   **⚠ Corrected — the obvious two-value predicate is wrong.** `mirror_tick`
   (`research/run_task.py:283`) writes `metrics.get("status")` **verbatim** from the
   engine; the column is not a constrained enum. The full measured vocabulary is
   `queued`, `running`, `cancelled`, `needs_input`, `failed`, `completed`,
   `needs_report_spec`, `completed_degraded`, `parked`. The app-level rule this index
   backs up is `_RETRYABLE_RUN_STATUSES = {"failed","cancelled","needs_input"}`
   (`research_routes.py:151`) — so **`needs_report_spec` is NOT retryable**: a run sitting
   there is alive and awaiting an operator's spec, and `POST /report-spec` re-queues that
   SAME run. An index of only `('queued','running')` would admit a concurrent trigger in
   exactly that state, making the DB backstop disagree with the app rule it exists to
   enforce.

   **⚠ `alembic check` DOES run here — the review's account of BL-1 was half wrong, and the
   real reason to replace it is stronger.** The CLI form (`python -m alembic check`) genuinely
   exits 1 on every tree — `alembic.ini:24` has an empty `sqlalchemy.url` and there is no
   persistent database — so removing it from eight gate slots was right. But the claim that
   "no CI config runs it" is FALSE: `tests/test_ai_dedup.py:541
   test_alembic_check_reports_no_drift` calls `command.check(cfg)` against the testcontainer,
   and it passes against 0016.

   The actual hole is sharper: **alembic 1.18.4's postgresql `compare_indexes` compares the
   unique flag and the expressions only — it NEVER inspects `postgresql_where`.** So a drifted
   partial-index predicate passes `alembic check` silently. `alembic check` was never going to
   catch the one kind of drift this migration can suffer. That is what the new
   `test_orm_declaration_matches_the_deployed_index` closes, by reading
   `pg_indexes.indexdef` and comparing name, uniqueness AND predicate.

   **Pin the correspondence with a test**, not just the index: every status outside the
   index predicate must be in `_RETRYABLE_RUN_STATUSES` or in `RESEARCH_TERMINAL`, with an
   anti-vacuity guard on the enumerated set size. That is what stops the two lists drifting
   when a future engine status is added on one side only.

   **Do not invert it into `NOT IN (terminal)`.** Because statuses are written verbatim, an
   unknown future status would match a negative predicate and block that intake's triggers
   permanently. A positive `IN (...)` fails open on an unknown status, which is the right
   direction with the app-level 409 still in front of it.
2. **CAS the status flip** with the existing `patch_if` (`db/repository.py:120`), expecting
   the `old_status` that was read. `rowcount == 0` gives 409.
3. **Compute `attempt` inside the write transaction**, not from the earlier read.

`patch_if` was built and tested in 23.1 for exactly this class of bug — reuse it, do not
write a second CAS primitive.

**D-23.2-13 — the same read-then-write shape exists in Tribunal** at
`tribunal/nestor_pulse_sdk/runs/api.py:517` (`submit_report_spec`) and `:550` (`resume_run`).
`resume_run`'s docstring asserts *"two concurrent clicks cannot both succeed"* — the code
does a plain read-then-write under READ COMMITTED and does not guarantee that. Fix both, and
**fix the docstring**: a comment claiming a guarantee the code does not provide is worse than
no comment.

---

## 7. F-06 — An email outage erases a successful, paid research run

`write_fn` in `research/run_task.py:800-850` calls `finalize_completed(session, ...)` and
then `resend.send(...)` **inside the same** `tenant_session` block. `run_with_session_release`
(`db/ai_session.py:150-199`) routes any exception to `on_error`, which finalizes the run to
EXACTLY `failed`. So a mail outage rolls back the completion and mislabels a run that
succeeded — encouraging an unnecessary ~$45 rerun.

**D-23.2-14 — apply the pattern this codebase already uses.** `submit_intake`
(`intake_routes.py:1333-1341`) already does this correctly: it sends
`_send_admin_validated` **after** the status flip and audit, with the comment *"a mail
failure must NEVER fail the validate."* Move the research completion mail out of the write
transaction to after it commits, and swallow-and-log a send failure. This is not a new
design; it is the existing house rule applied to the one place that breaks it.

**⚠ It is THREE sends, not one — and the parked branch is the worse of them.** `write_fn`
spans `run_task.py:807-943` and contains `resend.send` at **`:847` (completed), `:913` and
`:938`**. The parked branch carries the identical inversion, and there it is worse: a raise
routes to `on_error` -> `finalize_failed`, relabelling a **parked** run as `failed` and
destroying the resume affordance for research already paid for. Route all three through one
pending-mail mechanism; fixing only the completion send would be arbitrary.

**Do NOT "fix" the fourth `resend.send` at `:958`.** A raw grep shows four. `on_error`
begins at `:944`, so `:958` is on the error path and is ALREADY wrapped in
`try/except Exception: pass` with the comment *"mail is best-effort on the error path"*. It
is correct as it stands. This note exists because the grep count and the defect count
disagree, and the next reader will otherwise either change it or think the plan missed it.

**D-23.2-15 — the transactional outbox is DEFERRED.** The audit recommends one. The defect
here is a correctness *inversion* (success recorded as failure); the outbox is a *durability*
improvement (guaranteed eventual delivery). Fixing the inversion fully closes the finding.
Record the outbox as a deferral with this reasoning, not as an unfinished task.

---

## 8. F-08 — `structure_answers` accepts unfiltered model-invented field keys

`ai/skills/structure_answers.py:137-141` sources valid keys from `intake.template_id` then
`IntakeTemplateRepository`. The normal creation path never sets `template_id`
(`frontend/src/lib/api/intakes.ts:43` sends only `client_name`), so `template_keys` is `[]`.
Line 227 then reads `if valid_keys and field_key not in valid_keys` — **an empty list
disables filtering entirely**, and the model's invented keys are written.

Meanwhile `GET /intakes/templates` serves the canonical in-repo schema to everyone (D-CANON,
`intake_routes.py:482-500`); per-space template rows are no longer read on the client path at
all.

**D-23.2-16 — source `valid_keys` from the canonical schema** and delete the
`if valid_keys and` fallback so an empty set denies rather than permits. Admin-only keys are
legitimate targets here (this skill runs as an operator), so filter to
*canonical-schema-known*, not to *client-visible*.

---

## 9. WHAT NOT TO FIX — scope fences

These are real, and they are deliberately NOT in this phase. Touching them is scope creep.

1. **`research_router` must stay mounted** at `main.py:152` (D-23.1-10), and the LEGACY
   Supabase `run-research` edge function stays prohibited.
   `tests/test_no_run_research_route.py` and `test_scope_guard_run_research.py` enforce
   this — keep them green, do not edit them.
2. **The ten pinned client routes stay OPEN.** `test_client_surface_open.py` (blob
   `b37224a6c385f853adae2f5f9d9d9fd69cb88261`) survived all of 23.1 byte-unchanged. This
   phase changes what those routes RETURN and ACCEPT, so some of its assertions will need to
   move from "200" to "200 with a filtered body". **Change assertions deliberately and say
   so in the plan summary — never let a status-code assertion silently absorb a body
   change.**
3. **`REAP_SQL` in `tribunal/.../worker.py` stays UNFENCED** (23.1 D-23.1-06). Fencing it
   strands abandoned runs in `running` forever.
4. **No frontend redesign.** Frontend changes are limited to what the server contract change
   requires. Oversized route files, the draft-cache namespacing and the dead components are
   section 10 deferrals.
5. **No dependency upgrades.** npm audit currently reports 12 (10 high, 2 moderate) — mostly
   build-time. Out of scope.
6. **`min-instances`, worker behaviour and the cost governor** — untouched.

### Non-goals

* No new client-facing feature.
* No change to the intake status vocabulary or the transition maps.
* **ZERO provider spend. No Tribunal run. No deploy.** This phase ends at "built, tested,
  committed", exactly as 23.1 did.

---

## 10. Deferrals — named, with reasons

| id | Item | Why deferred |
|---|---|---|
| DEF-23.2-01 | Per-request membership/space status enforcement | D-23.2-11 — changes the authorization model; own phase |
| DEF-23.2-02 | Transactional outbox for notifications | D-23.2-15 — durability, not correctness |
| DEF-23.2-03 | Durable background dispatch + reconciler (`BackgroundTasks` survives no restart) | Real, architectural, orthogonal to all seven findings |
| DEF-23.2-04 | Tribunal "ownership lost mid-provider-call": a heartbeat affecting 0 rows does not cancel the pipeline, and the advisory lock is released before execution | 23.1 fenced the WRITES; cancelling in-flight work is a different mechanism |
| DEF-23.2-05 | Versioned/immutable snapshot of approved research inputs | Larger than answer-write policy; D-23.2-05 stops the drift, versioning is the next step |
| DEF-23.2-06 | localStorage draft: user namespacing, revision compare, expiry (`IntakeForm.tsx:106` merges cache OVER server) | Frontend-only; real but not authorization |
| DEF-23.2-07 | Pagination + payload bounds on `list_intakes` (`intake_routes.py:420` filters in Python after an unpaginated `repo.list()`) | Performance/limits, not confidentiality |
| DEF-23.2-08 | npm audit 12 findings; non-reproducible backend pins (`uv:latest`, broad ranges) | Supply chain; needs a runtime-image scan first |
| DEF-23.2-09 | Frontend lint: 28,924 errors, of which exactly **60** are non-formatting (54 `no-explicit-any`, 4 `no-empty`, 2 `prefer-const`) | Measured this session. A repo-wide prettier pass would bury every real diff |
| DEF-23.2-10 | Dead code: `ContextPackPDF.tsx` (zero importers — the `downloadContextPackPDF` hits are a same-named LOCAL function, the substring trap), `ResearchResultsPanel.tsx` (only a comment references it), `supabasePublic` (zero consumers) | Cleanup; verify non-TS entry points first |
| DEF-23.2-11 | Doc contradictions: `AGENTS.md:27` still says "zero automated tests" and `:41`/`:103` call the backend a placeholder | Documentation; one pass, not this phase |

**Still OPEN operator work inherited from 23.1** (not this phase's, but do not let it get
lost): tribunal services not deployed; the live `role=user` 404/200 observation never made;
the tribunal CI gap — `cloudbuild.test-critical.yaml:32` runs four test files and **none** of
the 37 ownership/idempotency tests; tfstate bucket; `app_superadmin` rotation.

---

## 11. Traps, measured this session

1. **Prove a gate by walking the dependency graph.** `_flatten_routes` in
   `test_client_surface_open.py` exists because FastAPI 0.141 does not flatten
   `include_router` at include time — include-level dependencies (which is how `ai_router`
   is gated) never appear in a leaf's `route.dependant`. A grep for `superadmin_gate` in
   `ai_routes.py` finds the router-level line and tells you nothing about any individual
   route.
2. **The substring trap, again.** `downloadContextPackPDF` contains `ContextPackPDF`;
   `"research"` contains `"search"`. Match imports, not substrings.
3. **Signature order is load-bearing.** In `trigger_research` the gate must stay ABOVE
   `get_tenant_repo` or a null-space caller gets a 403 existence oracle instead of a 404
   (`research_routes.py:263-270`). Any new dependency added to a gated route goes AFTER the
   gate.
4. **Migration 0014 is the template for 0016**, including the parts that are easy to skip:
   `NO FORCE ROW LEVEL SECURITY`, then the duplicate-resolution UPDATE, then `FORCE`
   restored in a `finally:`, then `create_index`. `SET row_security = off` **errors** on a
   FORCE-RLS table rather than bypassing it. Production may hold duplicate rows; a migration
   that assumes it does not will abort the deploy.
5. **A test whose seed encodes the defect will assert the defect.** In 23.1,
   `test_ai_context_pack.py` seeded `'reviewed'` and had to move to `'validated_by_client'`
   — 8 insertions, 1 deletion, **zero assertions touched**. Expect the same shape here:
   fixtures that seed `delivered` intakes and then write answers are asserting F-02.
6. **`git commit` with no pathspec commits the whole index.** Use
   `git commit -- <paths>` and verify with `git show --stat`, never a summary.
7. **The worktree stale-base trap has a known root cause now**: agent worktrees are cut from
   `origin/master`, which sat at `a3a0c96` from 2026-07-24 until the 23.1 push. It is now
   `922fd91`. **Re-test isolation before assuming it is broken** — but keep the abort gate
   (`merge-base != BASE`, never `rev-list --count == 0`, which reads green while stale).
8. **⭐ When you re-measure someone else's number, use THEIR EXACT PATTERN — or you are
   measuring a different thing and will call a true number false.**

   This trap was written into this file as a real finding, and the finding was WRONG. The
   plans gate on `grep -rn "extra_questions_proposed\|bias_radar" backend/app --include=*.py`
   and report **6 lines**. Reviewing them, I re-ran the grep as
   `"bias_radar\|blind_spots"` — silently dropping `extra_questions_proposed` — got 3 lines,
   and concluded both that the plans' figure was false and that `research/brief.py`
   "contains neither token". Measured with the plans' own pattern: **6 lines exactly**, and
   `brief.py:59,62,564,573` really does carry `extra_questions_proposed` at precisely those
   lines. The plans were right; the review was wrong.

   The residual real defect is one digit: plan 01 greps a THIRD term (`blind_spots`) and
   still reports 6, where its own pattern yields **7**. Files and line numbers are correct.

   Two rules follow, and they matter more than the numbers:
   * A re-measurement that changes the pattern is not a re-measurement. Copy the command
     verbatim.
   * "Their number is false" is a claim that needs the same evidentiary bar as the number
     itself. This review made that claim, committed it, and messaged it to the planner
     before checking — the exact failure it exists to prevent, one layer up.

9. **⭐ FIXING THIS FILE DOES NOT FIX THE PLANS THAT QUOTE IT — always check downstream.**
   Eleven plans cite CONTEXT by section, and a correction here is **invisible to anyone who only
   reads CONTEXT**. It has already happened twice in this phase, both times to the author of
   this warning:

   * `9220824` corrected the isolation table here and left plan 05 carrying the identical two
     errors, in the file an executor actually runs from. Caught only because plan 05 had not yet
     been launched.
   * `e4f0277` fixed section 1's unpinned-route list and thereby ORPHANED plan 11's instruction
     to "record that discrepancy" — a `<done>` criterion that could no longer be honestly
     satisfied, i.e. a correction here manufactured a blocker there.

   **Standing check: after editing CONTEXT, grep the plans for the claim you just changed.**

10. **Cite by SYMBOL or TEST NAME, never by line alone.** This phase accumulated a dozen
    line-number drifts and three stale citations — `test_intake_cross_tenant.py:588` (blank; the
    real reference is `test_upsert_answers_cross_tenant_returns_404_answers_unchanged`, def
    `:569`, PATCH `:600`), plan 02's insertion point (`:161-166`, not `:159-164`), and plan 10's
    `tenant_session` block (`:387-403`, not `:355-395`). Related trap: `with tenant_session(identity) as txs:`
    occurs **five times** in `research_routes.py` — locate it by enclosing FUNCTION, never by
    grepping the phrase. Line numbers rot; names do not.

### Three seams the plans must close, found during planning review

* **`upsert_answers` returns the leak it just closed.** It ends with
  `answers_repo.list_for_intake(intake_id)` (`intake_routes.py:598`) — the SAME unfiltered
  list as `list_answers`. Filtering only the GET re-opens F-01 through the write response.
  Both must project through ONE shared helper.
* **Response-code precedence must be pinned, not just the codes.** Order:
  ownership 404 -> lifecycle 409 -> schema membership 422 -> admin-only 404 -> field/status
  409 -> value 422. Ownership stays FIRST because
  `test_intake_cross_tenant.py::test_upsert_answers_cross_tenant_returns_404_answers_unchanged`
  (def at `:569`, the PATCH at `:600-604` — an earlier draft cited `:588`, which is blank) writes
  the non-canonical key `q1` cross-tenant and asserts EXACTLY 404 — a policy-first order
  answers 422 and breaks a tenant-isolation test.
* **`_flatten_template_keys`'s docstring encodes F-08 as intended behaviour** ("Returns []
  … in which case the write phase accepts the LLM's field_keys as-is"). Change the docstring
  with the code, or the next reader restores the bug on purpose.

---

## 12. The rate that should change how plans are written

**In phase 23.1, 18 of 18 plans carried at least one false, vacuous, already-green,
self-contradictory or arithmetically-wrong acceptance criterion — and the plan-checker
passed every one.** Not one was caught by a gate; all surfaced when an executor ran the plan
against the real tree and refused to contort the code. Two would have caused damage: one
demanded a grep return 1 when the truth was 14, and one required a pre-existing test to pass
unedited when that test had pinned the defect as expected behaviour.

**Every executor in this phase is briefed to report a false criterion with the measured
number instead of obeying it.** A criterion that is already green at HEAD is decoration, not
a gate — say so and replace it.

---

## 13. Test environment

A working backend venv for this session lives at
`<scratchpad>/1394013e-049c-41ee-93f5-4e994f8182a3/scratchpad/venv-be` (Python 3.12.6,
installed with `pip install -e ".[dev]"` from `backend/pyproject.toml` — **there is no
`requirements.txt`**; a plain `-r requirements.txt` install fails).

Baseline to beat: **608 passed / 2 skipped / 0 failed** (measured, 124s — a slower wall time
than another measurement of the same tree is machine load, not a discrepancy; gate on counts,
never on seconds). Frontend: tsc 0, vitest 154, i18n-audit PASS.

### Tribunal — a PROVEN local recipe, and two tests that are RED at HEAD

Tribunal needs its own venv (the backend venv has no `structlog`). `pip install -r
tribunal/requirements.txt` partially fails on this box; follow it with
`pip install structlog sqlalchemy fastapi httpx asyncpg pydantic pydantic-settings pytest
pytest-asyncio alembic`. Verified working venv: `<scratchpad>/venv-tri`.

The DB-backed tribunal tests are NOT unrunnable locally. Measured recipe.

**Why `-p 55432:5432` and not 5432** — two reasons, and the second is a trap:
`--network=host` does not work on Docker Desktop for Windows; and **port 5432 is held by a
native `postgres.exe` Windows SERVICE (PID 11056, confirmed via `netstat -ano` +
`tasklist`), not by a container.** `docker ps` shows NOTHING on 5432 — `cronos-postgres` is
mapped to **15432** — so an executor who checks `docker ps` will conclude 5432 is free, bind
it, and silently talk to the wrong database. (An earlier draft of this section blamed
`cronos-postgres` for holding 5432. That was wrong, and wrong in the direction that makes
the trap invisible.)

```bash
docker run -d --name trib-pg -p 55432:5432 -e POSTGRES_PASSWORD=ci-bootstrap-pw postgres:15
docker exec trib-pg psql -U postgres -c "CREATE ROLE app_user LOGIN PASSWORD 'ci-app-pw' NOSUPERUSER" \
  -c "CREATE ROLE worker_user LOGIN PASSWORD 'ci-worker-pw' NOSUPERUSER" \
  -c "CREATE DATABASE tribunal OWNER app_user"
docker exec trib-pg psql -U postgres -c "ALTER ROLE postgres SET search_path = tribunal, public"
export DATABASE_URL="postgresql+asyncpg://postgres:ci-bootstrap-pw@localhost:55432/tribunal"
(cd tribunal/nestor_pulse_sdk && python -m alembic upgrade head)
```

**The DSN choice is load-bearing and the suite polices it.** These tests refuse to produce a
vacuous green, and each failure mode LOOKS like a broken environment while actually being
the suite defending itself:

* as **`worker_user`** — 15 ERRORS, `permission denied for database tribunal` (the role has
  no database-level privilege; the fixture runs alembic).
* as **`postgres` superuser** — the CI `test-critical` shape. **57 passed / 12 skipped / 2
  failed** across the four ownership+resume files. The skips are honest: cross-tenant denial
  tests correctly refusing to run with RLS bypassed, plus 4 needing `google`.
* as **`app_user`** — the ownership/reclaim tests SKIP themselves (*"neither a superuser nor
  worker_user, so RLS hides the seeded `run` rows … THIS IS NOT A PASS, it is a vacuous
  green refused"*), **but `test_checkpoint_resume.py` runs FULLY: 31 passed, 0 skipped, 0
  failed** — against 23 passed / 8 skipped as superuser.

**⭐ So `app_user` is the FAITHFUL DSN for the checkpoint/resume file, not merely house
style.** The superuser run silently skips its eight cross-tenant denial tests because RLS is
bypassed. A gate of "23 passed, 8 skipped" is eight assertions weaker than it looks. **Gate
plan 05 on `app_user` and on `31 passed, 0 skipped` — where a skip is a setup failure, not a
pass.** (An earlier draft of this section recommended the superuser DSN, mirroring CI. That
was the weaker choice.)

### ⭐ TEST ISOLATION IS BROKEN IN THE TRIBUNAL SUITE — and it changes the CI-gap fix

**Corrected.** An earlier draft of this section said two tests are "RED at HEAD". That was
imprecise and would have made plan 05's criterion look false when it is not. The truth is
sharper and more useful — **these two tests are green alone and red only after certain
siblings run in the same process**:

**⚠ EVERY ROW IS DSN-QUALIFIED. An unqualified row in this table is a bug** — an earlier
draft had five unqualified rows, four of which were wrong once the recommended DSN above
flipped to `app_user`. The DSN is not a detail here; it decides whether the defect appears
at all.

| Invocation | no `DATABASE_URL` | **`app_user`** | `postgres` superuser |
|---|---|---|---|
| `test_checkpoint_resume.py` ALONE | 23 passed, 8 skipped | **31 passed, 0 skipped** | 23 passed, 8 skipped |
| `test_run_ownership_fence.py` + it | **32 passed, 18 skipped, 0 FAILED** | **2 failed**, 38 passed, 10 skipped | 2 failed, 40 passed |
| `test_stale_reclaim.py` + it | — | **2 failed**, 29 passed, 5 skipped | 2 failed, 26 passed |
| `test_run_api_idempotency.py` + it | — | 43 passed, 4 skipped — **clean** | 35 passed — clean |
| all four together | — | 50 passed, 19 skipped, 2 failed | 57 passed, 12 skipped, 2 failed |

**⭐ The pollution is DSN-DEPENDENT, and an earlier draft of this section said it was not.**
With **no** `DATABASE_URL`, `ownership_fence + checkpoint_resume` is **32 passed, 0 failed** —
completely clean. The mechanism is now obvious in hindsight: without a DSN the worker-facing
tests SKIP, and a skipped test configures nothing. They have to actually RUN to leave logging
in the state that breaks `caplog`. So "run them together and it breaks" is only true once a
database is present — which is exactly the configuration plan 05 now mandates.

The two casualties are always the same:
`test_a_payload_from_another_checkpoint_version_is_discarded` and
`test_an_oversized_payload_is_refused_and_nothing_is_written`. Both assert on `caplog`
records for WARNINGs emitted by `pipeline/tribunal/checkpoints.py:234` through **stdlib
`logging`**. Running either worker-facing file first leaves logging in a state where
`caplog` no longer captures that logger. **A test-isolation defect, not a product defect.**

Three consequences, and the third is the one that matters:

1. Plan 05's *"`test_checkpoint_resume.py` must pass unmodified"* is a VALID criterion — it
   passes in isolation. Keep it, and run that file in its own pytest invocation.
2. Never "fix" product logging to make these green. There is nothing wrong with the product.
3. **⛔ The obvious fix to the tribunal CI gap would have produced a red build.** The standing
   operator item is "append 3 filenames to `cloudbuild.test-critical.yaml:32`". If
   `test_checkpoint_resume.py` ends up sharing ONE pytest invocation with
   `test_run_ownership_fence.py` or `test_stale_reclaim.py`, CI gets two spurious failures —
   which reads as "the new tests broke the build" and gets reverted, leaving the 37-of-37 gap
   open for another cycle. **Give the checkpoint file its own invocation, or fix the leak
   first.** This is now the most valuable thing known about that operator item.

(`test_report_planner.py` separately fails to COLLECT — no `google` module — so a whole-suite
tribunal count is still not a usable gate. Plan 05's "gate on named files" rule stands.)
