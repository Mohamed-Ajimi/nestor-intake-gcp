---
phase: 23.2-authorization-depth-field-level-confidentiality-answer-lifec
plan: 11
type: execute
wave: 5
depends_on: [23.2-02, 23.2-03, 23.2-04, 23.2-05, 23.2-07, 23.2-08, 23.2-10]
files_modified:
  - backend/tests/test_client_surface_open.py
  - .planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/deferred-items.md
autonomous: true
requirements: [TENANT-02, SEC-02, SEC-03, SEC-04, SEC-05, COST-01]

must_haves:
  truths:
    - "At the merged phase head the whole backend suite is green, not just each plan's slice in its own worktree."
    - "All ten pinned client routes still answer EXACTLY 200 to a role=user after every projection and policy change in this phase."
    - "The pin is widened from ten client routes to seventeen — every /intakes route a role=user can actually reach is now positively asserted, not just the ten 23.1 happened to list."
    - "The /intakes route inventory is unchanged: this phase added and removed no route."
    - "No route acquired a role gate: the recursive dependency walk finds superadmin_gate on exactly the routes 23.1 put it on."
    - "The seams BETWEEN plans hold — every place two plans touched the same behaviour from different sides is checked once, at the merge."
    - "Every deferral this phase names is written down with its reason, and every residual risk is stated plainly."
  artifacts:
    - path: ".planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/deferred-items.md"
      provides: "DEF-23.2-01..11 plus anything this phase discovered and deliberately did not fix"
  key_links:
    - from: "backend/tests/test_client_surface_open.py"
      to: "app.main's real route tree"
      via: "_flatten_routes recursive walk, not a grep"
      pattern: "_flatten_routes"
---

<objective>
The phase-integration pass. Ten plans have each proved their own slice inside their own worktree.
This plan is the only one that looks at the MERGED tree.

`23.2-CONTEXT.md` § 12: in phase 23.1, **18 of 18 plans carried at least one false, vacuous,
already-green, self-contradictory or arithmetically-wrong acceptance criterion, and the
plan-checker passed every one.** Not one was caught by a gate. And every plan in 23.1 was green
in its own worktree while two blockers lived in the SEAMS between them.

Purpose: green gates say nothing about seams. This plan is the seam check.

Output: a measured whole-tree verification, the deferral register, and — only if a seam requires
it — a reconciling edit to `test_client_surface_open.py`.

## THIS PLAN WRITES ALMOST NO CODE

Its deliverable is measurement and a document. If you find yourself implementing a fix here,
STOP: either it belongs to a plan that missed it (say which, with the evidence), or it is new
scope (say so, and do not do it). The one exception is a reconciling edit to
`test_client_surface_open.py` if and only if a seam demands it — and any such edit must be
enumerated with old and new assertions, per CONTEXT § 9.2.

## THE EIGHT SEAMS, NAMED

Check each one explicitly. These are the places two plans touched the same behaviour from
different sides, and none of them is covered by either plan's own suite.

1. **23.2-06 × 23.2-09 — the answers response.** 23.2-06 filtered `list_answers`. 23.2-09's
   `upsert_answers` returns the SAME `answers_repo.list_for_intake(...)` list. A `role=user`
   write must return a filtered body. 23.2-09 was told to close this; verify it did, from the
   outside, with a fresh test call rather than by reading 23.2-09's SUMMARY.
2. **23.2-01 × everyone — one derivation, no copies.** Every consumer must import from
   `app.intake_canonical`. Verify there is exactly ONE definition of the admin-only key set in
   `backend/app`.
3. **23.2-07 × 23.2-08 — the two AI skills.** `context_pack` filters admin-only keys OUT of its
   prompt; `structure_answers` deliberately keeps them IN its writable set. Confirm they are
   opposite ON PURPOSE and that neither imported the other's rule.
4. **23.2-09 × 23.2-08 — two writers, two policies.** `upsert_answers` binds a `role=user` to the
   canonical schema; `structure_answers` writes as an operator through `upsert_extracted` and
   bypasses `upsert_answers` entirely. Confirm the skill still writes admin-only keys after
   23.2-09 landed — i.e. that 23.2-09's policy did not leak into the repository layer.
5. **23.2-10 × 23.2-05 — two alembic lines.** Exactly one new migration exists on the INTAKE
   line (0016) and ZERO new files under `tribunal/nestor_pulse_sdk/alembic/versions/`.
6. **23.2-10 × every integration suite — the schema everyone runs on.** `conftest._run_migrations`
   applies `alembic upgrade head` for every integration test, so 0016 now runs under all of them.
   The whole-suite run is the check.
7. **23.2-02 × 23.2-06 — storage and the report, in three directions.** A `role=user` can still
   READ their report's signed URL (pinned row 10); can no longer DELETE the report object
   (D-23.2-08, 404); and can no longer UPLOAD into `reports/` or `artifacts/` (D-23.2-17, 422).
   Check all three in one pass, and check the fourth thing that makes them coherent: exactly ONE
   `CLIENT_WRITABLE_CATEGORIES` exists, in `storage/keys.py`, imported by both routes —
   `grep -c "frozenset" backend/app/api/storage_routes.py` returns **0**.
8. **`skill-runs/stream` vs `research/stream` — do not conflate them.** `GET
   /intakes/{id}/skill-runs/stream` lives on `intake_router` and is **OPEN**; `GET
   /intakes/{id}/research/stream` lives on `research_router` and is **GATED** (23.1 gated the
   research one only). And note CONTEXT § 11 trap 2: `"research"` CONTAINS `"search"`, so any
   substring-based reasoning about these two routes is unsound — which is exactly why the walk
   resolves routes rather than matching strings. Confirm both are in their expected state after
   the phase: `skill-runs/stream` open, `research/stream` gated.

## WHAT THIS PLAN MUST NOT DO

- **Do NOT deploy.** CONTEXT § 9 non-goals: ZERO provider spend, no Tribunal run, no deploy. The
  phase ends at built + tested + committed. Do not run `gcloud builds submit` or
  `gcloud run deploy`.
- **Do NOT trigger a research run.**
- **Do NOT weaken any assertion to make the suite green.** A red test at the merge is a finding
  about a plan, and the plan is what gets fixed.
- **Do NOT close the inherited operator work** listed in CONTEXT § 10 (tribunal services not
  deployed; the live `role=user` 404/200 observation; the tribunal CI gap; the tfstate bucket;
  the `app_superadmin` rotation). Carry it forward in the deferral document; do not attempt it.
- **Do NOT implement any of DEF-23.2-01..11.**

## IF AN ACCEPTANCE CRITERION HERE IS FALSE

If any criterion in this plan is false, vacuous, or already green at HEAD, **report it with the
measured number** and do not contort the code to satisfy it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

<!-- THE AUTHORITY. § 9 fences, § 10 deferrals, § 11 traps, § 12 on criteria. -->
@.planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/23.2-CONTEXT.md

<interfaces>
`backend/tests/test_client_surface_open.py` (verified at `922fd91`, 12 tests, all green):

* `_PINNED_INTAKE_ROUTES` — a machine-generated inventory of all **43** `/intakes` routes.
* `_flatten_routes(routes, prefix="", inherited=())` (`:711-739`) — the recursive walker.
  **FastAPI 0.141 does NOT flatten `include_router` at include time**: `app.routes` holds
  `_IncludedRouter` placeholders, and include-level dependencies (which is how `ai_router` is
  gated) never appear in a leaf's `route.dependant`. A non-recursive read of `app.routes` finds
  ZERO `/intakes` routes.
  **This is the only correct way to prove a gate. NEVER prove a gate with `grep`** — a grep for
  `superadmin_gate` in `ai_routes.py` finds the router-level line and tells you nothing about any
  individual route (CONTEXT § 11 trap 1).
* `test_client_route_inventory_is_pinned` (`:742`) — fails on ADDITION and on REMOVAL.
* `test_mutation_proof_is_recorded` — walks row 7's resolved dependency tree recursively and
  fails the moment `superadmin_gate` appears anywhere in it.

`.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/deferred-items.md`
— the house format for the deferral document you are writing.

The seven scope-fence suites that must be green AND unmodified across the whole phase:
`tests/test_no_run_research_route.py`, `tests/test_scope_guard_run_research.py`,
`tests/test_research_trigger_gate.py`, `tests/test_operator_verb_gate.py`,
`tests/test_ai_router_gate.py`, `tests/test_superadmin_gate.py`,
`tests/test_intake_cross_tenant.py`.
</interfaces>

<environment>
⭐ **The backend suite RUNS LOCALLY against a real Postgres.** Docker daemon **29.6.2** is up.

    PY="C:/Users/ajimimo/AppData/Local/Temp/claude/C--Users-ajimimo-Desktop-MOELD-nestor-intake-gcp/1394013e-049c-41ee-93f5-4e994f8182a3/scratchpad/venv-be/Scripts/python.exe"

Tribunal needs its OWN venv (the backend one lacks `structlog`):

    PYT="C:/Users/ajimimo/AppData/Local/Temp/claude/C--Users-ajimimo-Desktop-MOELD-nestor-intake-gcp/1394013e-049c-41ee-93f5-4e994f8182a3/scratchpad/venv-tri/Scripts/python.exe"

⚠ `pip install -r tribunal/requirements.txt` partially fails on this box; follow it with
`pip install structlog sqlalchemy fastapi httpx asyncpg pydantic pydantic-settings pytest pytest-asyncio`.
The tribunal suite as a WHOLE does not run clean here — gate on NAMED files, never on a
whole-suite tribunal count. Two concrete reasons, both measured at `922fd91`:

* `test_report_planner.py` fails to COLLECT — `ModuleNotFoundError: No module named 'google'`.
  Exclude it with `--ignore` and say why.
* `test_run_ownership_fence.py` and `test_stale_reclaim.py` reconfigure logging such that two
  `caplog` assertions in `test_checkpoint_resume.py` go red when batched with them —
  `test_run_api_idempotency.py` does NOT have this effect. The two tests PASS in isolation
  (31/0/0 as `app_user`). **Do not exclude those two tests**; keep the tribunal invocation in
  this plan limited to `test_checkpoint_resume.py` + `test_run_state_cas.py`, which is a clean
  combination, and carry the ordering defect into the deferral register as a new finding.

⭐ **The DB-backed tribunal tests DO run locally**, and the `app_user` DSN is the faithful one.
Plan 23.2-05 carries the proven recipe verbatim; reuse the same container rather than standing up
a second one.

**MEASURED at `922fd91`:**
* backend: **608 passed, 2 skipped, 0 failed** (~94s)
* `tests/test_client_surface_open.py`: **12 passed**
* `tribunal .../test_checkpoint_resume.py` in venv-tri: **31 passed, 0 skipped** under the
  `app_user` DSN; **23 passed, 8 skipped** with no `DATABASE_URL`
* frontend: `npx tsc --noEmit` 0 errors, `npm test` **154** passing, `node scripts/i18n-audit.mjs`
  PASS

NO PROVIDER CALL. NO TRIBUNAL RUN. NO DEPLOY.
</environment>

<tasks>

<task type="auto">
  <name>Task 1: Whole-tree verification at the merged head</name>

  <files>backend/tests/test_client_surface_open.py</files>

  <read_first>
    - `backend/tests/test_client_surface_open.py` in full, INCLUDING its header paragraph on
      EXACT status codes and the `_flatten_routes` docstring.
    - All ten SUMMARY files from this phase, `23.2-01-SUMMARY.md` .. `23.2-10-SUMMARY.md`.
      Read them for the DEVIATIONS and the "pre-existing test edited" enumerations, not for
      reassurance.
  </read_first>

  <behavior>
    Run and record, at the merged head:

    - `cd backend && $PY -m pytest -q` — record the exact pass/skip/fail line. Compare against
      608/2/0 plus the sum of every plan's new tests as reported in the ten SUMMARYs. **If the
      arithmetic does not add up, that discrepancy is the finding** — chase it before anything
      else and name the plan it belongs to.
    - The ORM/database drift test from plan 23.2-10 is green (it lives in
      `tests/test_research_dispatch_dedup.py` and runs inside `pytest -q`). ⚠ **Do NOT run
      `alembic check`** — measured at `922fd91` it raises
      `sqlalchemy.exc.ArgumentError: Could not parse SQLAlchemy URL` on every tree, correct or
      not, because `alembic.ini:24` is empty and `DATABASE_URL` is unset outside the pytest
      harness. A command that always fails is not a gate.
    - `cd backend && $PY -m pytest tests/test_client_surface_open.py -v` — **12 tests, all
      green**, and specifically:
      - the ten `*_open_to_user` tests still assert EXACTLY 200 (no `!= 404`, no `< 400`, no
        tuple membership — read the assertions, do not just watch them pass);
      - `test_client_route_inventory_is_pinned` green, i.e. this phase added and removed no
        `/intakes` route;
      - `test_mutation_proof_is_recorded` green, i.e. no gate crept onto row 7.
    - **The gate walk, done properly, with the MEASURED numbers.** Using
      `_flatten_routes(main.app.routes)` (or `<scratchpad>/gatewalk.py`, the approved script that
      wraps the same walk), enumerate every leaf and its resolved + inherited dependencies. Assert
      all of the following, measured at `922fd91` by that walk and recorded in CONTEXT § 1:
      - **65 routes total** across the app, **26 carrying `superadmin_gate`**.
      - Of the **43** `/intakes` routes, **26 gated and 17 open** — the 26 are exactly
        `intake_router` 8 of 22, `research_router` 11 of 11, `ai_router` 7 of 7 (a router-level
        `Depends`, which is why the walk must be recursive).
      - `admin_router` **0 gated / 13 total** — NOT a hole. Every admin route depends on
        `get_admin_session`, which raises 403 "Superadmin only" before opening a transaction
        (`db/session.py:372-375`). Assert 0, and assert `tests/test_admin_routes.py`
        `test_user_role_403_on_every_admin_route` is green: that pairing is what makes the 0
        meaningful rather than alarming.
      - `storage_router` **0 gated / 3 total**, unchanged — plan 23.2-02 changed what
        `delete_objects` ACCEPTS, never who may call it.
      - Anti-vacuity: `assert routes_walked == 65` and `assert intakes_walked ==
        len(_PINNED_INTAKE_ROUTES) == 43`.
      **This is the machine-checked residue that no plan in this phase attached or moved a gate.**
      Do it with the walker, never with a grep — `grep -c "superadmin_gate" intake_routes.py`
      returns **17** because it counts the import line, the docstrings and the comments; the real
      answer is 8. CONTEXT § 1 records that exact grep artifact as the correction that produced
      these numbers.
    - `cd backend && $PY -m pytest tests/test_no_run_research_route.py tests/test_scope_guard_run_research.py tests/test_research_trigger_gate.py tests/test_operator_verb_gate.py tests/test_ai_router_gate.py tests/test_superadmin_gate.py tests/test_intake_cross_tenant.py -v`
      — all green, and `git diff --stat` across those seven files is EMPTY for the whole phase
      (`git diff 922fd91..HEAD --stat -- <the seven paths>`).
    - `grep -n "research_router" backend/app/main.py` still shows the include at line **152**
      (measured at `922fd91`: `protected_router.include_router(research_router)` at `:152`).
    - `cd tribunal && $PYT -m pytest nestor_pulse_sdk/tests/test_checkpoint_resume.py nestor_pulse_sdk/tests/test_run_state_cas.py -q -rs`
      under the `app_user` DSN — record the counts and any skips VERBATIM. Expect
      `test_checkpoint_resume.py`'s 31 to be green, including the two `caplog` tests: this
      two-file combination is clean, and a red one here is a REGRESSION, not the known
      cross-file pollution. A skip is not a pass; if the CAS DB tests skipped, say so plainly
      here as well as in 23.2-05's SUMMARY.
    - `cd frontend && npm ci && npx tsc --noEmit && npm test && node scripts/i18n-audit.mjs` —
      record all three numbers. `git diff 922fd91..HEAD --stat -- frontend/` should be EMPTY for
      this whole phase; if it is not, name every file and say which plan changed it and why.
    - `git diff 922fd91..HEAD --stat` in full — read it once, end to end, and confirm every
      changed file appears in some plan's `files_modified`. A file changed by no plan is a
      finding.
  </behavior>

  <action>
    Then walk the eight seams from the objective, one at a time, and record a verdict for each
    with the command or test that produced it. Where a seam has no covering test, WRITE one —
    the smallest possible — and say which seam it covers.

    Seam 1 (the answers response) and seam 4 (two writers) are the two most likely to be broken;
    do those first.

    For seam 2, the check is: exactly one definition of the admin-only derivation exists.
    `grep -rn "admin_only" backend/app --include=*.py` and confirm every hit is inside
    `app/intake_canonical.py`. Measured at `922fd91` this grep returned **0 lines**, so after the
    phase every hit is new and every one must be in that one file.

    If a seam is broken, do NOT fix it here. Record it, name the plan, and report it up — a fix
    written in the integration pass has no plan, no test-first record and no reviewer.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; $PY -m pytest -q &amp;&amp; $PY -m pytest tests/test_client_surface_open.py -v</automated>
  </verify>

  <done>
    Every number above is recorded in the SUMMARY as an observed value, not as "green". The seven
    seams each carry a verdict and the evidence that produced it. Any arithmetic discrepancy in
    the test count is chased to a named plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: The deferral register and the residual-risk statements</name>

  <files>.planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/deferred-items.md</files>

  <read_first>
    - `.planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/23.2-CONTEXT.md`
      § 10 — the eleven named deferrals and the inherited operator work.
    - `.planning/phases/23.1-platform-hardening-authorization-boundary-space-deactivation/deferred-items.md`
      — the house format.
    - The ten SUMMARY files — for anything a plan discovered and deliberately did not fix.
  </read_first>

  <behavior>
    The document contains:

    - **DEF-23.2-01 .. DEF-23.2-11** verbatim from CONTEXT § 10, each with its reason. Eleven
      rows; assert the count.
    - **The D-23.2-11 residual risk**, in these exact terms (from plan 23.2-03's SUMMARY):
      after D-23.2-09 the database can no longer over-claim, but a token minted before an
      *individual* deactivation still authenticates until it expires; the window is bounded by
      the ID-token TTL (about 1h) and `check_revoked=True` already closes it whenever the IdP
      revoke succeeded. Not weakened, not paraphrased into "fully fixed".
    - **The D-23.2-15 outbox deferral** (from 23.2-04's SUMMARY): the defect fixed was a
      correctness INVERSION; the outbox is a DURABILITY improvement; after this phase a mail can
      still be lost between the commit and the send, and that is a strictly better failure than
      the one removed.
    - **The `required` / `min_length` / `min_items` non-enforcement** (from 23.2-09's SUMMARY),
      with the `IntakeForm.tsx:214-217` / `:26-30` / `:33-40` evidence and the note that its home
      is the `/submit` verb.
    - **New findings this phase made and did not fix**, each with the plan that found it, taken
      from the ten SUMMARYs. One is known in advance and must appear: the tribunal
      **cross-file logging pollution** — `test_run_ownership_fence.py` and `test_stale_reclaim.py`
      turn two `caplog` assertions in `test_checkpoint_resume.py` red when batched, while
      `test_run_api_idempotency.py` does not; the two tests pass in isolation. Reproducible,
      orthogonal to every decision in this phase, and it belongs to whoever owns the tribunal CI
      gap. Record it with the isolation table from 23.2-05's SUMMARY, NOT as a flake. ⚠ The `upload_file` category gap is **NOT** one of them: it was
      promoted to **D-23.2-17** and FIXED in plan 23.2-02 alongside D-23.2-08, from one shared
      `CLIENT_WRITABLE_CATEGORIES` constant. If a SUMMARY still calls it a deferral, that is a
      stale note — correct it and say so. Record only findings the SUMMARYs actually leave open.
    - **The inherited operator work** from CONTEXT § 10, carried forward unchanged: tribunal
      services not deployed; the live `role=user` 404/200 observation never made; the tribunal CI
      gap (`cloudbuild.test-critical.yaml:32` runs four test files and none of the 37
      ownership/idempotency tests) — noting that 23.2-05 added `cloudbuild.test-cas.yaml` for
      THIS phase's own tests and that this does NOT close that gap, and that the new config is
      wired to no trigger; the tfstate bucket; the `app_superadmin` rotation.

    - **⚠ AN OPERATOR-FACING WARNING ATTACHED TO THAT CI GAP — write it under its own heading, in
      the register AND in the phase SUMMARY, so it does not die with this session.** The standing
      remediation for the tribunal CI gap is "append the missing test files to
      `cloudbuild.test-critical.yaml:32`". Verified at `922fd91`: that line is a SINGLE
      `python -m pytest <files>` invocation. So if `test_checkpoint_resume.py` is ever appended
      there alongside `test_run_ownership_fence.py` or `test_stale_reclaim.py`, CI gains **two
      spurious failures** from the logging-isolation defect above. Those will read as "the new
      tests broke the build", the change gets reverted, and the 37-of-37 gap stays open for
      another cycle — the gap closing itself out of existence by looking like a regression.

      The remediation is one of two things, and the note must say both:
      1. give `test_checkpoint_resume.py` its **own** pytest invocation in that config, or
      2. fix the logging leak first — both casualties assert on `caplog` for WARNINGs emitted
         via stdlib logging from `pipeline/tribunal/checkpoints.py:234`, and running either
         worker-facing file first leaves that logger uncaptured.

      **Do NOT do either here.** The CI gap is explicitly out of scope (CONTEXT § 9). This is a
      note for whoever picks it up, and its whole value is that it arrives BEFORE they spend a
      cycle on a revert.
    - **The deploy state**, stated plainly: this phase is BUILT, TESTED and COMMITTED. Nothing is
      deployed. No `role=user` was observed against a live service. Every claim in this phase is
      a local test result.
  </behavior>

  <action>
    Write the document. Mirror 23.1's `deferred-items.md` structure so the two read alike.

    Be precise about what is a deferral (a decision, with a reason) versus what is unfinished
    (nothing here should be unfinished — if something is, say so under its own heading).

    Do NOT copy CONTEXT § 10 mechanically without checking it against the SUMMARYs. DEF-23.2-10
    (dead code) and DEF-23.2-09 (frontend lint) name measured numbers; if a plan's work changed
    them, correct the number and say you did.
  </action>

  <verify>
    <automated>test -f .planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/deferred-items.md &amp;&amp; grep -c "DEF-23.2-" .planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/deferred-items.md</automated>
  </verify>

  <done>
    The file exists, carries all eleven `DEF-23.2-*` ids plus any new ones, and contains the three
    residual-risk statements verbatim. The deploy state is stated in one unambiguous paragraph.
    The operator-facing `cloudbuild.test-critical.yaml:32` warning appears under its own heading,
    naming both remediations, in the register AND in the phase SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Widen the client-surface pin from ten routes to seventeen</name>

  <files>backend/tests/test_client_surface_open.py</files>

  <read_first>
    - `backend/tests/test_client_surface_open.py` in full — the header table of the TEN pinned
      routes, the ten `*_open_to_user` tests and their EXACT-status-code discipline, and the
      seeding helpers (`_seed_space`, `_seed_intake`, `_seed_skill_run`, `_seed_report`,
      `_cleanup`).
    - `23.2-CONTEXT.md` § 1, "The client-reachable set this phase must reason about — all 17".
    - The 23.2-02 and 23.2-06 SUMMARYs — two of the seven already gained coverage elsewhere and
      you must not duplicate it blindly.
  </read_first>

  <behavior>
    CONTEXT § 1: **seventeen** `/intakes` routes are reachable by `role=user`, and the 23.1 pin
    positively asserts only **ten**. The other seven are open by the same design decision and are
    asserted NOWHERE — so a future gate could close one of them with every gate in this repo
    green. Widening the pin is cheap and is exactly the counterweight this file exists to be.

    The seven, derived by subtracting the pinned ten from the reachable seventeen:

    | Route | Note |
    |---|---|
    | `POST /intakes` | client creates their own intake |
    | `PATCH /intakes/{id}` | `client_name` only; benign |
    | `GET /intakes/{id}/context-pack` | scoped-empty 200 by design — never 404 |
    | `GET /intakes/{id}/skill-runs/stream` | **SSE** — see the caution below |
    | `GET /intakes/{id}/sources` | scoped-empty 200 by design |
    | `POST /intakes/{id}/storage/uploads` | client upload — **pin with `category="attachments"`** |
    | `DELETE /intakes/{id}/storage/objects` | **pin with an `audio/` or `attachments/` key** |

    ⛔ **THE TWO STORAGE PINS MUST NAME THEIR CATEGORY — this is the one that will bite.** After
    plan 23.2-02, a `role=user` uploading `category="reports"` correctly gets **422**, and
    deleting a `reports/` key correctly gets **404** (D-23.2-17 / D-23.2-08). This task also says
    "if a pin goes red you have found a regression — STOP and name the plan". So a pin written
    with a `reports/` key manufactures a false regression alarm **inside the plan whose entire job
    is telling a real regression from correct new behaviour**, and it would point the finger at
    plan 23.2-02 for doing exactly what it was asked to do. Write `attachments` / `audio`
    explicitly into both header-table rows AND both test docstrings, with a one-line note saying
    why the category is named.

    ⚠ **Derive the seven yourself by subtraction** — 17 reachable minus the 10 in the test
    file's own header table — rather than copying any list. CONTEXT § 1 now carries a correct
    seven-row table with measured expected codes (fixed in `e4f0277`, after an earlier draft of
    that paragraph said "seven" and listed six, omitting `DELETE .../storage/objects`). Use it to
    CHECK your subtraction, not as the source. If your subtraction and that table disagree, the
    disagreement is the finding — report it with both lists.

    For each new pin:
    - EXACTLY the expected 2xx for a `role=user` on their OWN space. No `!= 404`, no `< 400`, no
      tuple membership — the file's header forbids all three and they are green for a 500.
    - The expected code per route, MEASURED at `922fd91`, not guessed: `POST /intakes` is
      **201** (`intake_routes.py:443` — `status_code=status.HTTP_201_CREATED`);
      `POST .../storage/uploads` is **201** (`storage_routes.py:135-137`); the other five are
      **200**. `GET .../context-pack` (`intake_routes.py:630`) and `GET .../sources`
      (`intake_routes.py:664`) are scoped-empty 200s by design — never 404, and their docstrings
      explain why that non-distinguishability is the security property.
    - A one-line docstring naming the frontend caller, matching the existing ten's style. If a
      route has NO frontend caller today, say so — that is worth knowing and is not a reason to
      skip the pin.
    - ANTI-VACUITY: update the header table to seventeen rows and add an assertion that the
      number of `*_open_to_user` tests in the module is **17**, derived by introspecting the
      module's callables rather than hard-counted in prose. `assert checked == 17`.

    ⚠ **`GET /intakes/{id}/skill-runs/stream` is an SSE endpoint.** `TestClient.get` on a
    streaming response can block. Use `client.stream("GET", ...)` and assert the status code from
    the response object without consuming the body, or use whatever shape
    `tests/test_sse_stream.py` already established — read that file first. **If you cannot pin it
    safely without a timeout hazard, EXCLUDE it explicitly**, add a comment in the header table
    saying it is deliberately unpinned and why, and report it in the SUMMARY. Sixteen honest pins
    plus one documented exclusion beats seventeen with a flaky test.

    Every one of these routes must be green WITHOUT any production change. If a pin goes red, you
    have found a regression from an earlier plan in this phase — STOP, name the plan, and report
    it. **Do not change production code here** to make a new pin pass.
  </behavior>

  <action>
    This is the ONLY task in this plan that writes tests, and it writes pins, not behaviour.

    Extend the file's header table from ten rows to seventeen (or sixteen plus the documented
    exclusion), keeping the "ROWS 7 AND 8 ARE THE DANGEROUS PAIR" note and the EXACT-STATUS-CODES
    paragraph byte-identical — they are the file's contract and this task does not renegotiate it.

    Reuse the existing seeding helpers. Add no new harness. Where a route needs a seeded artifact
    (`context-pack`, `sources`), prefer the scoped-empty 200 case: it needs no seed, it is the
    documented design (`intake_routes.py:629`, `:940`), and it is the case an over-wide gate would
    break just as surely as the populated one.

    Do NOT touch the existing ten tests. `git diff` on this file should show additions plus the
    header table edit and nothing else.
  </action>

  <verify>
    <automated>cd backend &amp;&amp; $PY -m pytest tests/test_client_surface_open.py -v &amp;&amp; $PY -m pytest -q</automated>
  </verify>

  <done>
    The pin covers seventeen routes (or sixteen plus one documented, justified exclusion), every
    assertion is an EXACT status code, the `checked == 17` anti-vacuity guard is in place, and the
    ten pre-existing tests are unmodified. Full suite still green. The SUMMARY states the seven
    routes as DERIVED by subtraction and confirms they match CONTEXT § 1's table (or reports the
    disagreement).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan worktree → merged head | Ten plans each proved a slice in isolation; the merged behaviour was never executed until now |
| phase → operator | The SUMMARY and the deferral register are what the operator acts on; an overstated claim here becomes a wrong deploy decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.2-11-01 | Information disclosure | A seam re-opens a leak a plan closed — most likely `upsert_answers` returning the unfiltered answer list after 23.2-06 filtered `list_answers` | mitigate | Seam 1 checked from the outside with a fresh call, not by reading 23.2-09's SUMMARY |
| T-23.2-11-02 | Denial of service (availability) | A projection or policy change silently gated a client route; every static gate in this repo stays green while a live client feature dies | mitigate | The recursive `_flatten_routes` gate walk with `checked == 43`, plus the ten EXACT-200 pins read line by line, not merely run |
| T-23.2-11-03 | Denial of service (availability) | 23.2-09's write policy leaked into the repository layer and broke `structure_answers`' operator writes | mitigate | Seam 4: confirm the skill still writes admin-only keys after the policy landed |
| T-23.2-11-04 | Denial of service (deploy) | Two migrations, or a migration on the wrong alembic line, or an ORM/migration drift that only surfaces at deploy | mitigate | Seam 5: exactly one new file under the intake line, zero under the tribunal line; plus plan 23.2-10's pytest drift test comparing `pg_indexes.indexdef` to `ResearchRun.__table__.indexes`. NOT `alembic check`, which fails on every tree here |
| T-23.2-11-05 | Repudiation | The phase reports "all green" while a tribunal DB test SKIPPED and proved nothing | mitigate | Skips recorded verbatim with `-rs`; "a skip is not a pass" stated in the SUMMARY and in the deferral register |
| T-23.2-11-06 | Repudiation | The deferral register overstates what was fixed, and the operator skips work that is still owed | mitigate | The three residual-risk statements reproduced verbatim from their plans; the inherited operator work carried forward unchanged; the deploy state stated in one paragraph |
| T-23.2-11-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NOTHING. `npm ci` uses the COMMITTED lockfile — never `npm install`, which drifts it |
</threat_model>

<verification>
1. `cd backend && $PY -m pytest -q` at the merged head — recorded, and the arithmetic against
   608/2/0 plus each plan's new tests reconciles or the discrepancy is named.
2. Plan 23.2-10's ORM/database drift test is green inside `pytest -q`. `alembic check` is
   deliberately NOT run — it raises `ArgumentError: Could not parse SQLAlchemy URL` on every
   tree here (`alembic.ini:24` empty, no persistent `DATABASE_URL`).
3. `cd backend && $PY -m pytest tests/test_client_surface_open.py -v` — 12 green; the ten
   EXACT-200 assertions read and confirmed exact.
4. The `_flatten_routes` gate walk asserts `checked == 43` and an unchanged `superadmin_gate` set.
5. `git diff 922fd91..HEAD --stat -- backend/tests/test_no_run_research_route.py backend/tests/test_scope_guard_run_research.py backend/tests/test_research_trigger_gate.py backend/tests/test_operator_verb_gate.py backend/tests/test_ai_router_gate.py backend/tests/test_superadmin_gate.py backend/tests/test_intake_cross_tenant.py`
   is EMPTY.
6. `git diff 922fd91..HEAD --stat -- frontend/` is EMPTY, or every file is named with its plan
   and reason.
7. `grep -rn "admin_only" backend/app --include=*.py` — every hit inside
   `app/intake_canonical.py` (measured at `922fd91`: 0 hits repo-wide).
8. `grep -n "research_router" backend/app/main.py` — still mounted at `:152`.
9. Frontend: tsc 0, vitest 154, i18n-audit PASS — recorded as numbers.
10. `deferred-items.md` exists with all eleven `DEF-23.2-*` ids and the three residual-risk
    statements.
</verification>

<success_criteria>
- The whole backend suite is green at the merged head and its count reconciles with the ten
  SUMMARYs, or the discrepancy is named and attributed.
- 65 routes walked, 26 gated, 43 of them `/intakes` (26 gated / 17 open); `admin_router` still
  0 of 13 with its 403 test green; the ten client routes still EXACTLY 200.
- The client-surface pin covers seventeen routes (or sixteen plus one documented exclusion).
- The seven scope-fence suites green and byte-unchanged across the phase.
- Zero frontend diff; tsc 0, vitest 154, i18n-audit PASS.
- All eight named seams carry a verdict with evidence.
- `deferred-items.md` written, with DEF-23.2-01..11, the three residual-risk statements, the new
  non-fixes (including the tribunal cross-file logging-isolation defect with its isolation
  table), the inherited operator work, the `cloudbuild.test-critical.yaml:32` warning under its
  own heading, and an unambiguous "nothing is deployed" paragraph.
- ZERO provider spend, no Tribunal run, no deploy.
</success_criteria>

<output>
Create `.planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/23.2-11-SUMMARY.md` when done.
</output>
