---
phase: 23.2-authorization-depth-field-level-confidentiality-answer-lifec
plan: 11
subsystem: phase-integration
tags: [integration, seams, authorization, client-surface, deferrals]
requires: [23.2-01, 23.2-02, 23.2-03, 23.2-04, 23.2-05, 23.2-06, 23.2-07, 23.2-08, 23.2-09, 23.2-10]
provides:
  - "A measured whole-tree verification at the merged phase head"
  - "A verdict per seam, with the command that produced it"
  - "The client-surface pin widened from 10 routes to 17"
  - "The phase deferral register (15 items) + the operator CI warning"
affects: [backend/tests/test_client_surface_open.py]
tech-stack:
  added: []
  patterns: ["pin-by-subtraction from a recursive route walk", "anti-vacuity by module introspection"]
key-files:
  created:
    - .planning/phases/23.2-authorization-depth-field-level-confidentiality-answer-lifec/deferred-items.md
  modified:
    - backend/tests/test_client_surface_open.py
decisions:
  - "17 honest pins, no documented exclusion: the SSE route is pinned safely with TestClient.stream + a TERMINAL run"
  - "Storage pins name attachments/audio explicitly, so a correct post-plan-02 422/404 cannot read as a regression"
  - "No seam was fixed here: a fix written in the integration pass has no plan, no test-first record and no reviewer"
metrics:
  duration: ~75min
  completed: 2026-09-06
  backend_tests: "728 -> 736 passed, 2 skipped, 0 failed"
---

# Phase 23.2 Plan 11: Phase-Integration Pass Summary

The merged tree holds. All eight seams verified with evidence, the client-surface pin widened
from ten routes to seventeen (all honest, no exclusion needed), the test-count arithmetic
reconciles to the test, and the deferral register carries fifteen items plus the operator CI
warning. **Zero provider spend, no Tribunal run, no deploy.**

## Base

`622cc89`, merge-base `622cc89` — the abort gate passed exactly. `0016_research_runs_single_inflight.py`
present; `grep -c "_visible_answer_rows\|admin_only_field_keys" backend/app/api/intake_routes.py` = **9**.

---

## Whole-tree measurements at the merged head

| Check | Observed |
|---|---|
| `pytest -q` at base `622cc89` | **728 passed, 2 skipped, 0 failed** (181.11s) |
| `pytest -q` after this plan's pins | **736 passed, 2 skipped, 0 failed** (120.52s) |
| `tests/test_client_surface_open.py` | 12 passed at base -> **20 passed** after widening |
| The seven scope-fence suites | **81 passed**; `git diff 922fd91..HEAD --stat` over all seven is **EMPTY** |
| `test_admin_routes.py -k user_role_403` | **8 passed, 16 deselected** |
| `grep -n "research_router" backend/app/main.py` | mounted at **`:152`** (unchanged) |
| Tribunal `test_checkpoint_resume.py` + `test_run_state_cas.py`, `app_user` DSN | **43 passed, 0 skipped, 0 failed** (12.44s) |
| Frontend `npx tsc --noEmit` | **0 errors** |
| Frontend `npm test` | **10 files, 154 tests passed** |
| Frontend `node scripts/i18n-audit.mjs` | **PASS** — A/B/C clean, 104 CHECK-D advisories (pre-existing) |
| `git diff 922fd91..HEAD --stat -- frontend/` | **EMPTY** |
| `git diff 922fd91..HEAD --stat -- tribunal/nestor_pulse_sdk/alembic/` | **EMPTY** |

**A skip is not a pass.** The tribunal pair ran with `-rs` and reported **zero** skips under the
`app_user` DSN — the DB-backed CAS tests really executed. The backend's 2 skips are the
pre-existing baseline pair, unchanged since `922fd91`.

### The test-count arithmetic reconciles EXACTLY

Each plan's own measured delta, from its SUMMARY:

| Plan | 01 | 02 | 03 | 04 | 05 | 06 | 07 | 08 | 09 | 10 | sum |
|---|---|---|---|---|---|---|---|---|---|---|---|
| new backend tests | +10 | +19 | +7 | +4 | 0 | +11 | +1 | +1 | +50 | +17 | **+120** |

`608 + 120 = 728`. **No discrepancy to chase.** (Plan 05 added 12 tests on the TRIBUNAL line,
which `pytest -q` in `backend/` does not collect — hence 0 in this row, not a missing plan.)
Note the SUMMARYs quote different baselines (608 for wave 1-2, 648 for wave 3, 661 for wave 4,
711 for wave 5) because each ran in its own worktree at its own wave head; the DELTAS are what
sum, and they sum to the merged total.

### The gate walk — measured with the recursive walker, never with a grep

| Assertion | Observed |
|---|---|
| routes total | **65 leaves** = **61 APIRoutes** + 4 Starlette doc routes (see finding 1) |
| carrying `superadmin_gate` | **26** |
| `/intakes` routes | **43** — matching `len(_PINNED_INTAKE_ROUTES)` |
| `/intakes` gated / open | **26 gated / 17 open** |
| `intake_router` | **8 gated / 22** |
| `research_router` | **11 gated / 11** |
| `ai_router` | **7 gated / 7** (router-level `Depends` — invisible to a non-recursive read) |
| `admin_router` | **0 gated / 13** — and `test_user_role_403_on_every_admin_route` is **green (8 passed)**, which is what makes the 0 meaningful rather than alarming |
| `storage_router` | **0 gated / 3** — unchanged; plan 02 changed what `delete_objects` ACCEPTS, never who may call it |

Anti-vacuity held: `intakes_walked == 43` and `routes_walked` was asserted, not assumed.

**The grep artifact reproduced:** `grep -c "superadmin_gate" backend/app/api/intake_routes.py`
returns **17** at the merged head (it counts the import line, docstrings and comments). The
walker's answer is **8**. This is exactly why the gate proof is a dependency-graph walk.

**No plan in this phase attached or moved a gate.**

---

## The eight seams — verdict and evidence

| # | Seam | Verdict | Evidence |
|---|---|---|---|
| 1 | 23.2-06 × 23.2-09 — the answers response | ✅ CLOSED | `test_route_successful_client_write_returns_a_filtered_body` (`test_answer_write_policy.py:952`) seeds all four admin keys, PATCHes as `role=user`, asserts each admin key ABSENT and the written key PRESENT — anti-vacuous in both directions. Code: ONE `_visible_answer_rows` helper, two call sites (`intake_routes.py:692` read, `:771` write). |
| 2 | 23.2-01 × everyone — one derivation | ✅ PASS | `grep -rn "def admin_only_field_keys" backend/app` = **exactly one** hit, `intake_canonical.py:118`. All 24 other `admin_only` hits are imports, call sites or comments. Three consumers import it: `context_pack.py:53`, `intake_routes.py:61`, `intake_write_policy.py:133`. No copies. |
| 3 | 23.2-07 × 23.2-08 — the two AI skills | ✅ OPPOSITE ON PURPOSE | `context_pack.py` imports **only** `admin_only_field_keys` and EXCLUDES those keys from the prompt (`:181`). `structure_answers.py` imports **only** `canonical_field_keys` and its module docstring states "Admin-only keys are INCLUDED" (`:9`, `:139`). Neither imports the other's rule. **Do not harmonise these.** |
| 4 | 23.2-09 × 23.2-08 — two writers, two policies | ✅ NO LEAK | `check_answer_batch` has **exactly one** call site repo-wide: `intake_routes.py:756`, inside the route. `app.intake_write_policy` is imported at `intake_routes.py:64` and **nowhere in `db/`**. `structure_answers` writes via `upsert_extracted_in_space` / `upsert_extracted` (`:253`/`:258`), which never traverse the policy. Belt and braces: the policy returns immediately for `role == "superadmin"` anyway. |
| 5 | 23.2-10 × 23.2-05 — two alembic lines | ✅ PASS | `git diff --diff-filter=A` over both version dirs adds **exactly one** file: `backend/app/db/alembic/versions/0016_research_runs_single_inflight.py`. **ZERO** added under `tribunal/nestor_pulse_sdk/alembic/`. ⚠ The tribunal line already carries an unrelated `0016_source_resolved_url.py` (its head is 0018) — a grep for "0016" finds two files that are not versions of each other. |
| 6 | 23.2-10 × every integration suite | ✅ PASS | `conftest._run_migrations` calls `command.upgrade(cfg, "head")` (`conftest.py:187`), so 0016 applies under every integration test. The whole-suite **736 passed / 0 failed** is the check. |
| 7 | 23.2-02 × 23.2-06 — storage in three directions | ✅ COHERENT | Read: signed-url pin green (row 10). Delete: `reports/` -> 404 for `role=user`. Upload: `reports`/`artifacts` -> 422. The fourth, coherence check: **exactly one** `CLIENT_WRITABLE_CATEGORIES`, at `storage/keys.py:47`, imported at `storage_routes.py:61` and read by BOTH `upload_file` (`:206`) and `delete_objects` (`:381`). **`grep -c "frozenset" backend/app/api/storage_routes.py` = 0.** No copy; the two directions cannot drift. |
| 8 | `skill-runs/stream` vs `research/stream` | ✅ AS EXPECTED | The walker places `GET /intakes/{id}/skill-runs/stream` in the **OPEN** list (`intake_routes`) and `GET /intakes/{id}/research/stream` in the **GATED** list (`research_routes`). Resolved by route object, never by string — `"research"` contains `"search"` and both end in `/stream`. |

**No seam was broken, so nothing was fixed here.** That was the correct outcome: a fix written
in the integration pass would have had no plan, no test-first record and no reviewer.

---

## Task 3 — the pin widened from ten routes to seventeen

**Derived by subtraction, then checked against CONTEXT § 1 — they agree exactly.** 17 open
`/intakes` routes (from the walker) minus the 10 in the test file's own header table gives:

| Row | Route | Expected | Verified at source |
|---|---|---|---|
| 11 | `POST /intakes` | **201** | `intake_routes.py:525` `status_code=HTTP_201_CREATED` |
| 12 | `PATCH /intakes/{id}` | 200 | `:633`, default |
| 13 | `GET /intakes/{id}/context-pack` | 200 | `:803`, scoped-empty by design |
| 14 | `GET /intakes/{id}/skill-runs/stream` | 200 | SSE |
| 15 | `GET /intakes/{id}/sources` | 200 | `:837`, scoped-empty by design |
| 16 | `POST /intakes/{id}/storage/uploads` | **201** | `storage_routes.py:141-142` |
| 17 | `DELETE /intakes/{id}/storage/objects` | 200 | `:317`, default |

No disagreement with CONTEXT § 1's seven-row table.

**All seventeen pinned honestly — no exclusion was needed.** Row 14 (SSE) is driven with
`TestClient(app).stream("GET", ...)` plus a **TERMINAL** skill run (`_seed_skill_run` writes
`status='succeeded'`), the shape `tests/test_sse_stream.py:174` established: the at-connect
snapshot closes the stream immediately, so there is no timeout hazard.

**Rows 16 and 17 name their category, in the header table AND both docstrings.** After plan 02,
a `role=user` uploading `reports` correctly gets 422 and deleting a `reports/` key correctly
gets 404. A pin written with a `reports/` key would have gone red *because plan 02 worked* —
a false regression alarm inside the plan whose job is telling real regressions from correct new
behaviour. Both pins use `attachments`, one of the two in `CLIENT_WRITABLE_CATEGORIES`.

**Anti-vacuity:** `test_every_open_client_route_is_pinned` derives the pin count by module
introspection AND independently derives the ungated `/intakes` route count from
`_flatten_routes` over the real app, asserts them equal, then asserts 17. A hard-coded number
alone cannot satisfy it.

**Falsifiability proven by two temporary mutations, both reverted (production diff EMPTY):**

| Mutation | Result |
|---|---|
| inline `if identity.role != "superadmin": raise 404` at the top of `create_intake` | `test_create_intake_open_to_user` **RED** |
| `superadmin_gate` on `GET /intakes/{id}/sources` at route level | `test_sources_open_to_user` **RED** *and* `test_every_open_client_route_is_pinned` **RED** — both arms live |

**No production code was changed.** `git diff --stat -- backend/app/` after revert: empty.
The ten pre-existing pins are untouched; the diff on this file is additions plus the header
table edit.

---

## Changed assertions across the phase, old vs new

Only two pre-existing assertions in the client-surface pin changed during the whole phase, both
by earlier plans; I read the diff rather than trusting the SUMMARYs:

| File | Old | New | Plan / reason |
|---|---|---|---|
| `test_client_surface_open.py` | probe key `"stay_open_probe"` | `"geo_scope"` | 23.2-09 — since D-23.2-05 a client write is bound to the canonical schema, so an invented key is 422. **Only the seed changed; `assert resp.status_code == 200` is byte-identical.** |
| `test_client_surface_open.py::test_list_templates_open_to_user` | (nothing after `len(body) >= 1`) | + an `admin_only` section-leak assertion | 23.2-06 — the route now returns a FILTERED body; the status assertion alone no longer describes the contract |
| `test_research_event_cursor.py` | `test_the_intake_alembic_head_is_0015` / `heads == {"0015"}` | `..._0016` / `{"0016"}` | 23.2-10 — a deliberate head hardcode MUST move when a migration lands |
| `test_ai_dedup.py` | head-pinned precondition on 0015 | repaired for 0016 | 23.2-10, same reason |

**All ten `*_open_to_user` assertions were read line by line, not merely watched pass**: every
one is `assert resp.status_code == 200`. No `!= 404`, no `< 400`, no tuple membership anywhere
in the file. The seven new pins follow the same discipline (two of them at exactly 201).

**Every changed file traces to a plan.** 30 non-`.planning` files changed; 28 appear in some
plan's `files_modified`. The two that do not — `test_ai_dedup.py` and
`test_research_event_cursor.py` — are enumerated in **plan 23.2-10's SUMMARY** (lines 17-18,
33-34) as a documented **Rule 3** deviation with reasons. Nothing was changed by no plan.
(Minor: plan 10's PLAN frontmatter declares `backend/tests/test_research_routes.py`, which was
never modified — an over-declaration, harmless.)

---

## False, imprecise or self-contradicting acceptance criteria — reported with measured values

| # | Criterion as written | Measured reality |
|---|---|---|
| 1 | Task 1: "**65 routes total** across the app, 26 carrying `superadmin_gate`" and "`assert routes_walked == 65`" | **Imprecise denominator.** 65 is the count of ALL leaves; **4 of them are Starlette `Route` objects** (`/openapi.json`, `/docs`, `/docs/oauth2-redirect`, `/redoc`) with no `dependant` at all — they cannot carry a gate. The gate-bearing population is **61 APIRoutes**. The 26/43/17 figures and every per-router split are exactly right. Reported rather than contorted: `assert routes_walked == 65` is only satisfiable by counting routes that structurally cannot be gated. |
| 2 | Task 1 `<done>`: "The **seven** seams each carry a verdict" | **False — there are EIGHT.** The objective heading was corrected to "THE EIGHT SEAMS" in `20bbb47`, but this residual "seven" in the `<done>` block (plan line 287) survived the fix. All eight carry a verdict above. |
| 3 | `<verification>` step 3 and `<interfaces>`: "`test_client_surface_open.py` — **12 green**" | **Falsified by Task 3 of the same plan**, which widens the file to 17 pins + the counter = **20 tests**. Internally inconsistent: verification item 3 asserts a number Task 3 is instructed to change. Measured: 12 at base, **20** at completion. |
| 4 | Task 2: "DEF-23.2-01 .. DEF-23.2-11 ... **Eleven rows; assert the count**" | **CONTEXT contradicts itself.** § 10's table does carry eleven rows, but **`23.2-CONTEXT.md:218` already allocates `DEF-23.2-12`** in § 2. So the "assert eleven" criterion is true of the table and false of the document. Recorded in the register as the twelfth item rather than silently renumbered. |
| 5 | Task 2: "the tribunal cross-file logging pollution ... a **new** finding this phase made" | **Not new to this plan** — plan 23.2-05 recorded it. Also **not merely inherited**: I re-measured four of its seven isolation rows independently rather than copying, and the DSN-dependence (32 passed / 0 failed with no `DATABASE_URL`) is what makes it invisible to a DSN-less CI. |
| 6 | CONTEXT § 1's line references for the seven new pins (`intake_routes.py:443`, `:630`, `:664`; `storage_routes.py:135-137`) | **All four line numbers drifted** — the real declarations are `:525`, `:803`, `:837` and `:141-142`, because this phase added ~200 lines to `intake_routes.py`. **Every expected STATUS CODE was correct**; only the citations aged. Verified at source, not copied. |
| 7 | Environment block: "MEASURED at `922fd91`: backend **608 passed**" | Correct for `922fd91`, stale for this plan's base — at `622cc89` it is **728**. Not a defect: the plan's own `<behavior>` asks for `608 + the sum of each plan's new tests`, which reconciles exactly (see above). |

Nothing was contorted to satisfy any of these.

---

## The deferral register

`.planning/phases/.../deferred-items.md` — **15 `DEF-23.2-*` ids**: the eleven from CONTEXT § 10
verbatim with reasons, plus four this phase found and deliberately did not fix.

* **DEF-23.2-12** — a real user-visible **DATA-LOSS** defect (plan 09). Verified independently:
  `intake.$id.tsx:106` sets `editable: intake.status === "draft"`, and `IntakeForm.tsx:210`
  opens `if (!editable) return true`. So during the validation phase the client's PATCH is
  skipped **and reports success**; `doSubmit` gates on that and advances the status having
  persisted nothing. `confirmedDiffKeys` (`:129`) is local `useState`, never transmitted. A
  client ticks "keep Nestor's proposal", presses Akkoord, and the tick is discarded. One-line
  frontend fix; frontend is fenced this phase. The server already accepts exactly those writes
  (D-23.2-05) — **defence in depth, not a live-path rescue**.
* **DEF-23.2-13** — `required` / `min_length` / `min_items` are browser-only; home is `/submit`.
* **DEF-23.2-14** — the tribunal cross-file `caplog` pollution, with its full isolation table.
* **DEF-23.2-15** — alembic 1.18.4's `compare_indexes` never inspects `postgresql_where`.

**DEF-23.2-09 / -10 numbers re-checked and NOT stale**: both name frontend figures and the
phase's frontend diff is EMPTY, so neither moved.

**Corrected a stale note**: the `upload_file` category gap is **NOT** a deferral — it was
promoted to D-23.2-17 and FIXED in plan 02 from the shared `CLIENT_WRITABLE_CATEGORIES`.

Also carried: the three residual-risk statements verbatim (D-23.2-11 token window, D-23.2-15
outbox durability, the minimum-constraint gap), and the inherited operator work unchanged.

---

## ⚠ OPERATOR WARNING — carried here as well as in the register, so it does not die with this session

**Do NOT append `test_checkpoint_resume.py` to `tribunal/cloudbuild.test-critical.yaml:32`
alongside `test_run_ownership_fence.py` or `test_stale_reclaim.py`.**

Verified at `622cc89`: that line is a **SINGLE** `python -m pytest` invocation over four files
(`test_schema_isolation.py`, `test_advisory_lock_exactly_once.py`, `test_hash_chain_replay.py`,
`test_verification_report_endpoint.py`) — one process, one logging configuration.

**Reproduced independently, twice, under the `app_user` DSN:**

| Combination | Result |
|---|---|
| `test_run_ownership_fence.py` + `test_checkpoint_resume.py` | **2 failed**, 38 passed, 10 skipped |
| `test_stale_reclaim.py` + `test_checkpoint_resume.py` | **2 failed**, 29 passed, 5 skipped |
| the same combination with **no** `DATABASE_URL` | **32 passed, 0 failed**, 18 skipped — CLEAN |

Same two casualties every time: `test_a_payload_from_another_checkpoint_version_is_discarded`
and `test_an_oversized_payload_is_refused_and_nothing_is_written`. Both assert on `caplog` for
WARNINGs emitted via **stdlib logging** from `pipeline/tribunal/checkpoints.py:234`.

So the standing remediation ("append the missing files to `:32`") would give CI **two spurious
failures** that read as *"the new tests broke the build"*, get reverted, and leave the
37-of-37 gap open another cycle — **the gap closing itself out of existence by looking like a
regression.** It is **DSN-dependent**, so a CI job without a faithful DSN will never see it
coming.

**Two remediations, both named:** (1) give `test_checkpoint_resume.py` its OWN pytest
invocation in that config, or (2) fix the logging leak first. **Neither was done here** — the
CI gap is fenced by CONTEXT § 9.

---

## Inherited operator work — carried forward, NOT closed

1. Tribunal services not deployed (`tribunal-api-00023-bc6` / `tribunal-worker-00009-fkm`).
2. The live `role=user` 404/200 observation never made.
3. The tribunal CI gap. ⚠ Plan 05's new `cloudbuild.test-cas.yaml` covers only THIS phase's own
   file, **does not close the gap**, and **is wired to no trigger**.
4. The tfstate bucket.
5. `app_superadmin` rotation.

---

## Deploy state

**BUILT, TESTED, COMMITTED. Nothing is deployed.** No `gcloud builds submit`, no
`gcloud run deploy`, no provider call, no Tribunal run — **zero spend** across this plan and the
phase. **No `role=user` was ever observed against a live service**; every claim is a local test
result against a testcontainer Postgres.

## Deviations from Plan

None requiring a rule. The plan permitted "sixteen honest pins plus one documented exclusion";
all seventeen pinned safely, so no exclusion was taken. No production code was changed, no seam
was fixed, and no deferral was implemented.

## Commits

| Commit | Message |
|---|---|
| `36fd80e` | `test(23.2-11): widen the client-surface pin from ten routes to seventeen` |
| (this file) | `docs(23.2-11): phase deferral register` + SUMMARY |

## Self-Check: PASSED

- `backend/tests/test_client_surface_open.py` — FOUND, 20 tests collected and passing.
- `.planning/phases/.../deferred-items.md` — FOUND, 15 distinct `DEF-23.2-*` ids.
- Commit `36fd80e` — resolves in `git log`.
- Full suite re-measured after the final code commit: **736 passed, 2 skipped, 0 failed**.
- Production diff after both temporary mutations were reverted: **EMPTY**.
