---
phase: 15-engine-enhancements-plan-critique-draft-tournament-deferred-
plan: 05
type: execute
wave: 4
depends_on: [15-04]
files_modified:
  - frontend/src/lib/api/research.ts
  - frontend/src/components/intake/ResearchRunProgress.tsx
  - frontend/src/components/intake/AuditBodyPanel.tsx
  - frontend/src/components/intake/VerificationReport.tsx
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/locales/en/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/nl/intake.json
autonomous: true
requirements: [ENGINE-09]
must_haves:
  truths:
    - "The research run progress panel renders a Replit-style agent-feed: agent cards with task prompt, per-row cost, status, visible retries, and per-block summary cards"
    - "Clicking an agent card's audit_id drill-down opens a collapsible/side panel that fetches and renders the redacted audit body (request/response) per the D15 mockup — not a no-op stub"
    - "intakeId and runId are threaded route → ResearchRunProgress → AuditBodyPanel so getAuditBody is called with all three arguments (intakeId, runId, auditId)"
    - "A superadmin-only VerificationReport renders the gate funnel, verdicts, superseded/reconciled notes, unverified list, and true itemized cost from the recorded run"
    - "Both surfaces mount ONLY under admin.pulse.* — no client route imports them (16-D-08)"
    - "Every new UI string is keyed in en/fr/nl and i18n-audit passes"
  artifacts:
    - path: "frontend/src/components/intake/ResearchRunProgress.tsx"
      provides: "D15 agent-feed renderer over enriched stage_detail"
      contains: "cost"
    - path: "frontend/src/components/intake/VerificationReport.tsx"
      provides: "superadmin funnel + verdicts + unverified + cost panel"
      min_lines: 40
    - path: "frontend/src/lib/api/research.ts"
      provides: "getVerification() + getAuditBody() + VerificationReport/feed/AuditBody types"
      contains: "getVerification"
    - path: "frontend/src/components/intake/AuditBodyPanel.tsx"
      provides: "drill-down panel rendering the redacted audit body (request/response)"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/intake/VerificationReport.tsx"
      to: "research.ts getVerification"
      via: "fetch on mount"
      pattern: "getVerification"
    - from: "frontend/src/components/intake/ResearchRunProgress.tsx"
      to: "stage_detail items"
      via: "toStageRows enriched renderer"
      pattern: "toStageRows"
    - from: "frontend/src/components/intake/AuditBodyPanel.tsx"
      to: "research.ts getAuditBody"
      via: "fetch on drill-down open (intakeId, runId, auditId)"
      pattern: "getAuditBody"
    - from: "frontend/src/components/intake/ResearchRunProgress.tsx"
      to: "AuditBodyPanel"
      via: "intakeId (prop) + runId (from run.id) threaded into the drill-down panel"
      pattern: "AuditBodyPanel"
---

<objective>
Grow the existing `ResearchRunProgress` panel into the D15 Replit-style activity feed (agent cards, task-prompt expand, per-row cost, visible retries, per-block summary cards) and add a superadmin-only `VerificationReport` component (funnel + verdicts + superseded/reconciled + unverified + true itemized cost), both anchored on the admin intake detail page. Prove it all against the recorded run — no live LLM run.

Purpose: This is the operator's professionalism-and-confidence surface (the explicit visual bar is `replit view.png` + the D15 mockup). The feed EXTENDS the Phase-16 dynamic-stage contract — new stages/items render for free.
Output: enriched research.ts types + getVerification, the D15 feed renderer, the VerificationReport component, the drill-down AuditBodyPanel wired with intakeId+runId, and i18n keys in three locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/RESEARCH-ENGINE-DECISIONS.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-RESEARCH.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-PATTERNS.md

<interfaces>
<!-- Extracted from repo. Visual bar: replit view.png (repo root) + RESEARCH-ENGINE-DECISIONS.md D15 mockup. -->

Dynamic flatten to extend (ResearchRunProgress.tsx:43-62) — D-07 contract, ZERO hardcoded stage count:
  function toStageRows(run): StageRow[] {
    for (const [stageKey, group] of Object.entries(run.stage_detail)) {
      const items = group?.items ?? [];
      for (const [idx, item] of items.entries())
        rows.push({ key: `${stageKey}:${idx}`, name: item.name, status: item.status /*, cost, retry, task_prompt */ });
    }
  }
  SSE hook useActiveResearchRun (71-110): openResearchStream + cancelled-cleanup + terminal close().

Prop threading (VERIFIED in repo during revision — the blocker fix):
  admin.pulse.intakes.$id.tsx:1172 currently instantiates:
    <ResearchRunProgress intakeId={intake.id} onRetry={onRetryResearch} />
  So `intakeId` is ALREADY passed from the route. `runId` is NOT a route prop — it is internal to
  ResearchRunProgress via the SSE hook: useActiveResearchRun returns `run: ResearchRun | null`, and
  `run.id` is the run id. Threading for the audit drill-down: ResearchRunProgress owns `intakeId` (prop)
  and `run.id` (from useActiveResearchRun); it passes BOTH into AuditBodyPanel as `intakeId` + `runId`.
  AuditBodyPanel then calls getAuditBody(intakeId, runId, auditId) with all three. The route file only
  changes if a new prop is needed — since intakeId is already threaded and runId is internal, keep the
  route's <ResearchRunProgress .../> call as-is UNLESS a runId must be lifted; if the route already has
  the run id available and prefers to pass it, add `runId={...}` there. Default: no route-signature change,
  runId sourced from run.id inside the component. (Route file is in files_modified to allow that
  optional prop wire and to satisfy the key-link audit; if untouched, the SUMMARY notes it was verified
  no-change.)

research.ts one-shot fetch shape to clone (getBundleUrl 98-106) — never fork the transport:
  export function getBundleUrl(intakeId, runId): Promise<ApiResult<{url; expires_in}>> {
    return apiFetch<...>(`/intakes/${intakeId}/research/${runId}/bundle-url`, { method: "GET" }); }
  Existing types: ResearchStageItem (39), ResearchStageDetail (44), ResearchRun (49).

Enriched stage_detail item (from Plan 15-01 fixture + Plan 15-03): {name, status, task_prompt, cost_usd, facts, retry{attempt,max,wait_s}, audit_id}
  + per-stage summary {duration_s, actions, items_read, cost_usd}. The recorded-run fixture (15-01)
  seeds these enriched fields, so the feed renders real per-row cost + audit_id drill-down at UAT.
Verification report shape (from Plan 15-03 build_verification_report): funnel, verdict lists
  (support/refute/insufficient), superseded, reconciled, unverified list, true cost + cost_pending.

D15 required feed elements (RESEARCH-ENGINE-DECISIONS D15): agent cards on spin-up with task (expandable);
  per-agent status spinner→"done · 14 facts · $0.12"; narration lines; per-block summary
  "Worked for X · N actions · M items read · $Y"; collapse/expand + scroll-to-latest; retries shown as
  "retry 2/3 — waiting 8s" not hidden; feed stays frozen+clickable after run.

Placement (16-D-08): mount ONLY at admin.pulse.intakes.$id.tsx (line ~1172, where ResearchRunProgress lives).
  NEVER import into a client route. VerificationReport opens behind the D-09 summary card's "View verification report".
i18n: every string keyed en/fr/nl intake.json; i18n-audit.mjs is a HARD gate (CLAUDE.md).
Style: @/ imports only; sonner toasts; return-no-throw; shadcn ui/ not modified directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: research.ts — getVerification + enriched feed/verification types</name>
  <read_first>
    - frontend/src/lib/api/research.ts (getBundleUrl 98-106; ResearchStageItem 39; ResearchRun 49; ApiResult import)
  </read_first>
  <action>
    In `research.ts`, extend `ResearchStageItem` with optional `task_prompt?: string`, `cost_usd?: string`, `facts?: number`, `retry?: { attempt: number; max: number; wait_s: number }`, `audit_id?: string`, and add an optional per-stage `summary?: { duration_s: number; actions: number; items_read?: number; cost_usd: string }` on the stage-detail group type. Add a `VerificationReport` type (funnel, verdict lists, superseded[], reconciled[], unverified list, cost { total: string; pending: boolean }) and an `AuditBody` type `{ audit_id: string; provider: string | null; model: string | null; request: unknown; response: unknown }` (matching Plan 15-03's AuditBody / Plan 15-04's proxy). A `Citation` type is NOT added here (that is Plan 15-06). Add `export function getVerification(intakeId, runId): Promise<ApiResult<VerificationReport>>` cloning `getBundleUrl`'s one-shot `apiFetch` shape, hitting `/intakes/${intakeId}/research/${runId}/verification`, method GET, return-no-throw. Also add `export function getAuditBody(intakeId, runId, auditId): Promise<ApiResult<AuditBody>>` cloning the same shape, hitting `/intakes/${intakeId}/research/${runId}/audit/${auditId}`, method GET, return-no-throw.
    Do NOT fork the transport — reuse `apiFetch`. Do NOT modify the SSE `openResearchStream` fn.
  </action>
  <verify>
    <!-- Bare tsc — exit code propagates (NO `|| echo` mask; global type errors fail the gate). -->
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function getVerification' research.ts` == 1, hitting `/verification`, method GET, returning ApiResult<VerificationReport>
    - `grep -c 'export function getAuditBody' research.ts` == 1, hitting `/research/${runId}/audit/${auditId}`, method GET, returning ApiResult<AuditBody>, signature `(intakeId, runId, auditId)`
    - ResearchStageItem gains task_prompt/cost_usd/facts/retry/audit_id as OPTIONAL fields (existing runs still type-check)
    - A `VerificationReport` type and an `AuditBody` type ({audit_id, provider, model, request, response}) exist
    - getVerification + getAuditBody call `apiFetch` (not a forked transport); openResearchStream unchanged
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (no type errors anywhere — not just in research.ts)
  </acceptance_criteria>
  <done>research.ts exposes getVerification + getAuditBody(intakeId, runId, auditId) and the enriched feed/verification types; transport unforked; full typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: ResearchRunProgress — D15 agent-feed renderer + audit drill-down threading</name>
  <read_first>
    - frontend/src/components/intake/ResearchRunProgress.tsx (toStageRows 43-62; useActiveResearchRun 71-110; summary card ~311; current status icons/lucide imports; run.id source)
    - frontend/src/routes/admin.pulse.intakes.$id.tsx (line ~1172 — the `<ResearchRunProgress intakeId={intake.id} onRetry={...} />` instantiation)
    - frontend/src/lib/api/research.ts (getAuditBody + AuditBody type from Task 1)
    - .planning/RESEARCH-ENGINE-DECISIONS.md (D15 mockup + required elements — the drill-down-to-audit-blob is part of the locked mockup)
    - replit view.png (visual bar — the executor should open this image)
  </read_first>
  <action>
    Grow `toStageRows` to carry the enriched item fields (cost_usd, task_prompt, retry, facts, audit_id) into each row — additively; a row missing them renders as today (D-07 contract preserved). Add an `AgentCard` sub-component rendering: task title + expandable task_prompt (collapse/expand, "Show less"), a status line mapping status→lucide icon (spinner running / check done / rotate retry / warning failed / dot pending) with `done · {facts} facts · ${cost_usd}`, a visible retry state `retry {attempt}/{max} — waiting {wait_s}s`, and per-stage summary cards `Worked for {duration_s} · {actions} actions · {items_read} items read · ${cost_usd}`. Add a "scroll to latest" affordance and calm-by-default collapsing. Keep the SSE hook and terminal-close behavior unchanged so a completed (recorded) run's feed "stays frozen and clickable".

    AUDIT DRILL-DOWN WIRING (blocker fix — the panel must be able to make a valid seam call). Create `frontend/src/components/intake/AuditBodyPanel.tsx`: props `{ intakeId: string; runId: string; auditId: string }`; it fetches `getAuditBody(intakeId, runId, auditId)` (return-no-throw; sonner toast on error) and renders the redacted request/response body in a collapsible/side panel per the mockup (JSON/markdown-safe rendering; provider+model header; NO live-URL fetch, GCS-sourced body only). Thread the ids: ResearchRunProgress ALREADY receives `intakeId` as a prop (route passes `intakeId={intake.id}` at admin.pulse.intakes.$id.tsx:1172); the `runId` comes from the SSE run — `useActiveResearchRun` returns `run`, use `run.id` as `runId`. In `ResearchRunProgress`, wire the agent card's `audit_id` to an `onDrillDown(auditId)` handler that opens `AuditBodyPanel` passing `intakeId={intakeId} runId={run.id} auditId={auditId}` (all three) — NOT a no-op stub. When a row has no `audit_id`, OR when `run?.id` is absent, the drill-down affordance is hidden (guard against calling getAuditBody without a runId). If lifting `run.id` to a route-supplied `runId` prop is cleaner in the executor's judgement, add an optional `runId?` prop to ResearchRunProgress and pass it from admin.pulse.intakes.$id.tsx (route file is in files_modified for exactly this optional wire); otherwise source it from `run.id` internally and leave the route call unchanged. Superadmin-only by PLACEMENT — do not import into any client route. Every visible string via i18n keys (added in Task 3).
  </action>
  <verify>
    <!-- Bare tsc (exit code propagates) + a client-route-import guard that FAILS (non-zero) if the feed/drill-down
         panels leak into any non-admin route (16-D-08). -->
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
    <automated>cd frontend && ! grep -rEln 'VerificationReport|AuditBodyPanel' src/routes/ --include='*.tsx' | grep -v 'admin\.'</automated>
  </verify>
  <acceptance_criteria>
    - toStageRows carries cost_usd/task_prompt/retry/facts/audit_id onto rows without breaking rows that lack them
    - An AgentCard renders task title, expandable prompt, status icon, `done · N facts · $X`, retry state, and per-block summary
    - Retry state is rendered as a visible card (not hidden) — grep finds a retry label using the i18n key
    - `onDrillDown` opens `AuditBodyPanel` which fetches `getAuditBody(intakeId, runId, auditId)` with ALL THREE arguments (source assertion: grep finds `getAuditBody(` called with three args / props intakeId+runId+auditId passed to AuditBodyPanel) and renders the redacted request/response body (NOT a no-op stub); the affordance is hidden when audit_id OR run.id is absent; grep finds NO `() => {}`/no-op wired to onDrillDown
    - AuditBodyPanel signature is `{ intakeId, runId, auditId }` and renders GCS-sourced body only (no live-URL fetch); superadmin placement only
    - The SSE hook (useActiveResearchRun) and terminal close are unchanged; neither component is imported by any client route (the grep guard exits 0 = no non-admin route import)
    - All new visible strings use i18n keys (no hardcoded literals — i18n-audit will gate)
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (full typecheck, no `|| echo` mask)
  </acceptance_criteria>
  <done>The feed renders agent cards, per-row cost, visible retries, per-block summaries, and a REAL audit-body drill-down panel that receives intakeId+runId+auditId, over the enriched stage_detail, extending (not replacing) the dynamic-stage contract.</done>
</task>

<task type="auto">
  <name>Task 3: VerificationReport component + en/fr/nl i18n keys</name>
  <read_first>
    - frontend/src/components/intake/ResearchRunProgress.tsx (superadmin placement + fetch/toast pattern + SSE-off read style)
    - frontend/src/components/intake/AdminResearchResultsPanel.tsx (admin panel + react-markdown rendering analog)
    - frontend/src/locales/en/intake.json (existing key structure)
    - frontend/src/locales/nl/intake.json
    - frontend/src/locales/fr/intake.json
    - frontend/src/lib/api/research.ts (getVerification + VerificationReport type from Task 1)
  </read_first>
  <action>
    Create `frontend/src/components/intake/VerificationReport.tsx` — a superadmin-only component that fetches `getVerification(intakeId, runId)` on mount (return-no-throw; sonner toast on error) and renders: the verification funnel (distilled / selected / sessions / verdicts / skipped+why / failed-loud counts), verdict sections (refuted claims with skeptic evidence + effect, support, insufficient), superseded/scoped findings with their temporal caveat inline, reconciled contradictions with the chosen canonical value, the honest UNVERIFIED list, and true itemized cost showing each class plus a "tool fees: pending" state when `cost.pending` is true (never a number when pending). Render markdown fields via the existing react-markdown setup used in admin panels. Mount it behind the D-09 summary card's "View verification report" action at the existing admin anchor — do NOT create a new route and do NOT import into a client route.
    Add every new string as keys in `frontend/src/locales/{en,fr,nl}/intake.json` (feed AgentCard labels + AuditBodyPanel labels from Task 2 AND VerificationReport labels) — all three locales, matching the existing nesting. Run/emulate `i18n-audit.mjs` mentally: no hardcoded UI string may remain.
  </action>
  <verify>
    <!-- i18n hard gate + bare tsc (exit code propagates) + client-route-import guard (16-D-08) for VerificationReport. -->
    <automated>cd frontend && node scripts/i18n-audit.mjs</automated>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
    <automated>cd frontend && ! grep -rEln 'VerificationReport|AuditBodyPanel' src/routes/ --include='*.tsx' | grep -v 'admin\.'</automated>
  </verify>
  <acceptance_criteria>
    - VerificationReport.tsx fetches getVerification, is superadmin-placement only, and renders funnel + verdicts + superseded + reconciled + unverified + itemized cost
    - When `cost.pending` is true, a "pending" label renders and NO numeric placeholder is shown for the pending class
    - Every new string exists in en/fr/nl intake.json (same keys in all three); `i18n-audit.mjs` exits green
    - The component is not imported outside the admin route tree — the client-route-import grep guard exits 0 (fails non-zero if VerificationReport/AuditBodyPanel appear in a non-admin route import)
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (full typecheck, no mask)
  </acceptance_criteria>
  <done>The superadmin verification report renders all required content from the recorded run with facts-only cost, fully i18n'd in three locales; the 16-D-08 client-blindness invariant is enforced by an automated route-import guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React component → client role | Research surfaces must never mount on a client-reachable route (16-D-08) |
| UI → intake API | Reads go through superadmin proxy (Plan 15-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Information Disclosure | VerificationReport/feed on a client route | mitigate | mount ONLY under admin.pulse.*; automated route-import grep guard (Tasks 2/3) exits non-zero on any non-admin route import; server proxy is superadmin-only (Plan 15-04) |
| T-15-13 | Information Disclosure | estimated cost shown to operator | mitigate | pending state renders a label, never a number when cost.pending (C1 facts-only) |
| T-15-13b | Information Disclosure | audit-body drill-down on a client route / re-exposing keys | mitigate | AuditBodyPanel mounts superadmin-side only (route-import guard) + goes through the Plan 15-04 superadmin proxy; body is already redacted server-side; no live-URL fetch |
| T-15-SC | Tampering | npm installs | mitigate | none this plan — no new packages (reuse lucide-react/sonner/react-markdown already in repo) |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` clean (exit 0, no `|| echo` mask); `npm run build` succeeds (author-by-construction — run in Cloud Build/CI).
- `node scripts/i18n-audit.mjs` green (all new strings keyed en/fr/nl).
- Client-route-import guard: `! grep -rEln 'VerificationReport|AuditBodyPanel' src/routes/ --include='*.tsx' | grep -v 'admin\.'` exits 0 (16-D-08 enforced automatically).
- Manual UAT (deferred, no live run): operator opens the recorded run on admin intake detail, walks the feed + verification report vs replit view.png + D15 mockup.
</verification>

<success_criteria>
- D15 agent-feed foundation renders per the operator-agreed mockup, per-row cost visible, extending the dynamic-stage contract (Phase-15 SC2).
- Superadmin-only verification report renders funnel + verdicts from recorded data, and the feed's audit_id drill-down renders the real redacted audit body per the D15 mockup, with getAuditBody called with all three ids (Phase-15 SC1/SC2).
- Cost display is facts-only with a pending state, never an estimate (Phase-15 SC3).
</success_criteria>

<output>
Create `.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-05-SUMMARY.md` when done
</output>
