---
phase: 15-engine-enhancements-plan-critique-draft-tournament-deferred-
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - tribunal/nestor_pulse_sdk/audit/cost_table.py
  - tribunal/nestor_pulse_sdk/audit/cost_prices.json
  - tribunal/nestor_pulse_sdk/audit/audited_llm_client.py
  - tribunal/nestor_pulse_sdk/tests/test_cost_cache_write.py
autonomous: true
requirements: [ENGINE-09]
must_haves:
  truths:
    - "Anthropic cache-CREATE tokens are priced at the cache_creation_5m rate and included in per-call cost"
    - "Anthropic web_search/web_fetch tool-fee counts are recorded per call and priced"
    - "Gemini deep-research usageMetadata (prompt/candidate/thoughts tokens) is recorded so compute() prices it, or the row is marked pending — never estimated"
    - "Unknown model still returns NULL cost, never a guess"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/audit/cost_table.py"
      provides: "compute() with cache_creation_tokens param + tool-fee accounting"
      contains: "cache_creation_tokens"
    - path: "tribunal/nestor_pulse_sdk/tests/test_cost_cache_write.py"
      provides: "cache-write + tool-fee + DR-usage + NULL-on-unknown tests"
      min_lines: 40
  key_links:
    - from: "tribunal/nestor_pulse_sdk/audit/audited_llm_client.py"
      to: "cost_table.compute"
      via: "cache_creation_input_tokens threaded into compute + persisted"
      pattern: "cache_creation_tokens"
    - from: "tribunal/nestor_pulse_sdk/audit/audited_llm_client.py"
      to: "audit_log.cache_creation_tokens"
      via: "persist non-hashed column"
      pattern: "cache_creation"
---

<objective>
Fix the three C1 cost-truth defects in the tribunal audited client so displayed cost is facts-only: (a) charge Anthropic cache-CREATE tokens, (b) count + price web_search/web_fetch tool fees, (c) record Gemini deep-research usageMetadata (or mark pending). Never estimate; unknown model still returns NULL.

Purpose: `run.cost_usd_total = SUM(audit_log.cost_usd)`, so fixing per-call cost fixes the run total. The last run's panel showed ~€5 vs ~$43–45 real. This makes the numbers countable and honest.
Output: Corrected `compute()`, tool-fee + DR-usage recording, price-file entries, and a new cost test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/RESEARCH-ENGINE-DECISIONS.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-RESEARCH.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-PATTERNS.md

<interfaces>
<!-- Extracted from repo. -->

cost_table.compute() core formula (cost_table.py:111-126) — extend with a cache-create term:
  non_cached = max(0, prompt_tokens - cached_tokens)
  prompt_cost = non_cached * (entry["prompt"]/1e6)
  cache_cost  = cached_tokens * (entry["cache_read"]/1e6)
  completion_cost = completion_tokens * (entry["completion"]/1e6)
  return prompt_cost + cache_cost + completion_cost
  ADD: cache_create_cost = cache_creation_tokens * (entry["cache_creation_5m"]/1e6)  # rate already in JSON

Unknown-model guard to PRESERVE (cost_table.py:103-109) — never guess:
  if key not in prices: log warning; return None   (Pitfall 5 / test_unknown_model_null)

Anthropic extraction ALREADY present (audited_llm_client.py:304-318) — cache_creation_input_tokens
  is extracted at line 309 then DROPPED. Thread it into compute() AND persist to audit_log.cache_creation_tokens.

Gemini DR (audited_llm_client.py:754-839) — gemini_deep_research_raw returns {status, report} and DROPS
  interaction.usageMetadata (promptTokenCount, candidatesTokenCount, thoughtsTokenCount; thoughts bill at output rate).

C1 rules (RESEARCH-ENGINE-DECISIONS.md Area 10): count all 4 Anthropic token classes + web_search
  ($10/1000 searches) + web_fetch fees; record DR usageMetadata; Gemini search/grounding tool fees NOT
  itemized live → row shows "pending", exact amount backfilled from GCP billing. NO estimate ever.

Frozen-payload safety: cost_usd + all new cost columns are OUTSIDE _payload_for_row (Plan 15-01 proved this).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute() — charge cache-CREATE tokens + preserve NULL-on-unknown</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/audit/cost_table.py (compute() 77-126, unknown-model guard 103-109)
    - tribunal/nestor_pulse_sdk/audit/cost_prices.json (cache_creation_5m entries)
    - tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py (test_anthropic_cache_tokens_extracted — assertion style)
  </read_first>
  <behavior>
    - compute(..., cache_creation_tokens=N) charges N × (entry["cache_creation_5m"]/1e6) on top of prompt+cache_read+completion
    - cache_creation_tokens defaults to 0 → existing callers keep identical results (backward compatible)
    - Unknown provider/model still returns None (never a guessed number)
    - Decimal arithmetic throughout (no float) — mirror existing Decimal(str(...)) pattern
  </behavior>
  <action>
    Add a `cache_creation_tokens: int = 0` keyword param to `compute()`. After the existing three cost terms, add `cache_create_per_token = Decimal(str(entry["cache_creation_5m"])) / Decimal("1000000")` and `cache_create_cost = Decimal(str(cache_creation_tokens)) * cache_create_per_token`, then include it in the returned sum. Keep the unknown-model `return None` guard untouched. Every non-anthropic provider passes cache_creation_tokens=0 by default so no other formula changes.
    Verify `cost_prices.json` contains a `cache_creation_5m` value for the sonnet model actually used (sonnet-4-6); if any anthropic entry used at run time lacks `cache_creation_5m`, add it at the published 1.25× base-input rate (fact from provider docs, not an estimate) — do NOT invent a rate for a model not in the run.
  </action>
  <verify>
    <automated>cd tribunal && python -c "import ast; ast.parse(open('nestor_pulse_sdk/audit/cost_table.py').read()); import json; json.load(open('nestor_pulse_sdk/audit/cost_prices.json')); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cache_creation_tokens' cost_table.py` >= 2 (param + term)
    - The unknown-model `return None` branch is unchanged (grep still finds the Pitfall-5 warning + return None)
    - cost_prices.json remains valid JSON and every anthropic model referenced in the run has a `cache_creation_5m` key
    - No float literals introduced in the new cost term (Decimal(str(...)) pattern only)
  </acceptance_criteria>
  <done>compute() prices cache-write tokens at the 5m rate; unknown models still return NULL; JSON valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: audited_llm_client — thread cache-create, count tool fees, record DR usageMetadata</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/audit/audited_llm_client.py (anthropic extract 304-318; gemini_deep_research_raw 754-839)
    - tribunal/nestor_pulse_sdk/audit/writer.py (how a per-call audit row is written — where to persist cache_creation_tokens + tool counts)
    - tribunal/nestor_pulse_sdk/db/models/audit_log.py (the new cache_creation_tokens column from Plan 15-01)
    - tribunal/nestor_pulse_sdk/audit/cost_prices.json (web_search/web_fetch fee entries)
  </read_first>
  <behavior>
    - Anthropic path passes cache_creation_input_tokens into compute() AND persists it to audit_log.cache_creation_tokens
    - web_search/web_fetch server-tool result counts are recorded per call; web_search fee = searches × published per-search price ($10/1000) added to that call's cost
    - Gemini DR path reads interaction.usageMetadata (promptTokenCount/candidatesTokenCount/thoughtsTokenCount) and records tokens into the DR audit row so compute() prices it; if usageMetadata is absent for a call, that call's tool-fee/grounding cost is marked pending (cost_pending) — never estimated
    - Nothing enters the hashed payload
  </behavior>
  <action>
    In the anthropic branch, pass `cache_creation_tokens=cache_creation_input_tokens` into `self._costs.compute(...)` and persist `cache_creation_input_tokens` into the new `audit_log.cache_creation_tokens` column via the writer. Count web_search/web_fetch usage from the response's server-tool-use / result blocks; add a `web_search` fee entry to `cost_prices.json` (published $10/1000 searches) and include `searches × fee` in that call's `cost_usd`. Record the per-call tool counts into the audit row (new non-hashed metadata — reuse an existing JSON/metadata column if writer already has one; do not add to `_payload_for_row`).
    In `gemini_deep_research_raw`, read `interaction.get("usageMetadata", {})` and return it alongside `{status, report}`; at the call site record promptTokenCount/candidatesTokenCount/thoughtsTokenCount into the DR audit row (thoughts billed at output rate) so `compute()` prices the DR call. If `usageMetadata` is missing, set the run's `cost_pending=True` for the deep-research grounding fee and DO NOT write any placeholder number.
    Add the `cost_prices.json` `web_search`/`web_fetch` fee entries as facts (published rate), and resolve the deep-research ESTIMATE comment at cost_prices.json:84 — either price at the real published rate (fact) or mark that class pending; never display the estimate.
  </action>
  <verify>
    <automated>cd tribunal && python -c "import ast; ast.parse(open('nestor_pulse_sdk/audit/audited_llm_client.py').read()); import json; json.load(open('nestor_pulse_sdk/audit/cost_prices.json')); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cache_creation_tokens=cache_creation_input_tokens' audited_llm_client.py` >= 1 (threaded into compute)
    - audited_llm_client persists cache_creation to the audit row (grep finds the writer call carrying cache_creation)
    - `gemini_deep_research_raw` reads `usageMetadata` and no longer discards it (grep finds usageMetadata used, not only stored-as-nothing)
    - cost_prices.json has a `web_search` fee entry; the line-84 ESTIMATE comment is resolved (either real published rate OR a pending marker — no displayed estimate)
    - No new field added to hash_chain.py `_payload_for_row`
  </acceptance_criteria>
  <done>All three cost classes flow into per-call cost or a pending flag; nothing is estimated; the hashed payload is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_cost_cache_write — cache-write, tool fees, DR usage, NULL-on-unknown</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py (test_anthropic_cache_tokens_extracted — mock usage with cache_creation_input_tokens=50)
    - tribunal/nestor_pulse_sdk/audit/cost_table.py (compute signature after Task 1)
    - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py (recorded funnel/cost constants — for a DR-usage assertion if usable)
  </read_first>
  <behavior>
    - test_cache_write_charged: compute() with cache_creation_tokens>0 costs strictly more than the same call with 0, by exactly tokens × cache_creation_5m rate
    - test_web_search_fee_added: a call reporting N searches adds N × $0.01 to cost_usd
    - test_dr_usage_recorded: a DR interaction with usageMetadata yields non-zero DR token cost (or cost_pending set when absent)
    - test_unknown_model_null: unknown provider/model → compute() returns None (no guess)
  </behavior>
  <action>
    Create `tribunal/nestor_pulse_sdk/tests/test_cost_cache_write.py` with the four tests above, mirroring the mock-usage assertion style of `test_anthropic_cache_tokens_extracted`. Use exact expected Decimals computed from the prices in `cost_prices.json` (no approximations). For the DR-usage test, feed a synthetic `usageMetadata` dict and assert the recorded DR audit row carries the token counts; add a companion assertion that a missing usageMetadata sets `cost_pending` rather than writing a number.
  </action>
  <verify>
    <automated>cd tribunal && python -c "import ast; ast.parse(open('nestor_pulse_sdk/tests/test_cost_cache_write.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File defines test_cache_write_charged, test_web_search_fee_added, test_dr_usage_recorded, test_unknown_model_null
    - Expected costs are exact Decimals derived from cost_prices.json (no `~`, no rounded guesses — Pitfall 3)
    - Cloud Build target `pytest nestor_pulse_sdk/tests/test_cost_cache_write.py -x` documented in SUMMARY as the per-task gate (local run impossible)
    - test_unknown_model_null asserts `compute(...) is None`
  </acceptance_criteria>
  <done>The cost fixes are provable via Cloud Build: cache-write charged, tool fees added, DR usage recorded, unknown model NULL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider usage → audit row | Recorded token/tool counts must be exact, never guessed |
| new cost columns → frozen chain | Cost writes must stay outside the hashed payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | audit chain via cost persistence | mitigate | cache_creation_tokens/tool-counts/cost_pending are non-hashed columns (Plan 15-01); no _payload_for_row change |
| T-15-05 | Repudiation | fabricated/estimated cost numbers | mitigate | C1 facts-only: unknown → NULL, un-itemized DR grounding → pending, never a placeholder; test_unknown_model_null enforces |
| T-15-SC | Tampering | pip installs | mitigate | none this plan — no new packages (RESEARCH §Package Legitimacy: not required) |
</threat_model>

<verification>
- Cloud Build: `pytest nestor_pulse_sdk/tests/test_cost_cache_write.py -x` green.
- Cloud Build: `pytest nestor_pulse_sdk/tests/test_hash_chain_replay.py -x` still green (chain unaffected by cost writes).
- cost_prices.json parses; no estimate is ever displayed (line-84 comment resolved).
</verification>

<success_criteria>
- Every countable cost class counted; un-itemizable Gemini grounding fee = pending, not estimated (Phase-15 SC3).
- verify_chain green (Phase-15 SC5) — cost writes are non-hashed.
</success_criteria>

<output>
Create `.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-02-SUMMARY.md` when done
</output>
