---
phase: 15-engine-enhancements-plan-critique-draft-tournament-deferred-
plan: 07
type: execute
wave: 6
depends_on: [15-01, 15-02, 15-03, 15-04, 15-05, 15-06]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - .planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-UAT.md
autonomous: false
requirements: [ENGINE-09]
must_haves:
  truths:
    - "The runbook has a Phase 15 section with the dual rebuild order (tribunal-api + tribunal-worker), the tribunal alembic 0011 migrate step, the frontend rebuild, and an explicit no-new-secrets note"
    - "A UAT walkthrough exists that the operator runs against the recorded run-4cbb5311 on the admin intake detail page — feed replay, verification report, every [n] citation — with no live LLM run"
    - "verify_chain is re-run against the deployed audit data as the SC5 gate"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "Phase 15 deploy section"
      contains: "Phase 15"
    - path: ".planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-UAT.md"
      provides: "operator recorded-run walkthrough + V-02 checklist"
      min_lines: 30
  key_links:
    - from: "infra/DEPLOY-RUNBOOK.md"
      to: "tribunal-migrate job (0011)"
      via: "alembic upgrade head in the tribunal schema"
      pattern: "0011"
---

<objective>
Extend `infra/DEPLOY-RUNBOOK.md` with a Phase 15 deploy section (author-by-construction; no local Docker) and author the operator UAT walkthrough against the recorded run-4cbb5311. Then run the deploy + UAT as a blocking operator checkpoint.

Purpose: Phase 15 is code-complete only after the cost fix ships (tribunal image rebuild), the 0011 migration applies (verdict table + cost columns), and the new surfaces reach the browser (frontend rebuild). The recurring deploy-gap lesson (code-complete != deployed) means the runbook + UAT are mandatory. UAT is the V-02 operator-sign-off surface.
Output: a Phase 15 runbook section, a 15-UAT.md walkthrough, and an executed operator session.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/RESEARCH-ENGINE-DECISIONS.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-CONTEXT.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-RESEARCH.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-VALIDATION.md

<interfaces>
<!-- Extracted from repo (infra/DEPLOY-RUNBOOK.md). -->

Phase 17 dual-rebuild pattern (Step 17.a, lines 1335-1374) — the analog: tribunal-api REBUILT FIRST via
  Cloud Build (`--substitutions=_IMAGE=.../tribunal-api:${SHA}`), redeployed at $SHA, URL confirmed
  UNCHANGED via `gcloud run services describe` WITHOUT a path (Pitfall 4). For Phase 15, BOTH
  tribunal-worker (cost fix in audited_llm_client — the run-emitting side) AND tribunal-api (new
  /verification + enriched /metrics + numbering endpoints) change → BOTH rebuild.

tribunal-migrate Job pattern (Step 13.f, line 837 + checklist 1752): `gcloud run jobs execute
  tribunal-migrate --wait` runs `alembic upgrade head` into the tribunal schema (app_user DSN). Phase 15
  brings the line to 0011. Image-pin lesson (MEMORY, Phase 17): the migrate job must run the JUST-BUILT
  image, not a stale tag.

Frontend rebuild pattern (Step 17.d / 18.c): `nestor-frontend` REBUILT via Cloud Build, SAME
  `_API_BASE_URL`/`_FB_*` substitutions as Phase 12 (no URL re-wiring), NO `VITE_SUPABASE_*` (bundle
  guard green), regenerated routeTree.gen.ts only if routes changed (Phase 15 adds NO new route —
  components mount on the existing admin.pulse.intakes.$id anchor).

No new secrets expected (RESEARCH §Runtime State Inventory: SERPAPI is Phase 15.2/D10, not here).
V-02 UAT checklist (CONTEXT V-02): verification report renders funnel+verdicts+drill-down; feed shows
  agent-level activity + per-row cost; every [n] resolves; cost itemized facts-only with pending state;
  verify_chain green — ALL against the recorded run, no live LLM run (Anthropic cap until 2026-08-01).
Visual bar for UAT: replit view.png (repo root) + RESEARCH-ENGINE-DECISIONS.md D15 mockup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEPLOY-RUNBOOK Phase 15 section</name>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md (Step 17.a dual-rebuild 1335-1374; Step 13.f migrate 837; Step 17.d / 18.c frontend rebuild; checklist entries 1752/1770/1773)
  </read_first>
  <action>
    Add a `## Phase 15 — Research engine redesign: operator surfaces (dual tribunal REBUILD + 0011 migrate + frontend REBUILD, NO new secret)` section after the Phase 18 section, mirroring the Phase 17 structure. Steps, in order:
    Step 15.a — REBUILD both Tribunal images via Cloud Build at one `$SHA`: tribunal-worker (cost fix in audited_llm_client/cost_table) AND tribunal-api (new /verification + enriched /metrics + citation numbering); redeploy both at $SHA via the retargeted deploy scripts; confirm tribunal-api URL UNCHANGED via `gcloud run services describe` WITHOUT a path (Pitfall 4). State the reason BOTH rebuild: the worker emits cost, the api serves the read surfaces.
    Step 15.b — Run the `tribunal-migrate` Job with `--wait` to apply alembic 0011 (audit_log.cache_creation_tokens, run.cost_pending, run.verification_summary, verification_verdict RLS table) into the tribunal schema, using the JUST-BUILT image (image-pin lesson) — NOT the intake nestor line.
    Step 15.c — REBUILD `nestor-frontend` via Cloud Build with the SAME `_API_BASE_URL`/`_FB_*` substitutions as Phase 12 (no URL re-wiring), NO `VITE_SUPABASE_*` (bundle guard), no routeTree regeneration (no new route). Deploy.
    Step 15.d — Verify without printing secrets: the cost/verification/citation/chain pytest targets green in Cloud Build; `verify_chain` re-run green on the deployed audit data (SC5); the recorded run's `/verification` + enriched `/metrics` return 200 for a superadmin and 404 for a client.
    Add an explicit "NO new secret this phase (SERPAPI is Phase 15.2)" note and append matching Phase-15 bullets to the runbook checklist block (mirror the Step 17 checklist entries: dual rebuild, 0011 migrate, frontend rebuild).
  </action>
  <verify>
    <automated>cd "C:/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp" && grep -c "Phase 15 — Research engine redesign" infra/DEPLOY-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - A `## Phase 15 — Research engine redesign` section exists after the Phase 18 section
    - It contains Steps 15.a–15.d with: BOTH tribunal images rebuilt at one $SHA, `tribunal-migrate --wait` applying 0011, frontend REBUILD with no VITE_SUPABASE_*, and verify_chain re-run
    - `grep -c '0011' infra/DEPLOY-RUNBOOK.md` shows the new 0011 migrate reference present
    - The section states NO new secret this phase and the migration lands in the tribunal (not nestor) line
    - Phase-15 checklist bullets appended mirroring the Step 17 checklist entries
  </acceptance_criteria>
  <done>The runbook Phase 15 section documents the dual rebuild, 0011 migrate, frontend rebuild, verify_chain gate, and no-new-secret note by construction.</done>
</task>

<task type="auto">
  <name>Task 2: 15-UAT.md operator walkthrough + V-02 checklist</name>
  <files>.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-UAT.md</files>
  <read_first>
    - .planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-CONTEXT.md (V-02 acceptance + specifics: replit view.png visual bar)
    - .planning/RESEARCH-ENGINE-DECISIONS.md (D12/D13/D15 mockup + C1 pending rule)
    - .planning/STAKEHOLDER-NOTES.md (§2026-07-24 verification-report required content)
  </read_first>
  <action>
    Create `.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-UAT.md` — a browser walkthrough the operator runs against the RECORDED run-4cbb5311 on the admin intake detail page (NO live LLM run). Derive the walkthrough PRIMARILY from the ROADMAP § Phase 15 success criteria (SC1–SC5) and CONTEXT V-02 (operator acceptance); use 15-VALIDATION.md only as the manual-check registry (it lists which behaviors are manual-only), not as the authority for the criteria themselves. Include numbered steps with expected outcomes for each of the 5 success criteria:
    1. Open the recorded run's intake detail as superadmin → the D15 feed renders agent cards with task prompts, per-row cost, per-block summary cards, and visible retries; click an agent card's drill-down and confirm the redacted audit body (request/response) opens in the side/collapsible panel per the mockup; compare against `replit view.png` + the D15 mockup. Confirm a client-role login sees NONE of it (16-D-08).
    2. Click "View verification report" (D-09 summary card) → the funnel (distilled/selected/sessions/verdicts/skipped+why/failed-loud), refuted claims with evidence, superseded/scoped findings with temporal caveats, reconciled contradictions with canonical values, and the honest UNVERIFIED list all render.
    3. Inspect the cost display → every countable class is itemized (cache writes, search fees, DR usage); any un-itemizable Gemini grounding fee shows "pending", never a number.
    4. Click every `[n]` citation in the report body → each opens a panel with title, publication date, quality tier, and the stored snapshot (dead links still resolve); no number dangles.
    5. Confirm `verify_chain` green on the deployed audit data (per runbook Step 15.d).
    End with the V-02 sign-off block: the hard checklist above PLUS operator sign-off after reading the new surfaces next to the recorded baseline (`docs/tribunal-run-reports/run-20260722-4cbb5311/`). Mark items pass/fail with space for operator notes.
  </action>
  <verify>
    <automated>cd "C:/Users/ajimimo/Desktop/MOELD/nestor-intake-gcp" && test -f ".planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-UAT.md" && grep -c "verify_chain" ".planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-UAT.md"</automated>
  </verify>
  <acceptance_criteria>
    - 15-UAT.md exists with numbered walkthrough steps covering all 5 Phase-15 success criteria against the recorded run
    - It explicitly states NO live LLM run is needed and references replit view.png + the D15 mockup as the visual bar
    - It includes a client-role check confirming 16-D-08 (client sees nothing)
    - It ends with the V-02 sign-off block (hard checklist + operator sign-off next to the recorded baseline)
    - `grep -c 'pending' 15-UAT.md` >= 1 (the facts-only pending-cost check is present)
  </acceptance_criteria>
  <done>The operator has a complete recorded-run UAT script proving all 5 criteria without a live run, ending in the V-02 sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator deploy + recorded-run UAT session</name>
  <files>.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-UAT.md</files>
  <action>
    OPERATOR-RUN checkpoint (no autonomous file edits). The operator executes runbook Steps 15.a–15.d and the 15-UAT.md walkthrough, then records pass/fail + the V-02 sign-off in 15-UAT.md. Claude has already automated everything automatable (code, tests authored, runbook + UAT script written); this task is the human verification AFTER deploy.
  </action>
  <what-built>
    Phase 15 is code-complete across tribunal (cost fixes, verification report + endpoint, citation numbering, 0011 migration), the intake seam (superadmin proxies + denial tests), and the frontend (D15 feed, VerificationReport, CitationPanel, i18n). The runbook Phase 15 section (Task 1) and 15-UAT.md (Task 2) are authored.
  </what-built>
  <how-to-verify>
    1. Follow runbook Steps 15.a–15.d: rebuild both Tribunal images at one $SHA + redeploy; run `tribunal-migrate --wait` (0011); rebuild + deploy `nestor-frontend`; run the Cloud Build pytest targets + `verify_chain`.
    2. Open the admin intake detail for the recorded run-4cbb5311 as superadmin and walk 15-UAT.md steps 1–4 (feed vs replit view.png incl. audit-body drill-down, verification report content, facts-only cost with pending state, every [n] resolves).
    3. Log in as a client for the same space and confirm NONE of the research surfaces are visible (16-D-08).
    4. Confirm `verify_chain` returned green (SC5).
    5. Read the new report next to `docs/tribunal-run-reports/run-20260722-4cbb5311/` and record the V-02 sign-off in 15-UAT.md.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms deploy done, UAT steps 1–5 pass, verify_chain green, client-blind confirmed, and V-02 signed off in 15-UAT.md.</human-check>
  </verify>
  <done>Operator has deployed Phase 15, walked the recorded-run UAT, confirmed verify_chain green + client isolation, and signed off V-02.</done>
  <resume-signal>Type "approved" once the UAT checklist passes and V-02 is signed off, or describe the gaps to close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → live audit chain | Migration + cost fix must not break verify_chain on deployed data |
| deployed surfaces → client role | Client must see nothing post-deploy (16-D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-17 | Tampering | 0011 migration breaking the live chain | mitigate | Step 15.d re-runs verify_chain green on deployed data before sign-off (SC5) |
| T-15-18 | Information Disclosure | deployed feed/report reachable by client | mitigate | UAT step 3 client-role check confirms 16-D-08 live |
| T-15-19 | Tampering | stale migrate-job image applying wrong revision | mitigate | Step 15.b pins the just-built image (image-pin lesson) |
| T-15-SC | Tampering | npm/pip installs during deploy | mitigate | none this phase — no new packages; Cloud Build uses the existing image deps |
</threat_model>

<verification>
- Runbook Phase 15 section present with Steps 15.a–15.d + checklist bullets.
- 15-UAT.md covers all 5 criteria against the recorded run; V-02 sign-off block present.
- Operator checkpoint: deploy done, UAT passed, verify_chain green, client-blind confirmed.
</verification>

<success_criteria>
- Phase 15 surfaces deployed and UAT'd against the recorded run with no live LLM run.
- verify_chain green on deployed audit data (Phase-15 SC5).
- V-02 operator sign-off recorded next to the recorded baseline.
</success_criteria>

<output>
Create `.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-07-SUMMARY.md` when done
</output>
