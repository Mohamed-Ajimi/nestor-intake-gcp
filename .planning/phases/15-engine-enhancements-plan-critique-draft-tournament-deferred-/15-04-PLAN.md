---
phase: 15-engine-enhancements-plan-critique-draft-tournament-deferred-
plan: 04
type: execute
wave: 3
depends_on: [15-03]
files_modified:
  - backend/app/research/tribunal_client.py
  - backend/app/api/research_routes.py
  - backend/tests/test_research_cross_tenant.py
autonomous: true
requirements: [ENGINE-09]
must_haves:
  truths:
    - "The intake backend can fetch a run's verification report, a source snapshot, and an audit body (drill-down) from Tribunal via the OIDC seam"
    - "Both new intake read routes are superadmin-only, space-scoped, and existence-hide cross-tenant/missing as 404"
    - "The denial trio (cross-tenant 404, user-role 404, null-space 404) passes for each new route with no seam call on denial"
  artifacts:
    - path: "backend/app/research/tribunal_client.py"
      provides: "get_verification() + get_source() + get_audit_body() seam methods"
      contains: "get_verification"
    - path: "backend/app/api/research_routes.py"
      provides: "superadmin-only /verification + /sources proxy routes"
      contains: "verification"
    - path: "backend/tests/test_research_cross_tenant.py"
      provides: "denial trio for both new routes"
      contains: "verification"
  key_links:
    - from: "backend/app/api/research_routes.py"
      to: "tribunal_client.get_verification"
      via: "seam call outside DB session"
      pattern: "get_verification"
    - from: "backend/app/api/research_routes.py"
      to: "_superadmin_gate"
      via: "existence-hidden superadmin dependency"
      pattern: "_superadmin_gate"
---

<objective>
Add the intake-side seam so the superadmin frontend can reach the new tribunal read surfaces: `tribunal_client.get_verification()` + `get_source()`, and two superadmin-only, space-scoped, existence-hidden intake proxy routes — each joining the CI-gated cross-tenant denial suite day one.

Purpose: SEAM-01 makes the intake backend the sole caller of Tribunal. The frontend never calls Tribunal directly — it goes through these proxies, which enforce 16-D-08 (client sees nothing) and the broken-RLS-must-not-recur rule.
Output: Two seam methods, two proxy routes, and denial-trio tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-RESEARCH.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-PATTERNS.md

<interfaces>
<!-- Extracted from repo. -->

Seam method shape to clone (tribunal_client.py:185-208 get_metrics) — keyword-only, blocking httpx,
  reuse _headers/_mint_id_token verbatim (NO new OIDC code; audience = path-less service_url, Pitfall 4):
  def get_metrics(*, service_url, space_id, acting_user_id, acting_email, run_id) -> dict:
    resp = httpx.get(f"{service_url}/api/runs/{run_id}/metrics",
      headers=_headers(service_url, space_id, acting_user_id, acting_email), timeout=_TIMEOUT_S)
    resp.raise_for_status(); return resp.json()

Superadmin existence-hidden gate (research_routes.py:318-328) — declared BEFORE get_tenant_repo:
  def _superadmin_gate(identity=Depends(get_current_identity)) -> Identity:
    if identity.role != "superadmin": raise HTTPException(404, "Intake not found")
    return identity

Read-route body pattern (research_routes.py:331-363 get_bundle_url) — sync def (pg8000 threadpool),
  404-hide the run, scope by intake, seam call OUTSIDE any held DB connection:
  intake = repo.get(intake_id); if intake is None: 404
  run = ResearchRunRepository(repo.session, identity).get(run_id)
  if run is None or str(run.intake_id) != str(intake_id): 404
  ... seam call outside DB session

Denial trio (backend/tests/test_research_cross_tenant.py:298-500) — copy verbatim per route:
  test_<route>_cross_tenant_404  (space-B user → 404, no seam call, no id leak)
  test_<route>_user_role_404     (user IN correct space → 404; superadmin-only)
  test_<route>_null_space_404    (null-space user → 404; role gate fires before null-space 403)
  Assertions: resp.status_code == 404; str(foreign_id) not in resp.text; fake_tribunal_client[...] == 0

New tribunal endpoints from Plan 15-03: GET /api/runs/{run_id}/verification, GET /api/sources/{source_id},
  GET /api/runs/{run_id}/audit/{audit_id} (redacted audit body — the feed drill-down target).
Only stream_research_run is async def; these new routes are sync def.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: seam methods get_verification() + get_source()</name>
  <read_first>
    - backend/app/research/tribunal_client.py (get_metrics/get_report 185-233; _headers/_mint_id_token 59-85)
  </read_first>
  <action>
    Add two keyword-only functions to `tribunal_client.py` cloning the `get_metrics` shape exactly:
    `get_verification(*, service_url, space_id, acting_user_id, acting_email, run_id) -> dict` → `httpx.get(f"{service_url}/api/runs/{run_id}/verification", headers=_headers(...), timeout=_TIMEOUT_S)` then `raise_for_status()` + `return resp.json()`.
    `get_source(*, service_url, space_id, acting_user_id, acting_email, source_id) -> dict` → `GET {service_url}/api/sources/{source_id}`, same shape.
    `get_audit_body(*, service_url, space_id, acting_user_id, acting_email, run_id, audit_id) -> dict` → `GET {service_url}/api/runs/{run_id}/audit/{audit_id}`, same shape (the feed drill-down proxy target).
    Reuse `_headers`/`_mint_id_token` verbatim — do NOT write new OIDC code and do NOT change the audience (path-less `service_url`, Pitfall 4). None of these methods persist anything.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast; ast.parse(open('app/research/tribunal_client.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def get_verification' tribunal_client.py` == 1, `grep -c 'def get_source' tribunal_client.py` == 1, and `grep -c 'def get_audit_body' tribunal_client.py` == 1
    - All three call `_headers(service_url, space_id, acting_user_id, acting_email)` and `resp.raise_for_status()`
    - No new OIDC/token-minting code added (all reuse `_mint_id_token` via `_headers`)
    - URLs are `/api/runs/{run_id}/verification`, `/api/sources/{source_id}`, and `/api/runs/{run_id}/audit/{audit_id}`
  </acceptance_criteria>
  <done>Three seam methods (verification, source, audit-body) mirror get_metrics exactly and reuse the existing OIDC header discipline.</done>
</task>

<task type="auto">
  <name>Task 2: superadmin-only proxy routes for verification + source</name>
  <read_first>
    - backend/app/api/research_routes.py (_superadmin_gate 318-328; get_bundle_url 331-383; router prefix 74)
    - backend/app/research/tribunal_client.py (the Task-1 methods)
  </read_first>
  <action>
    Add two sync `def` routes on `research_router` copying the `get_bundle_url` body verbatim (superadmin existence-hidden, intake-scoped, seam call outside any held DB session):
    `GET /{intake_id}/research/{run_id}/verification` — depends on `_superadmin_gate`; defense-in-depth `if identity.role != "superadmin": 404`; load intake (404 if None); load run via `ResearchRunRepository(...).get(run_id)`, 404 if `run is None or str(run.intake_id) != str(intake_id)`; then OUTSIDE the DB session call `tribunal_client.get_verification(service_url=..., space_id=identity.space_id, acting_user_id=..., acting_email=..., run_id=run_id)` and return its JSON.
    `GET /{intake_id}/research/sources/{source_id}` — same gate + intake-existence check; call `tribunal_client.get_source(...)`. (Note: do NOT overload `frontend/src/lib/api/sources.ts` / intake-upload sources — this is the research-citation source, a distinct concern.)
    `GET /{intake_id}/research/{run_id}/audit/{audit_id}` — same superadmin gate + intake+run existence check (404 if `run is None or str(run.intake_id) != str(intake_id)`); then OUTSIDE the DB session call `tribunal_client.get_audit_body(service_url=..., space_id=identity.space_id, acting_user_id=..., acting_email=..., run_id=run_id, audit_id=audit_id)` and return its JSON (the feed drill-down body).
    All are sync def (pg8000 threadpool); only the existing SSE stream is async.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast; ast.parse(open('app/api/research_routes.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Three new routes exist: `/{intake_id}/research/{run_id}/verification`, `/{intake_id}/research/sources/{source_id}`, and `/{intake_id}/research/{run_id}/audit/{audit_id}`
    - All use `Depends(_superadmin_gate)` AND a defense-in-depth in-body superadmin 404 check
    - All 404 when run/intake missing or `run.intake_id != intake_id` (existence-hidden)
    - The seam call happens after the DB lookups (outside the held session), matching get_bundle_url
    - All handlers are `def` (sync), not `async def`
  </acceptance_criteria>
  <done>Three proxy routes (verification, source, audit-body) enforce superadmin + space-scope + existence-hiding and call the seam outside the DB session.</done>
</task>

<task type="auto">
  <name>Task 3: denial trio for both new routes</name>
  <read_first>
    - backend/tests/test_research_cross_tenant.py (bundle-url + verify-chain denial trios 298-500; fake_tribunal_client fixture)
  </read_first>
  <action>
    Extend `backend/tests/test_research_cross_tenant.py` with a denial trio for EACH new route (9 tests total):
    verification route → `test_verification_cross_tenant_404`, `test_verification_user_role_404`, `test_verification_null_space_404`;
    source route → `test_research_source_cross_tenant_404`, `test_research_source_user_role_404`, `test_research_source_null_space_404`;
    audit-body route → `test_audit_body_cross_tenant_404`, `test_audit_body_user_role_404`, `test_audit_body_null_space_404`.
    Each asserts `resp.status_code == 404`, the foreign id string is `not in resp.text`, and the `fake_tribunal_client` call counter for that method is 0 on denial (no seam call). Copy the existing bundle-url trio structure verbatim; register the three new seam methods (get_verification, get_source, get_audit_body) in the `fake_tribunal_client` fixture.
    HAPPY-PATH ASSERTION (warning fix — pre-UAT proof the backend→seam path works): add `test_verification_superadmin_happy_path` — a superadmin, same-space call to `/{intake_id}/research/{run_id}/verification` with the fake_tribunal_client returning the recorded-run funnel shape asserts status 200 AND a non-empty funnel object in the body (e.g. `resp.json()["funnel"]` present and distilled count > 0). This proves the full intake→seam→(fake tribunal) path returns real funnel data, not just that denials 404.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast; ast.parse(open('tests/test_research_cross_tenant.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Nine new denial tests exist (three per route: verification, source, audit-body), named per the trio convention
    - Each asserts status 404, `str(foreign_id) not in resp.text`, and no seam call on denial
    - A happy-path test (`test_verification_superadmin_happy_path`) asserts status 200 AND a non-empty funnel shape (distilled count > 0) via the fake_tribunal_client — pre-UAT proof of the backend→seam path
    - The fake_tribunal_client fixture gains get_verification + get_source + get_audit_body stubs with call counters
    - Cloud Build target `pytest tests/test_research_cross_tenant.py -x` documented in SUMMARY as the gate
  </acceptance_criteria>
  <done>All three new routes are proven superadmin-only, space-scoped, and existence-hidden by the denial trio, and the verification happy path returns a real funnel via the seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → intake API | Superadmin-only research reads; client must see nothing (16-D-08) |
| intake → tribunal seam | Sole caller (SEAM-01); OIDC audience discipline |
| intake API → cross-tenant/other-role | Existence-hidden 404, never distinguishable 403 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Information Disclosure | client role reading verification/source | mitigate | _superadmin_gate 404 + defense-in-depth; user-role-404 denial test |
| T-15-10 | Broken Access Control | cross-space run_id/source_id via proxy | mitigate | intake-scope + run.intake_id check + RLS in tribunal; cross-tenant-404 denial test asserts no seam call |
| T-15-11 | Spoofing | forged OIDC audience on seam | mitigate | reuse _headers/_mint_id_token verbatim (path-less audience, Pitfall 4) |
| T-15-11b | Information Disclosure | audit-body drill-down via proxy (client/cross-tenant) | mitigate | _superadmin_gate + intake/run scope + tribunal RLS 404 (Plan 15-03); audit-body denial trio asserts no seam call on denial |
| T-15-SC | Tampering | pip installs | mitigate | none this plan — no new packages |
</threat_model>

<verification>
- Cloud Build: `pytest tests/test_research_cross_tenant.py -x` green (9 new denial tests + verification happy-path funnel assertion).
- No frontend or tribunal file touched here (seam boundary only).
</verification>

<success_criteria>
- Frontend can reach verification + source data through superadmin-only proxies (Phase-15 SC1/SC4 seam).
- Client role and cross-tenant access denied day one (16-D-08).
</success_criteria>

<output>
Create `.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-04-SUMMARY.md` when done
</output>
