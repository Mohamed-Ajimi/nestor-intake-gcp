---
phase: 15-engine-enhancements-plan-critique-draft-tournament-deferred-
plan: 06
type: execute
wave: 5
depends_on: [15-05]
files_modified:
  - frontend/src/lib/api/research.ts
  - frontend/src/components/intake/CitationPanel.tsx
  - frontend/src/locales/en/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/nl/intake.json
autonomous: true
requirements: [ENGINE-09]
must_haves:
  truths:
    - "Numbered [n] citations render as clickable references in the report; clicking [n] opens a side panel showing title, publication date, quality tier, and the stored snapshot text"
    - "The panel renders the stored snapshot (never re-fetches the live URL) so dead links still resolve"
    - "Every citation number resolves to a source; single-source and outdated-fact caveats are shown"
    - "New UI strings keyed in en/fr/nl; i18n-audit passes"
  artifacts:
    - path: "frontend/src/components/intake/CitationPanel.tsx"
      provides: "[n] side-panel over snapshot_text"
      min_lines: 35
    - path: "frontend/src/lib/api/research.ts"
      provides: "getSource() + Citation/Source types"
      contains: "getSource"
  key_links:
    - from: "frontend/src/components/intake/CitationPanel.tsx"
      to: "research.ts getSource"
      via: "fetch snapshot on [n] click"
      pattern: "getSource"
---

<objective>
Add the client-quality numbered citation surface (D13): a `getSource()` client fn + `CitationPanel` that opens when a `[n]` marker is clicked, rendering title, publication date, quality tier, and the DB-stored snapshot text (never re-fetching the live URL). Prove every number resolves against the recorded run.

Purpose: D13 is the client-facing quality bar — numbering is generated from the DB (Plan 15-03), and clicking a number must always resolve, even for dead links, via the stored snapshot. This closes Phase-15 SC4.
Output: getSource + Citation/Source types in research.ts, the CitationPanel component, and i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/RESEARCH-ENGINE-DECISIONS.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-RESEARCH.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-PATTERNS.md

<interfaces>
<!-- Extracted from repo. -->

research.ts one-shot fetch to clone (getBundleUrl 98-106 / getVerification from Plan 15-05):
  return apiFetch<...>(`/intakes/${intakeId}/research/${...}`, { method: "GET" });  // never fork transport

Intake proxy from Plan 15-04: GET /intakes/{intakeId}/research/sources/{sourceId}
  → tribunal GET /api/sources/{id} returning {id, url, title, provider, fetched_at, snapshot_text}, 404 on RLS miss.

D13 citation panel (RESEARCH-ENGINE-DECISIONS): clicking [n] opens a panel with title, date, tier
  (1 official/2 serious press/3 blog) AND the stored snapshot of the source text (survives dead links —
  snapshots already exist). Single-source statements marked as such; verification-flagged outdated facts
  carry their temporal note inline. Numbering GENERATED from the DB (Plan 15-03 numbering.py), never the model.

IMPORTANT: do NOT overload frontend/src/lib/api/sources.ts — that is intake-UPLOAD sources, a different
  concern. Citation sources go through research.ts + the research/sources proxy route.
CitationPanel renders snapshot_text DIRECTLY — it must NEVER re-fetch the live URL (renderer.py contract).
Placement: admin intake detail (superadmin) alongside the report body; i18n en/fr/nl; i18n-audit hard gate.
Style: @/ imports, sonner toast, return-no-throw, shadcn ui/ not modified directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: research.ts — getSource() + Citation/Source types</name>
  <read_first>
    - frontend/src/lib/api/research.ts (getBundleUrl / getVerification one-shot apiFetch shape; ApiResult import)
  </read_first>
  <action>
    Add a `CitationSource` type to `research.ts`: `{ id: string; url: string; title: string | null; provider: string | null; fetched_at: string | null; snapshot_text: string | null }` (mirrors the tribunal renderer payload), plus a `Citation` type `{ n: number; source_id: string; title: string | null; publication_date: string | null; quality_tier: 1 | 2 | 3; single_source: boolean; temporal_note?: string | null }` matching Plan 15-03's numbering output. Add `export function getSource(intakeId, sourceId): Promise<ApiResult<CitationSource>>` cloning `getVerification`'s shape, hitting `/intakes/${intakeId}/research/sources/${sourceId}`, method GET, return-no-throw. Reuse `apiFetch` — do not fork the transport, do not touch `sources.ts`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "research.ts" || echo "no research.ts type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function getSource' research.ts` == 1, hitting `/research/sources/${sourceId}`, method GET, returning ApiResult<CitationSource>
    - `CitationSource` and `Citation` types exist matching the tribunal renderer + numbering shapes
    - `frontend/src/lib/api/sources.ts` is NOT modified (grep git diff shows no change to that file)
    - `npx tsc --noEmit` reports no new errors in research.ts
  </acceptance_criteria>
  <done>research.ts exposes getSource + Citation types via the unforked transport; intake-upload sources.ts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: CitationPanel component + en/fr/nl i18n keys</name>
  <read_first>
    - frontend/src/components/intake/ResearchRunProgress.tsx (fetch/toast pattern + superadmin placement)
    - frontend/src/components/intake/VerificationReport.tsx (Plan 15-05 — sibling panel style + markdown)
    - frontend/src/lib/api/research.ts (getSource + Citation/CitationSource types from Task 1)
    - frontend/src/locales/en/intake.json
    - frontend/src/locales/nl/intake.json
    - frontend/src/locales/fr/intake.json
  </read_first>
  <action>
    Create `frontend/src/components/intake/CitationPanel.tsx`: given a clicked `[n]` (a `Citation`), fetch `getSource(intakeId, citation.source_id)` (return-no-throw; sonner toast on error) and render a side panel showing the number, title, publication date, quality tier (map 1→official / 2→serious press / 3→blog via i18n labels), a "single source" badge when `single_source`, the inline `temporal_note` when present (outdated-fact caveat), and the stored `snapshot_text` rendered directly. The panel MUST render `snapshot_text` and MUST NOT re-fetch the live URL (dead-link survival — renderer.py contract). Provide the clickable `[n]` markers by exposing a small `renderCitationMarker(n, onOpen)` helper the report body uses; wire the marker to open the panel. Mount only on the admin intake detail (superadmin) beside the report — never a client route.
    Add all new strings (panel labels, tier labels, single-source badge, "opens stored snapshot" helper text) to `frontend/src/locales/{en,fr,nl}/intake.json` in all three locales.
  </action>
  <verify>
    <automated>cd frontend && node scripts/i18n-audit.mjs 2>&1 | tail -5 && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "CitationPanel" || echo "no CitationPanel type errors"</automated>
  </verify>
  <acceptance_criteria>
    - CitationPanel.tsx fetches getSource and renders number, title, publication date, quality tier, single-source badge, temporal note, and snapshot_text
    - The panel renders `snapshot_text` and contains NO fetch of the source's live `url` (grep finds no fetch(citation url) — only getSource)
    - Quality-tier labels (1/2/3) and all panel strings are i18n keys present in en/fr/nl; `i18n-audit.mjs` exits green
    - The component is not imported outside the admin route tree
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>Clicking [n] opens a panel with title/date/tier + the stored snapshot; dead links still resolve; fully i18n'd.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CitationPanel → intake API | Snapshot fetched via superadmin proxy (Plan 15-04); citations are client-facing quality but this panel mounts superadmin-side |
| panel → live source URL | Must render stored snapshot, never re-fetch live (SSRF + dead-link) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Information Disclosure | citation source cross-tenant read | mitigate | goes through the superadmin space-scoped proxy (Plan 15-04) + tribunal RLS 404 (Plan 15-03) |
| T-15-15 | Server-Side Request Forgery | panel re-fetching arbitrary live source URL | mitigate | render stored snapshot_text only; acceptance asserts no live-URL fetch |
| T-15-16 | Tampering | unresolvable model-emitted [n] | mitigate | numbering generated from DB (Plan 15-03); every number resolves (tribunal all-resolve test) |
| T-15-SC | Tampering | npm installs | mitigate | none this plan — no new packages |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds (Cloud Build/CI).
- `node scripts/i18n-audit.mjs` green.
- Manual UAT (deferred, no live run): operator clicks every [n] in the recorded run's report — each opens a panel with the stored snapshot.
</verification>

<success_criteria>
- Citations render numbered, clickable, generated from the DB; every number resolves; snapshots survive dead links (Phase-15 SC4).
</success_criteria>

<output>
Create `.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-06-SUMMARY.md` when done
</output>
