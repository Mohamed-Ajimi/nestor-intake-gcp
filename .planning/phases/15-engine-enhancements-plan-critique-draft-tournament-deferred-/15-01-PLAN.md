---
phase: 15-engine-enhancements-plan-critique-draft-tournament-deferred-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/alembic/versions/0011_cost_verification.py
  - tribunal/nestor_pulse_sdk/db/models/audit_log.py
  - tribunal/nestor_pulse_sdk/db/models/run.py
  - tribunal/nestor_pulse_sdk/db/models/verification_verdict.py
  - tribunal/nestor_pulse_sdk/db/models/__init__.py
  - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/__init__.py
  - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py
  - tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py
autonomous: true
requirements: [ENGINE-09]
must_haves:
  truths:
    - "A tribunal Alembic 0011 migration adds cache_creation_tokens + cost_pending columns and a verification_verdict RLS table without touching the intake nestor line"
    - "The recorded run-4cbb5311 fixture loads run/audit_log/claim/source/claim_source/verdict/stage_detail rows ordered by mtime (not seq)"
    - "verify_chain stays green on the fixture AFTER the migration — no frozen payload field renamed"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/alembic/versions/0011_cost_verification.py"
      provides: "additive columns + verification_verdict RLS table, down_revision 0010"
      contains: "down_revision"
    - path: "tribunal/nestor_pulse_sdk/db/models/verification_verdict.py"
      provides: "queryable per-claim verdict read model"
      contains: "verification_verdict"
    - path: "tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py"
      provides: "recorded-run fixture loader ordered by mtime"
      min_lines: 30
  key_links:
    - from: "tribunal/nestor_pulse_sdk/alembic/versions/0011_cost_verification.py"
      to: "0010_rls_empty_tenant.py"
      via: "down_revision chain"
      pattern: "down_revision\\s*=\\s*[\"']0010"
    - from: "tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py"
      to: "_payload_for_row"
      via: "post-migration green assertion"
      pattern: "verify_chain"
---

<objective>
Build the Phase-15 foundation: the tribunal Alembic 0011 migration (additive cost columns + a `verification_verdict` RLS read-model table), the SQLAlchemy models for them, and the recorded run-4cbb5311 test fixture that every downstream surface test depends on. Prove `verify_chain` stays green after the migration.

Purpose: Every other Phase-15 plan (cost fixes, verification report, citations, feed, seam, UI) reads from these tables and this fixture. This is the interface-and-data contract wave — no cost logic or endpoints yet.
Output: Migration 0011, three model changes, the fixture loader, and an extended hash-chain replay test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RESEARCH-ENGINE-DECISIONS.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-RESEARCH.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-PATTERNS.md

<interfaces>
<!-- Extracted from repo. Executor uses these directly — no exploration needed. -->

Frozen hash-chain payload (tribunal/nestor_pulse_sdk/audit/hash_chain.py:_payload_for_row) — 11 fields, MUST stay frozen:
  provider, model, started_at, duration_ms, prompt_tokens, completion_tokens, cached_tokens, gcs_uri, seq, tenant_id, run_id
  cost_usd is ALREADY outside this set (nullable, freely recomputed). New columns go OUTSIDE too.

Existing audit_log column to mirror (db/models/audit_log.py:69-73):
  cached_tokens: Mapped[int] = mapped_column(default=0, nullable=False)
  cost_usd: Mapped[Decimal | None] = mapped_column(Numeric(12, 6), nullable=True)

Verdict shape produced by group_skeptic._parse_group_verdict (persist THIS shape):
  {verdict ∈ support|refute|insufficient, confidence, evidence_refs, citations,
   reconciliation:{disputed, relation, note, canonical}}

Alembic line: tribunal/nestor_pulse_sdk/alembic/versions/, latest = 0010_rls_empty_tenant.py.
  NEW revision = 0011, down_revision = "0010". This is a SEPARATE alembic_version from the intake
  nestor line (backend/app/db/alembic/versions/) — never put research columns there (Pitfall 2).

RLS table pattern (0003_citation_schema.py claim table): id PK, tenant_id FK org.id CASCADE,
  run_id FK run.id CASCADE, index (tenant_id, run_id), ENABLE + FORCE RLS,
  USING/WITH CHECK (tenant_id = current_setting('app.tenant_id')::uuid).

Recorded-run data (source of truth for fixture):
  docs/tribunal-run-reports/run-20260722-4cbb5311/index.json  (per-call index)
  docs/tribunal-run-reports/run-20260722-4cbb5311/calls/       (per-call extracts)
  docs/tribunal-run-reports/run-20260722-4cbb5311/selection-experiment/*.tsv (1,162 distilled claims + gate buckets)
  Ordering: by GCS object mtime — all 228 blobs have seq=0 in this OLD run (Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic 0011 migration — additive cost columns + verification_verdict RLS table</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/alembic/versions/0006_run_stage_progress.py (add-column pattern)
    - tribunal/nestor_pulse_sdk/alembic/versions/0003_citation_schema.py (RLS table pattern: ENABLE+FORCE RLS, tenant policy, FKs, index)
    - tribunal/nestor_pulse_sdk/alembic/versions/0010_rls_empty_tenant.py (current head — confirm revision id for down_revision)
    - tribunal/nestor_pulse_sdk/audit/hash_chain.py (confirm _payload_for_row does NOT include the new columns)
  </read_first>
  <action>
    Create `tribunal/nestor_pulse_sdk/alembic/versions/0011_cost_verification.py` with `revision = "0011"` and `down_revision = "0010"`.
    upgrade() does three additive things: (1) `op.add_column("audit_log", sa.Column("cache_creation_tokens", sa.Integer(), nullable=True))`; (2) `op.add_column("run", sa.Column("cost_pending", sa.Boolean(), server_default=sa.text("false"), nullable=False))`; (3) create table `verification_verdict` with columns: `id` UUID PK, `tenant_id` UUID FK `org.id` ondelete CASCADE not-null, `run_id` UUID FK `run.id` ondelete CASCADE not-null, `claim_id` UUID nullable (links to claim when available), `verdict` text not-null, `confidence` text nullable, `evidence_refs` JSONB nullable, `reconciliation` JSONB nullable (carries disputed/relation/note/canonical), `created_at` timestamptz server_default now(). Add index `idx_verification_verdict_tenant_run` on (`tenant_id`,`run_id`). Then ENABLE + FORCE ROW LEVEL SECURITY and CREATE POLICY `verification_verdict_tenant_isolation` USING and WITH CHECK `tenant_id = current_setting('app.tenant_id')::uuid` — copy the exact policy shape from 0003_citation_schema.py.
    Also add a run-level verification funnel store: `op.add_column("run", sa.Column("verification_summary", postgresql.JSONB(), nullable=True))` (holds distilled/selected/sessions/verdicts/skipped/failed counts — populated later; nullable now).
    downgrade() reverses in inverse order (drop policy+table, then run.verification_summary, run.cost_pending, audit_log.cache_creation_tokens).
    Do NOT add any of these to the intake `nestor` Alembic line. Do NOT touch hash_chain.py.
  </action>
  <verify>
    <automated>cd tribunal && python -c "import ast,sys; ast.parse(open('nestor_pulse_sdk/alembic/versions/0011_cost_verification.py').read()); print('parse ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `tribunal/nestor_pulse_sdk/alembic/versions/0011_cost_verification.py` exists and `grep -c 'down_revision = "0010"'` returns 1
    - `grep -c 'ENABLE ROW LEVEL SECURITY' 0011_cost_verification.py` >= 1 and a FORCE ROW LEVEL SECURITY line is present
    - `grep -c "current_setting('app.tenant_id')::uuid" 0011_cost_verification.py` >= 2 (USING + WITH CHECK)
    - Columns added: audit_log.cache_creation_tokens, run.cost_pending, run.verification_summary — all present as op.add_column calls
    - No occurrence of the string `nestor` schema-qualifier or the intake alembic path in the migration
  </acceptance_criteria>
  <done>Migration 0011 adds the three additive columns + the RLS verification_verdict table on down_revision 0010; nothing in the hashed payload changes.</done>
</task>

<task type="auto">
  <name>Task 2: SQLAlchemy models — audit_log/run additive columns + verification_verdict model</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/db/models/audit_log.py (mirror cached_tokens column)
    - tribunal/nestor_pulse_sdk/db/models/run.py (mirror stage_detail JSONB / current_stage columns)
    - tribunal/nestor_pulse_sdk/db/models/claim.py (Mapped column + tenant_id/run_id FK style for the new model)
    - tribunal/nestor_pulse_sdk/db/models/__init__.py (model export registry)
  </read_first>
  <action>
    In `audit_log.py`, add `cache_creation_tokens: Mapped[int | None] = mapped_column(default=None, nullable=True)` in the same block as `cached_tokens`. Do NOT modify `__table_args__` or any hashed field.
    In `run.py`, add `cost_pending: Mapped[bool] = mapped_column(server_default=text("false"), default=False, nullable=False)` and `verification_summary: Mapped[dict | None] = mapped_column(JSONB, nullable=True)`, mirroring the existing `stage_detail` JSONB column declaration.
    Create `tribunal/nestor_pulse_sdk/db/models/verification_verdict.py` with a `VerificationVerdict` model matching the 0011 table exactly: id (UUID PK, default uuid4), tenant_id (UUID FK org.id), run_id (UUID FK run.id), claim_id (UUID nullable), verdict (str), confidence (str | None), evidence_refs (JSONB | None), reconciliation (JSONB | None), created_at. Copy the tablename/RLS-aware base-class convention from `claim.py`.
    Register `VerificationVerdict` in `db/models/__init__.py` alongside the other model exports.
  </action>
  <verify>
    <automated>cd tribunal && python -c "import ast; [ast.parse(open(f).read()) for f in ['nestor_pulse_sdk/db/models/audit_log.py','nestor_pulse_sdk/db/models/run.py','nestor_pulse_sdk/db/models/verification_verdict.py','nestor_pulse_sdk/db/models/__init__.py']]; print('parse ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cache_creation_tokens' db/models/audit_log.py` >= 1 and the line is nullable=True
    - `grep -c 'cost_pending' db/models/run.py` >= 1 and `grep -c 'verification_summary' db/models/run.py` >= 1
    - `verification_verdict.py` defines class VerificationVerdict with columns id, tenant_id, run_id, claim_id, verdict, confidence, evidence_refs, reconciliation, created_at
    - `grep -c 'VerificationVerdict' db/models/__init__.py` >= 1 (registered)
    - audit_log.py `__table_args__` / hashed-field block is byte-identical except the one added non-hashed column (no reordering of existing hashed columns)
  </acceptance_criteria>
  <done>Models mirror the 0011 schema; the new columns are all non-hashed; VerificationVerdict is importable and registered.</done>
</task>

<task type="auto">
  <name>Task 3: Recorded run-4cbb5311 fixture loader + post-migration hash-chain replay test</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/conftest.py (seeding + session fixtures)
    - tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py (_build_chain_rows, test_anthropic_cache_tokens_extracted, verify_chain usage)
    - tribunal/nestor_pulse_sdk/audit/hash_chain.py (_payload_for_row 11-field set, verify_chain signature)
    - docs/tribunal-run-reports/run-20260722-4cbb5311/index.json (per-call index — provenance)
    - docs/tribunal-run-reports/run-20260722-4cbb5311/selection-experiment/claims-classified-full.tsv (gate buckets for funnel seeding)
  </read_first>
  <action>
    Create `tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/` package: `__init__.py` and `loader.py`. `loader.py` exposes `load_recorded_run(session, tenant_id)` that seeds a `run` row + `audit_log` rows + `claim`/`source`/`claim_source` rows + extracted `verification_verdict` rows + a populated `stage_detail` JSONB, all reconstructed from the committed `docs/tribunal-run-reports/run-20260722-4cbb5311/` extracts (index.json, calls/, selection-experiment/*.tsv). Order audit rows by GCS object mtime (from index.json), NOT by `seq` (all seq=0 in this old run — Pitfall 6). Provide a small documented constant for the recorded gate-funnel counts derived from `claims-classified-full.tsv` (distilled=1162 and the keep/drop bucket totals) so downstream funnel tests can assert exact recorded numbers. If the recorded live DB rows are unavailable, rebuild purely from the committed extracts (A2 fallback) — the loader must not depend on GCS at test time.
    Extend `test_hash_chain_replay.py` with `test_chain_green_after_cost_migration`: build the fixture's audit rows through `_build_chain_rows`, assert `verify_chain(...)` returns green, AND assert `cache_creation_tokens`, `cost_pending`, `verification_summary` are NOT among the 11 fields of `_payload_for_row` (introspect the frozen set).
  </action>
  <verify>
    <automated>cd tribunal && python -c "import ast; [ast.parse(open(f).read()) for f in ['nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py','nestor_pulse_sdk/tests/test_hash_chain_replay.py']]; print('parse ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/run_4cbb5311/loader.py` defines `load_recorded_run` and a documented recorded-funnel-counts constant sourced from the selection-experiment TSVs (distilled total 1162 present)
    - loader orders audit rows by mtime; the string `seq` does not appear as a sort key (a comment cites Pitfall 6)
    - `test_hash_chain_replay.py` gains a test asserting verify_chain green post-migration AND asserting the three new column names are absent from `_payload_for_row`'s field set
    - `grep -vE '^#' loader.py | grep -c 'load_recorded_run'` >= 1
    - Cloud Build target `pytest nestor_pulse_sdk/tests/test_hash_chain_replay.py -x` is the phase-gate command for this task (documented in SUMMARY; local run impossible — author-by-construction)
  </acceptance_criteria>
  <done>The fixture loads the recorded run from committed extracts, ordered by mtime, and the replay test proves verify_chain stays green with the new non-hashed columns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → tribunal DB | Verdict/cost rows written into the tribunal schema; must be tenant-scoped |
| tribunal DB → cross-tenant reader | RLS must prevent a space reading another space's verdicts |
| migration → frozen audit chain | New columns must not enter the hashed payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | audit hash-chain via new cost columns | mitigate | new columns live OUTSIDE `_payload_for_row`; `test_chain_green_after_cost_migration` asserts green + absence-from-payload |
| T-15-02 | Information Disclosure | verification_verdict cross-tenant read | mitigate | ENABLE+FORCE RLS + tenant_id policy on the 0011 table; denial tests added in Plan 15-03 (endpoint) and 15-04 (seam) |
| T-15-03 | Tampering | writing research columns into the intake `nestor` Alembic line | mitigate | migration lives in tribunal's own line, down_revision 0010; acceptance asserts no nestor path |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | none this plan (no new packages — RESEARCH §Package Legitimacy Audit: not required) |
</threat_model>

<verification>
- Cloud Build: `pytest nestor_pulse_sdk/tests/test_hash_chain_replay.py -x` green (verify_chain post-migration).
- `alembic heads` shows a single head at 0011 in the tribunal line (author-by-construction; run in migrate-job at deploy).
- No new file under `backend/app/db/alembic/versions/`.
</verification>

<success_criteria>
- Migration 0011 + models + fixture exist; verify_chain green on the fixture after migration (Phase-15 SC5).
- verification_verdict table has FORCE RLS + tenant policy (isolation day one).
- Fixture reconstructs the recorded run from committed extracts, ordered by mtime.
</success_criteria>

<output>
Create `.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-01-SUMMARY.md` when done
</output>
