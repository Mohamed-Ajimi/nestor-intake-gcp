---
phase: 15-engine-enhancements-plan-critique-draft-tournament-deferred-
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - tribunal/nestor_pulse_sdk/verification/__init__.py
  - tribunal/nestor_pulse_sdk/verification/report.py
  - tribunal/nestor_pulse_sdk/citations/numbering.py
  - tribunal/nestor_pulse_sdk/runs/api.py
  - tribunal/nestor_pulse_sdk/runs/schemas.py
  - tribunal/nestor_pulse_sdk/audit/gcs_blob.py
  - tribunal/nestor_pulse_sdk/tests/test_verification_report_endpoint.py
  - tribunal/nestor_pulse_sdk/tests/test_citation_numbering.py
  - tribunal/nestor_pulse_sdk/tests/test_audit_body_endpoint.py
autonomous: true
requirements: [ENGINE-09]
must_haves:
  truths:
    - "GET /runs/{id}/verification returns the funnel + per-claim verdicts + superseded/reconciled notes + unverified list + true cost, from persisted rows"
    - "Citation [n] numbers are generated deterministically from claim/claim_source ordering, never from the writing model, and every number resolves to a source"
    - "The verification endpoint 404s on RLS miss and is tenant-scoped"
    - "The /metrics feed items carry per-row cost + task_prompt + status + retry + audit_id (enriched stage_detail schema)"
    - "GET /runs/{id}/audit/{audit_id} returns the redacted request/response audit body (drill-down target for the feed's audit_id), RLS-scoped and 404-on-miss"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/verification/report.py"
      provides: "build_verification_report() shaping verdicts + funnel + cost"
      min_lines: 40
    - path: "tribunal/nestor_pulse_sdk/citations/numbering.py"
      provides: "deterministic [n] generation from claim/claim_source ordering"
      min_lines: 25
    - path: "tribunal/nestor_pulse_sdk/runs/api.py"
      provides: "GET /runs/{id}/verification + GET /runs/{id}/audit/{audit_id} + enriched /metrics"
      contains: "verification"
    - path: "tribunal/nestor_pulse_sdk/audit/gcs_blob.py"
      provides: "download_audit_body(gcs_uri) reader for drill-down"
      contains: "download_audit_body"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/runs/api.py"
      to: "verification/report.build_verification_report"
      via: "endpoint calls shaper"
      pattern: "build_verification_report"
    - from: "tribunal/nestor_pulse_sdk/citations/numbering.py"
      to: "claim/claim_source"
      via: "ordering by claim.position"
      pattern: "position"
---

<objective>
Add the tribunal-side read surfaces over the recorded run: (1) `build_verification_report()` shaping persisted verdicts + funnel + true cost into the STAKEHOLDER-NOTES report shape, exposed at `GET /runs/{id}/verification` (RLS-scoped, 404-on-miss); (2) deterministic citation `[n]` numbering generated from claim/claim_source ordering; (3) enrich the `/metrics` feed `stage_detail` item schema with per-row cost/task_prompt/retry/audit_id (schema + renderer contract; the emitting engine is 15.2).

Purpose: The verification report and citations are the operator's post-run truth surfaces. All data already exists (verdicts persisted in Plan 15-01's table; claims/sources in the 3-table model) — this plumbs it into read endpoints.
Output: verification module, citation numbering, enriched /metrics + new /verification endpoint, and two test files with RLS denial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/RESEARCH-ENGINE-DECISIONS.md
@.planning/STAKEHOLDER-NOTES.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-RESEARCH.md
@.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-PATTERNS.md

<interfaces>
<!-- Extracted from repo. -->

get_run_metrics read-endpoint shape (runs/api.py:786-849) — copy the 404-on-RLS-miss guard:
  run = (await session.execute(select(Run).where(Run.id == run_id))).scalar_one_or_none()
  if run is None: raise HTTPException(404, "run not found")
  returns RunMetrics(..., stages=..., current_stage=..., stage_detail=run.stage_detail)

VerificationVerdict model (Plan 15-01): id, tenant_id, run_id, claim_id, verdict, confidence,
  evidence_refs, reconciliation{disputed,relation,note,canonical}, created_at.
run.verification_summary JSONB (Plan 15-01): distilled/selected/sessions/verdicts/skipped/failed counts.

Verification report content REQUIRED by STAKEHOLDER-NOTES §2026-07-24:
  funnel (distilled / selected / sessions / verdicts / skipped+why / failed-loud) + refuted claims with
  skeptic evidence + superseded/scoped findings with temporal caveats + reconciled contradictions with
  chosen canonical + honest list of UNVERIFIED claims + true cost (cache-write + DR usage from Plan 15-02).

Citation model (citations/extractor.py): claim(text, facet, position, run_id) + source(url,title,provider,
  fetched_at,snapshot_text) + claim_source(claim_id, source_id, snippet). All carry tenant_id + RLS (0003).
D13: [n] GENERATED from claim.position / first-appearance ordering — NEVER the writing model
  (that produced last run's 28 stripped markers). Quality tier (1 official/2 press/3 blog) + pub date:
  NOT stored columns today (Open Q A3) — derive from provider/domain heuristic this phase (chain-safe;
  a stored column is a later, still-non-hashed option if operator wants authoritative tiers).

Enriched stage_detail item (D15 feed schema — Feed Data Model, RESEARCH):
  {name, status(running|done|retry|failed|pending), task_prompt, cost_usd, facts, retry{attempt,max,wait_s}, audit_id}
  plus per-stage summary {duration_s, actions, items_read, cost_usd}. stage_detail is NOT hashed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_verification_report() + enriched /metrics stage_detail schema</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/runs/api.py (get_run_metrics 786-849; _markdown_to_sections 67-109 shaping style)
    - tribunal/nestor_pulse_sdk/runs/schemas.py (RunMetrics + stage item schema to extend)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/group_skeptic.py (_parse_group_verdict — the verdict shape to render)
    - tribunal/nestor_pulse_sdk/db/models/verification_verdict.py (Plan 15-01 model)
    - .planning/STAKEHOLDER-NOTES.md (§2026-07-24 verification-report required content)
  </read_first>
  <behavior>
    - build_verification_report(session, run) returns funnel counts (from run.verification_summary), verdict lists split by verdict∈{support,refute,insufficient}, superseded/scoped findings (reconciliation.note/canonical), reconciled contradictions (reconciliation.disputed+canonical), an UNVERIFIED list (claims with no verdict row), and true cost (run.cost_usd_total + cost_pending state). It reads the REAL verification_verdict rows seeded by Plan 15-01's verdict_extract (parsed from the committed emit_group_verdict extracts) — the recorded run's refute rows carry non-null reconciliation, so the refuted-with-evidence section renders from real data.
    - The shaper reads ONLY persisted rows (verification_verdict + run + claim) — it never re-parses GCS blobs
    - stage_detail item schema in schemas.py gains optional fields cost_usd, task_prompt, facts, retry, audit_id + per-stage summary — all optional so today's runs still validate
  </behavior>
  <action>
    Create `tribunal/nestor_pulse_sdk/verification/__init__.py` and `verification/report.py` with `build_verification_report(session, run)` returning a dict/dataclass matching the STAKEHOLDER-NOTES content list. Funnel numbers come from `run.verification_summary`; verdicts + reconciliation come from `verification_verdict` rows for the run; the UNVERIFIED list is claims for the run with no matching verdict row (distinguish honestly — a count with reasons where derivable). True cost = `run.cost_usd_total` plus a `cost_pending` boolean surfaced from Plan 15-02.
    In `runs/schemas.py`, extend the stage-detail item model with optional `cost_usd`, `task_prompt`, `facts`, `retry`, `audit_id`, and an optional per-stage `summary` object (all `Optional`, default None) — do NOT make them required (today's recorded stage_detail lacks them). In `runs/api.py`, ensure `get_run_metrics` still returns `run.stage_detail` verbatim (the enriched fields ride through the JSONB for free — D-07 contract).
  </action>
  <verify>
    <!-- PRIMARY: the report/endpoint test proves shaping over real seeded verdict rows. SECONDARY: ast guard. -->
    <automated>cd tribunal && pytest nestor_pulse_sdk/tests/test_verification_report_endpoint.py -x</automated>
    <automated>cd tribunal && python -c "import ast; [ast.parse(open(f).read()) for f in ['nestor_pulse_sdk/verification/report.py','nestor_pulse_sdk/runs/schemas.py','nestor_pulse_sdk/runs/api.py']]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `verification/report.py` defines `build_verification_report` returning funnel + verdict lists + superseded + reconciled + unverified + true-cost keys (all six STAKEHOLDER-NOTES content areas present)
    - The shaper contains no GCS/blob read (grep finds no gcs/storage/blob import in report.py)
    - schemas.py stage item gains cost_usd/task_prompt/facts/retry/audit_id/summary as Optional (grep finds them, all with Optional or default None)
    - get_run_metrics still returns stage_detail unchanged (no field-stripping)
  </acceptance_criteria>
  <done>The verification report is shaped from persisted rows and the feed item schema is enriched — both additive, no blob re-parse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /runs/{id}/verification endpoint + RLS/tenant scoping</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/runs/api.py (get_run_metrics 404-on-RLS-miss guard + router registration)
    - tribunal/nestor_pulse_sdk/citations/renderer.py (get_source — 404 on RLS miss read shape)
    - tribunal/nestor_pulse_sdk/db/rls.py (set_tenant_context — how reads set tenant)
    - tribunal/nestor_pulse_sdk/verification/report.py (Task 1 shaper)
  </read_first>
  <behavior>
    - GET /runs/{run_id}/verification: run scalar_one_or_none → 404 if None (RLS miss reads as absent)
    - Response is the build_verification_report() shape
    - Reads run under the caller's tenant context; a cross-tenant run_id returns 404, not another space's data
  </behavior>
  <action>
    Add `@router.get("/{run_id}/verification")` in `runs/api.py` (async, next to get_run_metrics). Load the run with `scalar_one_or_none()`; `raise HTTPException(404, "run not found")` when None (RLS-miss == 404, mirroring renderer.py). Call `build_verification_report(session, run)` and return it via a new `VerificationReport` response model added to `runs/schemas.py`. Ensure tenant context is set on the session per the existing metrics/read path (do not bypass RLS). Register nothing new beyond the route on the existing runs router.
  </action>
  <verify>
    <!-- PRIMARY: endpoint test incl. RLS 404 denial. SECONDARY: ast guard. -->
    <automated>cd tribunal && pytest nestor_pulse_sdk/tests/test_verification_report_endpoint.py -x</automated>
    <automated>cd tribunal && python -c "import ast; [ast.parse(open(f).read()) for f in ['nestor_pulse_sdk/runs/api.py','nestor_pulse_sdk/runs/schemas.py']]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/{run_id}/verification' runs/api.py` >= 1 and the handler is async
    - The handler uses `scalar_one_or_none()` + `HTTPException(404` (RLS-miss == 404, no distinguishable 403)
    - A `VerificationReport` response model exists in schemas.py
    - The route calls `build_verification_report`
  </acceptance_criteria>
  <done>The verification endpoint returns the report for a tenant's own run and 404s for anything else.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: citation [n] numbering + verification/citation tests with RLS denial</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/citations/extractor.py (claim.position ordering + persist)
    - tribunal/nestor_pulse_sdk/citations/renderer.py (get_source contract to reuse)
    - tribunal/nestor_pulse_sdk/tests/test_citation_resolution.py (claim→source resolution assertions)
    - tribunal/nestor_pulse_sdk/tests/test_rls_isolation.py (tenant-isolation read test shape)
    - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py (fixture with claims/sources/verdicts)
  </read_first>
  <behavior>
    - number_citations(session, run_id) returns an ordered [n]→source mapping deterministic on claim.position (stable across calls)
    - every [n] resolves to a source row (no dangling number); single-source claims flagged
    - quality tier (1/2/3) + publication date derived from provider/domain heuristic (not required as stored columns)
    - test: cross-tenant read of /verification and of a foreign source returns 404/empty; no id leak in body
  </behavior>
  <action>
    Create `tribunal/nestor_pulse_sdk/citations/numbering.py` with `number_citations(session, run_id)` that orders claim/claim_source by `claim.position` (first-appearance), assigns 1-based `[n]`, and returns each number's source metadata (title, url, fetched_at as publication-date proxy, derived quality tier from a provider/domain map, single_source flag). Numbering is generated from the DB only — no model text parsing (D13).
    Create `tribunal/nestor_pulse_sdk/tests/test_citation_numbering.py`: assert determinism (two calls identical), all-resolve (every [n] has a source), single-source flag correctness, using the run_4cbb5311 fixture.
    Create `tribunal/nestor_pulse_sdk/tests/test_verification_report_endpoint.py`: (a) funnel/verdict shaping matches the recorded funnel constants from the fixture; (b) an RLS denial test — a run_id belonging to another tenant returns 404 and the foreign run_id string does not appear in the response body. Mirror test_rls_isolation.py context-switching.
  </action>
  <verify>
    <!-- PRIMARY: numbering + verification endpoint tests (determinism, all-resolve, RLS 404). SECONDARY: ast guard. -->
    <automated>cd tribunal && pytest nestor_pulse_sdk/tests/test_citation_numbering.py nestor_pulse_sdk/tests/test_verification_report_endpoint.py -x</automated>
    <automated>cd tribunal && python -c "import ast; [ast.parse(open(f).read()) for f in ['nestor_pulse_sdk/citations/numbering.py','nestor_pulse_sdk/tests/test_citation_numbering.py','nestor_pulse_sdk/tests/test_verification_report_endpoint.py']]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - numbering.py orders by `claim.position` (grep finds position) and derives tier/date without requiring a new stored column
    - test_citation_numbering.py asserts determinism + all-resolve + single_source flag
    - test_verification_report_endpoint.py has a funnel-shape test AND an RLS cross-tenant 404 test asserting `str(foreign_run_id) not in resp.text`
    - Cloud Build targets `pytest nestor_pulse_sdk/tests/test_citation_numbering.py nestor_pulse_sdk/tests/test_verification_report_endpoint.py -x` documented in SUMMARY
  </acceptance_criteria>
  <done>[n] is deterministic and always resolves; the verification endpoint is proven tenant-isolated by a denial test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: audit-body drill-down endpoint GET /runs/{id}/audit/{audit_id} + GCS reader</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/audit/api.py (all_llm_calls_for_run — _audit_row_dto shape, RLS via get_db_session; gcs_uri exposed but body NOT)
    - tribunal/nestor_pulse_sdk/audit/gcs_blob.py (upload_audit_body 78 — bucket resolution + _redact_dict 57; mirror for a download helper)
    - tribunal/nestor_pulse_sdk/runs/api.py (get_run_metrics 786-849 — 404-on-RLS-miss guard + router registration)
    - tribunal/nestor_pulse_sdk/db/models/audit_log.py (id/run_id/gcs_uri columns)
    - tribunal/nestor_pulse_sdk/tests/test_rls_isolation.py (tenant-context switch pattern for the denial test)
  </read_first>
  <behavior>
    - download_audit_body(gcs_uri) fetches the stored JSON body from GCS and returns the ALREADY-REDACTED request/response dict (bodies were redacted at upload — never re-expose keys); returns None on a missing/error:// uri
    - GET /runs/{run_id}/audit/{audit_id}: loads the audit_log row scoped to run_id under the caller's tenant context; scalar_one_or_none → 404 if None (RLS miss reads absent); returns {audit_id, provider, model, request, response} from the GCS body
    - A cross-tenant audit_id (or one whose run_id != path run_id) returns 404, not another space's body
    - The endpoint returns the body only — it NEVER returns hash/prev_hash (mirrors _audit_row_dto omission)
  </behavior>
  <action>
    Add `download_audit_body(gcs_uri: str) -> dict | None` to `audit/gcs_blob.py` mirroring `upload_audit_body`'s bucket resolution (AUDIT_GCS_BUCKET env). It downloads the JSON object at `gcs_uri`, `json.loads` it, and returns the dict as-stored (bodies are ALREADY redacted at upload — do NOT re-fetch keys, do NOT add new secret exposure). Return None for an `error://` uri or a missing object (log at warning). Do NOT add a live-URL fetch of any source — GCS only.
    In `runs/api.py`, add `@router.get("/{run_id}/audit/{audit_id}")` (async, next to the verification route). Load the audit_log row filtered by `AuditLog.id == audit_id` AND `AuditLog.run_id == run_id` with `scalar_one_or_none()`; `raise HTTPException(404, "audit not found")` when None (RLS-miss == 404). Call `download_audit_body(row.gcs_uri)`; if that is None, 404. Return an `AuditBody` response model (added to `runs/schemas.py`): `{audit_id, provider, model, request, response}` — request/response taken from the downloaded body; hash/prev_hash NEVER included. Set tenant context per the existing metrics/read path (do not bypass RLS).
    Create `tribunal/nestor_pulse_sdk/tests/test_audit_body_endpoint.py`: (a) a happy-path test — a superadmin/same-tenant call for a fixture audit_id returns the redacted body with request+response keys and NO hash key; (b) an RLS denial test — a foreign-tenant audit_id returns 404 and the foreign id string is not in the response body. Mirror test_rls_isolation.py context switching. Mock `download_audit_body` (no live GCS in tests).
  </action>
  <verify>
    <!-- PRIMARY: the audit-body endpoint test (happy path + RLS 404). SECONDARY: ast guard. -->
    <automated>cd tribunal && pytest nestor_pulse_sdk/tests/test_audit_body_endpoint.py -x</automated>
    <automated>cd tribunal && python -c "import ast; [ast.parse(open(f).read()) for f in ['nestor_pulse_sdk/audit/gcs_blob.py','nestor_pulse_sdk/runs/api.py','nestor_pulse_sdk/tests/test_audit_body_endpoint.py']]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `download_audit_body` exists in gcs_blob.py, reads GCS only (no live source-URL fetch), returns the already-redacted body or None
    - `grep -c '/{run_id}/audit/{audit_id}' runs/api.py` >= 1 and the handler is async, uses scalar_one_or_none + HTTPException(404
    - An `AuditBody` response model exists in schemas.py returning {audit_id, provider, model, request, response} — hash/prev_hash NEVER present
    - test_audit_body_endpoint.py has a happy-path (redacted body, no hash) test AND a cross-tenant 404 test (`str(foreign_audit_id) not in resp.text`), mocking download_audit_body
    - Cloud Build target `pytest nestor_pulse_sdk/tests/test_audit_body_endpoint.py -x` documented in SUMMARY
  </acceptance_criteria>
  <done>The feed's audit_id drill-down has a real, tenant-isolated tribunal endpoint returning the redacted audit body from GCS — no dead no-op stub downstream.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tribunal API → caller | verification/citation reads must be tenant-scoped |
| DB verdicts/claims → cross-tenant reader | RLS 404-on-miss must hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Information Disclosure | GET /verification cross-tenant verdict leak | mitigate | scalar_one_or_none → 404 (RLS miss reads absent); test_verification_report_endpoint RLS denial test |
| T-15-07 | Information Disclosure | citation numbering exposing another tenant's sources | mitigate | claim/source RLS (0003) + number_citations reads under tenant context; all-resolve test scoped to run |
| T-15-08 | Tampering | model-emitted citation numbers (unresolvable) | mitigate | D13: [n] generated from claim.position DB ordering; determinism + all-resolve tests |
| T-15-08b | Information Disclosure | audit-body drill-down cross-tenant read | mitigate | GET /audit/{audit_id} scoped by run_id + RLS scalar_one_or_none → 404; download_audit_body returns already-redacted body only; test_audit_body_endpoint RLS 404 test |
| T-15-08c | Information Disclosure | re-exposing redacted keys / hashes in the audit body | mitigate | bodies redacted at upload; response model omits hash/prev_hash; reader never re-fetches keys |
| T-15-SC | Tampering | pip installs | mitigate | none this plan — no new packages |
</threat_model>

<verification>
- Cloud Build: `pytest nestor_pulse_sdk/tests/test_citation_numbering.py nestor_pulse_sdk/tests/test_verification_report_endpoint.py -x` green.
- /verification returns funnel+verdicts+unverified+true-cost from persisted rows (no blob read).
- Every [n] resolves; cross-tenant run_id → 404.
</verification>

<success_criteria>
- Verification report renderable from recorded data (Phase-15 SC1).
- Numbered citations resolve, generated from the DB (Phase-15 SC4).
- Feed item schema enriched, additive (Phase-15 SC2 backbone).
</success_criteria>

<output>
Create `.planning/phases/15-engine-enhancements-plan-critique-draft-tournament-deferred-/15-03-SUMMARY.md` when done
</output>
