---
phase: 13-tribunal-re-home-infra-baseline
plan: 03
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - infra/main.tf
  - infra/variables.tf
  - infra/DEPLOY-RUNBOOK.md
  - tribunal/cloudbuild.api.yaml
  - tribunal/cloudbuild.worker.yaml
  - tribunal/cloudbuild.test.yaml
  - tribunal/infrastructure/cloud-run/deploy-api.sh
  - tribunal/infrastructure/cloud-run/deploy-worker.sh
autonomous: true
requirements: [ENGINE-01, ENGINE-04, ENGINE-08]
user_setup:
  - service: gemini
    why: "Tribunal's third provider arm (D-06 — all three providers enabled day one)"
    env_vars:
      - name: Nestor_Gemini
        source: "Reuse the GEMINI/GOOGLE_API_KEY value from the old Tribunal project (project-cb01b861); reseed as a Secret Manager secret in the intake project during the live session (Plan 04)"
must_haves:
  truths:
    - "The runbook enumerates every new Tribunal secret, env var, IAM binding, DB role, bucket, and migration-Job/deploy step (by-construction IaC; terraform apply is blocked)"
    - "The IaC describes two new Cloud Run services (tribunal-api, always-on tribunal-worker), a tribunal-migrate Job, the audit bucket (7y Unlocked retention), and app_user/worker_user DB roles"
    - "worker_user is granted USAGE/DML on the `tribunal` schema ONLY, never `nestor` (isolation firewall)"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "Operator-run Phase 13 Tribunal re-home + teardown steps"
      contains: "Phase 13"
    - path: "tribunal/cloudbuild.worker.yaml"
      provides: "Cloud Build config for the tribunal-worker image (no local Docker)"
      contains: "tribunal-worker"
    - path: "infra/main.tf"
      provides: "By-construction IaC: services, secrets, bucket (7y Unlocked), DB roles, migrate Job"
      contains: "tribunal"
  key_links:
    - from: "infra/main.tf audit bucket"
      to: "audit/gcs_blob.py 7y Unlocked retention"
      via: "object-level retention, mode=Unlocked (NOT Bucket Lock)"
      pattern: "Unlocked|retention"
    - from: "tribunal-worker service"
      to: "DATABASE_URL_WORKER secret"
      via: "--set-secrets DATABASE_URL=DATABASE_URL_WORKER:latest"
      pattern: "DATABASE_URL_WORKER"
---

<objective>
Author, by construction, every infrastructure artifact needed to deploy the re-homed Tribunal engine
into the intake "Nestor Pulse" GCP project — WITHOUT running anything live (that is Plan 04, operator-run:
dev machine has no Python/Docker and `terraform apply` is blocked). This plan produces: (1) a new
`## Phase 13 — Tribunal re-home` runbook section enumerating every secret/env/IAM/role/bucket/deploy/
teardown step; (2) by-construction Terraform in `infra/main.tf` describing the two Cloud Run services,
the `tribunal-migrate` Job, the audit bucket, and the DB roles; (3) Cloud Build configs for the two
Tribunal images + the test suite; (4) retargeted deploy scripts (old project -> intake project).

Purpose: The recurring "deployed but not wired" IaC-drift gap (STATE.md Phase-5 carry-over) is closed
by making the runbook the single enumerated source of truth for the live session.

Output: Runbook Phase 13 section (incl. teardown of old `project-cb01b861` as the FINAL post-proof step),
by-construction `infra/main.tf`/`variables.tf` blocks, `tribunal/cloudbuild.{api,worker,test}.yaml`,
and retargeted `tribunal/infrastructure/cloud-run/deploy-{api,worker}.sh`.

**Locked decisions surfaced here:**
- DB topology = **`tribunal` schema** (Plan 02 owns the migration edits; this plan wires the schema/roles).
- Concurrency mechanism for D-08 5+ (RESEARCH Open Q2) = **raise worker `max-instances` to 5** (multiple
  SKIP-LOCKED pollers made safe by Plan 02's advisory lock — the smallest change from today's single worker).
- Provider secret naming (RESEARCH Open Q3) = **reseed under the `Nestor_*` names** the copied
  `secrets_bootstrap.py` already reads (`Nestor_Claude`/`Nestor_OpenAI`/`Nestor_Gemini`) — no bootstrap
  refactor; lower-effort and keeps the copied code byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tribunal-re-home-infra-baseline/13-CONTEXT.md
@.planning/phases/13-tribunal-re-home-infra-baseline/13-RESEARCH.md
@.planning/phases/13-tribunal-re-home-infra-baseline/13-PATTERNS.md

<interfaces>
Concrete intake-project values (from infra/DEPLOY-RUNBOOK.md + infra/main.tf — use these, do not invent):
- Region: europe-west1
- Project id env var: $GOOGLE_PROJECT (operator sets it; e.g. the "Nestor Pulse" project, acct tools@dotto.be)
- Runtime SA: nestor-run@${GOOGLE_PROJECT}.iam.gserviceaccount.com
- Artifact Registry repo: `nestor` (image tag form: europe-west1-docker.pkg.dev/${GOOGLE_PROJECT}/nestor/<image>:<tag>)
- Cloud SQL instance: var.instance_name (google_sql_database_instance.main.connection_name = ${GOOGLE_PROJECT}:europe-west1:<instance>)
- App database: var.db_name
- Existing intake secrets (DIFFERENT names from what Tribunal reads): nestor-anthropic-api-key, nestor-openai-api-key
- Existing image build idiom: `gcloud builds submit <dir> --tag "$IMAGE" --project="$GOOGLE_PROJECT"`
- Existing migrate Job: google_cloud_run_v2_job.migrate ("nestor-migrate") — the shape to mirror for tribunal-migrate

Old project to tear down (D-02, FINAL post-proof step): project-cb01b861 — Cloud Run nestor-pulse-api +
nestor-pulse-worker, Cloud SQL nestor-prod-pg, Artifact Registry nestor-pulse repo.

Tribunal secret->env mapping the copied secrets_bootstrap.py expects (reseed under these names — Open Q3):
  Nestor_Gemini -> GOOGLE_API_KEY ; Nestor_Claude -> ANTHROPIC_API_KEY ; Nestor_OpenAI -> OPENAI_API_KEY
Plus NEW DB secrets: DATABASE_URL (app_user), DATABASE_URL_WORKER (worker_user), and AUDIT_GCS_BUCKET.
DATABASE_URL form (asyncpg unix socket): postgresql+asyncpg://<user>:<pw>@/<db>?host=/cloudsql/${GOOGLE_PROJECT}:europe-west1:<instance>

Old deploy flags to replicate (from Nestor deploy-worker.sh / deploy-api.sh):
  worker: --no-cpu-throttling --min-instances=1 --timeout=3600 --no-allow-unauthenticated --memory=2Gi --cpu=1
          --set-env-vars=...,NESTOR_TRIBUNAL_UNCAPPED=1  (D-07 stays uncapped this phase)
          --set-secrets DATABASE_URL=DATABASE_URL_WORKER:latest, ANTHROPIC_API_KEY=Nestor_Claude:latest,
                        GOOGLE_API_KEY=Nestor_Gemini:latest, OPENAI_API_KEY=Nestor_OpenAI:latest
  api:    --min-instances=0 --max-instances=3 --timeout=300 --memory=1Gi --cpu=1 --concurrency=80
          --set-secrets DATABASE_URL=DATABASE_URL:latest + the three Nestor_* provider secrets
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: By-construction IaC — services, migrate Job, audit bucket, DB roles, secrets (infra/main.tf + variables.tf)</name>
  <files>infra/main.tf, infra/variables.tf</files>
  <read_first>
    - infra/main.tf (analogs to mirror: google_cloud_run_v2_service.api ~line 358-403; scaling/resources blocks; secret trio anthropic_api_key ~163-186; native secret injection value_source.secret_key_ref ~462-493; google_storage_bucket.uploads ~301-326 + objectAdmin ~333-337; google_sql_user.app_superadmin ~116-121 BUILT_IN + random_password; google_cloud_run_v2_job.migrate ~531-575)
    - infra/variables.tf (variable declaration idioms: instance_name, db_name, region, project, service_name)
    - tribunal/nestor_pulse_sdk/audit/gcs_blob.py (VERIFY the retention value: 7 years, mode="Unlocked", NOT Bucket Lock, per-object retention — D-09)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-PATTERNS.md §infra/main.tf EXTEND (exact resource shapes: worker min=1/cpu_idle=false/max=5; secret trio; audit bucket 7y Unlocked; app_user/worker_user BUILT_IN)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-RESEARCH.md §Runtime State Inventory + §Pitfall 6 (drift-honest IaC; every secret value seeded out-of-band in the runbook)
  </read_first>
  <action>
    Add by-construction (NOT applied — describes target state; terraform apply is blocked, RESEARCH Pitfall 6)
    Terraform resources to `infra/main.tf`, mirroring the existing block shapes:
    (1) **tribunal-worker** Cloud Run service — mirror `google_cloud_run_v2_service.api` but with
    `scaling { min_instance_count = 1  max_instance_count = 5 }` (D-04 always-on + D-08 5+ target),
    `resources { cpu_idle = false }` (no-cpu-throttling), `timeout` 3600s, ingress internal / no
    unauthenticated, image = the tribunal-worker Artifact Registry tag, `--add-cloudsql-instances` equivalent
    (`volumes`/`vpc`/annotations as the existing services use), env `NESTOR_TRIBUNAL_UNCAPPED=1`
    (D-07 uncapped this phase), `NESTOR_WORKER_STALE_MINUTES` env present (value calibrated in Phase 16),
    and secret injection for DATABASE_URL=DATABASE_URL_WORKER, ANTHROPIC_API_KEY=Nestor_Claude,
    GOOGLE_API_KEY=Nestor_Gemini, OPENAI_API_KEY=Nestor_OpenAI.
    (2) **tribunal-api** Cloud Run service — mirror the api service; `min=0 max=3 timeout=300`; secret
    injection DATABASE_URL=DATABASE_URL (app_user) + the three Nestor_* provider secrets.
    (3) **tribunal-migrate** Cloud Run Job — mirror `google_cloud_run_v2_job.migrate`; image = tribunal image;
    `args = ["alembic","upgrade","head"]`; DATABASE_URL secret (app_user, asyncpg — NOT INSTANCE_CONNECTION_NAME/IAM);
    depends on the tribunal schema/roles existing.
    (4) **Audit-evidence GCS bucket** — mirror `google_storage_bucket.uploads`; ADD 7-year per-object
    retention with `mode="Unlocked"` (NOT Bucket Lock — D-09, verified against gcs_blob.py); `force_destroy=false`;
    bucket-scoped `storage.objectAdmin` binding to the runtime SA. Expose its name via a new `AUDIT_GCS_BUCKET` secret/env.
    (5) **Secrets** (drift-honest): secret resources with `replication`/`auto` and a `count`-gated version
    (value seeded out-of-band per runbook) + resource-scoped `secretmanager.secretAccessor` to the runtime SA,
    for: Nestor_Gemini, Nestor_Claude, Nestor_OpenAI, DATABASE_URL, DATABASE_URL_WORKER, AUDIT_GCS_BUCKET.
    (6) **DB roles** — `app_user` + `worker_user` as BUILT_IN `google_sql_user` (password/asyncpg, NOT IAM —
    RESEARCH Pitfall 5) with `random_password`. Add a comment that `worker_user` is granted USAGE/DML on the
    `tribunal` schema ONLY (never `nestor`) — the actual GRANT lives in migration 0008 (Plan 02) run under
    `search_path=tribunal`; the runbook (Task 2) documents creating the roles out-of-band first.
    Add any new `infra/variables.tf` variables you introduce (e.g. tribunal service names, audit bucket name).
  </action>
  <verify>
    <automated>grep -q "tribunal-worker" infra/main.tf && grep -q "tribunal-api" infra/main.tf && grep -q "tribunal-migrate" infra/main.tf && grep -qi "Unlocked" infra/main.tf && grep -q "DATABASE_URL_WORKER" infra/main.tf && grep -q "worker_user" infra/main.tf && grep -q "min_instance_count = 1" infra/main.tf && grep -q "cpu_idle" infra/main.tf && echo IAC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `infra/main.tf` declares Cloud Run services `tribunal-worker` (min=1, max=5, cpu_idle=false, timeout 3600) and `tribunal-api` (min=0, max=3, timeout 300)
    - `infra/main.tf` declares a `tribunal-migrate` Cloud Run Job with `args = ["alembic","upgrade","head"]`
    - `infra/main.tf` declares an audit-evidence bucket with 7-year per-object retention and `mode = "Unlocked"` (the literal `Unlocked` is present; the literal `BucketLock`/bucket-level `retention_policy` lock is NOT used)
    - `infra/main.tf` declares secret resources for Nestor_Gemini, Nestor_Claude, Nestor_OpenAI, DATABASE_URL, DATABASE_URL_WORKER, AUDIT_GCS_BUCKET, each with a resource-scoped `secretAccessor` to the runtime SA
    - `infra/main.tf` declares BUILT_IN `google_sql_user` resources `app_user` and `worker_user` (not IAM users), with a comment restricting worker_user to the `tribunal` schema
    - `worker-tribunal` secret injection includes `DATABASE_URL=DATABASE_URL_WORKER` and `NESTOR_TRIBUNAL_UNCAPPED=1`
  </acceptance_criteria>
  <done>infra/main.tf describes the full Tribunal footprint (2 services + migrate Job + audit bucket + 6 secrets + 2 DB roles) by construction, mirroring existing intake resource shapes, with the D-09 7y Unlocked retention and D-08 max=5 worker sizing.</done>
</task>

<task type="auto">
  <name>Task 2: Runbook Phase 13 section + retargeted deploy scripts + Cloud Build configs</name>
  <files>infra/DEPLOY-RUNBOOK.md, tribunal/cloudbuild.api.yaml, tribunal/cloudbuild.worker.yaml, tribunal/cloudbuild.test.yaml, tribunal/infrastructure/cloud-run/deploy-api.sh, tribunal/infrastructure/cloud-run/deploy-worker.sh</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md (self-analog: Phase 7 secret-create + resource-scoped accessor + out-of-band value seed; Phase 9 bucket + IAM; Phase 12 image rebuild + service deploy + migrate Job execute `gcloud run jobs execute nestor-migrate --wait`; the IaC-DRIFT reality preamble ~line 26-46)
    - cloudbuild.test.yaml (this repo's test-gate pattern: start pgvector/pgvector:pg16 container, pg_isready poll, run pytest against a fixed DATABASE_URL, non-zero exit fails the build)
    - tribunal/infrastructure/cloud-run/deploy-api.sh + deploy-worker.sh (the copied OLD scripts — retarget PROJECT/INSTANCE/SA/registry from project-cb01b861/nestor-prod-pg to the intake project)
    - C:\Users\ajimimo\Desktop\MOELD\Nestor\infrastructure\cloud-run\DEPLOY.md (old deploy shape + the teardown/pause pattern to mirror for D-02)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-PATTERNS.md §Shared Patterns (secret create idiom, native injection, migrate Job execute, Cloud Build no-local-docker) + §DEPLOY-RUNBOOK.md EXTEND (teardown step)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-CONTEXT.md D-02/D-06/D-09 (teardown-after-proof, Gemini reseed, retention)
  </read_first>
  <action>
    Add a new `## Phase 13 — Tribunal re-home` section to `infra/DEPLOY-RUNBOOK.md`, following the existing
    phase-section idioms, enumerating IN ORDER for the operator live session (Plan 04 executes them):
    (a) create Artifact Registry `nestor` repo images context (reuse existing `nestor` repo);
    (b) create the 6 secrets as empty resources + resource-scoped `secretAccessor` to the runtime SA, then
    add each VALUE out-of-band (`gcloud secrets versions add <name> --data-file=-`, Ctrl-D, never echoed) —
    including reseeding `Nestor_Gemini` from the old project's key (D-06) and composing the two DATABASE_URL/
    DATABASE_URL_WORKER asyncpg-unix-socket URLs;
    (c) create the audit bucket with 7y Unlocked per-object retention (D-09) + bucket-scoped objectAdmin;
    (d) create the `app_user` + `worker_user` Cloud SQL BUILT_IN roles out-of-band (`gcloud sql users create`)
    and note the `CREATE SCHEMA tribunal` happens via the migrate Job's env.py (Plan 02);
    (e) build both images via Cloud Build (`gcloud builds submit tribunal --config tribunal/cloudbuild.api.yaml`
    / `.worker.yaml`, or `--tag europe-west1-docker.pkg.dev/${GOOGLE_PROJECT}/nestor/tribunal-{api,worker}:<sha>`);
    (f) run the `tribunal-migrate` Job (`gcloud run jobs execute tribunal-migrate --region europe-west1 --wait`);
    (g) deploy `tribunal-worker` (always-on, max=5) then `tribunal-api`;
    (h) a `checkpoint`-style note that the E2E proof + verify_chain + concurrency test + duration/cost
    recording are Plan 04;
    (i) a FINAL, clearly-marked **post-proof teardown** step (D-02): delete old `project-cb01b861` Cloud Run
    services (nestor-pulse-api, nestor-pulse-worker), the `nestor-prod-pg` instance, and the old
    `nestor-pulse` Artifact Registry repo — STRICTLY sequenced AFTER the Plan 04 proof run is green.
    Carry the IaC-DRIFT preamble note (terraform apply blocked; runbook is the source of truth).
    Then RETARGET the two copied deploy scripts `tribunal/infrastructure/cloud-run/deploy-{api,worker}.sh`:
    change `PROJECT` default from `project-cb01b861...` to read `$GOOGLE_PROJECT`, `INSTANCE` to the intake
    instance connection name, `SA` to `nestor-run@${GOOGLE_PROJECT}...`, and point image URLs at the `nestor`
    Artifact Registry repo; keep the worker's flags (`--no-cpu-throttling --min-instances=1 --timeout=3600`,
    but set `--max-instances=5` per D-08) and `NESTOR_TRIBUNAL_UNCAPPED=1` (D-07). Finally author three
    Cloud Build configs under `tribunal/`: `cloudbuild.api.yaml` + `cloudbuild.worker.yaml` (docker build +
    push of each image, mirroring the frontend cloudbuild.yaml shape, `logging: CLOUD_LOGGING_ONLY`,
    `timeout: "1200s"`), and `cloudbuild.test.yaml` (mirror this repo's test gate: start a `postgres`/`pgvector`
    container, poll readiness, `pip install` the Tribunal deps from `tribunal/requirements.txt`, run
    `pytest tribunal/nestor_pulse_sdk/tests/ -q` against a fixed DATABASE_URL; non-zero exit fails the build).
  </action>
  <verify>
    <automated>grep -q "Phase 13" infra/DEPLOY-RUNBOOK.md && grep -qi "teardown" infra/DEPLOY-RUNBOOK.md && grep -q "project-cb01b861" infra/DEPLOY-RUNBOOK.md && grep -q "tribunal-migrate" infra/DEPLOY-RUNBOOK.md && test -f tribunal/cloudbuild.api.yaml && test -f tribunal/cloudbuild.worker.yaml && test -f tribunal/cloudbuild.test.yaml && grep -q "tribunal-worker" tribunal/cloudbuild.worker.yaml && [ "$(grep -c 'project-cb01b861' tribunal/infrastructure/cloud-run/deploy-worker.sh)" -eq 0 ] && grep -q "max-instances=5" tribunal/infrastructure/cloud-run/deploy-worker.sh && echo RUNBOOK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `infra/DEPLOY-RUNBOOK.md` contains a `## Phase 13 — Tribunal re-home` section enumerating: the 6 secrets (create + out-of-band value seed), audit bucket (7y Unlocked), app_user/worker_user role creation, both image builds, the `tribunal-migrate` Job execute, and both service deploys
    - The runbook contains a clearly-marked FINAL post-proof teardown step referencing `project-cb01b861`, sequenced AFTER the proof run
    - `tribunal/cloudbuild.api.yaml`, `cloudbuild.worker.yaml`, `cloudbuild.test.yaml` all exist; the test config runs `pytest tribunal/nestor_pulse_sdk/tests/` against a Postgres container
    - `tribunal/infrastructure/cloud-run/deploy-worker.sh` and `deploy-api.sh` contain ZERO `project-cb01b861` literals (retargeted to `$GOOGLE_PROJECT`)
    - `deploy-worker.sh` sets `--max-instances=5` (D-08) while keeping `--min-instances=1`, `--no-cpu-throttling`, `--timeout=3600`, and `NESTOR_TRIBUNAL_UNCAPPED=1`
    - No secret VALUE appears in any committed file (only `gcloud secrets versions add ... --data-file=-` stdin idiom)
  </acceptance_criteria>
  <done>The runbook is the enumerated single source of truth for the Plan 04 live session (deploy + teardown), deploy scripts point at the intake project, and Cloud Build configs exist for both images + the test gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Secret Manager -> Cloud Run env | Secret values injected as references at deploy; never baked into images or committed |
| worker_user role -> schemas | Grants must reach `tribunal` only, never the intake `nestor` schema |
| audit bucket -> legal chain | Bucket must exist with 7y retention BEFORE any run, or the run's own chain dangles |
| old project (project-cb01b861) -> teardown | Destructive; must be strictly after the proof is green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Information Disclosure | Secret value echoed/committed in runbook or deploy script | mitigate | Only the `--data-file=-` stdin idiom in the runbook; no value literals; acceptance criterion greps for absence |
| T-13-09 | Elevation of Privilege | worker_user granted on `nestor` (cross-schema reach) | mitigate | Runbook + 0008 (Plan 02) grant worker_user on `tribunal` ONLY; IaC comment enforces the firewall; Phase 14 extends the denial suite |
| T-13-10 | Repudiation / Tampering | Audit bucket missing/no retention at proof time -> dangling/forgeable chain | mitigate | Runbook creates the bucket with 7y Unlocked retention BEFORE the proof run (ordered step c precedes proof) |
| T-13-11 | DoS (cost) / Availability | Premature teardown of old project before proof green -> lost fallback | mitigate | Teardown is the explicit FINAL post-proof step (D-02), clearly sequenced after Plan 04's green gate |
| T-13-12 | Tampering | Least-privilege violation: project-wide secretAccessor instead of resource-scoped | mitigate | Runbook + IaC use resource-scoped `secretAccessor` bindings (Phase-7 idiom), never project-wide |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | Cloud Build test config installs from the verbatim-carried `tribunal/requirements.txt` (Plan 01 audit: no new selection). No new package chosen. |
</threat_model>

<verification>
- Runbook has a Phase 13 section (secrets, bucket, roles, images, migrate Job, deploys) + a final post-proof teardown step (project-cb01b861)
- `infra/main.tf` describes 2 services + migrate Job + audit bucket (7y Unlocked) + 6 secrets + 2 DB roles by construction
- Deploy scripts retargeted (no old-project literals; worker max=5)
- Cloud Build configs exist for both images + the test suite
- No secret value committed anywhere
</verification>

<success_criteria>
- ENGINE-01: the full deploy/migration footprint is authored (services, migrate Job, schema/roles) ready for the operator live session
- ENGINE-04: the audit bucket with 7y Unlocked retention (D-09) is specified to exist before the proof run
- ENGINE-08: worker sized max=5 (D-08) with the advisory lock making multiple pollers safe
</success_criteria>

<output>
Create `.planning/phases/13-tribunal-re-home-infra-baseline/13-03-SUMMARY.md` when done
</output>
