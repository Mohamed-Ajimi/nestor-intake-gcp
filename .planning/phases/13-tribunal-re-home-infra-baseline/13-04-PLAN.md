---
phase: 13-tribunal-re-home-infra-baseline
plan: 04
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - .planning/phases/13-tribunal-re-home-infra-baseline/13-PROOF-RESULTS.md
  - infra/DEPLOY-RUNBOOK.md
autonomous: false
requirements: [ENGINE-01, ENGINE-02, ENGINE-04, ENGINE-08]
user_setup:
  - service: gemini
    why: "Reseed the Gemini key into the intake project before deploy (D-06)"
    env_vars:
      - name: Nestor_Gemini
        source: "Old Tribunal project (project-cb01b861) Secret Manager -> reseed into intake project"
    dashboard_config:
      - task: "Confirm Anthropic + OpenAI + Gemini keys are all seeded (Nestor_Claude, Nestor_OpenAI, Nestor_Gemini) — D-06 all three providers"
        location: "Intake project Secret Manager"
must_haves:
  truths:
    - "tribunal-api and tribunal-worker run as Cloud Run services in the intake project; the tribunal schema is migrated via tribunal_alembic_version"
    - "verify_chain returns green against the re-homed audit hash-chain (ENGINE-04 legal gate)"
    - "Two simultaneous runs from different spaces complete without interfering (advisory lock proven; validated toward the 5+ target)"
    - "One real LUKOIL-benchmark research run completes end-to-end green, with its measured duration and cost recorded"
  artifacts:
    - path: ".planning/phases/13-tribunal-re-home-infra-baseline/13-PROOF-RESULTS.md"
      provides: "Recorded proof-run duration + cost + chain-verify + concurrency result (feeds Phase 16 stale calibration)"
      contains: "duration"
  key_links:
    - from: "run_tribunal_smoke.py / test_tribunal_e2e.py"
      to: "verify_chain_endpoint"
      via: "chain-verify assertion on the proof run"
      pattern: "verify_chain|chain"
---

<objective>
Operator-run live session that turns the authored artifacts (Plans 01–03) into a proven, live re-home
and closes all four phase gates. Because the dev machine has NO Python/Docker and `terraform apply` is
blocked (RESEARCH §Environment Availability), every step here is executed by the operator via gcloud /
Cloud Build following the Plan-03 runbook — this plan is a sequence of human-verify checkpoints, not
autonomous code. It: seeds secrets, builds images, runs the migrate Job, deploys both services, runs the
Tribunal test suite in Cloud Build, drives the D-05 LUKOIL benchmark E2E proof run, asserts `verify_chain`
green (ENGINE-04), runs the ≥2-concurrent-runs-from-different-spaces advisory-lock proof (ENGINE-08,
validated toward D-08's 5+), RECORDS the run duration + cost (ENGINE-02, feeds Phase 16), and — strictly
AFTER the proof is green — tears down the old `project-cb01b861` deployment (D-02).

Purpose: Prove the engine survived the move (chain intact, concurrency-safe, one real run green) before
any Phase 14+ feature builds on it.

Output: `13-PROOF-RESULTS.md` capturing run_id/duration/cost/recall/chain result + the concurrency result,
and the runbook updated with the actual deployed revisions + recorded values.

**Note:** `NESTOR_TRIBUNAL_UNCAPPED=1` stays ON (D-07) — this phase only RECORDS cost; enforcement is Phase 16.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tribunal-re-home-infra-baseline/13-CONTEXT.md
@.planning/phases/13-tribunal-re-home-infra-baseline/13-RESEARCH.md
@infra/DEPLOY-RUNBOOK.md

<interfaces>
Proof vehicles (copied in Plan 01, ready-made — RESEARCH §Code Examples):
- tribunal/nestor_pulse_sdk/scripts/run_tribunal_smoke.py — in-process TribunalPipeline run against live
  Cloud SQL; self-provisions ephemeral org/project/run; prints run_id/tenant_id, total+grounded claims,
  recall %, sum(cost_usd), chain ok/broken_at, elapsed seconds. Guards: DEMO_MODE must be unset;
  NESTOR_SDK_ORCHESTRATOR=tribunal required. Accepts --brief (feed the LUKOIL brief), --tenant-id/--project-id.
- tribunal/nestor_pulse_sdk/tests/test_tribunal_e2e.py — NESTOR_E2E=1 gated; asserts non-empty output,
  ≥1 claim, ≥1 claim_source, chain verifies, recall in [0,1] and >0.
- LUKOIL benchmark brief text: C:\Users\ajimimo\Desktop\MOELD\Nestor\lukoil_questions.py (COMBINED_BRIEF)
  + deep_research_compare/ known outputs (referenced read-only from the sibling repo — D-05).
- verify_chain: GET /api/audit/verify/{run_id} on tribunal-api, OR the smoke script's inline chain check.

Concurrency proof (ENGINE-08 / D-08): trigger ≥2 runs from DIFFERENT tenant_ids simultaneously (two
smoke invocations with distinct --tenant-id, or POST /api/runs to the deployed worker) and confirm both
complete with verify_chain green and no forked chain / double-run — validated toward the 5+ target.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Seed secrets, build images, migrate the tribunal schema, deploy both services</name>
  <what-built>
    Plans 01–03 authored: the copied engine (with isolated Alembic line + advisory lock), by-construction
    IaC, the Phase-13 runbook, retargeted deploy scripts, and Cloud Build configs. This checkpoint is the
    operator executing the runbook's Phase-13 section live in the intake "Nestor Pulse" project.
  </what-built>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 13 — Tribunal re-home (the enumerated steps a–g authored in Plan 03)
    - tribunal/cloudbuild.api.yaml, tribunal/cloudbuild.worker.yaml (image builds)
    - tribunal/infrastructure/cloud-run/deploy-api.sh, deploy-worker.sh (retargeted deploy scripts)
  </read_first>
  <files>infra/DEPLOY-RUNBOOK.md (operator annotates deployed revisions)</files>
  <action>
    Operator-run (no autonomous code): execute the infra/DEPLOY-RUNBOOK.md Phase 13 steps in order — seed the
    6 secrets out-of-band (reseed Nestor_Gemini from the old project per D-06; compose DATABASE_URL +
    DATABASE_URL_WORKER asyncpg-unix-socket URLs), create the audit bucket with 7y Unlocked per-object
    retention (D-09) + bucket-scoped objectAdmin, create the app_user + worker_user Cloud SQL BUILT_IN roles,
    build both Tribunal images via Cloud Build, run gcloud run jobs execute tribunal-migrate --region
    europe-west1 --wait, then deploy tribunal-worker (min=1, max=5, no-cpu-throttling, timeout=3600) and
    tribunal-api. The FIRST Cloud Build of the carried-verbatim tribunal/requirements.txt is the Plan-01
    legitimacy gate — confirm it builds cleanly before deploying. Annotate the deployed revisions into the runbook.
  </action>
  <how-to-verify>
    Following infra/DEPLOY-RUNBOOK.md § Phase 13, run the FIRST-BUILD gate: this is the first live Cloud
    Build of the carried-verbatim `tribunal/requirements.txt` — confirm the package set builds cleanly
    before proceeding (satisfies the Plan-01 legitimacy checkpoint). Then, as the operator (project
    "Nestor Pulse", acct tools@dotto.be, region europe-west1):
    1. Seed the 6 secrets out-of-band (reseed Nestor_Gemini from the old project — D-06; confirm all three
       provider keys present — D-06 all-three; compose DATABASE_URL + DATABASE_URL_WORKER asyncpg URLs).
    2. Create the audit bucket with 7y Unlocked per-object retention (D-09) + bucket-scoped objectAdmin.
    3. Create app_user + worker_user Cloud SQL BUILT_IN roles.
    4. Build both images via Cloud Build; run `gcloud run jobs execute tribunal-migrate --region europe-west1 --wait`.
    5. Confirm the migration landed in the tribunal schema: `\dt tribunal.*` shows `run`, `audit_log`, ...;
       the version table is `tribunal.tribunal_alembic_version`; `\d tribunal.run` CHECK includes needs_report_spec.
    6. Deploy tribunal-worker (min=1, max=5, no-cpu-throttling, timeout=3600) then tribunal-api; hit
       `/healthz` and `/readyz` on tribunal-api (expect 200; readyz needs Cloud SQL RUNNABLE).
    Confirm: worker starts polling (logs show `worker_started`); no `SCHEMA public` grant errors; the worker
    binds worker_user (not app_user — the DATABASE_URL_WORKER guard).
  </how-to-verify>
  <verify>
    <human-check>tribunal-api /healthz + /readyz return 200; `\dt tribunal.*` lists the engine tables; version table is tribunal_alembic_version; worker logs show it polling as worker_user with no SCHEMA-public grant errors</human-check>
  </verify>
  <acceptance_criteria>
    - Both Cloud Run services (tribunal-api, tribunal-worker) show a live revision in the intake project
    - `gcloud run jobs execute tribunal-migrate --wait` exited 0; `tribunal.run` / `tribunal.audit_log` tables exist; `tribunal.tribunal_alembic_version` exists (NOT public.alembic_version)
    - tribunal-api `/healthz` returns 200 and `/readyz` returns 200
    - Worker logs show `worker_started` and no `claim_one` ZERO-rows stall (0008 grants landed in `tribunal`)
  </acceptance_criteria>
  <done>Both tribunal-api and tribunal-worker are live in the intake project; the tribunal schema is migrated via tribunal_alembic_version; the worker polls as worker_user with no SCHEMA-public grant errors.</done>
  <resume-signal>Type "deployed" when both services are live and the schema migrated, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Run the Tribunal test suite in Cloud Build + the LUKOIL E2E proof run; assert verify_chain green; record duration + cost</name>
  <what-built>
    The deployed engine (Task 1) plus the copied proof vehicles (run_tribunal_smoke.py / test_tribunal_e2e.py)
    and the LUKOIL benchmark brief (D-05).
  </what-built>
  <read_first>
    - tribunal/nestor_pulse_sdk/scripts/run_tribunal_smoke.py (the proof vehicle — flags, output fields, guards)
    - tribunal/nestor_pulse_sdk/tests/test_tribunal_e2e.py (NESTOR_E2E-gated assertions)
    - tribunal/cloudbuild.test.yaml (the suite gate)
    - C:\Users\ajimimo\Desktop\MOELD\Nestor\lukoil_questions.py (COMBINED_BRIEF for the proof — D-05)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-RESEARCH.md §Validation Architecture (per-req test map)
  </read_first>
  <files>.planning/phases/13-tribunal-re-home-infra-baseline/13-PROOF-RESULTS.md (operator records the proof-run numbers)</files>
  <action>
    Operator-run: run the full Tribunal suite via gcloud builds submit tribunal --config tribunal/cloudbuild.test.yaml
    (must include the new test_schema_isolation.py + test_advisory_lock_exactly_once.py and exit 0), then drive
    the D-05 LUKOIL benchmark proof run through run_tribunal_smoke.py (--brief the LUKOIL COMBINED_BRIEF) or
    NESTOR_E2E=1 pytest test_tribunal_e2e.py. Assert verify_chain = OK (broken_at None — ENGINE-04 gate) and
    ≥1 claim + ≥1 claim_source + recall > 0. RECORD run_id, elapsed seconds (max run length), cost_usd, and
    recall into 13-PROOF-RESULTS.md — these feed Phase 16 stale-run calibration (D-07 keeps UNCAPPED=1; cost is
    recorded, not enforced).
  </action>
  <how-to-verify>
    1. Run the full Tribunal test suite via Cloud Build (`tribunal/cloudbuild.test.yaml`) — including
       the NEW test_schema_isolation.py and test_advisory_lock_exactly_once.py (Plan 02). Confirm green.
    2. Drive the D-05 proof run: feed the LUKOIL COMBINED_BRIEF to `run_tribunal_smoke.py`
       (`NESTOR_SDK_ORCHESTRATOR=tribunal ... --brief "<LUKOIL combined brief>"`), OR run
       `NESTOR_E2E=1 pytest tribunal/nestor_pulse_sdk/tests/test_tribunal_e2e.py -x`. Confirm:
       non-empty output, ≥1 claim + ≥1 claim_source, recall > 0, and **chain = OK (verify_chain green —
       ENGINE-04 legal gate)**.
    3. RECORD the wall-clock duration (elapsed seconds) + cost_usd from the smoke summary — these feed
       Phase 16 stale-run calibration (ENGINE-02, D-07). Write them into 13-PROOF-RESULTS.md.
    Guard: DEMO_MODE must be unset; if the brief triggers clarification, use a more specific brief.
  </how-to-verify>
  <verify>
    <human-check>Cloud Build test suite green (incl. the two new tests); the LUKOIL proof run prints chain=OK, total_claims>0, and a recorded elapsed + cost_usd; values captured in 13-PROOF-RESULTS.md</human-check>
  </verify>
  <acceptance_criteria>
    - The Cloud Build run of `tribunal/cloudbuild.test.yaml` exited 0 (full suite green, including test_schema_isolation.py and test_advisory_lock_exactly_once.py)
    - The LUKOIL E2E proof run completed with chain-verify = OK (broken_at is None) — ENGINE-04 gate green
    - The proof run persisted ≥1 claim and ≥1 claim_source; recall > 0
    - `13-PROOF-RESULTS.md` records: run_id, elapsed seconds (max run length), cost_usd, recall %, chain result
  </acceptance_criteria>
  <done>The Cloud Build suite is green (incl. both new tests); the LUKOIL E2E proof run completed with verify_chain=OK; run_id, duration, cost, and recall are recorded in 13-PROOF-RESULTS.md.</done>
  <resume-signal>Type "proof-green" with the recorded duration + cost, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Prove ≥2 concurrent runs from different spaces; then tear down the old project (post-proof)</name>
  <what-built>
    The advisory-lock-guarded worker (Plan 02, sized max=5 in Plan 03) plus the green single-run proof (Task 2).
  </what-built>
  <read_first>
    - tribunal/nestor_pulse_sdk/runs/execute.py (the per-run advisory lock — the mechanism under test)
    - tribunal/nestor_pulse_sdk/scripts/run_tribunal_smoke.py (--tenant-id lets you drive distinct spaces)
    - infra/DEPLOY-RUNBOOK.md § Phase 13 FINAL teardown step (D-02 — the old project-cb01b861 resources)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-CONTEXT.md D-02/D-08 (teardown-after-proof; 5+ target)
  </read_first>
  <files>.planning/phases/13-tribunal-re-home-infra-baseline/13-PROOF-RESULTS.md, infra/DEPLOY-RUNBOOK.md</files>
  <action>
    Operator-run: trigger ≥2 runs SIMULTANEOUSLY from DIFFERENT tenant_ids (two run_tribunal_smoke.py invocations
    with distinct --tenant-id, or two POST /api/runs to the deployed worker) and confirm BOTH complete with
    verify_chain=OK and no forked chain / double-run (advisory lock + claimable-set guard held) — push toward ~5
    to validate the D-08 target; ≥2 is the required gate. Record the result in 13-PROOF-RESULTS.md. ONLY AFTER
    Task 2 AND this concurrency proof are green (D-02 sequencing), tear down the old project-cb01b861 deployment
    (delete Cloud Run nestor-pulse-api + nestor-pulse-worker, the nestor-prod-pg instance, and the old nestor-pulse
    Artifact Registry repo) per the runbook FINAL step; confirm the intake services still serve; record actual
    deployed revisions + teardown-done in the runbook.
  </action>
  <how-to-verify>
    1. Concurrency proof (ENGINE-08 / D-08): trigger ≥2 runs SIMULTANEOUSLY from DIFFERENT tenant_ids
       (two smoke invocations with distinct --tenant-id, or two POST /api/runs to the deployed worker with
       different spaces). Confirm BOTH complete, BOTH have verify_chain = OK, and neither forked the chain
       or double-ran (the advisory lock + claimable-set guard held). Ideally push toward ~5 concurrent to
       validate the D-08 target, but ≥2 is the required gate. Record the result in 13-PROOF-RESULTS.md.
    2. ONLY AFTER Task 2's proof AND this concurrency proof are green (D-02 sequencing): tear down the old
       standalone deployment on `project-cb01b861` — delete Cloud Run `nestor-pulse-api` + `nestor-pulse-worker`,
       delete the `nestor-prod-pg` Cloud SQL instance, and remove the old `nestor-pulse` Artifact Registry
       repo — following the runbook's FINAL teardown step. Confirm the intake-project services still serve
       (no dependency on the old project). Update the runbook with the actual deployed revisions.
  </how-to-verify>
  <verify>
    <human-check>≥2 concurrent runs from different spaces both complete with verify_chain=OK and no double-run; old project-cb01b861 services + nestor-prod-pg deleted AFTER the proof; intake tribunal services still healthy</human-check>
  </verify>
  <acceptance_criteria>
    - ≥2 simultaneous runs from DIFFERENT tenant_ids both completed with chain-verify = OK; no forked chain; recorded in `13-PROOF-RESULTS.md`
    - The old `project-cb01b861` Cloud Run services + `nestor-prod-pg` instance are deleted (D-02), performed strictly after the single-run + concurrency proofs were green
    - tribunal-api `/readyz` still returns 200 after teardown (no cross-dependency on the old project)
    - `infra/DEPLOY-RUNBOOK.md` records the actual deployed revisions and links the teardown as done
  </acceptance_criteria>
  <done>≥2 concurrent runs from different spaces completed with verify_chain=OK and no double-run; the old project-cb01b861 deployment is torn down strictly after the proof; the intake tribunal services remain healthy.</done>
  <resume-signal>Type "concurrency-green + torn-down" or describe what remains.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator session -> live GCP | Destructive/paid operations (real research run, teardown) run under tools@dotto.be |
| concurrent worker executors -> audit chain | Two live runs must not fork the chain or double-spend |
| old project -> teardown | Irreversible deletes; must be strictly post-proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Tampering / Repudiation | Re-homed audit chain broken by the move | mitigate | `verify_chain` is the hard phase gate (Task 2) — chain must be OK before proceeding (ENGINE-04) |
| T-13-14 | Tampering (audit) / DoS(cost) | Concurrent runs fork the chain or double-run | mitigate | ≥2-concurrent proof (Task 3) confirms the advisory lock holds; both runs verify_chain green |
| T-13-15 | DoS (cost) | Uncapped proof run burns unbounded provider spend | accept | D-07 keeps UNCAPPED=1 this phase; exposure low (no client-facing runs, D-10); cost RECORDED; cap is Phase 16 |
| T-13-16 | Availability / Data loss | Premature teardown before proof green | mitigate | Teardown gated behind both proofs being green (Task 3 ordering, D-02) |
| T-13-17 | Information Disclosure | Secret values surfaced in operator logs during seeding | mitigate | Runbook uses `--data-file=-` stdin; operator never echoes values; `gcloud describe` shows references only |
| T-13-SC | Tampering | First live Cloud Build of the carried package set | mitigate | Task 1 first-build gate confirms the verbatim `requirements.txt` builds cleanly (the Plan-01 legitimacy checkpoint, non-auto-approvable) before deploy |
</threat_model>

<verification>
- Both services live in the intake project; tribunal schema migrated via tribunal_alembic_version (ENGINE-01)
- Cloud Build test suite green incl. the two new tests
- LUKOIL E2E proof run green; verify_chain = OK (ENGINE-04); duration + cost recorded (ENGINE-02)
- ≥2 concurrent runs from different spaces green, no forked chain (ENGINE-08)
- Old project-cb01b861 torn down strictly after the proof (D-02)
</verification>

<success_criteria>
- All four phase gates closed: ENGINE-01 (live services + isolated schema), ENGINE-02 (one real run green + duration/cost recorded), ENGINE-04 (verify_chain green), ENGINE-08 (≥2 concurrent from different spaces proven)
- The re-homed engine is proven ready for Phase 14+ to build on; the old deployment is retired
</success_criteria>

<output>
Create `.planning/phases/13-tribunal-re-home-infra-baseline/13-04-SUMMARY.md` when done
</output>
