---
phase: 13-tribunal-re-home-infra-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/
  - tribunal/nestor_pulse/secrets.py
  - tribunal/requirements.txt
  - tribunal/infrastructure/
  - tribunal/.gcloudignore
  - tribunal/pyproject.toml
  - tribunal/README.md
autonomous: true
requirements: [ENGINE-01, ENGINE-04]
must_haves:
  truths:
    - "The Tribunal engine source lives under tribunal/ in this repo and imports resolve without reaching the sibling Nestor repo"
    - "The frozen audit hash-chain (audit/hash_chain.py) is present byte-identical — the tenant_id/gcs_uri payload is unchanged"
    - "The only nestor_pulse.* import on the tribunal engine path is nestor_pulse.secrets (CI grep gate proves it)"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/audit/hash_chain.py"
      provides: "Frozen tamper-evident audit hash-chain (ENGINE-04)"
      contains: "tenant_id"
    - path: "tribunal/nestor_pulse/secrets.py"
      provides: "The sole cross-package dependency (DATABASE_URL / bucket loader)"
      contains: "def load_secrets_into_env"
    - path: "tribunal/requirements.txt"
      provides: "Pinned Python 3.11.9 dependency set carried verbatim"
      contains: "asyncpg==0.31.0"
    - path: "tribunal/.gcloudignore"
      provides: "Excludes __pycache__/.venv/.pytest_cache from Cloud Build context"
      contains: "__pycache__"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/secrets_bootstrap.py"
      to: "tribunal/nestor_pulse/secrets.py"
      via: "from nestor_pulse.secrets import load_secrets_into_env"
      pattern: "from nestor_pulse.secrets import"
---

<objective>
Copy the working Tribunal deep-research engine (`nestor_pulse_sdk` + the single cross-package
module `nestor_pulse/secrets.py`) verbatim into a new top-level `tribunal/` directory in this repo,
so that from Phase 13 onward all Tribunal changes/plans/commits live here and the old Nestor repo
becomes a frozen reference (D-01). This is a lift-and-shift: the copied files are carried byte-identical
(the audit hash-chain and pinned deps are integrity-critical). This plan does the COPY and proves the
import graph is clean; the two NEW code changes (env.py isolation + advisory lock) are Plan 02.

Purpose: Establish the engine in this repo as the base every downstream plan builds on, without
altering any integrity-critical seam (the audit chain must survive the move — ENGINE-04).

Output: A `tribunal/` tree containing the engine, its sole cross-dep, pinned requirements, deploy
Dockerfiles/scripts (for Plan 03 to retarget), the test suite + smoke script (for Plan 04 to run),
and a `.gcloudignore`. A CI grep gate proves the only `nestor_pulse.*` import on the engine path is
`nestor_pulse.secrets`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tribunal-re-home-infra-baseline/13-CONTEXT.md
@.planning/phases/13-tribunal-re-home-infra-baseline/13-RESEARCH.md
@.planning/phases/13-tribunal-re-home-infra-baseline/13-PATTERNS.md

<copy_manifest>
Source repo (frozen reference, copy FROM here): C:\Users\ajimimo\Desktop\MOELD\Nestor\
Destination (this repo): tribunal/

COPY these (RESEARCH §Copy Manifest — verified against the import graph):
- Nestor/nestor_pulse_sdk/**            -> tribunal/nestor_pulse_sdk/**   (entire engine, verbatim)
- Nestor/nestor_pulse/secrets.py        -> tribunal/nestor_pulse/secrets.py   (ONLY this module from nestor_pulse/)
- Nestor/requirements.txt               -> tribunal/requirements.txt
- Nestor/pyproject.toml                 -> tribunal/pyproject.toml   (pytest config the suite needs)
- Nestor/infrastructure/cloud-run/**    -> tribunal/infrastructure/**  (Dockerfiles + deploy scripts, for Plan 03 to retarget)

DO NOT COPY (out of scope for the tribunal path — RESEARCH §Copy Manifest "Do NOT copy"):
- Nestor/nestor_pulse/ package body beyond secrets.py (the ADK arm — engine="adk", not deployed)
- Nestor/nestor_pulse_sdk/web/ static UI, AWS/, deep_research_compare/ (huge; the proof brief text is referenced read-only from the sibling repo in Plan 04)
- Any __pycache__/, .venv/, .pytest_cache/ directories
- The sibling repo's .planning/ and git history (plain copy per D-01 discretion)
</copy_manifest>

<interfaces>
The sole cross-package import to preserve (RESEARCH Assumption A1 — CORRECTS CONTEXT D-01's
"nestor_pulse.tools.*" claim, which is FALSE for the tribunal path):

From tribunal/nestor_pulse_sdk/secrets_bootstrap.py:
  from nestor_pulse.secrets import load_secrets_into_env   # the ONE real nestor_pulse dep

The ADK arm's lazy import lives in runs/adapter.py (engine="adk", out of scope). The authoritative
CI grep gate (from the sibling repo's adapter.py header) is:
  grep -rE "from nestor_pulse[. ]" tribunal/nestor_pulse_sdk/ | grep -v adapter.py   ->  must be EMPTY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the Tribunal engine + sole cross-dep into tribunal/</name>
  <files>tribunal/nestor_pulse_sdk/, tribunal/nestor_pulse/secrets.py, tribunal/requirements.txt, tribunal/pyproject.toml, tribunal/infrastructure/, tribunal/README.md</files>
  <read_first>
    - C:\Users\ajimimo\Desktop\MOELD\Nestor\nestor_pulse_sdk\ (source tree — confirm the subdirs: alembic, audit, db, runs, pipeline, citations, tools, scripts, tests, server.py, secrets_bootstrap.py, health.py)
    - C:\Users\ajimimo\Desktop\MOELD\Nestor\nestor_pulse\secrets.py (the sole cross-dep; imports only stdlib logging/os/typing)
    - C:\Users\ajimimo\Desktop\MOELD\Nestor\requirements.txt (pinned Py 3.11.9 set)
    - C:\Users\ajimimo\Desktop\MOELD\Nestor\infrastructure\cloud-run\ (Dockerfiles + deploy-api.sh/deploy-worker.sh/build-and-push.sh/artifact-registry-create.sh/DEPLOY.md)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-RESEARCH.md §Copy Manifest (the exact copy/do-not-copy list)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-PATTERNS.md §File Classification A (verbatim-copy list)
  </read_first>
  <action>
    Create the top-level `tribunal/` directory and copy the manifest above from the sibling repo
    `C:\Users\ajimimo\Desktop\MOELD\Nestor\` into it, PRESERVING directory structure. Copy the ENTIRE
    `nestor_pulse_sdk/` (engine: alembic, audit, db, runs, pipeline, citations, tools, scripts, tests,
    server.py, secrets_bootstrap.py, health.py), plus `nestor_pulse/secrets.py` (create the
    `tribunal/nestor_pulse/` package dir with an `__init__.py` if the source has one), `requirements.txt`,
    `pyproject.toml`, and `infrastructure/cloud-run/` (rename to `tribunal/infrastructure/` — Plan 03
    retargets its Dockerfiles/scripts). Use a copy that EXCLUDES `__pycache__/`, `.venv/`, `.pytest_cache/`,
    `.git/`, `.planning/` (e.g. robocopy /XD or a filtered cp). Keep every copied file byte-identical —
    especially `nestor_pulse_sdk/audit/hash_chain.py` (FROZEN, ENGINE-04 legal gate). Do NOT re-resolve
    or bump `requirements.txt` (keep Python 3.11.9 / asyncpg / anthropic==0.104.1 pins — NOT the intake
    3.12/pg8000 stack). Add a short `tribunal/README.md` noting: this is the re-homed Tribunal engine
    (D-01), Python 3.11.9 image, separate from `backend/` (3.12/pg8000), source frozen at the sibling
    Nestor repo. Do NOT copy `web/`, `AWS/`, `deep_research_compare/`, or the `nestor_pulse/` package
    body beyond `secrets.py`.
  </action>
  <verify>
    <automated>test -f tribunal/nestor_pulse_sdk/audit/hash_chain.py && test -f tribunal/nestor_pulse/secrets.py && test -f tribunal/requirements.txt && grep -q "asyncpg==0.31.0" tribunal/requirements.txt && grep -q "tenant_id" tribunal/nestor_pulse_sdk/audit/hash_chain.py && grep -q "def load_secrets_into_env" tribunal/nestor_pulse/secrets.py && echo COPY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `tribunal/nestor_pulse_sdk/` contains subdirs: alembic, audit, db, runs, pipeline, citations, tools, scripts, tests
    - `tribunal/nestor_pulse_sdk/audit/hash_chain.py` exists and contains the literal `tenant_id` (frozen payload preserved)
    - `tribunal/nestor_pulse/secrets.py` exists and contains `def load_secrets_into_env`
    - `tribunal/requirements.txt` contains `asyncpg==0.31.0` and `anthropic==0.104.1` (pins carried verbatim, not re-resolved)
    - `tribunal/infrastructure/` contains `cloud-run/api/Dockerfile`, `cloud-run/worker/Dockerfile`, `deploy-api.sh`, `deploy-worker.sh`
    - No `__pycache__`, `.venv`, or `.pytest_cache` directory exists anywhere under `tribunal/` (find returns empty)
    - `tribunal/nestor_pulse_sdk/web/` does NOT exist (static UI excluded)
  </acceptance_criteria>
  <done>The Tribunal engine tree is present under tribunal/ with the frozen hash-chain and sole cross-dep intact; junk dirs excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Add .gcloudignore + prove the import graph is clean (CI grep gate)</name>
  <files>tribunal/.gcloudignore</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/runs/adapter.py (contains the authoritative CI grep-gate comment; the only file allowed to import nestor_pulse for the ADK arm)
    - tribunal/nestor_pulse_sdk/secrets_bootstrap.py (the file that legitimately does `from nestor_pulse.secrets import load_secrets_into_env`)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-RESEARCH.md §Assumption A1 (the grep gate is authoritative; run it on the COPIED tree before first deploy)
    - .gcloudignore or infra/DEPLOY-RUNBOOK.md (this repo's Cloud Build ignore idioms, if present)
  </read_first>
  <action>
    Write `tribunal/.gcloudignore` excluding `__pycache__/`, `*.pyc`, `.venv/`, `.pytest_cache/`,
    `.git/`, `.planning/`, `**/*.env`, and any local secret files, so `gcloud builds submit` from the
    `tribunal/` tree ships a clean context (RESEARCH §Runtime State Inventory). Then run the authoritative
    import-graph gate from RESEARCH Assumption A1 against the COPIED tree:
    `grep -rE "from nestor_pulse[. ]" tribunal/nestor_pulse_sdk/ | grep -v adapter.py` — this MUST return
    only the single `secrets_bootstrap.py` line `from nestor_pulse.secrets import load_secrets_into_env`
    (the sole real cross-dep) and nothing else. This corrects CONTEXT D-01's false `nestor_pulse.tools.*`
    claim: verify `pipeline/deep_researchers/degraded_parallel.py` imports `nestor_pulse_sdk.tools.*_adapter`
    (in-package), NOT `nestor_pulse.tools.*`. If any unexpected `from nestor_pulse.` import appears outside
    `adapter.py` and `secrets_bootstrap.py`, STOP and flag it — the copied image would ImportError at boot.
  </action>
  <verify>
    <automated>test -f tribunal/.gcloudignore && grep -q "__pycache__" tribunal/.gcloudignore && [ "$(grep -rE 'from nestor_pulse[. ]' tribunal/nestor_pulse_sdk/ | grep -v 'adapter.py' | grep -v 'secrets_bootstrap.py' | wc -l)" -eq 0 ] && grep -q "nestor_pulse_sdk.tools" tribunal/nestor_pulse_sdk/pipeline/deep_researchers/degraded_parallel.py && echo GRAPH_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `tribunal/.gcloudignore` exists and contains `__pycache__` and `.venv`
    - `grep -rE "from nestor_pulse[. ]" tribunal/nestor_pulse_sdk/ | grep -v adapter.py | grep -v secrets_bootstrap.py` returns ZERO lines
    - `tribunal/nestor_pulse_sdk/secrets_bootstrap.py` contains `from nestor_pulse.secrets import load_secrets_into_env`
    - `tribunal/nestor_pulse_sdk/pipeline/deep_researchers/degraded_parallel.py` imports `nestor_pulse_sdk.tools` (in-package), confirming CONTEXT's `nestor_pulse.tools.*` claim is corrected
  </acceptance_criteria>
  <done>The Cloud Build context is clean and the import graph is proven to have exactly one nestor_pulse cross-dep (secrets), matching RESEARCH's correction of CONTEXT D-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sibling repo -> this repo (file copy) | Integrity-critical files (audit hash-chain, pinned deps) must survive the copy byte-identical |
| copied engine -> Cloud Build context | `.gcloudignore` must prevent local secrets/`.env`/caches leaking into the build image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | audit/hash_chain.py copied with an altered/renamed field | mitigate | Copy byte-identical; acceptance criterion greps `tenant_id` present; Plan 04 runs verify_chain as the live gate |
| T-13-02 | Tampering | Missed lazy `nestor_pulse.*` import -> ImportError at boot after copy | mitigate | CI grep gate (RESEARCH A1) run on the copied tree; must return exactly one secrets line |
| T-13-03 | Information Disclosure | Local `.env`/secret file swept into the Cloud Build context | mitigate | `.gcloudignore` excludes `*.env`, `.venv`, `__pycache__`; secrets are injected at deploy (Plan 03), never baked |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages selected — `requirements.txt` carried verbatim from the live standalone deployment (RESEARCH §Package Legitimacy Audit). All pins already run in production; no `[ASSUMED]`/`[SUS]` gate needed. First live build is gated by Plan 04's operator checkpoint. |
</threat_model>

<verification>
- `tribunal/` exists with the engine, sole cross-dep, pinned requirements, deploy scripts, tests, smoke script
- Frozen `audit/hash_chain.py` present and unaltered (contains `tenant_id`)
- Import-graph grep gate returns exactly one `nestor_pulse` cross-dep (`secrets`)
- No `__pycache__`/`.venv`/`.pytest_cache` under `tribunal/`; `.gcloudignore` present
</verification>

<success_criteria>
- The Tribunal engine is re-homed under `tribunal/` in this repo, byte-identical on all integrity-critical files (ENGINE-01 base, ENGINE-04 frozen chain carried)
- Downstream plans (02 env.py + lock, 03 deploy, 04 proof) can operate entirely within this repo
</success_criteria>

<output>
Create `.planning/phases/13-tribunal-re-home-infra-baseline/13-01-SUMMARY.md` when done
</output>
