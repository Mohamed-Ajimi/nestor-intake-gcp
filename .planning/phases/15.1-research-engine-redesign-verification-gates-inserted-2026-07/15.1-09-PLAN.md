---
phase: 15.1-research-engine-redesign-verification-gates
plan: 09
type: execute
wave: 5
depends_on: ["15.1-06", "15.1-08"]
files_modified:
  - tribunal/nestor_pulse_sdk/tests/test_gate_replay.py
  - tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py
autonomous: true
requirements: [ENGINE-10]

must_haves:
  truths:
    - "Feeding the recorded 1,162 claims plus the recorded answer key through the REAL gate machinery reproduces kept 456 / dropped 706 / selected_verify 424 / skipped_stable 32"
    - "Those numbers propagate unchanged into run.verification_summary and the verification report"
    - "The phase-closing test makes zero LLM calls and cannot flake"
    - "The calibration test never gates CI and contains no agreement-percentage threshold"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/tests/test_gate_replay.py"
      provides: "the G-01 CI gate — deterministic fixture replay through real gates"
      contains: "_AnswerKeyGateAudited"
      min_lines: 150
    - path: "tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py"
      provides: "the informational, operator-run August calibration (G-01 + G-05)"
      contains: "pytest.mark.live"
  key_links:
    - from: "tests/test_gate_replay.py"
      to: "gates.apply_gates"
      via: "real production code driven by a fake audited replaying the answer key"
      pattern: "apply_gates\\("
    - from: "tests/test_gate_replay.py"
      to: "loader.load_selection_experiment"
      via: "the single shared fixture parser"
      pattern: "load_selection_experiment"
---

<objective>
Write G-01's two tests — only one of which gates the phase.

**The CI test** (`test_gate_replay.py`) feeds the 1,162 fixture claims plus the recorded
KEEP/DROP/SKIP_STABLE answer key through the REAL gate machinery — the real batching, the real
semaphore, the real parser, the real fallback logic, the real funnel arithmetic — and asserts
the funnel lands exactly on `kept 456 / dropped 706 / selected_verify 424 / skipped_stable 32`,
and that those numbers propagate to `run.verification_summary` and the verification report.
Zero LLM calls, zero flakiness, closes the phase.

**The calibration test** (`test_gate_calibration.py`) turns the REAL classifier loose on the
fixture and REPORTS agreement against the blind answer key. Written now, hand-run by the
operator after 2026-08-01. Never a CI gate, never a threshold assertion.

Purpose: the 456/424 labels came from blind LLM agents. A production LLM gate cannot hit them
exactly, so demanding exact reproduction from a live classifier would park 15.1 as incomplete
until August and block 15.2.

**G-13 boundary:** the CI test asserts FIVE gate-computed numbers, not six. `verify_sessions
176` is a recorded pass-through constant (176 group-skeptic calls that RETURNED while 776 were
cap-rejected) — it measures an incident, not the funnel. It must not appear in any assertion in
either file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-CONTEXT.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-RESEARCH.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-PATTERNS.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-VALIDATION.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-05-SUMMARY.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-06-SUMMARY.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-08-SUMMARY.md

<interfaces>
<!-- The recommended seam (RESEARCH Pattern 2 — "the single most important design decision
     in this phase"). Build _AnswerKeyGateAudited in the TEST module: -->
1. parse the prompt's `INDEX | CLAIM_TEXT` block back out of `contents`
   (gates.py renders it the same way grouping.py:125-127 does)
2. map each claim text back to its 1-based fixture id (build text → id ONCE at module scope
   from load_selection_experiment())
3. look up the recorded decision from the classified / strict answer keys
4. render those decisions in the gate's OWN plain-text line format and return them as .text
5. distinguish the two prompts (materiality vs stability) by a marker in `contents`

<!-- ANTI-RECOMMENDATION (RESEARCH, explicit): do NOT add an injectable
     `classifier: Callable` parameter to the gate. It would be a second, untested production
     code path that bypasses the batching/parsing/fallback logic this test exists to prove. -->

<!-- Fixture loading without a database (test_verification_report_endpoint.py:41-53) -->
run = load_recorded_run(session=None, tenant_id=tenant_id)   # pure construction, no DB, no GCS
verdict_rows = run._fixture_verdict_rows
shape_verification_report(verdict_rows=…, funnel=run.verification_summary, claim_count=…,
                          cost_usd_total=Decimal(…), cost_pending=…)

<!-- CI-gate vs calibration boundary — ENFORCED, not just documented -->
                | test_gate_replay.py            | test_gate_calibration.py
Marker          | none (runs by default)         | @pytest.mark.live
LLM calls       | ZERO                           | real gemini-2.5-flash via a real AuditedLLMClient
Assertion       | exact funnel equality, hard fail | NONE — print()/-s report only
Run by          | every Cloud Build              | the operator, by hand, after 2026-08-01
Closes the phase| YES                            | NO
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The G-01 CI gate — deterministic answer-key replay through the real gates</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_gate_replay.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py (post plan 15.1-01: `load_selection_experiment()`, the 13-key `RECORDED_FUNNEL_COUNTS`, `build_verification_summary()`, `load_recorded_run()`)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/gates.py (post plan 15.1-05: the exact prompt line formats, `_parse_gate_lines`' allowed values, and `apply_gates`' return contract)
    - tribunal/nestor_pulse_sdk/tests/test_distiller_coverage.py lines 20-44 (the content-aware fake — the shape `_AnswerKeyGateAudited` copies)
    - tribunal/nestor_pulse_sdk/tests/test_verification_report_endpoint.py lines 41-61 (`_report_from_fixture`, the `session=None` pure-construction idiom)
    - 15.1-RESEARCH.md § Pattern 2 (the recommended seam, in full) and § Pitfall 1 (the 176-session trap)
  </read_first>
  <action>
Create `tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` — the test that closes this phase.
Standard Tribunal docstring: WHY (G-01 CI gate + G-04 ordering), the coverage list, the
Cloud Build invocation, and one sentence stating this file makes ZERO LLM calls.

Build `_AnswerKeyGateAudited` per the `<interfaces>` seam. Build the `text → id` map ONCE at
module scope from `load_selection_experiment()` (1,162 lookups per batch otherwise). Key the map
on the same 240-char truncation the gate applies, so the prompt text round-trips exactly; if two
claims truncate to the same key, resolve deterministically by first occurrence and assert in the
test that the collision count is small enough not to move the funnel (report it in the failure
message if it does). Do NOT reach into `gates.py` internals beyond its prompt line formats.

Tests:

1. `test_fixture_loads_the_recorded_population` — `load_selection_experiment()` returns 1,162
   claims, 1,162 classified entries and 456 strict entries; the strict id set is EXACTLY the set
   of KEEP ids; the classified reason tallies are `KEEP 456 / NOT_FALSIFIABLE 358 /
   NOT_LOAD_BEARING 320 / BOTH 28`; the strict tallies are `VERIFY 424 / SKIP_STABLE 32`.

2. `test_replay_reproduces_the_recorded_funnel` — **the phase gate.** Run
   `await apply_gates(claims=<the 1,162 fixture claims>, audited=_AnswerKeyGateAudited(),
   run_id=uuid4(), tenant_id=uuid4(), decision_context=<the blind selection rule from
   kept-claims-EN.md line 3>)` and assert the returned funnel's FIVE gate-computed numbers
   exactly: `distilled 1162`, `kept 456`, `dropped 706`, `selected_verify 424`,
   `skipped_stable 32`, plus `gate_errors 0` and the three DROP reasons
   `358 / 320 / 28`. Assert each against `RECORDED_FUNNEL_COUNTS[...]` rather than a literal, so
   the constant stays the single source of the recorded numbers. Add an explicit comment: the
   sixth constant, `verify_sessions`, is NOT asserted here — G-13, it is incident-derived.

3. `test_every_claim_carries_a_gate_decision` — all 1,162 claims have a `claim["gate"]` dict
   with a `decision`, a `reason`, and a `strict` that is `None` exactly when the decision is
   `DROP`. Nothing is unaccounted for.

4. `test_cluster_survives_if_any_member_survives` — the G-04 step-3 proof named in
   15.1-VALIDATION.md. Build small clusters from fixture claims mixing a `VERIFY` member with
   `DROP`/`SKIP_STABLE` members and assert the pipeline's `_group_selected` helper (imported
   from `pipeline.py`) returns True; an all-dropped cluster returns False.

5. `test_funnel_reaches_verification_summary` — the name 15.1-VALIDATION.md binds. Prove
   propagation without a database: assert the pipeline's result-carrier key
   (`"verification_summary"`) exists in the pipeline return contract and that the exact funnel
   dict the gates produced is what `shape_verification_report(funnel=<that dict>, …)` returns as
   `report["funnel"]`, key for key. Then assert the same for the fixture path:
   `load_recorded_run(session=None, …).verification_summary == RECORDED_FUNNEL_COUNTS`.

6. `test_report_accounting_matches_the_replayed_funnel` — feed the replayed funnel (plus a
   hand-set `checked` / `should_have_been_checked` pair) into `shape_verification_report` and
   assert `accounting.not_checkable.total == dropped + skipped_stable` and the three buckets sum
   to `distilled`.

7. `test_healthy_replay_reports_zero_bucket_three` — with every selected claim checked, the
   report says `verification_degraded is False` and `should_have_been_checked == 0`.

8. `test_injected_failure_replay_reports_a_non_zero_bucket_three` — subclass the fake so a slice
   of batches raises; assert the funnel carries `gate_errors > 0`, no claim was dropped by the
   failure, and (with a matching `unchecked_selected`) the report reads degraded and states it
   in words. This is the operator's headline test: a gutted run must not report green.

9. `test_replay_makes_no_live_llm_calls` — assert the fake recorded a call count equal to the
   expected batch count (`ceil(1162 / gate batch)` for gate 1 plus `ceil(456 / gate batch)` for
   gate 2) and that no real client class was instantiated.

Every assertion carries a consequence-naming message. Use `RECORDED_FUNNEL_COUNTS` for the
recorded numbers throughout; the only literal numbers allowed in this file are batch-count
arithmetic.
  </action>
  <acceptance_criteria>
    - `grep -c "def test_" tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` returns >= 9.
    - `grep -c "_AnswerKeyGateAudited" tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` returns >= 3.
    - `grep -c "verify_sessions" tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` returns 0 — G-13: the incident constant is never asserted by a gate test.
    - `grep -c "176" tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` returns 0.
    - `grep -c "classifier=" tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` returns 0 (no injectable-classifier seam was introduced).
    - `grep -c "pytest.mark.live\|AuditedLLMClient(" tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` returns 0.
    - `grep -c "docs/tribunal-run-reports" tribunal/nestor_pulse_sdk/tests/test_gate_replay.py` returns 0 (fixture resolves through `_report_dir()` only).
    - `python -m pytest nestor_pulse_sdk/tests/test_gate_replay.py -x -v` passes in Cloud Build with 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>
  <done>The recorded 1,162-claim fixture replays through the real gate machinery, reproduces the five gate-computed funnel numbers exactly, and proves they reach `run.verification_summary` and the verification report — with zero LLM calls.</done>
</task>

<task type="auto">
  <name>Task 2: The informational calibration test, marked live and self-skipping</name>
  <files>tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/test_outcomes_spike.py lines 495-520 (the only existing `@pytest.mark.live` usage — copy the marker + docstring convention only; its body is a `pytest.skip` placeholder)
    - tribunal/pyproject.toml lines 11-24 (the `live` marker is REGISTERED but NOT deselected by default; `--strict-markers` is on)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/gates.py and grouping.py (the real `apply_gates` and `group_claims` this test drives)
    - 15.1-CONTEXT.md G-01 (calibration half) and G-05
    - 15.1-VALIDATION.md § "CI-gate vs calibration boundary (G-01 — enforced, not just documented)"
  </read_first>
  <action>
Create `tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py`. Open with a header comment
block stating, in words: **this file contains NO pass/fail threshold. It reports numbers. It is
never a CI gate.** Then the standard docstring: WHY (G-01 informational + G-05), the run
command `pytest -m live nestor_pulse_sdk/tests/test_gate_calibration.py -s`, the requirement of
real provider credentials, the rough cost, and the fact that it is operator-run after
2026-08-01 (Anthropic monthly cap).

Belt and braces on the CI boundary — the marker alone is NOT enough, because `pyproject.toml`
only REGISTERS `live`, it does not deselect it:
  a) decorate each test `@pytest.mark.live`;
  b) as the FIRST statement in each test body, `pytest.skip(...)` unless an explicit opt-in env
     var is set (e.g. `NESTOR_GATE_CALIBRATION == "1"`), with a skip message naming G-01 and the
     2026-08-01 date. This mirrors the existing `test_outcomes_spike.py` placeholder shape and
     guarantees the full-suite build can never fire a provider call even if someone drops
     `-m "not live"`.

Tests (all report-only):

1. `test_gate_agreement_against_the_blind_answer_key` — run the REAL `apply_gates` with a real
   `AuditedLLMClient` over a sample of the fixture claims (sample size from an env var, default
   a few hundred, so a first run is cheap; document how to run the full 1,162). Compare each
   claim's gate decision to the recorded answer key and `print()` a table: overall agreement %,
   a KEEP/DROP confusion matrix, agreement per DROP reason, and the VERIFY/SKIP_STABLE agreement
   among the KEEPs. Print the 10 largest disagreements with the claim text so the operator can
   read them. **No assert on any percentage.** A bare `assert True` or no assert at all is the
   only acceptable ending.

2. `test_known_contradiction_pairs_cluster_together` — the G-05 measurement. Run the REAL
   `group_claims` over the fixture claims and report, per pair, whether the two members landed
   in the same cluster, for the four known shipped contradictions: Aral 16% vs 21% share;
   LUKOIL NL 46 vs ~70/75 stations; Zeeland "sold to Carlyle" vs "bought by TotalEnergies";
   Gunvor vs Carlyle. Locate each member by a text substring search over the fixture claims, and
   print the found ids so a miss is diagnosable rather than silent. **Report only — no assert on
   the outcome.** Print a clear "NOT FOUND IN FIXTURE" line if a substring does not match, so a
   fixture drift does not masquerade as a clustering failure.

Both tests print a final one-line summary prefixed with a stable marker (e.g.
`[15.1-CALIBRATION]`) so the operator can grep the run output. State in a closing comment that
these numbers feed 15.2's live validation (V-01/V-02/V-03) and nothing else.
  </action>
  <acceptance_criteria>
    - `grep -c "pytest.mark.live" tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py` returns >= 2 (one per test).
    - `grep -c "pytest.skip" tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py` returns >= 2 (the env-var opt-in guard).
    - **Threshold guard (the enforced G-01 boundary):** `grep -nE "assert[^\n]*(agreement|>=|<=|> *0\.|< *0\.|pct|percent)" tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py` returns NOTHING. Record the command and its empty output in the SUMMARY.
    - `grep -c "print(" tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py` returns >= 4.
    - `grep -c "2026-08-01" tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py` returns >= 1.
    - Running `python -m pytest nestor_pulse_sdk/tests/ -m "not live" --collect-only -q` collects the file but selects neither test; and a plain `python -m pytest nestor_pulse_sdk/tests/test_gate_calibration.py -v` (no marker filter) reports both tests SKIPPED, not executed — proven in the Cloud Build log.
  </acceptance_criteria>
  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>
  <done>The calibration test exists, is doubly fenced out of CI (marker + env-var skip), reports agreement and contradiction-clustering numbers via `print`, and contains no threshold assertion — grep-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture → CI assertion | The phase's entire proof rests on these numbers being parsed correctly |
| CI build → provider APIs | A live test escaping into CI costs money and, before 2026-08-01, hits a hard cap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-41 | Repudiation | a phase gate that passes for the wrong reason | mitigate | Assertions reference `RECORDED_FUNNEL_COUNTS` rather than literals; `verify_sessions` grep-proven absent (G-13); the fake's call count is asserted so a silently-skipped gate cannot pass |
| T-15.1-42 | Denial of Service (cost / cap) | the calibration test firing in CI | mitigate | Double fence: `@pytest.mark.live` plus a first-statement `pytest.skip` behind an explicit env var; `cloudbuild.test-gates.yaml` also passes `-m "not live"` |
| T-15.1-43 | Tampering | a threshold assertion creeping into the calibration file | mitigate | The grep-based threshold guard in Task 2's acceptance criteria, with its empty output recorded in the SUMMARY |
| T-15.1-44 | Tampering | a test-only injectable classifier creating an untested production path | mitigate | `grep -c "classifier="` returns 0; the fake satisfies the existing duck-typed `audited` seam, driving the real batching/parsing/fallback |
| T-15.1-45 | Information Disclosure | fixture claim text printed by the calibration report | accept | Superadmin/operator-run locally; the fixture is already committed to this repo |
| T-15.1-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase; `tribunal/requirements.txt` unmodified |
</threat_model>

<verification>
- `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml` green, with `test_gate_replay.py` passing and both calibration tests reported as skipped.
- `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml` green.
- The threshold-guard grep is run and its empty output pasted into the SUMMARY.
- `git diff --name-only` lists exactly the two test files.
</verification>

<success_criteria>
- The recorded fixture replays through the real gate machinery and reproduces the five gate-computed funnel numbers (SC1).
- Propagation to `run.verification_summary` and the verification report is asserted (SC1).
- Bucket 3 is 0 on the healthy replay and non-zero on the injected-failure replay (G-08/G-10).
- The calibration test is written, fenced out of CI two ways, and asserts no threshold (G-01/G-05).
</success_criteria>

<output>
Create `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-09-SUMMARY.md` when done
</output>
