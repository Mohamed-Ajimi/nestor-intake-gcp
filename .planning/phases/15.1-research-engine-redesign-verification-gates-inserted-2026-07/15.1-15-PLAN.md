---
phase: 15.1-research-engine-redesign-verification-gates
plan: 15
type: execute
wave: 9
depends_on: ["15.1-12", "15.1-13", "15.1-14", "15.1-16"]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - .planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md
autonomous: true
requirements: [ENGINE-10]

must_haves:
  truths:
    - "The full Tribunal suite runs in Cloud Build with NO NEW failures against the 418 passed / 38 skipped / 0 failed baseline"
    - "verify_chain still returns (True, None) and the frozen audit payload still has exactly 11 fields after a schema change and a new write path (SC3 non-regression)"
    - "Migration 0012 is APPLIED live in the tribunal schema, proven by a `Running upgrade 0011 -> 0012` log line rather than a silent no-op"
    - "Both Tribunal services run a new revision built from the gap-closure code, recorded with revision ids and digests"
    - "The frontend is rebuilt and deployed, so the superseded verdict section and its caveat are actually on the operator's screen and not merely in the payload"
    - "WR-01 and WR-10 are recorded as explicit Phase-15.2 deferrals so they cannot be silently lost"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "Phase 15.1 gap-closure deploy section (alembic 0012 + dual rebuild + frontend rebuild) and corrections to the now-false no-migration / no-frontend-rebuild statements"
      contains: "0012"
    - path: ".planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md"
      provides: "gap-closure phase gate, deploy record, closed Known Gaps, the 15.2 deferral list and the extended browser checklist"
      contains: "Deferred to Phase 15.2"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py"
      to: "the live tribunal schema"
      via: "the tribunal-migrate Cloud Run Job, repinned to the just-built image FIRST"
      pattern: "tribunal-migrate"
    - from: "the gap-closure code"
      to: "the next real Tribunal run"
      via: "a dual rebuild of tribunal-worker (writes verdicts) and tribunal-api (serves the report)"
      pattern: "deploy-worker.sh"
    - from: "plan 15.1-16's VerificationReport.tsx section"
      to: "the operator's browser"
      via: "the frontend image rebuild + Cloud Run deploy (Step 15.1.g)"
      pattern: "frontend/cloudbuild.yaml"
---

<objective>
Close the phase for real: prove the suite, prove the audit chain still holds after a schema change
and a new write path, apply the migration live, ship all three images, and write down what 15.2
inherits.

Purpose: 15.1's original closing plan (15.1-10) recorded "NO migration this phase" and a
no-migration, no-frontend deploy. The gap-closure pass invalidates BOTH. Migration `0012` now
exists in the TRIBUNAL alembic line and MUST be applied before the new writer can insert into
`verification_verdict.superseded_note`; and plan 15.1-16 changes `VerificationReport.tsx`, so the
frontend image must be rebuilt or the newly-populated `verdicts.superseded` section and its caveat
exist only in the JSON. This project also has a documented recurring failure — phases 6, 8, 10 and
11 were each "executed but not deployed" — and a documented image-pin failure where a migrate Job
left on a stale image connects, finds itself "at head" on the OLD revision set, exits 0 and logs
nothing. All three traps are live in this plan.

Two review warnings are explicitly NOT closed here. WR-01 (the coverage gate is unreachable dead
code) and WR-10 (verdicts attributed to gate-DROPped group members can break the one-bucket funnel
invariant) are deferred to Phase 15.2 by operator decision, because 15.2 restructures the pipeline
core and will be in these same lines. This plan records them; it writes no code for them.

**Wave numbering:** the gap-closure set CONTINUES phase numbering — plans 01-10 hold waves 1-6,
closure plans hold waves 7-9. This plan is wave 9 and runs last.

Output: the phase-gate results, the applied migration, the three deployed images, the runbook
section, and a 15.1-UAT.md whose Known Gaps section is finally accurate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-CONTEXT.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-VERIFICATION.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-REVIEW.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-11-SUMMARY.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-12-SUMMARY.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-13-SUMMARY.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-14-SUMMARY.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-16-SUMMARY.md

<interfaces>
<!-- ENVIRONMENT FACTS (hard constraints, not preferences) -->
* No Python and no Docker on this box. NOTHING is pip-installed, NO pytest runs locally.
  Every BACKEND test result in this plan comes from Cloud Build. `gcloud` IS available.
  Node/npm ARE available — plan 15.1-16 already gated the frontend locally (i18n audit, tsc, build).
* `GOOGLE_PROJECT` is UNSET. Every gcloud invocation must carry the literal project id
  `project-cb01b861-cb4a-438d-b9a` (export it once at the top of the session).
* The Anthropic account is at its MONTHLY cap until 2026-08-01. NO live LLM run, NO test that
  fires a provider call. The `live`-marked calibration test stays double-fenced
  (`@pytest.mark.live` + the env-gated inline `pytest.skip`); do not touch that fence.

<!-- The three existing gate configs (do not invent new ones) -->
tribunal/cloudbuild.test-gates.yaml     no-Postgres 15.1 gate      baseline 124 passed
tribunal/cloudbuild.test-critical.yaml  schema/lock/chain/report   baseline  33 passed
tribunal/cloudbuild.test.yaml           full suite                 baseline 418 passed / 38 skipped / 0 FAILED
Invocation:
  gcloud builds submit tribunal --config=tribunal/<config>.yaml --project="$GOOGLE_PROJECT"
THE PHASE GATE IS: no NEW failures against that baseline, plus verify_chain proven green.

<!-- The SC3 contract, asserted by tests/test_hash_chain_replay.py::test_chain_green_after_cost_migration -->
verify_chain() -> (True, None)
len(frozen_fields) == 11
cache_creation_tokens / cost_pending / verification_summary each ABSENT from _payload_for_row keys
=> superseded_note must NOT appear in _payload_for_row either. A schema change that added a
   frozen audit field would break the EU AI Act Art. 12 chain (hard deadline 2026-08-02).

<!-- The migrate-Job image-pin lesson, hit live 2026-07-22 on nestor-migrate and recorded in
     infra/DEPLOY-RUNBOOK.md § Phase 15 Step 15.b: the Job does NOT track the service image.
     Redeploying the services leaves the Job pinned to its OLD image; executing it then is a
     SILENT NO-OP (alembic connects, finds itself at head on the stale revision set, exits 0,
     logs no `Running upgrade` line). ALWAYS repin the Job to the just-built image FIRST. -->

<!-- FRONTEND deploy: frontend/cloudbuild.yaml is a docker build passing --build-arg VITE_* from
     --substitutions. The established procedure is § Phase 12 Step 12.3, reused verbatim by
     § Phase 15 Step 15.c and § Phase 17 Step 17.d. Constraints carried from those sections:
       * Reuse the SAME _API_BASE_URL + VITE_FIREBASE_* substitutions captured at Phase 12 Step
         12.3 — no URL re-wiring, no new env, no new secret.
       * NEVER pass VITE_SUPABASE_* — an in-image bundle guard FAILS the build if a Supabase
         signature leaks.
       * Deploy with --port 8080 (the Nitro node-server binds $PORT).
       * routeTree.gen.ts does NOT need regenerating — plan 15.1-16 adds no route, it edits an
         existing component that already mounts on admin.pulse.intakes.$id.
     Last recorded frontend revision: nestor-frontend-00025-4w8 (image 20260724-231312). -->

<!-- The previous deploy record (15.1-UAT.md § Deploy Record) is the format to match: image tag,
     build ids, registry digests, both revision ids, traffic percentages, digest-pin proof, and a
     post-deploy verification table. It currently states "Migrate Job executed? NO — correctly not
     run", "tribunal alembic head stays 0011" and "Frontend / nestor-api rebuilt? NO" — the first
     two and the frontend half of the third are now FALSE and must be superseded, not deleted. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the three Cloud Build gates and prove SC3 has not regressed</name>
  <files>.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md</files>
  <read_first>
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md` § Phase Gate and § Full-suite tallies (lines 15-95 — the exact table format, the recorded baselines of 124 / 33 / 418-38-0, and the NEW-vs-PRE-EXISTING failure comparison the closure gate must reproduce)
    - `tribunal/cloudbuild.test-gates.yaml` (as edited by plan 15.1-11 — confirm the three new test filenames are in the collected list, otherwise the closure tests are not actually being proven)
    - `tribunal/cloudbuild.test-critical.yaml` (the Postgres-provisioning gate that holds the hash-chain replay and the verification-report endpoint test)
    - `tribunal/cloudbuild.test.yaml` (the full suite, its `timeout: 3600s`, and the keyless-build caveat in its header — the gate is NO NEW failures, not absolute green)
    - `tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py` lines 755-860 (`test_chain_green_after_cost_migration` — the three SC3 assertions this task must see PASS, not skip)
    - `.planning/phases/13-*/13-PROOF-RESULTS.md` (the recorded list of pre-existing keyless-build failures the "no NEW failures" comparison is measured against)
    - The five gap-closure SUMMARYs (`15.1-11` … `15.1-14`, `15.1-16`) — so the gate results can be attributed to the changes that caused them
  </read_first>
  <action>
FIRST assert the worktree base and that all five dependency plans actually landed on disk:
`git merge-base --is-ancestor c4b4314 HEAD && echo BASE_OK`, then
`test -f tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py`,
`grep -q "INSERT INTO verification_verdict" tribunal/nestor_pulse_sdk/citations/extractor.py`,
`grep -q "unverified_from_accounting" tribunal/nestor_pulse_sdk/verification/report.py`,
`grep -q '"key": "gate"' tribunal/nestor_pulse_sdk/runs/stages.py`,
`grep -q "verdicts?.superseded" frontend/src/components/intake/VerificationReport.tsx`,
`test -f tribunal/nestor_pulse_sdk/tests/test_verdict_write_path.py`,
`test -f tribunal/nestor_pulse_sdk/tests/test_verdict_publication.py`,
`test -f tribunal/nestor_pulse_sdk/tests/test_stage_schema.py`.
If ANY fails, STOP and report the missing dependency rather than running a gate that would prove
nothing.

Export the project once — `GOOGLE_PROJECT` is unset on this box:
`export GOOGLE_PROJECT=project-cb01b861-cb4a-438d-b9a`.

Run the three gates in order, capturing each build id and its result:

  1. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"`
     — the fast no-Postgres gate. Baseline 124 passed. It must now collect and pass
     `test_verdict_publication.py`, `test_stage_schema.py` and `test_verdict_write_path.py`.
     Confirm in the log's "collecting:" echo that all three filenames appear — the config's
     `ls … || true` guard means a typo'd filename silently disappears instead of failing.
  2. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT"`
     — baseline 33 passed. This is the SC3 proof. Read the log for
     `test_chain_green_after_cost_migration` and confirm it is reported PASSED and not SKIPPED.
  3. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test.yaml --project="$GOOGLE_PROJECT"`
     — the full suite. Baseline 418 passed / 38 skipped / 0 failed.

Append a `## Gap-closure phase gate (plans 15.1-11 … 15.1-16)` section to `15.1-UAT.md`, matching
the existing § Phase Gate format, recording: each build id, its config, its pass/fail line, the
full-suite pass/fail/skip tallies, and a NEW-vs-PRE-EXISTING failure comparison table whose NEW
column must be empty. Also record, from plan 15.1-16's SUMMARY, the three frontend gate results
(`node scripts/i18n-audit.mjs`, `npx tsc --noEmit`, `npm run build`) — the frontend is proven
locally, not in Cloud Build, and the record should say so rather than leave a reader assuming a
build config covered it.

Under a `### SC3 after a schema change and a new write path` sub-heading, record the three
`test_hash_chain_replay` assertions verbatim (`verify_chain() == (True, None)`,
`len(frozen_fields) == 11`, the three additive columns absent from `_payload_for_row`) and add the
one this pass specifically needs: `superseded_note` is NOT a frozen payload field. Prove it with
`grep -c "superseded_note"` against wherever `_payload_for_row` lives (locate it with
`grep -rn "_payload_for_row" tribunal/nestor_pulse_sdk/ | head`) returning 0, and paste both the
command and its output.

**If any NEW failure appears, STOP.** Fix it in this task before Task 2. Do not deploy a red tree,
and do not proceed to a live migration on failing code.

Do not run any `live`-marked test and do not set `NESTOR_GATE_CALIBRATION` — the Anthropic monthly
cap is in force until 2026-08-01.
  </action>
  <acceptance_criteria>
    - All nine pre-flight assertions pass and are recorded in the SUMMARY.
    - `grep -c "Gap-closure phase gate" .planning/phases/15.1-*/15.1-UAT.md` returns 1.
    - The new section lists three build ids with their configs and outcomes, as pasted `gcloud` output.
    - The section records the fast gate's "collecting:" echo containing all three new test filenames.
    - The section records `test_chain_green_after_cost_migration` as PASSED, explicitly not SKIPPED.
    - The section contains a NEW-vs-PRE-EXISTING failure table whose NEW column is empty, measured against 418 passed / 38 skipped / 0 failed.
    - The section records the three frontend gate results and states explicitly that they ran locally via node/npm, not in Cloud Build.
    - The section contains the `_payload_for_row` grep command and its `0` output for `superseded_note`.
    - No `live`-marked test was run: `grep -c "NESTOR_GATE_CALIBRATION" .planning/phases/15.1-*/15.1-UAT.md` shows it only in the pre-existing deferred-UAT text, never as a command this task executed.
  </acceptance_criteria>
  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test.yaml --project=project-cb01b861-cb4a-438d-b9a</automated>
  </verify>
  <done>All three Cloud Build gates are recorded with build ids, the full suite shows no NEW failures against the 418/38/0 baseline, the three new test files are provably collected, the frontend gates are recorded as locally proven, and `verify_chain` is green with exactly 11 frozen payload fields after a schema change and a new write path.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration 0012 live and redeploy both Tribunal images plus the frontend</name>
  <files>infra/DEPLOY-RUNBOOK.md, .planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md</files>
  <read_first>
    - `infra/DEPLOY-RUNBOOK.md` § Phase 15.1 (around lines 1985-2140 — the preamble's "NO migration / NO Cloud Run env change / NO frontend rebuild / NO nestor-api rebuild" bullet list, and Steps 15.1.a through 15.1.d; the no-migration and no-frontend-rebuild claims are made FALSE by this pass and must be corrected in place rather than left as traps)
    - `infra/DEPLOY-RUNBOOK.md` § Phase 15 Step 15.b (lines 1818-1856 — the ONLY proven-live procedure for a TRIBUNAL-line migration: repin the `tribunal-migrate` Job to the just-built `tribunal-api:$SHA` FIRST, execute with `--wait`, then confirm the `Running upgrade` log line; plus the read-only schema confirmation block)
    - `infra/DEPLOY-RUNBOOK.md` § Phase 15 Step 15.c and § Phase 12 Step 12.3 (the established FRONTEND rebuild + deploy flow: `frontend/cloudbuild.yaml`, `--build-arg VITE_*` via `--substitutions`, the same `_API_BASE_URL` + Firebase values as Phase 12, the NO-`VITE_SUPABASE_*` bundle guard, `--port 8080`, and how the run.app URL is read back deterministically)
    - `infra/DEPLOY-RUNBOOK.md` § Phase 13 Step 13.f (the original `tribunal-migrate` Job definition, the app_user DSN, and the `worker_user` GRANT note)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md` § Deploy Record (lines 177-248 — the exact record format to match, the previous `$SHA` `20260725-220005`, both previous revision ids, the digest-pin proof block, and the now-false "Migrate Job executed? NO" / "head stays 0011" / "Frontend rebuilt? NO" rows)
    - `tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py` (the revision actually being applied)
    - `infra/DEPLOY-RUNBOOK.md` line 2161 — the ALREADY-EXISTING `### Step 15.1.e — Deferred operator UAT (batched, NOT run at deploy time)`, and its summary-checklist row at line 2249. The letter `e` is TAKEN. That is why this task's new sections are `f` and `g`, and why the acceptance criteria assert exactly one occurrence of each heading — two `Step 15.1.e` headings in one section would be an unresolvable cross-reference for the next operator.
    - `infra/DEPLOY-RUNBOOK.md` lines 2240-2255 (the § Phase 15.1 summary checklist rows `Step 15.1.a` … `Step 15.1.e` — the format the two new rows must match)
    - `.planning/STATE.md` § Blockers/Concerns (the carried IaC-DRIFT item: Terraform state was never adopted and `terraform apply` is FORBIDDEN on this project — the runbook is the source of truth)
  </read_first>
  <action>
Append a `### Step 15.1.f — Gap closure (plans 15.1-11 … 15.1-16): alembic 0012 + dual Tribunal
rebuild` and a `### Step 15.1.g — Gap closure: FRONTEND rebuild + deploy` section to
`infra/DEPLOY-RUNBOOK.md`'s § Phase 15.1, and correct the section's stale claims in place:

**Letter collision — read before writing a heading.** `### Step 15.1.e` ALREADY EXISTS at
`infra/DEPLOY-RUNBOOK.md:2161` ("Deferred operator UAT (batched, NOT run at deploy time)"), and it is
referenced again in the § Phase 15.1 summary checklist at line 2249. Do NOT reuse `e`, do NOT
renumber or rewrite the existing section, and do NOT fold the new content into it. The new sections
are `f` and `g`; leave `e` exactly as it is. Also add ONE summary-checklist row per new step to the
§ Phase 15.1 checklist (lines ~2240-2255), matching the existing `- [ ] Step 15.1.x — …` format,
placed after the existing `Step 15.1.e` row so the checklist reads in letter order.


  - Keep Step 15.1.a's original text (it was true for the original pass) but add a bold pointer note
    at its top stating that the 2026-07-25 gap-closure pass DOES land a tribunal-line migration and
    that Step 15.1.f supersedes the "do NOT run the migrate Job" instruction for that pass.
  - In the § Phase 15.1 preamble bullet list, annotate the "NO frontend rebuild" bullet: true for the
    original pass, SUPERSEDED by Step 15.1.g for the gap-closure pass, because plan 15.1-16 edits
    `VerificationReport.tsx` and three locale files.
  Leaving either instruction unqualified would be a trap for the next reader.

Step 15.1.f must state and then be EXECUTED, strictly in this order:

  1. **Order matters.** Build the two Tribunal images FIRST, then migrate, THEN deploy. The new
     column must exist before the new writer runs: if the code ships ahead of the migration, every
     verdict INSERT fails, Stage 7's `try/except` swallows it, and the run completes with zero
     verdicts — the exact hollow surface this pass exists to close. And the migrate Job runs the
     alembic revisions baked into an IMAGE, so the image carrying `0012` must exist before the Job
     can apply it.
  2. `export GOOGLE_PROJECT=project-cb01b861-cb4a-438d-b9a` and `export SHA="$(date +%Y%m%d-%H%M%S)"`.
     Take `REGION` exactly as the existing runbook sections do (do not invent a new value; the prior
     record shows `-ew` europe-west1 service URLs).
  3. Build BOTH Tribunal images at that ONE `$SHA` with `gcloud builds submit tribunal --config=…worker.yaml`
     and `…api.yaml`, `--substitutions=_IMAGE=…/nestor/tribunal-worker:${SHA}` (and `tribunal-api`),
     exactly as Step 15.1.c does. Both are required: the worker writes verdict rows, the api serves
     the shaper.
  4. **Repin `tribunal-migrate` to the just-built `tribunal-api:${SHA}` BEFORE executing it** —
     `gcloud run jobs update tribunal-migrate --region "$REGION" --project="$GOOGLE_PROJECT" --image …:${SHA}`
     — then `gcloud run jobs execute tribunal-migrate --region "$REGION" --project="$GOOGLE_PROJECT" --wait`.
     Quote the image-pin lesson verbatim from § Phase 15 Step 15.b: an unpinned Job is a SILENT
     NO-OP that exits 0 and logs no upgrade line.
  5. **Confirm the upgrade actually ran** by finding `Running upgrade 0011 -> 0012` in the Job
     execution log. Absence of that line means the Job ran on a stale image — repin and re-execute.
     Paste the log line into the record.
  6. Confirm the schema read-only, in the same style as Step 15.b's confirmation block:
     `\d tribunal.verification_verdict` shows `superseded_note text` nullable; ENABLE + FORCE ROW
     LEVEL SECURITY and the `verification_verdict_tenant_isolation` policy are STILL present and
     unchanged (the migration must not have dropped or recreated them); the tribunal-line alembic
     head is `0012`. State explicitly that this is the TRIBUNAL line and NOT the intake `nestor`
     line, whose 0011/0012 belong to Phases 16/17.
  7. Deploy worker first, then api, via the retargeted scripts with the tag pinned:
     `IMAGE_TAG="$SHA" tribunal/infrastructure/cloud-run/deploy-worker.sh` then `deploy-api.sh`.
     Never a hand-rolled `gcloud run deploy` for these two services — the scripts preserve the
     Phase-14 lockdown (tribunal-run SA, `--no-allow-unauthenticated`, invoker `nestor-run` only).
  8. Post-deploy verification for the two services: both `latestReadyRevisionName` values, both
     traffic percentages, the digest-pin proof (`gcloud run revisions describe …
     --format='value(spec.containers[0].image)'` resolving to an `@sha256:` digest, never
     `:latest`), the unchanged `tribunal-api` URL captured WITHOUT a path, and `/readyz` with an
     identity token. **NO live research run** — the Anthropic monthly cap resets 2026-08-01.
  9. State what is still NOT touched: no new secret, no Cloud Run env change (the gap-closure code
     adds no tunable), and no `nestor-api` rebuild (no intake-side surface changed).

Step 15.1.g — the frontend — must state and then be EXECUTED:

  - WHY it is required this pass: plan 15.1-16 edits `frontend/src/components/intake/VerificationReport.tsx`
    (a new section for `report.verdicts.superseded`, plus the `superseded_note` caveat fallback),
    `frontend/src/lib/api/research.ts` and the three `intake.json` locale files. Without a rebuild
    the newly-populated verdict class exists only in the JSON payload and the operator still cannot
    see a superseded verdict — which would leave SC2's "forced inline caveat" clause unmet on the
    operator surface even after every backend fix landed.
  - Reuse the § Phase 12 Step 12.3 / § Phase 15 Step 15.c flow verbatim: build
    `frontend/cloudbuild.yaml` with the SAME `_API_BASE_URL` and `VITE_FIREBASE_*` substitutions
    already captured, at the same `$SHA`; NEVER pass `VITE_SUPABASE_*` (the in-image bundle guard
    fails the build if a Supabase signature leaks); deploy with `--port 8080`.
  - State that `routeTree.gen.ts` does NOT need regenerating — no route was added; the component
    already mounts on the existing `admin.pulse.intakes.$id` anchor via `ResearchRunProgress`.
  - Record the new frontend revision id and its digest, and note the previous revision it supersedes
    (last recorded: `nestor-frontend-00025-4w8`, image `20260724-231312`).

Then execute both steps and append a `## Gap-closure deploy record` section to `15.1-UAT.md`,
matching the existing § Deploy Record format: the new `$SHA`, all three build ids, all three
registry digests, the migrate Job execution id AND its `Running upgrade 0011 -> 0012` line, all
three new revision ids, traffic percentages, the digest-pin proof, and a post-deploy verification
table. In that table, explicitly supersede the three now-false rows from the previous record —
"Migrate Job executed?" becomes **YES** with the execution id, the alembic head row becomes
`0012_verdict_superseded_note.py`, and the frontend row becomes **YES (rebuilt)** with its revision
id while `nestor-api` stays **NO**. Add a one-line pointer under the ORIGINAL § Deploy Record saying
those rows were true for the 2026-07-25 220005 deploy and are superseded by this section; do not
silently rewrite history.

If any step is blocked by credentials this executor does not hold, record the exact blocking command
and mark that row **PENDING** rather than claiming success. A false deploy record is the specific
failure mode this project has hit four times.
  </action>
  <acceptance_criteria>
    - `grep -c "^### Step 15.1.f" infra/DEPLOY-RUNBOOK.md` returns exactly 1 and `grep -c "^### Step 15.1.g" infra/DEPLOY-RUNBOOK.md` returns exactly 1 — one heading each, no duplicates.
    - `grep -c "^### Step 15.1.e" infra/DEPLOY-RUNBOOK.md` still returns exactly 1, and `grep -n "^### Step 15.1.e" infra/DEPLOY-RUNBOOK.md` still resolves to the pre-existing "Deferred operator UAT" section — the new content did NOT reuse or overwrite the taken letter.
    - `grep -c "^### Step 15.1." infra/DEPLOY-RUNBOOK.md` returns exactly 7 (a, b, c, d, e, f, g) — no letter is duplicated or skipped.
    - The § Phase 15.1 summary checklist gained exactly two rows: `grep -c "^- \[ \] Step 15.1.f" infra/DEPLOY-RUNBOOK.md` returns 1 and `grep -c "^- \[ \] Step 15.1.g" infra/DEPLOY-RUNBOOK.md` returns 1, both placed after the existing `Step 15.1.e` checklist row.
    - The new Step 15.1.f contains both `gcloud run jobs update tribunal-migrate` and `gcloud run jobs execute tribunal-migrate`, and the literal string `Running upgrade 0011 -> 0012` as the confirmation to look for.
    - Step 15.1.a carries a pointer note naming Step 15.1.f and stating that the gap-closure pass DOES land a migration.
    - The § Phase 15.1 preamble's "NO frontend rebuild" bullet carries a SUPERSEDED annotation naming Step 15.1.g.
    - `grep -c "deploy-worker.sh" infra/DEPLOY-RUNBOOK.md` increases by 1 (the new section pins the tag through the retargeted script).
    - Step 15.1.g names `frontend/cloudbuild.yaml`, states the NO-`VITE_SUPABASE_*` bundle guard, and states that `routeTree.gen.ts` needs no regeneration.
    - `grep -c "Gap-closure deploy record" .planning/phases/15.1-*/15.1-UAT.md` returns 1.
    - The record contains THREE new revision ids (tribunal-worker, tribunal-api, nestor-frontend), three `@sha256:` digests, and the migrate Job execution id — or an explicit PENDING row naming the exact blocking command.
    - The record states NO live research run was triggered and NO `nestor-api` rebuild was needed.
    - `gcloud run services describe tribunal-api --region="$REGION" --project=project-cb01b861-cb4a-438d-b9a --format='value(status.latestReadyRevisionName)'` output is pasted verbatim into the record.
    - The schema confirmation records that the `verification_verdict_tenant_isolation` policy and FORCE RLS are still present after the migration.
  </acceptance_criteria>
  <verify>
    <automated>gcloud run services describe tribunal-api --region="$REGION" --project=project-cb01b861-cb4a-438d-b9a --format='value(status.latestReadyRevisionName,status.traffic)'</automated>
  </verify>
  <done>Migration 0012 is applied live with a proven `Running upgrade` line and the RLS policy intact; both Tribunal services and the frontend run new digest-pinned revisions built from the gap-closure code; and the runbook documents the procedure while correcting both the no-migration and the no-frontend-rebuild instructions.</done>
</task>

<task type="auto">
  <name>Task 3: Close the Known Gaps, record the 15.2 deferrals, extend the browser checklist</name>
  <files>.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md</files>
  <read_first>
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md` § Known gaps carried out of Phase 15.1 (lines 251-296 — gap 1 `verification_degraded_text` renders nowhere, and gap 2 `superseded_note` is a declared contract that is not persisted, including its Option A / Option B storage question) and § Deferred Browser UAT (lines 298-470 — the eight-item checklist format and the standing operator direction banner)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-VERIFICATION.md` (the two SC2 gap entries and their `missing:` lists — each bullet must map to a closed item here)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-REVIEW.md` § WR-01 (lines 200-232) and § WR-10 (search for the WR-10 heading) — the two findings being deferred, so the deferral text carries their actual content and not just their ids
    - The five gap-closure SUMMARYs (`15.1-11` … `15.1-14`, `15.1-16`) — for the concrete evidence each closure claim cites
    - `.planning/ROADMAP.md` § Phase 15.2 (its stated scope: pipeline core restructure, R1-R7 reliability, D13/D14 report changes — the reason WR-01 and WR-10 belong there)
  </read_first>
  <action>
Three edits to `15.1-UAT.md`. Do not delete history — supersede it in place, with dates.

(1) **Known gaps — resolve gap 2, add the two gaps that were never disclosed.**
  - Under § Known gaps item 2 (`superseded_note` not persisted end-to-end), append a
    `**RESOLVED 2026-07-25 (gap-closure pass)**` block recording that Option A was chosen by the
    operator — a real `superseded_note TEXT` column via tribunal migration `0012` — and listing the
    four legs now closed: wired into `contested_notes` so synthesis presents the caveat as data
    (plan 15.1-13), emitted by `_verdict_dto` so it survives the API round trip (plan 15.1-12),
    persisted by `persist_tribunal_claims` (plans 15.1-11 + 15.1-14), and RENDERED in the amber
    inline caveat box (plan 15.1-16). Name the test that proves each.
  - Add a NEW numbered item `3. No production writer existed for verification_verdict` with the
    honest note that this was NOT disclosed in the original Known Gaps — it was surfaced by
    15.1-VERIFICATION.md — followed by its `**RESOLVED 2026-07-25**` block: the writer, the
    `claim_id` linkage, the dropped-claim verdicts with NULL `claim_id`, and plan 15.1-12's
    `verdicts_total == 0` fallback as the standing regression guard. State plainly that until this
    pass, `verdicts.{support,refute,insufficient,superseded}` was permanently `[]` on every real run
    and the report published `funnel.checked` beside `unverified == total_claims`.
  - Add a NEW numbered item `4. The superseded verdict class had no rendering site` — also NOT
    disclosed originally, found during gap-closure planning review. Record the trace honestly:
    `report.py`'s classing branch is `if/elif`, so a `superseded` verdict goes exclusively into
    `verdicts.superseded`; `VerificationReport.tsx` rendered `refute`/`support`/`insufficient`, the
    TOP-LEVEL `report.superseded` and `report.reconciled`, but never `verdicts.superseded`; the
    top-level list requires a `canonical` value (`report.py:302-305`), which a single-member
    superseded group does not have; and `VerdictItemRow` read its amber caveat only from
    `reconciliation.note`. Net: Phase 15.1's own new verdict class made a previously-visible row
    (it used to come back as `support`) invisible — latent only because no verdict rows existed.
    Then its `**RESOLVED 2026-07-25**` block: plan 15.1-16's dedicated section under a new i18n key
    in nl/en/fr plus the `superseded_note` caveat fallback, deployed via Step 15.1.g.
  - Leave item 1 (`verification_degraded_text` rendered by nothing) OPEN and unchanged — it is a
    separate frontend gap still owned by 15.2's R6 work. State explicitly that plan 15.1-16
    deliberately did NOT close it: it is a different key, a different block, and 15.2 is already
    opening that surface to promote the marker into a real terminal status.

(2) **Add a `## Deferred to Phase 15.2` section** — the record that keeps two live warnings from
being lost. One sub-heading each, and for each: the review id, the file and line, what the defect
actually is in plain language, why it is deferred (15.2 restructures the pipeline core and will be
in these same lines), and what it costs to leave open until then.
  - **WR-01 — the coverage gate is unreachable dead code.** `pipeline.py:570` seeds
    `verdicts_by_claim` with EVERY claim, so `adjudications` (lines 845-852) is `True`
    unconditionally, `check_coverage` never fails a claim, `coverage["pass"]` is always `True`, the
    re-entry `while` never executes and `reentry_count` is permanently 0. The documented last-chance
    recovery for a claim whose skeptic crashed does not exist. The funnel stays honest via a separate
    ground-truth reconciliation, so this does not break truth-telling — it removes a recovery
    mechanism the phase's own comments rely on. Record the review's one-line fix
    (`{id(c): True for c in claims if verdicts_by_claim.get(id(c))}`) so 15.2 does not re-derive it.
  - **WR-10 — verdicts attributed to gate-DROPped group members.** `pipeline.py:699-703` files a
    group's verdicts onto every member including ones the gates dropped, which can break the stated
    funnel invariant "every claim lands in exactly one bucket describing what happened to it", in the
    "did more than we said" direction. Record both options the review offers (skip dropped members,
    or add a `checked_incidentally` funnel line and subtract it from bucket 2).
  - Close the section with a line stating these are operator-approved deferrals from the 2026-07-25
    gap-closure scoping, that no code was written for them in this pass, and that they belong to the
    same 15.2 plans that touch `pipeline.py`'s verify stage.

(3) **Extend § Deferred Browser UAT** with new items for what the closure pass changed, in the
existing format (what to click, what must be true), keeping the standing "one combined Phase-15\*
session, after a real live run post-2026-08-01, no live-DB seeding" banner:
  - After a live run, the verification report's verdict sections (support / refute / insufficient /
    superseded) are POPULATED, and `counts.verdicts_total` is greater than zero. Before this pass
    they were empty on every real run.
  - The unverified block no longer reads `N of N`: `unverified.count` is materially less than
    `unverified.total_claims`, and it does not contradict the funnel's `checked` figure.
  - A `superseded` verdict, if the run produced one, appears under its OWN heading — distinct from
    the existing "Superseded / scoped findings" section — and shows its caveat text (what changed,
    from when) in the amber inline box, INCLUDING when the group carries no canonical value.
  - Switching the UI language to NL and FR shows a translated heading for that new section, not a
    raw key and not the English string.
  - The delivered report body carries the `[SUPERSEDED] …` caveat where the skeptic marked a claim
    superseded, i.e. the writing model presented pipeline data rather than phrasing from memory.
  - The D15 feed shows a `gate` stage row with a human label ("Verification gates") rather than a
    bare key, now that `ENGINE_STAGES` declares it.
  - The degradation sentence, when it fires, reports gate errors as CLAIMS and never as "gate
    batch(es)".
  - The client-facing appendix in the generated report says "Not independently fact-checked" and
    nowhere says "low-stakes supporting detail".
  Number these continuing from the existing eight so the checklist stays one ordered list.
  </action>
  <acceptance_criteria>
    - `grep -c "Deferred to Phase 15.2" .planning/phases/15.1-*/15.1-UAT.md` returns 1.
    - `grep -c "WR-01" .planning/phases/15.1-*/15.1-UAT.md` returns >= 1 and `grep -c "WR-10" .planning/phases/15.1-*/15.1-UAT.md` returns >= 1, both inside the deferral section.
    - `grep -c "RESOLVED 2026-07-25" .planning/phases/15.1-*/15.1-UAT.md` returns >= 3 (Known Gaps 2, 3 and 4).
    - The Known Gaps section contains a third numbered item naming the missing `verification_verdict` writer and a fourth naming the unrendered `verdicts.superseded` class, each stating it was not disclosed in the original record.
    - Known Gap 1 (`verification_degraded_text`) is still present, still marked open, and now states explicitly that plan 15.1-16 did not close it and why.
    - The Deferred Browser UAT list has at least 16 numbered items and each new one names an observable outcome.
    - At least one browser item checks the NL and FR heading of the new superseded-verdicts section.
    - `grep -c "no live-DB seeding\|do NOT seed" .planning/phases/15.1-*/15.1-UAT.md` returns >= 1 — the standing operator direction banner survived the edit.
    - `git diff --name-only` for this task lists only `15.1-UAT.md`.
  </acceptance_criteria>
  <verify>
    <human-check>The browser checklist items are executed in the combined Phase-15* UAT session against a live Tribunal run after 2026-08-01, per standing operator direction. This task's automated gate is the file's content, asserted above.</human-check>
    <automated>grep -q "Deferred to Phase 15.2" .planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md &amp;&amp; grep -q "WR-10" .planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md &amp;&amp; [ "$(grep -c 'RESOLVED 2026-07-25' .planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md)" -ge 3 ] &amp;&amp; echo GATE_OK</automated>
  </verify>
  <done>`15.1-UAT.md` records all three closed gaps with named evidence — including the two that were never disclosed originally — keeps the still-open frontend gap honest with a reason, carries WR-01 and WR-10 as explicit operator-approved 15.2 deferrals with their fixes recorded, and extends the deferred browser checklist with eight observable checks for what this pass changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built image → live Cloud Run services | Shipping the wrong or an unbuilt image puts unverified behaviour in front of the next real client run |
| migrate Job → live `tribunal` schema | DDL against live client data under the app_user DSN |
| deploy record → project memory | A false "deployed" claim is the failure mode this project has hit in phases 6, 8, 10 and 11 |
| deferral record → Phase 15.2 | An unrecorded warning is an abandoned warning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-76 | Repudiation | a migrate Job that silently no-ops on a stale image | mitigate | The Job is repinned to the just-built `tribunal-api:${SHA}` BEFORE execution, and the record must contain the `Running upgrade 0011 -> 0012` log line — absence of that line is defined as failure, not success |
| T-15.1-77 | Denial of Service | code deployed ahead of its migration | mitigate | Step 15.1.f fixes the order build → migrate → deploy, and states the failure mode explicitly: a missing column makes every verdict INSERT fail into Stage 7's swallow-and-continue guard, producing a zero-verdict run |
| T-15.1-78 | Elevation of Privilege | RLS lost during the schema change | mitigate | The post-migration confirmation asserts ENABLE + FORCE ROW LEVEL SECURITY and the `verification_verdict_tenant_isolation` policy are still present and unchanged |
| T-15.1-79 | Tampering | audit hash chain (EU AI Act Art. 12, deadline 2026-08-02) | mitigate | Task 1 records `verify_chain() == (True, None)` and exactly 11 frozen payload fields as PASSED, plus a grep proving `superseded_note` is not a `_payload_for_row` field |
| T-15.1-80 | Tampering | deploying `:latest` instead of the tag just built | mitigate | All three deploys pin the tag (`IMAGE_TAG=$SHA` through the retargeted scripts for Tribunal; the built tag for the frontend), and the record pastes the `@sha256:` digest each revision resolves to |
| T-15.1-81 | Repudiation | a false deploy or gate record | mitigate | Every row is pasted `gcloud` output; a blocked step is recorded PENDING with the exact failing command rather than claimed |
| T-15.1-82 | Denial of Service (cost) | a live research run or a live-marked test firing under the Anthropic monthly cap | mitigate | Post-deploy verification is `describe` + `/readyz` only; no run is triggered, `NESTOR_GATE_CALIBRATION` is never set, and the gate configs keep their `-m "not live"` fence |
| T-15.1-83 | Repudiation | two live warnings lost between phases | mitigate | WR-01 and WR-10 are written into a dedicated `## Deferred to Phase 15.2` section with their file/line, plain-language defect and recorded fix |
| T-15.1-90 | Information Disclosure | a Supabase credential signature leaking into the frontend bundle | mitigate | Step 15.1.g reuses the Phase-12 substitution set and explicitly forbids `VITE_SUPABASE_*`; the in-image bundle guard fails the build if a signature is present |
| T-15.1-91 | Repudiation | backend fixes deployed while the operator surface still cannot show them | mitigate | Step 15.1.g is mandatory this pass and its revision id is a required row in the deploy record; without it `verdicts.superseded` would remain invisible and SC2's caveat clause unmet on screen |
| T-15.1-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this pass; `tribunal/requirements.txt` and `frontend/package.json` unmodified |
</threat_model>

<verification>
- Three Cloud Build gates recorded with build ids; NEW-failure column empty against 418/38/0.
- `test_chain_green_after_cost_migration` recorded PASSED, not skipped; `superseded_note` proven absent from `_payload_for_row`.
- The three frontend gates (i18n audit, tsc, build) recorded, with their local-execution noted.
- `Running upgrade 0011 -> 0012` present in the migrate Job log; tribunal alembic head is `0012`; FORCE RLS + tenant policy still present on `verification_verdict`.
- Both Tribunal services AND the frontend on new digest-pinned revisions, with `latestReadyRevisionName` and traffic pasted.
- `15.1-UAT.md` carries the gap-closure phase gate, the gap-closure deploy record, three RESOLVED Known Gaps, one still-open Known Gap with its reason, a `## Deferred to Phase 15.2` section naming WR-01 and WR-10, and >= 16 browser checklist items.
- `git diff --name-only` lists only `infra/DEPLOY-RUNBOOK.md` and `15.1-UAT.md`.
</verification>

<success_criteria>
- The full suite is green against baseline with no NEW failures, proven in Cloud Build.
- SC3 has not regressed despite a schema change and a new production write path.
- Migration 0012 is live in the tribunal schema with its RLS policy intact, proven by an upgrade log line rather than an exit code.
- Both Tribunal services and the frontend run the gap-closure code, digest-pinned and recorded.
- The runbook no longer instructs a future reader that Phase 15.1 lands no migration and needs no frontend rebuild.
- WR-01 and WR-10 are carried forward as explicit, documented 15.2 deferrals, with no code written for them here.
</success_criteria>

<output>
Create `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-15-SUMMARY.md` when done
</output>
