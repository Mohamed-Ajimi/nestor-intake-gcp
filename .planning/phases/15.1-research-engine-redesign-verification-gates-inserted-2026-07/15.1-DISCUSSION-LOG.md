# Phase 15.1: Research Engine Redesign — Verification Gates - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-25
**Phase:** 15.1-Research Engine Redesign — Verification Gates (INSERTED 2026-07-24)
**Areas discussed:** Gate proof bar, Grouping depth, Superseded verdict, Dropped-706 accounting, Fail-loud, Corroboration signal

---

## Gate proof bar

The operator twice asked for plain-language clarification before answering. The mechanism
was explained (two yes/no sieves + a stability sieve, run as a cheap LLM classification pass
grounded in the client brief) and the tension named: the recorded 456/424 labels came from
blind LLM agents, so a production LLM gate cannot reproduce them exactly.

| Option | Description | Selected |
|--------|-------------|----------|
| Free CI test now + hand-run quality check later | Deterministic replay closes the phase; a separate calibration test reports agreement % against the blind answer key, run by hand after 2026-08-01, never a CI gate | ✓ |
| Machinery test only | Only the deterministic test; gate judgment first observed during the 15.2 live run | |
| Gate quality must be proven before closing | Phase stays open until the real classifier lands near 456/424 — blocks 15.1 until after 1 August | |

**User's choice:** Free CI test now + hand-run quality check later.
**Notes:** Keeps the phase closable before the Anthropic cap resets and unblocks 15.2, while
still producing an honest quality number in August that feeds the 15.2 validation.

---

## Old stakes filter

| Option | Description | Selected |
|--------|-------------|----------|
| Replace it as the selector, keep stakes for depth | Gates become the single answer to what gets checked; stakes keeps only the per-session search/fetch budget | ✓ |
| Both filters stack | Gates pick 424, stakes still waves low-stakes through — two compounding filters, funnel stops explaining reality | |
| Remove stakes entirely | Gates select, every check runs at equal depth — loses the depth cost lever | |

**User's choice:** Replace it as the selector, keep stakes for depth.
**Notes:** Removes the hidden second filter that made the funnel dishonest; the recorded
176-session shape still reproduces because that came from group-level depth, not selection.

---

## Grouping depth

Context given: 163 of 177 groups were singletons; near-miss labels across languages meant
same-topic claims never met; four flat contradictions shipped to the client.

| Option | Description | Selected |
|--------|-------------|----------|
| Replace bucketing with real AI clustering | Hand the AI the claim list, form groups of same-fact claims however worded — the mechanism already locked for 15.2 (B-04), so 15.2 inherits it | ✓ |
| Clean up the labels first | Canonicalize subject/property labels, keep bucketing — smaller change but the semantic work is unavoidable and 15.2 still needs real clustering | |
| Keep bucketing, add a contradiction hunt | Separate pass hunting contradicting pairs — catches contradictions only, 92%-singleton problem survives | |

**User's choice:** Replace bucketing with real AI clustering.
**Notes:** Claude corrected its own framing mid-area — the option text said "cluster the
surviving ~424", but D11 locks cluster-BEFORE-gate. Recorded in CONTEXT.md as G-04:
cluster 1,162 → gates per claim → cluster survives if any member does → prioritize by
corroboration. Corroboration is a cluster property, so clusters must exist first.

---

## Superseded verdict

Context given: the KPAnG case — six "support" verdicts on true-but-obsolete claims, with
"superseded since 1 April 2026" stranded in a free-text reconciliation note.

| Option | Description | Selected |
|--------|-------------|----------|
| Make it a fourth verdict word | support / refute / insufficient / superseded — the checker must pick one, so the nuance can't hide in a comment | ✓ |
| Keep three verdicts, add a structured date field | Less disruption, but "support" stays the headline word so counts and appendices still call obsolete claims verified | |
| Both: fourth verdict AND the date field | Most faithful; more work in the output form and two things to keep in sync | |

**User's choice:** Make it a fourth verdict word.

### Follow-up: what the report must do with it

| Option | Description | Selected |
|--------|-------------|----------|
| Keep it, force an inline caveat | Fact stays in the body with a pipeline-supplied caveat attached, plus the "Disputed & changed" section | ✓ |
| Pull it out of the body entirely | Zero chance of reading as current, but loses context and silently omits history | |
| Keep it, and also block it from carrying conclusions | Safest against bad advice; unenforceable in a writing model | |

**User's choice:** Keep it, force an inline caveat.
**Notes:** Caveat text comes from pipeline data — the writing model presents it, it does not
phrase it from memory.

---

## Dropped-706 accounting

Context given: today `unverified = total - with_verdict` lumps "never checkable" together
with "should have been checked but crashed" — the P1 defect.

| Option | Description | Selected |
|--------|-------------|----------|
| Three honest buckets, each claim carries its reason | CHECKED / NOT CHECKABLE (with reason) / SHOULD HAVE BEEN CHECKED BUT WASN'T — the third must never be silently empty | ✓ |
| Two buckets: checked vs not checked | Simpler, but the number that matters stays buried in 706 recommendations and boilerplate | |
| Three buckets, plus flag suspicious drops | Adds a gate-mistake review surface nobody asked for; calibration already measures gate quality | |

**User's choice:** Three honest buckets, each claim carries its reason.

### Follow-up: the client-facing appendix

The operator interrupted to ask **"report is based on claims only?"** — a correct challenge.
Claude verified in `pipeline.py` and reported back: **no**, the report is written from
scrubbed prose; claims are extracted probes, and only *refuted* claims trigger passage
removal. Consequences: the gates delete nothing from the report; no coverage percentage has
an honest denominator; and an unchecked claim leaves its passage standing untouched. The
question was re-posed with corrected options.

| Option | Description | Selected |
|--------|-------------|----------|
| Describe the process and what was removed | No percentages; state what was probed, removed, flagged, and left unexamined | |
| Removals only, no funnel numbers | Short and unmisreadable, but silent on what was not checked | |
| Nothing client-facing — superadmin only | Drop the coverage paragraph; all accounting stays superadmin-side | ✓ |

**User's choice:** Nothing client-facing — superadmin only.
**Notes:** Coherent with D-08 and the Phase-18 delivery model — the superadmin hand-crafts
the client PDF from raw output, so an auto-generated appendix would never reach the client
as written. The accounting exists to inform the operator. (This overrode Claude's
recommendation, on a sound reading of the delivery model.)

---

## Fail-loud

Context given: 776 cap rejections in 55 seconds gutted the checking stage and the run still
reported completed-green. Claude verified the live status vocabulary
(`queued|running|completed|failed|cancelled` — no parked, no degraded) and found four API
endpoints gating on `status == "completed"`.

| Option | Description | Selected |
|--------|-------------|----------|
| Keep the statuses, add a loud truth marker | Status stays completed; `verification_degraded` marker + non-zero unexamined count stated in words; 15.2's R6 promotes it to a real status | ✓ |
| Add completed_degraded status now | Honest at status level, but every completed-only gate must be updated or the superadmin loses access to exactly the runs worth inspecting; front-runs R6 | |
| Hard-fail a zero-verdict run | Unmissable, but `failed` locks the report and audit bundle, discarding a ~$45 run's good research | |

**User's choice:** Keep the statuses, add a loud truth marker.

### Follow-up: gate errors

| Option | Description | Selected |
|--------|-------------|----------|
| Fail toward MORE checking, recorded | Retry with backoff; on continued failure send those claims for checking anyway with a visible gate-error line; budget governor is the backstop | ✓ |
| Stop the gate stage, mark degraded | Cheaper and honest on screen, but turns a transient hiccup into permanently unexamined passages | |
| Abort the run on any gate error | Disproportionate — discards paid-for research because the cheapest step hiccuped | |

**User's choice:** Fail toward MORE checking, recorded.

---

## Corroboration signal

Before asking, Claude inspected `claim_distiller` and found two live bugs: provider
provenance is known (`units: (provider_name, chunk)`) but discarded, and `_dedupe_claims`
deletes cross-researcher duplicates (2,976 → 1,162) — destroying the very agreement signal
D9 wants. Merging instead of deleting yields the same 1,162 count, so the fixture still
reproduces.

| Option | Description | Selected |
|--------|-------------|----------|
| Stop throwing the signal away — build it now | Tag provenance; dedupe MERGES into `found_by: [...]`; corroboration count falls out for free; 15.2's fact lists only sharpen it | ✓ |
| Plumbing only, flat signal until 15.2 | Zero distiller risk, but ships untested and leaves the deletion bug another phase | |
| Defer corroboration wholly to 15.2 | Smallest phase, but drops a success criterion and leaves queue ordering arbitrary | |

**User's choice:** Stop throwing the signal away — build it now.

---

## Claude's Discretion

- Gate model + batching parameters (follow the `grouping.py` flash/plain-text/audited convention).
- How the gate prompt is grounded in the client brief / context pack.
- Storage shape for the three buckets and the gate-error line (additive keys only).
- Cluster size caps / blob-group guards; gate retry and backoff parameters.
- Calibration test sampling strategy and how it reports agreement.
- Whether the replay drives or asserts against `RECORDED_FUNNEL_COUNTS`.

## Deferred Ideas

- Superadmin review surface for low-confidence gate drops (rejected in G-08).
- Operator override to force-check a specific gated-out claim (new capability).
- `completed_degraded` run status (deliberately left to 15.2's R6).
- Blocking superseded claims from carrying conclusions (unenforceable in a writing model).
- Embedding-based clustering (Phase 19+ upgrade path; B-04 chose LLM-based).
- A client-facing verification/coverage statement (G-09 chose superadmin-only).
