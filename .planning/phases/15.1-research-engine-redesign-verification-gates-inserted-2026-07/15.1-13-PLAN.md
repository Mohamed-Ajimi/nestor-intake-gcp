---
phase: 15.1-research-engine-redesign-verification-gates
plan: 13
type: execute
wave: 7
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py
  - tribunal/nestor_pulse_sdk/runs/stages.py
  - tribunal/nestor_pulse_sdk/tests/test_superseded_verdict.py
  - tribunal/nestor_pulse_sdk/tests/test_stage_schema.py
  - tribunal/nestor_pulse_sdk/tests/test_verification_buckets.py
autonomous: true
requirements: [ENGINE-10]

must_haves:
  truths:
    - "A superseded verdict's caveat reaches synthesis as DATA in contested_notes, so the writing model presents it instead of phrasing it from memory (G-07)"
    - "Every stage key the pipeline writes to run.current_stage is declared in ENGINE_STAGES, so the API's stage schema can never omit a stage a run reports being in"
    - "The client-facing appendix no longer describes unverified claims as low-stakes supporting detail — a sentence G-02 made false"
    - "No 15.1 gate vocabulary leaks into the client-facing appendix: G-14's containment rule still holds"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py"
      provides: "superseded-caveat collection + contested_notes wiring + the corrected appendix line"
      contains: "SUPERSEDED"
    - path: "tribunal/nestor_pulse_sdk/runs/stages.py"
      provides: "the gate stage declared between distill and verify in ENGINE_STAGES['tribunal']"
      contains: "Verification gates"
    - path: "tribunal/nestor_pulse_sdk/tests/test_stage_schema.py"
      provides: "every set_stage key in the pipeline is declared in ENGINE_STAGES"
      min_lines: 50
  key_links:
    - from: "group_skeptic._parse_group_verdict superseded_note"
      to: "synthesize_report contested_notes"
      via: "_collect_superseded_notes -> superseded_notes -> contested_notes.extend"
      pattern: "contested_notes.extend"
    - from: "pipeline.py set_stage(..., 'gate', ...)"
      to: "runs/api.py RunMetrics.stages via stages_for(run.engine)"
      via: "ENGINE_STAGES['tribunal'] declaration"
      pattern: '"key": "gate"'
---

<objective>
Make the superseded caveat travel to the writing model, declare the `gate` stage the pipeline
already reports, and delete a client-facing sentence this phase made false.

Purpose: three findings from 15.1-REVIEW.md that the operator has scoped into gap closure.
CR-01 leg a — `superseded_note` lands in `verdicts_by_claim` and dies there; `contested_notes`,
the list `synthesize_report` actually receives, is built only from `group_reconciliations` and
`conflict_detector` output, so a claim the skeptic marked "true until 1 April 2026" ships in the
report body with no caveat anywhere. That is exactly the KPAnG failure G-06/G-07 exist to close.
WR-03 — `set_stage(..., "gate", ...)` writes `run.current_stage = 'gate'` while
`ENGINE_STAGES["tribunal"]` never declares it, so `RunMetrics.stages` ships a nine-stage schema
that omits the stage the run says it is in. WR-11 — the delivered appendix still calls unverified
claims "low-stakes supporting detail" although under G-02 stakes no longer selects anything.

**Wave numbering:** the gap-closure set CONTINUES phase numbering — plans 01-10 hold waves 1-6,
closure plans hold waves 7-9. This plan is wave 7 and depends on nothing. Plan 15.1-14 edits this
same `pipeline.py` in wave 8, AFTER this plan lands.

Output: a caveat that reaches synthesis, a complete stage schema, and an honest appendix line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-CONTEXT.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-VERIFICATION.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-REVIEW.md

<interfaces>
<!-- The verdict dict shape _flush_groups receives, from group_skeptic._parse_group_verdict:139-149 -->
{"verdict": "support|refute|insufficient|superseded",
 "confidence": float,
 "evidence_refs": list[str],
 "citations": list[str],
 "superseded_note": str}          # "" unless verdict == "superseded"

<!-- The group result _one_group_pass returns (group_skeptic.run_group_skeptic) -->
{"verdicts_by_index": {int: verdict_dict}, "reconciliation": {...}, "citations": [...]}

<!-- contested_notes today (pipeline.py:968-1001): built ONLY from group_reconciliations
     (scoped/disputed group notes) and conflict_detector output. It is the third positional
     input to synthesize_report (pipeline.py:1223-1230). superseded_note has zero occurrences
     in pipeline/synthesis/ — verified by grep in 15.1-VERIFICATION.md. -->

<!-- ENGINE_STAGES["tribunal"] today (runs/stages.py:38-49) — nine entries, no `gate`:
     intake, research_division, deep_research, distill, verify, adjudicate, coverage,
     conflict, synthesize. ENGINE_STAGES["sdk"] is the SAME list object (line 64 alias). -->

<!-- Consumer check already performed during planning, so no consumer edit is needed:
     * runs/api.py:846 builds RunMetrics.stages from stages_for(run.engine) — declarative.
     * frontend ResearchRunProgress.tsx:71 renders "whatever the run reports — a run with 10
       stages renders 10 blocks"; it hardcodes no stage count.
     * The intake backend (backend/app/research/*, backend/app/api/research_routes.py) contains
       NO hardcoded engine stage list — grepped, zero hits. Phase 16's progress bridge renders
       the stage list dynamically per the roadmap's "dynamic stage-list contract".
     Review IN-08 (jsonb key ordering puts the short key `gate` first in stage_detail) is a
     PRE-EXISTING ordering flaw and is NOT in this plan's scope. -->

<!-- The false appendix line (pipeline.py:1399):
       f"*   **Waved through unverified (low-stakes supporting detail):** {n_unverified}",
     n_unverified is now "claims with no verdict" = gate-dropped + skipped-stable + members of
     unselected clusters. Nothing to do with stakes. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry the superseded caveat into contested_notes (CR-01 leg a)</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py, tribunal/nestor_pulse_sdk/tests/test_superseded_verdict.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` lines 560-730 (the verify stage: the `verdicts_by_claim` seeding at 570, `group_reconciliations` at 573, `unchecked_ids`/`_book_unchecked` at 590-609, and `_flush_groups` at 687-713 where verdicts are unpacked from `verdicts_by_index` and the group's `reconciliation` is harvested)
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` lines 940-1010 (the `contested_notes` construction from `group_reconciliations`, the `[DISPUTED]` / `[scope-dependent]` tag convention to imitate, and the conflict-detector append at 996-1001)
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` lines 1215-1235 (the `synthesize_report(...)` call — confirm `contested_notes` is the list that actually reaches synthesis)
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/group_skeptic.py` lines 110-182 (`_parse_group_verdict` — where `superseded_note` is produced, that it is `""` for every non-superseded verdict, and `_insufficient_group`'s default shape)
    - `tribunal/nestor_pulse_sdk/tests/test_superseded_verdict.py` (the WHOLE file — its module docstring, its Cloud Build invocation header, and the existing parse-boundary tests; review IN-02 notes its line-141 membership assertion is weak, which is context for how thorough the new assertions must be)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-REVIEW.md` § CR-01 (lines 80-138 — including the fix sketch showing `[SUPERSEDED] <claim text>: <note>` appended alongside the existing recon handling)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-CONTEXT.md` § G-07 (the caveat is supplied from pipeline DATA; the writing model presents it, it does not phrase it from memory)
  </read_first>
  <action>
FIRST assert the worktree base: run `git merge-base --is-ancestor c4b4314 HEAD && echo BASE_OK`.
Executor worktrees here are historically cut from a stale ref — 8 of 10 agents in the original 15.1
ran 62-110 commits behind master. If BASE_OK does not print, STOP and report; every line number in
this plan is written against master at `c4b4314`.

Add a module-level pure helper to `pipeline.py`, next to the other module-level helpers such as
`_verification_appendix`:

  `_collect_superseded_notes(claims, verdicts_by_index) -> list[str]`

  - Iterate the group's claims by index; for each index with a verdict whose `verdict` is
    `"superseded"` and whose `superseded_note` is a non-empty string after `.strip()`, emit one
    line formatted `"[SUPERSEDED] {claim_text_first_120_chars}: {note}"`, imitating the existing
    `[DISPUTED]` / `[scope-dependent]` tag convention at pipeline.py:973-974.
  - Read claim text via `(c.get("text") or c.get("claim_text") or "")` — both shapes exist in this
    codebase (see `persist_tribunal_claims`).
  - Replace newlines in both the claim text and the note with spaces before truncating, so a single
    note can never inject extra lines into the synthesis prompt block.
  - Emit nothing for any other verdict value, for a missing index, or for an empty note. Never raise:
    a malformed verdict dict must yield no line rather than an exception, because this runs inside
    the verify stage's gather loop.

Make it a PURE function taking plain data, with no closure over pipeline state — that is what makes
it testable with no DB, no LLM and no pipeline run.

Then wire it:

  - Declare `superseded_notes: list[str] = []` next to `group_reconciliations: list[dict] = []`
    (pipeline.py:573) so the nested `_flush_groups` closes over it.
  - Inside `_flush_groups`, after the `verdicts_by_index` unpacking loop for a non-None result,
    extend `superseded_notes` with `_collect_superseded_notes(grp["claims"], vbi)`.
  - Add a module constant `_SUPERSEDED_NOTE_CAP = 40` and, where the notes are merged into
    `contested_notes` (immediately after the existing `for r in group_reconciliations:` loop around
    line 974), append de-duplicated notes up to that cap with
    `contested_notes.extend(...)`. If the cap truncates, emit a `log.warning` naming how many notes
    were dropped — the phase's fail-loud rule means a silently shortened caveat list is not
    acceptable. Add a comment stating the cap exists to bound the synthesis prompt, not to hide
    anything.
  - Do NOT change the `synthesize_report(...)` call signature; `contested_notes` is already its
    third input, which is exactly why this is the wiring point.
  - Do NOT touch the per-claim (non-group) verify branch: `superseded_note` is produced only by
    `group_skeptic._parse_group_verdict` (grep-confirmed — `superseded_note` appears only in
    `group_skeptic.py`, `tools.py`, `runs/schemas.py` and tests). Add a one-line comment recording
    that, so the omission reads as deliberate.

Then add tests to `tribunal/nestor_pulse_sdk/tests/test_superseded_verdict.py`:

  1. A superseded verdict with a note produces exactly one `[SUPERSEDED] ...: ...` line containing
     both the claim text prefix and the note text.
  2. `support` / `refute` / `insufficient` verdicts produce NO line even when a stray
     `superseded_note` is attached (mirrors the parser's own stray-note containment at
     `test_superseded_verdict.py:243-251`).
  3. A superseded verdict with an empty or whitespace-only note produces no line.
  4. A claim text and a note that both contain `\n` yield a single-line output — the prompt-block
     injection guard.
  5. A wiring assertion: `inspect.getsource(TribunalPipeline.run)` contains both
     `_collect_superseded_notes(` and `contested_notes.extend(`, with a docstring explaining that
     source inspection is the available wiring gate here because the dev box has no Python and no
     DB to drive a full pipeline run, and that CR-01's whole defect was a producer wired to nothing.
  </action>
  <acceptance_criteria>
    - `git merge-base --is-ancestor c4b4314 HEAD` exits 0.
    - `grep -c "_collect_superseded_notes" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns >= 3 (definition, call in `_flush_groups`, and at least one reference in a comment or the merge site).
    - `grep -c "contested_notes.extend" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns >= 1.
    - `grep -c "_SUPERSEDED_NOTE_CAP" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns >= 2.
    - `grep -c "\[SUPERSEDED\]" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns >= 1.
    - `grep -c "superseded_notes" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns >= 3 (declaration, extend inside `_flush_groups`, merge into `contested_notes`).
    - `grep -c "_collect_superseded_notes\|contested_notes.extend" tribunal/nestor_pulse_sdk/tests/test_superseded_verdict.py` returns >= 2.
    - `grep -c "^def test_\|^    def test_" tribunal/nestor_pulse_sdk/tests/test_superseded_verdict.py` is at least 5 greater than the pre-edit count (record both counts in the SUMMARY).
    - The verify-stage behaviour is otherwise unchanged: `grep -c "_book_unchecked" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` is unchanged from its pre-edit value.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "_collect_superseded_notes" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py &amp;&amp; grep -q "contested_notes.extend" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py &amp;&amp; grep -q "_collect_superseded_notes" tribunal/nestor_pulse_sdk/tests/test_superseded_verdict.py &amp;&amp; echo GATE_OK</automated>
  </verify>
  <done>A superseded verdict's caveat is formatted into a `[SUPERSEDED]` note, collected during group flushing and merged into the `contested_notes` list `synthesize_report` receives, with a bounded, logged cap and five tests pinning both the formatting rules and the wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the gate stage in ENGINE_STAGES and gate it against the pipeline (WR-03)</name>
  <files>tribunal/nestor_pulse_sdk/runs/stages.py, tribunal/nestor_pulse_sdk/tests/test_stage_schema.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/runs/stages.py` (the WHOLE file — the module docstring explaining that the ordered schema lives here and not in the DB, `ENGINE_STAGES` at 38-62, the `ENGINE_STAGES["sdk"] = ENGINE_STAGES["tribunal"]` alias at 64, `stages_for` at 67-69, and `set_stage`'s docstring rule at line 84 that `stage_key` must be "a key from ENGINE_STAGES[engine], or 'done'")
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` lines 500-570 and 930-945 (every `set_stage(...)` call site, including the `gate` writes at 536-562 the schema currently omits)
    - `tribunal/nestor_pulse_sdk/runs/api.py` around line 846 (`stages=stages_for(run.engine)` — the single consumer that ships the schema to the UI)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-REVIEW.md` § WR-03 (lines 257-279 — including the exact declaration the review specifies) and § IN-08 (lines 568-574 — the jsonb key-ordering flaw that is explicitly NOT in scope here)
    - `tribunal/cloudbuild.test-gates.yaml` (plan 15.1-11 pre-registered `test_stage_schema.py`; the filename must match exactly)
  </read_first>
  <action>
In `runs/stages.py`, insert `{"key": "gate", "label": "Verification gates"}` into
`ENGINE_STAGES["tribunal"]` BETWEEN the `distill` and `verify` entries, matching the surrounding
alignment style exactly. Nothing else in the file changes — the `sdk` alias picks it up
automatically because it is the same list object, and `stages_for` is already generic.

Add a short comment on the new entry naming WR-03 and stating that the pipeline has been writing
this key since Phase 15.1 (`pipeline.py:536-562`), so an undeclared stage was shipping in
`run.current_stage` while `RunMetrics.stages` omitted it.

Create `tribunal/nestor_pulse_sdk/tests/test_stage_schema.py` — filename exactly as pre-registered
in `cloudbuild.test-gates.yaml`. Module docstring in the house style: what it proves, that it is
pure (no DB, no network), and the `gcloud builds submit tribunal
--config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"` invocation line.

Tests:

  1. `test_gate_stage_is_declared_between_distill_and_verify` — the key list of
     `stages_for("tribunal")` contains `"gate"` exactly once, and its index is exactly one greater
     than `distill`'s and one less than `verify`'s.
  2. `test_every_stage_has_a_key_and_a_label` — every entry is a dict with non-empty `key` and
     `label`; keys are unique.
  3. `test_sdk_alias_shares_the_tribunal_schema` — `stages_for("sdk") == stages_for("tribunal")`.
  4. `test_every_set_stage_key_in_the_pipeline_is_declared` — the generalised gate that makes this
     class of defect impossible to reintroduce. Read
     `nestor_pulse_sdk/pipeline/tribunal/pipeline.py` as TEXT (resolve the path from
     `nestor_pulse_sdk.pipeline.tribunal.pipeline.__file__`, never a repo-root relative path — Cloud
     Build ships only the `tribunal/` subtree). Extract every stage key with a DOTALL regex over
     `set_stage\([^)]*?"([a-z_]+)"` and assert: the extracted set is non-empty and has at least 8
     distinct keys (a vacuity guard — an empty match set must fail, not pass), and every extracted
     key is either declared in `ENGINE_STAGES["tribunal"]` or is the literal `"done"`. The failure
     message must name the offending key and point at `runs/stages.py`.
  5. `test_unknown_engine_returns_empty_list` — `stages_for("nope") == []` (the documented
     fallback).

Do NOT attempt to fix review IN-08 (jsonb orders `stage_detail` keys by length so `gate` renders
first in the feed). It is a pre-existing ordering flaw in a different mechanism and is out of this
gap-closure scope.
  </action>
  <acceptance_criteria>
    - `grep -c '"key": "gate"' tribunal/nestor_pulse_sdk/runs/stages.py` returns 1.
    - `grep -c "Verification gates" tribunal/nestor_pulse_sdk/runs/stages.py` returns 1.
    - In `runs/stages.py`, the line number of `"key": "gate"` is greater than that of `"key": "distill"` and less than that of `"key": "verify"` — verify with `grep -n '"key": "distill"\|"key": "gate"\|"key": "verify"' tribunal/nestor_pulse_sdk/runs/stages.py` and record the three line numbers in the SUMMARY.
    - `grep -c '"key": "gate"' tribunal/nestor_pulse_sdk/runs/stages.py` returns 1 and not 2 — the `adk` schema must NOT gain the entry.
    - `tribunal/nestor_pulse_sdk/tests/test_stage_schema.py` exists, is >= 50 lines, and `grep -c "^def test_" ...` returns >= 5.
    - `grep -c "set_stage" tribunal/nestor_pulse_sdk/tests/test_stage_schema.py` returns >= 1 (the generalised gate exists).
    - `grep -c "__file__" tribunal/nestor_pulse_sdk/tests/test_stage_schema.py` returns >= 1 (the pipeline source is resolved through the package, not a repo-root path).
    - `grep -c "live" tribunal/nestor_pulse_sdk/tests/test_stage_schema.py` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"key": "gate"' tribunal/nestor_pulse_sdk/runs/stages.py &amp;&amp; test -f tribunal/nestor_pulse_sdk/tests/test_stage_schema.py &amp;&amp; [ "$(grep -c '^def test_' tribunal/nestor_pulse_sdk/tests/test_stage_schema.py)" -ge 5 ] &amp;&amp; echo GATE_OK</automated>
  </verify>
  <done>`ENGINE_STAGES["tribunal"]` declares `gate` between `distill` and `verify`, the `sdk` alias inherits it, and a source-driven test fails if the pipeline ever writes a stage key the schema does not declare.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the false "low-stakes supporting detail" appendix line (WR-11)</name>
  <files>tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py, tribunal/nestor_pulse_sdk/tests/test_verification_buckets.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` lines 1374-1440 (`_verification_appendix` in full — its honesty-contract docstring, the exact bullet formatting `*   **Label:** {value}`, the facet breakdown, the contested line and the re-entry line)
    - `tribunal/nestor_pulse_sdk/tests/test_verification_buckets.py` lines 250-315 (`test_client_appendix_unchanged` — the G-14 containment list of forbidden 15.1 vocabulary and the three positive-control assertions)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-REVIEW.md` § WR-11 (lines 490-517 — the exact replacement text the review specifies and its "no new 15.1 vocabulary" argument)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-CONTEXT.md` § G-14 and § G-09 (the operator ruling that scoped `_verification_appendix` out of 15.1, and the reason: the client never receives this generated report — only the hand-crafted PDF the superadmin submits)
  </read_first>
  <action>
In `_verification_appendix`, replace the line
`f"*   **Waved through unverified (low-stakes supporting detail):** {n_unverified}",`
with
`f"*   **Not independently fact-checked:** {n_unverified}",`.

Change nothing else in the function: same bullet prefix, same position in the `lines` list, same
`n_unverified` value, same surrounding lines.

Add a short comment above it recording: under G-02 stakes no longer selects what gets checked — the
gates do — so `n_unverified` is now "claims with no verdict" (gate-dropped + skipped-stable +
members of unselected clusters) and has nothing to do with stakes; the previous sentence was
factually wrong about its own engine (review WR-11). Also record that the operator reopened and
resolved this in the 2026-07-25 gap-closure scope: the sentence is CORRECTED without introducing
any 15.1 gate vocabulary, so G-14's containment rule stands unchanged.

Note for the executor, do not act on it: this text is emitted as literal English markdown into the
report body by server-side Python. It is not routed through the frontend i18n layer, so there are
no `nl`/`fr`/`en` keys to add and `frontend/scripts/i18n-audit.mjs` is not involved. Per D-08/G-09
the client receives only the hand-crafted PDF the superadmin writes.

Then update `test_client_appendix_unchanged` in `tests/test_verification_buckets.py`:

  - Keep the entire existing forbidden-vocabulary loop and all three positive controls unchanged —
    G-14 containment is still being enforced.
  - Add `assert "low-stakes" not in text` and `assert "Waved through" not in text`, each with a
    message naming WR-11 and stating that stakes no longer selects.
  - Add `assert "**Not independently fact-checked:** 20" in text` as the positive control for the
    replacement (the helper already passes `n_unverified=20`).
  - Extend the test docstring with a paragraph recording that the operator reopened this specific
    sentence in the 15.1 gap-closure pass, that the fix adds no 15.1 vocabulary, and that the
    containment rule itself is unchanged — so the next reader does not mistake this for G-14 being
    abandoned.
  </action>
  <acceptance_criteria>
    - `grep -c "low-stakes supporting detail" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns 0 outside comment lines — verify with `grep -v '^[[:space:]]*#' tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py | grep -c "low-stakes supporting detail"` returning 0.
    - `grep -c "Not independently fact-checked" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns 1.
    - `grep -c "Waved through" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` returns 0.
    - `grep -c "Independently fact-checked against the live web" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` still returns 1 (the untouched neighbouring line, proving a surgical edit).
    - `grep -c 'low-stakes" not in text\|"low-stakes" not in text' tribunal/nestor_pulse_sdk/tests/test_verification_buckets.py` returns >= 1.
    - `grep -c "Not independently fact-checked:\*\* 20" tribunal/nestor_pulse_sdk/tests/test_verification_buckets.py` returns 1.
    - `grep -c "SHOULD HAVE BEEN CHECKED" tribunal/nestor_pulse_sdk/tests/test_verification_buckets.py` returns >= 1 (the G-14 forbidden-vocabulary loop survived intact).
    - `grep -rn "low-stakes supporting detail" frontend/src/i18n/` returns nothing (confirming no i18n key mirrors this string).
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -v '^[[:space:]]*#' tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py | grep -c 'low-stakes supporting detail')" = "0" ] &amp;&amp; grep -q "Not independently fact-checked" tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py &amp;&amp; grep -q "Not independently fact-checked" tribunal/nestor_pulse_sdk/tests/test_verification_buckets.py &amp;&amp; echo GATE_OK</automated>
  </verify>
  <done>The appendix states "Not independently fact-checked" instead of a stakes-based claim G-02 falsified; the G-14 containment test still enforces its full forbidden-vocabulary list and now also fails if the false sentence ever returns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-generated `superseded_note` → synthesis prompt | Untrusted LLM output is concatenated into the prompt block a second model reads |
| `superseded_note` → delivered report body | The caveat text reaches a document a human then hand-crafts a client PDF from |
| `run.current_stage` → API stage schema → UI | An undeclared stage key renders as a raw string with no label |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-63 | Tampering | prompt injection via `superseded_note` into the synthesis prompt | mitigate | `_collect_superseded_notes` strips newlines from both the claim text and the note and truncates the claim text to 120 chars, so a note cannot open a new prompt line or impersonate another entry — the same self-contained control review IN-06 asks for on the gate prompts |
| T-15.1-64 | Denial of Service | an unbounded caveat list blowing the synthesis prompt budget | mitigate | `_SUPERSEDED_NOTE_CAP = 40` bounds the merged list, de-duplicated, with a `log.warning` when truncation occurs so the shortening is never silent |
| T-15.1-65 | Repudiation | a stage the run reports but the schema denies | mitigate | `gate` is declared in `ENGINE_STAGES`, and `test_every_set_stage_key_in_the_pipeline_is_declared` fails the build if any future `set_stage` key is undeclared |
| T-15.1-66 | Information Disclosure | 15.1 gate vocabulary leaking into the client-facing appendix | mitigate | The G-14 containment loop in `test_client_appendix_unchanged` is preserved verbatim; the replacement sentence contains no gate/bucket vocabulary and is asserted by an exact-string positive control |
| T-15.1-67 | Tampering | SC3 audit-chain regression | mitigate | No audit payload field, no `_payload_for_row` entry and no migration is touched by this plan; `verify_chain` green with exactly 11 frozen fields is re-proven in plan 15.1-15 |
| T-15.1-68 | Denial of Service (cost) | a CI-fired live LLM call under the Anthropic monthly cap | mitigate | Both new/edited test files are pure and carry no `live` marker; no provider client is constructed |
| T-15.1-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `tribunal/requirements.txt` unmodified |
</threat_model>

<verification>
- `_collect_superseded_notes` exists as a module-level pure helper, is called from `_flush_groups`, and its output reaches `contested_notes` via `contested_notes.extend`.
- `ENGINE_STAGES["tribunal"]` declares `gate` between `distill` and `verify`; the `adk` schema is unchanged.
- `tests/test_stage_schema.py` fails if any `set_stage` key in the pipeline is undeclared.
- No non-comment line of `pipeline.py` contains "low-stakes supporting detail" or "Waved through".
- `test_client_appendix_unchanged` still enforces the full G-14 forbidden-vocabulary list.
- `git diff --name-only` lists only the five files in `files_modified`.
</verification>

<success_criteria>
- The superseded caveat travels to synthesis as data, bounded and loudly truncated if capped.
- Every stage key the pipeline writes is declared, and a test keeps it that way.
- The delivered appendix no longer asserts a stakes-based selection rule that no longer exists.
- G-14's containment rule is intact and now also guards against the false sentence returning.
</success_criteria>

<output>
Create `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-13-SUMMARY.md` when done
</output>
