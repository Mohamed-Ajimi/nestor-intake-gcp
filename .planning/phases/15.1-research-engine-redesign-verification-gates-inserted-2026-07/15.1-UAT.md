# Phase 15.1 — Verification Gates: Phase Gate, Deploy Record & Deferred UAT

**Purpose:** the closing record for Phase 15.1. It holds (1) the three Cloud Build gates that
prove the phase, (2) the deploy record for the two Tribunal Cloud Run services, and (3) the
browser checks that CANNOT be run today and are therefore batched into the combined Phase-15\*
operator session.

**Environment note carried through every command below:** `GOOGLE_PROJECT` is **unset** on the
executing box, so every `gcloud` invocation in this file carries the literal project id
`project-cb01b861-cb4a-438d-b9a`. The dev box also has **no Python and no Docker** — every
verification in this phase ran in Cloud Build, never locally.

---

## Phase Gate

Three Cloud Build gates, run in order against the fully merged phase tree
(all nine prior plans present; base fast-forwarded to `master` at `8d249ea`).

| # | Build id | Config | Result |
|---|----------|--------|--------|
| 1 | `ac0d044e-82b5-439f-83e7-d932c139cc6c` | `tribunal/cloudbuild.test-gates.yaml` | **SUCCESS** — `124 passed, 2 deselected in 2.21s` (126 collected) |
| 2 | `e27da028-5192-466a-8a34-b972ff755692` | `tribunal/cloudbuild.test-critical.yaml` | **SUCCESS** — `33 passed, 5 warnings in 1.94s`, 0 failed |
| 3 | `fcae1e80-2c59-42d0-aedb-a307a432b7d8` | `tribunal/cloudbuild.test.yaml` (full suite) | **SUCCESS** — `418 passed, 38 skipped, 1 warning in 7.62s`, **0 failed** |

Invocations, verbatim:

```bash
gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml    --project=project-cb01b861-cb4a-438d-b9a
gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project=project-cb01b861-cb4a-438d-b9a
gcloud builds submit tribunal --config=tribunal/cloudbuild.test.yaml          --project=project-cb01b861-cb4a-438d-b9a
```

### Full-suite tallies — NEW vs PRE-EXISTING failures

The standing caveat in `cloudbuild.test.yaml`'s lineage (recorded in
`.planning/phases/13-tribunal-re-home-infra-baseline/13-PROOF-RESULTS.md`) is that some carried
tests need live provider API keys and were expected to fail in a keyless build — so the gate for
this phase is **NO NEW failures**, not absolute green. In practice the key-dependent tests now
resolve as **skips**, not failures, so the whole suite is green.

| Measure | Pre-existing baseline (build `99a0b5a2`, plan 15.1-09) | This gate (build `fcae1e80`) | New? |
|---|---|---|---|
| passed | 418 | 418 | — |
| skipped | 38 | 38 | — |
| **failed** | **0** | **0** | **NONE** |

| NEW failures | PRE-EXISTING failures |
|---|---|
| *(empty)* | *(empty — the 13-PROOF-RESULTS keyless-build failures now manifest as the 38 skips, not as failures)* |

**The NEW column is empty. The gate passes.**

The 38 skips include the two `@pytest.mark.live` calibration tests. Their double fence is visible
across gates 1 and 3: gate 1 runs with `-m "not live"` and reports them **deselected (2)**; gate 3
runs with **no marker filter at all** and their inline env-gated `pytest.skip` catches them. Neither
path fires a provider call — which matters, because the Anthropic account is at its monthly cap
until 2026-08-01.

### SC3 — `verify_chain` proven green, not inferred

`test_hash_chain_replay.py::test_chain_green_after_cost_migration` is recorded **PASSED** (not
skipped) in build `e27da028`, at 69% of that build's test line:

```
nestor_pulse_sdk/tests/test_hash_chain_replay.py::test_chain_green_after_cost_migration PASSED [ 69%]
```

Its three assertions are intact in source (`test_hash_chain_replay.py` lines 774-795):

- `verify_chain(run_id, session)` returns `(True, None)`;
- `assert len(frozen_fields) == 11` — `_payload_for_row` stays at exactly **11 frozen fields**;
- `cache_creation_tokens`, `cost_pending` and `verification_summary` are each asserted **absent**
  from the hashed payload — additive columns, never chain members.

This is the EU AI Act Art. 12 constraint (deadline 2026-08-02) discharged for this phase: no frozen
audit payload field was renamed and no field was added to the chain.

### No migration this phase

```
$ ls tribunal/nestor_pulse_sdk/alembic/versions/ | tail -1
0011_cost_verification.py
```

The tribunal alembic head stays at **0011**. `run.verification_summary` (JSONB) and
`verification_verdict.verdict` (free `TEXT`, **no CHECK constraint**) both already exist from that
migration, so the fourth verdict value and the funnel needed no schema change. **Do not run a
migrate Job for Phase 15.1** — see the runbook section.

---

## Success Criteria Trace

The three ROADMAP Phase 15.1 success criteria, each mapped to the named test that proves it.

### SC1 — the gate pipeline replays the recorded 1,162-claim fixture and reproduces the recorded keep/drop numbers

**Proven by:** `tribunal/nestor_pulse_sdk/tests/test_gate_replay.py::test_replay_reproduces_the_recorded_funnel`

The 1,162 fixture claims plus the recorded blind answer key are driven through the **real**
`gates.apply_gates()` — real 40-claim batching, real `asyncio.Semaphore` fan-out, real
`_parse_gate_lines` bounds checking, real funnel arithmetic. The only substitution is the model
itself (`_AnswerKeyGateAudited`, a duck-typed fake on the existing `audited` seam; `grep -c
"classifier=" gates.py` → **0**, so no test-only production path was created).

The five **gate-computed** funnel numbers:

| Funnel key | Value | Gate-computed? |
|---|---|---|
| `distilled` | 1162 | yes |
| `kept` | 456 | yes |
| `dropped` | 706 | yes |
| `selected_verify` | 424 | yes |
| `skipped_stable` | 32 | yes |
| ~~`verify_sessions`~~ | ~~176~~ | **NO — deliberately excluded, see below** |

**`verify_sessions: 176` is deliberately NOT asserted (G-13).** It counts the group-skeptic passes
that *returned* during the 2026-07-22 run, when a further 776 attempts were hard-400'd by the
Anthropic usage cap — it measures an outage, not the funnel. It remains in
`RECORDED_FUNNEL_COUNTS` unchanged (two pre-existing tests compare that dict by full equality) and
carries the inline marker `# incident-derived (G-13) -- NOT a gate assertion`. `grep -c
"verify_sessions"` in `test_gate_replay.py` returns **0**.

Supporting proofs in the same file: `test_fixture_loads_the_recorded_population` (population +
answer-key integrity, zero prompt-key collisions), `test_every_claim_carries_a_gate_decision`
(every claim's checked status is gate-attributable), `test_replay_makes_no_live_llm_calls` (exactly
42 fake calls — `ceil(1162/40) + ceil(456/40)`).

### SC2 — materiality + error-likelihood gates, canonical grouping, corroboration prioritization run in the verification stage, with fail-loud on gate errors and a "superseded" verdict available

| Sub-criterion | Proving test(s) |
|---|---|
| Materiality + error-likelihood gates run in the stage | `test_gate_selector.py::test_the_group_queue_is_built_from_the_gate_and_the_order_helper` · `test_gate_selector.py::test_triage_claims_is_not_called_from_the_verify_path` (G-02: the gates replaced stakes triage as the selector) |
| Canonical grouping (LLM clustering, G-03) | `test_tribunal_grouping.py` (9 added tests over `grouping._cluster_keys` / `_parse_cluster_lines` / `_assemble_groups`) |
| Cluster-before-gate ordering (G-04) | `test_gate_replay.py::test_cluster_survives_if_any_member_survives` — driven by REAL replayed claims |
| Corroboration prioritization (G-04 step 4 / D9) | `test_gate_selector.py` corroboration-ascending + tie-determinism cases, over `claim["found_by"]` from `test_claim_distiller.py::TestClaimProvenance` / `TestDedupeMerges` |
| Fail-loud on gate errors (G-11) | `test_gate_failure_modes.py::test_gate_batch_failure_sends_claims_to_checking` · `::test_usage_cap_400_is_not_retried` |
| Fail-loud on a gutted stage (G-10) | `test_gate_replay.py::test_injected_failure_replay_reports_a_non_zero_bucket_three` · `test_fail_loud.py::test_degraded_run_still_reports_completed_status` |
| Honest three-bucket accounting (G-08) | `test_verification_buckets.py::test_three_buckets_sum_to_distilled` · `::test_bucket_three_is_the_headline_number` · `::test_accounting_is_none_without_a_funnel` |
| Client surface untouched (G-09 / G-14) | `test_verification_buckets.py::test_client_appendix_unchanged` |
| `superseded` verdict available end-to-end | `test_superseded_verdict.py` (producer side: tool enum, `_parse_group_verdict`, adjudication survival) + `test_verification_buckets.py` boundary round-trip (`report["verdicts"]["superseded"]`) |
| The funnel reaches the persisted column | `test_gate_replay.py::test_funnel_reaches_verification_summary` · `::test_worker_persists_the_replayed_funnel_onto_the_run_row` |

### SC3 — `verify_chain` stays green, frozen payload fields unchanged

**Proven by:** `tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py::test_chain_green_after_cost_migration`
— recorded PASSED in build `e27da028` (see § Phase Gate above), with `len(frozen_fields) == 11`
and the three additive columns asserted absent from the hashed payload.

---

## G-01 boundary evidence — the calibration file carries no threshold

The CI-gate/calibration boundary is **enforced by a grep, not documented by a comment**. The guard
command and its output, run against the merged tree:

```
$ grep -nE "assert[^\n]*(agreement|>=|<=|> *0\.|< *0\.|pct|percent)" \
    tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py
$
```

**Empty.** Stronger than the criterion requires:

```
$ grep -c "assert" tribunal/nestor_pulse_sdk/tests/test_gate_calibration.py
0
```

The word `assert` does not appear in the file at all, so there is no half-written comparison for a
later edit to complete into a threshold. A green Phase 15.1 does not depend on a coin the model
flips — because the honest response to that kind of red build is to weaken the check.

---

## Deploy Record

**Deployed:** 2026-07-25 (UTC), immediately after the three phase gates above went green.
**Image tag (`$SHA`):** `20260725-220005` — one tag, both images.
**Runbook section executed:** `infra/DEPLOY-RUNBOOK.md` § Phase 15.1 Steps 15.1.a – 15.1.d.

This section exists because phases 6, 8, 10 and 11 in this project were each recorded as
"executed" while their code sat undeployed. Every row below is pasted `gcloud` output, not a claim.

### Images built

| Build id | Config | Image | Result |
|---|---|---|---|
| `8cf5b039-b25f-42a5-aa0d-81e3b7e0c3b1` | `tribunal/cloudbuild.worker.yaml` | `…/nestor/tribunal-worker:20260725-220005` | **SUCCESS** |
| `27fadc91-bf37-4b0f-a2c4-09e40f44920f` | `tribunal/cloudbuild.api.yaml` | `…/nestor/tribunal-api:20260725-220005` | **SUCCESS** |

Registry confirmation (`gcloud artifacts docker images list … --filter="tags:20260725-220005"`):

```
…/nestor/tribunal-api     20260725-220005  sha256:58af11edf1d6f9c73536a3be2f1a28aef16eb2239264cbd4e192784f3f021ed7
…/nestor/tribunal-worker  20260725-220005  sha256:67805d4c2f17a6f5882f9fc153de2e2a14d1c6b9b714476142674e4976f59439
```

### Services deployed

Deployed worker-first-then-api via the retargeted scripts
(`IMAGE_TAG=20260725-220005 tribunal/infrastructure/cloud-run/deploy-worker.sh`, then
`deploy-api.sh`), which pin the tag and preserve the Phase-14 lockdown.

| Service | New revision | Traffic | Previous revision (superseded) |
|---|---|---|---|
| `tribunal-worker` | `tribunal-worker-20260725-220005-220550` | **100%** | `tribunal-worker-20260724-214354-214808` |
| `tribunal-api` | `tribunal-api-20260725-220005-220647` | **100%** | `tribunal-api-20260724-214354-214930` |

`gcloud run services describe … --format='value(status.latestReadyRevisionName)'`:

```
tribunal-worker-20260725-220005-220550
tribunal-api-20260725-220005-220647
```

`gcloud run services describe … --format='value(status.traffic)'`:

```
{'latestRevision': True, 'percent': 100, 'revisionName': 'tribunal-api-20260725-220005-220647'}
{'latestRevision': True, 'percent': 100, 'revisionName': 'tribunal-worker-20260725-220005-220550'}
```

**Image-pin proven, not assumed.** Each new revision resolves to the exact digest built above —
`gcloud run revisions describe … --format='value(spec.containers[0].image)'`:

```
…/nestor/tribunal-worker@sha256:67805d4c2f17a6f5882f9fc153de2e2a14d1c6b9b714476142674e4976f59439
…/nestor/tribunal-api@sha256:58af11edf1d6f9c73536a3be2f1a28aef16eb2239264cbd4e192784f3f021ed7
```

Neither revision references a floating `:latest` tag.

### Post-deploy verification

| Check | Result |
|---|---|
| `tribunal-api` URL unchanged from Phase 14/15/16/17 | `https://tribunal-api-ybkr7metoq-ew.a.run.app` — **unchanged** (captured WITHOUT a path, Pitfall 4; the seam env was NOT re-set) |
| `/readyz` on the new `tribunal-api` revision (identity token, service stays `--no-allow-unauthenticated`) | **HTTP 200** |
| Migrate Job executed? | **NO — correctly not run.** No migration this phase; tribunal alembic head stays `0011_cost_verification.py` |
| Cloud Run env changed? | **NO.** All ten gate/cluster tunables read at import with production-safe defaults |
| New secret created? | **NO.** |
| Frontend / `nestor-api` rebuilt? | **NO.** No surface on either side changed this phase |
| Live research run triggered? | **NO — deliberately.** The Anthropic monthly cap resets 2026-08-01; the gate code is proven by the zero-LLM fixture replay above |

**Nothing is PENDING. The deploy completed in full.**

---

## Known gaps carried out of Phase 15.1

Recorded here rather than quietly closed, so 15.2 inherits them with their context.

### 1. `verification_degraded_text` is emitted by the backend but rendered by nothing

G-10 requires that a degraded run states its degradation **in words**, at the top of the
verification report and in the feed's closing summary. Half of that is live, half is not:

- **Live:** the D15 feed's closing verify row (`pipeline._verify_closing_item`) leads with the full
  sentence — `VERIFICATION DEGRADED — N of M selected claims were never checked …` — and uses
  `status="failed"`, which `ResearchRunProgress.tsx` already renders with a red `AlertTriangle`.
- **Live:** `should_have_been_checked` and `verification_degraded` are funnel keys, and
  `VerificationReport.tsx` renders the funnel via `Object.entries(report.funnel)` — so they DO
  appear in the report, as raw-key labels.
- **Not live:** the API also returns `report["verification_degraded_text"]` (the full sentence) and
  `report["accounting"]` (the three buckets), and **no frontend component reads either**
  (`grep -rn "verification_degraded\|accounting" frontend/src/` → no binding). So the operator sees
  the degradation as numbers in the funnel row, not as the sentence at the top of the report that
  G-10 asks for.

Closing this is a frontend change (`VerificationReport.tsx`), deliberately outside this plan's
two-file scope. It belongs with 15.2's R6 work, which is already opening this surface to promote
the marker into a real terminal status.

### 2. `superseded_note` is a declared contract that is not persisted end-to-end

Plan 15.1-03 taught the skeptic to emit `superseded_note` (the G-07 caveat: what changed and from
when) and carries it in the parsed pipeline dict. Plan 15.1-06 declared it on the
`VerificationVerdictItem` pydantic model so it can cross the API boundary. **But nothing writes it
to storage and `_verdict_dto` does not emit it** — `_verdict_dto` was deliberately left untouched
per threat T-15.1-25, and 15.1-03 deliberately added no `verification_verdict` column.

So today the field declares the contract without carrying data. Wiring it end-to-end needs a
**storage decision** that this phase had no mandate to make:

- **Option A** — a new `superseded_note TEXT` column on `verification_verdict` (a real migration,
  `0012` in the tribunal line).
- **Option B** — a sub-key on the existing `reconciliation` field (no migration, but it overloads a
  field that already carries free text and would need a parse contract).

This belongs to whoever owns the 15.2 migration — the same person promoting `verification_degraded`
into a terminal status under R6, since 15.2's D14 "Disputed & changed" section is the intended
consumer of `superseded_note`.

---
