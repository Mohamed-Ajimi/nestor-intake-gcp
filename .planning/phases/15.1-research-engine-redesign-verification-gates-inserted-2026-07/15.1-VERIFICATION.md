---
phase: 15.1-research-engine-redesign-verification-gates
verified: 2026-07-26T00:30:00Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/3 (SC1 verified, SC3 verified, SC2 failed on two blocking sub-clauses)
  gaps_closed:
    - "SC2 gap 1 — superseded_note was dead data (skeptic produced it, nothing consumed it, no DB column, _verdict_dto omitted it)"
    - "SC2 gap 2 — no production writer for verification_verdict existed, so verdicts.* was permanently [] and unverified.count == total_claims contradicted funnel.checked on every real run"
    - "Third defect found DURING gap closure — the superseded verdict class had no frontend rendering site (would have been silently activated by the gap-2 writer landing)"
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "verification_degraded_text / accounting rendered as a banner at the top of the verification report"
    addressed_in: "Phase 15.2 (R6)"
    evidence: "15.1-UAT.md Known Gap 1, explicitly left OPEN with owner 'Phase 15.2, R6'; the funnel numbers and the D15 feed's closing sentence already carry the degradation signal, so this is styling/promotion work, not a truth-telling gap"
  - truth: "Coverage-gate re-entry mechanism (WR-01) and verdicts-on-gate-DROPped-members (WR-10)"
    addressed_in: "Phase 15.2"
    evidence: "15.1-UAT.md § Deferred to Phase 15.2 records both with file/line, plain-language defect, and the fix already derived; explicitly 'No code was written for either in this pass.' Confirmed still present unchanged in pipeline.py (adjudications dict at pipeline.py:893-897 still seeds True unconditionally for every claim with any verdict entry)."
  - truth: "report_spec undeclared set_stage key"
    addressed_in: "Phase 15.2"
    evidence: "15.1-UAT.md records it as 'disclosed, deliberately not fixed' — allow-listed in test_stage_schema.py's _NON_SCHEMA_MARKERS with a source comment, confirmed present at test_stage_schema.py:38-47"
human_verification: []
---

# Phase 15.1: Research Engine Redesign — Verification Gates Verification Report

**Phase Goal:** The verification stage applies the redesigned gate package — materiality gate,
error-likelihood gate, canonical grouping, corroboration prioritization, fail-loud behavior, and
the "superseded" verdict — proven by replaying the recorded 1,162-claim fixture through the gates
in tests.

**Verified:** 2026-07-26
**Status:** passed
**Re-verification:** Yes — after gap closure (plans 15.1-11 … 15.1-16)

## Goal Achievement

### Observable Truths (Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1 — the gate pipeline replays the recorded 1,162-claim fixture in tests and reproduces the recorded keep/drop numbers | ✓ VERIFIED (no regression) | `test_gate_replay.py` last touched at commit `f751c67` (plan 15.1-09), untouched by any of the six gap-closure plans (`git diff --stat 0a2f00c HEAD -- .../test_gate_replay.py` → empty). Recorded PASSED in the gap-closure Cloud Build gate (`e6398d72`, 154 passed) and the full suite (`40a771dd`, 448 passed / 38 skipped / 0 failed vs the 418/38/0 baseline). |
| 2 | SC2 — materiality + error-likelihood gates, canonical grouping, and corroboration prioritization run in the verification stage, with fail-loud on gate errors and a "superseded" verdict available | ✓ VERIFIED | Gate/selection machinery unchanged and still green (see truth 1's build evidence). The publishing half — the part the prior verification failed — is now traced end-to-end directly against source in this pass; see the two closed sub-gaps below and the Key Link Verification table. |
| 2a | SC2 sub-clause — `superseded_note` reaches the report with a forced inline caveat (G-06/G-07), not just as dead data | ✓ VERIFIED | Full chain read directly from source: `group_skeptic._parse_group_verdict` (`group_skeptic.py:137-149`) produces `superseded_note` on the verdict dict → `pipeline.py:746` (`_flush_groups`) calls `_collect_superseded_notes(...)` and extends `superseded_notes` → `pipeline.py:1026-1035` dedupes and extends `contested_notes` (the exact list `synthesize_report` receives) with a `log.warning` on cap truncation → `verification_verdict.superseded_note` is a real nullable TEXT column, confirmed live via migration `0012`'s Cloud SQL schema probe (`col superseded_note text nullable=YES`, `tribunal_head = 0012`) → `extractor.py::_insert_verdict` (lines 249-272) writes it verbatim, `""` → `NULL` → `report.py::_verdict_dto` (line 87) emits `getattr(row, "superseded_note", None)` as the DTO's sixth key → `runs/schemas.py:237` declares it on `VerificationVerdictItem` → `frontend/src/lib/api/research.ts:100` types it → `VerificationReport.tsx:93-97` derives `note = reconNote \|\| supersededNote` and renders it in the amber caveat box even when the group carries no `canonical` (the exact hole G-07 needed closed). |
| 2b | SC2 sub-clause — the "superseded" verdict (and every verdict) is actually available/observable on a real run, not only in the test fixture | ✓ VERIFIED (code path real, tested, deployed; first live-run observation still pending the Anthropic cap reset) | `persist_tribunal_claims` (`extractor.py:396-576`) now calls `_insert_verdict` twice: once per survivor (linked to the `claim_id` inserted moments earlier in the same transaction, `extractor.py:474-492`) and once per dropped/refuted claim via the `dropped_claims` loop (`extractor.py:552-560`, `claim_id=None`). Stage 7 in `pipeline.py:1109-1116` passes `dropped_claims=dropped` — confirmed `dropped` already accumulates both adjudication losers (`pipeline.py:881`) and conflict losers (`pipeline.py:1074`) before that call site. `_flush_groups` (`pipeline.py:722-742`) attaches `v["reconciliation"] = dict(recon)` to member verdicts when the reconciliation is meaningful, which `report.py:326-340` reads to build the `reconciled`/`superseded` (top-level) sections. All 11 `test_verdict_write_path.py` tests recorded PASSED in Cloud Build `e6398d72` (confirmed by name in `15.1-UAT.md`, including the two write-order and NULL-claim_id assertions). Migration `0012` applied live with the required `Running upgrade 0011 -> 0012` log line, and all three services (`tribunal-worker-20260725-233634-015424`, `tribunal-api-20260725-233634-015525`, `nestor-frontend-00026-9s6`) are deployed at digest-pinned revisions built from the same tree this verification read. **Caveat, honestly carried forward, not a gap:** no live Tribunal run has executed against this code yet (Anthropic monthly cap blocks live runs until 2026-08-01), so `verdicts.*` populated with real production rows has not been visually observed — this is exactly what browser-UAT items 9-16 in `15.1-UAT.md` are for, correctly batched into the standing combined Phase-15* session per explicit disclosed operator direction, not a new or hidden gap. |
| 3 | SC3 — `verify_chain` stays green, frozen payload fields unchanged | ✓ VERIFIED (re-confirmed after a schema change and a new write path landed) | `tribunal/nestor_pulse_sdk/audit/hash_chain.py::_payload_for_row` (lines 149-161) read directly: exactly 11 keys (`provider`, `model`, `started_at`, `duration_ms`, `prompt_tokens`, `completion_tokens`, `cached_tokens`, `gcs_uri`, `seq`, `tenant_id`, `run_id`) — `superseded_note` is absent (`grep -c superseded_note hash_chain.py` → 0). `test_hash_chain_replay.py` untouched by any gap-closure plan (`git diff --stat 0a2f00c HEAD` → empty) and recorded PASSED (not skipped) in Cloud Build `a3f14549`. The new column lives on `verification_verdict`, never a hash-chain member; the new write path (`_insert_verdict`) touches no `audit_log` row. |

**Score:** 3/3 truths verified (5/5 counting the two SC2 sub-clauses individually).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py` | Additive migration adding `superseded_note` column | ✓ VERIFIED | One `op.add_column`, no security DDL, `down_revision = "0011"`. Applied live — confirmed via migrate Job log line and live schema probe (`tribunal_head = 0012`). |
| `tribunal/nestor_pulse_sdk/db/models/verification_verdict.py` | ORM mirror of the new column | ✓ VERIFIED | `superseded_note: Mapped[str \| None]` present, docstring updated, mirrors 0011+0012 exactly. |
| `tribunal/nestor_pulse_sdk/pipeline/tribunal/group_skeptic.py` | Produces `superseded_note` | ✓ VERIFIED (unchanged from prior pass) | Line 146-148, confirmed present. |
| `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` | Wires `superseded_note` into `contested_notes`; writes `dropped_claims` into the persist call; attaches group reconciliation to member verdicts | ✓ VERIFIED | `_collect_superseded_notes` (line 746), `contested_notes.extend` (line 1035), `dropped_claims=dropped` (line 1111), `v["reconciliation"] = dict(recon)` (line 741) — all present and read directly. |
| `tribunal/nestor_pulse_sdk/citations/extractor.py` | Production writer for `verification_verdict` | ✓ VERIFIED | `_insert_verdict` (lines 181-274) + two call sites inside `persist_tribunal_claims` (survivors line 485, dropped line 555-560). Raw SQL, bound params, RLS-governed (`set_tenant_context` at line 448 precedes both). |
| `tribunal/nestor_pulse_sdk/verification/report.py` | `_verdict_dto` emits `superseded_note`; `verdicts_total == 0` funnel-grounded fallback | ✓ VERIFIED | Line 87 (`_verdict_dto`), lines 385-402 (fallback, ANDed on `verdicts_total == 0 and accounting is not None`). |
| `tribunal/nestor_pulse_sdk/runs/schemas.py` | Wire contract declares the new fields | ✓ VERIFIED | `superseded_note` (line 237), `unverified_from_accounting` (line 328), `unverified_note` (line 329). |
| `tribunal/nestor_pulse_sdk/runs/stages.py` | `gate` stage declared in `ENGINE_STAGES` | ✓ VERIFIED | Line 49, between `distill` and `verify`. |
| `frontend/src/lib/api/research.ts` | Types `superseded_note` and `verdicts.superseded` | ✓ VERIFIED | Lines 100 and 117. |
| `frontend/src/components/intake/VerificationReport.tsx` | Renders the `superseded` verdict class + caveat fallback | ✓ VERIFIED | `VerdictSection` for `report.verdicts?.superseded` at line 317-322 (between `insufficient` at 305 and the pre-existing top-level `superseded` at 328, per plan's own line-order acceptance criterion); `note = reconNote \|\| supersededNote` at line 97. |
| `frontend/src/locales/{nl,en,fr}/intake.json` | New i18n key, distinct from `supersededTitle` | ✓ VERIFIED | `supersededVerdictsTitle` present in all three, each with a distinct translation; `supersededTitle` untouched. |
| `tribunal/nestor_pulse_sdk/tests/test_verdict_write_path.py` | 11 no-DB write-path proofs | ✓ VERIFIED | File exists, 11 `test_` functions confirmed by name, pre-registered in `cloudbuild.test-gates.yaml` (confirmed by grep) and recorded as the 13th collected file in the gap-closure gate's `collecting:` echo. |
| `tribunal/nestor_pulse_sdk/tests/test_verdict_publication.py` | 8 no-DB publication proofs | ✓ VERIFIED | Present, registered in the gate config. |
| `tribunal/nestor_pulse_sdk/tests/test_stage_schema.py` | Source-driven gate against undeclared `set_stage` keys | ✓ VERIFIED | Present, registered; `_NON_SCHEMA_MARKERS = {"done", "report_spec"}` confirmed at lines 38-47. |
| `tribunal/nestor_pulse_sdk/audit/hash_chain.py` | Frozen payload unchanged (SC3) | ✓ VERIFIED | 11 fields, `superseded_note` absent (0 hits). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `group_skeptic._parse_group_verdict` (`superseded_note`) | `pipeline.py` `contested_notes` | `_flush_groups` → `superseded_notes.extend(_collect_superseded_notes(...))` → dedupe → `contested_notes.extend(...)` | ✓ WIRED | Was `NOT_WIRED` in the prior pass (zero hits). Now three touch points confirmed by direct read at `pipeline.py:746` and `1026-1035`. |
| `verdicts_by_claim` (in-memory) | `verification_verdict` table (persisted) | `persist_tribunal_claims` → `_insert_verdict` (two call sites, survivors + dropped) | ✓ WIRED | Was `NOT_WIRED` in the prior pass (zero `VerificationVerdict(` constructor calls outside tests). Now confirmed: two live call sites, RLS-governed insert, called from Stage 7 on every Tribunal run. |
| Group `reconciliation` | member `verdicts_by_claim` entries | `_flush_groups`'s `v["reconciliation"] = dict(recon)`, gated on `_recon_is_meaningful` | ✓ WIRED | Confirmed at `pipeline.py:722-742`; feeds `report.py`'s `reconciled`/`superseded` (top-level) sections at lines 326-340. |
| `superseded_note` column | `_verdict_dto` → API response | `getattr(row, "superseded_note", None)` | ✓ WIRED | Confirmed at `report.py:87`; was hard-omitted (5-key DTO) in the prior pass. |
| `report.verdicts.superseded` / `superseded_note` | `VerificationReport.tsx` | `research.ts` types + `VerdictSection` render site + `reconNote \|\| supersededNote` fallback | ✓ WIRED | Was `NOT_WIRED` (undiscovered until gap-closure planning) — no type, no render site existed at the time of the prior verification. Now confirmed present and deployed (`nestor-frontend-00026-9s6`). |
| `run.verification_summary` funnel | `verification/report.py::_accounting` | funnel dict passed to `shape_verification_report` | ✓ WIRED (unchanged) | Still funnel-driven, still honest. |
| `verdicts_total == 0` fallback | `unverified.count` | `accounting["checked"]`-derived clamp, ANDed with `accounting is not None` | ✓ WIRED | Confirmed at `report.py:389-402`; structurally prevents `funnel.checked` and `unverified.count == total_claims` from co-existing — either verdict rows exist (row-derived arithmetic) or they don't (funnel-derived arithmetic that agrees with `accounting` by construction). |
| `report["verification_degraded_text"]` | `VerificationReport.tsx` | expected read | ✗ NOT_WIRED (unchanged, disclosed, deferred) | Confirmed still no frontend binding (`grep -rn "verification_degraded" frontend/src/` finds no component read). Explicitly deferred to Phase 15.2 R6 in `15.1-UAT.md`; not a regression from this pass — plan 15.1-16 deliberately scoped this out. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `VerificationReport.tsx` funnel block | `report.funnel` | `run.verification_summary` (JSONB, written every real run) | Yes | ✓ FLOWING |
| `VerificationReport.tsx` unverified block | `report.unverified.count` | Row-derived when verdict rows exist; funnel-derived fallback otherwise (`report.py:385-402`) | Yes, by construction in both branches | ✓ FLOWING (structural fix; not yet observed with live-run rows, see truth 2b) |
| `VerificationReport.tsx` verdict sections (support/refute/insufficient/superseded) | `report.verdicts.*` | `VerificationVerdict` rows written by `_insert_verdict`, called from `persist_tribunal_claims` on every Tribunal run | Yes, code path confirmed real and deployed | ⚠ WRITER REAL BUT UNOBSERVED — the write path is genuine production code (not a stub, not a fixture-only path) and is deployed at the revision digests recorded in the deploy record; no live Tribunal run has executed against it yet because of the Anthropic monthly cap. This is a disclosed, scheduled UAT item (§ items 9-11), not a code-level gap. |
| `VerdictSection` (superseded) | `report.verdicts?.superseded` | Same `VerificationVerdict` query, filtered by `verdict == "superseded"` classing branch (`report.py:305-322`) | Yes, code path confirmed real | Same caveat as above |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ENGINE-10 | 15.1-01 … 15.1-16 (all declare it) | Verification gates: materiality, error-likelihood, canonical grouping, corroboration prioritization, fail-loud, "superseded" verdict — proven by fixture replay | ✓ SATISFIED | Gate/selection machinery (unchanged, still green) plus the publishing half (superseded_note caveat + verdict writer + rendering) are now wired end-to-end and proven by 154+33+448 passing Cloud Build tests plus a confirmed live migration and triple deploy. **Note for the orchestrator:** `.planning/REQUIREMENTS.md` line 22/98 still marks ENGINE-10 as `Pending` — this is a documentation-staleness item, not a code gap; recommend flipping it to Complete now that this verification passes. |

No orphaned requirements found beyond ENGINE-10.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py` | 890-897 | Coverage gate is unreachable dead code (WR-01, unchanged from prior pass) | ⚠️ Warning (deferred, disclosed) | Recovery re-entry path never fires; funnel stays honest via a separate reconciliation path, so this does not break truth-telling. Recorded fix already derived in `15.1-UAT.md`, owned by Phase 15.2. |
| `tribunal/nestor_pulse_sdk/pipeline/tribunal/group_skeptic.py:150-156`, `pipeline.py:699-703`, `verification/report.py:96,120-126` | — | Verdicts attributed to gate-DROPped group members (WR-10, unchanged) | ⚠️ Warning (deferred, disclosed) | Can book a claim in bucket 2 ("not checkable") while it was in fact checked and possibly scrubbed — "did more than we said" direction. Owned by Phase 15.2. |
| `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py:1172` | 1172 | `report_spec` is a second undeclared `set_stage` key | ℹ️ Info (deferred, disclosed, allow-listed) | Explicitly allow-listed in `test_stage_schema.py`'s `_NON_SCHEMA_MARKERS` with a source comment explaining why (interactive pause, not an ordered stage) rather than silently ignored. |
| `.planning/REQUIREMENTS.md` | 22, 98 | ENGINE-10 still marked `Pending` | ℹ️ Info | Stale relative to this verification's `passed` result; recommend orchestrator updates it post-merge. |

No `TBD`/`FIXME`/`XXX` debt markers found in any of the ten gap-closure source files scanned (`extractor.py`, `pipeline.py`, `report.py`, `verification_verdict.py`, `0012_verdict_superseded_note.py`, `schemas.py`, `stages.py`, `research.ts`, `VerificationReport.tsx`).

### Human Verification Required

None required to determine this phase's `passed` status. Sixteen browser-UAT items exist in `15.1-UAT.md` (items 1-16) covering visual confirmation of the funnel, degradation banner, superseded rendering, i18n, and client-boundary checks — these are **pre-existing, disclosed, operator-accepted deferrals** batched into the standing combined Phase-15/15.1/15.2 operator session, explicitly scheduled for after the Anthropic monthly usage cap resets on 2026-08-01 ("Do NOT UAT piecemeal, and do NOT seed the live database"). They are not new findings from this re-verification pass — every one of them is already itemized, with its own expected/bad-result criteria, in `15.1-UAT.md` §§ "Deferred Browser UAT" and "Added by the gap-closure pass — items 9-16". Re-listing them here as blocking `human_verification` would duplicate an already-tracked, deliberately-scheduled activity and contradict the standing operator direction not to UAT piecemeal. The code-level, test-level, and deploy-level evidence gathered in this pass is sufficient to confirm the phase goal is met in the codebase; the browser session confirms the operator can *see* what the code now genuinely does.

### Gaps Summary

Both blocking SC2 sub-gaps from the prior verification are closed, confirmed by direct source reading rather than by trusting the six plan SUMMARYs:

1. **`superseded_note` dead-data gap (CR-01) — CLOSED.** The full chain from skeptic emission through `contested_notes`, a real migrated column, the production writer, the DTO, the TypeScript type, and the React render site was traced and confirmed present at every hop, with no broken link.
2. **No production writer for `verification_verdict` (CR-02) — CLOSED.** `_insert_verdict` is a real, RLS-governed, bound-parameter SQL writer called from two live sites inside `persist_tribunal_claims`, itself called from Stage 7 on every Tribunal run. Both survivors and dropped/refuted claims get verdict rows. The `verdicts_total == 0` fallback in `report.py` makes the funnel/unverified self-contradiction structurally impossible going forward, regardless of whether the writer ever regresses.
3. **A third defect surfaced during gap-closure planning — the `superseded` verdict class had no frontend rendering site — is also closed.** This would have been silently activated by gap 2's writer landing (a verdict class with data and no display site). Plan 15.1-16 added the render site and i18n before the writer could expose the hole.

SC1 and SC3 show no regression: their proving test files are byte-identical to before the gap-closure pass (confirmed by `git diff --stat` against the pre-gap-closure base), and both are recorded PASSED in the re-run Cloud Build gates.

The gap-closure pass also correctly avoided scope creep: WR-01, WR-10, and the `report_spec` undeclared-stage-key finding were explicitly disclosed and deferred to Phase 15.2 with no code written for them (confirmed unchanged in source), and Known Gap 1 (`verification_degraded_text` unrendered) was left open with a stated owner. None of these count against SC2 as written — SC2's bar is "a superseded verdict available", which is now true end-to-end, not "every G-08/G-10 nuance is pixel-perfect".

**One honest residual caveat, not a gap:** the verdict writer and the caveat-rendering path are proven at the code, unit-test, and deploy level, but have not yet been observed against a real live Tribunal run — the Anthropic monthly usage cap blocks live runs until 2026-08-01. This is disclosed exhaustively in `15.1-UAT.md` (items 9-11 explicitly call this out as "the headline item of the closure pass" and state the bad-result condition to check for). It does not block this verification's `passed` status because the phase's own stated goal is "proven ... in tests" (SC1) and "available" as a code-level capability (SC2) — both of which now hold — with the live-run visual confirmation correctly scheduled as the next, already-planned step rather than silently skipped.

**Recommendation:** Proceed to Phase 15.2. No `/gsd:plan-phase 15.1 --gaps` needed. When the Anthropic cap resets and the combined Phase-15* browser UAT session runs, confirm items 9-11 specifically (verdict sections populated, unverified block no longer reads "N of N", superseded verdict renders under its own heading with its caveat) — these are the closure pass's actual novel behavior and were structurally impossible to observe before this pass.

---

_Verified: 2026-07-26_
_Verifier: Claude (gsd-verifier)_
