---
phase: 15.1-research-engine-redesign-verification-gates
plan: 10
type: execute
wave: 6
depends_on: ["15.1-09"]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - .planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md
autonomous: true
requirements: [ENGINE-10]

must_haves:
  truths:
    - "The full Tribunal suite runs in Cloud Build and introduces no NEW failures"
    - "verify_chain is proven green after the phase — no frozen audit payload field renamed"
    - "The gate code is live on Cloud Run, not merely committed"
    - "The batched Phase-15* browser UAT has a written checklist waiting for the post-2026-08-01 live run"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "Phase 15.1 deploy section (tribunal image rebuild + redeploy, no migration)"
      contains: "Phase 15.1"
    - path: ".planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md"
      provides: "deploy record + the deferred browser-UAT checklist folded into the Phase-15* batch"
      contains: "Deploy Record"
  key_links:
    - from: "infra/DEPLOY-RUNBOOK.md"
      to: "tribunal/cloudbuild.api.yaml + cloudbuild.worker.yaml"
      via: "the dual rebuild both Tribunal services need for gate code to take effect"
      pattern: "cloudbuild.worker.yaml"
    - from: "15.1-UAT.md"
      to: "15.2 end-of-phase UAT session"
      via: "operator standing direction — one combined Phase-15* UAT against a live run"
      pattern: "15.2"
---

<objective>
Close the phase: prove the whole suite, prove the audit chain, ship the code to Cloud Run, and
write down the browser checks the operator will run once a live Tribunal run is possible.

Purpose: this repo has a documented recurring gap — phases 6, 8, 10 and 11 were all "executed
but not deployed". Gate code that only exists in git changes nothing about the next real run.
And SC3 (`verify_chain` stays green) needs an explicit end-of-phase proof, not an inference.

Operator standing direction (2026-07-24, unchanged): run ONE combined Phase-15* browser UAT
once 15, 15.1 and 15.2 are all ready, against a live run after the Anthropic monthly cap resets
2026-08-01. Do NOT UAT piecemeal, and do NOT seed the live database. So this plan writes the
checklist and defers its execution — it does not open a blocking checkpoint.

Output: the full-suite phase gate result, the deployed images, the runbook section, and
`15.1-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-CONTEXT.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-RESEARCH.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-VALIDATION.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-09-SUMMARY.md

<interfaces>
<!-- The phase gate's standing caveat (cloudbuild.test.yaml header, verbatim) -->
"some tests require live provider API keys and will fail in this keyless build environment —
 triage deferred (see 13-PROOF-RESULTS.md); the phase-critical subset (schema isolation,
 advisory lock, hash chain, RLS) is green."
⇒ the phase gate asserts NO NEW FAILURES, not absolute green.

<!-- ROADMAP Phase 15.1 success criteria (all three must be traceable in the SUMMARY) -->
SC1: the gate pipeline replays the recorded 1,162-claim fixture in tests and reproduces the
     recorded keep/drop numbers.
SC2: materiality + error-likelihood gates, canonical grouping, corroboration prioritization run
     in the verification stage, with fail-loud on gate errors and a "superseded" verdict available.
SC3: verify_chain stays green — frozen payload fields unchanged.

<!-- No migration this phase: run.verification_summary (JSONB) and verification_verdict.verdict
     (free TEXT, no CHECK) both already exist from migration 0011. Latest migration stays 0011. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite phase gate and the SC3 audit-chain proof</name>
  <files>.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md</files>
  <read_first>
    - tribunal/cloudbuild.test.yaml (whole file — the full-suite config, its testcontainers setup, the `timeout: 3600s`, and the keyless-build caveat in its header)
    - tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py lines 755-860 (`test_chain_green_after_cost_migration` — the 11-frozen-field assertion, the additive-column tuple, and the `RECORDED_FUNNEL_COUNTS` equality)
    - .planning/phases/13-*/13-PROOF-RESULTS.md (the recorded list of pre-existing keyless-build failures — the baseline "no NEW failures" is measured against)
    - 15.1-VALIDATION.md § Sampling Rate ("Before /gsd:verify-work: full suite … assert no NEW failures")
  </read_first>
  <action>
Run the three Cloud Build gates in order and capture their results:

1. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"`
   — the fast 15.1 gate: all seven new test files plus the three extended ones, no Postgres, no
   live calls. Must be green.
2. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT"`
   — schema isolation, advisory lock, hash chain, verification report endpoint. Must be green.
   This is the SC3 proof: `test_hash_chain_replay.py` asserts `verify_chain` returns
   `(True, None)`, that `_payload_for_row` stays at exactly 11 frozen fields, and that
   `cache_creation_tokens` / `cost_pending` / `verification_summary` are absent from the hashed
   payload. Confirm in the log output that this test PASSED rather than was skipped.
3. `gcloud builds submit tribunal --config=tribunal/cloudbuild.test.yaml --project="$GOOGLE_PROJECT"`
   — the full suite. Compare its failure list against the recorded pre-existing baseline in
   `13-PROOF-RESULTS.md`. The gate is **NO NEW failures**, not absolute green — some carried
   tests require live provider API keys and fail in this keyless environment, a known deferred
   chore.

Create `15.1-UAT.md` and record, under a `## Phase Gate` heading: each build id, its config, its
pass/fail line, the full-suite pass/fail/skip tallies, and an explicit NEW-versus-PRE-EXISTING
failure comparison table. If any NEW failure appears, stop and fix it in this task before
proceeding to Task 2 — do not deploy a red tree.

Under a `## Success Criteria Trace` heading, map each of the three ROADMAP success criteria to
the specific test(s) that prove it, naming the file and the test function, and — for SC1 — the
five gate-computed funnel numbers with a note that `verify_sessions` is deliberately excluded
per G-13.

Also record the G-01 boundary evidence: paste the threshold-guard grep from plan 15.1-09 and
its empty output, proving the calibration file contains no agreement threshold.
  </action>
  <acceptance_criteria>
    - `.planning/phases/15.1-.../15.1-UAT.md` exists with a `## Phase Gate` section listing three build ids and their outcomes.
    - The file records `test_chain_green_after_cost_migration` as PASSED (not skipped) with the `len(frozen_fields) == 11` assertion intact.
    - The file contains a NEW-vs-PRE-EXISTING failure table; the NEW column is empty.
    - The file contains a `## Success Criteria Trace` section mapping SC1, SC2 and SC3 to named test functions.
    - The file contains the threshold-guard grep command and its empty output.
    - `ls tribunal/nestor_pulse_sdk/alembic/versions/ | tail -1` shows `0011_cost_verification.py` — no migration was added this phase.
  </acceptance_criteria>
  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>
  <done>All three Cloud Build gates are recorded, the full suite shows no NEW failures, `verify_chain` is proven green with 11 frozen payload fields, and all three ROADMAP success criteria trace to named tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build and deploy the Tribunal images, and write the runbook section</name>
  <files>infra/DEPLOY-RUNBOOK.md, .planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md (the whole file — in particular the most recent phase section, so the new one matches its heading style, command style and record format)
    - tribunal/cloudbuild.api.yaml and tribunal/cloudbuild.worker.yaml (the two image builds — both must be rebuilt; gate code lives in the shared `nestor_pulse_sdk` package)
    - 15.1-RESEARCH.md § Runtime State Inventory (the "Build artifacts" row: both Cloud Run images must be rebuilt and redeployed; and the "Secrets / env vars" row: all new tunables read `os.environ.get(..., default)`, so NO Cloud Run env change is required)
    - .planning/STATE.md § Blockers/Concerns (the carried IaC-drift item — Terraform state was never adopted, so the runbook is the source of truth for deploys)
  </read_first>
  <action>
Append a `## Phase 15.1 — Verification Gates` section to `infra/DEPLOY-RUNBOOK.md`, matching the
existing sections' style. It must state:

  - **No migration.** The latest Alembic revision stays `0011`. `run.verification_summary`
    (JSONB) and `verification_verdict.verdict` (free TEXT, no CHECK) already exist. Do NOT run a
    migrate job for this phase.
  - **No Cloud Run env change.** Every new tunable
    (`NESTOR_TRIBUNAL_GATE_BATCH`, `_GATE_CONCURRENCY`, `_GATE_RETRIES`, `_GATE_BACKOFF_S`,
    `NESTOR_TRIBUNAL_CLUSTER`, `_CLUSTER_BATCH`, `_CLUSTER_MAX_BLOCK`, `_CLUSTER_CONCURRENCY`)
    is read at import with a production-safe default. List them with their defaults and note the
    two rollback levers: `NESTOR_TRIBUNAL_CLUSTER=false` restores exact-key bucketing, and the
    existing `NESTOR_TRIBUNAL_GROUP_VERIFY` still switches the per-claim fallback branch.
  - **Dual rebuild required.** Both `nestor-tribunal-api` and the worker must be rebuilt: the
    gate, cluster and funnel code lives in the shared `nestor_pulse_sdk` package and the worker
    is what writes `run.verification_summary`. Give the exact `gcloud builds submit` and
    `gcloud run deploy` commands, following the existing sections' formatting. Note the
    image-pin lesson already recorded in the runbook for prior phases — deploy the specific
    tag just built, never `:latest`.
  - **Verification after deploy:** `gcloud run services describe` for both services showing the
    new revision serving 100%, and a `/readyz` check. No live research run is triggered — the
    Anthropic monthly cap resets 2026-08-01.

Then execute that section: build both images and deploy both services. Record in `15.1-UAT.md`
under a `## Deploy Record` heading: the image tag/SHA, both service names, both new revision
ids, the timestamp, and the `describe`/`readyz` output lines. If a deploy step needs
authentication the executor does not have, record the exact blocking command and mark the deploy
row PENDING rather than claiming success — a false deploy record is the specific failure mode
this task exists to prevent.
  </action>
  <acceptance_criteria>
    - `grep -c "Phase 15.1" infra/DEPLOY-RUNBOOK.md` returns >= 1.
    - `grep -c "cloudbuild.worker.yaml" infra/DEPLOY-RUNBOOK.md` returns >= 1 within the new section.
    - `grep -c "NESTOR_TRIBUNAL_CLUSTER" infra/DEPLOY-RUNBOOK.md` returns >= 1 (the rollback lever is documented).
    - The new section explicitly states that no migration and no env change are required.
    - `15.1-UAT.md` contains a `## Deploy Record` section with both service names, both revision ids and the image tag — or an explicit PENDING row naming the blocking command.
    - `gcloud run services describe nestor-tribunal-api --format='value(status.latestReadyRevisionName)'` output is pasted into the record.
  </acceptance_criteria>
  <verify>
    <automated>gcloud run services describe nestor-tribunal-api --region="$GCP_REGION" --project="$GOOGLE_PROJECT" --format='value(status.latestReadyRevisionName,status.traffic)'</automated>
  </verify>
  <done>The runbook has a Phase 15.1 section documenting the dual rebuild, the no-migration/no-env-change facts and the rollback levers; both Tribunal services run a new revision built from this phase's code, recorded with revision ids in `15.1-UAT.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Write the deferred browser-UAT checklist into the Phase-15* batch</name>
  <files>.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md</files>
  <read_first>
    - .planning/phases/15-*/15-UAT.md (the Phase 15 UAT file — the checklist format to match, and its recorded operator deferral)
    - .planning/STATE.md § Session Continuity and § Current Position (the standing operator direction: ONE combined Phase-15* UAT after a live run post-2026-08-01; no live-DB seeding)
    - frontend/src/components/intake/VerificationReport.tsx lines 265-335 (what the operator will actually see: the funnel rendered via `Object.entries(report.funnel)` — new keys render automatically with the raw key as the label)
    - 15.1-CONTEXT.md G-09 and G-10 (superadmin-only; degradation stated in words)
  </read_first>
  <action>
Add a `## Deferred Browser UAT (batched with Phase 15 / 15.2)` section to `15.1-UAT.md`, matching
the Phase 15 UAT checklist format. State the standing operator direction at the top: one
combined Phase-15* walkthrough, against a real live Tribunal run after 2026-08-01, no live-DB
seeding.

Checklist items, each with what to click and what must be true:

1. After a live run completes, the superadmin verification report shows the gate funnel with the
   new keys (`not_falsifiable`, `not_load_bearing`, `both`, `checked`,
   `should_have_been_checked`, `gate_errors`) — note that they render with raw key labels until
   i18n labels are added, which is optional polish, not a blocker.
2. `should_have_been_checked` reads 0 on a healthy run; when it does not, the degradation
   sentence is visible at the top of the report in words, not as an icon (G-10).
3. The run's status still reads `completed` — no `completed_degraded` anywhere in the UI (G-10;
   15.2's R6 owns that promotion).
4. Nothing from the verification accounting appears in the client-facing delivered report
   (G-09 / G-14) — check a delivered intake as a client-role user.
5. The D15 feed shows a `gate` stage row with the funnel counts, and a closing verify summary
   line that states degradation in words when it occurred.
6. A `superseded` verdict, if the run produced one, appears in its own bucket and NOT inside
   `insufficient`; the existing reconciliation-derived "superseded / scoped" section still shows
   its own items (the name collision is invisible to the operator).
7. Cost sanity: the run's total is materially below the ~$43-45 baseline of run 4cbb5311,
   consistent with the ~950 → ~150 checks economic bar. Record the actual number — this is a
   measurement, not a pass/fail gate.
8. The August calibration: `pytest -m live nestor_pulse_sdk/tests/test_gate_calibration.py -s`
   with the opt-in env var set. Record the agreement table and the four contradiction-pair
   results. **Informational only — no threshold, never a gate** (G-01, G-05). Its numbers feed
   15.2's V-01/V-02/V-03.

Close the section with a line stating these items are carried into the combined Phase-15*
session and must not be run piecemeal.
  </action>
  <acceptance_criteria>
    - `grep -c "Deferred Browser UAT" .planning/phases/15.1-*/15.1-UAT.md` returns 1.
    - The section contains >= 8 checklist items, each with an observable expected outcome.
    - `grep -c "completed_degraded" .planning/phases/15.1-*/15.1-UAT.md` returns >= 1 and appears only as a MUST-NOT-EXIST check.
    - `grep -c "test_gate_calibration" .planning/phases/15.1-*/15.1-UAT.md` returns >= 1, described as informational with no threshold.
    - `grep -c "2026-08-01" .planning/phases/15.1-*/15.1-UAT.md` returns >= 1.
  </acceptance_criteria>
  <verify>
    <human-check>Deferred by standing operator direction — executed in the combined Phase-15* UAT session after a live Tribunal run post-2026-08-01. This task's automated gate is the file's existence and content, asserted above.</human-check>
    <automated>test -f .planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md &amp;&amp; grep -c "Deferred Browser UAT" .planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md</automated>
  </verify>
  <done>`15.1-UAT.md` carries an eight-item browser checklist with observable outcomes, explicitly batched into the combined Phase-15* session and explicitly not to be run piecemeal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built image → Cloud Run production service | Deploying the wrong or an unbuilt image ships unverified behaviour to the next real client run |
| deploy record → project memory | A false "deployed" claim is the exact failure mode that recurred in phases 6, 8, 10 and 11 |
| Cloud Build service account → GCP resources | The deploy path uses privileged credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-46 | Repudiation | a false deploy record | mitigate | Revision ids and `gcloud run services describe` output pasted into `15.1-UAT.md`; a blocked step is recorded PENDING with the exact failing command rather than claimed |
| T-15.1-47 | Tampering | deploying `:latest` instead of the tag just built | mitigate | The runbook section mandates deploying the specific built tag, carrying forward the recorded image-pin lesson from earlier phases |
| T-15.1-48 | Tampering | audit hash chain (V6 — do not touch) | mitigate | SC3 is proven explicitly in Task 1: `verify_chain → (True, None)` and `_payload_for_row` at 11 frozen fields, confirmed PASSED (not skipped) in the build log |
| T-15.1-49 | Denial of Service | a schema drift between deployed code and the database | mitigate | Runbook states no migration is required and the check `ls alembic/versions | tail -1` confirms `0011` remains the head |
| T-15.1-50 | Information Disclosure | verification accounting reaching a client | mitigate | UAT item 4 checks a delivered intake as a client-role user; the code-level guarantee is plan 15.1-06's `test_client_appendix_unchanged` |
| T-15.1-51 | Denial of Service (cost / cap) | a live research run triggered before the cap resets | mitigate | The runbook's post-deploy verification is `describe` + `/readyz` only; no run is triggered, and the calibration test stays behind its env-var opt-in |
| T-15.1-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase; `tribunal/requirements.txt` unmodified |
</threat_model>

<verification>
- Three Cloud Build gates recorded in `15.1-UAT.md`, with no NEW failures against the `13-PROOF-RESULTS.md` baseline.
- `test_hash_chain_replay.py::test_chain_green_after_cost_migration` recorded as PASSED (SC3).
- Both Tribunal Cloud Run services showing a new revision built from this phase's code, with revision ids recorded.
- `ls tribunal/nestor_pulse_sdk/alembic/versions/ | tail -1` still `0011_cost_verification.py`.
- `git diff --name-only` lists only `infra/DEPLOY-RUNBOOK.md` and the new `15.1-UAT.md`.
</verification>

<success_criteria>
- The full suite runs in Cloud Build with no NEW failures.
- `verify_chain` is proven green with the frozen payload unchanged (SC3).
- The gate code is live on both Tribunal Cloud Run services, recorded with revision ids.
- The runbook documents the dual rebuild, the no-migration/no-env-change facts, and both rollback levers.
- The deferred browser checks are written down and folded into the combined Phase-15* UAT session.
</success_criteria>

<output>
Create `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-10-SUMMARY.md` when done
</output>
