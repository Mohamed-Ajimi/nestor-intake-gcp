---
phase: 15.1-research-engine-redesign-verification-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py
  - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/__init__.py
  - tribunal/cloudbuild.test-gates.yaml
  - tribunal/cloudbuild.test-critical.yaml
autonomous: true
requirements: [ENGINE-10]

must_haves:
  truths:
    - "Every 15.1 test reads the selection-experiment fixture through ONE shared parser that handles CRLF, LF, and embedded tabs"
    - "The recorded funnel constant carries the G-08 accounting numbers, and the two existing full-dict-equality tests stay green"
    - "verify_sessions=176 is marked in source as incident-derived and is asserted by no gate test (G-13)"
    - "A Cloud Build config exists that runs only the new 15.1 test files with no Postgres and no live LLM calls"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py"
      provides: "load_selection_experiment() + extended RECORDED_FUNNEL_COUNTS"
      contains: "def load_selection_experiment"
    - path: "tribunal/cloudbuild.test-gates.yaml"
      provides: "no-Postgres, no-live phase gate for the 15.1 test files"
      contains: "not live"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py"
      to: "loader._report_dir()"
      via: "in-package recorded/ path resolution (never repo-root docs/)"
      pattern: "_report_dir\\(\\)"
    - from: "tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/__init__.py"
      to: "load_selection_experiment"
      via: "barrel re-export + __all__"
      pattern: "load_selection_experiment"
---

<objective>
Build the shared foundation every other 15.1 plan depends on: one fixture parser for the
selection-experiment TSVs, and the extended recorded funnel constant that encodes the G-08
accounting numbers. Also stand up the cheap Cloud Build gate the rest of the phase verifies
against.

Purpose: RESEARCH § Fixture Contract documents three parsing traps (no header, mixed line
endings, 74 rows with literal tabs inside `evidence`). Writing that parser once, in
`loader.py`, is the difference between one correct reader and eight subtly-wrong ones.
`RECORDED_FUNNEL_COUNTS` must be extended and `build_verification_summary()` re-verified in
the SAME task because two existing tests compare that dict by full equality.

Output: `load_selection_experiment()`, the 13-key `RECORDED_FUNNEL_COUNTS`, and
`tribunal/cloudbuild.test-gates.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-CONTEXT.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-RESEARCH.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-PATTERNS.md

<interfaces>
<!-- THE 15.1 FUNNEL CONTRACT. Identical in every plan of this phase. -->
<!-- Lives in run.verification_summary (JSONB) and in report["funnel"]. -->

FUNNEL KEYS (existing 6 — NEVER renamed):
  distilled                int   # claims after distill + dedupe
  kept                     int   # materiality-gate survivors
  dropped                  int   # materiality-gate rejects
  selected_verify          int   # kept minus skipped_stable
  skipped_stable           int   # error-likelihood-gate skips
  verify_sessions          int   # INCIDENT-DERIVED (G-13) — NEVER asserted by a gate test

FUNNEL KEYS (new in 15.1 — ADDITIVE ONLY):
  not_falsifiable          int   # G-08 bucket-2 reason
  not_load_bearing         int   # G-08 bucket-2 reason
  both                     int   # G-08 bucket-2 reason
  checked                  int   # G-08 bucket 1 — claims that got a verdict
  should_have_been_checked int   # G-08 bucket 3 — MUST be 0 on a healthy run
  gate_errors              int   # G-11 — gate batches that failed after retries
  verification_degraded    bool  # G-10 marker == (should_have_been_checked > 0)

INVARIANTS (asserted by tests across the phase):
  distilled == kept + dropped
  kept      == selected_verify + skipped_stable
  dropped   == not_falsifiable + not_load_bearing + both
  distilled == checked + (dropped + skipped_stable) + should_have_been_checked
  verification_degraded == (should_have_been_checked > 0)

RECORDED VALUES for run 4cbb5311 (sources named per key in the task action):
  distilled 1162 · kept 456 · dropped 706 · selected_verify 424 · skipped_stable 32
  verify_sessions 176 · not_falsifiable 358 · not_load_bearing 320 · both 28
  checked 198 · should_have_been_checked 226 · gate_errors 0 · verification_degraded True
  (198 + 738 + 226 == 1162 ✓ ; 358 + 320 + 28 == 706 ✓ ; 424 + 32 == 456 ✓)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add load_selection_experiment() to the recorded-run fixture loader</name>
  <files>tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py, tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/__init__.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py (whole file — especially `_report_dir()` at 67-86, `_load_index()` at ~88, `RECORDED_FUNNEL_COUNTS` at 44-64, `build_verification_summary()` at 189-191, `load_recorded_run()` docstring at 194-207)
    - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/__init__.py (lines 1-40 — the import block and `__all__`)
    - tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py lines 671-704 (`_parse_distiller_response` — the `line.split("\t", 2)` contract this parser must match)
    - 15.1-PATTERNS.md § 4 "Fixture reader extension" (the `_report_dir()` Cloud Build safety property and the parsing contract)
    - 15.1-RESEARCH.md § The Fixture Contract (row counts, headers, line endings, id semantics)
  </read_first>
  <action>
Add a single public reader `load_selection_experiment()` to `loader.py`, placed directly
after `build_verification_summary()` so the recorded-data helpers stay together.

Signature and return contract:
`def load_selection_experiment() -> tuple[list[dict], dict[int, str], dict[int, str]]`
returning `(claims, classified, strict)` where:
  - `claims` is a list of 1,162 dicts in the EXACT shape `claim_distiller` emits —
    `{"id": int, "text": str, "facet": str, "evidence": str}` — with `id` being the
    **1-based line number** of `claims-distilled-full.tsv` (there is no id column; the other
    two TSVs join on this number).
  - `classified` maps `id -> reason` from `claims-classified-full.tsv`, one of the literal
    strings `KEEP`, `NOT_FALSIFIABLE`, `NOT_LOAD_BEARING`, `BOTH` (the file's `reason`
    column is literally `KEEP` on KEEP rows; use `reason`, not `decision`).
  - `strict` maps `id -> decision` from `keep-strict.tsv`, one of `VERIFY`, `SKIP_STABLE`,
    for exactly the 456 KEEP ids.

Resolve the directory as `_report_dir() / "selection-experiment"`. Do NOT construct a
repo-root `docs/` path — `gcloud builds submit tribunal` ships only the `tribunal/` subtree.
Read every file with `read_text(encoding="utf-8")` (the TSVs contain em-dashes, `€` and
accented Dutch).

Parsing rules that must live here and only here:
  - split the raw text on `"\n"`, then `line.rstrip("\r")` per line — `claims-distilled-full.tsv`
    and `claims-classified-full.tsv` are CRLF while `keep-strict.tsv` is LF-only;
  - skip empty lines;
  - `claims-distilled-full.tsv` has **NO header** — line 1 is data;
  - use `line.split("\t", 2)` with maxsplit=2 for the distilled file (74 of the 1,162 rows
    carry literal tabs inside `evidence`; a bare `split("\t")` or `csv.reader` mis-shapes them);
  - the other two files have a header line that must be dropped, and exactly 2 or 3 columns —
    `split("\t")` is safe there.

Add module-level docstring lines above the function naming the three files, their row counts
(1162 / 1162 / 456), their line endings, and the 1-based-line-number id rule, following the
documented-constant comment convention already used above `RECORDED_FUNNEL_COUNTS`.

Then re-export `load_selection_experiment` from
`tests/fixtures/run_4cbb5311/__init__.py` — add it to both the `from ... .loader import (...)`
block and to `__all__`, keeping the existing alphabetical-ish ordering.
  </action>
  <acceptance_criteria>
    - `grep -n "def load_selection_experiment" tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py` returns exactly one line.
    - `grep -c 'split("\\t", 2)' tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py` returns >= 1 (the maxsplit=2 rule is present).
    - `grep -n "docs/tribunal-run-reports" tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py` shows the path ONLY inside the pre-existing `_report_dir()` fallback, not inside the new function.
    - `grep -n "load_selection_experiment" tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/__init__.py` matches in both the import block and `__all__`.
    - The function returns 1162 claims, 1162 classified entries and 456 strict entries when exercised (proven by the Task 3 Cloud Build run of `test_gate_replay.py` in plan 15.1-09; this task's own gate is the source assertions above plus the regression run in Task 2).
  </acceptance_criteria>
  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>
  <done>`load_selection_experiment()` exists in `loader.py`, resolves through `_report_dir()`, uses `split("\t", 2)` and per-line `rstrip("\r")`, and is exported from the fixture barrel.</done>
</task>

<task type="auto">
  <name>Task 2: Extend RECORDED_FUNNEL_COUNTS and build_verification_summary() in lockstep</name>
  <files>tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py lines 44-64 (the documented-constant comment block + `RECORDED_FUNNEL_COUNTS`) and 189-191 (`build_verification_summary`)
    - tribunal/nestor_pulse_sdk/tests/test_hash_chain_replay.py lines 840-855 (the `run.verification_summary == RECORDED_FUNNEL_COUNTS` full-dict assertion)
    - tribunal/nestor_pulse_sdk/tests/test_verification_report_endpoint.py lines 41-61 (the `report["funnel"] == RECORDED_FUNNEL_COUNTS` full-dict assertion)
    - 15.1-CONTEXT.md § "Amendments from research" G-13 (verify_sessions is incident-derived)
    - 15.1-RESEARCH.md § Pitfall 1 (the 176-session trap) and § Code Examples ("Both are full-dict equality")
  </read_first>
  <action>
This task MUST change the constant and its builder together — two existing tests compare the
dict by full equality, so a split would leave the tree red between commits.

Widen the annotation to `RECORDED_FUNNEL_COUNTS: dict[str, int | bool]` and extend it from 6
to 13 keys, exactly as listed in the `<interfaces>` funnel contract above. Keep the six
existing keys and their values byte-identical.

Extend the documented-constant comment block directly above the dict so each new key names
its source:
  - `not_falsifiable 358` / `not_load_bearing 320` / `both 28` — the `reason` column tallies
    of `claims-classified-full.tsv` (they sum to `dropped` 706).
  - `checked 198` — the summed `#claims` across the 176 rows of `recorded/GROUPS.md`
    (per `recorded/REPORT.md` §3.5: the 176 completed group-skeptic passes covered 198 claim
    slots).
  - `should_have_been_checked 226` — DERIVED: `selected_verify 424 - checked 198`. These are
    the claims the gates selected that the 776 cap-rejected passes never reached. Note in the
    comment that this is arithmetic, not a directly recorded number.
  - `gate_errors 0` — the recorded run had no gate stage; zero by construction.
  - `verification_degraded True` — the recorded run WAS degraded (776 cap-400s in 55s).

Add an inline comment on the `verify_sessions` line marking it, per G-13, as
**incident-derived (176 group-skeptic calls that RETURNED, not a funnel output) and therefore
NEVER asserted by a gate test** — it is a recorded pass-through constant only.

Do NOT change `build_verification_summary()`'s body: it must stay `return dict(RECORDED_FUNNEL_COUNTS)`
so both full-dict-equality assertions remain green automatically. Update only its docstring
to mention that it now carries the G-08 accounting keys and the G-10 marker.

Add nothing to `audit_log` and no new column anywhere — `_payload_for_row` must stay at its 11
frozen fields (EU AI Act Art. 12, deadline 2026-08-02).
  </action>
  <acceptance_criteria>
    - `grep -c '":' ...` is not a valid gate here; instead: `grep -v '^#' tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py | grep -c '"should_have_been_checked"'` returns >= 1.
    - `grep -v '^ *#' tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py | grep -c '"verification_degraded"'` returns >= 1.
    - `grep -n "verify_sessions" tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py` shows a line whose comment contains the words `incident` and `NOT a gate assertion`.
    - `grep -n "return dict(RECORDED_FUNNEL_COUNTS)" tribunal/nestor_pulse_sdk/tests/fixtures/run_4cbb5311/loader.py` still matches (builder body unchanged).
    - `grep -rn "_payload_for_row" tribunal/nestor_pulse_sdk/audit/hash_chain.py | head -3` — no field added; `test_hash_chain_replay.py::test_chain_green_after_cost_migration` still asserts `len(frozen_fields) == 11`.
    - Cloud Build run of `test_hash_chain_replay.py` passes, including `run.verification_summary == RECORDED_FUNNEL_COUNTS`.
  </acceptance_criteria>
  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>
  <done>`RECORDED_FUNNEL_COUNTS` has 13 keys with sourced comments, `verify_sessions` carries the G-13 incident marker, `build_verification_summary()` still returns a copy of the constant, and `test_hash_chain_replay.py` + `test_verification_report_endpoint.py` both pass in Cloud Build.</done>
</task>

<task type="auto">
  <name>Task 3: Add the no-Postgres 15.1 Cloud Build gate and widen the critical gate</name>
  <files>tribunal/cloudbuild.test-gates.yaml, tribunal/cloudbuild.test-critical.yaml</files>
  <read_first>
    - tribunal/cloudbuild.test-critical.yaml (whole file, 32 lines — the shape to strip Postgres out of)
    - tribunal/cloudbuild.test.yaml lines 1-30 (the "no NEW failures" caveat header and the invocation convention)
    - tribunal/pyproject.toml lines 11-24 (`asyncio_mode`, registered markers incl. `live`, `--strict-markers`)
    - 15.1-PATTERNS.md § 8 "Cloud Build test config" (the exact lines to keep and to drop)
  </read_first>
  <action>
Create `tribunal/cloudbuild.test-gates.yaml` — the fast phase gate for this phase. Copy
`cloudbuild.test-critical.yaml`'s shape but DROP the entire Postgres preamble (`docker pull
postgres:15`, `docker run -d --name pg …`, the `pg_isready` loop, the three `psql` role
statements) and DROP the `-e DATABASE_URL=…` env. The 15.1 tests are pure: fixture
`session=None`, hand-written fake `audited`, no DB.

Keep: `name: "gcr.io/cloud-builders/docker"`, `entrypoint: bash`, the
`-v /workspace:/workspace -w /workspace` mount, the `python:3.11-slim` image,
`pip install --no-cache-dir -q -r requirements.txt`, `options: logging: CLOUD_LOGGING_ONLY`.

The pytest invocation must tolerate files that have not landed yet (this config is created in
wave 1 but its target files arrive across waves 1-5). Build the file list with `ls` and a
`2>/dev/null` guard, then run pytest on the result:

  FILES=$(ls nestor_pulse_sdk/tests/test_gate_replay.py \
             nestor_pulse_sdk/tests/test_gate_selector.py \
             nestor_pulse_sdk/tests/test_gate_failure_modes.py \
             nestor_pulse_sdk/tests/test_gate_calibration.py \
             nestor_pulse_sdk/tests/test_superseded_verdict.py \
             nestor_pulse_sdk/tests/test_verification_buckets.py \
             nestor_pulse_sdk/tests/test_fail_loud.py \
             nestor_pulse_sdk/tests/test_tribunal_grouping.py \
             nestor_pulse_sdk/tests/test_claim_distiller.py \
             nestor_pulse_sdk/tests/test_tribunal_adjudicate.py 2>/dev/null)
  python -m pytest $FILES -v -rs --tb=short -m "not live"

The `-m "not live"` is load-bearing: `pyproject.toml` only REGISTERS the `live` marker, it
does not deselect it, so without this flag the August calibration test would fire a real LLM
call inside CI. Add a header comment on the file stating that, and stating that this config
needs no Postgres and no provider API keys.

Then edit `tribunal/cloudbuild.test-critical.yaml`: append
`nestor_pulse_sdk/tests/test_verification_report_endpoint.py` to its existing pytest file list
(after `test_hash_chain_replay.py`). That file holds the second
`RECORDED_FUNNEL_COUNTS` full-dict-equality assertion plus an RLS-denial half that needs the
Postgres this config already provides. Update the file's header comment to name the addition.
  </action>
  <acceptance_criteria>
    - `tribunal/cloudbuild.test-gates.yaml` exists and `grep -c "postgres" tribunal/cloudbuild.test-gates.yaml` returns 0.
    - `grep -c 'DATABASE_URL' tribunal/cloudbuild.test-gates.yaml` returns 0.
    - `grep -c -- '-m "not live"' tribunal/cloudbuild.test-gates.yaml` returns 1.
    - `grep -c "test_verification_report_endpoint.py" tribunal/cloudbuild.test-critical.yaml` returns 1.
    - `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml` exits 0 (at this point it runs only the pre-existing `test_tribunal_grouping.py`, `test_claim_distiller.py`, `test_tribunal_adjudicate.py`).
  </acceptance_criteria>
  <verify>
    <automated>gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml --project="$GOOGLE_PROJECT"</automated>
  </verify>
  <done>Both configs exist and exit 0 in Cloud Build; the gates config provisions no database and deselects `live`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture files → test process | The selection-experiment TSVs are trusted repo content, but malformed parsing silently changes the counts the whole phase is proven against |
| Cloud Build worker → provider APIs | A misconfigured test config could fire real LLM calls during CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-01 | Tampering | `loader.load_selection_experiment` parsing | mitigate | `split("\t", 2)` + per-line `rstrip("\r")` + explicit `encoding="utf-8"`; 1162/1162/456 row counts asserted downstream (plan 15.1-09) |
| T-15.1-02 | Repudiation | `RECORDED_FUNNEL_COUNTS["verify_sessions"]` | mitigate | G-13 inline comment marks it incident-derived; no gate test may assert it (grep-checked in plan 15.1-09) |
| T-15.1-03 | Denial of Service (self-inflicted, cost) | `cloudbuild.test-gates.yaml` | mitigate | `-m "not live"` deselects the calibration test; the config injects no API keys |
| T-15.1-04 | Tampering | audit hash chain (V6 — do not touch) | mitigate | No `audit_log` column added; `_payload_for_row` stays at 11 frozen fields; `test_hash_chain_replay.py` re-run in Task 2's verification |
| T-15.1-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH § Package Legitimacy Audit); `tribunal/requirements.txt` unmodified |
</threat_model>

<verification>
- `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-critical.yaml` green (schema isolation, advisory lock, hash chain, verification report endpoint).
- `gcloud builds submit tribunal --config=tribunal/cloudbuild.test-gates.yaml` green.
- `git diff --stat` touches only the four files in `files_modified`.
</verification>

<success_criteria>
- `load_selection_experiment()` is the phase's single fixture parser and is barrel-exported.
- `RECORDED_FUNNEL_COUNTS` carries all 13 contract keys with per-key source comments.
- Both pre-existing full-dict-equality assertions still pass.
- `verify_sessions` is marked incident-derived in source (G-13).
- A no-Postgres, no-live Cloud Build config exists for the rest of the phase to verify against.
</success_criteria>

<output>
Create `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-01-SUMMARY.md` when done
</output>
