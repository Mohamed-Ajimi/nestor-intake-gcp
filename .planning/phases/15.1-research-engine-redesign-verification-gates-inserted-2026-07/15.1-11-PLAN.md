---
phase: 15.1-research-engine-redesign-verification-gates
plan: 11
type: execute
wave: 7
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/db/models/verification_verdict.py
  - tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py
  - tribunal/cloudbuild.test-gates.yaml
autonomous: true
requirements: [ENGINE-10]

must_haves:
  truths:
    - "The TRIBUNAL alembic line has a 0012 revision with down_revision 0011 that adds verification_verdict.superseded_note"
    - "The ORM model declares superseded_note as nullable Text, mirroring the migration exactly"
    - "The new column is covered by the EXISTING verification_verdict FORCE-RLS tenant policy — no policy is created, altered or dropped"
    - "The frozen audit-hash payload is untouched: the migration names no audit_log column and no _payload_for_row field"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py"
      provides: "additive superseded_note TEXT column on verification_verdict (tribunal line)"
      contains: "down_revision"
    - path: "tribunal/nestor_pulse_sdk/db/models/verification_verdict.py"
      provides: "superseded_note mapped column + updated column docstring"
      contains: "superseded_note"
    - path: "tribunal/cloudbuild.test-gates.yaml"
      provides: "pre-registration of the three new gap-closure test files in the no-Postgres gate"
      contains: "test_verdict_write_path.py"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py"
      to: "tribunal/nestor_pulse_sdk/db/models/verification_verdict.py"
      via: "the ORM model mirrors the migration table shape EXACTLY (the file's own stated contract)"
      pattern: "superseded_note"
    - from: "tribunal/cloudbuild.test-gates.yaml"
      to: "the three new test files landed by plans 15.1-12/13/14"
      via: "the `ls ... || true` file list tolerates files that do not exist yet"
      pattern: "test_verdict_publication.py"
---

<objective>
Give `superseded_note` a real home: an additive `TEXT` column on `verification_verdict` in the
TRIBUNAL alembic line, mirrored on the ORM model, plus pre-registration of the three new
gap-closure test files in the fast no-Postgres Cloud Build gate.

Purpose: 15.1-VERIFICATION.md SC2 gap 1 and code review CR-01 both end at the same wall — "there
is no DB column to persist it in". The operator has ruled (gap-closure scope, 2026-07-25) that
this closes FULLY this phase, storage included: a real column, not a sub-key smuggled into the
existing `reconciliation` JSONB. This plan is the foundation the verdict writer (15.1-14) needs;
it deliberately lands no writer of its own so the schema change can ship and be reviewed alone.

**Wave numbering:** this gap-closure set CONTINUES the existing phase numbering. Plans 15.1-01
through 15.1-10 occupy waves 1-6; the closure set uses waves 7, 8 and 9. Nothing in plans 01-10
is modified.

Output: migration `0012_verdict_superseded_note.py`, the mirrored ORM column, and an updated
`cloudbuild.test-gates.yaml` file list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-CONTEXT.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-VERIFICATION.md
@.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-REVIEW.md

<interfaces>
<!-- The tribunal alembic line is SEPARATE from the intake backend's. Two schemas, two
     alembic_version tables, two revision sequences (v1.1 roadmap decision). The intake
     backend already has 0011 and 0012 of its OWN under backend/app/db/alembic/versions/ —
     those belong to Phases 16 and 17 and are NOT this line. The tribunal head today is
     0011_cost_verification.py (revision "0011", down_revision "0010"). -->

<!-- The existing table, from 0011_cost_verification.py — the shape the new column joins: -->
verification_verdict(
  id UUID PK, tenant_id UUID FK org.id CASCADE, run_id UUID FK run.id CASCADE,
  claim_id UUID NULL, verdict TEXT NOT NULL, confidence TEXT NULL,
  evidence_refs JSONB NULL, reconciliation JSONB NULL, created_at timestamptz NOT NULL
)
INDEX idx_verification_verdict_tenant_run (tenant_id, run_id)
ENABLE + FORCE ROW LEVEL SECURITY
POLICY verification_verdict_tenant_isolation
  USING (tenant_id = current_setting('app.tenant_id')::uuid)
  WITH CHECK (tenant_id = current_setting('app.tenant_id')::uuid)

<!-- RLS FACT that governs this plan's security posture: a Postgres row-level policy is a
     TABLE-level object evaluated against row values. Adding a column does NOT create a
     bypass and does NOT require a new or re-created policy. Touching the policy would be a
     regression risk, not a hardening step. -->

<!-- The consumer chain the column feeds (built in later plans, listed so the shape is fixed
     here): pipeline `_flush_groups` -> verdict dict key `superseded_note` (str, "" when the
     verdict is not superseded, per group_skeptic.py:146-148) -> persist_tribunal_claims
     INSERT (15.1-14) -> _verdict_dto (15.1-12) -> VerificationVerdictItem.superseded_note
     (already declared, runs/schemas.py:237). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the superseded_note column — migration 0012 and its ORM mirror</name>
  <files>tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py, tribunal/nestor_pulse_sdk/db/models/verification_verdict.py</files>
  <read_first>
    - `tribunal/nestor_pulse_sdk/db/models/verification_verdict.py` (the WHOLE file — the module docstring's "Columns:" list, the "Mirrors migration 0011's `verification_verdict` table EXACTLY" contract on line 9-11, the `mapped_column` idiom, and the stale verdict-vocabulary line 20)
    - `tribunal/nestor_pulse_sdk/alembic/versions/0011_cost_verification.py` (the WHOLE file — the header docstring format, `revision`/`down_revision`/`branch_labels`/`depends_on` block at lines 47-50, the `op.add_column("run", sa.Column(...))` idiom used for the three additive columns, the `verification_verdict` create_table block at lines 82-131, the RLS ENABLE/FORCE/POLICY statements, and the inverse-order `downgrade()`)
    - `tribunal/nestor_pulse_sdk/alembic/versions/0010_rls_empty_tenant.py` (for a second sample of the header + revision-block house style)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-REVIEW.md` § CR-01 (lines 80-138 — the exact statement that "`verification_verdict` has no `superseded_note` column, and the phase deliberately ships no migration, so it cannot be persisted")
    - `infra/DEPLOY-RUNBOOK.md` § Phase 15 Step 15.b (lines 1818-1856 — how a tribunal-line migration is applied live and the image-pin lesson; this plan does NOT deploy, but the migration must be written so that step works unchanged)
  </read_first>
  <action>
FIRST, before any edit, assert the worktree base is current. Executor worktrees in this repo are
historically cut from a STALE ref — 8 of the 10 agents in the original Phase 15.1 ran 62-110
commits behind master. Run `git log -1 --format=%H` and `git merge-base --is-ancestor c4b4314 HEAD
&& echo BASE_OK`. If `BASE_OK` does not print, STOP and report the stale base rather than editing
files: every line number and every "current state" claim in this plan is written against master at
`c4b4314`.

Then create `tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py`:

  - `revision: str = "0012"`, `down_revision: Union[str, None] = "0011"`, `branch_labels = None`,
    `depends_on = None` — same four-line block and same `from typing import Sequence, Union`
    import as 0011.
  - `upgrade()` performs EXACTLY ONE DDL statement:
    `op.add_column("verification_verdict", sa.Column("superseded_note", sa.Text(), nullable=True))`.
    No schema= kwarg (the migration runs with search_path set to the tribunal schema — same as
    every `op.add_column` in 0011).
  - `downgrade()` performs exactly one: `op.drop_column("verification_verdict", "superseded_note")`.
  - Do NOT emit `CREATE POLICY`, `DROP POLICY`, `ENABLE/FORCE ROW LEVEL SECURITY`, `CREATE INDEX`,
    or any `GRANT`. The table already has FORCE RLS and the
    `verification_verdict_tenant_isolation` policy from 0011; a Postgres policy is a TABLE-level
    object evaluated against row values, so a new column is covered automatically. Re-issuing any
    of those statements is a regression risk with zero security benefit.
  - Do NOT touch `audit_log` or anything the hash chain reads. State this in the header docstring:
    the column is additive, lands OUTSIDE the frozen `_payload_for_row` payload, and cannot move
    `verify_chain` off `(True, None)` or change the frozen field count of 11.
  - Header docstring must name: what the column carries (the G-07 caveat text the group skeptic
    supplies with a `superseded` verdict — what changed and from when), why it is nullable (every
    non-superseded verdict has none; existing rows predate it), that it is the TRIBUNAL alembic
    line (not the intake `nestor` line, whose 0011/0012 belong to Phases 16/17), and that it
    closes review finding CR-01's storage leg.

Then update `tribunal/nestor_pulse_sdk/db/models/verification_verdict.py`:

  - Add `superseded_note: Mapped[str | None] = mapped_column(Text, nullable=True)` immediately
    after the `reconciliation` column, so the ORM field order mirrors the physical column order.
  - Add the matching entry to the module docstring's `Columns:` list, in the same
    `  - name         TYPE  (notes)` alignment style as its neighbours.
  - Update the docstring's "Mirrors migration 0011's `verification_verdict` table EXACTLY" line to
    name 0011 AND 0012, so the mirror contract stays literally true.
  - Correct the stale vocabulary on the `verdict` line of the docstring: it reads
    `(support | refute | insufficient)` and must read `(support | refute | insufficient |
    superseded)`. The column has no CHECK constraint, so this docstring is the only place the
    verdict vocabulary is documented — leaving it three-valued while the writer persists a fourth
    value would make the file's own documentation false. This is a docstring accuracy fix on a
    line this task already owns, not new scope.
  </action>
  <acceptance_criteria>
    - `git merge-base --is-ancestor c4b4314 HEAD` exits 0 (recorded in the SUMMARY).
    - `ls tribunal/nestor_pulse_sdk/alembic/versions/ | tail -1` prints `0012_verdict_superseded_note.py`.
    - `grep -c 'revision: str = "0012"' tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py` returns 1.
    - `grep -c 'down_revision: Union\[str, None\] = "0011"' tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py` returns 1.
    - `grep -c "add_column" tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py` returns exactly 1 and `grep -c "drop_column" ...` returns exactly 1.
    - `grep -ci "POLICY\|ROW LEVEL SECURITY\|GRANT\|create_index" tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py` returns 0 outside comment lines — verify with `grep -v '^\s*#' tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py | grep -ci "op.execute\|create_index\|GRANT"` returning 0.
    - `grep -v '^\s*#' tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py | grep -c "audit_log"` returns 0 (SC3 non-regression: the frozen audit payload is untouched).
    - `grep -c "superseded_note" tribunal/nestor_pulse_sdk/db/models/verification_verdict.py` returns >= 2 (the mapped column and the docstring entry).
    - `grep -c "support | refute | insufficient | superseded" tribunal/nestor_pulse_sdk/db/models/verification_verdict.py` returns 1.
    - `python -c "import ast,sys; ast.parse(open('tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py').read())"` is NOT runnable locally (no Python on this box) — instead assert syntax by the Cloud Build gate in plan 15.1-15; do not claim a local parse.
  </acceptance_criteria>
  <verify>
    <automated>ls tribunal/nestor_pulse_sdk/alembic/versions/ | tail -1 | grep -q "0012_verdict_superseded_note.py" &amp;&amp; grep -q 'down_revision: Union\[str, None\] = "0011"' tribunal/nestor_pulse_sdk/alembic/versions/0012_verdict_superseded_note.py &amp;&amp; grep -q "superseded_note" tribunal/nestor_pulse_sdk/db/models/verification_verdict.py &amp;&amp; echo GATE_OK</automated>
  </verify>
  <done>The tribunal alembic head is `0012`, adding exactly one nullable TEXT column to `verification_verdict` with no policy/index/grant churn and no `audit_log` reference; the ORM model mirrors it and its docstring documents the column and the four-value verdict vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-register the three gap-closure test files in the no-Postgres gate</name>
  <files>tribunal/cloudbuild.test-gates.yaml</files>
  <read_first>
    - `tribunal/cloudbuild.test-gates.yaml` (the WHOLE file — in particular the header comment explaining WHY the file list is built with `ls ... 2>/dev/null || true`: files that do not exist yet are skipped instead of failing pytest collection, which is exactly the situation here; and the LOAD-BEARING `-m "not live"` filter that keeps the August calibration test from firing a real LLM call against a capped Anthropic account)
    - `tribunal/cloudbuild.test-critical.yaml` (the Postgres-provisioning sibling — so the executor can confirm the new pure test files belong in the gates config, not this one)
    - `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-UAT.md` § Phase Gate (the recorded 124-passed baseline for this config, which the closure gate is measured against in plan 15.1-15)
  </read_first>
  <action>
Add exactly three filenames to the `FILES=$$(ls ...)` list in `tribunal/cloudbuild.test-gates.yaml`,
keeping the existing entries and their `\`-continuation formatting untouched:

  - `nestor_pulse_sdk/tests/test_verdict_publication.py`  (landed by plan 15.1-12)
  - `nestor_pulse_sdk/tests/test_stage_schema.py`         (landed by plan 15.1-13)
  - `nestor_pulse_sdk/tests/test_verdict_write_path.py`   (landed by plan 15.1-14)

All three are pure: no Postgres, no provider key, no network. They therefore belong in THIS config
and not in `cloudbuild.test-critical.yaml`.

Append a short comment block above the `FILES=` assignment recording that these three arrive with
the 15.1 gap-closure plans (11-15) and that the `|| true` guard is what makes pre-registration
safe — do not remove or "tidy away" the `2>/dev/null || true`, and do not switch the list to a
glob: the explicit list is what keeps unrelated suite files out of this fast gate.

Do NOT change `-m "not live"`, the `timeout`, the machineType, or the `pip install` line.
  </action>
  <acceptance_criteria>
    - `grep -c "test_verdict_publication.py" tribunal/cloudbuild.test-gates.yaml` returns 1.
    - `grep -c "test_stage_schema.py" tribunal/cloudbuild.test-gates.yaml` returns 1.
    - `grep -c "test_verdict_write_path.py" tribunal/cloudbuild.test-gates.yaml` returns 1.
    - `grep -c 'test_gate_replay.py' tribunal/cloudbuild.test-gates.yaml` still returns 1 (no existing entry was dropped).
    - `grep -c '2>/dev/null || true' tribunal/cloudbuild.test-gates.yaml` returns 1 (the pre-registration guard survives).
    - `grep -c -- '-m "not live"' tribunal/cloudbuild.test-gates.yaml` returns 1 (the live-marker fence is intact — no LLM call fires from CI while the Anthropic monthly cap is in force until 2026-08-01).
    - `git diff --name-only` for this task lists ONLY `tribunal/cloudbuild.test-gates.yaml`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "test_verdict_write_path.py" tribunal/cloudbuild.test-gates.yaml &amp;&amp; grep -q "test_verdict_publication.py" tribunal/cloudbuild.test-gates.yaml &amp;&amp; grep -q "test_stage_schema.py" tribunal/cloudbuild.test-gates.yaml &amp;&amp; grep -q -- '-m "not live"' tribunal/cloudbuild.test-gates.yaml &amp;&amp; echo GATE_OK</automated>
  </verify>
  <done>The fast gate config lists the three new test files, keeps every prior entry, keeps the `|| true` pre-registration guard and keeps the `not live` fence, so plans 12/13/14 land tests into a gate that already collects them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alembic migration → production `tribunal` schema | DDL executed by the `tribunal-migrate` Job under the app_user DSN against live client data |
| new column → RLS policy surface | A column added to an RLS-FORCEd table must remain covered by the existing tenant policy, not become a side channel |
| CI gate config → what actually gets proven | A test file that is never collected is a test that does not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-52 | Elevation of Privilege | `verification_verdict` RLS coverage | mitigate | The migration issues NO policy statement; the 0011 `verification_verdict_tenant_isolation` policy plus FORCE RLS remain the sole gate, and a Postgres policy is table-level so the new column is covered by construction. Asserted by the "0 op.execute/GRANT/create_index" acceptance criterion |
| T-15.1-53 | Tampering | frozen audit hash chain (SC3) | mitigate | The migration names no `audit_log` column and no `_payload_for_row` field; asserted by `grep -c "audit_log" == 0`. `verify_chain` and the 11-field frozen payload are re-proven in plan 15.1-15 |
| T-15.1-54 | Denial of Service | schema/code drift between the two alembic lines | mitigate | `down_revision = "0011"` is pinned to the TRIBUNAL head verified by `ls versions/ | tail -1`; the header docstring states explicitly that the intake `nestor` 0011/0012 are a different line (Phases 16/17) |
| T-15.1-55 | Repudiation | a migration that silently no-ops live | mitigate | Applied in plan 15.1-15 via the runbook's image-pin procedure, confirming the `Running upgrade 0011 -> 0012` log line rather than a bare exit 0 |
| T-15.1-56 | Spoofing / cost | a CI-fired live LLM call while the Anthropic cap is in force | mitigate | The `-m "not live"` filter is asserted intact by an explicit acceptance criterion on the config edit |
| T-15.1-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this plan; `tribunal/requirements.txt` is not in `files_modified` |
</threat_model>

<verification>
- `ls tribunal/nestor_pulse_sdk/alembic/versions/ | tail -1` prints `0012_verdict_superseded_note.py`.
- The migration contains exactly one `add_column` and one `drop_column`, no policy/index/grant statement, and no `audit_log` reference.
- `grep -c "superseded_note" tribunal/nestor_pulse_sdk/db/models/verification_verdict.py` >= 2.
- The three new test filenames are collected by `tribunal/cloudbuild.test-gates.yaml`, with `-m "not live"` intact.
- `git diff --name-only` lists exactly the three files in `files_modified` and nothing else.
</verification>

<success_criteria>
- `superseded_note` has a real, nullable, additive TEXT column in the tribunal alembic line at revision 0012.
- The ORM model mirrors it exactly and documents the four-value verdict vocabulary.
- The existing FORCE-RLS tenant policy still covers the table; no policy statement was re-issued.
- The frozen audit payload is provably untouched by this migration.
- The fast no-Postgres gate already collects the three test files plans 12/13/14 will land.
</success_criteria>

<output>
Create `.planning/phases/15.1-research-engine-redesign-verification-gates-inserted-2026-07/15.1-11-SUMMARY.md` when done
</output>
