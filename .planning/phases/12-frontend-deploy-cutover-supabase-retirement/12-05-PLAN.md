---
phase: 12-frontend-deploy-cutover-supabase-retirement
plan: 05
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03, 12-04]
files_modified:
  - .planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-UAT.md
autonomous: false
requirements: [INFRA-05, QA-05]

must_haves:
  truths:
    - "The backend catch-up deploy is live (image rebuilt with jinja2/httpx, alembic at 0010, RESEND_API_KEY seeded, mail envs set, suite green)"
    - "The frontend is deployed as a Cloud Run container and returns SSR HTML at its run.app URL"
    - "The captured frontend URL is wired into backend CORS_ALLOWED_ORIGINS + APP_BASE_URL + bucket CORS + Firebase authorized domains"
    - "The consolidated 12-UAT parity gate is fully green for BOTH superadmin and user roles (draft → decomposed)"
    - "No Supabase key/URL is in the deployed bundle (D-11 guard green in the build)"
  artifacts:
    - path: ".planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-UAT.md"
      provides: "the parity gate, executed and marked green"
      contains: "PARITY"
  key_links:
    - from: "deployed frontend"
      to: "nestor-api"
      via: "Firebase ID token Bearer, CORS-allowed origin"
      pattern: "CORS"
---

<objective>
Execute the live cutover and drive the consolidated parity gate to green (INFRA-05 + QA-05): run the
backend catch-up deploy, build+deploy the frontend container, wire the captured run.app URL into the
four URL-dependent surfaces, then complete the two-role `draft → decomposed` E2E and every inherited
07-11 HUMAN-UAT item in `12-UAT.md`.

Purpose: All live GCP actions are user-run (dev box has no Python/Docker; real Firebase auth, mails,
AI calls). This plan is the operator checkpoint that executes the Plan-04 runbook against live GCP and
records the parity outcome. Independence (D-08) is proven code-side — no Supabase actions occur here.
Output: A deployed frontend + wired backend + a green `12-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-UAT.md
@infra/DEPLOY-RUNBOOK.md
@.planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run the live backend catch-up + frontend deploy + URL wiring (Plan-04 runbook)</name>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md (the Phase 12 section authored in Plan 04 — Steps 12.1-12.5)
    - .planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-CONTEXT.md (D-04 scope, D-08 no Supabase actions)
  </read_first>
  <action>Execute the Plan-04 DEPLOY-RUNBOOK Phase 12 steps (12.1-12.5) live via gcloud/Cloud Build/Firebase Console: backend catch-up rebuild, NDA asset drop, frontend build+deploy, capture FRONTEND_URL, wire the four URL surfaces. Claude cannot run these; the operator does. See how-to-verify for the ordered steps.</action>
$ACT1
  <what-built>
    Plans 01-04 produced the frontend container (node-server preset Dockerfile + cloudbuild.yaml + D-11
    bundle guard), the sources-read/transcribe gap closure, the IaC frontend service, and the complete
    Phase-12 DEPLOY-RUNBOOK section. This step executes that runbook live (Claude cannot run gcloud/Cloud
    Build/Firebase Console here; the operator runs them).
  </what-built>
  <how-to-verify>
    Follow `infra/DEPLOY-RUNBOOK.md` § Phase 12 in order:
    1. Step 12.1 — Backend catch-up: `gcloud builds submit` REBUILDS the nestor-api image (jinja2/httpx),
       run the alembic 0010 migration Job, confirm RESEND_API_KEY secret version exists, set APP_BASE_URL
       + NESTOR_ADMIN_EMAIL, run the full backend suite in Cloud Build. Confirm: the suite is green and
       the deployed rev reports alembic at head 0010 (no ModuleNotFoundError on a mail call).
    2. Step 12.2 — Drop the NDA PDF at `frontend/public/templates/NDA/Agenic-Nestor-Overeenkomst.pdf`.
    3. Step 12.3 — Build+deploy the frontend via `gcloud builds submit frontend --config=cloudbuild.yaml`
       (passing the VITE_ substitutions, NO Supabase vars) then `gcloud run deploy nestor-frontend`.
       Capture the printed `Service URL` = FRONTEND_URL. Confirm the build's D-11 guard step passed
       (no Supabase signature in `.output/`).
    4. Step 12.4 — Wire FRONTEND_URL: backend `CORS_ALLOWED_ORIGINS` + `APP_BASE_URL`, uploads-bucket
       CORS, Firebase Console authorized domains.
    5. Step 12.5 — `curl <FRONTEND_URL>` returns SSR HTML.
    Do NOT take ANY Supabase-side action (D-08). Do NOT touch the legacy Lovable/Cloudflare deploy (D-10).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms: backend suite green + alembic 0010 live; frontend deployed and curls SSR HTML at FRONTEND_URL; D-11 guard passed in the build; CORS/APP_BASE_URL/bucket-CORS/Firebase-authorized-domains all wired to the captured URL; no Supabase action taken.</human-check>
  </verify>
  <acceptance_criteria>
    - The backend suite ran green in Cloud Build and the live nestor-api reports alembic head 0010.
    - `curl <FRONTEND_URL>` returns SSR HTML (200).
    - The frontend build log shows the D-11 bundle guard passed (no Supabase signature).
    - Backend `CORS_ALLOWED_ORIGINS` and `APP_BASE_URL` equal the captured FRONTEND_URL; the uploads bucket CORS includes it; the run.app host is in Firebase authorized domains.
    - No Supabase-side action was taken (D-08); the legacy frontend was left alone (D-10).
  </acceptance_criteria>
  <resume-signal>Reply "deployed" with the captured FRONTEND_URL, or describe any deploy failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Complete the two-role parity gate and mark 12-UAT green</name>
  <read_first>
    - .planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-UAT.md (the consolidated checklist from Plan 01 — every inherited item + the two-role E2E)
    - .planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-CONTEXT.md (D-05 parity = nothing outstanding; D-06 seeded test space + real invite flow)
  </read_first>
  <action>Run the consolidated 12-UAT parity checklist against the live FRONTEND_URL for both roles (superadmin + invited test user), ticking every inherited 07-11 item plus the two-role draft-to-decomposed E2E, and mark the gate PARITY GREEN. See how-to-verify for the ordered steps.</action>
$ACT2
  <what-built>
    The deployed, wired GCP stack from Task 1. This step runs the consolidated parity checklist against
    it for both roles and records the result.
  </what-built>
  <how-to-verify>
    Against the live FRONTEND_URL, work through `12-UAT.md`:
    1. Superadmin role: run a full `draft → … → decomposed` intake (auth, isolation, AI ports incl.
       structure/extract/embeddings/transcribe, SSE progress, storage upload/download/delete, i18n
       NL/FR/EN switching). Confirm the flow STOPS at `decomposed` (run-research/Tribunal unreachable).
    2. User role: create a dedicated test client space; invite a test account through the REAL invite
       flow (invitation mail → set-password → login, D-06); run the same intake as that user. Verify
       tenant isolation: the user sees ONLY their space; a cross-space intake/SSE stream returns
       scoped-empty/404 in a real browser (08-UAT #3).
    3. Tick EVERY inherited 07-11 item (transcribe E2E, keyless audio download, delete CORS-DELETE
       click-through, superadmin audio upload, edit-mode deferred-delete, RecipientPicker, invite
       click-through, NL/FR/EN switch+persistence+carry, invite-email locale, FR/EN tone, backend suite).
    Mark each `- [ ]` → `- [x]` as it passes; the gate is PARITY GREEN only when all boxes are ticked.
    Record any failure as a gap (feeds a `/gsd:plan-phase --gaps` follow-up if needed).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms every 12-UAT checkbox is ticked for BOTH roles, the flow stops at decomposed, tenant isolation holds cross-space in a real browser, and the gate line reads PARITY GREEN.</human-check>
  </verify>
  <acceptance_criteria>
    - Every `- [ ]` item in `12-UAT.md` is marked `- [x]` (superadmin + user), including all inherited 07-11 items and the two-role E2E.
    - The user-role account was provisioned through the REAL invite flow (invitation mail + set-password + login), not seeded directly (D-06).
    - A cross-space read/SSE attempt in a real browser returned scoped-empty/404 (isolation proven, 08-UAT #3).
    - Both role runs reached `decomposed` and no `run-research`/Tribunal action was reachable.
    - The `12-UAT.md` gate line reads PARITY GREEN (or open gaps are recorded for a --gaps follow-up).
  </acceptance_criteria>
  <resume-signal>Reply "parity green" when all 12-UAT items pass for both roles, or list the failing items.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user browser (2 roles) → live GCP stack | real auth + tenant isolation exercised end-to-end |
| cross-space access attempt | a user must never read another space's data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-15 | Elevation of Privilege / Info Disclosure | cross-tenant access | mitigate | The user-role run explicitly probes cross-space read + SSE in a real browser (08-UAT #3) and must get scoped-empty/404 — isolation proven live before the gate greens (Task 2). |
| T-12-16 | Spoofing | invite/auth flow | mitigate | The test user comes in through the REAL invite flow (mail → set-password → login), validating Phase-10 invite mail + Phase-5 set-password + Firebase auth on the live wired stack (Task 2, D-06). |
| T-12-17 | Elevation of Privilege | scope ceiling | mitigate | Both role runs must STOP at `decomposed`; the operator confirms run-research/Tribunal is unreachable (INTAKE-05 scope guard, Task 2). |
| T-12-18 | Information Disclosure | deployed bundle | mitigate | The D-11 guard ran in the frontend build (Task 1); the operator confirms it passed, proving no Supabase key/URL shipped (success criterion 1). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs (deploy + UAT only). Per 12-RESEARCH this phase installs nothing new; no slopcheck checkpoint required. |
</threat_model>

<verification>
- Task 1: backend suite green + alembic 0010 live; frontend curls SSR HTML; D-11 guard passed; URL wired to CORS/APP_BASE_URL/bucket-CORS/Firebase.
- Task 2: every 12-UAT item ticked for both roles; cross-space isolation proven; gate reads PARITY GREEN.
</verification>

<success_criteria>
- INFRA-05: the SSR frontend runs as a Cloud Run container against the new backend with no Supabase key in the bundle.
- QA-05: the `draft → decomposed` flow is validated on GCP for both superadmin and user; the GCP stack is fully independent (no Supabase env vars/calls/keys), and no Supabase-side action was taken (D-08).
</success_criteria>

<output>
Create `.planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-05-SUMMARY.md` when done.
</output>
