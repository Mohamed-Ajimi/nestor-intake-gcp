---
phase: 12-frontend-deploy-cutover-supabase-retirement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/intake_routes.py
  - backend/tests/test_intake_routes.py
  - frontend/src/lib/api/sources.ts
  - frontend/src/components/intake/AISkillsPanel.tsx
  - frontend/src/locales/en/intake.json
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
autonomous: true
requirements: [QA-05]

must_haves:
  truths:
    - "A GET intakes/{id}/sources endpoint returns the intake's audio sources, space-scoped server-side (existence-hidden)"
    - "The transcribe CTA is no longer permanently disabled — it wires each audio source id to skills.transcribeSource"
    - "A cross-tenant/missing intake returns a scoped-empty sources list (never a distinguishable 403)"
  artifacts:
    - path: "backend/app/api/intake_routes.py"
      provides: "GET /{intake_id}/sources read projection"
      contains: "def list_intake_sources"
    - path: "frontend/src/lib/api/sources.ts"
      provides: "sources read seam over apiFetch"
      contains: "getIntakeSources"
    - path: "frontend/src/components/intake/AISkillsPanel.tsx"
      provides: "per-source transcribe wiring (CTA no longer hard-disabled)"
  key_links:
    - from: "frontend/src/lib/api/sources.ts"
      to: "/intakes/{id}/sources"
      via: "apiFetch GET"
      pattern: "/sources"
    - from: "frontend/src/components/intake/AISkillsPanel.tsx"
      to: "skills.transcribeSource"
      via: "per-source transcribe trigger"
      pattern: "transcribeSource"
---

<objective>
Close the two real D-12 residuals blocking the parity gate: (1) add the missing sources-read surface
`GET /intakes/{intake_id}/sources` (space-scoped, existence-hidden) that the transcribe CTA needs, and
(2) wire the frontend transcribe CTA to each returned audio source id so it is no longer permanently
`disabled` — unblocking 07-UAT #7 and 09-UAT #8 (audio transcription E2E).

Purpose: 12-PATTERNS confirms almost all D-12 gap-closure is ALREADY implemented (context-pack read,
progress UX, copy-intake-link, template-asset scheme). The ONE code residual is the sources-read
endpoint + transcribe wiring — the transcribe dispatch already exists (`skills.transcribeSource`,
`POST /intakes/{id}/sources/{source_id}/transcribe`); only the `source_id` read is missing.
Output: A new scoped read endpoint + its test, a small frontend read seam, and a wired transcribe CTA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-PATTERNS.md
@backend/app/api/intake_routes.py
@backend/app/db/models/sources.py
@backend/app/db/repository.py
@frontend/src/lib/api/contextPack.ts
@frontend/src/components/intake/AISkillsPanel.tsx

<interfaces>
Existing backend building blocks (VERIFIED — reuse, do not rebuild):
  - IntakeSourceRepository(TenantRepository[IntakeSource]) with `.list_for_intake(intake_id)`
    (backend/app/db/repository.py:526-537) — already space-scoped.
  - Dependency provider get_intake_and_source_repos (backend/app/db/session.py:146) yields an
    IntakeSourceRepository under Depends(get_current_identity). A single-repo provider can be added
    next to get_skill_run_repo (session.py:185) mirroring its shape if a cleaner Depends is preferred.
  - IntakeSource columns (backend/app/db/models/sources.py): id, space_id, intake_id, kind
    ('audio'|'document'|...), storage_bucket, storage_path, file_name, language, created_at.
  - Endpoint-to-mirror: list_skill_runs (intake_routes.py:507) — `@intake_router.get("/{intake_id}/...")`,
    scoped repo via Depends, returns a Pydantic *View projection built from a helper `_..._view`.
  - Existence-hidden read contract (get_context_pack, intake_routes.py:529): scoped repo applies the
    per-user space WHERE (superadmin bypass via 0003); a cross-tenant/missing intake yields a
    scoped-empty result — a scoped-empty 200, never a distinguishable 403 (no BOLA/IDOR).

Existing transcribe dispatch (VERIFIED — already present, just needs a source_id fed in):
  - Backend: POST /intakes/{intake_id}/sources/{source_id}/transcribe (ai_routes.py:170).
  - Frontend: skills.transcribeSource(intakeId, sourceId) (frontend/src/lib/api/skills.ts:38).

Frontend read-seam analog: frontend/src/lib/api/contextPack.ts — apiFetch<T> GET, never fork the
transport, add no authorization decision (render whatever the scoped endpoint returns).

AISkillsPanel current transcribe residual (AISkillsPanel.tsx:107-127): the CTA is `disabled` with
title `t("aiSkills.transcribeDisabledTitle")` and passes an empty id. i18n key
`aiSkills.transcribeDisabledTitle` exists in en/nl/fr intake.json (line ~229 in en).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GET /intakes/{intake_id}/sources scoped read endpoint + test</name>
  <read_first>
    - backend/app/api/intake_routes.py (list_skill_runs :507 and get_context_pack :529 — mirror the endpoint shape, the Depends-scoped repo, the *View projection helper, and the existence-hidden docstring)
    - backend/app/db/repository.py (IntakeSourceRepository :526 + list_for_intake :537; get_intake_and_source_repos in session.py for the provider)
    - backend/app/db/session.py (get_skill_run_repo :185 — the single-repo provider pattern to mirror if adding get_intake_source_repo)
    - backend/app/db/models/sources.py (IntakeSource columns to project)
    - backend/tests/test_intake_routes.py (how endpoints are exercised: scoped client fixtures, in-scope vs cross-tenant assertions)
    - backend/tests/test_intake_cross_tenant.py (the cross-tenant existence-hidden assertion pattern)
  </read_first>
  <files>backend/app/api/intake_routes.py, backend/tests/test_intake_routes.py</files>
  <behavior>
    - Test 1: An in-scope intake with N audio sources → `GET /intakes/{id}/sources` returns 200 with a
      list of N items, each carrying id, kind, file_name, language, created_at (NO space_id/storage_bucket
      leaked in the projection).
    - Test 2: An in-scope intake with no sources → returns 200 with an empty list.
    - Test 3: A user requesting another space's intake id → returns a scoped-empty 200 (empty list),
      NOT a 403 and NOT a distinguishable error (existence-hidden, mirrors get_context_pack).
    - Test 4: A superadmin → sees the sources for any space's intake (0003 bypass).
  </behavior>
  <action>
    Add `@intake_router.get("/{intake_id}/sources")` `def list_intake_sources(intake_id, repo=Depends(...))`
    mirroring `list_skill_runs`. Use the space-scoped IntakeSourceRepository (either via the existing
    `get_intake_and_source_repos` provider or a new single-repo `get_intake_source_repo` in session.py
    mirroring `get_skill_run_repo` — pick the cleaner Depends; document the choice in the summary).
    Call `repo.list_for_intake(intake_id)` and project each row through a new `_intake_source_view` helper
    into a Pydantic `IntakeSourceView(BaseModel)` exposing ONLY: id (str), kind, file_name, language,
    created_at — NEVER space_id, storage_bucket, or storage_path (no tenant/storage identifiers leak,
    same discipline as _context_pack_view). Return a list (or a `{ sources: [...] }` wrapper — match
    whatever shape the frontend seam in Task 2 consumes; keep them consistent). Add the existence-hidden
    docstring: the scoped repo's per-user space WHERE means a cross-tenant/missing intake reads
    scoped-empty (200 empty list), never a distinguishable 403. Mount under the existing intake_router
    (inherits protected_router's Depends(get_current_identity) via app/main.py). Write the four tests in
    test_intake_routes.py first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_intake_routes.py -k sources -x</automated>
    <note>This task itself creates the tests (TDD RED→GREEN), so the command is valid once the task completes. It executes in Cloud Build per the project pattern — the dev box has no Python.</note>
  </verify>
  <acceptance_criteria>
    - `intake_routes.py` contains `def list_intake_sources` mounted at `GET /{intake_id}/sources` under `intake_router`.
    - The projection (`IntakeSourceView` / `_intake_source_view`) exposes id, kind, file_name, language, created_at and does NOT contain `space_id`, `storage_bucket`, or `storage_path` fields (grep the view class).
    - `test_intake_routes.py` has tests asserting: in-scope list returns the sources; cross-tenant returns a scoped-empty list (200, not 403); superadmin sees any space's sources.
    - The endpoint uses a space-scoped repo via `Depends` (no client-supplied space param).
  </acceptance_criteria>
  <done>A space-scoped, existence-hidden GET /intakes/{id}/sources endpoint exists with tests proving in-scope read, empty-scope, cross-tenant denial (scoped-empty), and superadmin bypass.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend sources read seam + wire the transcribe CTA per source</name>
  <read_first>
    - frontend/src/lib/api/contextPack.ts (the apiFetch GET read-seam analog — mirror its shape/comments)
    - frontend/src/lib/api/skills.ts (transcribeSource(intakeId, sourceId) — the dispatch already exists)
    - frontend/src/components/intake/AISkillsPanel.tsx (the disabled transcribe block at :107-127; the `run(key, startedMsg, trigger)` helper at :30-47)
    - frontend/src/locales/en/intake.json (the `aiSkills` block ~:217-231 — existing transcribe keys)
    - frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json (same `aiSkills` block for parallel keys)
  </read_first>
  <files>frontend/src/lib/api/sources.ts, frontend/src/components/intake/AISkillsPanel.tsx, frontend/src/locales/en/intake.json, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json</files>
  <action>
    Create `frontend/src/lib/api/sources.ts` mirroring `contextPack.ts`: an `IntakeSourceView` type
    matching the backend projection (id, kind, file_name, language, created_at) and a
    `getIntakeSources(intakeId): Promise<ApiResult<...>>` calling `apiFetch` GET `/intakes/${intakeId}/sources`
    (match the exact response shape returned by Task 1 — list vs `{ sources }`). Never fork the transport;
    add no authorization decision (render whatever the scoped endpoint returns).
    In `AISkillsPanel.tsx`: fetch the intake's sources on mount (a small `useState` + `useEffect` calling
    `getIntakeSources(intakeId)`, filtering to `kind === "audio"`). Replace the hard-`disabled` transcribe
    block (:107-127) with: if there are no audio sources, keep a disabled CTA whose title uses the existing
    `aiSkills.transcribeDisabledTitle` key; if there ARE audio sources, render an ENABLED transcribe control
    that lets the operator transcribe a source — either one button per audio source (labeled with
    `file_name`) or a single button that transcribes the first/selected source — each calling
    `run("transcribe", t("aiSkills.transcribeStarted"), () => skills.transcribeSource(intakeId, source.id))`.
    Remove the stale "empty id is unreachable" comment. Add any NEW i18n key you introduce (e.g. a
    per-source label like `aiSkills.transcribeSourceBtn`) to en/nl/fr intake.json `aiSkills` blocks in
    parallel (Phase-11 discipline — no hardcoded Dutch/English strings; the ci_no_hardcoded_dutch guard
    must stay green). Reuse `skills.transcribeSource` — do NOT add a new dispatch.
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -5 && node -e "['en','nl','fr'].forEach(l=>{const a=require('./src/locales/'+l+'/intake.json').aiSkills; if(!a) throw new Error('missing aiSkills '+l);}); console.log('i18n-parallel-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/api/sources.ts` exists, exports `getIntakeSources`, and calls `apiFetch` GET on `/intakes/${intakeId}/sources` (reuses the shared transport).
    - `AISkillsPanel.tsx` imports and calls `getIntakeSources`, filters `kind === "audio"`, and the transcribe control is NOT unconditionally `disabled` — it is enabled when audio sources exist and calls `skills.transcribeSource(intakeId, <realSourceId>)` (no empty-string id passed when enabled).
    - Any new i18n key added to `aiSkills` exists in all three locale files (`en`/`nl`/`fr`) — the node check prints `i18n-parallel-ok`.
    - `npm run lint` passes; `bash scripts/ci_no_hardcoded_dutch.sh frontend/src` stays green (no hardcoded strings introduced).
    - The stale "empty id is unreachable" comment is removed.
  </acceptance_criteria>
  <done>The transcribe CTA reads real audio sources and dispatches per source id; the disabled-only state remains only when no audio source exists; i18n stays parallel across en/nl/fr.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /intakes/{id}/sources | untrusted `intake_id` path param crosses into a tenant-scoped read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure (BOLA/IDOR) | GET /{intake_id}/sources | mitigate | Space scope derives from the verified Identity ONLY (scoped repo `Depends`), never a client param; a cross-tenant/missing intake reads scoped-empty (200 empty list), never a distinguishable 403 (Task 1, mirrors get_context_pack T-7-09-01). Test 3 proves it. |
| T-12-08 | Information Disclosure | IntakeSourceView projection | mitigate | The projection exposes only id/kind/file_name/language/created_at — NO space_id/storage_bucket/storage_path (no tenant/storage identifiers leak to the browser) (Task 1). |
| T-12-09 | Elevation of Privilege | transcribe dispatch | mitigate | Reuses the existing `POST /sources/{source_id}/transcribe` which is itself space-scoped server-side; the frontend passes only a source id it just read from the scoped list — no new authz surface (Task 2). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs; reuses existing apiFetch + repositories. Per 12-RESEARCH this phase installs nothing new; no slopcheck checkpoint required. |
</threat_model>

<verification>
- Backend (Cloud Build): `pytest tests/test_intake_routes.py -k sources` green — in-scope, empty, cross-tenant scoped-empty, superadmin.
- Frontend: `npm run lint` clean; sources.ts exports getIntakeSources; transcribe CTA enabled when audio sources exist; i18n parallel across en/nl/fr; ci_no_hardcoded_dutch green.
</verification>

<success_criteria>
- The sources-read surface exists and is tenant-safe; the transcribe CTA is wired to real source ids,
  unblocking the audio transcription items in the consolidated parity gate (07-UAT #7, 09-UAT #8).
</success_criteria>

<output>
Create `.planning/phases/12-frontend-deploy-cutover-supabase-retirement/12-03-SUMMARY.md` when done.
</output>
