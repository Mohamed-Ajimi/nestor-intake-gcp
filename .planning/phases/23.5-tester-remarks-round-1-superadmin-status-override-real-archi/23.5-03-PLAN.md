---
phase: 23.5-tester-remarks-round-1-superadmin-status-override-real-archi
plan: 03
type: execute
wave: 3
depends_on: ["23.5-02"]
files_modified:
  - frontend/src/locales/en/admin.json
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/fr/admin.json
  - frontend/src/locales/en/intake.json
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/routes/admin.pulse.intakes.new.tsx
  - backend/app/ai/skills/apply.py
  - backend/app/ai/skills/context_pack.py
  - backend/app/ai/skills/extract_insights.py
  - backend/app/api/intake_routes.py
autonomous: false
requirements: [TR4-NAMING]

must_haves:
  truths:
    - "The create-intake form asks for a PROJECT name, not a client name, in all three languages"
    - "The intakes list column and search placeholder say project, not client, in all three languages"
    - "Every operator-facing use of the word 'space' reads as 'client' in all three languages"
    - "The AI skills' markdown context header says Project, not Klantnaam"
    - "The client's own intake form still calls its organisation field 'Klantnaam (organisatie)' — unchanged"
    - "No locale KEY is renamed, so 3-way parity is preserved by construction"
    - "Create intake is not clickable with no client selected"
  artifacts:
    - path: "frontend/src/locales/en/admin.json"
      provides: "project-name and client wording (values only)"
    - path: "backend/app/ai/skills/apply.py"
      provides: "the corrected markdown context header"
    - path: "frontend/src/routes/admin.pulse.intakes.new.tsx"
      provides: "the no-client-selected guard on the create button"
  key_links:
    - from: "frontend/scripts/i18n-audit.mjs"
      to: "frontend/src/locales/{nl,fr,en}/{admin,intake}.json"
      via: "CHECK A 3-way key parity + CHECK B literal key resolution"
      pattern: "node scripts/i18n-audit.mjs"
---

<objective>
Remark 4, label-only (D-23.5-03): "CLIENT NAME" becomes "PROJECT NAME" and "SPACE" becomes
"CLIENT", in Dutch, French and English, in the frontend copy and in the AI skills' context
header. The `intakes.client_name` column, every locale KEY and the client's own
`client_info.client_name` form field are untouched — there is no migration and no key churn.

Purpose: the space already IS the client, so the free-text field the operator fills in is a
project name. The current copy makes an operator believe they are naming a second client.

Output: value-only locale edits in six JSON files, three backend skill headers, one UI guard,
and a human read of all three languages — because the 2026-08-13 lesson is that locale gates
prove keys exist, never that copy is TRUE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23.5-tester-remarks-round-1-superadmin-status-override-real-archi/23.5-CONTEXT.md
@.planning/phases/23.5-tester-remarks-round-1-superadmin-status-override-real-archi/23.5-02-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- The exact surface, enumerated at HEAD. -->

`frontend/src/locales/en/admin.json` — the strings that change VALUE (keys stay):
```
nav.manageSpaces                     "spaces"
spaces.title/.subtitle/.new/.empty.title/.empty.body/.deactivate.title/.deactivate.body/.toast.deactivated
spaceForm.createTitle/.editTitle
templates.subtitle/.spaceLabel/.spacePlaceholder/.emptyTitle/.chooseSpace
invite.spaceLabel/.spacePlaceholder/.chooseSpace
users.subtitle/.emptyTitle/.colSpace
spaceSwitcher.*
intakesNew.title/.subtitle/.clientNameLabel/.clientNamePlaceholder/.clientNameRequired/.clientLabel
intakesList.colClient/.searchPlaceholder/.subtitleFiltered
```
`grep -ic space frontend/src/locales/en/admin.json` == 26 at HEAD (the audit surface size).

`frontend/src/locales/en/intake.json` — 4 occurrences of "space" at lines 87, 96, 170, 323.

`backend/app/ai/skills/apply.py:96` and `context_pack.py:111`:
```
f"**Klantnaam**: {client_name or ''}",
```
`backend/app/ai/skills/extract_insights.py:120`:
```
lines.append(f"Klant: {client_name}")
```

DO NOT TOUCH `backend/app/data/pulse_intake_v1.json:101` — `"nl": "Klantnaam (organisatie)"`.
Inside the client's own form that label correctly means the client's organisation.

`backend/app/api/intake_routes.py:878-881` — the four Dutch subject constants, ported verbatim
from the legacy `send-pulse-mail.ts` and pinned in `backend/tests/conftest.py:778`,
`backend/tests/test_mail_render.py:360,365` and `backend/tests/test_mail_locale.py:176`.

`frontend/src/routes/admin.pulse.intakes.new.tsx:26-50` — `clientName` state, `validate()`,
`createIntake({ client_name })`. The active space comes from `withActiveSpace` in
`frontend/src/lib/active-space.tsx`; with no space selected the backend answers 422
("Select a client (space) before creating an intake.", `intake_routes.py` create handler).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: locale values — project name, and space becomes client, in nl/fr/en</name>
  <files>frontend/src/locales/en/admin.json, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/intake.json, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/routes/admin.pulse.intakes.new.tsx</files>
  <read_first>
    - frontend/src/locales/en/admin.json in full, then nl/admin.json and fr/admin.json (the three must stay key-identical)
    - frontend/src/locales/en/intake.json lines 80-100, 165-175, 318-328
    - frontend/src/routes/admin.pulse.intakes.new.tsx in full
    - frontend/src/lib/active-space.tsx (how the active space is read, so the guard reads the same source)
    - frontend/scripts/i18n-audit.mjs lines 1-40 (what CHECK A/B/C actually prove, and what they do not)
    - .planning/phases/23.5-*/23.5-CONTEXT.md § Remark 4
  </read_first>
  <action>
    Change locale VALUES only. Do not add, remove or rename a single key: parity is then
    preserved by construction and CHECK A cannot go red for this plan's reasons.

    Group A — project name. In `intakesNew.*`: the title, subtitle, `clientNameLabel`,
    `clientNamePlaceholder`, `clientNameRequired`, `clientLabel` and `createdBody` must read as
    a PROJECT name. EN `clientNameLabel` becomes "Project name"; the placeholder must stop
    being a company example ("e.g. Acme Inc.") and become a project example; the subtitle must
    stop saying "Give the client a name" and say that the intake is created for the active
    client under a project name. In `intakesList.*`: `colClient` becomes the project column and
    `searchPlaceholder` searches project or client. Same in nl and fr, written as native copy
    and not as a word-for-word translation of the English.

    Group B — space becomes client. Every operator-facing "space"/"spaces" string listed in
    `<interfaces>` becomes "client"/"clients" (nl: "klant"/"klanten"; fr: "client"/"clients"),
    across `nav.manageSpaces`, `spaces.*`, `spaceForm.*`, `templates.*`, `invite.*`, `users.*`,
    `spaceSwitcher.*` in `admin.json`, and the four "space" strings in `intake.json`. The route
    is already `/admin/pulse/clients`, so the copy is what is lagging, not the URL.

    Group C — the create guard (the cheap item recorded in CONTEXT § "Also seen"). In
    `admin.pulse.intakes.new.tsx`, read the active space the same way `createIntake` does and
    render the submit button `disabled` when none is selected, with an inline hint from a new
    key `intakesNew.noClientSelected` added to all three locale files. This is the ONE new key
    in this plan; add it to all three files in the same edit so parity never goes red.

    Nothing else in `new.tsx` changes: the state variable may keep the name `clientName`
    (it maps to the `client_name` column, which is not being renamed), and the comment above it
    must say so explicitly so the next reader does not "fix" the mismatch.
  </action>
  <acceptance_criteria>
    - `cd frontend && node scripts/i18n-audit.mjs` exits 0 (CHECK A/B/C clean)
    - `cd frontend && npx tsc --noEmit` exits 0 and `npx vitest run` exits 0
    - `node -e "const a=require('./frontend/src/locales/en/admin.json'),b=require('./frontend/src/locales/nl/admin.json'),c=require('./frontend/src/locales/fr/admin.json');const k=o=>JSON.stringify(Object.keys(JSON.parse(JSON.stringify(o))).sort());const flat=(o,p='',out=[])=>{for(const[x,v]of Object.entries(o)){const q=p?p+'.'+x:x;typeof v==='object'&&v?flat(v,q,out):out.push(q)}return out.sort().join('|')};if(flat(a)!==flat(b)||flat(a)!==flat(c))process.exit(1)"` exits 0 (3-way key parity, checked independently of the audit script)
    - `grep -ic '\"[^\"]*space' frontend/src/locales/en/admin.json` — every remaining match is a KEY name, not a value: `node -e "const d=require('./frontend/src/locales/en/admin.json');const flat=(o,p='',out=[])=>{for(const[x,v]of Object.entries(o)){const q=p?p+'.'+x:x;typeof v==='object'&&v?flat(v,q,out):out.push([q,v])}return out};const bad=flat(d).filter(([,v])=>/space/i.test(v));if(bad.length){console.log(bad);process.exit(1)}"` exits 0
    - The same value-side check exits 0 for nl (`ruimte`/`space`) and fr (`espace`/`space`), and for all three `intake.json` files
    - `node -e "const d=require('./frontend/src/locales/en/admin.json');if(!/project/i.test(d.intakesNew.clientNameLabel))process.exit(1);if(!/project/i.test(d.intakesList.colClient))process.exit(1)"` exits 0, and the nl/fr equivalents assert `project`/`projet`
    - `for L in en nl fr; do node -e "const d=require('./frontend/src/locales/$L/admin.json'); if(!d.intakesNew.noClientSelected) process.exit(1)"; done` exits 0
    - `grep -c 'disabled' frontend/src/routes/admin.pulse.intakes.new.tsx` >= 1 and the disabled condition references the active space
    - `git diff --stat frontend/src/components/ui/` is EMPTY
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && node scripts/i18n-audit.mjs && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>The operator UI says project where it means project and client where it means client, in three languages, with zero key churn and one deliberately added key.</done>
</task>

<task type="auto">
  <name>Task 2: backend copy — the AI skills' context header, and a per-string audit of the mail subjects</name>
  <files>backend/app/ai/skills/apply.py, backend/app/ai/skills/context_pack.py, backend/app/ai/skills/extract_insights.py, backend/app/api/intake_routes.py</files>
  <read_first>
    - backend/app/ai/skills/apply.py lines 80-130 (the markdown context builder AND the prompt/system text that consumes it)
    - backend/app/ai/skills/context_pack.py lines 95-140 (same)
    - backend/app/ai/skills/extract_insights.py lines 105-140 (same)
    - backend/app/api/intake_routes.py lines 874-915 (`_SUBJECT_*` and `_SUBJECTS`) and 1235-1270 (`_run_intake_send`, where `client = intake.client_name or "team"`) and 1588-1612 (`_send_admin_validated`)
    - backend/app/mail/templates/ (every template that interpolates `client_name` or `project_title`)
    - backend/tests/conftest.py line ~778, backend/tests/test_mail_render.py lines 355-370, backend/tests/test_mail_locale.py line ~176 — the three places the current subject strings are pinned
    - backend/app/data/pulse_intake_v1.json line 101 — the string that must NOT change
  </read_first>
  <action>
    Skill headers. In `apply.py` and `context_pack.py` change the markdown line
    `**Klantnaam**: {client_name or ''}` to a project label (`**Projectnaam**:`), and in
    `extract_insights.py` change `Klant: {client_name}` likewise. BEFORE editing, grep each
    skill's own prompt/system text and its output parser for the literal `Klantnaam` and for
    `Klant:` and record the result in the SUMMARY: if any prompt instructs the model to look
    for that header, the header and the instruction move together in the same edit. The header
    is context the model reads, not a parsed key, and the proof of that is the grep, not an
    assumption.

    Mail subjects — an AUDIT, not a blanket rewrite. For each of the four `_SUBJECT_*`
    constants and for each mail template that interpolates `client_name` / `project_title`,
    decide and RECORD one of two outcomes:
      * the interpolated value is the SUBJECT of a sentence ("Klant heeft gevalideerd — {client}",
        "Onderzoeksresultaten klaar — {client}") — a project name reads correctly in that slot,
        so leave the string byte-unchanged;
      * the surrounding words LABEL the value as the client (e.g. a template line reading
        "Klant: {{ client_name }}") — reword the label to name the project.
    Any string that is changed must have its pinned test updated in the SAME edit
    (`conftest.py:778`, `test_mail_render.py:360,365`, `test_mail_locale.py:176`), and the mail
    suites must be green afterwards. Write the per-string decision table into the SUMMARY —
    "no change" is a recorded decision here, not an omission.

    Do NOT touch `backend/app/data/pulse_intake_v1.json`. Do NOT rename the
    `intakes.client_name` column, the `IntakeCreate.client_name` field, the
    `IntakePatch.client_name` field or the `IntakeView.client_name` field — D-23.5-03 is a
    label change and there is no migration in this phase.
  </action>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_mail_render.py tests/test_mail_locale.py tests/test_mail_endpoints.py tests/test_intake_validate_mail.py -x` exits 0
    - `cd backend && python -m pytest tests/test_ai_apply_skill.py tests/test_ai_context_pack.py tests/test_ai_structure_extract.py -x` exits 0
    - `grep -rn 'Klantnaam' backend/app/ai/` returns NOTHING
    - `grep -n 'Klantnaam' backend/app/data/pulse_intake_v1.json` returns exactly one line (line 101, unchanged) — `git diff backend/app/data/pulse_intake_v1.json` is EMPTY
    - `grep -rn 'client_name' backend/app/db/models/intake.py` is unchanged — `git diff backend/app/db/models/` is EMPTY
    - `git diff backend/app/db/alembic/` is EMPTY
    - The SUMMARY contains a table with one row per `_SUBJECT_*` constant and per client-name-interpolating template, each row carrying "changed" or "unchanged" and a reason
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_mail_render.py tests/test_mail_locale.py tests/test_mail_endpoints.py tests/test_ai_apply_skill.py tests/test_ai_context_pack.py -x</automated>
  </verify>
  <done>The AI context header names the project, the mail copy has been audited string by string with a recorded decision each, and no schema, column or template-field name moved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: read all three languages in a browser</name>
  <what-built>Locale values across `admin.json` and `intake.json` in nl/fr/en: "Client name" is now "Project name" on the create form, the intakes list column and search read as project, and every operator-facing "space" reads as "client". The AI skills' markdown context header names the project. The create button is disabled with no client selected.</what-built>
  <how-to-verify>
    1. `cd frontend && npm ci && npm run dev`, open the dev server, log in as superadmin.
    2. Switch the UI language to NL, then FR, then EN (the language switcher in the admin shell). For each language, visit and READ:
       - `/admin/pulse/clients` — the page title, subtitle, the "new" button, the empty state, the deactivate dialog
       - `/admin/pulse/intakes` — the column headers, the search placeholder, the subtitle, the show-archived toggle
       - `/admin/pulse/intakes/new` — the title, subtitle, field label, placeholder, the required-field error (submit empty), and the disabled state with NO client selected
       - the users page and the invite dialog — the client/space label
    3. Confirm for each language that: nothing still says space/ruimte/espace where it means a client; the create form asks for a PROJECT name and its example is a project, not a company; the copy reads as native NL / FR, not as machine translation.
    4. The gates already passed — `i18n-audit.mjs` proves the keys resolve. It does NOT prove the sentences are true. In phase 23 a label asserted the OPPOSITE of its own figure in all three languages with every gate green. You are the only check on meaning.
  </how-to-verify>
  <files>frontend/src/locales/en/admin.json, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/intake.json, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/routes/admin.pulse.intakes.new.tsx</files>
  <read_first>
    - frontend/src/locales/en/admin.json, nl/admin.json, fr/admin.json (the changed values, before looking at the screen)
    - .planning/phases/23-*/23-CONTEXT.md and the phase-23 review record — the label that asserted the opposite of its own figure in all three languages with every gate green
  </read_first>
  <action>Operator-driven read. Run the frontend locally and read the pages listed under `how-to-verify` in each of NL, FR and EN, then report per language and per page. No file is edited by this task; any correction found becomes an edit back in Task 1 before the plan closes.</action>
  <acceptance_criteria>
    - The operator has replied "approved", OR every reported wrong string has been fixed in Task 1 and the reader has re-read that page in that language
    - `cd frontend && node scripts/i18n-audit.mjs` still exits 0 after any correction
    - The SUMMARY records, per language, which pages were read and by whom
  </acceptance_criteria>
  <verify>
    <human-check>All three languages read on all four page groups; operator replied "approved".</human-check>
  </verify>
  <done>A human has read NL, FR and EN on the clients page, the intakes list, the create form and the invite dialog, and confirmed the copy is true — not merely present.</done>
  <resume-signal>Type "approved" or list, per language and per page, the strings that are still wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locale JSON -> rendered UI | operator-facing copy; no user input crosses here |
| `client_name` -> AI skill prompt | an operator-supplied string is interpolated into a provider prompt (pre-existing path, unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.5-03-I | Information disclosure | copy change leaking a schema/field name | accept | the change is display wording only; no key, column or API field name moves — asserted by the empty `git diff` on `backend/app/db/models/` and `alembic/` |
| T-23.5-03-T | Tampering | a prompt that PARSES the changed markdown header | mitigate | a grep of each skill's own prompt and parser for `Klantnaam` / `Klant:` is run BEFORE the edit and its result recorded; the skill suites are the gate |
| T-23.5-03-D | Denial of service | a renamed locale key breaking the UI at runtime | mitigate | values only, one deliberately added key added to all three files in the same edit; `i18n-audit.mjs` CHECK A + an independent 3-way key-parity assertion |
| T-23.5-03-COPY | Repudiation | copy that is grammatical and false | mitigate | the blocking human read of all three languages (Task 3) — the 2026-08-13 lesson, where tsc + vitest + key-parity + i18n-audit all passed on a label that contradicted its own figure |
| T-23.5-03-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NO package — `git diff frontend/package.json backend/pyproject.toml` must be empty |
</threat_model>

<verification>
- `cd frontend && node scripts/i18n-audit.mjs && npx tsc --noEmit && npx vitest run` all exit 0
- `cd backend && python -m pytest tests/test_mail_render.py tests/test_mail_locale.py tests/test_mail_endpoints.py tests/test_ai_apply_skill.py tests/test_ai_context_pack.py tests/test_schema_shape_locale.py -x` exits 0
- `git diff backend/app/db/ backend/app/data/` is EMPTY
- The human read in Task 3 is approved
</verification>

<success_criteria>
- Project means project and client means client, in three languages, everywhere an operator looks
- The client's own form still calls its organisation field what it is
- No key, column, API field or migration moved
- A human has read all three languages and said so
</success_criteria>

<output>
Create `.planning/phases/23.5-tester-remarks-round-1-superadmin-status-override-real-archi/23.5-03-SUMMARY.md` when done.
</output>
