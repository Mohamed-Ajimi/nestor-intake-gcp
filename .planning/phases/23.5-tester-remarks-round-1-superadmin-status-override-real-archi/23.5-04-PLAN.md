---
phase: 23.5-tester-remarks-round-1-superadmin-status-override-real-archi
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/runtime_flags.py
  - tribunal/nestor_pulse_sdk/citations/extractor.py
  - tribunal/nestor_pulse_sdk/tests/test_runtime_flags.py
  - tribunal/nestor_pulse_sdk/tests/test_citation_replay.py
  - tribunal/nestor_pulse_sdk/tests/fixtures/citations_replay/__init__.py
  - tribunal/nestor_pulse_sdk/tests/fixtures/citations_replay/golden_flags_off.json
autonomous: true
requirements: [TR2-SOURCES]

must_haves:
  truths:
    - "With NESTOR_CITATIONS_V2 unset, the set of claim_source rows the persist loop would write is byte-identical to today's, proven against a golden generated from unmodified code"
    - "With the flags on, a skeptic's web_search/web_fetch URL is never written as a claim_source row of every claim in its group — it stays on the verdict as evidence"
    - "With the flags on, every written source title either equals the URL's own host or belongs to a redirect URL; a title is never stamped on a foreign host"
    - "With the flags on, a provider's self-declared grade is never stamped on a URL that provider did not supply"
    - "The replay harness runs with no database, no network and no provider call, on a bare dev box"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/runtime_flags.py"
      provides: "the phase-23.5 kill switches, read at CALL time"
      exports: ["citations_v2", "skeptic_as_evidence", "per_url_meta", "primary_anchor", "ledger_numberable_only", "render_resolved", "synthesis_continue_truncated"]
    - path: "tribunal/nestor_pulse_sdk/tests/fixtures/citations_replay/__init__.py"
      provides: "the offline fixture mirroring the shapes measured on run 7784e71c"
    - path: "tribunal/nestor_pulse_sdk/tests/fixtures/citations_replay/golden_flags_off.json"
      provides: "the flags-off golden, generated from unmodified code before any edit"
    - path: "tribunal/nestor_pulse_sdk/citations/extractor.py"
      provides: "_plan_claim_sources — the pure decision function the persist loop consumes"
      contains: "_plan_claim_sources"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/citations/extractor.py persist loop"
      to: "_plan_claim_sources"
      via: "the loop iterates the planned entries instead of deciding inline"
      pattern: "_plan_claim_sources\\("
    - from: "tribunal/nestor_pulse_sdk/citations/extractor.py::_gather_source_urls"
      to: "_plan_claim_sources"
      via: "one source of truth so the resolution pre-pass and the persist loop cannot drift"
      pattern: "_plan_claim_sources\\("
---

<objective>
Remark 2, mechanisms 1 and 2, behind kill switches that default to today's behaviour
(D-23.5-04): stop the skeptic's whole-session URL list from becoming a `claim_source` row of
every claim in its group, and stop one claim's display domain and self-grade from being
stamped on URLs from other hosts.

Purpose: run 7784e71c wrote 4,999 `claim_source` rows and 2,681 numbered sources for 586
claims, of which 1,824 carried a wrong label. Both numbers come from these two mechanisms.

Output: a call-time flags module, an offline replay fixture built from the measured shapes, a
flags-off golden generated from unmodified code, one pure decision function extracted out of
the persist loop, and the two mechanism fixes behind their switches.

**This is the sensitive plan.** It touches NOTHING under `runs/` (the worker, the claim SQL,
the heartbeat — the D-23.1-06 fence), nothing under `audit/`, no provider client, and no
alembic revision. `pipeline/tribunal/group_skeptic.py` is deliberately NOT modified: the
fan-out is corrected at the persist boundary so the verdict keeps its evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23.5-tester-remarks-round-1-superadmin-status-override-real-archi/23.5-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
<!-- Extracted at HEAD. These are the contracts; do not go exploring for them. -->

`tribunal/nestor_pulse_sdk/citations/extractor.py`:
```
def _as_list(value) -> list                       # a non-list is [] (a string is NOT iterated)
def _gather_source_urls(claims: list[dict], verdicts_by_claim: dict) -> list[str]
    # per-call dedupe, first-seen order. Reads, per claim:
    #   claim["source_urls"], claim["evidence_refs"]            <- the PROVIDER's own urls
    #   verdict["evidence_refs"], verdict["citations"]          <- the SKEPTIC's session urls
    # Called from TWO places: the Stage-7 resolution pre-pass (whole run) and the
    # per-claim persist loop (single-element list). They must not drift.
async def _upsert_source(session, *, tenant_id, url, provider, snapshot_text,
                         title=None, resolved_url=None, resolution_status=None) -> uuid
    # dedupes by (tenant_id, content_hash); title/resolved_url/resolution_status are NOT
    # part of content_hash; on conflict the EXISTING row wins and keeps what it had
async def _link_claim_source(session, *, tenant_id, claim_id, source_id,
                             snippet=None, provider_quality=None) -> None
```
The persist loop (~line 1038-1115 of `persist_tribunal_claims`) today decides, per claim:
```
deduped_urls   = _gather_source_urls([claim], verdicts_by_claim)
quality_by_url = claim.get("provider_quality_by_url") if isinstance(..., dict) else {}
claim_quality  = claim.get("provider_quality")
source_title   = str(claim.get("source_domain") or "").strip() or None      # ONE title
source_title   = source_title[:_SOURCE_TITLE_MAX_CHARS]
for url in deduped_urls:
    resolved_target   = resolved_map.get(url)
    resolution_status = None if (url not in resolved_map or not is_redirect_url(url)) \
                        else ("resolved" if resolved_target else "unresolved")
    sid = await _upsert_source(..., url=url, provider="tribunal_skeptic",
                               snapshot_text=url, title=source_title,
                               resolved_url=resolved_target,
                               resolution_status=resolution_status)
    await _link_claim_source(..., provider_quality=quality_by_url.get(url) or claim_quality)
```

`tribunal/nestor_pulse_sdk/pipeline/tribunal/group_skeptic.py::_parse_group_verdict` puts
`citations=list(citations)` on EVERY member claim's verdict, where `citations` is
`skeptic.py::_collect_citation_urls(content)` = every `web_search` RESULT url + every
`web_fetch` url of the WHOLE group session. That stays as it is — the verdict row is where
that evidence belongs.

`tribunal/nestor_pulse_sdk/citations/redirect_resolver.py`:
```
def is_redirect_url(url: object) -> bool
```
`tribunal/nestor_pulse_sdk/citations/numbering.py`:
```
def _domain(url: str | None) -> str    # netloc lowercased, leading "www." stripped
```

The existing env-flag idiom in this tree is a module-level
`os.environ.get("NESTOR_TRIBUNAL_...", default)` read at IMPORT (see
`citations/anchors.py:87` `_ANCHORS_ENABLED`). That idiom is NOT reused here — see Task 1.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the kill switches, and the offline replay fixture</name>
  <files>tribunal/nestor_pulse_sdk/runtime_flags.py, tribunal/nestor_pulse_sdk/tests/test_runtime_flags.py, tribunal/nestor_pulse_sdk/tests/fixtures/citations_replay/__init__.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/citations/anchors.py lines 70-95 (`_ANCHORS_ENABLED` — the import-time idiom, and why it cannot be monkeypatched)
    - tribunal/nestor_pulse_sdk/tests/test_citation_anchors.py lines 265-285 (the `skipif(not _ANCHORS_ENABLED)` consequence of that idiom — the trap this plan avoids)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/gates.py lines 68-108 (the NESTOR_TRIBUNAL_* naming + documented-default convention to mirror)
    - tribunal/nestor_pulse_sdk/tests/fixtures/ (the existing fixture layout — `run_4cbb5311/`, `run_7dcf51d5/`)
    - tribunal/nestor_pulse_sdk/citations/extractor.py lines 600-720 (`_as_list`, `_gather_source_urls`, `_verdicts_for` — the exact shapes the fixture must produce)
    - .planning/phases/23.5-*/23.5-CONTEXT.md § Remark 2 (the five mechanisms and the measured numbers)
  </read_first>
  <behavior>
    - With no environment set, `citations_v2()` is False and every citation sub-switch returns False
    - With `NESTOR_CITATIONS_V2=true` and nothing else set, every citation sub-switch returns True
    - With `NESTOR_CITATIONS_V2=true` and `NESTOR_CITATIONS_PER_URL_META=false`, `per_url_meta()` is False and the other four are True
    - With `NESTOR_CITATIONS_V2` unset but `NESTOR_CITATIONS_PER_URL_META=true`, `per_url_meta()` is still False (the master is an AND-gate)
    - `synthesis_continue_truncated()` is False by default and is NOT gated by `NESTOR_CITATIONS_V2`
    - Every accessor reflects a `monkeypatch.setenv` made AFTER import, in the same process
  </behavior>
  <action>
    Create `tribunal/nestor_pulse_sdk/runtime_flags.py`. It holds the phase-23.5 kill switches
    and nothing else: no DB import, no provider import, no logging config.

    Design, and state the rationale in the module docstring:
      * ONE master, `NESTOR_CITATIONS_V2`, default `false` — the single lever the operator
        flips to revert the whole Sources-list change by configuration (D-23.5-04). Five
        fine-grained switches each default `true` but are ANDed with the master, so they are
        inert until the master is on and exist only to bisect a bad dev run without a rebuild.
        Defaulting the fine-grained switches to `false` as well would mean a master flip
        changed nothing, which is a worse failure mode than one that is loud.
      * `NESTOR_SYNTHESIS_CONTINUE_TRUNCATED` is a SEPARATE master, default `false`, NOT gated
        by `NESTOR_CITATIONS_V2` — it is a different subsystem (the writer) with a different
        blast radius (one extra provider call per truncated section), and tying it to the
        citation master would make one lever mean two things.
      * Every accessor reads `os.environ` AT CALL TIME, not at import. The import-time idiom
        used by `_ANCHORS_ENABLED` is the reason `test_citation_anchors.py` has to
        `skipif(not _ANCHORS_ENABLED)` instead of testing both states, and the flags-off
        golden test in Task 2 is worth nothing if it cannot flip a flag in-process.

    Public callables, exactly these names and exactly these env names:
      `citations_v2()` -> `NESTOR_CITATIONS_V2` (default false, the master)
      `skeptic_as_evidence()` -> `NESTOR_CITATIONS_SKEPTIC_AS_EVIDENCE` (default true, AND master)
      `per_url_meta()` -> `NESTOR_CITATIONS_PER_URL_META` (default true, AND master)
      `primary_anchor()` -> `NESTOR_CITATIONS_PRIMARY_ANCHOR` (default true, AND master)
      `ledger_numberable_only()` -> `NESTOR_CITATIONS_LEDGER_NUMBERABLE_ONLY` (default true, AND master)
      `render_resolved()` -> `NESTOR_CITATIONS_RENDER_RESOLVED` (default true, AND master)
      `synthesis_continue_truncated()` -> `NESTOR_SYNTHESIS_CONTINUE_TRUNCATED` (default false, independent)
    plus a private `_truthy(raw: str | None, default: bool) -> bool` accepting
    `1/true/yes/on` and `0/false/no/off`, case-insensitive, whitespace-stripped, falling back
    to `default` on anything else (including an empty string).

    Write `tests/test_runtime_flags.py` covering the behaviour list with `monkeypatch.setenv`
    / `delenv`. No DB, no network, no `pytest.mark.integration`.

    Create `tests/fixtures/citations_replay/__init__.py` — a pure-python module, NO DB read and
    NO prod data, mirroring the shapes measured on run 7784e71c and recorded in
    `gs://nestor-pulse-prod-nestor-audit/diagnostics/7784e71c/diag.txt`. It must export
    `CLAIMS: list[dict]`, `VERDICTS_BY_CLAIM: dict` and `RESOLVED_MAP: dict[str, str | None]`
    in exactly the shapes `persist_tribunal_claims` receives them, and it must contain at
    least one of each of these, each with a comment naming the measured phenomenon it stands
    for:
      1. a claim with ZERO source urls and no verdict citations (the 159 sourceless claims);
      2. a claim with exactly ONE provider url (the median);
      3. a claim with 20+ urls of which most arrive via its group's verdict `citations`
         (the max-58 tail);
      4. a GROUP of three claims sharing ONE verdict whose `citations` list fans out across
         all three (mechanism 1, the 4,999-row driver);
      5. a claim whose `source_domain` is one host while its url set includes two FOREIGN
         hosts (mechanism 2, the 1,824 wrong labels — model it on the measured
         wikipedia.org / ebay.de / ah.nl shape);
      6. a `vertexaisearch.cloud.google.com/grounding-api-redirect/...` url present in
         `RESOLVED_MAP` with a real publisher target (a resolved redirect), and a second
         redirect mapped to `None` (an attempted-and-failed resolution);
      7. a `distiller_fallback` claim whose `source_domain` is empty and which carries no
         usable url (mechanism 5's driver — consumed by plan 05);
      8. an `evidence_refs` entry that is free text of the form `"https://host/path (quote)"`
         rather than a bare url (the garbage-tier-label minor finding);
      9. a claim carrying `provider_quality_by_url` for SOME of its urls and a scalar
         `provider_quality` for itself (mechanism 2's grade half).
    Add a module-level docstring stating plainly that these are SYNTHETIC shapes, not copied
    rows, that the absolute counts here are fixture counts and NOT the 2,681/1,824 figures, and
    that the production totals are measured on the dev run in plan 07.
  </action>
  <acceptance_criteria>
    - `cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_runtime_flags.py -x` exits 0 and reports at least 6 passing tests
    - `cd tribunal && python -c "import os; os.environ.pop('NESTOR_CITATIONS_V2',None); from nestor_pulse_sdk import runtime_flags as f; assert f.citations_v2() is False and f.per_url_meta() is False and f.synthesis_continue_truncated() is False; os.environ['NESTOR_CITATIONS_V2']='true'; assert f.per_url_meta() is True and f.synthesis_continue_truncated() is False; print('ok')"` prints `ok`
    - `grep -c 'os.environ' tribunal/nestor_pulse_sdk/runtime_flags.py` >= 1 and `grep -vn '^ *#' tribunal/nestor_pulse_sdk/runtime_flags.py | grep -c '^[0-9]*: *_[A-Z_]* *= *os.environ'` == 0 (no module-level import-time flag constant)
    - `cd tribunal && python -c "from nestor_pulse_sdk.tests.fixtures.citations_replay import CLAIMS, VERDICTS_BY_CLAIM, RESOLVED_MAP; assert len(CLAIMS) >= 9; assert any(len(c.get('source_urls') or [])+len(c.get('evidence_refs') or [])==0 for c in CLAIMS); assert any(v for v in RESOLVED_MAP.values()) and any(v is None for v in RESOLVED_MAP.values()); print('ok')"` prints `ok`
    - `grep -rn 'DATABASE_URL\|get_sessionmaker\|psql\|AsyncSession' tribunal/nestor_pulse_sdk/tests/fixtures/citations_replay/__init__.py` returns NOTHING
  </acceptance_criteria>
  <verify>
    <automated>cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_runtime_flags.py -x</automated>
  </verify>
  <done>Seven named switches with documented defaults, readable at call time and provable in both states, plus a synthetic fixture that carries every shape the prod defect was measured in — and no database anywhere near it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extract _plan_claim_sources with NO behaviour change, and pin it with a golden generated from unmodified code</name>
  <files>tribunal/nestor_pulse_sdk/citations/extractor.py, tribunal/nestor_pulse_sdk/tests/test_citation_replay.py, tribunal/nestor_pulse_sdk/tests/fixtures/citations_replay/golden_flags_off.json</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/citations/extractor.py lines 600-730 (`_as_list`, `_gather_source_urls`, `_verdicts_for`) and 1000-1120 (the persist loop being refactored)
    - tribunal/nestor_pulse_sdk/citations/redirect_resolver.py lines 145-180 (`is_redirect_url`)
    - tribunal/nestor_pulse_sdk/citations/numbering.py lines 70-90 (`_domain`)
    - tribunal/nestor_pulse_sdk/tests/fixtures/citations_replay/__init__.py (written in Task 1)
    - tribunal/nestor_pulse_sdk/tests/test_citation_recall.py and test_citation_resolution.py (the existing pure-test conventions in this area)
  </read_first>
  <behavior>
    - `_plan_claim_sources(claim, verdicts_by_claim, resolved_map)` returns a list of dicts, one per URL that WOULD be upserted+linked, in the same first-seen order the loop writes them today
    - Each entry carries exactly: `url`, `title`, `provider_quality`, `resolved_url`, `resolution_status`, `origin`
    - With the flags off, for every fixture claim the returned entries equal the stored golden byte for byte
    - With the flags off, `[e["url"] for e in _plan_claim_sources(...)]` equals `_gather_source_urls([claim], verdicts_by_claim)` as computed at HEAD, for every fixture claim
  </behavior>
  <action>
    STEP A — GENERATE THE GOLDEN BEFORE TOUCHING `extractor.py`. Write a throwaway script (not
    committed to `nestor_pulse_sdk/`) that imports the Task-1 fixture and the UNMODIFIED
    `extractor` and, for each claim in `CLAIMS`, reproduces the loop's decisions exactly as
    quoted in `<interfaces>` above: `_gather_source_urls([claim], VERDICTS_BY_CLAIM)` for the
    url list; `source_title` from `claim["source_domain"]` truncated at
    `_SOURCE_TITLE_MAX_CHARS`; `provider_quality` as `quality_by_url.get(url) or claim_quality`;
    and the three-state `resolution_status` rule. Serialise the result to
    `tests/fixtures/citations_replay/golden_flags_off.json` as a JSON object keyed by the
    claim's fixture id, each value a list of the six-key entries, with `"origin": "provider"`
    omitted (origin is new information and is NOT part of the flags-off golden — the golden
    compares the five pre-existing fields only). COMMIT the golden on its own, with a message
    naming the commit SHA of the unmodified `extractor.py` it was generated from. The whole
    value of this file is that it was produced by code nobody had edited yet.

    STEP B — EXTRACT, with zero behaviour change. Add `_plan_claim_sources(claim: dict,
    verdicts_by_claim: dict, resolved_map: dict) -> list[dict]` to `extractor.py`, directly
    above `_gather_source_urls`. It walks the claim's own `source_urls` then `evidence_refs`
    then, per `_verdicts_for`, each verdict's `evidence_refs` then `citations` — the SAME order
    and the SAME `_as_list` / `isinstance(url, str)` guards `_gather_source_urls` uses today,
    because the ORDER is what the first-seen dedupe and the citation numbering depend on. Each
    entry records `origin="provider"` for urls taken from the claim's own two fields and
    `origin="skeptic"` for urls taken from a verdict. Title, grade and resolution status are
    computed by the HEAD rules exactly as quoted above. Dedupe by stripped url, first-seen,
    per call.

    Then reimplement `_gather_source_urls` as the url projection of `_plan_claim_sources` over
    the supplied claims, preserving its cross-call dedupe and its docstring's contract that the
    Stage-7 pre-pass and the persist loop can never drift. Then rewrite the persist loop to
    iterate the planned entries instead of deciding inline: the loop's body becomes
    `_upsert_source(..., url=e["url"], title=e["title"], resolved_url=e["resolved_url"],
    resolution_status=e["resolution_status"])` followed by `_link_claim_source(...,
    provider_quality=e["provider_quality"])`. `provider="tribunal_skeptic"` and
    `snapshot_text=url` stay exactly as they are — changing either would change
    `content_hash` and therefore source dedupe, which is out of scope here and is Phase 24's
    `0019`.

    STEP C — PIN IT. Write `tests/test_citation_replay.py` with the flags-OFF golden test: for
    each fixture claim, `_plan_claim_sources` with `NESTOR_CITATIONS_V2` deleted from the
    environment must equal the stored golden entry-for-entry and field-for-field (comparing the
    five golden fields), and the url projection must equal `_gather_source_urls`'s output for
    the same claim. The comparison is an equality on whole lists, not a length or a membership
    check. Module is pure: no DB, no `integration` marker.

    Touch nothing else. `group_skeptic.py`, `skeptic.py`, `numbering.py`, `anchors.py`,
    `pipeline.py` and everything under `runs/` and `audit/` are untouched by this task.
  </action>
  <acceptance_criteria>
    - `cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_citation_replay.py -x` exits 0
    - `cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_citation_recall.py nestor_pulse_sdk/tests/test_citation_resolution.py nestor_pulse_sdk/tests/test_citation_dedupe.py nestor_pulse_sdk/tests/test_citation_roundtrip.py nestor_pulse_sdk/tests/test_claim_attribution.py -x` exits 0
    - `git log --oneline -- tribunal/nestor_pulse_sdk/tests/fixtures/citations_replay/golden_flags_off.json` shows the golden committed in an EARLIER commit than the one touching `tribunal/nestor_pulse_sdk/citations/extractor.py`
    - `grep -c '_plan_claim_sources' tribunal/nestor_pulse_sdk/citations/extractor.py` >= 3 (definition, `_gather_source_urls`, persist loop)
    - `grep -n 'source_title' tribunal/nestor_pulse_sdk/citations/extractor.py` shows the per-claim single-title assignment is gone from the persist loop (it now lives inside `_plan_claim_sources`)
    - `git diff --stat tribunal/nestor_pulse_sdk/runs/ tribunal/nestor_pulse_sdk/audit/ tribunal/nestor_pulse_sdk/pipeline/tribunal/group_skeptic.py tribunal/nestor_pulse_sdk/pipeline/tribunal/skeptic.py tribunal/nestor_pulse_sdk/alembic/` is EMPTY
    - `grep -n 'snapshot_text=url' tribunal/nestor_pulse_sdk/citations/extractor.py` still returns a line (content_hash inputs unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_citation_replay.py nestor_pulse_sdk/tests/test_citation_recall.py nestor_pulse_sdk/tests/test_citation_resolution.py nestor_pulse_sdk/tests/test_citation_dedupe.py -x</automated>
  </verify>
  <done>The persist loop's decisions are a pure function, the pre-pass and the loop share it, and a golden produced by code nobody had edited proves the refactor changed nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: mechanisms 1 and 2 behind their switches</name>
  <files>tribunal/nestor_pulse_sdk/citations/extractor.py, tribunal/nestor_pulse_sdk/tests/test_citation_replay.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/citations/extractor.py — `_plan_claim_sources` as written in Task 2
    - tribunal/nestor_pulse_sdk/runtime_flags.py (written in Task 1)
    - tribunal/nestor_pulse_sdk/citations/redirect_resolver.py `is_redirect_url`
    - tribunal/nestor_pulse_sdk/citations/numbering.py `_domain` and `derive_quality_tier`
    - tribunal/nestor_pulse_sdk/pipeline/synthesis/steps.py lines 478-560 (`build_graded_sources_section` — how `title` becomes the rendered label, i.e. what "label" means in the acceptance numbers)
    - .planning/phases/23.5-*/23.5-CONTEXT.md § Remark 2 fix (a) and (b), and the acceptance numbers
  </read_first>
  <behavior>
    - With `NESTOR_CITATIONS_V2` unset, `_plan_claim_sources` output is unchanged (the Task-2 golden test still passes, untouched)
    - With the flags on, no entry has `origin == "skeptic"` — for every fixture claim, and in particular for the three-claim group of fixture shape 4
    - With the flags on, for every entry whose `title` is not None: either `is_redirect_url(entry["url"])` is True, or `_domain(entry["url"]) == entry["title"]`
    - With the flags on, no entry carries a `provider_quality` for a url absent from that claim's own `provider_quality_by_url` unless the url is one of the claim's own `source_urls`
    - With the flags on, the total number of planned entries across all fixture claims is strictly LESS than with the flags off, and the ratio is asserted against a recorded constant in the test
    - The verdicts are not mutated: `VERDICTS_BY_CLAIM` is byte-identical before and after the call, so the skeptic's evidence still reaches the verdict row
  </behavior>
  <action>
    Mechanism 1 (`skeptic_as_evidence()`): inside `_plan_claim_sources`, when the switch is on,
    the verdict-derived urls are still WALKED (so the ordering logic is one code path, not two)
    but are not emitted as entries. Write the reason in the docstring in the module's own
    voice: a skeptic's `web_search` result list is the evidence for the GROUP's verdict, not a
    citation of each member claim, and copying it onto every member is what turned 586 claims
    into 4,999 `claim_source` rows with a median of 1 and a max of 58. The evidence is not
    lost — it stays on the verdict row, which `_insert_verdict` already writes, and
    `group_skeptic.py` is deliberately not touched.

    Mechanism 2 (`per_url_meta()`): when the switch is on, compute title and grade PER URL
    instead of once per claim.
      * Title: `claim["source_domain"]` is applied to a url ONLY IF `is_redirect_url(url)` (the
        provider's own opaque redirect, where the display domain is the only honest label) OR
        `_domain(url) == source_domain` (the url really is on that host). Otherwise the title
        is `None`, and `build_graded_sources_section` falls back to the url's own display
        domain — which is honest, and is exactly what drives the acceptance number "labels not
        equal to host = 0".
      * Grade: `provider_quality` is `quality_by_url.get(url)`, falling back to the scalar
        `claim_quality` ONLY for a url that appears in the claim's own `source_urls`. A url the
        claim did not supply gets `None` and falls through to `derive_quality_tier`'s domain
        heuristic at render time. Stamping "established press" on an ebay listing because some
        other url of the same claim was graded that way is the defect.
    Both switches are ANDed with the master inside `runtime_flags`, so a single
    `NESTOR_CITATIONS_V2=false` restores today's behaviour with no rebuild.

    Extend `tests/test_citation_replay.py` with the flags-ON block, using `monkeypatch.setenv`.
    Assert the behaviour list above as ZERO-COUNTS over the whole fixture, not as spot checks:
    build the list of violations and assert the list is empty, printing it on failure. Record
    the flags-off and flags-on entry totals as named module constants in the test with a
    comment saying they are FIXTURE counts and that the production 2,681-to-N collapse is
    measured on the dev run in plan 07.
  </action>
  <acceptance_criteria>
    - `cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_citation_replay.py -x` exits 0 and reports strictly more tests than after Task 2
    - The flags-OFF golden test from Task 2 is unmodified: `git diff` on `test_citation_replay.py` for this task shows ADDED lines only inside the flags-on block, and no change to the golden assertions
    - `cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_citation_recall.py nestor_pulse_sdk/tests/test_citation_resolution.py nestor_pulse_sdk/tests/test_citation_dedupe.py nestor_pulse_sdk/tests/test_citation_roundtrip.py nestor_pulse_sdk/tests/test_citation_numbering.py nestor_pulse_sdk/tests/test_citation_anchors.py nestor_pulse_sdk/tests/test_claim_attribution.py nestor_pulse_sdk/tests/test_scrub_research.py -x` exits 0
    - The test file contains an assertion of the literal form `assert violations == []` (or `assert not violations, violations`) for each of the three zero-counts — `grep -c 'violations' tribunal/nestor_pulse_sdk/tests/test_citation_replay.py` >= 6
    - `git diff --stat tribunal/nestor_pulse_sdk/runs/ tribunal/nestor_pulse_sdk/audit/ tribunal/nestor_pulse_sdk/pipeline/ tribunal/nestor_pulse_sdk/alembic/` is EMPTY
    - `grep -c 'runtime_flags' tribunal/nestor_pulse_sdk/citations/extractor.py` >= 1 and the import is a module import (`from nestor_pulse_sdk import runtime_flags`), NOT `from ... import skeptic_as_evidence` — a from-import would bind the function object and still read env at call time, but the module form keeps the call sites reading `runtime_flags.skeptic_as_evidence()` and self-documenting
  </acceptance_criteria>
  <verify>
    <automated>cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_citation_replay.py nestor_pulse_sdk/tests/test_citation_numbering.py nestor_pulse_sdk/tests/test_citation_anchors.py nestor_pulse_sdk/tests/test_citation_recall.py -x</automated>
  </verify>
  <done>With the master off, nothing changed. With it on, a skeptic url is never a claim's citation and a label is never stamped on a host it does not belong to — both proven as zero-counts over the fixture, offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider/skeptic model output -> `_plan_claim_sources` | every `source_urls`, `evidence_refs`, `citations` and `source_domain` value is model-authored and untrusted |
| `runtime_flags` env -> pipeline behaviour | a Cloud Run env var silently changes what a paid run writes |
| `_plan_claim_sources` -> `claim_source` / `source` rows | what this function returns becomes the report's citation graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-flag-default | Tampering | `runtime_flags` defaults | mitigate | `NESTOR_CITATIONS_V2` defaults `false`; the flags-off golden was generated from unmodified code and is compared entry-for-entry, so a default that silently changed prod behaviour fails a test rather than a run |
| T-23.5-04-T1 | Tampering | model-authored `source_domain` stamped as a title | mitigate | under `per_url_meta()` a title is applied only to a redirect url or a url whose `_domain` equals it; the zero-count assertion over the fixture is the gate |
| T-23.5-04-T2 | Tampering | model-authored url strings reaching the DB | accept | unchanged from HEAD — `_as_list` guards, `isinstance(url, str)` guards and `_upsert_source`'s bounds already apply; this plan adds no new url source |
| T-23.5-04-I | Information disclosure | one tenant's source row reached via dedupe | accept | `_upsert_source` dedupes on `(tenant_id, content_hash)` and RLS scopes every query; unchanged by this plan, and `content_hash` inputs (`snapshot_text=url`) are deliberately untouched |
| T-23.5-04-D | Denial of service | fewer resolutions / fewer rows | accept | the flags-on path writes strictly FEWER rows and issues strictly FEWER redirect resolutions than today; the failure direction is under-citation, which is visible in the report, not silent |
| T-23.5-04-FENCE | Tampering | drifting into the worker's fenced write path | mitigate | `git diff --stat` on `runs/`, `audit/`, `pipeline/` and `alembic/` must be EMPTY — D-23.1-06's heartbeat/claim fence and Phase 24's `0019` are both out of reach by construction |
| T-23.5-04-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NO package — `git diff tribunal/pyproject.toml tribunal/requirements*.txt` must be empty; if an install becomes necessary the executor STOPS and records a blocker |
</threat_model>

<verification>
- `cd tribunal && python -m pytest nestor_pulse_sdk/tests/test_runtime_flags.py nestor_pulse_sdk/tests/test_citation_replay.py nestor_pulse_sdk/tests/test_citation_numbering.py nestor_pulse_sdk/tests/test_citation_anchors.py nestor_pulse_sdk/tests/test_citation_recall.py nestor_pulse_sdk/tests/test_citation_resolution.py nestor_pulse_sdk/tests/test_citation_dedupe.py nestor_pulse_sdk/tests/test_citation_roundtrip.py nestor_pulse_sdk/tests/test_claim_attribution.py nestor_pulse_sdk/tests/test_scrub_research.py -x` exits 0
- `git diff --stat tribunal/nestor_pulse_sdk/runs/ tribunal/nestor_pulse_sdk/audit/ tribunal/nestor_pulse_sdk/pipeline/ tribunal/nestor_pulse_sdk/alembic/ backend/ frontend/ infra/` is EMPTY
- ZERO provider calls, ZERO database connections and ZERO network access were made by this plan's tests
</verification>

<success_criteria>
- Seven named switches, documented defaults, provable in both states, readable at call time
- A synthetic offline fixture carrying every shape the prod defect was measured in
- A flags-off golden generated from unmodified code and committed before the refactor
- Skeptic fan-out and cross-host labelling both fixed behind switches, proven as zero-counts
- Nothing under `runs/`, `audit/`, `pipeline/` or `alembic/` was touched
</success_criteria>

<output>
Create `.planning/phases/23.5-tester-remarks-round-1-superadmin-status-override-real-archi/23.5-04-SUMMARY.md` when done. Record the fixture's flags-off and flags-on entry totals and state plainly that they are fixture counts, not production counts.
</output>
