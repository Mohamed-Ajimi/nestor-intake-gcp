---
phase: 16-research-trigger-progress-bridge
plan: 05
type: execute
wave: 4
depends_on: [16-03, 16-04]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - .planning/phases/14-auth-retirement-integration-seam/14-HUMAN-UAT.md
  - .planning/phases/16-research-trigger-progress-bridge/16-HUMAN-UAT.md
autonomous: false
requirements: [ENGINE-03, SEAM-03, RUN-01, RUN-02]
must_haves:
  truths:
    - "The runbook has a Phase 16 section: rebuild nestor-api image (0011 migration + new modules), run the 0011 migration Job, set NESTOR_WORKER_STALE_MINUTES=90 on tribunal-worker, and confirm TRIBUNAL_SERVICE_URL on nestor-api"
    - "The operator triggers ONE real research run on a decomposed smoke intake and watches it complete with the progress panel + email"
    - "The first live trigger closes the deferred Phase-14 seam HTTP UAT (recorded in 14-HUMAN-UAT.md)"
    - "The stale-run reclaim window is set above the measured 17-19 min max (no double-runs)"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "Phase 16 deploy + live-UAT section"
      contains: "## Phase 16"
    - path: ".planning/phases/16-research-trigger-progress-bridge/16-HUMAN-UAT.md"
      provides: "operator live-session checklist for the trigger/progress/mail proof"
      contains: "trigger"
  key_links:
    - from: "infra/DEPLOY-RUNBOOK.md Phase 16 section"
      to: "migration 0011 + new backend modules"
      via: "REBUILD image not just env flip (recurring deploy-gap)"
      pattern: "REBUILD"
    - from: "16 first live trigger"
      to: ".planning/phases/14-auth-retirement-integration-seam/14-HUMAN-UAT.md"
      via: "records the deferred seam HTTP UAT as closed"
      pattern: "Phase 16"
---

<objective>
Author the Phase 16 deploy runbook section and the operator live-UAT checklist, then run the operator
live session: rebuild + deploy the `nestor-api` image (migration 0011 + the new research modules), run
the 0011 migration Job, set `NESTOR_WORKER_STALE_MINUTES=90` on `tribunal-worker` (ENGINE-03 partial —
above the measured 17-19 min max), confirm `TRIBUNAL_SERVICE_URL` is set on `nestor-api`, then trigger
ONE real research run on a decomposed smoke intake — watching the progress panel, the completion email,
and closing the deferred Phase-14 seam HTTP UAT.

Purpose: Nothing is real until it is deployed. The recurring deploy-gap is a config-only env flip on a
stale image — this plan forces the image REBUILD because new backend modules must ship. The live
session is also the first intake-originated seam call, which closes the deferred Phase-14 HTTP UAT.
Output: the runbook section, `16-HUMAN-UAT.md`, and the recorded live-run proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-research-trigger-progress-bridge/16-CONTEXT.md
@.planning/phases/16-research-trigger-progress-bridge/16-RESEARCH.md
@.planning/phases/16-research-trigger-progress-bridge/16-03-SUMMARY.md
@.planning/phases/16-research-trigger-progress-bridge/16-04-SUMMARY.md

<interfaces>
<!-- Live-config facts extracted this session. -->

DEPLOY GAPS + FACTS:
- Latest existing runbook section is "## Phase 14" (infra/DEPLOY-RUNBOOK.md ~line 913). Add "## Phase 16".
- Image REBUILD required: new modules research_routes.py, run_task.py, brief.py, research_runs model,
  mail templates + migration 0011 must ship. A config-only env flip on a stale image is the recurring
  deploy-gap (DEPLOY-RUNBOOK :519, MEMORY phase-10/phase-06). Cloud Build only (dev box has no Docker).
- Migration 0011: run the existing `nestor` migration Job (alembic upgrade head) — same pattern as
  prior intake migrations (0009/0010).
- TRIBUNAL_SERVICE_URL: ALREADY set on nestor-api = https://tribunal-api-ybkr7metoq-ew.a.run.app
  (14-HUMAN-UAT item 2, verified pass 2026-07-21). Confirm-only step, not a new set.
- NESTOR_WORKER_STALE_MINUTES: Tribunal-worker env, default 60 (worker.py:47). Set to 90
  (measured max run 17-19 min; A2). tribunal-worker is a Cloud Run service; env update + redeploy.
  This is a config-only change on the UNCHANGED worker image (no worker rebuild).
- NESTOR_TRIBUNAL_UNCAPPED stays ON (D-02 operator deferral) — do NOT flip it in this phase.
- Mail env already seeded (RESEND_API_KEY, APP_BASE_URL, NESTOR_ADMIN_EMAIL — Phase 10/12). No new secret.
- Anthropic credits LOW (MEMORY phase-14) — operator tops up BEFORE the live run.

DEFERRED PHASE-14 UAT (.planning/phases/14-auth-retirement-integration-seam/14-HUMAN-UAT.md):
  Item 1 "Live intake→tribunal HTTP seam call" is deferred — "closes naturally via Phase 16's trigger
  route (first real intake-originated seam call)". The first live trigger in THIS plan closes it:
  update item 1 result to pass with the Phase-16 run reference.

SMOKE TENANT: MEMORY phase-14 notes smoke tenant 1464b60d to clean; a decomposed smoke intake in a
smoke space is the live-run target. Record run id / cost / duration / verify_chain in the UAT + SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Phase 16 runbook section + 16-HUMAN-UAT checklist</name>
  <files>infra/DEPLOY-RUNBOOK.md, .planning/phases/16-research-trigger-progress-bridge/16-HUMAN-UAT.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 10 (Step 10.4 REBUILD-not-env-flip gap) + § Phase 13.e/13.f (Cloud Build image + migration Job) + § Phase 14 (SA/invoker/seam env conventions)
    - .planning/phases/14-auth-retirement-integration-seam/14-HUMAN-UAT.md (item 1 deferred + item 2 TRIBUNAL_SERVICE_URL pass — the format to mirror + the item to close)
    - .planning/phases/16-research-trigger-progress-bridge/16-VALIDATION.md § Manual-Only Verifications (the three live checks)
  </read_first>
  <action>
    Append a `## Phase 16 — Research trigger + progress bridge` section to `infra/DEPLOY-RUNBOOK.md`
    after the Phase 14 section, with numbered steps: (16.a) REBUILD the `nestor-api` image via Cloud
    Build (new modules + migration 0011 must ship — explicitly call out that a config-only env flip on
    a stale image is the recurring deploy-gap); (16.b) run the `nestor` migration Job to apply 0011
    (alembic upgrade head) and verify `research_runs` + its RLS policies exist; (16.c) confirm
    `TRIBUNAL_SERVICE_URL` is present on `nestor-api` (read-only describe — already set, do not
    re-set); (16.d) set `NESTOR_WORKER_STALE_MINUTES=90` on `tribunal-worker` (config-only env update
    on the unchanged worker image; verify via `gcloud run services describe tribunal-worker`) and
    NOTE that `NESTOR_TRIBUNAL_UNCAPPED` stays ON per D-02; (16.e) top up Anthropic credits before the
    live run; (16.f) the live trigger/progress/mail UAT (points to 16-HUMAN-UAT.md). Create
    `16-HUMAN-UAT.md` (mirror the 14-HUMAN-UAT.md frontmatter + Tests/Summary/Gaps format) with the
    three checks from 16-VALIDATION.md Manual-Only Verifications: (1) first live intake-originated seam
    trigger on a decomposed smoke intake → run reaches completed + email arrives (this ALSO closes the
    deferred Phase-14 item 1); (2) progress panel visual (dynamic stage list + ticking cost, intake
    design language); (3) `NESTOR_WORKER_STALE_MINUTES=90` shows on the worker.
  </action>
  <verify>
    <automated>grep -c "## Phase 16" infra/DEPLOY-RUNBOOK.md | grep -qv '^0$' && test -f .planning/phases/16-research-trigger-progress-bridge/16-HUMAN-UAT.md && echo "runbook+uat present"</automated>
  </verify>
  <acceptance_criteria>
    - `infra/DEPLOY-RUNBOOK.md` contains a `## Phase 16` section with steps 16.a–16.f.
    - Step 16.a explicitly calls out REBUILD-not-env-flip (the recurring deploy-gap).
    - Step 16.d sets `NESTOR_WORKER_STALE_MINUTES=90` and notes `NESTOR_TRIBUNAL_UNCAPPED` stays ON (D-02).
    - Step 16.c is a confirm-only (read-only) check of the already-set `TRIBUNAL_SERVICE_URL`.
    - `16-HUMAN-UAT.md` exists with the three manual checks and the frontmatter/Tests/Summary format of 14-HUMAN-UAT.md.
  </acceptance_criteria>
  <done>Runbook Phase 16 section + 16-HUMAN-UAT.md authored; REBUILD gap, stale-window, uncapped-stays-on, and the seam-UAT-closing note all recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    The Phase 16 backend + frontend are code-complete and green in Cloud Build (trigger route, poll
    driver, SSE stream, progress panel, mails, migration 0011). The runbook Phase 16 section and
    16-HUMAN-UAT.md are authored. This checkpoint is the operator live session: deploy, migrate, set
    the stale window, and prove ONE real research run end-to-end.
  </what-built>
  <how-to-verify>
    Follow `infra/DEPLOY-RUNBOOK.md` § Phase 16 exactly:
    1. Top up Anthropic credits (MEMORY: they are LOW).
    2. REBUILD + deploy the `nestor-api` image via Cloud Build (do NOT env-flip a stale image).
    3. Run the `nestor` migration Job (0011); confirm `research_runs` + its two RLS policies exist.
    4. Confirm `TRIBUNAL_SERVICE_URL` on `nestor-api` (read-only describe — already set).
    5. Set `NESTOR_WORKER_STALE_MINUTES=90` on `tribunal-worker`; verify via `gcloud run services describe tribunal-worker`. Leave `NESTOR_TRIBUNAL_UNCAPPED` ON (D-02).
    6. On a DECOMPOSED smoke intake in a smoke space, click "Start research" → confirm the dialog → observe: status flips to in_research, the progress panel renders the stage list dynamically with ticking cost, the run reaches `completed`, and the completion email arrives at your address.
    7. Record the run id, total cost, duration, and `verify_chain` result.
    8. Update `.planning/phases/14-auth-retirement-integration-seam/14-HUMAN-UAT.md` item 1 to PASS (the first intake-originated seam call), referencing this Phase-16 run.
    9. Record all results in `16-HUMAN-UAT.md`.
    Expected: run completes green (~17-19 min), progress panel + email work, no double-dispatch, no client-facing research surface visible to a client login, and item 1 of 14-HUMAN-UAT.md is closed.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded run id / cost / duration, or describe issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator gcloud → live Cloud Run services | config + image deploy; must rebuild, not env-flip a stale image |
| live intake backend → internal Tribunal API | first real intake-originated seam call (OIDC, Phase 14 proven) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-15 | Tampering | stale-image deploy (new modules not shipped) | mitigate | runbook 16.a forces a Cloud Build REBUILD; the recurring deploy-gap is called out explicitly |
| T-16-16 | DoS (cost) | ultra-long run double-dispatch | mitigate | NESTOR_WORKER_STALE_MINUTES=90 set above the measured 17-19 min max (ENGINE-03 partial) |
| T-16-17 | DoS (cost) | uncapped runaway run | accept | D-02 operator deferral — UNCAPPED stays ON; cap flip-on is Phase 20; mitigated meanwhile by superadmin-only trigger + server-composed brief |
| T-16-18 | Information Disclosure | client sees research surface during a live run | mitigate | UAT step verifies a client login shows NO research surface during in_research (D-08) |
</threat_model>

<verification>
- Runbook: `grep -c "## Phase 16" infra/DEPLOY-RUNBOOK.md` ≥ 1; `16-HUMAN-UAT.md` exists.
- Operator checkpoint: run reaches `completed`, progress panel + email verified, `NESTOR_WORKER_STALE_MINUTES=90` shows on the worker.
- 14-HUMAN-UAT.md item 1 updated to pass with the Phase-16 run reference.
</verification>

<success_criteria>
- `nestor-api` redeployed with 0011 applied and the new research modules live (ENGINE-03/SEAM-03/RUN-01/RUN-02 shipped).
- `NESTOR_WORKER_STALE_MINUTES=90` set above the measured max (ENGINE-03 partial — no double-runs).
- One real research run proven end-to-end: trigger → dynamic progress panel → completed → email.
- The deferred Phase-14 seam HTTP UAT is closed by this first live trigger.
</success_criteria>

<output>
Create `.planning/phases/16-research-trigger-progress-bridge/16-05-SUMMARY.md` when done.
</output>
