---
phase: 16-research-trigger-progress-bridge
plan: 03
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - backend/app/api/research_routes.py
  - backend/app/db/stream_session.py
  - backend/app/main.py
  - backend/tests/test_research_routes.py
  - backend/tests/test_research_cross_tenant.py
autonomous: true
requirements: [SEAM-03, RUN-01]
must_haves:
  truths:
    - "A superadmin POST to /intakes/{id}/research on a decomposed intake flips status to in_research, inserts a research_runs row, schedules the poll driver, and returns 202"
    - "A trigger on a non-decomposed intake returns 409; a 4th attempt returns a needs-investigation state without a seam call"
    - "The SSE stream emits research-run frames and closes only on {completed,failed,cancelled}"
    - "A cross-tenant intake trigger or stream is an existence-hidden 404; a null-space user is 403"
  artifacts:
    - path: "backend/app/api/research_routes.py"
      provides: "POST /intakes/{id}/research trigger + GET /intakes/{id}/research/stream SSE"
      exports: ["trigger_research", "stream_research_run"]
    - path: "backend/app/db/stream_session.py"
      provides: "read_latest_research_run_dict per-tick scoped read"
      contains: "def read_latest_research_run_dict"
  key_links:
    - from: "backend/app/api/research_routes.py"
      to: "backend/app/research/run_task.py::run_poll_driver"
      via: "BackgroundTasks.add_task after the 202"
      pattern: "add_task"
    - from: "backend/app/api/research_routes.py (SSE)"
      to: "backend/app/db/stream_session.py::read_latest_research_run_dict"
      via: "run_in_threadpool per tick"
      pattern: "read_latest_research_run_dict"
    - from: "backend/app/main.py"
      to: "research_router"
      via: "protected_router.include_router(research_router)"
      pattern: "include_router.research_router"
---

<objective>
Add the two intake-side HTTP surfaces: the trigger verb (`POST /intakes/{id}/research`) that flips
`decomposed → in_research`, assembles the brief, inserts the `research_runs` row, enforces the
3-attempt cap, and schedules the poll driver (SEAM-03, D-03/D-04/D-06); and the SSE stream
(`GET /intakes/{id}/research/stream`) that mirrors the run state to the browser with a dynamic stage
trace (RUN-01, D-07). Both are space-scoped with existence-hidden 404s and covered by day-one
cross-tenant denial tests (Pitfall 5).

Purpose: Wire the browser to the engine glue from Plan 02 through allow-listed, tenant-scoped
endpoints — the trigger is a discrete verb (not a generic PATCH status), and the SSE handler is the
ONE deliberate async def cloned from stream_skill_runs with the RESEARCH terminal set.
Output: `research_routes.py`, the `read_latest_research_run_dict` helper, the router mount, and the
trigger/SSE/denial/attempt-cap test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-research-trigger-progress-bridge/16-CONTEXT.md
@.planning/phases/16-research-trigger-progress-bridge/16-RESEARCH.md
@.planning/phases/16-research-trigger-progress-bridge/16-PATTERNS.md
@.planning/phases/16-research-trigger-progress-bridge/16-02-SUMMARY.md

<interfaces>
<!-- Contracts extracted this session. Executor implements against these directly. -->

TRIGGER ANALOG — backend/app/api/intake_routes.py::submit_intake (lines ~1215-1260) + the
transition-map guard (_SUBMIT_TRANSITIONS / _next_submit_status, ~1184-1212):
  _RESEARCH_TRANSITIONS = {"decomposed": "in_research"}   # ONLY reachable target
  def trigger_research(intake_id, background: BackgroundTasks, repo=Depends(get_tenant_repo),
                       identity=Depends(get_current_identity)):
    - intake = repo.get(intake_id); None → existence-hidden 404 (D-07)
    - new_status = _next_research_status(intake.status); 409 if not "decomposed" (D-06)
    - attempt-count: count prior research_runs for this intake; if >= 3 → return a
      "needs_investigation" response WITHOUT a seam call (D-04)
    - assemble brief (Plan 02 brief.assemble_brief); flip status via repo.patch(status=new_status)
    - audit.log(repo.session, actor_uid=identity.uid, event_type="intake.status_changed",
      target=str(intake_id), space_id=intake.space_id, metadata={"from": old, "to": new}) IN THE SAME TX
    - insert research_runs row (status="queued", attempt=n) via ResearchRunRepository create_in_space
      for the superadmin path
    - background.add_task(run_poll_driver, identity, intake_id, research_run_id, brief, attempt)
    - return 202 {research_run_id}

SSE ANALOG — backend/app/api/intake_routes.py::stream_skill_runs (lines ~1082-1145). Clone VERBATIM
except the terminal set. Reuse:
  - the pre-flight: run_in_threadpool(check_intake_in_scope, identity, intake_id);
    PermissionError → 403; falsy → existence-hidden 404 (stream_session.check_intake_in_scope already exists)
  - TICK_SECONDS=2.0, HEARTBEAT_SECONDS=15.0, MAX_STREAM_SECONDS=10*60, SSE_HEADERS,
    _sse_data framing, emit-on-change loop, ": ping" heartbeat, disconnect check
  - every DB touch via run_in_threadpool (blocking pg8000 must never run on the event loop)
  CHANGE:
    RESEARCH_TERMINAL = {"completed", "failed", "cancelled"}   # NOT {"succeeded","failed"} (AP-6/Pitfall 3)
  The SSE handler is the ONLY async def added; do NOT convert any other handler.

STREAM READ — backend/app/db/stream_session.py (EXTEND; mirror read_latest_run_dict lines 55-72):
  def read_latest_research_run_dict(identity, intake_id) -> dict | None:
    with tenant_session(identity) as session:
      run = ResearchRunRepository(session, identity).latest_for_intake(intake_id)
      if run is None: return None
      return {"id": str(run.id), "status": run.status,  # verbatim
              "current_stage": run.current_stage, "stage_detail": run.stage_detail,
              "cost_usd_total": str(run.cost_usd_total) if run.cost_usd_total is not None else None,
              "started_at": ..., "completed_at": ..., "error_message": run.error_message}

ROUTER MOUNT — backend/app/main.py: routers ride protected_router.include_router(...). intake_router
uses prefix="/intakes". Create research_router = APIRouter(prefix="/intakes", tags=["research"]) and
add protected_router.include_router(research_router) alongside the intake_router include (~line 143).

DENIAL ANALOGS — backend/tests/test_intake_cross_tenant.py + test_ai_cross_tenant.py (the pattern to
extend for research_runs). fake_tribunal_client + fake_resend fixtures from Plan 01/existing conftest.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: read_latest_research_run_dict + research_router trigger verb (SEAM-03)</name>
  <files>backend/app/db/stream_session.py, backend/app/api/research_routes.py, backend/app/main.py</files>
  <read_first>
    - backend/app/db/stream_session.py (read_latest_run_dict + check_intake_in_scope — the exact discipline to mirror)
    - backend/app/api/intake_routes.py (submit_intake + _SUBMIT_TRANSITIONS/_next_submit_status + the audit.log same-tx pattern, lines ~1174-1260)
    - backend/app/research/brief.py (assemble_brief signature — Plan 02)
    - backend/app/research/run_task.py (run_poll_driver signature — Plan 02)
    - backend/app/db/repository.py (ResearchRunRepository — create_in_space / latest_for_intake / a count-for-intake accessor for the attempt cap)
    - backend/app/main.py (router include wiring, ~lines 128-165)
    - backend/app/db/ai_session.py (create_running_skill_run — the "verify in scope, insert, release before 202" precedent for the superadmin create_in_space path)
  </read_first>
  <behavior>
    - trigger_research on a decomposed intake: flips status to in_research, inserts a research_runs row (status queued, attempt=n), audits {from,to} in the SAME tx, schedules run_poll_driver, returns 202 with research_run_id.
    - trigger on a non-decomposed status → 409.
    - trigger on a cross-tenant / missing intake → existence-hidden 404.
    - When 3 prior research_runs already exist for the intake, the 4th trigger returns a needs_investigation response and makes NO seam call / schedules NO driver (D-04).
    - read_latest_research_run_dict returns a plain dict (never an ORM row) with status carried verbatim and stage_detail included.
  </behavior>
  <action>
    Extend `stream_session.py` with `read_latest_research_run_dict(identity, intake_id)` mirroring
    `read_latest_run_dict` (plain dict, `status` verbatim, includes `current_stage`, `stage_detail`,
    `cost_usd_total` as str, timestamps, `error_message`). Create
    `backend/app/api/research_routes.py` with `research_router = APIRouter(prefix="/intakes",
    tags=["research"])`, `_RESEARCH_TRANSITIONS = {"decomposed": "in_research"}`, a
    `_next_research_status` guard (409 on miss), and `trigger_research(intake_id, background:
    BackgroundTasks, repo=Depends(get_tenant_repo), identity=Depends(get_current_identity))` following
    the `submit_intake` shape: 404 on missing, 409 on wrong status, attempt-cap check via a scoped
    count of `research_runs` for the intake (>=3 → return needs_investigation, no seam call, no
    driver), `brief.assemble_brief(...)`, `repo.patch(status="in_research")`, `audit.log(...)` in the
    SAME tx, insert the `research_runs` row (queued, attempt=n) through `ResearchRunRepository`
    (`create_in_space` for the superadmin path), then `background.add_task(run_poll_driver, identity,
    intake_id, research_run_id, brief, attempt)` and return 202 `{research_run_id}`. Register the
    router in `main.py` via `protected_router.include_router(research_router)` next to the intake_router
    include. Keep `trigger_research` a sync `def` (pg8000 threadpool).
  </action>
  <verify>
    <automated>MISSING — Cloud Build only. Run: pytest backend/tests/test_research_routes.py -k "trigger or attempt" -x</automated>
  </verify>
  <acceptance_criteria>
    - `stream_session.py` defines `read_latest_research_run_dict` returning a plain dict with `status` verbatim and `stage_detail`.
    - `research_routes.py` defines `_RESEARCH_TRANSITIONS = {"decomposed": "in_research"}` and `trigger_research`.
    - `trigger_research` audits `{from,to}` on `repo.session` in the same tx and calls `background.add_task(run_poll_driver, ...)`.
    - The 4th-attempt path returns a needs_investigation response and does NOT schedule the driver.
    - `main.py` includes `research_router` under `protected_router`.
  </acceptance_criteria>
  <done>Trigger verb flips status, inserts the run, audits, and schedules the driver; attempt cap enforced; router mounted; scoped read helper added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSE research stream (RUN-01, dynamic stage trace)</name>
  <files>backend/app/api/research_routes.py, backend/tests/test_research_routes.py</files>
  <read_first>
    - backend/app/api/intake_routes.py (stream_skill_runs async handler, lines ~1082-1145 — clone verbatim except the terminal set + the read function)
    - backend/app/db/stream_session.py (read_latest_research_run_dict from Task 1 + check_intake_in_scope)
    - backend/tests/conftest.py (SSE test helpers used by the skill-run stream tests, if any)
  </read_first>
  <behavior>
    - GET /intakes/{id}/research/stream emits a snapshot frame at connect, then emit-on-change frames, a ": ping" heartbeat ~every 15s, and closes on a terminal status in {completed, failed, cancelled}.
    - A null-space user → 403; a cross-tenant / missing intake → existence-hidden 404 (pre-flight before the stream opens).
    - Every DB read goes through run_in_threadpool (blocking pg8000 never on the event loop).
    - The frame carries current_stage + stage_detail so the frontend can render the stage list dynamically (no hardcoded count).
  </behavior>
  <action>
    Add `stream_research_run(intake_id, request, identity=Depends(get_current_identity))` as an
    `async def` in `research_routes.py`, cloned from `stream_skill_runs`. Define
    `RESEARCH_TERMINAL = {"completed", "failed", "cancelled"}` (NOT the skill-run set). Reuse the
    pre-flight (`run_in_threadpool(check_intake_in_scope, ...)` → 403 on PermissionError, 404 on
    falsy), the `TICK_SECONDS`/`HEARTBEAT_SECONDS`/`MAX_STREAM_SECONDS`/`SSE_HEADERS` constants and the
    `_sse_data` framing (import or re-declare locally — do not fork the transport semantics), and the
    emit-on-change loop, swapping `read_latest_run_dict` for `read_latest_research_run_dict` and the
    terminal set for `RESEARCH_TERMINAL`. This is the ONLY new async handler. Register it on
    `research_router` and ensure the literal `/research/stream` route is declared before any
    parameterized sibling. Add tests to `test_research_routes.py`: the stream emits frames and closes
    on `completed` (assert it does NOT hang past a `completed` status), and a `cancelled`/`failed`
    terminal also closes.
  </action>
  <verify>
    <automated>MISSING — Cloud Build only. Run: pytest backend/tests/test_research_routes.py::test_research_stream_terminal_set -x</automated>
  </verify>
  <acceptance_criteria>
    - `research_routes.py` defines `stream_research_run` as `async def` with `RESEARCH_TERMINAL = {"completed","failed","cancelled"}`.
    - `grep -v '^#' backend/app/api/research_routes.py | grep -c succeeded` returns 0 (research terminal set only).
    - `test_research_stream_terminal_set` asserts the stream closes on `completed` (does not hang).
    - The SSE read goes through `run_in_threadpool(read_latest_research_run_dict, ...)`.
    - Only one new async def is added.
  </acceptance_criteria>
  <done>SSE stream mirrors research_runs to the browser with a dynamic stage trace and the correct research terminal set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-tenant denial + SEAM-04 brief-guard tests (isolation gate)</name>
  <files>backend/tests/test_research_routes.py, backend/tests/test_research_cross_tenant.py</files>
  <read_first>
    - backend/tests/test_intake_cross_tenant.py (the two-space denial fixture pattern to extend)
    - backend/tests/test_ai_cross_tenant.py (AI-surface denial precedent)
    - backend/tests/conftest.py (fake_tribunal_client + fake_resend + two-space identity fixtures)
    - backend/app/api/research_routes.py (the trigger + stream under test from Tasks 1-2)
  </read_first>
  <behavior>
    - Space B's superadmin/user cannot trigger or stream space A's intake research — existence-hidden 404.
    - A null-space user hitting the stream → 403.
    - The assembled brief for a trigger never contains [INTERACTIVE_REPORT] and includes the enumerated questions (SEAM-04 asserted at the trigger boundary).
    - A trigger on a wrong-status intake → 409.
  </behavior>
  <action>
    Add `test_research_cross_tenant.py` extending the two-space denial pattern: assert a cross-tenant
    `POST /intakes/{A}/research` and `GET /intakes/{A}/research/stream` from space B both return 404
    (existence-hidden), and a null-space user gets 403 on the stream. In `test_research_routes.py`
    add `test_trigger_decomposed_ok` (202 + status flipped + driver scheduled, using
    `fake_tribunal_client`), `test_trigger_wrong_status_409`, `test_brief_never_opts_into_gates`
    (assert the brief passed to `create_run` via the fake has no `[INTERACTIVE_REPORT]` and includes
    the questions), `test_attempt_cap_3` (4th attempt → needs_investigation, no `create_run` call),
    and `test_completion_mail_to_trigger_user` (drive the fake metrics to `completed`, assert
    `fake_resend` recipient is the acting superadmin). Add the `research_runs` cross-tenant cases to
    the intake denial suite so the two-suite CI gate covers the new surface (STATE v1.1 blocker).
  </action>
  <verify>
    <automated>MISSING — Cloud Build only. Run: pytest backend/tests/test_research_cross_tenant.py backend/tests/test_research_routes.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_research_cross_tenant.py` asserts cross-tenant trigger AND stream both return 404, and null-space stream returns 403.
    - `test_research_routes.py::test_brief_never_opts_into_gates` asserts the brief handed to `create_run` has no `[INTERACTIVE_REPORT]` and includes enumerated questions.
    - `test_attempt_cap_3` asserts the 4th attempt makes no `create_run` call.
    - `test_completion_mail_to_trigger_user` asserts the mail recipient is the acting superadmin.
    - All new tests use `fake_tribunal_client` / `fake_resend` — none hit the real internal API.
  </acceptance_criteria>
  <done>Cross-tenant trigger + stream denied (existence-hidden 404), null-space 403, brief-guard + attempt-cap + mail-recipient proven; research_runs added to the denial suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → intake trigger/SSE routes | superadmin Bearer; space_id derived from the verified Identity, never request input |
| browser → internal Tribunal API | MUST NOT be reachable directly (bridged via intake SSE only) |
| intake DB session → research_runs | RLS + GUC confine to the caller's space |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Information Disclosure | cross-tenant research-run read (trigger or SSE) | mitigate | tenant_session + existence-hidden 404 pre-flight; day-one denial tests (test_research_cross_tenant.py) |
| T-16-09 | Elevation/bypass | browser calling internal Tribunal API directly | mitigate | no client route to Tribunal; SSE reads the intake mirror only; Tribunal is internal-only (Phase 14) |
| T-16-10 | Information Disclosure | client reaching research progress before delivery | mitigate | D-08: no client-facing research route added anywhere in this plan (REPORT-02) |
| T-16-11 | Tampering | forged X-Nestor-Tenant-Id via trigger input | mitigate | space_id set from the verified Identity in the seam headers (Plan 02), never from request body/path |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan (audit N/A) |
</threat_model>

<verification>
- Cloud Build: `pytest backend/tests/test_research_routes.py backend/tests/test_research_cross_tenant.py -x` green.
- Grep gate: `grep -v '^#' backend/app/api/research_routes.py | grep -c succeeded` returns 0.
- Two-suite gate: intake full suite + Tribunal critical gate green after this wave.
</verification>

<success_criteria>
- Trigger flips decomposed→in_research, assembles the brief, inserts the run, schedules the driver, returns 202 (SEAM-03).
- Attempt cap (3) + confirm-only-on-server enforcement structural via the transition map + count.
- SSE stream renders the dynamic stage trace and closes on the research terminal set (RUN-01).
- Cross-tenant trigger + stream denied existence-hidden; the denial suite covers research_runs.
</success_criteria>

<output>
Create `.planning/phases/16-research-trigger-progress-bridge/16-03-SUMMARY.md` when done.
</output>
