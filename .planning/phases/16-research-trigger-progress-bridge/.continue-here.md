# Phase 16 — Session Handoff (2026-07-21, ~20:30 local / 18:30 UTC)

## Where we are

`/gsd-execute-phase 16` — plans 16-01..16-04 **complete and merged to master** (worktree
waves 1-3, spot-checked, tracking updated). Plan **16-05 is parked at its human checkpoint**:
Task 1 (runbook § Phase 16 + 16-HUMAN-UAT.md) is done and merged; Task 2 (operator live UAT
run) is **in progress and blocked on an open defect** (see "Open mystery" below).
16-05 has **NO SUMMARY yet** — the plan is not complete. Do NOT mark the phase complete.

ROADMAP/STATE were updated in the working tree for 16-01..16-04 but are **uncommitted**
(`commit_docs=false` in config); the phase dir artifacts ARE committed (force-added).

## What the live UAT surfaced (7 defects found + fixed, 1 open)

1. **Radix dialog crash (fixed, deployed).** React error #185 opening the Start-research
   AlertDialog. Root cause: `focus-scope@1.1.10` unstable composed-ref loop under React
   19.2.7 ([radix#3963](https://github.com/radix-ui/primitives/issues/3963)). Fixed by
   `alert-dialog@1.1.20` + `dialog@1.1.20` (pins focus-scope 1.1.13). Commits `1dd447c`
   (pin all radix exact) + `6cfa41e` (dialog chain upgrade). NOTE: frontend/package-lock.json
   IS committed and drives `npm ci` in Docker — the "no lockfile" CLAUDE.md note is wrong/stale.
2. **Brief was EMPTY on a rich intake (fixed, deployed).** THREE stacked causes:
   a. `read_brief_inputs` read legacy `nestor.research_questions`/`decompositions` tables
      that **nothing in the GCP stack ever writes**;
   b. answers map read `a.value` (Text) only — list/object answers live in **`a.value_json`** (JSONB);
   c. wrong keys: AI-review writes the validated list under **`research_questions`**
      (AIReviewPanel), client raw field is `questions`, Nestor proposals are
      `extra_questions_proposed` (`{text, rationale, approved}` — approved only).
   Fixes in `backend/app/research/brief.py` + `backend/app/db/stream_session.py`
   (commits `f37fefb` + the value_json commit). Brief now also folds a ≤4000-char
   context-pack excerpt (via `intake.context_pack_artifact_id` → `research_artifacts.text_content`).
3. **Engine clarification gate (by design, now bypassed for intake briefs).** Tribunal
   Stage 1 `adaptive_intake` (gemini-2.5-flash) judges vague briefs and parks runs as
   `needs_input` with 2-3 questions. Operator decision: intake briefs are pre-validated →
   brief now emits TWO `[CLARIFICATION ANSWERS]` sections (engine `_CLAR_CAP=2` →
   force-proceed, `tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py:190`).
4. **Empty-brief 422 guard (new).** Trigger refuses (422, before flip/insert/seam call)
   when zero validated questions resolve.
5. **Retry path was unreachable (fixed, deployed).** Transition map only allowed
   `decomposed→in_research`; failed runs stranded intakes. Now: re-trigger allowed while
   `in_research` IFF latest mirror run status ∈ {failed, cancelled, needs_input}
   (`research_routes.py`). Active queued/running still 409s. Cap 3 unchanged.
6. **Panel dead-end on needs_input (fixed, deployed).** `ResearchRunProgress` now renders
   `needs_input` as the failure/retry card (frontend rev 00014-xcg).
7. **tribunal-worker stuck poll loop (restarted, NOT root-caused).** After parking run
   298954ac at 16:14:57Z the worker went silent ~2h (no claims, no logs). Claim step is
   OUTSIDE the try/except in `worker.py:280` — a hang in `claim_one` freezes it silently.
   Restarted via env nonce → rev `tribunal-worker-00007-zl9` (started 18:16:09Z, polls 2s).
   **Hardening candidate for gap-closure: heartbeat log + timeout on the claim query.**

## OPEN MYSTERY (blocking the UAT run)

User's retry at **18:08:31Z** → 202. Driver did ensure_org/ensure_project/create_run
(tribunal-api 201 at **18:08:39.877Z**) — then **zero `get_metrics` polls ever** for a new
run id. The restarted worker (polling from 18:16Z) **claims nothing** → suggests no new
queued engine run exists. Two competing theories, evidence pending from the operator:
- **Driver crashed right after create_run** (`rid = run["id"]` or first poll) → `on_error`
  → mirror row `failed` + failure MAIL sent. → Panel would show red failure card; inbox has
  "research failed" mail ~20:08 local. **ASK THE OPERATOR FOR BOTH** (asked, unanswered).
- **Idempotency dedupe** returned old run 298954ac (would mean attempt collision — but the
  retry-branch requires non-empty `prior`, contradicting attempt=1; considered unlikely).
Note: continuous 3.07s `get_metrics` polls for OLD run 298954ac in tribunal-api logs are a
**zombie driver** on a drained old nestor-api instance — ignore them; they will die with
the instance. First diagnostic next session: get panel state + inbox answer, then read the
mirror row (status/error_message via the panel or SSE) and check whether a fresh engine run
exists (seam access needed — see below).

## Live deploy state (all in project-cb01b861-cb4a-438d-b9a = Nestor Pulse, acct tools@dotto.be)

| Service | Live rev | Notes |
|---|---|---|
| nestor-api | `00030-4nl` (image backend:20260721-194917) | all backend fixes; CORS now includes BOTH frontend aliases (env `CORS_ALLOWED_ORIGINS`) |
| nestor-frontend | `00014-xcg` (frontend:20260721-195354) | radix fix + needs_input card |
| nestor-migrate job | image backend:20260721-164411 | migration **0011 applied** (`0010→0011`, exit 0) |
| tribunal-api | `00006-89x` | ANTHROPIC_API_KEY ← **Nestor_Claude2** |
| tribunal-worker | `00007-zl9` | Nestor_Claude2, `NESTOR_WORKER_STALE_MINUTES=90`, `NESTOR_TRIBUNAL_UNCAPPED=1` (stays ON, D-02), `NESTOR_RESTART_NONCE=20260721-restart1` |

Cloud Build suites: integration `152 passed / 4 failed` — the 4 are PRE-EXISTING mail-audit
defects (`test_mail_endpoints` ×2, `test_mail_locale` ×2 — WR-01/WR-04/D-16 audit-row on
failed sends), NOT Phase 16. Brief unit suite (deselected by `-m integration`): 11/11 via
ad-hoc config (scratchpad `cloudbuild.brief-tests.yaml` — recreate if needed: python:3.12-slim,
`uv pip install -e '.[dev]'`, `pytest tests/test_research_brief.py`).

## Commits this session (master, chronological)

`1045577` merge 16-01 → `73c099e` merge 16-02 → `8fc81a5`/`d034f74` merges 16-03/16-04 →
merge 16-05 task 1 → `1dd447c` radix pins → `6cfa41e` radix dialog 1.1.20 → `f37fefb`
brief sources + 422 + force-proceed → retry-path fix → value_json + research_questions key
→ needs_input panel card. All deployed.

## Gotchas rediscovered (use these, they cost hours)

- **Seam access for engine API** (read/cancel runs): needs `roles/iam.serviceAccountTokenCreator`
  on `nestor-run@…` for tools@dotto.be — the auto-mode classifier BLOCKS me granting it; the
  USER must run the grant via `!` prefix. Then:
  `gcloud auth print-identity-token --impersonate-service-account=nestor-run@project-cb01b861-cb4a-438d-b9a.iam.gserviceaccount.com --audiences="https://tribunal-api-ybkr7metoq-ew.a.run.app" --include-email`
  (**--include-email is mandatory** or app rejects "caller is not the intake backend").
  Requests need `X-Nestor-Tenant-Id` (= intake's space_id — UNKNOWN for smoke intake
  e08620c5; NOT 1464b60d-0c20-4c4e-bcf0-27b0301bdba5, that returned 404), plus
  `X-Acting-User-Id` + `X-Acting-User-Email`, plus `-d ''` on POST (GFE 411 otherwise).
  **Grant was REVOKED at end of session** (clean).
- **Tribunal audit trail**: `gs://project-cb01b861-cb4a-438d-b9a-nestor-audit/runs/<run_id>/`
  — per-LLM-call JSON incl. full prompts (how the empty brief was proven). No tenant_id inside.
- Old parked engine run `298954ac-fad8-4519-b5d8-5067ca6ec9f2` (needs_input, 3 questions)
  still parked — costs nothing; cancel via `POST /api/runs/{id}/cancel` once tenant known.
- Frontend deploy recipe: `gcloud builds submit frontend --config=frontend/cloudbuild.yaml
  --substitutions="_IMAGE=…/nestor/frontend:<ts>,_API_BASE_URL=https://nestor-api-1055853212188.europe-west1.run.app,_FB_API_KEY=AIzaSy…,_FB_AUTH_DOMAIN=project-….firebaseapp.com,_FB_PROJECT_ID=project-…"`
  (values in `frontend/.env.local`), then `gcloud run deploy nestor-frontend --image=…`.
- Local dev: `npm run dev` in frontend/ → localhost:8081 (CORS-allowed) against live API —
  used to get unminified React errors. A dev server may still be running on 8081.

## Deferred / follow-ups (record in 16-05 SUMMARY or STATE when closing)

- Pre-existing effect loops spamming max-update-depth warnings: `AIReviewPanel.tsx:254`,
  `AISkillsPanel.tsx:40` (dev console evidence) — likely contributed to prod radix crash.
- 4 pre-existing mail-audit test failures (above).
- FR/EN catalog parity for new research i18n keys (nl fallback covers).
- Worker loop hardening (heartbeat + claim timeout; claim outside try).
- IaC drift ADDED this session: CORS origins env, Nestor_Claude2 mounts (+IAM binding),
  stale-window 90, restart nonce, migrate-job image bump — none in Terraform (apply is
  FORBIDDEN on this project anyway).
- CLAUDE.md "no lockfile committed" claim is stale — package-lock.json exists + npm ci.
- Zombie old-revision driver still polling 298954ac — dies with the instance; harmless.

## Next session — exact order

1. Get from operator: (a) intake panel state after hard-refresh (spinner vs failure card +
   error text), (b) "research failed" mail ~20:08 local yes/no. This decides the open mystery.
2. If driver crashed: find the exact throw between `create_run` return and first
   `get_metrics` (`backend/app/research/run_task.py` call_fn); likely add defensive logging
   around the seam calls + fix; rebuild+deploy backend (recipe above).
3. If dedupe/attempt collision: audit `list_for_intake` superadmin visibility on
   research_runs in the ROUTE session (memory: superuser-voids-RLS class).
4. Re-run the live UAT click (attempt counter: mirror rows exist for attempts 1 and 2 —
   cap is 3; if a THIRD row was inserted at 18:08, the NEXT trigger returns
   needs_investigation — check mirror rows first; may need cap bump or row cleanup).
5. On green run: record run id/cost/duration/verify_chain in 16-HUMAN-UAT.md, set
   14-HUMAN-UAT item 1 → PASS, write 16-05-SUMMARY.md (include ALL fix commits + deferred
   list), then resume `/gsd-execute-phase 16` → verifier.

## Critical Anti-Patterns

| # | Anti-pattern | Severity | Guard |
|---|---|---|---|
| 1 | Trusting "it typechecks locally" for prod frontend crashes — the built image's dep set is what matters (and conversely: check the committed package-lock before blaming dep drift) | warning | Compare deployed bundle hash + `npm view <pkg> time`; lockfile drives npm ci |
| 2 | Debugging from logs alone when the data is auth-walled — hours lost guessing | warning | Ask the operator for on-screen evidence early; use the GCS audit bucket for engine-side ground truth |
| 3 | Assuming legacy-schema tables are populated by the new stack (research_questions/decompositions are ghosts) | warning | grep for WRITERS of a table before reading from it |
