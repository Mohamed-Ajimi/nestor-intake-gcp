---
phase: 16-research-trigger-progress-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/models/research_runs.py
  - backend/app/db/alembic/versions/0011_research_runs.py
  - backend/app/db/repository.py
  - backend/tests/conftest.py
  - backend/tests/test_research_runs_migration.py
autonomous: true
requirements: [ENGINE-03]
must_haves:
  truths:
    - "A research_runs table exists in the nestor schema with space_id RLS + superadmin bypass"
    - "A cross-tenant SELECT on research_runs returns zero rows for the wrong space GUC"
    - "The fake_tribunal_client fixture exists so no test hits the real internal Tribunal API"
  artifacts:
    - path: "backend/app/db/models/research_runs.py"
      provides: "ResearchRun ORM model (mirror table) with space-leading indexes"
      contains: "class ResearchRun"
    - path: "backend/app/db/alembic/versions/0011_research_runs.py"
      provides: "create research_runs + RLS space_isolation + superadmin_all + runtime-SA grants"
      contains: "revision = \"0011\""
    - path: "backend/tests/conftest.py"
      provides: "fake_tribunal_client fixture"
      contains: "def fake_tribunal_client"
  key_links:
    - from: "backend/app/db/alembic/versions/0011_research_runs.py"
      to: "backend/app/db/models/research_runs.py"
      via: "index names match __table_args__ 1:1 (alembic check gate)"
      pattern: "idx_research_runs_space_intake"
    - from: "0011 RLS policy"
      to: "app.current_space_id GUC"
      via: "NULLIF(current_setting('app.current_space_id', true), '')::uuid"
      pattern: "current_setting..app.current_space_id"
---

<objective>
Create the intake-side `research_runs` mirror table (model + migration 0011 with RLS), its scoped
repository accessor, and the `fake_tribunal_client` test fixture. This is the foundation every other
Phase 16 plan writes to and tests against — the table the poll driver mirrors into and the SSE stream
reads from, isolated per space from day one (Pitfall 5).

Purpose: A new tenant-owned surface is a fresh chance to reintroduce the broken-RLS bug class. This
plan lands that surface correctly (space_id FK + FORCE RLS + superadmin bypass + space-leading
indexes) BEFORE any reader/writer exists, and provides the fake seam client so no test ever hits the
real internal Tribunal API.
Output: `research_runs` table (migration 0011), `ResearchRun` model, `ResearchRunRepository`,
`fake_tribunal_client` conftest fixture, and a migration-apply test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-research-trigger-progress-bridge/16-CONTEXT.md
@.planning/phases/16-research-trigger-progress-bridge/16-RESEARCH.md
@.planning/phases/16-research-trigger-progress-bridge/16-PATTERNS.md

<interfaces>
<!-- Executor uses these directly. Extracted from the codebase this session. -->

ANALOG MODEL — backend/app/db/models/skill_run.py (copy the column + index shape):
- id: UUID PK default=uuid.uuid4
- space_id: UUID FK "nestor.organizations.id" ondelete="CASCADE" nullable=False
- intake_id: UUID FK "nestor.intakes.id" ondelete="CASCADE" nullable=False
- status: String nullable=False server_default (skill_run uses "queued")
- created_at / started_at / completed_at: DateTime(timezone=True)
- __table_args__ (space-LEADING composite indexes):
    Index("ix_skill_runs_space_id", "space_id")
    Index("idx_skill_runs_space_intake", "space_id", "intake_id")
    Index("idx_skill_runs_space_status", "space_id", "status")

ANALOG MIGRATION — backend/app/db/alembic/versions/0009_ai_ports.py (the canonical
new-tenant-table-with-RLS migration). Latest existing migration head is 0010_locale_columns.py,
so THIS migration is revision="0011", down_revision="0010".
RLS contract from 0009 (both forms load-bearing):
- ENABLE + FORCE ROW LEVEL SECURITY
- policy {table}_space_isolation:
    USING (space_id = NULLIF(current_setting('app.current_space_id', true), '')::uuid)
    WITH CHECK (space_id = NULLIF(current_setting('app.current_space_id', true), '')::uuid)
- policy {table}_superadmin_all:
    USING (current_user = 'app_superadmin')  WITH CHECK (current_user = 'app_superadmin')
- env-guarded runtime-SA GRANT DO-block (0009 lines ~131-158) copied verbatim
- explicit GRANT ... TO app_superadmin (0009 lines ~355-358)
- id server_default gen_random_uuid()

ANALOG FIXTURE — backend/tests/conftest.py fake_resend fixture (lines ~770-801). Mirror it for
fake_tribunal_client: monkeypatch the seam module's create_run/get_metrics/get_report with
capture-only fakes; use pytest.importorskip so conftest stays importable on a box without httpx;
return a capture dict {"create_run": [...], "get_metrics": [...]}.

REPOSITORY — backend/app/db/repository.py holds SkillRunRepository with .latest_for_intake(intake_id)
and .create / .create_in_space. Add ResearchRunRepository the same way (scoped, space_id from
Identity or create_in_space for the superadmin path).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResearchRun model + ResearchRunRepository</name>
  <files>backend/app/db/models/research_runs.py, backend/app/db/repository.py</files>
  <read_first>
    - backend/app/db/models/skill_run.py (the analog to copy column + index shape from)
    - backend/app/db/models/research.py (Decomposition/ResearchQuestion index naming convention)
    - backend/app/db/repository.py (SkillRunRepository: latest_for_intake, create, create_in_space)
    - backend/app/db/base.py (Base import path)
  </read_first>
  <behavior>
    - ResearchRun table name is "research_runs" with space_id + intake_id FKs to nestor.organizations.id / nestor.intakes.id (ondelete CASCADE).
    - status carries Tribunal literals VERBATIM: server_default "queued"; values {queued, running, completed, failed, cancelled} — NEVER remapped to skill-run {succeeded, failed}.
    - __table_args__ index names: ix_research_runs_space_id, idx_research_runs_space_intake (space_id, intake_id), idx_research_runs_space_status (space_id, status) — these MUST match the migration 1:1.
    - ResearchRunRepository.latest_for_intake(intake_id) returns the newest run row (order by created_at desc) scoped to the current space.
  </behavior>
  <action>
    Create `backend/app/db/models/research_runs.py` defining a `ResearchRun(Base)` model on
    `__tablename__ = "research_runs"`. Copy the id/space_id/intake_id/status/created_at/started_at/completed_at
    columns and the three space-leading composite indexes from `SkillRun`, renaming index prefixes to
    `research_runs`. Add these NEW columns (all nullable except attempt): `tribunal_run_id` String,
    `current_stage` String, `stage_detail` JSONB (shape `{stage_key: {items:[{name,status}]}}`),
    `cost_usd_total` Numeric, `attempt` Integer NOT NULL server_default "1" (D-04 attempt tracking),
    `error_message` Text, and `output_markdown` Text (persist raw output on completion per A4 —
    keeps Phase 17 a pure UI add). Carry `status` verbatim; server_default "queued".
    Then add `ResearchRunRepository` to `backend/app/db/repository.py` mirroring `SkillRunRepository`:
    scoped `create` / `create_in_space` and `latest_for_intake(intake_id)` (newest by created_at desc).
    Do NOT add trigger/poll logic here — this is model + repo only.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('backend/app/db/models/research_runs.py').read()); ast.parse(open('backend/app/db/repository.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/db/models/research_runs.py` defines `class ResearchRun` with `__tablename__ = "research_runs"`.
    - The model declares `tribunal_run_id`, `current_stage`, `stage_detail` (JSONB), `cost_usd_total` (Numeric), `attempt` (Integer NOT NULL), `error_message`, `output_markdown` columns.
    - `__table_args__` contains exactly the three index names `ix_research_runs_space_id`, `idx_research_runs_space_intake`, `idx_research_runs_space_status`.
    - `backend/app/db/repository.py` defines `ResearchRunRepository` with a `latest_for_intake` method.
    - No remap of status literals anywhere (grep the model for `succeeded` returns nothing).
  </acceptance_criteria>
  <done>ResearchRun model + ResearchRunRepository exist; index names match the intended migration; status carried verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0011 (create research_runs + RLS + grants) + migration-apply test</name>
  <files>backend/app/db/alembic/versions/0011_research_runs.py, backend/tests/test_research_runs_migration.py</files>
  <read_first>
    - backend/app/db/alembic/versions/0009_ai_ports.py (the RLS + grants + _space_id_col + _enable_rls helpers to copy verbatim)
    - backend/app/db/alembic/versions/0010_locale_columns.py (confirm it is the current head → down_revision)
    - backend/app/db/models/research_runs.py (index names the migration must match 1:1)
    - backend/tests/conftest.py (existing migration-apply / db harness fixtures to reuse)
  </read_first>
  <action>
    Create `0011_research_runs.py` with `revision = "0011"`, `down_revision = "0010"`. In `upgrade()`,
    `op.create_table("research_runs", ...)` in schema `nestor` with: `id` UUID PK
    server_default `gen_random_uuid()`; `space_id` UUID NOT NULL FK `nestor.organizations.id`
    ondelete CASCADE; `intake_id` UUID NOT NULL FK `nestor.intakes.id` ondelete CASCADE; `status`
    String NOT NULL server_default `'queued'`; `tribunal_run_id` String; `current_stage` String;
    `stage_detail` JSONB; `cost_usd_total` Numeric; `attempt` Integer NOT NULL server_default `'1'`;
    `error_message` Text; `output_markdown` Text; `created_at`/`started_at`/`completed_at`
    timestamptz (`created_at` server_default `now()`). Create the three indexes with names matching
    the ORM `__table_args__` exactly. Copy from 0009 verbatim: the `_enable_rls`-style ENABLE+FORCE
    RLS, the `research_runs_space_isolation` policy (USING + WITH CHECK on
    `space_id = NULLIF(current_setting('app.current_space_id', true), '')::uuid`), the
    `research_runs_superadmin_all` policy (`current_user = 'app_superadmin'`), the env-guarded
    runtime-SA GRANT DO-block, and the explicit `GRANT ... TO app_superadmin`. Implement `downgrade()`
    to drop the table (policies drop with it). Then create `test_research_runs_migration.py` in the
    existing migration-apply harness asserting: table `research_runs` exists after upgrade; both RLS
    policies exist (query `pg_policies`); the three named indexes exist.
  </action>
  <verify>
    <automated>MISSING — Cloud Build only (dev box has no Python). Run: pytest backend/tests/test_research_runs_migration.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `0011_research_runs.py` sets `revision = "0011"` and `down_revision = "0010"`.
    - The migration creates `nestor.research_runs` with `space_id` NOT NULL FK and the three named indexes matching the ORM.
    - Both `research_runs_space_isolation` (NULLIF form) and `research_runs_superadmin_all` (`current_user = 'app_superadmin'`) policies are created.
    - The runtime-SA GRANT DO-block and `GRANT ... TO app_superadmin` are present (copied from 0009).
    - `test_research_runs_migration.py` asserts table existence, both policies (via pg_policies), and the three indexes.
    - `alembic check` (or the harness equivalent) shows no ORM/migration drift (index names align 1:1).
  </acceptance_criteria>
  <done>Migration 0011 applies cleanly, creates research_runs with FORCE RLS + both policies + grants; the migration-apply test asserts the schema.</done>
</task>

<task type="auto">
  <name>Task 3: fake_tribunal_client conftest fixture</name>
  <files>backend/tests/conftest.py</files>
  <read_first>
    - backend/tests/conftest.py (the fake_resend fixture ~lines 770-801 — the exact shape to mirror; importorskip pattern)
    - backend/app/research/tribunal_client.py (the module whose create_run/get_metrics/get_report will be monkeypatched — note create_run/get_metrics/get_report do NOT exist yet; that is Plan 02, but the fixture patches by name so it is written now against the agreed signatures)
  </read_first>
  <action>
    Add a `fake_tribunal_client(monkeypatch)` fixture to `backend/tests/conftest.py` mirroring the
    `fake_resend` fixture shape. Use `tc = pytest.importorskip("app.research.tribunal_client")` so the
    fixture is importable on a box without httpx. Monkeypatch `tc.create_run`, `tc.get_metrics`,
    `tc.get_report`, `tc.ensure_org`, `tc.ensure_project` with capture-only fakes: `create_run`
    captures `{brief, idempotency_key, project_id}` and returns `{"id": "fake-run-id", "status": "queued"}`;
    `get_metrics` returns a scripted status sequence ending in `completed` (support overriding the
    sequence via the returned capture dict so a failure-path test can script a `failed` terminal);
    `get_report` returns `{"markdown": "fake report", "sources": []}`; `ensure_org` returns None;
    `ensure_project` returns `"fake-project-id"`. Return the capture dict
    `{"create_run": [...], "get_metrics_calls": int, "metrics_script": [...]}`. Do NOT hit any network.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('backend/tests/conftest.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `conftest.py` defines a `fake_tribunal_client` fixture using `pytest.importorskip("app.research.tribunal_client")`.
    - The fixture monkeypatches `create_run`, `get_metrics`, `get_report`, `ensure_org`, `ensure_project` on the seam module.
    - `create_run` returns `{"id": "fake-run-id", "status": "queued"}` and records `brief` + `idempotency_key`.
    - The metrics script is overridable so a test can drive a `failed` terminal.
    - No fixture code performs a real network call.
  </acceptance_criteria>
  <done>fake_tribunal_client fixture exists, importorskip-guarded, capture-only, network-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intake app-role DB session → research_runs rows | RLS must confine reads/writes to the caller's space_id GUC |
| superadmin DB session → research_runs rows | No GUC; bypass policy grants cross-space reach (audited) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | research_runs cross-tenant read | mitigate | FORCE RLS + space_isolation policy (NULLIF GUC form) copied verbatim from 0009; migration test asserts the policy exists |
| T-16-02 | Elevation | superadmin bypass via missing policy | mitigate | research_runs_superadmin_all policy (`current_user = 'app_superadmin'`) — without it a superadmin (no GUC) cannot touch the table; asserted in migration test |
| T-16-03 | Tampering | supply-chain (npm/pip installs) | accept | This plan installs NO packages (Package Legitimacy Audit: N/A); no install task exists |
</threat_model>

<verification>
- Cloud Build: `pytest backend/tests/test_research_runs_migration.py -x` green (table + 2 policies + 3 indexes).
- `alembic check` shows no drift between `ResearchRun.__table_args__` and migration 0011 index names.
- Full intake suite still imports (conftest fixture parse-ok; no real network in fixtures).
</verification>

<success_criteria>
- `nestor.research_runs` exists with space_id FK, FORCE RLS, both policies, runtime-SA + superadmin grants.
- Index names match the ORM 1:1 (alembic check gate green).
- `ResearchRunRepository.latest_for_intake` available for downstream plans.
- `fake_tribunal_client` fixture available so no downstream test hits the real internal API.
</success_criteria>

<output>
Create `.planning/phases/16-research-trigger-progress-bridge/16-01-SUMMARY.md` when done.
</output>
