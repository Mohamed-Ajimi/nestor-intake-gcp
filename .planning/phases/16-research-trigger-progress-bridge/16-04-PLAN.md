---
phase: 16-research-trigger-progress-bridge
plan: 04
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - frontend/src/lib/api/research.ts
  - frontend/src/components/intake/ResearchRunProgress.tsx
  - frontend/src/lib/intake-phase.ts
  - frontend/src/components/intake/NextStepBanner.tsx
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/lib/i18n/locales/nl/intake.json
autonomous: true
requirements: [RUN-01, SEAM-03]
must_haves:
  truths:
    - "Clicking Start research opens a confirm dialog; only on confirm does the 202 trigger fire (D-03)"
    - "During in_research the admin sees a progress panel rendering the stage list DYNAMICALLY from the run's stage trace (no hardcoded 9)"
    - "On completion the panel collapses to a summary card (completed timestamp, total cost, duration); on failure it shows what failed + re-trigger affordance"
    - "The client-facing UI is unchanged during in_research (no research surface added anywhere client-visible)"
  artifacts:
    - path: "frontend/src/components/intake/ResearchRunProgress.tsx"
      provides: "dynamic-stage progress panel + summary/failure end states, SSE-first with poll fallback"
      contains: "ResearchRunProgress"
    - path: "frontend/src/lib/api/research.ts"
      provides: "triggerResearch() + openResearchStream()"
      contains: "openResearchStream"
  key_links:
    - from: "frontend/src/lib/api/research.ts"
      to: "/intakes/{id}/research/stream"
      via: "raw fetch + ReadableStream reader, RESEARCH_TERMINAL set"
      pattern: "research/stream"
    - from: "frontend/src/components/intake/ResearchRunProgress.tsx"
      to: "run.stage_detail / stages[]"
      via: "map over the stage array (no hardcoded count)"
      pattern: "\\.map\\("
    - from: "frontend/src/components/intake/NextStepBanner.tsx"
      to: "AlertDialog confirm → triggerResearch"
      via: "onConfirm fires the 202 (D-03)"
      pattern: "AlertDialog"
---

<objective>
Build the admin-side research experience: a `Start research` confirm dialog (D-03), a live progress
panel that renders the stage list dynamically from the run's stage trace (RUN-01, D-07) with a summary
card / failure end state (D-09), and the API client + SSE reader that feed it — while leaving the
client-facing UI completely unchanged during `in_research` (D-08).

Purpose: This is the operator's window into a run. It reuses the proven SkillRunProgress SSE-first
hook and intake design language, but renders the FULL dynamic stage list so Phase 15's future added
pass costs the UI nothing.
Output: `research.ts`, `ResearchRunProgress.tsx`, additive `derivePhase` inputs, the confirm-dialog
trigger in `NextStepBanner`, the panel mounted on the admin detail route, and the NL i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-research-trigger-progress-bridge/16-CONTEXT.md
@.planning/phases/16-research-trigger-progress-bridge/16-RESEARCH.md
@.planning/phases/16-research-trigger-progress-bridge/16-PATTERNS.md

<interfaces>
<!-- Backend contract the frontend renders against (from Plan 03). Extracted this session. -->

SSE FRAME shape (GET /intakes/{id}/research/stream, from read_latest_research_run_dict):
  { id, status, current_stage, stage_detail, cost_usd_total, started_at, completed_at, error_message }
  - status ∈ {queued, running, completed, failed, cancelled}  (VERBATIM — never remapped)
  - stage_detail: JSONB { stage_key: { items: [{ name, status }] } }  — render the stage list from THIS
    (dynamic; NO hardcoded 9). status values done/running/pending drive per-stage state.
  - cost_usd_total: string | null ; timestamps ISO string | null.
  RESEARCH_TERMINAL = {"completed","failed","cancelled"}  (NOT {"succeeded","failed"}).

TRIGGER endpoint: POST /intakes/{id}/research → 202 { research_run_id }.

FRONTEND ANALOGS:
- frontend/src/lib/api/skillRunStream.ts — clone openSkillRunStream VERBATIM except:
    const RESEARCH_TERMINAL = new Set(["completed","failed","cancelled"]);
    URL = apiUrl(`/intakes/${intakeId}/research/stream`)
  Reuse currentIdToken() Bearer (never in URL/log), apiUrl(), raw fetch + getReader() frame parsing,
  ": ping" skip, JSON.parse-in-try/catch malformed-skip, null-snapshot guard, 404/401 → onFallback
  (no retry), 3× backoff, close()=abort. DO NOT wrap apiFetch for the stream (it buffers).
- frontend/src/lib/api/skillRuns.ts — triggerResearch mirrors getSkillRunFull: apiFetch over the
  token-attaching transport. triggerResearch(intakeId) -> Promise<ApiResult<{research_run_id:string}>>.
- frontend/src/components/intake/SkillRunProgress.tsx — the useActiveSkillRun SSE-first+poll-fallback
  hook (lines ~60-177) + the intake-design-language panel (~244-268): border-l-4 bg-paperLight,
  font-mono text-[11px] uppercase tracking-wider labels, #FF2D87 accent, tabular-nums elapsed clock,
  role="status" aria-live="polite".
- frontend/src/components/intake/NextStepBanner.tsx — awaiting_research_start case (lines ~258-278)
  already has onStartAutoResearch + busy.startResearch + the PrimaryBtn; in_research case (~280-283)
  currently static. AlertDialog pattern lives in frontend/src/routes/admin.spaces.tsx (~226-247):
  <AlertDialog open onOpenChange> → AlertDialogContent/Header/Title/Description/Footer with
  AlertDialogCancel + AlertDialogAction (action onClick fires the trigger only on confirm).
- frontend/src/lib/intake-phase.ts — derivePhase (lines 31-76). in_research derivation currently keys
  off hasResearchArtifacts (Pitfall 6 — NO writer in this flow). ADDITIVE change only.

DETAIL ROUTE mount: frontend/src/routes/admin.pulse.intakes.$id.tsx — NextStepBanner mounts at
~line 1051; onStartAutoResearch defined ~line 716; hasResearchArtifacts is FALSE this milestone
(~line 271). Mount ResearchRunProgress for status === "in_research".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: research.ts API client + SSE reader</name>
  <files>frontend/src/lib/api/research.ts</files>
  <read_first>
    - frontend/src/lib/api/skillRunStream.ts (the FULL SSE reader to clone — terminal set + URL are the only changes)
    - frontend/src/lib/api/skillRuns.ts (triggerResearch shape + the never-fork-transport convention)
    - frontend/src/lib/api/client.ts (currentIdToken, apiFetch, ApiResult type)
    - frontend/src/lib/firebase.ts (apiUrl base builder)
  </read_first>
  <behavior>
    - triggerResearch(intakeId) POSTs the trigger and returns ApiResult<{research_run_id: string}> (never throws).
    - openResearchStream reads the SSE stream, calls onEvent per frame (null-safe), onTerminal once status ∈ {completed,failed,cancelled}, and onFallback on no-token / 401·404 / backoff exhausted.
    - The reader never places the id token in the URL or logs; malformed frames are skipped.
  </behavior>
  <action>
    Create `frontend/src/lib/api/research.ts`. Add `triggerResearch(intakeId): Promise<ApiResult<{
    research_run_id: string }>>` mirroring `getSkillRunFull` (apiFetch, method POST). Clone
    `openSkillRunStream` as `openResearchStream(intakeId, onEvent, onTerminal, onFallback)` VERBATIM
    except: `const RESEARCH_TERMINAL = new Set(["completed","failed","cancelled"]);` and the URL
    `apiUrl(`/intakes/${intakeId}/research/stream`)`. Define a `ResearchRun` type matching the SSE
    frame shape (id, status, current_stage, stage_detail, cost_usd_total, started_at, completed_at,
    error_message). Reuse `currentIdToken` + `apiUrl`; do NOT wrap `apiFetch` for the stream.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -F "research.ts" || echo "research.ts typechecks clean")</automated>
  </verify>
  <acceptance_criteria>
    - `research.ts` exports `triggerResearch` and `openResearchStream` and a `ResearchRun` type.
    - `openResearchStream` uses `RESEARCH_TERMINAL = new Set(["completed","failed","cancelled"])` and the `/research/stream` URL.
    - The reader reuses `currentIdToken` (Bearer only) and `apiUrl`; the token never appears in the URL.
    - `npx tsc --noEmit` reports no errors originating in `research.ts`.
  </acceptance_criteria>
  <done>research.ts provides a return-no-throw trigger + a cloned SSE reader with the research terminal set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ResearchRunProgress panel (dynamic stages + summary/failure end states)</name>
  <files>frontend/src/components/intake/ResearchRunProgress.tsx, frontend/src/lib/i18n/locales/nl/intake.json</files>
  <read_first>
    - frontend/src/components/intake/SkillRunProgress.tsx (the useActiveSkillRun SSE-first+poll-fallback hook and the panel styling to copy)
    - frontend/src/lib/api/research.ts (openResearchStream + ResearchRun type from Task 1)
    - frontend/src/lib/i18n/locales/nl/intake.json (existing nextStep.* keys to extend for the panel + summary card copy)
  </read_first>
  <behavior>
    - The panel renders one row per stage from stage_detail/stages, each showing done/running/pending state — the list length is data-driven (a run with 10 stages renders 10 rows; no hardcoded count).
    - Running cost (cost_usd_total) and elapsed time render while the run is active.
    - On a terminal status the panel collapses to a summary card: completed timestamp, total cost, duration, stages green (D-09). On failed/cancelled it shows what failed (error_message) + a re-trigger affordance.
    - Uses SSE-first via openResearchStream with the existing bounded poll fallback pattern; cleans up on unmount.
  </behavior>
  <action>
    Create `frontend/src/components/intake/ResearchRunProgress.tsx`. Copy the `useActiveSkillRun`
    hook mechanics from `SkillRunProgress.tsx` (SSE primary via `openResearchStream`, bounded poll
    fallback, cancelled-cleanup flag, terminal → `stream.close()`) into a `useActiveResearchRun`
    hook. Render the intake-design-language panel (`border-l-4`, `bg-paperLight`,
    `font-mono text-[11px] uppercase tracking-wider` labels, `#FF2D87` accent, `tabular-nums` clock,
    `role="status" aria-live="polite"`). KEY: `.map` over the stage array from `stage_detail`/`stages`
    — one row per stage with done/running/pending state — NO hardcoded 9. Add running cost + elapsed.
    On a `RESEARCH_TERMINAL` status collapse to a summary card (completed timestamp, total cost,
    duration, stages green); failure card shows `error_message` + a re-trigger button surfaced via a
    prop callback. Add the NL i18n keys the panel needs to `nl/intake.json` (panel title, stage-state
    labels, summary-card labels, failure copy) following the existing `nextStep.*` key style.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -F "ResearchRunProgress" || echo "ResearchRunProgress typechecks clean")</automated>
  </verify>
  <acceptance_criteria>
    - `ResearchRunProgress.tsx` renders stage rows via `.map` over the run's stage array (no hardcoded stage count / no literal 9).
    - The panel shows running cost and an elapsed clock while active.
    - On a terminal status it renders a summary card (timestamp, total cost, duration); on failure it shows `error_message` + a re-trigger affordance.
    - It consumes `openResearchStream` (SSE-first) with a poll fallback and cleans up on unmount.
    - New NL keys exist in `nl/intake.json`; `npx tsc --noEmit` reports no errors from this component.
  </acceptance_criteria>
  <done>Dynamic-stage progress panel with summary + failure end states, SSE-first, in the intake design language.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Confirm-dialog trigger + panel wiring + additive derivePhase</name>
  <files>frontend/src/components/intake/NextStepBanner.tsx, frontend/src/lib/intake-phase.ts, frontend/src/routes/admin.pulse.intakes.$id.tsx</files>
  <read_first>
    - frontend/src/components/intake/NextStepBanner.tsx (awaiting_research_start + in_research cases; onStartAutoResearch prop; busy.startResearch)
    - frontend/src/routes/admin.spaces.tsx (the shadcn AlertDialog confirm pattern ~lines 226-247)
    - frontend/src/lib/intake-phase.ts (derivePhase — additive change only; do NOT remove the hasResearchArtifacts branch)
    - frontend/src/routes/admin.pulse.intakes.$id.tsx (NextStepBanner mount ~1051, onStartAutoResearch ~716, hasResearchArtifacts ~271)
    - frontend/src/components/ui/alert-dialog.tsx (shadcn primitive — do NOT modify, import only)
  </read_first>
  <behavior>
    - Clicking Start research opens an AlertDialog ("Start deep research for [client]? This runs for a while and costs money"); only AlertDialogAction (confirm) fires triggerResearch → 202 (D-03). Cancel does nothing.
    - After a successful trigger the status becomes in_research and the ResearchRunProgress panel mounts on the admin detail route.
    - derivePhase still returns in_research for status === "in_research" WITHOUT depending on hasResearchArtifacts having a writer; a completed run does NOT auto-advance to awaiting_report_upload (Phase 18 owns that).
    - The client-facing UI is unchanged during in_research (no client route/component added).
  </behavior>
  <action>
    In `NextStepBanner.tsx`, wrap the `awaiting_research_start` trigger in a shadcn `AlertDialog`
    (copy the structure from `admin.spaces.tsx`): the `AlertDialogAction` `onClick` calls
    `props.onStartAutoResearch` (which now fires `triggerResearch`) only on confirm; use i18n keys for
    title/body/cancel/confirm. In `admin.pulse.intakes.$id.tsx`, change `onStartAutoResearch` to call
    `triggerResearch(intakeId)` (return-no-throw; toast on error) and mount `<ResearchRunProgress .../>`
    when `intake.status === "in_research"`, passing a re-trigger callback that re-invokes the trigger
    (respecting the 3-attempt cap enforced server-side). In `intake-phase.ts`, make the change
    ADDITIVE: keep status-level visibility so `derivePhase` returns `in_research` for the
    `in_research` status without needing a `hasResearchArtifacts` writer, and do NOT let a completed
    run flip to `awaiting_report_upload` (Pitfall 6/10). Do NOT remove the existing
    `hasResearchArtifacts` branch. Add no client-facing research surface (D-08).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -E "NextStepBanner|intake-phase|admin.pulse.intakes" || echo "wiring typechecks clean")</automated>
  </verify>
  <acceptance_criteria>
    - `NextStepBanner.tsx` wraps the Start research action in an `AlertDialog`; only the confirm action fires the trigger.
    - `admin.pulse.intakes.$id.tsx` calls `triggerResearch(intakeId)` and mounts `ResearchRunProgress` when `status === "in_research"`.
    - `intake-phase.ts` change is additive (the `hasResearchArtifacts` branch is not removed) and a completed run does not auto-advance to `awaiting_report_upload`.
    - No client-facing route or component renders research state (grep the client intake/results routes shows no ResearchRunProgress import).
    - `npx tsc --noEmit` reports no new errors in the modified files.
  </acceptance_criteria>
  <done>Confirm-dialog trigger fires the 202 only on confirm; progress panel mounts on in_research; derivePhase change is additive; client UI untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → intake SSE/trigger | superadmin Bearer attached by the reader; never in URL/log |
| client browser → intake UI | must show NO research surface during in_research (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Information Disclosure | client-visible research progress before delivery | mitigate | D-08: no client route/component added; only the admin detail route mounts ResearchRunProgress; grep-asserted |
| T-16-13 | Information Disclosure | id token leaking via SSE URL/log | mitigate | cloned reader attaches Bearer header only, never URL/log (skillRunStream discipline) |
| T-16-14 | Tampering | stale hardcoded stage list masking a Phase-15 pass | mitigate | stage list rendered dynamically via .map over the run's stage array (no hardcoded count) |
| T-16-SC | Tampering | npm installs | accept | No new npm dependency (shadcn AlertDialog + existing transports only) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean for the new/modified files.
- Grep: no `ResearchRunProgress` import in client routes (intake.$token.tsx / results.$token.tsx).
- Manual/UAT (Plan 05 operator session): progress panel renders dynamic stages + cost during a live run.
</verification>

<success_criteria>
- Start research confirm dialog fires the 202 only on confirm (D-03, SEAM-03 client side).
- Live progress panel renders the stage list dynamically + running cost, collapsing to a summary card on completion (RUN-01, D-07/D-09).
- derivePhase change is additive; a completed run does not auto-advance to report upload.
- The client-facing UI is unchanged during in_research (D-08).
</success_criteria>

<output>
Create `.planning/phases/16-research-trigger-progress-bridge/16-04-SUMMARY.md` when done.
</output>
