---
phase: 09-gcs-storage
plan: 04
type: execute
wave: 2
# wave 2 override is intentional: infra-only scope, no code dependency on 09-01;
# the wave field is the authoritative scheduler input (empty depends_on is correct).
depends_on: []
files_modified:
  - infra/main.tf
  - infra/DEPLOY-RUNBOOK.md
autonomous: false
requirements: [INFRA-03]
user_setup:
  - service: gcp-storage
    why: "Provision the GCS bucket + IAM bindings (keyless signBlob) and inject STORAGE_BUCKET — the live project is gcloud-native (IaC drift), so these are applied via the runbook, not terraform apply"
    dashboard_config:
      - task: "Run the Phase-9 runbook gcloud steps (bucket create, objectAdmin binding, serviceAccountTokenCreator self-binding, STORAGE_BUCKET env, Cloud Build image rebuild)"
        location: "infra/DEPLOY-RUNBOOK.md § Phase 9"

must_haves:
  truths:
    - "infra/main.tf declares the private GCS bucket (uniform BLA + public-access-prevention enforced), a storage.objectAdmin binding for the runtime SA, and a serviceAccountTokenCreator self-binding for keyless signBlob"
    - "infra/main.tf injects STORAGE_BUCKET into the Cloud Run service env"
    - "The deploy runbook has a Phase-9 section with the equivalent gcloud commands, a Cloud Build image-rebuild step, and a summary-checklist row"
    - "The runbook documents the combined 7+8+9 live UAT (D-13) as one deploy, one session"
  artifacts:
    - path: "infra/main.tf"
      provides: "bucket + two IAM bindings + STORAGE_BUCKET env (inert until applied — drift)"
      contains: "google_storage_bucket"
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "Phase-9 gcloud runbook + combined UAT note"
      contains: "Phase 9"
  key_links:
    - from: "infra/main.tf"
      to: "keyless signBlob"
      via: "serviceAccountTokenCreator self-binding on the runtime SA"
      pattern: "serviceAccountTokenCreator"
    - from: "infra/main.tf"
      to: "Cloud Run service"
      via: "STORAGE_BUCKET env var"
      pattern: "STORAGE_BUCKET"
---

<objective>
Author the bucket + IAM provisioning in BOTH Terraform and the gcloud runbook (D-11 dual-authoring,
because the live project is gcloud-native and Terraform state was never adopted), and document the
combined 7+8+9 live UAT (D-13). The `infra/*.tf` edits are inert until applied out-of-band; the
runbook is the applied path. The live apply + UAT is a human checkpoint — it needs GCP credentials
the executor does not hold.

Purpose: INFRA-03 requires a GCS bucket replacing nestor-uploads AND the backend having IAM
permission to mint signed URLs via signBlob. This plan lays down the bucket (private, hardened per
D-12/D-07a), the storage.objectAdmin bucket binding, and the criterion-1 keyless-signing grant
(serviceAccountTokenCreator on the SA itself), and folds the storage verification into the already-
pending combined UAT. Touches only infra/ files — zero overlap with 09-01/02/03.

Output: `main.tf` additions (bucket + 2 IAM bindings + STORAGE_BUCKET env) + a Phase-9 runbook
section + summary-checklist rows + the combined-UAT note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-gcs-storage/09-CONTEXT.md
@.planning/phases/09-gcs-storage/09-RESEARCH.md
@.planning/phases/09-gcs-storage/09-PATTERNS.md

<interfaces>
<!-- main.tf analogs to mirror: -->
google_service_account.runtime               (main.tf:213 — .email / .name for the bindings)
google_project_iam_member.runtime_*          (main.tf:220-240 — the member = serviceAccount:${...email} shape)
google_cloud_run_v2_service.api env{}        (main.tf:247, INSTANCE_CONNECTION_NAME env at :296-299 — mirror for STORAGE_BUCKET)

<!-- Resources to ADD (RESEARCH § Terraform/Infra): -->
google_storage_bucket.uploads:
  name = "${var.project}-nestor-uploads"; location = var.region;
  uniform_bucket_level_access = true; public_access_prevention = "enforced";  # D-12/D-07a
  versioning OFF (default); no lifecycle rules; force_destroy = false
google_storage_bucket_iam_member.runtime_object_admin:
  bucket = google_storage_bucket.uploads.name; role = "roles/storage.objectAdmin";
  member = "serviceAccount:${google_service_account.runtime.email}"   # least-privilege, bucket-scoped
google_service_account_iam_member.runtime_token_creator_self:
  service_account_id = google_service_account.runtime.name; role = "roles/iam.serviceAccountTokenCreator";
  member = "serviceAccount:${google_service_account.runtime.email}"   # keyless signBlob (criterion 1)
Cloud Run env: { name = "STORAGE_BUCKET", value = google_storage_bucket.uploads.name }

<!-- Runbook analog to mirror (Phase 8 layout): DEPLOY-RUNBOOK.md:173-241 -->
</interfaces>

@infra/main.tf
@infra/DEPLOY-RUNBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the bucket + two IAM bindings + STORAGE_BUCKET env to main.tf (inert; drift-noted)</name>
  <read_first>
    - infra/main.tf (google_service_account.runtime at :213; the google_project_iam_member blocks at :220-240 for the member shape; the Cloud Run service + INSTANCE_CONNECTION_NAME env at :247,:296-299 to mirror STORAGE_BUCKET; the drift-note comment style at ~:259-260)
    - infra/variables.tf (confirm var.project / var.region names used for the bucket name + location)
    - .planning/phases/09-gcs-storage/09-RESEARCH.md § Terraform / Infra (the exact resource blocks) and § Pitfall 2 (why the self-binding is a separate grant from object access)
  </read_first>
  <action>
    Add to infra/main.tf, mirroring the existing runtime-SA IAM blocks:
    - `google_storage_bucket.uploads`: name `"${var.project}-nestor-uploads"` (confirm the actual
      project var name from variables.tf), location `var.region`, `uniform_bucket_level_access = true`,
      `public_access_prevention = "enforced"` (D-07a — zero public objects), no versioning block, no
      lifecycle rules (D-12), `force_destroy = false`.
    - `google_storage_bucket_iam_member.runtime_object_admin`: bucket =
      google_storage_bucket.uploads.name, role = "roles/storage.objectAdmin", member =
      "serviceAccount:${google_service_account.runtime.email}" (bucket-scoped least privilege).
    - `google_service_account_iam_member.runtime_token_creator_self`: service_account_id =
      google_service_account.runtime.name, role = "roles/iam.serviceAccountTokenCreator", member =
      "serviceAccount:${google_service_account.runtime.email}" — this is the keyless-signBlob grant
      (criterion 1), a SEPARATE grant from object access (Pitfall 2).
    - A `STORAGE_BUCKET` env block in the Cloud Run service `containers { env {...} }`, mirroring the
      INSTANCE_CONNECTION_NAME env at :296-299, value = google_storage_bucket.uploads.name.
    Add a drift-note comment (mirroring the existing ~:259-260 style) that these resources are INERT
    until applied out-of-band via the runbook, per the STATE.md IaC-drift blocker (D-11).
  </action>
  <verify>
    <automated>grep -q 'google_storage_bucket" "uploads"' infra/main.tf && grep -q 'roles/storage.objectAdmin' infra/main.tf && grep -q 'roles/iam.serviceAccountTokenCreator' infra/main.tf && grep -q 'public_access_prevention.*enforced' infra/main.tf && grep -q 'STORAGE_BUCKET' infra/main.tf</automated>
  </verify>
  <acceptance_criteria>
    - infra/main.tf declares `google_storage_bucket.uploads` with `uniform_bucket_level_access = true` and `public_access_prevention = "enforced"` and NO versioning/lifecycle blocks
    - infra/main.tf declares `google_storage_bucket_iam_member.runtime_object_admin` with role `roles/storage.objectAdmin`
    - infra/main.tf declares `google_service_account_iam_member.runtime_token_creator_self` with role `roles/iam.serviceAccountTokenCreator` and member = the runtime SA
    - infra/main.tf Cloud Run service contains a `STORAGE_BUCKET` env whose value is the bucket name
    - A drift-note comment marks these as inert-until-applied
  </acceptance_criteria>
  <done>The bucket, both IAM bindings, and the STORAGE_BUCKET env are in main.tf, hardened per D-07a/D-12, with the drift note.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase-9 runbook section (gcloud steps + Cloud Build rebuild + combined UAT) + checklist rows</name>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md (the Phase-8 section at :173-241 to mirror: Step 8.1 image rebuild, 8.2 config apply, 8.3 verify, and the Summary checklist rows at :232-241; the IaC-drift preamble at :26-45)
    - .planning/phases/09-gcs-storage/09-RESEARCH.md § Terraform / Infra (the gcloud runbook equivalents block: buckets create, add-iam-policy-binding x2, run services update --update-env-vars) and § Pitfall 7 (image rebuild)
    - .planning/STATE.md § Pending Todos (the deferred Phase 7 UAT that D-13 folds in) and § Blockers (the IaC-drift list D-11 extends)
  </read_first>
  <action>
    Add a `## Phase 9 — GCS storage: bucket + keyless signBlob IAM + image redeploy` section to
    infra/DEPLOY-RUNBOOK.md, mirroring the Phase-8 layout, with these steps (using the env-var
    preamble from RESEARCH: GOOGLE_PROJECT, REGION, RUNTIME_SA=nestor-run@..., BUCKET=${GOOGLE_PROJECT}-nestor-uploads):
    - Step 9.1 — `gcloud storage buckets create "gs://${BUCKET}" --location="$REGION"
      --uniform-bucket-level-access --public-access-prevention` (private, hardened; D-12/D-07a).
    - Step 9.2 — `gcloud storage buckets add-iam-policy-binding "gs://${BUCKET}"
      --member="serviceAccount:${RUNTIME_SA}" --role="roles/storage.objectAdmin"` (bucket-scoped).
    - Step 9.3 — `gcloud iam service-accounts add-iam-policy-binding "$RUNTIME_SA"
      --member="serviceAccount:${RUNTIME_SA}" --role="roles/iam.serviceAccountTokenCreator"`
      (keyless signBlob — criterion 1; a separate grant from object access, Pitfall 2).
    - Step 9.4 — Rebuild the backend image via Cloud Build (new deps google-cloud-storage +
      python-multipart are NOT in the live image — Pitfall 7; reuse the Step-3/8.1 Cloud Build idiom),
      then `gcloud run services update nestor-api --region "$REGION"
      --update-env-vars="STORAGE_BUCKET=${BUCKET}"` and repoint at the new tag.
    - Step 9.5 — the COMBINED 7+8+9 live UAT (D-13): ONE deploy, ONE session — upload attachment +
      audio -> transcribe -> structure-answers / extract-insights -> apply-intake-skill streaming
      over SSE -> download artifacts via signed URLs. Note it closes the deferred Phase-7 UAT (STATE
      pending todo) and the Phase-8 UAT together, and that Phase 8's stream route must be deployed in
      the SAME image rebuild (live Cloud Run is still v12).
    Add Summary-checklist rows mirroring :239-241 (Step 9.1-9.5). Note in the IaC-drift preamble that
    the main.tf storage resources are inert until these gcloud steps run (D-11 extends the drift list).
  </action>
  <verify>
    <automated>grep -q 'Phase 9' infra/DEPLOY-RUNBOOK.md && grep -q 'gcloud storage buckets create' infra/DEPLOY-RUNBOOK.md && grep -q 'roles/iam.serviceAccountTokenCreator' infra/DEPLOY-RUNBOOK.md && grep -q 'STORAGE_BUCKET' infra/DEPLOY-RUNBOOK.md && grep -qi 'combined' infra/DEPLOY-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - infra/DEPLOY-RUNBOOK.md contains a `## Phase 9` section with the four gcloud commands (buckets create, storage add-iam-policy-binding for objectAdmin, service-accounts add-iam-policy-binding for serviceAccountTokenCreator, run services update --update-env-vars STORAGE_BUCKET)
    - The runbook documents a Cloud Build image rebuild step (Pitfall 7) for the new deps
    - The runbook documents the combined 7+8+9 UAT as one deploy/one session and notes Phase 8's stream route ships in the same rebuild
    - Summary-checklist rows for the Phase-9 steps are added
  </acceptance_criteria>
  <done>The runbook has a complete Phase-9 gcloud section, the image-rebuild step, the combined-UAT note, and checklist rows.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Apply the Phase-9 runbook live + run the combined 7+8+9 UAT (D-13)</name>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 9 (the steps authored in Task 2)
  </read_first>
  <what-built>
    The main.tf resources (inert) and the runbook Phase-9 steps are authored. The live apply needs
    GCP credentials and a deployed service — the executor cannot do this. This is the D-13 gate.
  </what-built>
  <how-to-verify>
    1. Run runbook Steps 9.1-9.4 (bucket create, both IAM bindings, Cloud Build image rebuild with
       google-cloud-storage + python-multipart AND the Phase-8 stream route, STORAGE_BUCKET env,
       repoint the service — live Cloud Run is currently v12, no stream route).
    2. Run the combined UAT (Step 9.5): log in, open an intake, upload an attachment + an audio file,
       confirm transcribe runs, run apply-intake-skill and watch the SSE progress stream, then
       download the produced artifacts via the signed URLs.
    3. Confirm criterion 1: no SA JSON key exists anywhere; downloads land as attachments with the
       original filename; a download URL expires within 15 minutes.
  </how-to-verify>
  <action>
    HUMAN-ONLY: apply the runbook steps and run the UAT with live GCP credentials. Report the
    outcome (upload/download round-trip, transcribe, SSE, signed-URL TTL/disposition) back so the
    phase can be verified. If signBlob 403s, check the serviceAccountTokenCreator self-binding
    (Pitfall 2); if uploads 422, check the FormData Content-Type guard shipped (Pitfall 3); if a
    storage endpoint 500s with ModuleNotFoundError, the image rebuild did not include the new deps
    (Pitfall 7).
  </action>
  <verify>
    <human-check>Combined 7+8+9 UAT passes: attachment + audio upload, transcribe, SSE progress, signed-URL download (attachment disposition, ≤15-min TTL), no SA JSON key anywhere.</human-check>
  </verify>
  <acceptance_criteria>
    - The bucket exists, is private (public-access-prevention enforced), and objects are namespaced by space
    - A signed download URL works, forces attachment download with the original filename, and expires ≤15 min
    - Upload + audio-transcribe + SSE-streamed apply-intake-skill run end-to-end on the deployed service
    - No SA JSON key is present in the environment (keyless signBlob)
  </acceptance_criteria>
  <resume-signal>Report UAT results (pass, or the failing step + error) so the phase can be verified.</resume-signal>
  <done>The combined 7+8+9 live UAT is executed and its result reported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime SA → GCS bucket | Bucket-scoped storage.objectAdmin (least privilege), not project-wide |
| Runtime SA → IAM signBlob | Self-impersonation for keyless signing; no key material |
| Public internet → bucket | Public-access-prevention enforced; zero public objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Elevation of Privilege | keyless signBlob grant | mitigate | serviceAccountTokenCreator self-binding is the ONLY signing grant; no SA JSON key (Task 1); guarded by ci_no_sa_json_key.sh (09-01) |
| T-09-14 | Information Disclosure | public bucket / object exposure | mitigate | uniform BLA + public_access_prevention = enforced (Task 1, D-07a) |
| T-09-15 | Elevation of Privilege | over-broad storage role | mitigate | storage.objectAdmin scoped to THIS bucket only, not project-wide (Task 1) |
| T-09-16 | Tampering | drift between IaC and live state | accept | consciously extends the STATE.md IaC-drift list (D-11); reconcile is a Phase-12 gate; runbook is the applied path (Task 2) |
</threat_model>

<verification>
- `grep 'google_storage_bucket" "uploads"' infra/main.tf` matches; both IAM bindings + STORAGE_BUCKET env present.
- `grep 'Phase 9' infra/DEPLOY-RUNBOOK.md` matches; the four gcloud commands + Cloud Build step + combined-UAT note present.
- Live apply + combined UAT executed by the human (Task 3) and reported.
</verification>

<success_criteria>
- main.tf has the private hardened bucket + storage.objectAdmin + serviceAccountTokenCreator self-binding + STORAGE_BUCKET env, drift-noted.
- Runbook has the Phase-9 gcloud steps, the image rebuild, and the combined 7+8+9 UAT.
- The combined UAT is run live and reported.
</success_criteria>

<output>
Create `.planning/phases/09-gcs-storage/09-04-SUMMARY.md` when done.
</output>
