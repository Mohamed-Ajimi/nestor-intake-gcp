---
phase: 09-gcs-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/storage/__init__.py
  - backend/app/storage/keys.py
  - backend/app/storage/gcs.py
  - backend/app/core/config.py
  - backend/scripts/ci_no_sa_json_key.sh
  - backend/tests/conftest.py
  - backend/tests/test_storage_upload.py
  - backend/tests/test_storage_signed_url.py
  - backend/tests/test_storage_delete.py
  - backend/tests/test_storage_cross_tenant.py
autonomous: false
requirements: [INFRA-03, DOC-01, DOC-02]
user_setup: []

must_haves:
  truths:
    - "The GCS client seam (app/storage/gcs.py) exists with upload_object / signed_download_url / delete_object / download_bytes and constructs no SA JSON key"
    - "build_object_key produces {space_id}/{intake_id}/{category}/{uuid}-{sanitized_filename} and never trusts a client path"
    - "signed_download_url clamps TTL to <=900s and sets response_disposition=attachment"
    - "A grep-guard fails the build if any SA JSON key reference appears under backend/app/"
    - "Wave-0 RED test scaffolds exist for upload (413/415/scoped-key/audio-source), signed-url (TTL clamp + disposition), delete (ref cleanup), and cross-tenant denial, all faking app.storage.gcs"
  artifacts:
    - path: "backend/app/storage/gcs.py"
      provides: "GCS external-client seam (4 monkeypatch targets)"
      contains: "def signed_download_url"
    - path: "backend/app/storage/keys.py"
      provides: "build_object_key + sanitize_filename + CATEGORIES + ALLOWED_EXT"
      contains: "def build_object_key"
    - path: "backend/scripts/ci_no_sa_json_key.sh"
      provides: "criterion-1 no-SA-JSON-key grep-guard"
      contains: "from_service_account_file"
    - path: "backend/tests/conftest.py"
      provides: "fake_gcs fixture"
      contains: "fake_gcs"
    - path: "backend/tests/test_storage_upload.py"
      provides: "RED upload behavior tests"
      contains: "test_upload_writes_scoped_key"
  key_links:
    - from: "backend/app/storage/gcs.py"
      to: "backend/app/core/config.py"
      via: "get_settings().storage_bucket read inside function body"
      pattern: "get_settings\\(\\)\\.storage_bucket"
    - from: "backend/tests/conftest.py"
      to: "app.storage.gcs"
      via: "monkeypatch of upload_object/signed_download_url/delete_object/download_bytes"
      pattern: "app\\.storage\\.gcs\\."
---

<objective>
Build the Phase-9 storage foundation everything else depends on: install the two new packages (gated behind a legitimacy checkpoint), create the `app/storage/` external-client seam (the GCS-twin of `app/ai/clients.py`), the key-builder/sanitizer, the non-secret bucket config field, the criterion-1 grep-guard, and the Wave-0 RED test scaffolds + `fake_gcs` fixture that plan 09-02's endpoints will turn GREEN.

Purpose: This is the interface-first plan. `app.storage.gcs` (4 functions) and `app.storage.keys.build_object_key` are the contracts the route handlers and the transcribe seam consume; the test scaffolds define the behavior those handlers must satisfy. Nothing here depends on the routes existing.

Output: `backend/app/storage/` package, config field, grep-guard, `fake_gcs` fixture, and four RED test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-gcs-storage/09-CONTEXT.md
@.planning/phases/09-gcs-storage/09-RESEARCH.md
@.planning/phases/09-gcs-storage/09-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES; consumed by 09-02 (routes + transcribe seam) and 09-01's own tests. -->

app/storage/gcs.py (NEW — mirrors app/ai/clients.py factory-seam convention):
  def upload_object(key: str, data: bytes, content_type: str | None = None) -> None
  def signed_download_url(key: str, *, ttl_seconds: int, filename: str, content_type: str | None = None) -> str
  def delete_object(key: str) -> None
  def download_bytes(key: str) -> bytes
  # bucket name read from get_settings().storage_bucket INSIDE the body — never at module top-level
  # signed_download_url clamps ttl to _MAX_TTL_S = 900 (D-10); _DEFAULT_TTL_S = 300

app/storage/keys.py (NEW):
  CATEGORIES = frozenset({"attachments", "audio", "artifacts", "reports"})
  ALLOWED_EXT = frozenset of the 16 D-04 extensions
  def sanitize_filename(name: str, *, max_len: int = 200) -> str
  def build_object_key(space_id: str, intake_id: str, category: str, filename: str) -> str
    # returns f"{space_id}/{intake_id}/{category}/{uuid.uuid4()}-{sanitize_filename(filename)}"

app/core/config.py (MOD): Settings gains
  storage_bucket: str | None = None   # env STORAGE_BUCKET — NON-secret (keyless signBlob adds zero secrets)
</interfaces>

<!-- Analog seam to copy verbatim (docstring discipline + call-time construction): -->
@backend/app/ai/clients.py
<!-- Analog fixtures to clone (fake_openai / fake_anthropic): -->
@backend/tests/conftest.py
<!-- Existing grep-guard family the new guard joins: -->
@backend/scripts/ci_no_raw_db_access.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy, then add the two dependencies</name>
  <read_first>
    - backend/pyproject.toml (current [project.dependencies] — see how google-cloud-* / fastapi are pinned)
    - .planning/phases/09-gcs-storage/09-RESEARCH.md § Package Legitimacy Audit (the [ASSUMED] table)
  </read_first>
  <what-built>
    Nothing yet — this is the legitimacy gate the research flagged. Both new packages are tagged
    [ASSUMED] (slopcheck could not run — no Python on the dev box), so per protocol each install
    is verified against the public registry before it lands.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/google-cloud-storage/ — confirm it is the official Google
       client (source repo github.com/googleapis/python-storage), latest 3.x.
    2. Open https://pypi.org/project/python-multipart/ — confirm the DISTRIBUTION name is
       `python-multipart` (NOT the deprecated `multipart` package), source github.com/Kludex/python-multipart.
    3. Confirm the pins to add to backend/pyproject.toml [project.dependencies]:
       "google-cloud-storage>=3,<4" and "python-multipart>=0.0.9".
  </how-to-verify>
  <action>
    After approval, add exactly two entries to backend/pyproject.toml [project.dependencies]:
    `google-cloud-storage>=3,<4` and `python-multipart>=0.0.9`. Do NOT add the deprecated
    `multipart` distribution. `google-auth` arrives transitively via google-cloud-storage — do
    not pin it separately. Add nothing else. Note in the SUMMARY that the live image rebuild
    (Cloud Build) is a 09-04 runbook step (Pitfall 7 — new deps are not in the running image).
  </action>
  <verify>
    <automated>grep -q 'google-cloud-storage>=3,<4' backend/pyproject.toml && grep -q 'python-multipart>=0.0.9' backend/pyproject.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - backend/pyproject.toml contains the literal strings `google-cloud-storage>=3,<4` and `python-multipart>=0.0.9`
    - backend/pyproject.toml contains NO bare `multipart` dependency entry
    - Checkpoint was approved by the human before the edit
  </acceptance_criteria>
  <resume-signal>Type "approved" once both PyPI pages confirm the packages, or name the concern.</resume-signal>
  <done>Both packages verified on PyPI and pinned in pyproject.toml; no deprecated `multipart` entry.</done>
</task>

<task type="auto">
  <name>Task 2: Create the app/storage package (keys.py, gcs.py, __init__.py) + config field + grep-guard</name>
  <read_first>
    - backend/app/ai/clients.py (the factory-seam docstring discipline + call-time construction to mirror)
    - backend/app/core/config.py (Settings class + the non-secret firebase_project_id / model_transcription fields at :67,:88 — copy the docstring discipline)
    - backend/app/ai/skills/__init__.py (the re-export pattern download_audio_bytes uses — mirror it for download_bytes)
    - backend/scripts/ci_no_raw_db_access.sh (the grep-guard family shape: grep exit-code gate)
    - .planning/phases/09-gcs-storage/09-RESEARCH.md § Pattern 1 (the signed_download_url body) and § Code Examples (sanitize_filename + build_object_key)
    - frontend/src/components/intake/FinalReportBlock.tsx (sanitizeFilenameForStorage at :24-34 — the parity source for the Python port)
  </read_first>
  <action>
    Create backend/app/storage/__init__.py re-exporting `download_bytes` (and the other three gcs
    functions) from `app.storage.gcs` so `transcribe.py` can import the package seam — mirror
    app/ai/skills/__init__.py.

    Create backend/app/storage/keys.py with: CATEGORIES frozenset {"attachments","audio","artifacts","reports"};
    ALLOWED_EXT frozenset of the 16 D-04 extensions (.pdf .docx .xlsx .pptx .txt .md .csv .png .jpg
    .jpeg .webp .m4a .mp3 .wav .webm .ogg); sanitize_filename(name, *, max_len=200) porting
    FinalReportBlock.sanitizeFilenameForStorage (NFD-normalize, drop combining marks, map em/en-dash
    to '-', whitespace to '_', drop chars outside [A-Za-z0-9._-], collapse repeated _ and -, strip
    leading/trailing ._-, cap at max_len, fallback "file"); build_object_key(space_id, intake_id,
    category, filename) that raises ValueError on a category not in CATEGORIES and returns
    f"{space_id}/{intake_id}/{category}/{uuid.uuid4()}-{sanitize_filename(filename)}".

    Create backend/app/storage/gcs.py as the external-client seam (module docstring stating it is the
    test monkeypatch seam and constructs NO DB engines/sessions — HTTP/transport only). Define
    _MAX_TTL_S = 900 (D-10 ceiling), _DEFAULT_TTL_S = 300. Read the bucket via a helper that calls
    get_settings().storage_bucket INSIDE the function body (never at module top-level). Implement:
    upload_object(key, data, content_type=None) via storage.Client().bucket(...).blob(key).upload_from_string(data, content_type=content_type);
    signed_download_url(key, *, ttl_seconds, filename, content_type=None) per RESEARCH Pattern 1 —
    clamp ttl = max(1, min(int(ttl_seconds), _MAX_TTL_S)); google.auth.default() -> creds.refresh(google.auth.transport.requests.Request());
    blob.generate_signed_url(version="v4", expiration=timedelta(seconds=ttl), method="GET",
    service_account_email=creds.service_account_email, access_token=creds.token,
    response_disposition=f'attachment; filename="{filename}"', response_type=content_type or None);
    delete_object(key) via blob.delete() treating a NotFound as success (idempotent, Pitfall 6);
    download_bytes(key) via blob.download_as_bytes(). Add a docstring note that the exact keyless
    kwargs (service_account_email + access_token — Assumption A1) must be confirmed against the
    installed generate_signed_url signature in the Cloud Build run. Construct NO SA JSON key
    anywhere — rely on ADC (google.auth.default()) only.

    Add to backend/app/core/config.py Settings: `storage_bucket: str | None = None` mapped to env
    STORAGE_BUCKET, with a docstring line stating it is NON-secret and keyless signBlob adds no
    Secret Manager resource (contrast the AI keys, deliberately NOT in Settings).

    Create backend/scripts/ci_no_sa_json_key.sh mirroring ci_no_raw_db_access.sh: scan backend/app/
    and fail (non-zero exit) if any of these appear — `from_service_account_file`,
    `service_account.json`, `GOOGLE_APPLICATION_CREDENTIALS=.*json`. Strip comment lines
    (grep -v '^[[:space:]]*#') before counting so a header explaining the ban does not self-trip;
    exit 0 when clean. Make it executable (chmod +x).
  </action>
  <verify>
    <automated>bash backend/scripts/ci_no_sa_json_key.sh && grep -q 'def build_object_key' backend/app/storage/keys.py && grep -q 'def signed_download_url' backend/app/storage/gcs.py && grep -q 'storage_bucket' backend/app/core/config.py && grep -q '_MAX_TTL_S = 900' backend/app/storage/gcs.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/storage/keys.py: `build_object_key` returns a string matching `^[^/]+/[^/]+/(attachments|audio|artifacts|reports)/[0-9a-f-]{36}-.+$` for a valid category; raises ValueError for `category="bogus"`
    - backend/app/storage/gcs.py contains `_MAX_TTL_S = 900`, `generate_signed_url`, `service_account_email=`, `access_token=`, and `response_disposition=`
    - backend/app/storage/gcs.py contains NO `from_service_account_file` and NO `service_account.json`
    - backend/app/storage/gcs.py reads the bucket via `get_settings().storage_bucket` (grep matches) with no module-top-level `storage_bucket` assignment before the first `def`
    - backend/app/core/config.py Settings contains `storage_bucket: str | None = None`
    - `bash backend/scripts/ci_no_sa_json_key.sh` exits 0 against the current tree
    - backend/app/storage/__init__.py re-exports `download_bytes`
  </acceptance_criteria>
  <done>The app/storage package, config field, and grep-guard exist; the guard passes; keys/gcs contracts match the interfaces block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add fake_gcs fixture + Wave-0 RED test scaffolds (upload, signed-url, delete, cross-tenant)</name>
  <read_first>
    - backend/tests/conftest.py (fake_openai / fake_anthropic fixtures ~:447,:572 — clone the shape; two-space seeding, set_space, two_spaces)
    - backend/tests/test_ai_transcribe.py (the faked-seam harness style, raising=False monkeypatch)
    - backend/tests/test_intake_cross_tenant.py (denial-suite template: patch engine factories only, override get_current_identity, assert EXACTLY 404, pytestmark integration, importorskip guards)
    - .planning/phases/09-gcs-storage/09-RESEARCH.md § Validation Architecture (Req->Test map with exact test names) and § Code Examples (the fake_upload / fake signed-url monkeypatch example)
    - backend/app/storage/gcs.py (the four functions these tests fake — created in Task 2)
  </read_first>
  <behavior>
    - test_upload_writes_scoped_key (DOC-02): faked upload_object; POST multipart -> captured key startswith f"{space_id}/{intake_id}/attachments/"
    - test_upload_413_over_cap (D-02/D-03): a >25 MB body -> 413
    - test_upload_415_bad_type (D-04): a disallowed extension/MIME -> 415
    - test_audio_upload_creates_source (D-07): audio upload -> a space-scoped intake_sources row with storage_path == the object key
    - test_ttl_clamped_and_disposition (DOC-01/D-10): expires_in=99999 -> the handler clamps to <=900 and the faked signer records disposition=attachment
    - test_delete_cleans_ref (D-09): faked delete_object; deleting an audio key removes the object AND the matching intake_sources row in one request
    - cross-tenant (D-08): user-A upload/signed-url/delete against user-B's intake -> 404; a forged key whose prefix is not {space}/{intake}/ -> 404; null-space user -> 403
  </behavior>
  <action>
    Add a `fake_gcs` fixture to backend/tests/conftest.py mirroring `fake_openai`: it monkeypatches
    `app.storage.gcs.upload_object`, `app.storage.gcs.signed_download_url`,
    `app.storage.gcs.delete_object`, and `app.storage.gcs.download_bytes` with capture-only fakes
    (record args into a dict the test inspects; no network). The signed_download_url fake returns a
    deterministic `https://signed.example/{key}` and records the ttl_seconds + filename it was
    called with.

    Create the four RED test files with the behaviors above, all faking the app.storage.gcs seam
    (never touching real GCS): backend/tests/test_storage_upload.py,
    backend/tests/test_storage_signed_url.py, backend/tests/test_storage_delete.py,
    backend/tests/test_storage_cross_tenant.py. The cross-tenant file clones
    test_intake_cross_tenant.py exactly: mark `pytestmark = pytest.mark.integration`, guard imports
    with pytest.importorskip, patch ONLY the engine factories, override get_current_identity with a
    fabricated Identity, and assert EXACTLY 404 (never `in (403, 404)`).

    These tests reference the not-yet-existing 09-02 routes (POST /intakes/{id}/storage/uploads,
    GET /intakes/{id}/storage/signed-url, DELETE /intakes/{id}/storage/objects) — they are expected
    to be RED until 09-02 lands. State that in each file's header docstring (Wave-0 RED scaffold,
    GREEN after 09-02). Do NOT stub the routes here.
  </action>
  <verify>
    <automated>grep -q 'def fake_gcs' backend/tests/conftest.py && grep -q 'test_upload_writes_scoped_key' backend/tests/test_storage_upload.py && grep -q 'test_ttl_clamped_and_disposition' backend/tests/test_storage_signed_url.py && grep -q 'test_delete_cleans_ref' backend/tests/test_storage_delete.py && grep -Rq 'app.storage.gcs' backend/tests/test_storage_upload.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/conftest.py defines a `fake_gcs` fixture that monkeypatches all four `app.storage.gcs.*` functions
    - backend/tests/test_storage_upload.py contains `test_upload_writes_scoped_key`, `test_upload_413_over_cap`, `test_upload_415_bad_type`, `test_audio_upload_creates_source`
    - backend/tests/test_storage_signed_url.py contains `test_ttl_clamped_and_disposition`
    - backend/tests/test_storage_delete.py contains `test_delete_cleans_ref`
    - backend/tests/test_storage_cross_tenant.py contains `pytestmark = pytest.mark.integration` and asserts `== 404` (not `in (403, 404)`) for the cross-tenant cases and `== 403` for null-space
    - Every storage test file references `app.storage.gcs` (grep matches) and none contains a real bucket name or network call
  </acceptance_criteria>
  <done>fake_gcs and the four RED scaffolds exist, fake the seam, and encode the DOC-01/DOC-02/D-02..D-10 behaviors; they are RED pending 09-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloud Run runtime → GCP IAM | The attached SA signs blobs via IAM signBlob; no key material lives in the process |
| test process → app.storage.gcs | Tests fake the seam; no real network/bucket reached |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | SA JSON key exfiltration | mitigate | `ci_no_sa_json_key.sh` grep-guard bans `from_service_account_file` / `service_account.json` / `GOOGLE_APPLICATION_CREDENTIALS=.*json` under `app/`; gcs.py uses ADC only (Task 2) |
| T-09-02 | Tampering | path traversal via client filename | mitigate | `build_object_key` server-authors the key with a `{uuid}` prefix and `sanitize_filename` strips `/` and non-`[A-Za-z0-9._-]` chars (Task 2) |
| T-09-03 | Information Disclosure | signed-URL TTL abuse | mitigate | `signed_download_url` clamps ttl to `_MAX_TTL_S = 900` server-side (Task 2); RED test `test_ttl_clamped_and_disposition` asserts it (Task 3) |
| T-09-04 | Tampering | stored-XSS via inline-rendered object | mitigate | `response_disposition=attachment` forces download, never inline render (Task 2) |
| T-09-SC | Tampering | pip installs (google-cloud-storage, python-multipart) | mitigate | blocking-human legitimacy checkpoint (Task 1) verifies both [ASSUMED] packages on PyPI before install |
</threat_model>

<verification>
- `bash backend/scripts/ci_no_sa_json_key.sh` exits 0.
- The app/storage package imports cleanly (verified in the Cloud Build suite run at wave merge).
- The four storage test files + fake_gcs exist and reference the app.storage.gcs seam; they are RED until 09-02 (expected).
- Full suite runs in Cloud Build at wave merge (dev box has no Python — author by construction).
</verification>

<success_criteria>
- `app/storage/{__init__,keys,gcs}.py` exist; gcs.py has the four seam functions with keyless signing and a 900s TTL clamp; no SA JSON key reference.
- `Settings.storage_bucket` field added (non-secret).
- `ci_no_sa_json_key.sh` exists, is executable, and passes.
- `fake_gcs` fixture + four RED storage test files encode the phase behaviors.
</success_criteria>

<output>
Create `.planning/phases/09-gcs-storage/09-01-SUMMARY.md` when done.
</output>
