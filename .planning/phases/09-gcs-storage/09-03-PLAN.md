---
phase: 09-gcs-storage
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/src/lib/api/storage.ts
  - frontend/src/lib/api/client.ts
  - frontend/src/components/intake/FieldRenderer.tsx
  - frontend/src/components/intake/FieldDisplay.tsx
  - frontend/src/components/intake/FinalReportBlock.tsx
  - frontend/src/components/intake/ResearchArtifacts.tsx
  - frontend/src/components/intake/ResearchResultsPanel.tsx
autonomous: true
requirements: [DOC-01, DOC-02]
user_setup: []

must_haves:
  truths:
    - "The storage seam signatures drop `bucket` and client-authored `path`, and uploadFile now takes `category` and posts to /intakes/{id}/storage/uploads"
    - "removeFile and signedDownloadUrl are reshaped under /intakes/{intakeId}/storage/... and carry intakeId"
    - "apiFetch no longer forces application/json on a FormData body, so the browser sets the multipart boundary"
    - "All 5 call sites compile against the new signatures with the correct per-site category and no hardcoded nestor-uploads constant"
  artifacts:
    - path: "frontend/src/lib/api/storage.ts"
      provides: "finalized storage seam (no bucket/path; category on upload; intake-scoped delete/signed-url)"
      contains: "category"
    - path: "frontend/src/lib/api/client.ts"
      provides: "FormData Content-Type guard"
      contains: "instanceof FormData"
  key_links:
    - from: "frontend/src/lib/api/client.ts"
      to: "multipart upload"
      via: "skip JSON Content-Type when body is FormData"
      pattern: "instanceof FormData"
    - from: "frontend/src/components/intake/FieldRenderer.tsx"
      to: "storage.uploadFile"
      via: "category-tagged upload, no bucket"
      pattern: "category"
---

<objective>
Finalize the Phase-6 storage seam the CONTEXT explicitly deferred to Phase 9: reshape `storage.ts`
(drop `bucket`, drop client-authored `path`, add `category`, intake-scope `removeFile` /
`signedDownloadUrl`), apply the single load-bearing `apiFetch` FormData Content-Type guard, and
update all 5 call sites with the correct per-site category — deleting the three hardcoded
`"nestor-uploads"` constants and the client-side key builders.

Purpose: DOC-02's browser-never-authors-a-key contract is completed here. The server now owns the
key (D-05), so the frontend stops sending `bucket`/`path`; the FormData guard is what makes multipart
actually parse (Pitfall 3). This plan touches only frontend files — zero overlap with 09-02.

Output: finalized `storage.ts` + `client.ts` guard + 5 updated call sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-gcs-storage/09-CONTEXT.md
@.planning/phases/09-gcs-storage/09-RESEARCH.md
@.planning/phases/09-gcs-storage/09-PATTERNS.md

<interfaces>
<!-- Backend endpoint shapes this seam calls (from 09-02): -->
POST   /intakes/{intake_id}/storage/uploads     (multipart: file, category)   -> UploadedFileMeta
GET    /intakes/{intake_id}/storage/signed-url?path=<key>&expires_in=300      -> { url, expires_in }
DELETE /intakes/{intake_id}/storage/objects     (body: { paths: string[] })   -> { removed }

<!-- Current stub signatures (frontend/src/lib/api/storage.ts) — RESHAPE these: -->
uploadFile({ intakeId, bucket, path, file, filename, contentType })   // DROP bucket, DROP path; ADD category
removeFile({ bucket, paths })                                          // -> ({ intakeId, paths }); DROP bucket; DELETE /intakes/{intakeId}/storage/objects
signedDownloadUrl({ bucket, path, expiresIn })                         // -> ({ intakeId, path, expiresIn }); DROP bucket; GET /intakes/{intakeId}/storage/signed-url

<!-- The transport guard (frontend/src/lib/api/client.ts:53-55): -->
if (!headers.has("Content-Type")) { headers.set("Content-Type", "application/json"); }   // WRONG for FormData
// CHANGE TO:
if (!headers.has("Content-Type") && !(init?.body instanceof FormData)) { headers.set("Content-Type", "application/json"); }

<!-- Category per call site (RESEARCH Open Q3): -->
FieldRenderer file field  -> "attachments"  (audio-accept field -> "audio")
FinalReportBlock          -> "reports"
ResearchArtifacts         -> "artifacts"
ResearchResultsPanel      -> download only (no category; signedDownloadUrl)
FieldDisplay              -> download only (no category; signedDownloadUrl)
</interfaces>

@frontend/src/lib/api/storage.ts
@frontend/src/lib/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape storage.ts + add the apiFetch FormData guard</name>
  <read_first>
    - frontend/src/lib/api/storage.ts (the full stub — the three functions to reshape and their JSDoc)
    - frontend/src/lib/api/client.ts (apiFetch, the Content-Type default at :51-56, and the ApiResult contract)
    - .planning/phases/09-gcs-storage/09-RESEARCH.md § Pattern 4 (the exact FormData guard + why)
  </read_first>
  <action>
    In frontend/src/lib/api/client.ts, change the unconditional JSON Content-Type default to skip
    FormData bodies: `if (!headers.has("Content-Type") && !(init?.body instanceof FormData))`. This
    is the ONE load-bearing transport edit — do not touch anything else in apiFetch. Every existing
    JSON call keeps the default; only FormData lets the browser set the multipart boundary.

    In frontend/src/lib/api/storage.ts, reshape the three functions and update the header comment
    (it currently says "Phase-9 SEAM STUB" / "not yet implemented" — change to "finalized in Phase 9"):
    - uploadFile: args become `{ intakeId, file, filename, category, contentType? }` — DROP `bucket`
      and `path`. Build FormData with `file` (+ filename), `category`, and optional `content_type`.
      Keep POST /intakes/{intakeId}/storage/uploads. Do NOT append `bucket` or `path`.
    - removeFile: args become `{ intakeId, paths }` — DROP `bucket`. Call
      DELETE /intakes/{intakeId}/storage/objects with body `{ paths }`.
    - signedDownloadUrl: args become `{ intakeId, path, expiresIn? }` — DROP `bucket`. Build the
      query from `path` + `expires_in` only and call GET /intakes/{intakeId}/storage/signed-url.
    Keep the return-no-throw ApiResult contract and the UploadedFileMeta / SignedDownloadUrl types
    unchanged (path/filename/size/uploaded_at/mime_type).
  </action>
  <verify>
    <automated>cd frontend && grep -q 'instanceof FormData' src/lib/api/client.ts && grep -q 'category' src/lib/api/storage.ts && ! grep -q 'bucket' src/lib/api/storage.ts && npx tsc --noEmit 2>&1 | grep -v 'components/intake' | grep -c 'error TS' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/api/client.ts contains `!(init?.body instanceof FormData)` in the Content-Type default guard
    - frontend/src/lib/api/storage.ts contains NO occurrence of `bucket` and NO client-built `path` append on uploadFile
    - uploadFile's args include `category`; removeFile and signedDownloadUrl args include `intakeId`
    - The three functions POST/DELETE/GET to `/intakes/${intakeId}/storage/...` paths
  </acceptance_criteria>
  <done>storage.ts signatures are finalized (no bucket, category on upload, intake-scoped delete/signed-url) and client.ts skips JSON Content-Type for FormData.</done>
</task>

<task type="auto">
  <name>Task 2: Update the 5 call sites to the new signatures</name>
  <read_first>
    - frontend/src/components/intake/FieldRenderer.tsx (bucket at :374; uploadFile at :399-405; removeFile at :427,:460; signedDownloadUrl at :587-588)
    - frontend/src/components/intake/FieldDisplay.tsx (signedDownloadUrl at :272; FileRow bucket prop at :262-272; :202,:210 pass field.storage_bucket)
    - frontend/src/components/intake/FinalReportBlock.tsx (BUCKET const at :7; uploadFile at :73-75; signedDownloadUrl at :114-115; sanitizeFilenameForStorage at :24-34)
    - frontend/src/components/intake/ResearchArtifacts.tsx (BUCKET const at :10; uploadFile at :365,:480; signedDownloadUrl at :550; removeFile at :569)
    - frontend/src/components/intake/ResearchResultsPanel.tsx (BUCKET const at :22; signedDownloadUrl at :145-146)
    - frontend/src/lib/api/storage.ts (the finalized signatures from Task 1)
  </read_first>
  <action>
    Update every call site to the finalized signatures. At each one: remove the `bucket` argument,
    remove any client-side key/path construction, and pass the intakeId that is already in scope.

    - FieldRenderer.tsx: delete the `const bucket = field.storage_bucket ?? "nestor-uploads"` line
      (:374) and any client-side key builder. uploadFile now passes `category`: use `"attachments"`
      for file fields, or `"audio"` when the field's accept list is audio (derive from the field
      config already present). removeFile calls (:427,:460) pass `{ intakeId, paths }`.
      signedDownloadUrl (:587) passes `{ intakeId, path, expiresIn }`.
    - FieldDisplay.tsx: FileRow must receive `intakeId` instead of `bucket`; signedDownloadUrl
      (:272) passes `{ intakeId, path: file.path, expiresIn: 300 }`. Update the FileRow prop type and
      its two call sites (:202,:210) to pass intakeId (thread it from the component's props/context).
    - FinalReportBlock.tsx: delete `const BUCKET = "nestor-uploads"` (:7). uploadFile (:73) passes
      `category: "reports"` + intakeId; signedDownloadUrl (:114) passes intakeId. Keep the local
      sanitizeFilenameForStorage for display purposes only (the server now authors the stored key).
    - ResearchArtifacts.tsx: delete `const BUCKET` (:10). uploadFile (:365,:480) passes
      `category: "artifacts"` + intakeId; signedDownloadUrl (:550) and removeFile (:569) pass intakeId.
    - ResearchResultsPanel.tsx: delete `const BUCKET` (:22). signedDownloadUrl (:145) passes intakeId
      + path (download only, no category).

    No `"nestor-uploads"` literal may remain in any of these files. Do NOT modify shadcn ui/ files.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rq 'nestor-uploads' src/components/intake/ && npx tsc --noEmit 2>&1 | grep -c 'error TS' | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r 'nestor-uploads' frontend/src/components/intake/` returns nothing
    - No call to `storage.uploadFile` / `storage.removeFile` / `storage.signedDownloadUrl` passes a `bucket` argument in any of the 5 files
    - uploadFile call sites pass the correct category: FieldRenderer -> attachments|audio, FinalReportBlock -> reports, ResearchArtifacts -> artifacts
    - `npx tsc --noEmit` in frontend/ reports 0 `error TS` (full compile clean)
  </acceptance_criteria>
  <done>All 5 call sites compile against the finalized seam with correct per-site categories and no hardcoded bucket constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → backend | The frontend no longer authors object keys or names a bucket — the server owns both (DOC-02/D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | client-supplied object key / bucket | mitigate | seam drops `bucket` and client-authored `path` (Task 1); no call site sends them (Task 2) — the server authors the key (enforced backend-side in 09-02) |
| T-09-11 | Denial of Service (broken uploads) | multipart boundary stripped by JSON Content-Type | mitigate | FormData guard in apiFetch lets the browser set the boundary (Task 1, Pitfall 3) |
| T-09-12 | Information Disclosure | download of an artifact outside the caller's space | accept (frontend) / mitigate (backend) | the signed-url call carries intakeId; the authoritative ownership + prefix check is backend-side (09-02 T-09-05) — the frontend cannot self-authorize |
</threat_model>

<verification>
- `npx tsc --noEmit` in frontend/ is clean (0 error TS).
- No `nestor-uploads` literal remains under frontend/src/components/intake/.
- `client.ts` skips JSON Content-Type for FormData; `storage.ts` sends no bucket/path.
- End-to-end upload/download proven live in the 09-04 D-13 combined UAT.
</verification>

<success_criteria>
- storage.ts finalized (no bucket, category on upload, intake-scoped); apiFetch FormData guard applied.
- All 5 call sites updated with correct categories; no hardcoded bucket; frontend compiles clean.
</success_criteria>

<output>
Create `.planning/phases/09-gcs-storage/09-03-SUMMARY.md` when done.
</output>
