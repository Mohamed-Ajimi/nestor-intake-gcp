---
phase: 11-internationalization-nl-fr-en
plan: 04
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/components/admin/ProductShell.tsx
  - frontend/src/routes/admin.spaces.tsx
  - frontend/src/lib/api/admin.ts
  - frontend/src/components/admin/ClientDetailDrawer.tsx
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/fr/admin.json
  - frontend/src/locales/en/admin.json
autonomous: true
requirements: [I18N-01, I18N-02]
must_haves:
  truths:
    - "The admin Pulse intake-detail page renders fully in the active locale (56 strings externalized)"
    - "The admin chrome (ProductShell) shows a persisting LanguageSwitcher beside the space switcher"
    - "A superadmin can set a space's default_locale from the space create/edit UI, persisted via the admin seam"
    - "Admin date formatting (intake-detail, ClientDetailDrawer) follows the active language via getDateLocale"
  artifacts:
    - path: "frontend/src/components/admin/ProductShell.tsx"
      provides: "Admin chrome hosting the LanguageSwitcher (D-08)"
      contains: "LanguageSwitcher"
    - path: "frontend/src/routes/admin.spaces.tsx"
      provides: "Space dialog with a default_locale field (D-10)"
      contains: "default_locale"
  key_links:
    - from: "frontend/src/routes/admin.spaces.tsx"
      to: "frontend/src/lib/api/admin.ts"
      via: "default_locale sent through the space create/update seam"
      pattern: "default_locale"
    - from: "frontend/src/components/admin/ProductShell.tsx"
      to: "frontend/src/components/LanguageSwitcher.tsx"
      via: "switcher mounted in admin chrome"
      pattern: "LanguageSwitcher"
---

<objective>
Externalize the admin namespace: the highest-density file `admin.pulse.intakes.$id.tsx` (56
strings + a date-locale call site), the space dialog (adding the `default_locale` field, D-10),
ClientDetailDrawer (a date-locale call site + strings), mount the LanguageSwitcher in ProductShell
(D-08), and extend the admin seam to carry `default_locale`.

Purpose: The admin lifecycle surface is the operator's daily flow; it must render in NL/FR/EN and
let a superadmin set each space's default locale. Depends on 11-01 (i18n runtime + switcher +
getDateLocale) and 11-02 (the org `default_locale` column + resolution). Parallel to 11-03/05/06 —
disjoint file set.
Output: admin.json catalogs filled, admin-detail + space dialog + drawer externalized, switcher
mounted, default_locale field wired, admin date-locale sites swapped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internationalization-nl-fr-en/11-CONTEXT.md
@.planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md
@.planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md
@.planning/phases/11-internationalization-nl-fr-en/11-01-SUMMARY.md
@.planning/phases/11-internationalization-nl-fr-en/11-02-SUMMARY.md

<interfaces>
<!-- Executor: use these existing contracts directly. -->

From frontend/src/lib/i18n (11-01): default `i18n` (read `i18n.language`), `getDateLocale(lang)`.
From frontend/src/components/LanguageSwitcher.tsx (11-01): NL/FR/EN switcher (pass `persist`).
From react-i18next: `useTranslation("admin")` → `t("key")`.

From frontend/src/components/admin/ProductShell.tsx: SpaceSwitcher is mounted at line 61 — mount
the LanguageSwitcher adjacent to it in the same chrome.

From frontend/src/lib/api/admin.ts: the thin seam over apiFetch for space create/update. Extend
the space create/update payload + view type to include `default_locale` (matching the backend
org column from 11-02). Return-no-throw; never fork apiFetch.

Backend note: the org `default_locale` column exists (11-02). If the admin space create/update
endpoint does not already accept `default_locale`, extend the backend `SpacePatchBody` / create
body + `_space_view` to include it (backend/app/api/admin_routes.py) as part of this plan's seam
task — mirror the existing `name`/`slug` field handling and the `space.updated` audit metadata.

Density map: `admin.pulse.intakes.$id.tsx` = 56 (highest) + date-fns site; `admin.spaces.tsx` = 6
(hosts default_locale); `ClientDetailDrawer.tsx` = 8 + date-fns site.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Externalize admin.pulse.intakes.$id.tsx (56 strings) + date-locale swap</name>
  <files>frontend/src/routes/admin.pulse.intakes.$id.tsx, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <read_first>
    - frontend/src/routes/admin.pulse.intakes.$id.tsx (all 56 Dutch strings — page copy, phase banners, CTA labels, toasts; the date-fns `nl` import call site)
    - frontend/src/lib/i18n/date-locale.ts (getDateLocale)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (Shared Pattern: string externalization; date-fns call-site swap)
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (density map — this is the highest-density file, its own task unit)
  </read_first>
  <action>
    Add `const { t } = useTranslation("admin")`. Replace every hardcoded Dutch literal (phase-banner
    copy, CTA button labels, `toast.success`/`toast.error` messages, confirm dialogs, section
    headers, status labels) with `t("intakeDetail.<key>")`, organizing keys under an
    `intakeDetail` sub-object in the three `admin.json` catalogs (nl verbatim, fr/en drafted — D-12).
    Where a message wraps JSX or interpolates a value, use `t("key", { value })` or `<Trans>`.

    Swap the date-fns call site: replace `import { nl } from "date-fns/locale"` +
    `format(d, "...", { locale: nl })` with `getDateLocale(i18n.language)` (D-04).

    Toast error handling: where the page currently does `toast.error(error)` on an ApiResult
    failure, prefer the code-mapped translation — `toast.error(resolveErrorKey(code) ?
    t(resolveErrorKey(code)) : error)` using the 11-01 error-codes helper — falling back to the raw
    server text for unmapped codes (D-11). Apply this on the failure branches that surface backend
    errors to the user.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - No hardcoded Dutch stopword remains in admin.pulse.intakes.$id.tsx (grep the file → none outside comments)
    - The file imports `getDateLocale` and no longer imports `{ nl }` from date-fns/locale
    - At least one failure-branch toast uses the error-code map with raw fallback
    - Every new `t("intakeDetail.*")` key exists in nl/fr/en admin.json
  </acceptance_criteria>
  <done>The highest-density admin file renders in the active locale; date-locale + error toasts wired.</done>
</task>

<task type="auto">
  <name>Task 2: ProductShell switcher mount + ClientDetailDrawer externalize + date-locale</name>
  <files>frontend/src/components/admin/ProductShell.tsx, frontend/src/components/admin/ClientDetailDrawer.tsx, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <read_first>
    - frontend/src/components/admin/ProductShell.tsx (SpaceSwitcher mount at line 61 — mount LanguageSwitcher adjacent)
    - frontend/src/components/admin/ClientDetailDrawer.tsx (8 Dutch strings + the date-fns `nl` import call site)
    - frontend/src/components/LanguageSwitcher.tsx (11-01)
    - frontend/src/lib/i18n/date-locale.ts (getDateLocale)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (LanguageSwitcher mount points; Shared Pattern)
  </read_first>
  <action>
    In `ProductShell.tsx`, import and mount `<LanguageSwitcher persist />` adjacent to
    `<SpaceSwitcher />` in the admin chrome (D-08 admin location). Reuse the existing chrome layout
    so the switcher sits in the same header row.

    In `ClientDetailDrawer.tsx`, add `useTranslation("admin")`, replace the 8 Dutch literals with
    `t("clientDrawer.<key>")` (catalogs updated in all three locales), and swap the date-fns call
    site from `{ nl }` to `getDateLocale(i18n.language)` (D-04).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - ProductShell.tsx renders `<LanguageSwitcher ...>` in the same chrome as SpaceSwitcher
    - No hardcoded Dutch stopword remains in ClientDetailDrawer.tsx (grep → none outside comments)
    - ClientDetailDrawer imports `getDateLocale` and no longer imports `{ nl }` from date-fns/locale
    - New `t("clientDrawer.*")` keys exist in nl/fr/en admin.json
  </acceptance_criteria>
  <done>Admin chrome hosts the switcher; ClientDetailDrawer externalized + date-locale swapped.</done>
</task>

<task type="auto">
  <name>Task 3: Space dialog default_locale field + admin seam + backend field</name>
  <files>frontend/src/routes/admin.spaces.tsx, frontend/src/lib/api/admin.ts, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <read_first>
    - frontend/src/routes/admin.spaces.tsx (the space create/edit dialog — 6 Dutch strings + where the default_locale field slots in)
    - frontend/src/lib/api/admin.ts (space create/update seam functions + SpaceView type)
    - backend/app/api/admin_routes.py (update_space at line 406, create_space, _space_view, SpacePatchBody — extend for default_locale)
    - backend/app/db/models/organization.py (the default_locale column from 11-02)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (admin seam extension; D-10 space dialog)
  </read_first>
  <action>
    Backend: extend `admin_routes.py` so space create + `update_space` accept an optional
    `default_locale` (validated against {nl,fr,en} — raise `CodedError(422,"INVALID_LOCALE",...)` on
    a bad value, reusing the 11-02 contract), and `_space_view` returns it. Mirror the existing
    `name`/`slug` handling and record `default_locale` in the `space.updated` / `space.created`
    audit metadata when set. The AdminRepo create/update writes the org column.

    Frontend seam: extend `admin.ts` space create/update payload types + the `SpaceView` type to
    include `default_locale: "nl" | "fr" | "en"`. Return-no-throw; never fork apiFetch.

    UI: in `admin.spaces.tsx`, externalize the 6 Dutch strings (`useTranslation("admin")` →
    `t("spaces.<key>")`, catalogs in all three locales) AND add a `default_locale` selector to the
    space create/edit dialog (a small NL/FR/EN select composed from the existing shadcn `select`
    primitive — NOT editing ui/), defaulting new spaces to "nl" (D-09). Send the chosen value
    through the extended seam. Show the current value when editing.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>MISSING — backend admin_routes change runs in Cloud Build; extend the existing admin-routes test module by construction to cover default_locale create/update + invalid-locale rejection.</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - admin.spaces.tsx has no hardcoded Dutch stopword (grep → none outside comments) and renders a default_locale selector in the space dialog
    - admin.ts space create/update payload + SpaceView include `default_locale`
    - admin_routes.py create/update accept + persist + audit `default_locale`, rejecting non-{nl,fr,en} with CodedError INVALID_LOCALE
    - New `t("spaces.*")` keys exist in nl/fr/en admin.json
  </acceptance_criteria>
  <done>Superadmin can set a space's default_locale end-to-end; space dialog externalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| superadmin → space default_locale | Locale value written to the org row (display-only) |
| catalog value → React render | Translated strings interpolated into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Input Validation | space default_locale write | mitigate | Backend validates default_locale ∈ {nl,fr,en}, rejecting bad values with CodedError INVALID_LOCALE (Task 3); superadmin-only via get_admin_session |
| T-11-01 | Tampering (XSS) | admin catalog interpolation | mitigate | React auto-escapes; never dangerouslySetInnerHTML a catalog value (Tasks 1-3) |
| T-11-05 | Info Disclosure | admin error toasts | mitigate | Error-code map surfaces curated codes; raw fallback carries only the existing server detail, no new leak (Task 1) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- CI Dutch guard passes for the admin-namespace files touched (partial — full scan at phase gate)
- Switcher renders in ProductShell; default_locale selector renders in the space dialog
- Backend admin-routes default_locale test authored (Cloud Build at phase gate)
</verification>

<success_criteria>
- admin.pulse.intakes.$id.tsx + ClientDetailDrawer + admin.spaces externalized (NL/FR/EN)
- ProductShell hosts a persisting LanguageSwitcher
- default_locale set from the space dialog persists through the seam + backend with validation + audit
- Admin date-locale sites use getDateLocale
</success_criteria>

<output>
Create `.planning/phases/11-internationalization-nl-fr-en/11-04-SUMMARY.md` when done
</output>
