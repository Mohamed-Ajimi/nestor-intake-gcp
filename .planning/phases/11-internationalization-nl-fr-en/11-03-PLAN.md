---
phase: 11-internationalization-nl-fr-en
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - backend/app/data/pulse_intake_v1.json
  - frontend/src/lib/intake-types.ts
  - frontend/src/lib/i18n/localizeSchema.ts
  - frontend/src/lib/i18n/localizeSchema.test.ts
  - frontend/src/components/intake/IntakeForm.tsx
  - frontend/src/components/intake/FieldRenderer.tsx
  - frontend/src/components/intake/FieldDisplay.tsx
  - frontend/src/routes/intake.$id.tsx
  - frontend/src/routes/intake.index.tsx
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/en/intake.json
autonomous: true
requirements: [I18N-01, I18N-02]
must_haves:
  truths:
    - "The canonical intake schema carries nl/fr/en variants for every display string; the frontend renders the active locale with nl fallback"
    - "FieldRenderer/FieldDisplay/IntakeForm render translated chrome and the localized schema without raw {nl,fr,en} objects appearing in the DOM"
    - "The client intake form header shows a working LanguageSwitcher (persists post-login)"
    - "intake.index date formatting follows the active language via getDateLocale"
  artifacts:
    - path: "frontend/src/lib/i18n/localizeSchema.ts"
      provides: "localizeSchema(schema, lang) load-time flatten of locale-object strings to scalar shape"
      exports: ["localizeSchema"]
    - path: "backend/app/data/pulse_intake_v1.json"
      provides: "Multi-locale canonical schema (label/title/description/help/placeholder as {nl,fr,en})"
      contains: "\"fr\""
  key_links:
    - from: "frontend/src/components/intake/IntakeForm.tsx"
      to: "frontend/src/lib/i18n/localizeSchema.ts"
      via: "localizeSchema(template.schema, i18n.language) at load"
      pattern: "localizeSchema"
    - from: "frontend/src/components/intake/IntakeForm.tsx"
      to: "frontend/src/components/LanguageSwitcher.tsx"
      via: "switcher mounted in the client form header (D-08)"
      pattern: "LanguageSwitcher"
---

<objective>
Translate the intake FORM CONTENT in-place in the canonical JSON (locale-object shape), add the
`localizeSchema` load-time flatten so the schema consumers stay near-untouched, externalize the
intake form chrome (IntakeForm/FieldRenderer/FieldDisplay + the two client intake routes), mount
the LanguageSwitcher in the client form header, and switch intake.index's date formatting to
getDateLocale.

Purpose: This is the client-facing intake slice — the schema-shape change (Pitfall 4) and the
form chrome must move together or every schema consumer breaks. Depends on 11-01 (i18n runtime,
localize helper deps, switcher) and 11-02 (the schema is served unchanged by
`GET /intakes/templates`; no backend shape logic changes here).
Output: multi-locale canonical JSON, localizeSchema + test, intake-namespace catalogs filled,
form chrome externalized, switcher mounted, date-locale swapped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internationalization-nl-fr-en/11-CONTEXT.md
@.planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md
@.planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md
@.planning/phases/11-internationalization-nl-fr-en/11-01-SUMMARY.md

<interfaces>
<!-- Executor: use these existing contracts directly. -->

From frontend/src/lib/intake-types.ts (today `.label`/`.title`/`.description`/`.help`/`.placeholder`
are `string`). The SOURCE shape becomes locale-object; `localizeSchema` flattens back to scalar so
FieldRenderer/FieldDisplay keep reading `field.label` as a string.

From frontend/src/lib/i18n/index.ts (11-01): default export `i18n` (read `i18n.language`).
From frontend/src/lib/i18n/date-locale.ts (11-01): `getDateLocale(lang)`.
From frontend/src/components/LanguageSwitcher.tsx (11-01): NL/FR/EN switcher; pass `persist` true
here (authenticated client).
From react-i18next: `useTranslation("intake")` → `t("key")`.

`GET /intakes/templates` (backend/app/api/intake_routes.py `list_templates`) serves the ONE
canonical asset unchanged — the frontend picks the locale at load via localizeSchema. No backend
schema logic changes in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multi-locale canonical JSON + intake-types + localizeSchema (+ test)</name>
  <files>backend/app/data/pulse_intake_v1.json, frontend/src/lib/intake-types.ts, frontend/src/lib/i18n/localizeSchema.ts, frontend/src/lib/i18n/localizeSchema.test.ts</files>
  <read_first>
    - backend/app/data/pulse_intake_v1.json (the 14-section canonical schema — every label/title/description/help/placeholder to translate in-place)
    - frontend/src/lib/intake-types.ts (IntakeField/IntakeSection/IntakeSchema — the string fields becoming locale-objects)
    - frontend/src/components/intake/FieldRenderer.tsx + FieldDisplay.tsx (every `.label`/`.help`/`.placeholder`/`.title`/`.description` read site)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (intake-types + localizeSchema section; Pitfall 4 GOTCHA)
    - frontend/src/lib/intake-phase.test.ts (vitest structure to mirror)
  </read_first>
  <behavior>
    - localizeSchema(schema, "fr") returns a schema where every display string is the fr variant.
    - localizeSchema(schema, "en") with a field missing its en variant falls back to the nl variant (D-05).
    - localizeSchema(schema, "de") (unknown) falls back to nl for every string.
    - A field that is ALREADY a scalar string (legacy/edge) passes through unchanged (defensive).
    - The output shape is the current scalar `IntakeSchema` — no `{nl,fr,en}` objects survive.
  </behavior>
  <action>
    Convert `pulse_intake_v1.json` display strings to locale-object shape: every `label`, `title`,
    `description`, `help`, `placeholder`, and option `label`/`description`, plus the top-level
    `title`/`subtitle` and `submit.label`/`submit.confirmation_title`/`submit.confirmation_message`,
    become `{ "nl": "<existing>", "fr": "<translated>", "en": "<translated>" }`. Keep the existing
    Dutch text as the `nl` variant verbatim (D-05 in-place, nl fallback). Claude drafts fr/en
    (D-12; user reviews tone in UAT). Do NOT change field `key`s, `type`s, option `value`s, or any
    non-display attribute.

    Update `intake-types.ts`: introduce a `LocalizedString = string | { nl: string; fr?: string;
    en?: string }` source type; change the SOURCE `IntakeField`/`IntakeSection`/`IntakeSchema`
    display fields to `LocalizedString`. Add a RESOLVED shape (scalar strings) that consumers use —
    either a distinct `LocalizedIntakeSchema`/resolved alias, so `FieldRenderer`/`FieldDisplay`
    keep their `string` reads after localizeSchema flattens.

    Create `lib/i18n/localizeSchema.ts` exporting `localizeSchema(schema, lang)`: a pure load-time
    pass that walks the schema and replaces every `LocalizedString` with `pick(value, lang)` where
    `pick` returns `value` if it is a string, else `value[lang] ?? value.nl` (nl guaranteed
    fallback). Recurse into `sections[].fields[]`, nested `item.fields`, and `options[]`. Return the
    resolved scalar schema.

    Author `localizeSchema.test.ts` covering the five behaviors above.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npm run test -- src/lib/i18n/localizeSchema</automated>
  </verify>
  <acceptance_criteria>
    - `pulse_intake_v1.json` parses as JSON and every display string is a `{nl,fr,en}` object with the original Dutch under `nl`
    - Field `key`s, `type`s, and option `value`s are byte-unchanged (grep a sample section before/after)
    - localizeSchema.ts exports `localizeSchema`; a missing fr/en variant falls back to nl
    - localizeSchema.test.ts passes: fr resolution, en→nl fallback, unknown-lang→nl, scalar passthrough
    - `npx tsc --noEmit` passes with the new source/resolved types
  </acceptance_criteria>
  <done>Canonical schema is multi-locale, localizeSchema flattens to scalar with nl fallback, tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Externalize intake form chrome + wire localizeSchema + mount switcher</name>
  <files>frontend/src/components/intake/IntakeForm.tsx, frontend/src/components/intake/FieldRenderer.tsx, frontend/src/components/intake/FieldDisplay.tsx, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/intake.json</files>
  <read_first>
    - frontend/src/components/intake/IntakeForm.tsx (the 15 Dutch strings + the form header where the switcher mounts, D-08)
    - frontend/src/components/intake/FieldRenderer.tsx (6 Dutch strings + schema string reads)
    - frontend/src/components/intake/FieldDisplay.tsx (4 Dutch strings + the date-fns `nl` import call site)
    - frontend/src/components/LanguageSwitcher.tsx (11-01 — the switcher to mount)
    - frontend/src/lib/i18n/localizeSchema.ts (Task 1 — call at load)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (Shared Pattern: string externalization; date-fns call-site swap)
  </read_first>
  <action>
    In each file, add `const { t } = useTranslation("intake")` and replace every hardcoded Dutch
    literal (JSX text, `toast.*` messages, button labels, validation copy) with `t("key")`,
    adding the key to all three `intake.json` catalogs (nl verbatim, fr/en drafted — D-12).

    In `IntakeForm.tsx`, call `localizeSchema(template.schema, i18n.language)` at load so
    FieldRenderer/FieldDisplay receive the resolved scalar schema. Re-resolve when `i18n.language`
    changes (re-run on language change so the form re-renders in the new locale). Mount
    `<LanguageSwitcher persist />` in the client form header (D-08 — the client-facing switcher
    location).

    In `FieldDisplay.tsx`, replace `import { nl } from "date-fns/locale"` +
    `format(d, "...", { locale: nl })` with `getDateLocale(i18n.language)` (D-04 call-site swap).

    Keep `interpolation.escapeValue:false` safe by never `dangerouslySetInnerHTML`-ing a catalog
    value; use `<Trans>` only where a translated string wraps JSX.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - No hardcoded Dutch stopword remains in IntakeForm/FieldRenderer/FieldDisplay (grep the three files for the CI stopword set → none outside comments)
    - IntakeForm calls `localizeSchema(template.schema, i18n.language)` and mounts `<LanguageSwitcher persist ...>`
    - FieldDisplay imports `getDateLocale` and no longer imports `{ nl }` from date-fns/locale
    - Every new `t("...")` key exists in nl/fr/en intake.json
  </acceptance_criteria>
  <done>Form chrome externalized, schema localized at load, client switcher mounted, date-locale swapped.</done>
</task>

<task type="auto">
  <name>Task 3: Externalize the two client intake routes + intake.index date-locale</name>
  <files>frontend/src/routes/intake.$id.tsx, frontend/src/routes/intake.index.tsx, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/intake.json</files>
  <read_first>
    - frontend/src/routes/intake.$id.tsx (14 Dutch strings)
    - frontend/src/routes/intake.index.tsx (6 Dutch strings + the date-fns `nl` import call site)
    - frontend/src/lib/i18n/date-locale.ts (getDateLocale)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (Shared Pattern: string externalization)
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (density map — intake namespace)
  </read_first>
  <action>
    In both routes add `useTranslation("intake")` and replace every hardcoded Dutch literal (page
    copy, toasts, empty states, buttons) with `t("key")`, adding keys to the three `intake.json`
    catalogs (nl verbatim, fr/en drafted).

    In `intake.index.tsx`, swap `import { nl } from "date-fns/locale"` +
    `format(..., { locale: nl })` to `getDateLocale(i18n.language)` (D-04).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - No hardcoded Dutch stopword remains in intake.$id.tsx or intake.index.tsx (grep both → none outside comments)
    - intake.index.tsx imports `getDateLocale` and no longer imports `{ nl }` from date-fns/locale
    - Every new `t("...")` key exists in nl/fr/en intake.json
  </acceptance_criteria>
  <done>Both client intake routes externalized; intake.index date-locale swapped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical JSON → every intake consumer | ONE asset served to every caller; shape change is global |
| catalog/schema value → React render | Translated strings interpolated into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | canonical schema shape change | mitigate | localizeSchema flattens at load to the current scalar shape so no raw `{nl,fr,en}` object reaches the DOM; nl guaranteed fallback (Task 1) |
| T-11-01 | Tampering (XSS) | schema/catalog string interpolation | mitigate | React auto-escapes; `<Trans>` for JSX; never `dangerouslySetInnerHTML` a catalog/schema value (Task 2) |
| T-11-08 | Info Disclosure | canonical template read-only | accept | The template stays read-only; only display strings gain variants — no new writable surface (Task 1) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- localizeSchema tests green; canonical JSON parses with nl variants intact
- CI Dutch guard passes for the intake-namespace files touched here (partial — full scan at phase gate)
- Switcher renders in the client form header
</verification>

<success_criteria>
- Canonical schema carries nl/fr/en; localizeSchema resolves with nl fallback
- IntakeForm/FieldRenderer/FieldDisplay + both intake routes render translated chrome + localized schema
- Client form header has a persisting LanguageSwitcher
- FieldDisplay + intake.index use getDateLocale
</success_criteria>

<output>
Create `.planning/phases/11-internationalization-nl-fr-en/11-03-SUMMARY.md` when done
</output>
