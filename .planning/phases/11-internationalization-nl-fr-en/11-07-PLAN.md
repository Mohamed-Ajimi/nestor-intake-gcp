---
phase: 11-internationalization-nl-fr-en
plan: 07
type: execute
wave: 3
depends_on: [11-01, 11-04]
files_modified:
  - frontend/src/components/admin/InviteUserDialog.tsx
  - frontend/src/routes/admin.users.tsx
  - frontend/src/components/admin/ClientFormModal.tsx
  - frontend/src/components/admin/SpaceFormModal.tsx
  - frontend/src/components/admin/SpaceSwitcher.tsx
  - frontend/src/routes/admin.templates.tsx
  - frontend/src/routes/admin.pulse.intakes.new.tsx
  - frontend/src/routes/admin.pulse.clients.$id.tsx
  - frontend/src/routes/admin.pulse.clients.tsx
  - frontend/src/routes/admin.pulse.search.tsx
  - frontend/src/routes/admin.pulse.intakes.index.tsx
  - frontend/src/routes/admin.pulse.index.tsx
  - frontend/src/routes/admin.login.tsx
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/fr/admin.json
  - frontend/src/locales/en/admin.json
autonomous: true
requirements: [I18N-01]
must_haves:
  truths:
    - "The remaining admin dialogs and pulse/admin routes render fully in the active locale"
    - "SpaceFormModal / SpaceSwitcher / InviteUserDialog / admin.users render in NL/FR/EN"
  artifacts:
    - path: "frontend/src/components/admin/InviteUserDialog.tsx"
      provides: "Externalized invite dialog (18 strings)"
      contains: "useTranslation"
  key_links:
    - from: "frontend/src/routes/admin.users.tsx"
      to: "frontend/src/locales/nl/admin.json"
      via: "useTranslation('admin') keys"
      pattern: "useTranslation"
---

<objective>
Complete the admin-namespace externalization sweep: the invite/user-management surfaces
(InviteUserDialog 18, admin.users 17), the client/space dialogs (ClientFormModal 9, SpaceFormModal,
SpaceSwitcher 5), and the remaining pulse/admin routes (templates 6, intakes.new 6, clients.$id 6,
clients 5, pulse search/index/list, admin.login).

Purpose: This is the remaining-admin catch-all that, together with 11-03/04/05/06, drives the
"no hardcoded Dutch strings" success criterion toward true. Depends on 11-01 (i18n runtime +
catalogs) AND 11-04: both plans write the same three admin.json catalog files, so this plan runs in
Wave 3 AFTER 11-04 to keep those catalog edits sequential (no concurrent-write clobber). Keys stay
namespaced by sub-object; the component/route files touched here are disjoint from 11-03/04/05/06.
The FULL CI Dutch-string guard is NOT run here — it moved to the Wave-4 gate
plan 11-09, which depends on every externalization plan so the guard scans a fully-externalized tree.
Output: admin catalog completed for these surfaces, remaining admin files externalized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internationalization-nl-fr-en/11-CONTEXT.md
@.planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md
@.planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md
@.planning/phases/11-internationalization-nl-fr-en/11-01-SUMMARY.md

<interfaces>
<!-- Executor: use these existing contracts directly. -->

From react-i18next: `useTranslation("admin")` (management surfaces).
From frontend/src/lib/i18n (11-01): `resolveErrorKey(code)` for error toasts; `getDateLocale` if a
touched file has a date-fns `nl` site (none of the primary files here are in the D-04 in-scope date
list, but grep each touched file for `date-fns/locale` and swap if found).

Exemptions the CI guard already applies (do NOT externalize these): `admin.sales.*`,
`*.coming-soon*`, `src/components/ui/**`, `*.gen.ts`, `src/locales/**`, `.test.` files.

Density map (this set): InviteUserDialog 18, admin.users 17, ClientFormModal 9, SpaceSwitcher 5,
admin.templates 6, admin.pulse.intakes.new 6, admin.pulse.clients.$id 6, admin.pulse.clients 5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Externalize user/invite management surfaces</name>
  <files>frontend/src/components/admin/InviteUserDialog.tsx, frontend/src/routes/admin.users.tsx, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <read_first>
    - frontend/src/components/admin/InviteUserDialog.tsx (18 Dutch strings)
    - frontend/src/routes/admin.users.tsx (17 Dutch strings)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (Shared Pattern: string externalization)
  </read_first>
  <action>
    Add `useTranslation("admin")` to each; replace hardcoded Dutch literals with `t("invite.<key>")`
    / `t("users.<key>")` respectively, adding keys to the three `admin.json` catalogs (nl verbatim,
    fr/en drafted — D-12). Use the error-code map with raw fallback on failure toasts.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - No hardcoded Dutch stopword remains in InviteUserDialog.tsx or admin.users.tsx (grep both → none outside comments)
    - New `t("invite.*")` and `t("users.*")` keys exist in nl/fr/en admin.json
  </acceptance_criteria>
  <done>Invite + user-management surfaces render in the active locale.</done>
</task>

<task type="auto">
  <name>Task 2: Externalize client/space dialogs + remaining pulse routes</name>
  <files>frontend/src/components/admin/ClientFormModal.tsx, frontend/src/components/admin/SpaceFormModal.tsx, frontend/src/components/admin/SpaceSwitcher.tsx, frontend/src/routes/admin.templates.tsx, frontend/src/routes/admin.pulse.intakes.new.tsx, frontend/src/routes/admin.pulse.clients.$id.tsx, frontend/src/routes/admin.pulse.clients.tsx, frontend/src/routes/admin.pulse.search.tsx, frontend/src/routes/admin.pulse.intakes.index.tsx, frontend/src/routes/admin.pulse.index.tsx, frontend/src/routes/admin.login.tsx, frontend/src/locales/nl/admin.json, frontend/src/locales/fr/admin.json, frontend/src/locales/en/admin.json</files>
  <read_first>
    - frontend/src/components/admin/ClientFormModal.tsx, SpaceFormModal.tsx, SpaceSwitcher.tsx (dialog + switcher Dutch strings)
    - frontend/src/routes/admin.templates.tsx, admin.pulse.intakes.new.tsx, admin.pulse.clients.$id.tsx, admin.pulse.clients.tsx, admin.pulse.search.tsx, admin.pulse.intakes.index.tsx, admin.pulse.index.tsx, admin.login.tsx (route Dutch strings)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (Shared Pattern; date-fns swap if a site is found)
  </read_first>
  <action>
    Add `useTranslation("admin")` to each; replace hardcoded Dutch literals with namespaced keys
    (`clientModal.*`, `spaceModal.*`, `spaceSwitcher.*`, `templates.*`, `intakesNew.*`,
    `clientDetail.*`, `clients.*`, `search.*`, `intakesList.*`, `pulseIndex.*`, `adminLogin.*`),
    adding to the three `admin.json` catalogs (nl verbatim, fr/en drafted). Use the error-code map
    with raw fallback on failure toasts. If any touched file imports `{ nl }` from
    `date-fns/locale`, swap it to `getDateLocale(i18n.language)`.

    NOTE: `SpaceFormModal.tsx` is the space create/edit dialog component. If the `default_locale`
    field from 11-04 lives here rather than in `admin.spaces.tsx`, coordinate: this task externalizes
    its chrome strings only; the `default_locale` field wiring is owned by 11-04. Grep for a
    `default_locale` field before adding one (avoid duplicating 11-04's work).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - No hardcoded Dutch stopword remains in any of the 11 files (grep each → none outside comments)
    - New namespaced keys exist in nl/fr/en admin.json
    - Any date-fns `{ nl }` import in a touched file is swapped to getDateLocale
  </acceptance_criteria>
  <done>Client/space dialogs + remaining pulse routes externalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog value → React render | Translated strings interpolated into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering (XSS) | admin catalog interpolation | mitigate | React auto-escapes; never dangerouslySetInnerHTML a catalog value (Tasks 1-2) |
| T-11-05 | Info Disclosure | admin error toasts | mitigate | Error-code map surfaces curated codes; raw fallback carries only existing server detail, no new leak (Tasks 1-2) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- Per-file grep shows no hardcoded Dutch stopword in the touched surfaces (the FULL CI guard runs at the Wave-4 gate in 11-09)
</verification>

<success_criteria>
- Invite/user/client/space/pulse admin surfaces externalized (NL/FR/EN)
- admin.json catalog completed for these surfaces
</success_criteria>

<output>
Create `.planning/phases/11-internationalization-nl-fr-en/11-07-SUMMARY.md` when done
</output>
