---
phase: 11-internationalization-nl-fr-en
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/lib/i18n/index.ts
  - frontend/src/lib/i18n/detect.ts
  - frontend/src/lib/i18n/date-locale.ts
  - frontend/src/lib/i18n/date-locale.test.ts
  - frontend/src/lib/i18n/error-codes.ts
  - frontend/src/lib/i18n/error-codes.test.ts
  - frontend/src/lib/api/client.ts
  - frontend/src/lib/api/me.ts
  - frontend/src/components/LanguageSwitcher.tsx
  - frontend/src/routes/__root.tsx
  - frontend/src/locales/nl/common.json
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/nl/admin.json
  - frontend/src/locales/nl/auth.json
  - frontend/src/locales/fr/common.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/fr/admin.json
  - frontend/src/locales/fr/auth.json
  - frontend/src/locales/en/common.json
  - frontend/src/locales/en/intake.json
  - frontend/src/locales/en/admin.json
  - frontend/src/locales/en/auth.json
  - frontend/scripts/ci_no_hardcoded_dutch.sh
autonomous: false
requirements: [I18N-01, I18N-02]
must_haves:
  truths:
    - "react-i18next is installed and initializes with nl as the deterministic default and fallback"
    - "A LanguageSwitcher component flips the UI language on select and (post-login) persists via PATCH"
    - "getDateLocale(lang) returns the fr/enUS/nl date-fns Locale with nl fallback"
    - "apiFetch surfaces a machine `code` on the failure branch additively, keeping the raw error string"
    - "The CI guard exits non-zero when a hardcoded Dutch stopword appears in in-scope source"
  artifacts:
    - path: "frontend/src/lib/i18n/index.ts"
      provides: "Single synchronous i18next instance, bundled catalogs, nl fallback"
      contains: "initReactI18next"
    - path: "frontend/src/lib/i18n/date-locale.ts"
      provides: "getDateLocale(lang) date-fns locale resolver"
      exports: ["getDateLocale"]
    - path: "frontend/src/lib/i18n/error-codes.ts"
      provides: "code -> i18n key map for translated toasts"
      exports: ["ERROR_CODES"]
    - path: "frontend/src/lib/api/me.ts"
      provides: "getMe + patchLocale seam over apiFetch (return-no-throw)"
      exports: ["getMe", "patchLocale"]
    - path: "frontend/src/components/LanguageSwitcher.tsx"
      provides: "NL/FR/EN switcher composed from shadcn primitives"
      contains: "changeLanguage"
    - path: "frontend/scripts/ci_no_hardcoded_dutch.sh"
      provides: "Dutch-stopword CI guard (QA-02 exit-code style)"
      contains: "exit 1"
  key_links:
    - from: "frontend/src/routes/__root.tsx"
      to: "frontend/src/lib/i18n/index.ts"
      via: "I18nextProvider mount inside QueryClientProvider"
      pattern: "i18n"
    - from: "frontend/src/components/LanguageSwitcher.tsx"
      to: "frontend/src/lib/api/me.ts"
      via: "patchLocale on select (post-login)"
      pattern: "patchLocale"
    - from: "frontend/src/lib/api/client.ts"
      to: "frontend/src/lib/i18n/error-codes.ts"
      via: "additive code extraction consumed by toast callers"
      pattern: "code"
---

<objective>
Land the shared frontend i18n infrastructure that every externalization plan depends on:
the react-i18next instance + provider, the language switcher, the browser-detect helper, the
central date-locale helper, the error-code map + additive `code` extraction in `apiFetch`, the
`/me` seam functions, the bundled catalog skeleton (nl authored; fr/en seeded), and the
Dutch-string CI guard with a negative test.

Purpose: Nothing can be externalized until `useTranslation()`, the catalogs, and the switcher
exist. This is the Wave-1 foundation that unblocks all parallel externalization (11-03..11-06).
Output: A working (but sparsely populated) i18n runtime, a switcher, four shared helpers, the
`/me` seam, catalog files per locale/namespace, and a passing CI guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internationalization-nl-fr-en/11-CONTEXT.md
@.planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md
@.planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md

<interfaces>
<!-- Executor: use these existing contracts directly. -->

From frontend/src/lib/api/client.ts (the transport to extend additively — NEVER fork):
```typescript
export type ApiResult<T> = { success: true; data: T } | { success: false; error: string };
export async function apiFetch<T>(path: string, init?: RequestInit): Promise<ApiResult<T>>;
```
The failure branch (client.ts:66-97) already computes `message` from a string `detail`. Add
`code` extraction after that WITHOUT changing the existing string-detail raw-fallback path. The
failure variant gains an optional `code?: string`.

From frontend/src/routes/__root.tsx (provider mount point — RootComponent, lines 103-117):
`QueryClientProvider > AuthProvider > AuthRedirector + Outlet + Toaster`. Mount i18n inside
`QueryClientProvider`. `<html lang="en" translate="no">` at line 66 stays deterministic.

Analog for the switcher — frontend/src/components/admin/SpaceSwitcher.tsx (compact dropdown
composed from shadcn primitives, persist-on-select). Build LanguageSwitcher NEW (NOT in ui/).

Analog for the browser-detect SSR guard — frontend/src/lib/active-space.tsx `readPersisted()`
(`typeof window` guard + try/catch localStorage).

Analog for the CI guard — backend/scripts/ci_no_permissive_rls.sh (the exit-code IS the gate).

Analog for vitest tests — frontend/src/lib/intake-phase.test.ts (`import { describe, it, expect }`).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify + install i18next / react-i18next (package legitimacy gate)</name>
  <read_first>
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (§ Package Legitimacy Audit — both packages are [ASSUMED]; slopcheck was unavailable)
    - frontend/package.json (confirm date-fns@^4 already present; no i18n dep exists yet)
  </read_first>
  <what-built>
    The plan requires installing two NEW packages the research could not run slopcheck against:
    `i18next@^26` and `react-i18next@^17`. Per the package legitimacy gate (threat T-11-SC), the
    install is gated on human verification before it runs. Do NOT install
    `i18next-browser-languagedetector` — detection is hand-rolled in Task 2 (detect.ts).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/i18next — confirm it is the i18next-org package
       (repo github.com/i18next/i18next, ~15M weekly downloads, 8+ years).
    2. Open https://www.npmjs.com/package/react-i18next — confirm repo github.com/i18next/react-i18next,
       official React binding, high downloads.
    3. Confirm React 19 peer compatibility on the react-i18next page (supports React 18+).
  </how-to-verify>
  <action>
    After approval, run `cd frontend && npm install i18next@^26 react-i18next@^17`. Do NOT install
    the language-detector plugin. Confirm both appear under `dependencies` in frontend/package.json.
  </action>
  <resume-signal>Type "approved" to install, or describe concerns</resume-signal>
  <acceptance_criteria>
    - frontend/package.json `dependencies` contains `i18next` (^26.x) and `react-i18next` (^17.x)
    - `i18next-browser-languagedetector` is NOT present in package.json
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: i18n runtime — init, provider, detect, date-locale, error-codes (+ vitest)</name>
  <files>frontend/src/lib/i18n/index.ts, frontend/src/lib/i18n/detect.ts, frontend/src/lib/i18n/date-locale.ts, frontend/src/lib/i18n/date-locale.test.ts, frontend/src/lib/i18n/error-codes.ts, frontend/src/lib/i18n/error-codes.test.ts, frontend/src/routes/__root.tsx, frontend/src/locales/nl/common.json, frontend/src/locales/nl/intake.json, frontend/src/locales/nl/admin.json, frontend/src/locales/nl/auth.json, frontend/src/locales/fr/common.json, frontend/src/locales/fr/intake.json, frontend/src/locales/fr/admin.json, frontend/src/locales/fr/auth.json, frontend/src/locales/en/common.json, frontend/src/locales/en/intake.json, frontend/src/locales/en/admin.json, frontend/src/locales/en/auth.json</files>
  <read_first>
    - frontend/src/routes/__root.tsx (RootComponent provider nesting; `<html lang>` at line 66; the notranslate effect)
    - frontend/src/lib/active-space.tsx (the `typeof window` SSR-guard + localStorage try/catch to mirror in detect.ts)
    - frontend/src/lib/intake-phase.test.ts (vitest structure to mirror)
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (Pattern 1 init options, Pattern 3 date-locale, Pitfall 1 SSR)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (date-locale.ts, error-codes.ts, detect.ts, i18n init + provider wiring sections)
  </read_first>
  <behavior>
    - date-locale: getDateLocale("fr") returns date-fns `fr`; getDateLocale("en-US") returns `enUS`;
      getDateLocale("nl"), getDateLocale("de"), getDateLocale("") all return `nl` (nl fallback).
    - error-codes: a resolver given a known code returns its i18n key; given an unknown/undefined code
      returns undefined so the caller falls back to the raw server text (D-11 raw fallback).
    - detect: with `window` present and navigator.language "fr-BE" returns "fr"; "en" returns "en";
      "de" or unset returns "nl"; with `window` undefined (SSR) returns "nl" deterministically.
  </behavior>
  <action>
    Create `lib/i18n/index.ts`: a single i18next instance initialized synchronously at module load
    via `i18n.use(initReactI18next).init({...})` with `resources` importing all 12 bundled catalog
    JSON files (nl/fr/en × common/intake/admin/auth), `lng: "nl"`, `fallbackLng: "nl"`,
    `defaultNS: "common"`, `interpolation: { escapeValue: false }`, `returnNull: false`. Export the
    instance as default. Do NOT add an HTTP backend and do NOT add a language-detector plugin.

    Create `lib/i18n/detect.ts` exporting `detectLocale(): "nl" | "fr" | "en"` — `typeof window`
    guard returning "nl" during SSR, else read `navigator.language` first two chars lowercased,
    returning "fr" or "en" when they match, else "nl" (D-09).

    Create `lib/i18n/date-locale.ts` exporting `getDateLocale(lang: string): Locale` importing
    `{ nl, fr, enUS, type Locale }` from "date-fns/locale", returning `fr` for lang starting "fr",
    `enUS` for "en", else `nl` (D-04).

    Create `lib/i18n/error-codes.ts` exporting `ERROR_CODES: Record<string, string>` mapping a
    curated set of user-visible backend codes to `common:errors.*` i18n keys (seed with
    INTAKE_NOT_FOUND, INVALID_LOCALE, MAIL_SEND_FAILED, RECIPIENT_INVALID — the plan authors add
    more as backend codes land in 11-02/11-07). Export a `resolveErrorKey(code?: string): string |
    undefined` helper returning `ERROR_CODES[code]` or undefined.

    Create the 12 catalog JSON files. Author the `nl` catalogs with the keys these foundation
    helpers reference (`common.errors.intakeNotFound`, `common.errors.invalidLocale`,
    `common.errors.mailSendFailed`, `common.errors.recipientInvalid`, plus a `common.language`
    section with `nl`/`fr`/`en`/`label` labels for the switcher). Seed `fr` and `en` common.json
    with the same keys translated (D-12 Claude-drafted; user reviews tone in UAT). Create the
    remaining namespace files (intake/admin/auth per locale) as `{}` — externalization plans fill
    them. nl is the guaranteed fallback for any missing key.

    Wire the provider: in `__root.tsx` RootComponent, import `"@/lib/i18n"` (side-effect init) and
    wrap the tree in `<I18nextProvider i18n={i18n}>` inside `QueryClientProvider`. Keep the
    `<html lang>` deterministic and the existing `translate="no"` effect — do NOT do the resolved
    `changeLanguage` in a `useEffect` on the SSR shell (Pitfall 1). The post-login resolved
    changeLanguage lands in 11-06 (login) / boot; here only the provider + default nl.

    Author `date-locale.test.ts` and `error-codes.test.ts` as vitest suites covering the behaviors
    above.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>cd frontend && npm run test -- src/lib/i18n</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with the new i18n modules and provider wiring
    - `npm run test -- src/lib/i18n` runs date-locale.test.ts + error-codes.test.ts green
    - `lib/i18n/index.ts` calls `initReactI18next` with `fallbackLng: "nl"` and imports 12 catalog JSONs
    - All 12 `frontend/src/locales/{nl,fr,en}/{common,intake,admin,auth}.json` files exist and parse as JSON
    - `__root.tsx` imports the i18n singleton and mounts `I18nextProvider` inside `QueryClientProvider`
    - No `i18next-http-backend` and no language-detector plugin appear in index.ts imports
  </acceptance_criteria>
  <done>i18n runtime initializes in nl, four shared helpers exist and are tested, catalog skeleton present, provider mounted.</done>
</task>

<task type="auto">
  <name>Task 3: LanguageSwitcher + /me seam + additive apiFetch code extraction</name>
  <files>frontend/src/components/LanguageSwitcher.tsx, frontend/src/lib/api/me.ts, frontend/src/lib/api/client.ts</files>
  <read_first>
    - frontend/src/components/admin/SpaceSwitcher.tsx (compact dropdown composed from shadcn primitives, persist-on-select, TRIGGER_CLASS styling)
    - frontend/src/lib/api/admin.ts (thin one-function-per-route seam over apiFetch, return-no-throw)
    - frontend/src/lib/api/client.ts (the failure branch lines 66-98 — where `code` extraction lands additively; NEVER fork)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (LanguageSwitcher, me.ts, client.ts sections)
  </read_first>
  <action>
    Extend `client.ts` `apiFetch` failure branch: after computing `message`, read a top-level string
    `code` field from the parsed `body` (guard: `body && typeof body === "object" && "code" in body`)
    and add it to the returned failure object as `code?: string`. Add `code?: string` to the
    `ApiResult` failure variant (`{ success: false; error: string; code?: string }`). The existing
    string-`detail` raw-fallback path stays byte-for-byte unchanged. Do NOT throw; do NOT change the
    success variant.

    Create `lib/api/me.ts` with the `Me` type (`{ locale: "nl" | "fr" | "en" | null;
    space_default_locale: "nl" | "fr" | "en" }`), `getMe(): Promise<ApiResult<Me>>` calling
    `apiFetch<Me>("/me", { method: "GET" })`, and `patchLocale(locale): Promise<ApiResult<Me>>`
    calling `apiFetch<Me>("/me/locale", { method: "PATCH", body: JSON.stringify({ locale }) })`.
    Both return-no-throw. `locale` is display-only — never an authz input.

    Create `components/LanguageSwitcher.tsx` composed from the same shadcn primitives SpaceSwitcher
    uses (NOT editing ui/). Three fixed options nl/fr/en labelled from `common:language.*`. Accept a
    `persist?: boolean` prop (default true): `handleSelect(lang)` calls `i18n.changeLanguage(lang)`
    instantly, and when `persist` is true calls `void patchLocale(lang)` (ignore failure —
    best-effort auto-persist, D-10). When `persist` is false (pre-login mount, 11-06) it also writes
    the choice to localStorage for post-login reconciliation. Reuse SpaceSwitcher's trigger styling.
    Do NOT mount it anywhere in this plan — mounts land in 11-03/04/06.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - `ApiResult` failure variant includes `code?: string`; the success variant is unchanged
    - `apiFetch` reads a top-level `code` only on the `!resp.ok` branch; the existing `detail` string path is unchanged
    - `me.ts` exports `getMe` and `patchLocale`, both returning `Promise<ApiResult<Me>>`, neither throwing
    - `LanguageSwitcher.tsx` is under `components/` (NOT `components/ui/`) and calls `i18n.changeLanguage`
    - LanguageSwitcher calls `patchLocale` only when `persist` is truthy
  </acceptance_criteria>
  <done>Switcher, /me seam, and additive code-extraction exist; nothing mounted yet.</done>
</task>

<task type="auto">
  <name>Task 4: Dutch-string CI guard + negative test</name>
  <files>frontend/scripts/ci_no_hardcoded_dutch.sh</files>
  <read_first>
    - backend/scripts/ci_no_permissive_rls.sh (the exact exit-code-is-the-gate structure to copy)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (ci_no_hardcoded_dutch.sh section — exemptions + negative test)
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (CI guard skeleton + Pitfall 5 false positives)
  </read_first>
  <action>
    Create `frontend/scripts/ci_no_hardcoded_dutch.sh` mirroring `ci_no_permissive_rls.sh`:
    `set -euo pipefail`, a `SCAN_DIR="${1:-frontend/src}"` first arg (so the negative test can point
    it at a temp offender), a Dutch-stopword regex
    `\b(niet|geen|wordt|klant|ingelogd|opnieuw|versturen|opslaan|verwijderen|annuleren|beschikbaar|vernieuwen|ruimte|gebruiker|verplicht|mislukt)\b`,
    and an `if grep -rEni --include='*.ts' --include='*.tsx' "$PATTERN" "$SCAN_DIR" | grep -vE
    '(/locales/|\.gen\.ts|/ui/|admin\.sales\.|coming-soon|\.test\.)'; then echo ERROR >&2; exit 1; fi`
    ending with `echo OK; exit 0`. The exit code IS the gate — do NOT use `grep -c == 0`. Exempt:
    `src/locales/**`, `*.gen.ts`, `ui/**`, `admin.sales.*`, `coming-soon*`, `.test.` files.

    Embed the negative test as a runnable block at the bottom of the same script gated behind a
    `--self-test` flag (mirroring the project's negative-test convention): write
    `const x = "niet beschikbaar";` to a temp file under a non-exempt path, invoke the guard against
    it, assert the guard exits non-zero, clean up. Document in a header comment how CI invokes both
    the positive scan (`bash ci_no_hardcoded_dutch.sh`) and the self-test
    (`bash ci_no_hardcoded_dutch.sh --self-test`).

    NOTE: at this point the source still contains Dutch (externalization is 11-03..11-06), so the
    plain scan is EXPECTED to fail until those plans complete. This task only proves the guard
    mechanics (self-test passes). The phase gate runs the full scan after Wave 2.
  </action>
  <verify>
    <automated>bash frontend/scripts/ci_no_hardcoded_dutch.sh --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `bash frontend/scripts/ci_no_hardcoded_dutch.sh --self-test` exits 0 (proves a planted offender triggers non-zero on the inner scan)
    - The script uses grep's own exit code as the gate (no `grep -c == 0` construct present)
    - The exemption filter includes `/locales/`, `.gen.ts`, `/ui/`, `admin.sales.`, `coming-soon`, `.test.`
    - Running `grep -n 'grep -c' frontend/scripts/ci_no_hardcoded_dutch.sh` returns no match
  </acceptance_criteria>
  <done>The Dutch-string guard exists, mirrors QA-02 style, and its negative self-test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Two new packages installed (i18next, react-i18next) — supply-chain surface |
| catalog value → React render | Translated strings interpolated into the DOM |
| server error `code` → toast | Backend-emitted machine code consumed by the frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | npm installs (i18next, react-i18next) | mitigate | Task 1 blocking-human checkpoint verifies both on npmjs.com before install; language-detector plugin deliberately NOT installed |
| T-11-01 | Tampering (XSS) | i18n string interpolation | mitigate | `interpolation.escapeValue:false` is safe only because React auto-escapes; never `dangerouslySetInnerHTML` a catalog value (Task 2) |
| T-11-02 | Information Disclosure | `code` on apiFetch failure branch | accept | `code` is additive metadata; the raw `detail` path is unchanged; internal 4xx/5xx keep generic messages (curated enum lands in 11-02/11-07) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean in frontend/
- `npm run test -- src/lib/i18n` green (date-locale + error-codes)
- CI guard self-test exits 0
- i18n provider mounted; app still renders in nl with no console i18next init errors
</verification>

<success_criteria>
- react-i18next installed and initialized (nl default + fallback), provider mounted in __root.tsx
- getDateLocale, detectLocale, error-code resolver, LanguageSwitcher, getMe/patchLocale all exist and typecheck
- apiFetch surfaces `code` additively without changing the string-detail path
- 12 catalog files exist; nl common authored, fr/en common seeded, other namespaces empty
- CI guard exists with a passing negative self-test
</success_criteria>

<output>
Create `.planning/phases/11-internationalization-nl-fr-en/11-01-SUMMARY.md` when done
</output>
