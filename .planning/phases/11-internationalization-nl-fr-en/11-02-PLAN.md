---
phase: 11-internationalization-nl-fr-en
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/alembic/versions/0010_locale_columns.py
  - backend/app/db/models/organization.py
  - backend/app/db/models/membership.py
  - backend/app/api/me_routes.py
  - backend/app/api/errors.py
  - backend/app/main.py
  - backend/tests/test_me_routes.py
  - backend/tests/test_error_codes.py
  - backend/tests/test_schema_shape_locale.py
autonomous: true
requirements: [I18N-02, I18N-01]
must_haves:
  truths:
    - "organizations carry a NOT-NULL default_locale backfilled to 'nl'; memberships carry a nullable per-user locale override"
    - "GET /me returns the caller's resolved locale (user override → space default → nl), deriving identity only from the verified token"
    - "PATCH /me/locale persists the caller's override, rejecting any locale not in {nl,fr,en}"
    - "A CodedError exception handler emits {detail, code} additively; existing HTTPException string-detail raises are unchanged"
  artifacts:
    - path: "backend/app/db/alembic/versions/0010_locale_columns.py"
      provides: "down_revision 0009; adds default_locale (org, NOT NULL server_default 'nl') + membership locale override (nullable)"
      contains: "down_revision"
    - path: "backend/app/api/me_routes.py"
      provides: "GET /me + PATCH /me/locale under protected_router"
      contains: "me_router"
    - path: "backend/app/api/errors.py"
      provides: "CodedError custom exception + FastAPI handler emitting {detail, code}"
      contains: "CodedError"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/api/me_routes.py"
      via: "protected_router.include_router(me_router)"
      pattern: "me_router"
    - from: "backend/app/main.py"
      to: "backend/app/api/errors.py"
      via: "app.exception_handler(CodedError)"
      pattern: "exception_handler"
    - from: "backend/app/api/me_routes.py"
      to: "app.auth.identity.Identity"
      via: "get_current_identity re-derives the user server-side (identity.uid)"
      pattern: "get_current_identity"
---

<objective>
Add the backend locale surface: the 0010 migration + ORM columns for space `default_locale` and
the per-user membership `locale` override, the `GET /me` / `PATCH /me/locale` endpoints with the
D-07 resolution chain, and the additive error-code contract (`CodedError` + handler). Author the
backend tests by construction.

Purpose: The frontend switcher (11-01) and the boot read need a server home for locale and a
persist endpoint; the error-code toast map (11-01) needs a backend that emits `code`. This is the
Wave-1 backend foundation, parallel to 11-01 (zero file overlap).
Output: 0010 migration, two model columns, me_routes, errors.py + main.py wiring, three test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internationalization-nl-fr-en/11-CONTEXT.md
@.planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md
@.planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md

<interfaces>
<!-- Executor: use these existing contracts directly. -->

From backend/app/auth/identity.py:
```python
@dataclass(frozen=True)
class Identity:
    uid: str
    email: str | None
    role: str          # "superadmin" (space_id None) | "user" (space_id set)
    space_id: str | None
```
`get_current_identity` (app/auth/dependencies.py) is the verified-token boundary.

From backend/app/db/session.py — DI session/repo helpers (all sync `def`, one tx/request):
- `get_admin_session(identity)` — superadmin-only, superadmin engine, yields AdminRepo (line 260).
- `get_tenant_repo(identity)` — user-scoped tenant repo (line 52).
Every feature router mounts UNDER `protected_router` (main.py:134-157) via
`protected_router.include_router(...)`; there is NO second `app.include_router` per nested router.

From backend/app/api/admin_routes.py `update_space` (line 406) — the PATCH+Pydantic+404+audit shape
to mirror; all handlers are sync `def` (pg8000 blocking → threadpool).

From backend/app/db/models/membership.py `status` column (line 45) and organization.py `status`
(line 34) — the exact `String, nullable=False, server_default="..."` pattern to copy. Explicit
Index names, NO schema prefix (keeps `alembic check` clean).

Migration analog: backend/app/db/alembic/versions/0006_user_space_audit.py (col-add + server_default
backfill). Next revision is 0010, down_revision "0009", SCHEMA = "nestor".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 0010 migration + org/membership locale columns + schema-shape test</name>
  <files>backend/app/db/alembic/versions/0010_locale_columns.py, backend/app/db/models/organization.py, backend/app/db/models/membership.py, backend/tests/test_schema_shape_locale.py</files>
  <read_first>
    - backend/app/db/alembic/versions/0006_user_space_audit.py (col-add-with-server_default backfill pattern; revision frontmatter)
    - backend/app/db/models/organization.py (the `status` column at line 34 to mirror for default_locale)
    - backend/app/db/models/membership.py (the `status`/nullable-column patterns; __table_args__ explicit-index note)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (0010 migration + models sections; superadmin-no-membership GOTCHA)
    - backend/tests/ (an existing schema-shape test to mirror — e.g. the Phase 1/5 shape suites)
  </read_first>
  <action>
    Create `0010_locale_columns.py`: `revision = "0010"`, `down_revision = "0009"`, `SCHEMA =
    "nestor"`. Upgrade adds `organizations.default_locale` via `op.add_column` with
    `sa.String()`, `nullable=False`, `server_default=sa.text("'nl'")` (backfills existing rows to
    'nl' non-null), and `organization_memberships.locale` with `sa.String()`, `nullable=True`
    (null = "inherit space default", D-07). Downgrade drops both columns. No new index (scalar
    columns). Both target `schema=SCHEMA`.

    Add to `organization.py`: `default_locale: Mapped[str] = mapped_column(String, nullable=False,
    server_default="nl")` mirroring the `status` column exactly. App-level allowed set {nl,fr,en}
    enforced in code, NOT a PG enum (matches the status-column rationale, avoids alembic enum-alter).

    Add to `membership.py`: `locale: Mapped[str | None] = mapped_column(String, nullable=True)`.
    No new Index (keeps `alembic check` clean).

    SUPERADMIN LOCALE HOME (Open Q1 — DECIDED): the superadmin's own locale lives in the same
    nullable membership `locale` column WHEN a membership row exists; when a superadmin has NO
    membership row, `GET /me` returns `locale: null` and `space_default_locale: "nl"`, so the
    frontend falls back to its browser-detected / stored pref. No separate user_preferences table
    is introduced in v1 — document this rationale in the migration/model docstring. (A superadmin
    with no membership persists nothing server-side; their switcher choice lives in localStorage
    via the LanguageSwitcher `persist=false` fallback path from 11-01.)

    Author `test_schema_shape_locale.py`: assert (via the existing shape-test harness /
    information_schema query pattern) that `nestor.organizations` has a NOT-NULL `default_locale`
    with default 'nl', and `nestor.organization_memberships` has a nullable `locale` column.
  </action>
  <verify>
    <automated>MISSING — backend tests run in Cloud Build (no local Python/Docker). Author test_schema_shape_locale.py by construction; the executor confirms `alembic check`-clean intent by matching model+migration column names 1:1. Cloud Build suite is the phase-gate runner.</automated>
  </verify>
  <acceptance_criteria>
    - `0010_locale_columns.py` has `down_revision = "0009"` and `revision = "0010"`
    - Upgrade adds `default_locale` NOT NULL `server_default=sa.text("'nl'")` to organizations and nullable `locale` to organization_memberships, both `schema="nestor"`
    - organization.py contains `default_locale` mapped column with `server_default="nl"`; membership.py contains nullable `locale` mapped column
    - No new `Index(...)` is added for the locale columns (grep the migration + models for locale index — none)
    - test_schema_shape_locale.py asserts both columns' nullability + the org default
    - The migration docstring records the superadmin-no-membership decision (locale null → frontend fallback)
  </acceptance_criteria>
  <done>Migration + models + shape test authored; column names match 1:1 between ORM and migration.</done>
</task>

<task type="auto">
  <name>Task 2: me_routes (GET /me + PATCH /me/locale) + resolution chain + tests</name>
  <files>backend/app/api/me_routes.py, backend/app/main.py, backend/tests/test_me_routes.py</files>
  <read_first>
    - backend/app/api/admin_routes.py (update_space PATCH+Pydantic+404 shape at line 406; sync-def rationale at line 28)
    - backend/app/db/session.py (get_admin_session line 260; get_tenant_repo line 52 — pick the session dep that can read membership+org for both roles)
    - backend/app/auth/identity.py (Identity fields; superadmin space_id is None)
    - backend/app/main.py (protected_router.include_router pattern lines 134-157)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (me_routes.py section — router/mount/Pydantic/V5-validation)
  </read_first>
  <action>
    Create `me_routes.py` with `me_router = APIRouter(tags=["me"])`. Both handlers are sync `def`
    (pg8000 blocking). `_ALLOWED = {"nl", "fr", "en"}`.

    Pydantic models: `Me(BaseModel)` with `locale: str | None` (the user override, null=inherit) and
    `space_default_locale: str` (resolved from the org); `LocalePatchBody(BaseModel)` with
    `locale: str`.

    `GET /me`: `Depends(get_current_identity)` + a session dependency that can read the caller's
    membership row and its organization for BOTH roles. Re-derive the user from `identity.uid` /
    `identity.space_id` ONLY (never request input). Resolution: look up the membership by uid; the
    response `locale` = membership.locale (may be null); `space_default_locale` = the org's
    `default_locale` for `identity.space_id`, or "nl" when the caller is a superadmin with no
    membership / no space (superadmin locale-home decision from Task 1).

    `PATCH /me/locale`: validate `body.locale in _ALLOWED` else raise (see Task 3 — use `CodedError`
    with code `INVALID_LOCALE`; until 11-01's map lands it still returns a raw detail). Persist the
    override onto the caller's membership row (`membership.locale = body.locale`), scoped to
    `identity.uid`. A superadmin without a membership row: return the resolved `Me` without
    persisting (localStorage-only path) — do NOT create a membership row. Return the updated `Me`.
    SECURITY (V4/EoP): locale is display-only; the PATCH re-derives the user server-side, so a
    client-supplied locale can only change display, never widen access.

    Mount: add `protected_router.include_router(me_router)` in main.py next to the existing feature
    includes (do NOT add a second `app.include_router`).

    Author `test_me_routes.py`: (a) a user GET /me returns their membership locale + org default;
    (b) PATCH /me/locale with "fr" persists and a subsequent GET returns "fr"; (c) PATCH with an
    invalid locale ("de"/"xx") is rejected 422 and does NOT persist; (d) the persisted override is
    re-read at boot (persist→GET round-trip); (e) locale is derived from the token, not the body —
    a body cannot set another user's locale.
  </action>
  <verify>
    <automated>MISSING — backend tests run in Cloud Build. Author test_me_routes.py by construction covering the 5 behaviors; Cloud Build suite is the phase-gate runner.</automated>
  </verify>
  <acceptance_criteria>
    - me_routes.py defines `me_router`, `GET /me`, `PATCH /me/locale`, both sync `def`
    - PATCH rejects `locale ∉ {nl,fr,en}` (422/CodedError) and does not persist on rejection
    - GET /me resolution returns membership.locale (nullable) + org default_locale, falling back to "nl" for a superadmin with no membership
    - main.py contains `protected_router.include_router(me_router)` and no second app.include_router for it
    - Both handlers take `Identity = Depends(get_current_identity)` and never read locale from path/query for identity
    - test_me_routes.py covers persist, re-read-at-boot, invalid-locale rejection, and token-derived identity
  </acceptance_criteria>
  <done>/me endpoints exist under protected_router, resolution chain implemented, tests authored.</done>
</task>

<task type="auto">
  <name>Task 3: CodedError contract (errors.py + main.py handler) + test</name>
  <files>backend/app/api/errors.py, backend/app/main.py, backend/tests/test_error_codes.py</files>
  <read_first>
    - backend/app/main.py (the `app = FastAPI(...)` construction + readyz JSONResponse at line 184; where to register the handler)
    - backend/app/api/intake_routes.py (existing `raise HTTPException(status, "string")` sites — these stay unchanged)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (errors.py + main.py wiring section; option-b additive shape)
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (Pattern 4 + Open Q3 + Security Info-Disclosure note)
  </read_first>
  <action>
    Create `errors.py` with `class CodedError(Exception)` carrying `status_code: int`, `code: str`,
    `detail: str`. Register a FastAPI exception handler (`@app.exception_handler(CodedError)`) in
    main.py returning `JSONResponse({"detail": exc.detail, "code": exc.code},
    status_code=exc.status_code)`. `detail` stays a plain string so `apiFetch`'s existing
    string-detail raw-fallback path is untouched; `code` is read additively by the frontend
    (11-01 error-codes map).

    Define the curated user-facing code enum as module constants in errors.py — scope to errors
    users actually see (D-11): at minimum `INVALID_LOCALE`, `INTAKE_NOT_FOUND`, `RECIPIENT_INVALID`,
    `MAIL_SEND_FAILED`. These MUST match the keys the frontend `ERROR_CODES` map (11-01) references.
    SECURITY (Info Disclosure): the code set is a curated user-facing enum; internal 4xx/5xx keep
    generic messages via plain HTTPException — do NOT attach codes to internal errors or leak
    stack/detail.

    Migrate the `PATCH /me/locale` invalid-locale raise (Task 2) to `CodedError(422, "INVALID_LOCALE",
    "Invalid locale")` as the first consumer. Leave all other existing `HTTPException` raises
    untouched (they keep working via raw-text fallback).

    Author `test_error_codes.py`: assert that a raised `CodedError` produces a response body with
    both `detail` (string) and `code`, at the given status; assert a plain `HTTPException` still
    produces a string `detail` with no `code` (backward-compat).
  </action>
  <verify>
    <automated>MISSING — backend tests run in Cloud Build. Author test_error_codes.py by construction; Cloud Build suite is the phase-gate runner.</automated>
  </verify>
  <acceptance_criteria>
    - errors.py defines `CodedError` with `status_code`, `code`, `detail`
    - main.py registers `@app.exception_handler(CodedError)` returning `{"detail", "code"}` JSONResponse
    - The curated code constants include INVALID_LOCALE, INTAKE_NOT_FOUND, RECIPIENT_INVALID, MAIL_SEND_FAILED
    - `PATCH /me/locale` invalid-locale path raises `CodedError(..., "INVALID_LOCALE", ...)`
    - Existing HTTPException string-detail raises are unchanged (no code attached)
    - test_error_codes.py asserts the {detail, code} shape and the HTTPException backward-compat path
  </acceptance_criteria>
  <done>Additive error-code contract wired; INVALID_LOCALE is its first consumer; test authored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PATCH /me/locale | Untrusted locale value crosses here |
| verified token → identity | The only source of the caller's identity/space |
| CodedError → response body | New `code` field added to error responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Elevation of Privilege | PATCH /me/locale | mitigate | Identity re-derived from the verified token (identity.uid); a client-supplied locale changes display only, never widens access; locale is never an authz input (Task 2) |
| T-11-04 | Input Validation | PATCH /me/locale body | mitigate | `locale ∈ {nl,fr,en}` enforced; arbitrary values rejected 422 (Task 2/3) |
| T-11-05 | Information Disclosure | CodedError `code` path | mitigate | `code` is a curated user-visible enum; internal 4xx/5xx keep generic HTTPException messages, no stack/detail leak (Task 3) |
| T-11-06 | Tampering | superadmin locale persistence | accept | Superadmin without a membership row persists nothing server-side (localStorage-only); no new writable surface introduced (Task 1/2) |
</threat_model>

<verification>
- Column names match 1:1 between 0010 migration and ORM models (alembic check-clean intent)
- me_routes mounted under protected_router; PATCH validates the enum
- CodedError handler registered; existing raises unchanged
- Backend test modules authored (test_schema_shape_locale, test_me_routes, test_error_codes) — run in Cloud Build at the phase gate
</verification>

<success_criteria>
- default_locale (org, NOT NULL 'nl') + membership locale override (nullable) exist via 0010
- GET /me / PATCH /me/locale implement the user→space→nl chain with token-derived identity + enum validation
- CodedError emits {detail, code} additively; INVALID_LOCALE is its first consumer
- Three backend test modules authored by construction
</success_criteria>

<output>
Create `.planning/phases/11-internationalization-nl-fr-en/11-02-SUMMARY.md` when done
</output>
