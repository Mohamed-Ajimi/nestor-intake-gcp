---
phase: 11-internationalization-nl-fr-en
plan: 09
type: execute
wave: 4
depends_on: [11-03, 11-04, 11-05, 11-06, 11-07]
files_modified:
  - frontend/src/locales/nl/common.json
  - frontend/src/locales/fr/common.json
  - frontend/src/locales/en/common.json
autonomous: true
requirements: [I18N-01]
must_haves:
  truths:
    - "The long-tail small-hit source files that fall into the common namespace are externalized"
    - "The FULL CI Dutch-string guard passes across ALL in-scope frontend source (no hardcoded Dutch remains)"
  artifacts:
    - path: "frontend/src/locales/nl/common.json"
      provides: "Long-tail common-namespace keys (nl authored)"
      contains: "\"nl\""
  key_links:
    - from: "frontend/scripts/ci_no_hardcoded_dutch.sh"
      to: "frontend/src (all in-scope source)"
      via: "full plain scan exits 0 after every externalization plan has run"
      pattern: "ci_no_hardcoded_dutch"
---

<objective>
Phase-gate plan: after ALL externalization plans (11-03/04/05/06/07) have completed, sweep the
long tail of ~14 small source files (1-3 Dutch hits each) into the `common` namespace, then run the
FULL CI Dutch-string guard across all in-scope source and drive it to exit 0.

Purpose: The "no hardcoded Dutch strings" success criterion (I18N-01) is a whole-tree property — it
can only be verified once every plan that externalizes strings has finished. Running the full guard
inside a same-wave externalization plan would either fail on strings peer plans have not yet
externalized or pass prematurely. This plan therefore runs in Wave 4, AFTER 11-05 (Wave 3) and every
other externalization plan, as the single owner of the full-tree gate. It only writes the three
`common.json` catalog files (disjoint from all earlier plans' catalogs), so no file clobber.
Output: common catalog completed for the long-tail files, full CI guard green across in-scope source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internationalization-nl-fr-en/11-CONTEXT.md
@.planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md
@.planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md
@.planning/phases/11-internationalization-nl-fr-en/11-01-SUMMARY.md
@.planning/phases/11-internationalization-nl-fr-en/11-03-SUMMARY.md
@.planning/phases/11-internationalization-nl-fr-en/11-04-SUMMARY.md
@.planning/phases/11-internationalization-nl-fr-en/11-05-SUMMARY.md
@.planning/phases/11-internationalization-nl-fr-en/11-06-SUMMARY.md
@.planning/phases/11-internationalization-nl-fr-en/11-07-SUMMARY.md

<interfaces>
<!-- Executor: use these existing contracts directly. -->

From react-i18next: `useTranslation("common")` for the small-file long tail.
From frontend/src/lib/i18n (11-01): `resolveErrorKey(code)`; `getDateLocale` if a swept file has a
date-fns `nl` site.

Exemptions the CI guard already applies (do NOT externalize these): `admin.sales.*`,
`*.coming-soon*`, `src/components/ui/**`, `*.gen.ts`, `src/locales/**`, `.test.` files.

The full guard is the source-of-truth for completeness (D-13): `bash frontend/scripts/ci_no_hardcoded_dutch.sh`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Long-tail common sweep + FULL CI guard green</name>
  <files>
    <!-- The three common.json catalogs are the only files this plan is guaranteed to write. -->
    <!-- The long-tail source files below are DISCOVERED DYNAMICALLY by running the CI guard's -->
    <!-- plain scan (D-13: the scan is the source-of-truth). The list below is the representative -->
    <!-- ~14-file long tail from the RESEARCH density map (1-3 Dutch hits each → common namespace); -->
    <!-- the executor externalizes exactly the files the scan flags that are not owned by an -->
    <!-- earlier plan, so the concrete set may differ slightly from this representative list. -->
    frontend/src/locales/nl/common.json,
    frontend/src/locales/fr/common.json,
    frontend/src/locales/en/common.json,
    frontend/src/components/admin/ClientDetailDrawer.tsx,
    frontend/src/components/admin/ProductBadge.tsx,
    frontend/src/components/admin/StatusPill.tsx,
    frontend/src/components/intake/IntakeWorkflowStepper.tsx,
    frontend/src/components/intake/SkillRunProgress.tsx,
    frontend/src/components/intake/FieldDisplay.tsx,
    frontend/src/components/admin/clientPills.tsx,
    frontend/src/routes/__root.tsx,
    frontend/src/routes/index.tsx,
    frontend/src/router.tsx,
    frontend/src/lib/salesLabels.ts,
    frontend/src/lib/research-question.ts,
    frontend/src/components/NotFound.tsx
  </files>
  <read_first>
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (density map — the "~14 more files, 1-3 hits each → common" tail; the manual-sweep-is-source-of-truth note, A6)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (Shared Pattern: string externalization)
    - frontend/scripts/ci_no_hardcoded_dutch.sh (11-01 — the guard whose full scan must pass)
  </read_first>
  <action>
    Run the CI guard's plain scan (`bash frontend/scripts/ci_no_hardcoded_dutch.sh`) to enumerate
    every remaining in-scope file that still contains a Dutch stopword after 11-03/04/05/06/07 have
    completed. For each flagged file NOT already owned by another plan (the long-tail small-hit files
    — the representative set is listed in `<files>`, but the scan is authoritative), add
    `useTranslation("common")` and replace the literals with `t("<key>")`, adding keys to the three
    `common.json` catalogs (nl verbatim, fr/en drafted). If a swept file has a date-fns `{ nl }`
    import, swap it to `getDateLocale(i18n.language)`. Do NOT touch the exempt surfaces (sales,
    coming-soon, ui/, locales/, .gen.ts, .test.). Re-run the guard until it exits 0.

    This task is the D-13 manual-sweep completion + the phase gate: the CI guard's plain scan is the
    source-of-truth for completeness. Iterate file-by-file until the scan is clean across all
    in-scope source.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <automated>bash frontend/scripts/ci_no_hardcoded_dutch.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash frontend/scripts/ci_no_hardcoded_dutch.sh` exits 0 (no hardcoded Dutch in any in-scope source)
    - `npx tsc --noEmit` passes
    - Every long-tail file flagged by the scan is externalized to the `common` namespace (or is a legitimately-exempt surface)
    - New `t("...")` keys exist in nl/fr/en common.json
  </acceptance_criteria>
  <done>The full CI Dutch-string guard passes — no hardcoded Dutch remains in in-scope source (I18N-01 phase gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog value → React render | Translated strings interpolated into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering (XSS) | common catalog interpolation | mitigate | React auto-escapes; never dangerouslySetInnerHTML a catalog value (Task 1) |
| T-11-05 | Info Disclosure | long-tail error toasts | mitigate | Error-code map surfaces curated codes; raw fallback carries only existing server detail, no new leak (Task 1) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- FULL `bash frontend/scripts/ci_no_hardcoded_dutch.sh` exits 0 (this is the phase-gate guard, run only here after all externalization plans)
</verification>

<success_criteria>
- Long-tail common files externalized
- Full CI Dutch-string guard passes across all in-scope source (I18N-01 "no hardcoded Dutch" true)
</success_criteria>

<output>
Create `.planning/phases/11-internationalization-nl-fr-en/11-09-SUMMARY.md` when done
</output>
