---
phase: 11-internationalization-nl-fr-en
plan: 08
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - backend/app/mail/render.py
  - backend/app/mail/templates/nl/validation.html.j2
  - backend/app/mail/templates/nl/results.html.j2
  - backend/app/mail/templates/nl/invite.html.j2
  - backend/app/mail/templates/fr/validation.html.j2
  - backend/app/mail/templates/fr/results.html.j2
  - backend/app/mail/templates/fr/invite.html.j2
  - backend/app/mail/templates/en/validation.html.j2
  - backend/app/mail/templates/en/results.html.j2
  - backend/app/mail/templates/en/invite.html.j2
  - backend/app/api/intake_routes.py
  - backend/app/api/admin_routes.py
  - backend/tests/test_mail_locale.py
autonomous: true
requirements: [I18N-01, I18N-02]
must_haves:
  truths:
    - "The validation/results/invite mails render in the recipient's resolved locale (user pref → space default → nl), resolved server-side per recipient"
    - "An NL admin sending to an FR client produces an FR mail — locale is never taken from the sending admin's UI"
    - "A mixed-locale recipient list sends the correct locale variant to each recipient"
    - "admin_validated stays Dutch; _base stays structural-only; autoescape stays ON in every variant"
  artifacts:
    - path: "backend/app/mail/render.py"
      provides: "render_* functions accept a locale param and select the per-locale template variant"
      contains: "locale"
    - path: "backend/app/mail/templates/fr/validation.html.j2"
      provides: "FR validation mail variant"
      contains: "j2"
  key_links:
    - from: "backend/app/api/intake_routes.py"
      to: "backend/app/mail/render.py"
      via: "resolved recipient locale threaded into render_results/render_validation"
      pattern: "locale="
    - from: "backend/app/api/intake_routes.py"
      to: "backend/app/db/models/membership.py"
      via: "_resolve_recipient_locale reads membership.locale → org.default_locale → nl"
      pattern: "default_locale"
---

<objective>
Close the Phase 10 email-i18n deferral (D-02): give the validation/results/invite Jinja2 templates
NL/FR/EN variants, add a `locale` param to the `render_*` functions selecting the variant, and
resolve each recipient's locale server-side (user pref → space default → nl) in the send path so a
mixed-locale recipient list sends the right variant to each recipient. Author the mail-locale
tests by construction.

Purpose: An FR/EN client must receive mail in their language regardless of the sending admin's UI
language (D-07/D-10). Depends on 11-02 (the membership `locale` + org `default_locale` columns and
the resolution semantics). Wave 3 — the only plan that touches the mail send path.
Output: 9 locale template variants (nl/fr/en × validation/results/invite), render.py locale param,
recipient-locale resolution in the send path, invite-send locale in admin_routes, mail-locale tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internationalization-nl-fr-en/11-CONTEXT.md
@.planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md
@.planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md
@.planning/phases/11-internationalization-nl-fr-en/11-02-SUMMARY.md

<interfaces>
<!-- Executor: use these existing contracts directly. -->

From backend/app/mail/render.py — the current signatures to extend with a `locale: str = "nl"` param:
```python
def render_validation(*, first_name, project_title, cta_url, is_reminder, app_base_url=None) -> str
def render_results(*, first_name, project_title, cta_url, app_base_url=None) -> str
def render_invite(*, cta_url, app_base_url=None) -> str
def render_admin_validated(*, client_name, project_title, cta_url, app_base_url=None) -> str  # STAYS Dutch (D-02)
```
The module-level `_env = Environment(loader=FileSystemLoader(templates), autoescape=select_autoescape(["html","j2"]))`
— autoescape (T-10-01) MUST stay ON in every variant.

From backend/app/api/intake_routes.py:
- `_resolve_active_member_emails(session, space_id, recipient_ids)` (line 591) resolves recipient
  EMAILS from active memberships. Add a sibling that ALSO returns each recipient's resolved locale.
- `_run_intake_send` (line 731): renders once + `mail_resend.send(to=emails, ...)` one-shot at
  line 799. Today it renders ONE body for all recipients — for per-recipient locale this becomes a
  render+send per distinct locale. Preserve D-16 discipline: SEND FIRST, stamp `*_sent_at` + audit
  on 2xx ONLY (lines 797-814); do NOT regress the `{"success": False}` no-stamp/no-audit path.

From backend/app/api/admin_routes.py — the invite-mail send path (renders `render_invite`); the
invitee's locale resolves to the target space's `default_locale` (the invitee has no membership
locale yet at invite time) → nl fallback.

Resolution chain (D-07): membership.locale (user override, from 11-02) → organization.default_locale
(space, from 11-02) → "nl". Resolve SERVER-SIDE per recipient, NEVER from the sending admin's UI.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locale template variants + render.py locale param</name>
  <files>backend/app/mail/render.py, backend/app/mail/templates/nl/validation.html.j2, backend/app/mail/templates/nl/results.html.j2, backend/app/mail/templates/nl/invite.html.j2, backend/app/mail/templates/fr/validation.html.j2, backend/app/mail/templates/fr/results.html.j2, backend/app/mail/templates/fr/invite.html.j2, backend/app/mail/templates/en/validation.html.j2, backend/app/mail/templates/en/results.html.j2, backend/app/mail/templates/en/invite.html.j2</files>
  <read_first>
    - backend/app/mail/render.py (the render_* functions + the autoescape _env)
    - backend/app/mail/templates/validation.html.j2, results.html.j2, invite.html.j2, _base.html.j2 (the current Dutch templates + the shared base)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (render.py + templates section — option (a) per-locale dir)
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (Open Q4 — _base stays structural)
  </read_first>
  <action>
    Adopt the per-locale template-dir shape (Open Q4 option a): create
    `templates/{nl,fr,en}/{validation,results,invite}.html.j2`. Move the existing Dutch prose into
    the `nl/` variants verbatim; author `fr/` and `en/` variants with translated prose (D-12; user
    reviews tone in UAT). Keep `_base.html.j2` where it is and make it STRUCTURAL-ONLY (layout,
    colors, logo img) so it is shared across locales and carries no locale-specific prose;
    `admin_validated.html.j2` STAYS at the top level, STAYS Dutch (D-02). Every variant keeps
    `{% extends %}` on `_base` and keeps autoescape ON (T-10-01) — never mark a catalog/prose value
    `| safe`.

    Extend the `render_*` functions with a `locale: str = "nl"` param that resolves the template
    path as `f"{locale}/{name}.html.j2"`, falling back to `f"nl/{name}.html.j2"` when the requested
    locale variant is missing (nl guaranteed fallback). `render_admin_validated` is NOT given a
    locale param (stays Dutch). Signatures otherwise unchanged so existing callers keep working with
    the nl default.
  </action>
  <verify>
    <automated>MISSING — backend tests run in Cloud Build. Task 2 authors test_mail_locale.py asserting variant selection + nl fallback + autoescape; Cloud Build is the phase-gate runner.</automated>
  </verify>
  <acceptance_criteria>
    - 9 template files exist under `templates/{nl,fr,en}/{validation,results,invite}.html.j2`; each `{% extends %}` `_base`
    - The original Dutch prose is preserved verbatim in the `nl/` variants
    - render_validation/render_results/render_invite accept `locale: str = "nl"` and resolve `{locale}/{name}.html.j2` with nl fallback
    - render_admin_validated has NO locale param and still points at the top-level Dutch template
    - `_base.html.j2` carries no locale-specific prose; autoescape stays ON (no `| safe` on prose)
  </acceptance_criteria>
  <done>Per-locale mail variants exist; render_* select the variant with nl fallback; admin_validated + _base untouched-Dutch.</done>
</task>

<task type="auto">
  <name>Task 2: Recipient-locale resolution in the intake send path + invite send + tests</name>
  <files>backend/app/api/intake_routes.py, backend/app/api/admin_routes.py, backend/tests/test_mail_locale.py</files>
  <read_first>
    - backend/app/api/intake_routes.py (_resolve_active_member_emails line 591; _run_intake_send line 731; the render+send at 772/787/799; the D-16 send-first/stamp-on-2xx discipline at 797-814)
    - backend/app/api/admin_routes.py (the invite-mail send path calling render_invite)
    - backend/app/db/models/membership.py + organization.py (the locale + default_locale columns from 11-02)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (intake_routes _run_intake_send section — per-recipient locale loop; D-16 discipline)
  </read_first>
  <action>
    Add `_resolve_recipient_locale(session, space_id, recipient_ids)` (a sibling to
    `_resolve_active_member_emails`) that returns each active recipient's resolved locale via the
    D-07 chain: `membership.locale` (override) → `organization.default_locale` (space) → "nl". Read
    both columns from the same active-member query. Reject the same invalid ids
    `_resolve_active_member_emails` rejects (reuse its validation; do not silently drop).

    Rework `_run_intake_send`: group the resolved recipients by locale and, for each distinct
    locale, render the appropriate body (`render_results(..., locale=L)` /
    `render_validation(..., locale=L)`) and `mail_resend.send(to=<emails for L>, subject, html)`.
    Preserve D-16 discipline EXACTLY: SEND FIRST; only after a successful send stamp the
    `*_sent_at` column (once, not per-locale-group) and write the `mail.sent` audit row on the same
    tx; on ANY send failure return `{"success": False}` with NO stamp / NO audit. The APP_BASE_URL
    guard (line 761) stays. `recipient_count` in the response/audit remains the total across groups.
    Locale is resolved server-side per recipient — NEVER from the sending admin's UI.

    In `admin_routes.py` invite send: resolve the invitee's locale as the target space's
    `default_locale` → "nl" (the invitee has no membership locale yet) and pass `locale=` into
    `render_invite`.

    Author `test_mail_locale.py`: (a) a recipient with membership.locale="fr" gets the fr variant;
    (b) a recipient with no override but space default_locale="en" gets the en variant; (c) a
    recipient with neither gets nl; (d) a mixed-locale recipient list produces the correct variant
    per recipient (assert a send per distinct locale); (e) the sending admin's UI language does NOT
    affect the rendered variant; (f) D-16 preserved — a send failure produces `{"success": False}`
    with no stamp/audit; (g) invite mail uses the space default_locale.
  </action>
  <verify>
    <automated>MISSING — backend tests run in Cloud Build. Author test_mail_locale.py by construction covering behaviors (a)-(g); Cloud Build suite is the phase-gate runner.</automated>
  </verify>
  <acceptance_criteria>
    - `_resolve_recipient_locale` resolves membership.locale → org.default_locale → "nl" per recipient
    - `_run_intake_send` renders + sends per distinct locale group; stamp + audit happen once on 2xx only
    - The D-16 failure path is intact: send failure → `{"success": False}`, no `*_sent_at` stamp, no audit row
    - admin_routes invite send passes the space default_locale into render_invite
    - Locale is never read from the sending admin's request/UI (grep the send path — resolution uses membership/org only)
    - test_mail_locale.py covers per-recipient variant, mixed-locale, admin-UI-independence, D-16, and invite locale
  </acceptance_criteria>
  <done>Recipient-locale mail resolution works server-side per recipient with D-16 discipline preserved; tests authored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipient membership/org → mail locale | Locale resolved server-side from DB, not from request/UI |
| prose variant → mail body | Translated Jinja2 prose rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering (XSS) | mail locale variants | mitigate | autoescape stays ON in every variant (_env select_autoescape); `{{ var }}` escaped; no `| safe` on prose (Task 1) |
| T-11-11 | Information Disclosure | recipient-locale resolution | mitigate | Locale resolved from the recipient's OWN membership/space within the intake's space_id; the existing _resolve_active_member_emails rejects foreign/deactivated ids (422) — no cross-space recipient inference (Task 2) |
| T-11-12 | Tampering | D-16 send discipline | mitigate | SEND FIRST, stamp+audit on 2xx only, unchanged; per-locale grouping does not regress the no-stamp/no-audit failure path (Task 2) |
</threat_model>

<verification>
- 9 locale variants exist + extend _base; admin_validated stays Dutch
- render_* select the variant with nl fallback; autoescape ON
- Send path resolves per-recipient locale server-side; D-16 discipline intact
- test_mail_locale authored (Cloud Build at phase gate)
</verification>

<success_criteria>
- validation/results/invite mails render in the recipient's resolved locale (user→space→nl), server-side per recipient
- Mixed-locale recipient list sends the correct variant to each
- admin_validated + _base stay Dutch/structural; autoescape preserved
- Mail-locale tests authored by construction
</success_criteria>

<output>
Create `.planning/phases/11-internationalization-nl-fr-en/11-08-SUMMARY.md` when done
</output>
