---
phase: 11-internationalization-nl-fr-en
plan: 05
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - frontend/src/components/intake/ResearchResultsPanel.tsx
  - frontend/src/components/intake/AIReviewPanel.tsx
  - frontend/src/components/intake/AISkillsPanel.tsx
  - frontend/src/components/intake/NextStepBanner.tsx
  - frontend/src/components/intake/ResearchArtifacts.tsx
  - frontend/src/components/intake/RecipientPicker.tsx
  - frontend/src/components/intake/HandoffBlock.tsx
  - frontend/src/components/intake/FinalReportBlock.tsx
  - frontend/src/components/intake/ContextPackBlock.tsx
  - frontend/src/components/intake/ContextPackPDF.tsx
  - frontend/src/components/intake/NestorBriefingPDF.tsx
  - frontend/src/routes/intake.$id.results.tsx
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/en/intake.json
autonomous: true
requirements: [I18N-01]
must_haves:
  truths:
    - "The intake AI/results/artifact components render fully in the active locale"
    - "ContextPackPDF and NestorBriefingPDF render labels in the active language via pre-resolved strings passed as props (Pitfall 3)"
    - "The three intake-component date-locale call sites follow the active language via getDateLocale"
  artifacts:
    - path: "frontend/src/components/intake/ResearchResultsPanel.tsx"
      provides: "Externalized results panel (45 strings)"
      contains: "useTranslation"
    - path: "frontend/src/components/intake/ContextPackPDF.tsx"
      provides: "PDF labels from pre-resolved strings (no useTranslation inside the detached tree)"
      contains: "labels"
  key_links:
    - from: "frontend/src/components/intake/ContextPackBlock.tsx"
      to: "frontend/src/components/intake/ContextPackPDF.tsx"
      via: "resolved label strings passed as props at the pdf() call site"
      pattern: "labels"
    - from: "frontend/src/components/intake/NextStepBanner.tsx"
      to: "frontend/src/lib/i18n/date-locale.ts"
      via: "getDateLocale replaces the hardcoded nl import"
      pattern: "getDateLocale"
---

<objective>
Externalize the remaining intake components — the results/AI/artifact surfaces including the
second-highest-density file `ResearchResultsPanel.tsx` (45 strings), the two PDF exporters
(handled via the Pitfall-3 pre-resolved-props pattern), and the three date-locale call sites in
this set (NextStepBanner, ResearchArtifacts, ContextPackBlock) — plus the `intake.$id.results.tsx`
route.

Purpose: Completes the client-facing + review intake surfaces begun in 11-03. Depends only on
11-01 (i18n runtime, getDateLocale, catalogs). Disjoint file set from 11-03/04/06 → runs in
parallel. (Shares only the intake.json catalog files with 11-03; keys are namespaced by
sub-object so additive edits don't collide, and 11-03 authors the schema/form keys while this plan
authors the results/AI/PDF keys.)
Output: intake.json filled for the results/AI/PDF surfaces, 11 components + 1 route externalized,
PDF props threaded, three date-locale sites swapped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internationalization-nl-fr-en/11-CONTEXT.md
@.planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md
@.planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md
@.planning/phases/11-internationalization-nl-fr-en/11-01-SUMMARY.md

<interfaces>
<!-- Executor: use these existing contracts directly. -->

From frontend/src/lib/i18n (11-01): default `i18n` (read `i18n.language`), `getDateLocale(lang)`,
`resolveErrorKey(code)`.
From react-i18next: `useTranslation("intake")` → `t("key")`; `i18n.getFixedT(lang, "intake")` for
the PDF pre-resolution (Pitfall 3).

PDF EXCEPTION (verified call sites): `ContextPackPDF.tsx:305`, `NestorBriefingPDF.tsx:321` render
via `pdf(<Component/>).toBlob()` OUTSIDE the React tree / I18nextProvider — `useTranslation()`
inside them has NO provider context. Pass a pre-resolved `labels` object (built with `t()` /
`getFixedT` at the CALLING component, which IS inside the provider) into the PDF component as props.

Date-fns call sites in this set: `NextStepBanner.tsx`, `ResearchArtifacts.tsx`,
`ContextPackBlock.tsx` — swap `import { nl } from "date-fns/locale"` +
`format(..., { locale: nl })` → `getDateLocale(i18n.language)`.

Density map (this set): ResearchResultsPanel 45, AIReviewPanel 17, ResearchArtifacts 17,
NextStepBanner 13, RecipientPicker 11, ContextPackBlock 11, HandoffBlock 8, FinalReportBlock 7,
AISkillsPanel 5, NestorBriefingPDF 4, intake.$id.results.tsx 18.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Externalize ResearchResultsPanel + intake.$id.results route</name>
  <files>frontend/src/components/intake/ResearchResultsPanel.tsx, frontend/src/routes/intake.$id.results.tsx, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/intake.json</files>
  <read_first>
    - frontend/src/components/intake/ResearchResultsPanel.tsx (45 Dutch strings — the densest in this set)
    - frontend/src/routes/intake.$id.results.tsx (18 Dutch strings)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (Shared Pattern: string externalization)
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (density map — intake namespace)
  </read_first>
  <action>
    Add `const { t } = useTranslation("intake")` to each and replace every hardcoded Dutch literal
    (headings, section copy, `toast.*`, empty states, button labels) with `t("results.<key>")` for
    the results surfaces. Add keys under a `results` sub-object in the three `intake.json` catalogs
    (nl verbatim, fr/en drafted — D-12). Use `t("key", { value })` / `<Trans>` for interpolated or
    JSX-wrapping strings. Where a failure toast surfaces a backend error, use the error-code map
    with raw fallback (`resolveErrorKey(code)` → `t(...)` else raw).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - No hardcoded Dutch stopword remains in ResearchResultsPanel.tsx or intake.$id.results.tsx (grep both → none outside comments)
    - New `t("results.*")` keys exist in nl/fr/en intake.json
  </acceptance_criteria>
  <done>The densest results surfaces render in the active locale.</done>
</task>

<task type="auto">
  <name>Task 2: Externalize AI/artifact components + three date-locale swaps</name>
  <files>frontend/src/components/intake/AIReviewPanel.tsx, frontend/src/components/intake/AISkillsPanel.tsx, frontend/src/components/intake/NextStepBanner.tsx, frontend/src/components/intake/ResearchArtifacts.tsx, frontend/src/components/intake/RecipientPicker.tsx, frontend/src/components/intake/HandoffBlock.tsx, frontend/src/components/intake/FinalReportBlock.tsx, frontend/src/components/intake/ContextPackBlock.tsx, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/intake.json</files>
  <read_first>
    - frontend/src/components/intake/AIReviewPanel.tsx, AISkillsPanel.tsx, RecipientPicker.tsx, HandoffBlock.tsx, FinalReportBlock.tsx (Dutch strings per the density map)
    - frontend/src/components/intake/NextStepBanner.tsx, ResearchArtifacts.tsx, ContextPackBlock.tsx (Dutch strings + the date-fns `nl` import call sites)
    - frontend/src/lib/i18n/date-locale.ts (getDateLocale)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (Shared Pattern; date-fns call-site swap)
  </read_first>
  <action>
    In each file add `useTranslation("intake")` and replace hardcoded Dutch literals with
    `t("<sub>.<key>")` using a per-component sub-object (e.g. `aiReview.*`, `artifacts.*`,
    `nextStep.*`, `recipients.*`, `handoff.*`, `finalReport.*`, `aiSkills.*`, `contextPack.*`) in
    the three `intake.json` catalogs (nl verbatim, fr/en drafted). Use `<Trans>` for JSX-wrapping
    strings.

    Swap the three date-fns call sites (`NextStepBanner.tsx`, `ResearchArtifacts.tsx`,
    `ContextPackBlock.tsx`): replace `import { nl } from "date-fns/locale"` +
    `format(..., { locale: nl })` with `getDateLocale(i18n.language)` (D-04).

    Where failure toasts surface backend errors, use the error-code map with raw fallback.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - No hardcoded Dutch stopword remains in the 8 components (grep each → none outside comments)
    - NextStepBanner.tsx, ResearchArtifacts.tsx, ContextPackBlock.tsx import `getDateLocale` and no longer import `{ nl }` from date-fns/locale
    - New `t("...")` keys exist in nl/fr/en intake.json
  </acceptance_criteria>
  <done>AI/artifact components externalized; three date-locale sites swapped.</done>
</task>

<task type="auto">
  <name>Task 3: PDF label externalization via pre-resolved props (Pitfall 3)</name>
  <files>frontend/src/components/intake/ContextPackPDF.tsx, frontend/src/components/intake/NestorBriefingPDF.tsx, frontend/src/locales/nl/intake.json, frontend/src/locales/fr/intake.json, frontend/src/locales/en/intake.json</files>
  <read_first>
    - frontend/src/components/intake/ContextPackPDF.tsx (the pdf().toBlob() call at :305 — labels currently hardcoded Dutch)
    - frontend/src/components/intake/NestorBriefingPDF.tsx (the pdf().toBlob() call at :321)
    - frontend/src/components/intake/ContextPackBlock.tsx (the calling component that triggers the PDF — where t()/getFixedT resolution happens; note: ContextPackBlock is also edited in Task 2 — keep the PDF-props change consistent)
    - .planning/phases/11-internationalization-nl-fr-en/11-PATTERNS.md (Shared Pattern: PDF EXCEPTION)
    - .planning/phases/11-internationalization-nl-fr-en/11-RESEARCH.md (Pitfall 3)
  </read_first>
  <action>
    Add a `labels` prop (a typed object of the display strings the PDF renders) to `ContextPackPDF`
    and `NestorBriefingPDF`, and render those props instead of hardcoded Dutch. At the CALLING site
    (inside the provider — ContextPackBlock and wherever NestorBriefingPDF is invoked), build the
    `labels` object with `t("pdf.<key>")` or `i18n.getFixedT(i18n.language, "intake")` and pass it
    into the component before `pdf(<Component labels={labels} .../>).toBlob()`. Add the `pdf.*`
    keys to the three `intake.json` catalogs (nl verbatim, fr/en drafted). Do NOT call
    `useTranslation()` inside the PDF components — they render OUTSIDE the provider and would use
    defaults/throw (Pitfall 3).

    NOTE: ContextPackBlock.tsx is also modified in Task 2 (date-locale + chrome strings). Ensure the
    PDF-props wiring here is coherent with that edit — the same component both externalizes its own
    chrome and builds the PDF `labels`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes
    - ContextPackPDF.tsx and NestorBriefingPDF.tsx accept a `labels` prop and render no hardcoded Dutch stopword (grep → none outside comments)
    - Neither PDF component calls `useTranslation` (grep both → no `useTranslation`)
    - The calling site builds `labels` via `t(...)`/`getFixedT` and passes it into the `pdf(...)` call
    - New `t("pdf.*")` keys exist in nl/fr/en intake.json
  </acceptance_criteria>
  <done>PDF exports render labels in the active language via pre-resolved props; no in-PDF hook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog value → React render / PDF | Translated strings interpolated into the DOM and detached PDF tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering (XSS) | intake catalog interpolation | mitigate | React auto-escapes; @react-pdf renders text nodes (no HTML injection); never dangerouslySetInnerHTML a catalog value (Tasks 1-3) |
| T-11-05 | Info Disclosure | results/AI error toasts | mitigate | Error-code map surfaces curated codes; raw fallback carries only existing server detail, no new leak (Tasks 1-2) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- CI Dutch guard passes for this component set (partial — full scan at phase gate)
- No `useTranslation` inside the PDF components; PDFs receive resolved labels
</verification>

<success_criteria>
- ResearchResultsPanel + results route + 8 AI/artifact components externalized (NL/FR/EN)
- Both PDF exporters render labels via pre-resolved props (Pitfall 3)
- Three intake-component date-locale sites use getDateLocale
</success_criteria>

<output>
Create `.planning/phases/11-internationalization-nl-fr-en/11-05-SUMMARY.md` when done
</output>
