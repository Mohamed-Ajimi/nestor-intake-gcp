---
phase: 17-raw-output-audit-chain-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/db/models/research_runs.py
  - backend/app/db/alembic/versions/0012_research_run_chain_bundle.py
  - backend/app/db/stream_session.py
  - backend/app/research/tribunal_client.py
  - tribunal/nestor_pulse_sdk/runs/api.py
  - backend/tests/conftest.py
  - backend/tests/test_research_runs_migration.py
  - tribunal/nestor_pulse_sdk/tests/test_research_bundle_endpoint.py
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "research_runs carries chain_status / chain_broken_at / bundle_key columns (nullable) on the live schema"
    - "A completed Tribunal run exposes its scrubbed cleaned_reports over a new internal seam endpoint"
    - "The intake seam client can fetch the research bundle and run verify_chain over HTTP"
    - "The SSE frame carries the chain/lock state so the frontend can render it"
  artifacts:
    - path: "backend/app/db/alembic/versions/0012_research_run_chain_bundle.py"
      provides: "Migration 0012 adding chain_status, chain_broken_at, bundle_key (nullable)"
      contains: "down_revision"
    - path: "tribunal/nestor_pulse_sdk/runs/api.py"
      provides: "GET /{run_id}/research-bundle returning {cleaned_reports}"
      contains: "research-bundle"
    - path: "backend/app/research/tribunal_client.py"
      provides: "get_research_bundle + verify_chain seam methods"
      contains: "def verify_chain"
  key_links:
    - from: "backend/app/research/tribunal_client.py"
      to: "tribunal /api/runs/{run_id}/research-bundle"
      via: "httpx.get with _headers"
      pattern: "research-bundle"
    - from: "backend/app/db/stream_session.py"
      to: "research_runs.chain_status"
      via: "read_latest_research_run_dict dict"
      pattern: "chain_status"
---

<objective>
Lay the Phase-17 contracts and test fakes that Plans 02–03 build against: the `research_runs`
chain/lock/bundle columns (model + migration 0012), the SSE dict extension that carries the lock
state to the browser, the new Tribunal `GET /{run_id}/research-bundle` seam endpoint that serves the
scrubbed `cleaned_reports` (D-01), the two new intake seam-client methods (`get_research_bundle`,
`verify_chain`), and the extended `fake_tribunal_client` fixture (Wave 0 test scaffolding).

Purpose: interface-first — the completion-path build (Plan 02) and the download/re-verify routes
(Plan 03) both consume these columns, seam methods, and fakes. Landing them first means downstream
executors receive the contracts in their plan text instead of discovering them.
Output: migration 0012 + 3 nullable columns; a new read-only Tribunal endpoint; two seam methods; the
SSE dict fields; extended fixture; migration + endpoint tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-raw-output-audit-chain-guard/17-CONTEXT.md
@.planning/phases/17-raw-output-audit-chain-guard/17-RESEARCH.md
@.planning/phases/17-raw-output-audit-chain-guard/17-PATTERNS.md

<interfaces>
<!-- Contracts extracted from the codebase this session — use directly, no exploration needed. -->

Existing seam-method shape to clone (backend/app/research/tribunal_client.py:211-233, get_report):
- keyword-only args: service_url, space_id, acting_user_id, acting_email, run_id
- headers via `_headers(service_url, space_id, acting_user_id, acting_email)` (OIDC + tenant + acting-user)
- `httpx.get(f"{service_url}/api/...", headers=..., timeout=_TIMEOUT_S)` → `raise_for_status()` → `return resp.json()`

Tribunal run-lookup + synthesis_cache read (tribunal/nestor_pulse_sdk/runs/api.py — get_run_report @852, _latest reads):
- `run = (await session.execute(select(Run).where(Run.id == run_id))).scalar_one_or_none()`; None → HTTPException(404); status != "completed" → HTTPException(409)
- synthesis_cache row: `select(Output.body).where(Output.run_id == run_id, Output.format == "synthesis_cache").order_by(Output.created_at.desc()).limit(1)`
- the synthesis_cache body is `json.dumps({mission_brief, cleaned_reports, contested_notes, rejected_claims, verification})`; `cleaned_reports` is a list of `[provider_name, {report: ...}]`

Existing nullable-column pattern (backend/app/db/models/research_runs.py:82-86):
- `error_message: Mapped[str | None] = mapped_column(Text, nullable=True)`
- `output_markdown: Mapped[str | None] = mapped_column(Text, nullable=True)`

SSE dict keys already emitted (backend/app/db/stream_session.py:105-116): id, status, current_stage, stage_detail, cost_usd_total, started_at, completed_at, error_message.

fake_tribunal_client capture dict (backend/tests/conftest.py:859-902): create_run / get_metrics_calls / metrics_script; patches create_run, get_metrics, get_report, ensure_org, ensure_project with raising=False.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: research_runs chain/lock/bundle columns + migration 0012</name>
  <files>backend/app/db/models/research_runs.py, backend/app/db/alembic/versions/0012_research_run_chain_bundle.py, backend/tests/test_research_runs_migration.py</files>
  <read_first>
    - backend/app/db/models/research_runs.py (the model to extend; nullable-column pattern at lines 82-86; index shape at 97-101)
    - backend/app/db/alembic/versions/0011_research_runs.py (the migration analog — revision/down_revision/op.create_table shape, module docstring, RLS/policy discipline)
    - backend/tests/test_research_runs_migration.py (the assertion pattern to extend — how it checks columns/indexes exist)
  </read_first>
  <behavior>
    - Migration 0012 has revision="0012", down_revision="0011"; upgrade() adds exactly three nullable columns and downgrade() drops them.
    - After migrate, research_runs has chain_status (text/varchar, nullable), chain_broken_at (integer, nullable), bundle_key (text/varchar, nullable).
    - Migration adds NO new RLS policy, grant, or index (the columns inherit research_runs' existing row-level policy from 0011).
    - test_research_runs_migration.py asserts the three new columns exist with nullable=True.
  </behavior>
  <action>
    Add three NULLABLE columns to the ResearchRun model mirroring the existing `error_message` / `output_markdown` nullable pattern: `chain_status: Mapped[str | None]` (String, nullable=True — holds "verified" | "broken", NULL until the completion path runs), `chain_broken_at: Mapped[int | None]` (Integer, nullable=True — the broken row index verify_chain returns, NULL when verified/unrun), `bundle_key: Mapped[str | None]` (String, nullable=True — the GCS object key of the materialized zip, NULL until built). All three MUST be nullable so pre-existing live rows (smoke intake e08620c5 has 3) do not break. Do NOT add a server_default (leave NULL; the completion path sets the value).

    Create migration `0012_research_run_chain_bundle.py` cloning `0011_research_runs.py`'s structure: `revision = "0012"`, `down_revision = "0011"`, a module docstring explaining the add-column purpose. `upgrade()` calls `op.add_column("research_runs", sa.Column("chain_status", sa.String(), nullable=True), schema="nestor")` (match the schema qualifier 0011 uses — confirm from 0011) for each of the three columns. `downgrade()` calls `op.drop_column(...)` for each, reverse order. The migration MUST NOT touch RLS policies, grants, or indexes — the new columns inherit research_runs' existing FORCE-RLS row policy.

    Extend `test_research_runs_migration.py` to assert the three new columns exist and are nullable (mirror however the existing test inspects columns — information_schema query or SQLAlchemy inspector). Keep `alembic check` clean (model and migration agree).
  </action>
  <verify>
    <automated>MISSING — runs in Cloud Build at wave boundary: pytest backend/tests/test_research_runs_migration.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/db/models/research_runs.py` declares `chain_status`, `chain_broken_at`, `bundle_key` all with `nullable=True` and no server_default.
    - `0012_research_run_chain_bundle.py` has `revision = "0012"` and `down_revision = "0011"`; upgrade adds exactly 3 columns; downgrade drops exactly 3 columns.
    - The migration file contains no `op.create_index`, `CREATE POLICY`, or `GRANT`.
    - `test_research_runs_migration.py` contains assertions naming `chain_status`, `chain_broken_at`, and `bundle_key`.
  </acceptance_criteria>
  <done>Model + migration 0012 add the three nullable columns; migration test asserts their presence; alembic check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Tribunal /research-bundle seam endpoint (serves cleaned_reports, D-01)</name>
  <files>tribunal/nestor_pulse_sdk/runs/api.py, tribunal/nestor_pulse_sdk/tests/test_research_bundle_endpoint.py</files>
  <read_first>
    - tribunal/nestor_pulse_sdk/runs/api.py (get_run_report @852-924 for the run-lookup + 404/409 gate + Depends(get_db_session); the _latest synthesis_cache read pattern; the existing router + imports)
    - tribunal/nestor_pulse_sdk/pipeline/tribunal/pipeline.py (the synthesis_cache Output write — confirm the cleaned_reports shape is a list of [provider_name, {report: ...}] and rejected_claims is a SEPARATE key in the same body)
    - tribunal/nestor_pulse_sdk/db/models/output.py (Output model — format, body/gcs_uri fields)
  </read_first>
  <action>
    Add a new read-only endpoint `GET /{run_id}/research-bundle` on the existing runs router, cloning the `get_run_report` gate discipline. Signature: `async def get_run_research_bundle(run_id: uuid.UUID, session: AsyncSession = Depends(get_db_session))` — use the SAME `Depends(get_db_session)` tenant-scoping every other runs endpoint uses (SET LOCAL app.tenant_id from the seam's X-Nestor-Tenant-Id, RLS-scopes the Output read). Match whether `get_run_report` also depends on `get_current_user` and mirror it.

    Gate: load the Run by id; None → `HTTPException(404, "run not found")`; `run.status != "completed"` → `HTTPException(409, "bundle not available yet")`. Read the latest synthesis_cache Output body with `select(Output.body).where(Output.run_id == run_id, Output.format == "synthesis_cache").order_by(Output.created_at.desc()).limit(1)`; if absent → `HTTPException(409, "no cached research for this run")`. Parse the body JSON and return EXACTLY `{"cleaned_reports": bundle.get("cleaned_reports") or []}`.

    D-01 CRITICAL: return ONLY `cleaned_reports`. The synthesis_cache body ALSO contains `rejected_claims` — it is DELIBERATELY EXCLUDED from the response (the download never exposes discredited content). Do NOT add rejected_claims, contested_notes, or verification to the return dict.

    This endpoint READS only — it must NOT write any Output row, must NOT touch the audit chain, and must NOT alter the frozen canonical_json payload (14 D-05).

    Create `tribunal/nestor_pulse_sdk/tests/test_research_bundle_endpoint.py` covering: (a) a completed run with a synthesis_cache row returns `{cleaned_reports: [...]}` and the response contains NO `rejected_claims` key; (b) a non-completed run → 409; (c) a completed run with no synthesis_cache row → 409; (d) a run_id in a different tenant is not visible (RLS-scoped — 404 or empty per the suite's tenant-fixture convention). Follow the existing Tribunal runs/api test setup.
  </action>
  <verify>
    <automated>MISSING — runs in Cloud Build at wave boundary: pytest tribunal/nestor_pulse_sdk/tests/test_research_bundle_endpoint.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `runs/api.py` defines a route decorated `@router.get("/{run_id}/research-bundle")`.
    - The handler returns a dict whose ONLY key is `cleaned_reports`; grep of the handler body shows no `rejected_claims`, `contested_notes`, or `verification` in the return statement.
    - The handler raises 404 (run absent), 409 (not completed), and 409 (no synthesis_cache) on those paths.
    - `test_research_bundle_endpoint.py` asserts `"rejected_claims" not in <response body>` on the happy path.
  </acceptance_criteria>
  <done>The new endpoint serves cleaned_reports only, gated to completed runs, RLS-scoped, with tests proving rejected_claims never leak.</done>
</task>

<task type="auto">
  <name>Task 3: seam-client get_research_bundle + verify_chain; SSE dict + fixture extensions</name>
  <files>backend/app/research/tribunal_client.py, backend/app/db/stream_session.py, backend/tests/conftest.py</files>
  <read_first>
    - backend/app/research/tribunal_client.py (get_report @211-233 and get_metrics @185-208 — the exact keyword-only + _headers + httpx.get + raise_for_status + return resp.json() shape to clone; the SEAM SCOPE docstring discipline "this module persists NOTHING")
    - backend/app/db/stream_session.py (read_latest_research_run_dict @83-116 — the dict whose keys ARE the wire shape; extend it)
    - backend/tests/conftest.py (fake_tribunal_client @829-902 — the capture dict + the raising=False patch loop to extend)
  </read_first>
  <action>
    In `tribunal_client.py`, add two keyword-only seam methods cloned verbatim from `get_report`'s shape (reuse `_headers` / `_mint_id_token` — NO new OIDC code, audience stays the path-less service_url per Pitfall 4):
    - `get_research_bundle(*, service_url, space_id, acting_user_id, acting_email, run_id) -> dict`: `httpx.get(f"{service_url}/api/runs/{run_id}/research-bundle", headers=_headers(...), timeout=_TIMEOUT_S)` → `raise_for_status()` → `return resp.json()`. Returns `{cleaned_reports: [[name, {report:...}], ...]}`.
    - `verify_chain(*, service_url, space_id, acting_user_id, acting_email, run_id) -> dict`: `httpx.get(f"{service_url}/api/audit/verify/{run_id}", headers=_headers(...), timeout=_TIMEOUT_S)` → `raise_for_status()` → `return resp.json()`. Returns `{ok: bool, broken_at: int | None}`.
    Both persist NOTHING (keep the SEAM SCOPE docstring note). Note the empty-chain trap in the verify_chain docstring: `ok:true` on zero visible audit rows is trivially valid — the correct tenant header (already sent by the driver) is what makes the verdict meaningful.

    In `stream_session.py::read_latest_research_run_dict`, add three keys to the returned dict so the SSE frame carries the lock state: `"chain_status": run.chain_status`, `"chain_broken_at": run.chain_broken_at`, `"bundle_key": run.bundle_key`. Place them alongside the existing keys; keep the D-05 verbatim-status discipline unchanged.

    In `conftest.py::fake_tribunal_client`, extend the capture dict and patch loop: add `get_research_bundle` fake returning `{"cleaned_reports": [["angle-a", {"report": "x"}]]}` and `verify_chain` fake returning a configurable verdict — default `{"ok": True, "broken_at": None}`, overridable via a new capture key `capture["verify_verdict"]` so a broken-chain test can set `{"ok": False, "broken_at": 3}` before driving the poll. Patch both with `raising=False` (they land in this task, but keep the fixture's forward-compatible discipline). Record calls if the existing fixture records other calls (mirror the pattern).
  </action>
  <verify>
    <automated>MISSING — runs in Cloud Build at wave boundary: pytest backend/tests/test_research_run_task.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tribunal_client.py` defines `def get_research_bundle(` and `def verify_chain(`, both keyword-only, both calling `_headers(` and `raise_for_status()`.
    - `verify_chain` targets URL path `/api/audit/verify/`; `get_research_bundle` targets `/api/runs/` + `/research-bundle`.
    - `read_latest_research_run_dict` returns a dict containing keys `chain_status`, `chain_broken_at`, and `bundle_key`.
    - `fake_tribunal_client` capture dict exposes `verify_verdict` and both new fakes are registered in the patch loop.
  </acceptance_criteria>
  <done>Two seam methods added (persist nothing); SSE dict carries the lock state; the fixture drives both verified and broken verdicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intake backend → Tribunal API (HTTP seam) | The new `/research-bundle` + `verify_chain` calls cross the internal-auth seam (Phase-14 OIDC, X-Nestor-Tenant-Id) |
| Tribunal API → tribunal DB (RLS) | The synthesis_cache read is RLS-scoped by the seam's tenant header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | `/research-bundle` returning rejected_claims | mitigate | Return ONLY `cleaned_reports`; endpoint test asserts `rejected_claims not in body` (D-01) |
| T-17-02 | Information Disclosure | cross-tenant read of another org's synthesis_cache | mitigate | `Depends(get_db_session)` RLS-scopes the Output read by the seam's X-Nestor-Tenant-Id (Phase-14 discipline); endpoint test covers tenant isolation |
| T-17-03 | Tampering | new endpoint mutating the frozen audit chain | accept | Endpoint is READ-only — no Output write, no audit-chain touch; canonical_json payload untouched (14 D-05) |
| T-17-04 | Spoofing | unauthenticated caller reaching the new endpoint | mitigate | Endpoint inherits the runs router's `get_db_session`/`get_current_user` deps (internal-auth, IAM invoker = intake SA) — mirror `get_run_report` |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this phase installs NO new packages (17-RESEARCH Package Legitimacy Audit = N/A); all stdlib + already-in-image deps |
</threat_model>

<verification>
- Migration 0012 adds three nullable columns; `alembic check` clean.
- The Tribunal `/research-bundle` endpoint returns cleaned_reports only, 409 on non-completed / no-cache, RLS-scoped.
- The two seam methods target the correct URLs and reuse `_headers`.
- The SSE dict and fixture carry the new lock-state fields.
- Cloud Build (wave boundary): `pytest backend/tests/test_research_runs_migration.py tribunal/nestor_pulse_sdk/tests/test_research_bundle_endpoint.py -x`.
</verification>

<success_criteria>
- research_runs has chain_status / chain_broken_at / bundle_key (nullable) after migration 0012.
- A completed run's scrubbed cleaned_reports are reachable over a new internal seam endpoint, with rejected_claims proven excluded.
- The intake seam client can call get_research_bundle + verify_chain.
- Downstream plans have every contract + fake they need.
</success_criteria>

<output>
Create `.planning/phases/17-raw-output-audit-chain-guard/17-01-SUMMARY.md` when done
</output>
