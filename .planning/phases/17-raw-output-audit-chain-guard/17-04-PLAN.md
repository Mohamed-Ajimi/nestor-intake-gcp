---
phase: 17-raw-output-audit-chain-guard
plan: 04
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
  - .planning/phases/17-raw-output-audit-chain-guard/17-HUMAN-UAT.md
autonomous: false
requirements: [RUN-03]

must_haves:
  truths:
    - "The runbook has a Phase 17 section: REBUILD tribunal-api image (new /research-bundle endpoint), REBUILD nestor-api image (new bundle/route/model code), run migration 0012 via the nestor migration Job, in that order"
    - "The operator downloads the raw-output zip from a real completed run and confirms its contents (report.md + research/*.md + sources.json, no rejected claims)"
    - "verify_chain shows green as a hard gate on the completion path for the live run"
    - "A client login can never see or reach the raw-output download"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "Phase 17 deploy + live-UAT section"
      contains: "## Phase 17"
    - path: ".planning/phases/17-raw-output-audit-chain-guard/17-HUMAN-UAT.md"
      provides: "operator live-session checklist for the download + verify_chain proof"
      contains: "download"
  key_links:
    - from: "infra/DEPLOY-RUNBOOK.md Phase 17 section"
      to: "tribunal-api /research-bundle endpoint + nestor-api routes + migration 0012"
      via: "REBUILD both images (not env flip) + migrate — ordered tribunal-api first"
      pattern: "REBUILD"
    - from: "17 live download proof"
      to: "17-HUMAN-UAT.md"
      via: "records zip contents + verify_chain green"
      pattern: "verify_chain"
---

<objective>
Author the Phase 17 deploy runbook section and the operator live-UAT checklist, then run the operator
live session: REBUILD + deploy the `tribunal-api` image (the new `GET /{run_id}/research-bundle`
endpoint MUST ship before the intake finalize path calls it), REBUILD + deploy the `nestor-api` image
(new `bundle.py`, the extended `run_task.py`/`research_routes.py`, the migration, the frontend), run
migration 0012 via the `nestor` migration Job, then prove the raw-output download end-to-end on a real
completed run: verify_chain green, the zip downloads and contains the D-03 layout, and a client login
can never reach it.

Purpose: nothing is real until deployed. The recurring deploy-gap is a config-only env flip on a stale
image — this plan forces BOTH image REBUILDs in the correct order (tribunal-api first, because the
intake finalize path calls its new endpoint). The live proof is blocked on Anthropic credits (same
Phase-16 blocker), so it is an operator-runbook checkpoint, same pattern as 16-05.
Output: the runbook section, `17-HUMAN-UAT.md`, and the recorded download + verify_chain proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-raw-output-audit-chain-guard/17-CONTEXT.md
@.planning/phases/17-raw-output-audit-chain-guard/17-RESEARCH.md
@.planning/phases/17-raw-output-audit-chain-guard/17-VALIDATION.md
@.planning/phases/16-research-trigger-progress-bridge/16-05-PLAN.md

<interfaces>
<!-- Live-config facts + deploy conventions extracted this session. -->

DEPLOY GAPS + FACTS:
- The latest runbook section is "## Phase 16" (added by 16-05). Add "## Phase 17" after it.
- TWO image REBUILDs, ORDERED:
  1. tribunal-api — new `GET /{run_id}/research-bundle` endpoint (Plan 01, Task 2) must ship FIRST,
     because the intake finalize path (Plan 02) calls it. tribunal-api current rev per MEMORY/16 notes
     was 00007-vsf. Cloud Build image only (dev box has no local Python/Docker).
  2. nestor-api — new modules `app/research/bundle.py`, extended `run_task.py`/`research_routes.py`,
     `research_runs` model columns, frontend `research.ts`/`ResearchRunProgress.tsx`, migration 0012.
  A config-only env flip on a stale image is the recurring deploy-gap (DEPLOY-RUNBOOK, MEMORY
  phase-10/phase-06/phase-16). Both REBUILD via Cloud Build.
- Migration 0012: run the existing `nestor` migration Job (alembic upgrade head) — same pattern as
  0009/0010/0011. Adds three nullable columns to research_runs (chain_status, chain_broken_at, bundle_key).
- NO new secret or env var (17-RESEARCH Runtime State Inventory): reuses STORAGE_BUCKET (Phase-9, set)
  and tribunal_service_url (Phase-14/16, set). No AUDIT_GCS_BUCKET (D-05: app bucket, not audit bucket).
- Anthropic credits are the LIVE blocker (Nestor_Claude2 empty, Phase-16 UAT still parked). The operator
  tops up + completes the deferred Phase-16 run FIRST, then this download proof rides on that completed run.
- No frontend deploy gap beyond the standard frontend image rebuild (per Phase-12 frontend deploy runbook).

LIVE PROOF (from 17-VALIDATION.md Manual-Only Verifications):
  1. Live bundle download from a real completed run — click download on the summary card, verify the
     zip contains report.md, research/*.md, sources.json (and NO rejected claims).
  2. verify_chain green on a real run's audit chain — the summary card shows verified; optionally
     tamper-test in a scratch tenant to see the locked state + Re-verify.
  Plus the standing isolation check: a client login sees NO raw-output download anywhere.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Phase 17 runbook section + 17-HUMAN-UAT checklist</name>
  <files>infra/DEPLOY-RUNBOOK.md, .planning/phases/17-raw-output-audit-chain-guard/17-HUMAN-UAT.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 16 (the 16.a–16.f step format, the REBUILD-not-env-flip gap callout, the migration-Job step) + § Phase 13 (the tribunal-api Cloud Build image + deploy steps to mirror for the tribunal-api rebuild)
    - .planning/phases/16-research-trigger-progress-bridge/16-HUMAN-UAT.md (the frontmatter + Tests/Summary/Gaps format to mirror) — if absent, mirror 14-HUMAN-UAT.md
    - .planning/phases/17-raw-output-audit-chain-guard/17-VALIDATION.md § Manual-Only Verifications (the two live checks)
  </read_first>
  <action>
    Append a `## Phase 17 — Raw output + audit chain guard` section to `infra/DEPLOY-RUNBOOK.md` after
    the Phase 16 section, with ORDERED numbered steps:
    - (17.a) REBUILD + deploy the `tribunal-api` image via Cloud Build FIRST — the new
      `GET /{run_id}/research-bundle` endpoint must be live before the intake finalize path calls it.
      Explicitly call out REBUILD-not-env-flip (the recurring deploy-gap). Note the tribunal-worker image
      is UNCHANGED this phase (no worker rebuild).
    - (17.b) REBUILD + deploy the `nestor-api` image via Cloud Build — new `app/research/bundle.py`,
      the extended `run_task.py`/`research_routes.py`, the `research_runs` model columns, and migration
      0012 must ship.
    - (17.c) run the `nestor` migration Job to apply 0012 (alembic upgrade head); verify the three new
      nullable columns (`chain_status`, `chain_broken_at`, `bundle_key`) exist on `research_runs`.
    - (17.d) deploy the frontend image (download button + locked/re-verify UI) per the standard Phase-12
      frontend deploy steps.
    - (17.e) confirm NO new env/secret is needed — `STORAGE_BUCKET` and `TRIBUNAL_SERVICE_URL` are
      already set (read-only describe); explicitly note NO `AUDIT_GCS_BUCKET` (D-05: the app bucket).
    - (17.f) top up Anthropic credits + complete the parked Phase-16 live run FIRST (that produces the
      completed run this download proof needs), then run the live UAT (points to 17-HUMAN-UAT.md).
    Create `.planning/phases/17-raw-output-audit-chain-guard/17-HUMAN-UAT.md` (mirror the Phase-16 /
    Phase-14 HUMAN-UAT frontmatter + Tests/Summary/Gaps format) with the checks: (1) on the completed
    smoke run, the summary card shows chain state VERIFIED and a Download button; click it → the zip
    downloads → open it and confirm report.md + at least one research/*.md + sources.json are present
    and there is NO rejected-claims file/content; (2) confirm the chain-verified state came from the
    completion-path verify_chain gate (check the run's chain_status is "verified"); optionally, in a
    scratch tenant, tamper the chain to see the locked card + working Re-verify; (3) ISOLATION: log in as
    a CLIENT (user-role) and confirm no raw-output download, no research surface, is visible or reachable
    anywhere (REPORT-02).
  </action>
  <verify>
    <automated>grep -c "## Phase 17" infra/DEPLOY-RUNBOOK.md | grep -qv '^0$' && test -f .planning/phases/17-raw-output-audit-chain-guard/17-HUMAN-UAT.md && echo "runbook+uat present"</automated>
  </verify>
  <acceptance_criteria>
    - `infra/DEPLOY-RUNBOOK.md` contains a `## Phase 17` section with ordered steps 17.a–17.f.
    - Step 17.a rebuilds `tribunal-api` FIRST and calls out REBUILD-not-env-flip; step 17.b rebuilds `nestor-api`.
    - Step 17.c applies migration 0012 and names the three new columns.
    - Step 17.e explicitly states NO new env/secret and NO `AUDIT_GCS_BUCKET` (D-05).
    - `17-HUMAN-UAT.md` exists with the download-contents check, the verify_chain-green check, and the client-isolation check.
  </acceptance_criteria>
  <done>Runbook Phase 17 section + 17-HUMAN-UAT.md authored; ordered dual REBUILD, migration 0012, no-new-secret, and the download/verify_chain/isolation checks all recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    The Phase 17 backend + frontend are code-complete and green in Cloud Build (bundle builder, the
    completion-path verify_chain gate + materialization, the superadmin-only download + re-verify routes,
    the denial suite, the new Tribunal `/research-bundle` endpoint, migration 0012, and the completed-card
    download/locked/re-verify UI). The runbook Phase 17 section and 17-HUMAN-UAT.md are authored. This
    checkpoint is the operator live session: deploy both images (tribunal-api first), migrate, and prove
    the raw-output download + verify_chain gate on a real completed run.
  </what-built>
  <how-to-verify>
    Follow `infra/DEPLOY-RUNBOOK.md` § Phase 17 exactly:
    1. Top up Anthropic credits and complete the parked Phase-16 live run so a real `completed` run
       exists (this download proof rides on it).
    2. REBUILD + deploy the `tribunal-api` image via Cloud Build FIRST (the new `/research-bundle`
       endpoint must be live). Do NOT env-flip a stale image.
    3. REBUILD + deploy the `nestor-api` image via Cloud Build.
    4. Run the `nestor` migration Job (0012); confirm `chain_status`, `chain_broken_at`, `bundle_key`
       exist on `research_runs`.
    5. Deploy the frontend image (download + locked/re-verify UI).
    6. Confirm `STORAGE_BUCKET` + `TRIBUNAL_SERVICE_URL` are set (read-only describe); confirm NO
       `AUDIT_GCS_BUCKET` is used (D-05).
    7. On the completed smoke run's detail page, confirm the summary card shows chain state VERIFIED and
       a Download button. Click Download → confirm the zip downloads → open it and verify it contains
       `report.md`, at least one `research/*.md`, and `sources.json`, and that there is NO rejected-claims
       file or content anywhere in the zip.
    8. Confirm the chain-verified state was produced by the completion-path verify_chain gate (the run's
       `chain_status` is `verified`). Optionally, in a scratch tenant, tamper the audit chain and confirm
       the summary card shows the locked state with a working Re-verify button.
    9. ISOLATION: log in as a CLIENT (user-role) and confirm NO raw-output download and NO research
       surface is visible or reachable anywhere (REPORT-02 absolute rule).
    10. Record the run id, the zip contents, and the verify_chain result in `17-HUMAN-UAT.md`.
    Expected: verify_chain green on the completion path, the zip downloads with the D-03 layout and no
    discredited content, and a client can never reach any of it.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded run id and confirmed zip contents / verify_chain result, or describe issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator gcloud → live Cloud Run services | config + image deploy; must rebuild BOTH images in order, not env-flip a stale image |
| live intake finalize path → tribunal-api /research-bundle | first real call to the new endpoint — must be deployed first |
| client login → intake UI | the isolation wall: a client must see NO raw-output surface (REPORT-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-16 | Tampering | stale-image deploy (new /research-bundle or bundle code not shipped) | mitigate | Runbook 17.a/17.b force Cloud Build REBUILDs in order (tribunal-api first); the recurring deploy-gap is called out explicitly |
| T-17-17 | Information Disclosure | client reaching the raw-output download post-deploy | mitigate | UAT step 9 verifies a client login shows NO raw-output surface (REPORT-02); backed by the Plan-03 denial suite |
| T-17-18 | Tampering (evasion) | exporting on an unverified chain in production | mitigate | UAT step 8 confirms chain_status=verified came from the completion-path gate; broken → locked (D-06) proven in the scratch-tenant tamper test |
| T-17-19 | Information Disclosure | bundle written to the wrong bucket | mitigate | Step 17.e confirms the app bucket (STORAGE_BUCKET), NO AUDIT_GCS_BUCKET (D-05) |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new packages this phase (17-RESEARCH audit N/A); deploy ships existing-dep images only |
</threat_model>

<verification>
- Runbook: `grep -c "## Phase 17" infra/DEPLOY-RUNBOOK.md` ≥ 1; `17-HUMAN-UAT.md` exists.
- Operator checkpoint: verify_chain green on the completion path, the zip downloads with report.md + research/*.md + sources.json (no rejected claims), a client sees no raw-output surface.
- Both images rebuilt (tribunal-api first), migration 0012 applied.
</verification>

<success_criteria>
- `tribunal-api` + `nestor-api` redeployed (ordered) with 0012 applied and the new code live.
- RUN-03 SC1 proven live: superadmin downloads the full raw research output as a file with the D-03 layout.
- RUN-03 SC3 proven live: verify_chain runs as a hard gate on the completion path; broken → locked.
- REPORT-02 confirmed: a client can never access the raw output.
</success_criteria>

<output>
Create `.planning/phases/17-raw-output-audit-chain-guard/17-04-SUMMARY.md` when done.
</output>
