---
phase: 17-raw-output-audit-chain-guard
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - backend/app/api/research_routes.py
  - backend/tests/test_research_cross_tenant.py
  - backend/tests/test_research_bundle_download.py
  - frontend/src/lib/api/research.ts
  - frontend/src/components/intake/ResearchRunProgress.tsx
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "A superadmin can mint a signed download URL for a verified completed run's bundle"
    - "A client (user-role) or a cross-space caller can NEVER reach the download — existence-hidden 404"
    - "A broken-chain run's summary card shows a locked state with a working Re-verify action"
    - "If the completion-path build never ran, the download builds the bundle lazily (driver-death recovery)"
  artifacts:
    - path: "backend/app/api/research_routes.py"
      provides: "GET bundle-url + POST verify-chain routes (superadmin-only, space-scoped)"
      contains: "bundle-url"
    - path: "frontend/src/components/intake/ResearchRunProgress.tsx"
      provides: "Download button + chain-locked state + Re-verify action on the completed card"
      contains: "Download"
    - path: "backend/tests/test_research_cross_tenant.py"
      provides: "denial tests for both new routes"
      contains: "bundle"
  key_links:
    - from: "frontend/src/components/intake/ResearchRunProgress.tsx"
      to: "/intakes/{id}/research/{run}/bundle-url"
      via: "getBundleUrl → navigate to signed URL"
      pattern: "getBundleUrl"
    - from: "backend/app/api/research_routes.py"
      to: "gcs.signed_download_url"
      via: "space-scoped run lookup → signed URL"
      pattern: "signed_download_url"
---

<objective>
Add the superadmin-only, space-scoped download endpoint (mints a signed URL for the verified run's
bundle, with build-on-download-if-missing recovery) and the re-verify endpoint (re-runs verify_chain,
lifts the lock on a now-passing chain). Extend the cross-tenant denial suite to cover both routes from
day one. Surface the download button + the chain-locked/re-verify state on the Phase-16 completion
summary card (16 D-09 anchor) — admin route only, never client-visible (REPORT-02).

Purpose: delivers RUN-03 SC1 (superadmin download) + SC2 (client/cross-space denial). This is the
only client-facing-adjacent surface — the isolation wall is the point.
Output: two intake routes + denial tests + download happy-path/recovery test + frontend transport +
completed-card UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-raw-output-audit-chain-guard/17-CONTEXT.md
@.planning/phases/17-raw-output-audit-chain-guard/17-RESEARCH.md
@.planning/phases/17-raw-output-audit-chain-guard/17-PATTERNS.md

<interfaces>
<!-- Contracts from Plans 01-02 + existing seams. -->

research_runs columns (Plan 01): chain_status (str|None: "verified"|"broken"), chain_broken_at (int|None), bundle_key (str|None).
SSE dict (Plan 01, read_latest_research_run_dict) already carries chain_status / chain_broken_at / bundle_key.

Bundle builder (Plan 02): `build_bundle_zip(report, bundle, sources) -> bytes` in backend/app/research/bundle.py — reused by the download route's build-on-download-if-missing path.
Seam methods (Plan 01): get_report, get_research_bundle, verify_chain (all keyword-only: service_url, space_id, acting_user_id, acting_email, run_id).

GCS (backend/app/storage/gcs.py):
- `signed_download_url(key, *, ttl_seconds, filename, content_type=None) -> str` — clamps TTL ≤900s, forces attachment disposition, keyless V4.
- `_clamp_ttl(ttl_seconds) -> int` — the advertised effective TTL.
- `upload_object(key, data, content_type=None)`.

Route discipline (backend/app/api/research_routes.py — the file to extend):
- research_router = APIRouter(prefix="/intakes"); mounted UNDER protected_router (inherits get_current_identity); sync `def` handlers (pg8000), NEVER async (except the one SSE handler).
- `repo: IntakeRepository = Depends(get_tenant_repo)` — superadmin → bypass engine, user → space-scoped engine.
- `identity: Identity = Depends(get_current_identity)`.
- Existence-hidden 404 pattern: `intake = repo.get(intake_id); if intake is None: raise HTTPException(404, "Intake not found")`.
- `ResearchRunRepository(repo.session, identity).get(run_id)` — the scoped run lookup.
- Audit-in-tx: `tenant_session(identity)` + `audit.log(txs, ...)` if audit-logging the re-verify.
- Identity.role: "superadmin" | "user".

Storage signed-url response convention (backend/app/api/storage_routes.py::create_signed_url):
- returns `{url, expires_in}`; `_normalize_intake_id` (malformed → 404).

Frontend transport (frontend/src/lib/api/research.ts):
- `triggerResearch(intakeId)` = `apiFetch(...)` one-shot (never fork the transport); returns ApiResult (return-no-throw).
- `ResearchRun` type @49-58 — extend with chain_status/chain_broken_at/bundle_key.
- The SSE `ResearchRun` frame already carries the new fields once the backend dict emits them.

Frontend component (frontend/src/components/intake/ResearchRunProgress.tsx):
- Completed summary card @184-210 (green left-border #DFF940, CheckCircle2). Failed card @213-258 (red #DC2626, XCircle, onRetry button @248-256).
- Uses `useTranslation("intake")` + `t(...)`; component lives ONLY on the admin detail route (never a client route — T-16-12).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: bundle-url download + verify-chain routes (superadmin-only, space-scoped)</name>
  <files>backend/app/api/research_routes.py</files>
  <read_first>
    - backend/app/api/research_routes.py (the WHOLE file — router setup, trigger_research @108-240 for the existence-hidden 404 + tenant_session + audit-in-tx patterns, the sync-def discipline)
    - backend/app/api/storage_routes.py (create_signed_url — the {url, expires_in} shape, _clamp_ttl usage, existence-hidden 404, _normalize_intake_id)
    - backend/app/storage/gcs.py (signed_download_url + _clamp_ttl + upload_object signatures)
    - backend/app/storage/keys.py (build_object_key — "artifacts" category)
    - backend/app/research/bundle.py (build_bundle_zip — for the lazy-rebuild path; from Plan 02)
    - backend/app/db/repository.py (ResearchRunRepository.get / list_for_intake — the scoped run lookup)
  </read_first>
  <action>
    Add two sync-`def` handlers to `research_router` (inherits get_current_identity via protected_router — do NOT add auth deps of your own; do NOT make them async).

    ROUTE A — `@research_router.get("/{intake_id}/research/{run_id}/bundle-url")` → `get_bundle_url(intake_id, run_id, repo=Depends(get_tenant_repo), identity=Depends(get_current_identity)) -> dict`:
    - Superadmin gate (defense-in-depth, Open Q2): `if identity.role != "superadmin": raise HTTPException(404, "Intake not found")` — EXISTENCE-HIDDEN 404 (NOT 403), because clients are all user-role and RUN-03 says they can NEVER access it (Pitfall 5).
    - Existence-hidden intake lookup: `intake = repo.get(intake_id); if intake is None: raise HTTPException(404, "Intake not found")`.
    - Scoped run lookup: `run = ResearchRunRepository(repo.session, identity).get(run_id); if run is None or str(run.intake_id) != str(intake_id): raise HTTPException(404, "Run not found")`.
    - Availability gate (D-06/D-09): `if run.status != "completed" or run.chain_status != "verified": raise HTTPException(409, "Raw output is not available")`.
    - `key = run.bundle_key`. If `key is None` (driver-death recovery, Pattern 2): re-run the fetch+build+upload in a helper `_build_and_store_bundle(...)` — call get_report + get_research_bundle via the seam (service_url from settings, space_id = str(intake.space_id), acting_user_id/email from identity), build via `build_bundle_zip`, upload to the deterministic key `build_object_key(str(intake.space_id), str(intake_id), "artifacts", f"raw-output-{run_id}.zip")`, then patch `research_runs.bundle_key` in a fresh `tenant_session`. The helper must run its seam+GCS I/O with NO DB connection held (mirror Plan 02's pool-safety contract), opening a session only to patch. Then `key = <the new key>`.
    - Mint: `url = gcs.signed_download_url(key, ttl_seconds=300, filename=f"raw-output-{run_id}.zip", content_type="application/zip")`; return `{"url": url, "expires_in": gcs._clamp_ttl(300)}` (300s per Open Q3; clamped ≤900s).

    ROUTE B — `@research_router.post("/{intake_id}/research/{run_id}/verify-chain")` → `reverify_chain(...) -> dict` (D-08):
    - Same superadmin gate + existence-hidden intake + scoped run lookup as Route A.
    - Call `tribunal_client.verify_chain(run_id=run.tribunal_run_id, service_url=settings.tribunal_service_url, space_id=str(intake.space_id), acting_user_id=identity.uid, acting_email=identity.email)` OUTSIDE any DB session (seam I/O holds no connection).
    - Open a fresh `tenant_session` and patch: if `verdict.get("ok")` → `chain_status="verified"`, `chain_broken_at=None` (lock lifts); else → `chain_status="broken"`, `chain_broken_at=verdict.get("broken_at")` (lock stays). Optionally `audit.log` the re-verify action in the same tx (recommended if cheap — reuse the trigger's audit-in-tx pattern). Return `{"chain_status": <new status>}`.
    - Note (D-08): a now-verified re-verify does NOT auto-build the bundle here — the next download click does the build-on-download-if-missing. Keep this route lock-state-only.

    Both routes: `space_id` is NEVER read from the request (TENANT-02) — it comes from the resolved intake row. Keep the module's D-03 no-raw-DB-symbol discipline (reach the DB only via the injected repo + tenant_session + stream_session helpers; if a new scoped read is needed, add it to app/db/stream_session.py rather than importing get_engine here).
  </action>
  <verify>
    <automated>MISSING — runs in Cloud Build at wave boundary: pytest backend/tests/test_research_bundle_download.py backend/tests/test_research_cross_tenant.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `research_routes.py` defines a GET route matching `/{intake_id}/research/{run_id}/bundle-url` and a POST route matching `/{intake_id}/research/{run_id}/verify-chain`, both sync `def` (no `async def`).
    - Both handlers raise `HTTPException(404, ...)` when `identity.role != "superadmin"` (existence-hidden, not 403).
    - `get_bundle_url` raises 409 when `run.chain_status != "verified"` and returns `{"url", "expires_in"}` on success.
    - `get_bundle_url` has a `bundle_key is None` branch that builds + uploads via `build_bundle_zip` + `gcs.upload_object` before minting.
    - No `get_engine` / `sessionmaker` import is added to `research_routes.py` (ci_no_raw_db_access grep stays green).
  </acceptance_criteria>
  <done>Superadmin-only, space-scoped download + re-verify routes with existence-hidden denial, availability gate, and build-on-download recovery.</done>
</task>

<task type="auto">
  <name>Task 2: denial suite + download happy-path/recovery tests</name>
  <files>backend/tests/test_research_cross_tenant.py, backend/tests/test_research_bundle_download.py</files>
  <read_first>
    - backend/tests/test_research_cross_tenant.py (the 3-test structure @147-246 — _seed_space/_seed_intake/_patch_engines/_cleanup helpers, _as/_user/_null_space_user, the EXACTLY-404 + "foreign id not in body" assertions)
    - backend/tests/conftest.py (fake_tribunal_client with verify_verdict + get_research_bundle; fake_gcs uploads/signed_urls capture; the _seed helpers if defined there)
    - backend/app/api/research_routes.py (the two new routes from Task 1 — the exact paths + status codes to assert)
  </read_first>
  <action>
    Extend `test_research_cross_tenant.py` with denial tests for BOTH new routes, mirroring the existing verbatim pattern (`_patch_engines(monkeypatch, engine)`, `app.dependency_overrides[get_current_identity] = _as(...)`, assert EXACTLY the status, assert the foreign id is NOT in the body). For EACH route (bundle-url GET, verify-chain POST) add three cases:
    - space-B user calling space-A's run → EXACTLY 404 (existence-hidden; foreign intake/run id not in body).
    - a `user`-role caller (even in the correct space) → EXACTLY 404 (superadmin-only, existence-hidden — proves the role gate).
    - a null-space user → EXACTLY 403 (default-deny) OR 404 per the superadmin-gate ordering; assert whichever the Task-1 gate produces and document it (the superadmin role-check fires first → 404; confirm against the implementation and pin the exact code).
    Seed a completed + chain-verified run for the happy denials so the availability gate is not what produces the 404.

    Create `test_research_bundle_download.py`:
    - Happy path: seed a completed run with chain_status="verified" + a bundle_key; superadmin GET bundle-url → 200, body has `url` + `expires_in`, `fake_gcs["signed_urls"]` recorded the bundle_key with ttl ≤900.
    - Not-verified: a completed run with chain_status="broken" → superadmin GET → 409 (D-06 lock).
    - Build-on-download-if-missing: a completed + verified run with bundle_key=NULL → superadmin GET → 200; assert `fake_gcs["uploads"]` gained exactly one entry (the lazy build) whose key startswith `{space}/{intake}/artifacts/`, and the row's bundle_key is now set.
    - Re-verify lifts lock: a broken run → superadmin POST verify-chain with `fake_tribunal_client["verify_verdict"]={"ok":True,"broken_at":None}` → 200, body `chain_status`=="verified", row updated.
  </action>
  <verify>
    <automated>MISSING — runs in Cloud Build at wave boundary: pytest backend/tests/test_research_bundle_download.py backend/tests/test_research_cross_tenant.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_research_cross_tenant.py` gains at least 6 new assertions (3 per new route) each asserting an exact status code and `str(foreign_id) not in resp.text`.
    - `test_research_bundle_download.py` exists with the happy-path, not-verified-409, build-on-download-if-missing, and re-verify-lifts-lock cases.
    - The build-on-download test asserts `len(fake_gcs["uploads"]) == 1` and the uploaded key startswith `{space}/{intake}/artifacts/`.
  </acceptance_criteria>
  <done>Both new routes are in the CI-gated denial suite from day one; the download happy path, lock, recovery, and re-verify are all covered.</done>
</task>

<task type="auto">
  <name>Task 3: frontend download button + chain-locked state + re-verify action</name>
  <files>frontend/src/lib/api/research.ts, frontend/src/components/intake/ResearchRunProgress.tsx</files>
  <read_first>
    - frontend/src/lib/api/research.ts (the WHOLE file — triggerResearch @75-81 apiFetch one-shot pattern; ResearchRun type @49-58; the RETURN-NO-THROW discipline; never-fork-the-transport note)
    - frontend/src/components/intake/ResearchRunProgress.tsx (the completed card @184-210 and failed card @213-258; the onRetry button @248-256; useTranslation usage; the styling constants #DFF940 / #DC2626)
    - frontend/src/lib/api/client.ts (apiFetch / ApiResult — return-no-throw contract)
  </read_first>
  <action>
    In `research.ts`:
    - Add `chain_status: string | null`, `chain_broken_at: number | null`, `bundle_key: string | null` to the `ResearchRun` type (the SSE frame already carries them once the backend dict emits — Plan 01).
    - Add `getBundleUrl(intakeId, runId): Promise<ApiResult<{ url: string; expires_in: number }>>` = `apiFetch(\`/intakes/${intakeId}/research/${runId}/bundle-url\`, { method: "GET" })`.
    - Add `reVerifyChain(intakeId, runId): Promise<ApiResult<{ chain_status: string }>>` = `apiFetch(\`/intakes/${intakeId}/research/${runId}/verify-chain\`, { method: "POST" })`.
    Reuse `apiFetch` verbatim (never fork the transport); both return ApiResult (return-no-throw).

    In `ResearchRunProgress.tsx`, extend the terminal `status === "completed"` branch (the green card @184-210):
    - When `run.chain_status === "verified"`: render a `[Download]` button below the metrics row. On click, call `getBundleUrl(intakeId, run.id)`; on success, navigate the browser to the returned `url` (e.g. `window.location.href = res.data.url` or an anchor — the signed URL forces attachment disposition server-side so it downloads). On error, show a toast via the existing toast mechanism (sonner) — do NOT throw. Style the button like the existing onRetry button (`bg-ink px-4 py-2 font-mono text-xs uppercase tracking-wider text-paper`).
    - When `run.chain_status === "broken"`: render a DISTINCT locked/error state on the completed card (D-07 UI-only) — a red left-border (#DC2626) treatment mirroring the failed card, an AlertTriangle/lock icon, a locked message, and a `[Re-verify]` button that calls `reVerifyChain(intakeId, run.id)`; on success the SSE stream will push the new chain_status (or trigger a refetch of the active run). No email variant (D-07).
    Add the needed i18n keys to the intake translation namespace (download label, locked title/body, re-verify label) following the existing `research.*` key convention — add to all locale files the project maintains (NL/FR/EN per CLAUDE.md i18n) if they are colocated; if the plan-level i18n files are elsewhere, add the NL key and mirror the existing research.* keys' locale coverage.

    SECURITY (T-16-12): this component is imported ONLY by the admin intake detail route. Do NOT add it to any client-facing route. The download/re-verify affordances inherit that admin-only placement.
  </action>
  <verify>
    <automated>MISSING — no frontend test harness in this repo; verified by lint/typecheck: cd frontend && npm run lint (author-by-construction — runs in the frontend build, not locally per project constraints)</automated>
    <human-check>Admin opens a completed+verified run → sees Download → click downloads the zip. A broken-chain run shows the locked card + Re-verify. (Deferred to the Plan-04 operator runbook — needs a real completed run, blocked on Anthropic credits.)</human-check>
  </verify>
  <acceptance_criteria>
    - `research.ts` exports `getBundleUrl` and `reVerifyChain`, both using `apiFetch` (no raw fetch), and the `ResearchRun` type has `chain_status`, `chain_broken_at`, `bundle_key`.
    - `ResearchRunProgress.tsx` renders a download control only when `status === "completed" && chain_status === "verified"`, and a locked+re-verify control when `chain_status === "broken"`.
    - The download click path calls `getBundleUrl` and navigates to `res.data.url`; no `throw` on the error path (toast instead).
    - The component is not imported by any file under a client/token route (grep for the import shows only the admin detail route).
  </acceptance_criteria>
  <done>The completed card offers a working download for verified runs and a locked+re-verify state for broken chains, admin-only, return-no-throw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (admin) → intake API | download + re-verify requests carry the admin's Bearer token; the routes gate superadmin-only |
| intake API → GCS | signed-URL mint / lazy bundle upload under the space-scoped key |
| intake API → Tribunal seam | re-verify's verify_chain call (internal-auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | Information Disclosure | cross-tenant download of another space's raw output | mitigate | Space-scoped run lookup (`run.intake_id == intake_id`, repo scope) + existence-hidden 404; denial suite covers space-B → 404 (Pitfall 5) |
| T-17-11 | Elevation of Privilege | client (user-role) reaching the download | mitigate | Explicit `identity.role != "superadmin" → 404` (existence-hidden); denial suite covers user-role → 404 (RUN-03: clients NEVER) |
| T-17-12 | Information Disclosure | signed-URL leakage / over-long lifetime | mitigate | TTL 300s clamped ≤900s; forced `attachment` disposition; keyless V4 signing (Phase-9 seam) |
| T-17-13 | Tampering | stored-XSS via report/provider markdown rendered inline | mitigate | Forced `attachment` disposition — the browser downloads the zip, never renders it (T-09-04) |
| T-17-14 | Denial of Service | lazy build-on-download holding a DB connection across GCS/seam I/O | mitigate | `_build_and_store_bundle` runs seam+GCS I/O with no session held; opens a session only to patch bundle_key (mirrors Plan 02) |
| T-17-15 | Information Disclosure | download/re-verify UI leaking onto a client route | mitigate | Component imported only by the admin detail route (T-16-12); acceptance criterion greps the import graph |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new packages this phase (17-RESEARCH audit N/A) |
</threat_model>

<verification>
- Both routes are superadmin-only + space-scoped with existence-hidden 404; denial suite proves space-B, user-role, and null-space cases.
- The download mints a signed URL for verified runs; build-on-download-if-missing recovers a NULL bundle_key.
- Re-verify lifts the lock on a now-passing chain.
- The completed card shows Download (verified) or locked+Re-verify (broken), admin-only.
- Cloud Build (wave boundary): `pytest backend/tests/test_research_bundle_download.py backend/tests/test_research_cross_tenant.py -x`.
</verification>

<success_criteria>
- SC1 (RUN-03): superadmin can download the full raw research output as a file (signed URL, space-scoped).
- SC2 (RUN-03): a client can never access the raw output — endpoint is superadmin-only, denies cross-space + client access, in the CI-gated denial suite.
</success_criteria>

<output>
Create `.planning/phases/17-raw-output-audit-chain-guard/17-03-SUMMARY.md` when done
</output>
