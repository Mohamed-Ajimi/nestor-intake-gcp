---
phase: 14-auth-retirement-integration-seam
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/research/__init__.py
  - backend/app/research/tribunal_client.py
  - backend/app/core/config.py
  - backend/tests/test_tribunal_client.py
autonomous: true
requirements: [SEAM-02]

must_haves:
  truths:
    - "The intake backend can mint a Google-signed OIDC ID token for the Tribunal service URL audience (keyless via ADC)"
    - "The client forwards X-Nestor-Tenant-Id (= space_id) and X-Acting-User-Id / X-Acting-User-Email (the superadmin) on every seam call"
    - "ensure_org(space_id, ...) and ensure_project(space_id, ...) call the Tribunal /ensure endpoints and raise on non-2xx"
    - "The Tribunal service URL is non-secret typed config (Settings.tribunal_service_url), NOT read as a call-time secret"
  artifacts:
    - path: "backend/app/research/tribunal_client.py"
      provides: "OIDC minting + ensure_org / ensure_project HTTP client (minimal, D-06)"
      contains: "def ensure_org"
    - path: "backend/app/core/config.py"
      provides: "tribunal_service_url non-secret Settings field"
      contains: "tribunal_service_url"
  key_links:
    - from: "backend/app/research/tribunal_client.py"
      to: "google.oauth2.id_token.fetch_id_token"
      via: "keyless OIDC minting from the attached SA (ADC)"
      pattern: "fetch_id_token"
    - from: "backend/app/research/tribunal_client.py"
      to: "Tribunal /api/orgs/ensure"
      via: "httpx.post with Bearer token + acting-user headers"
      pattern: "orgs/ensure"
---

<objective>
Give the intake backend the minimal HTTP client machinery to drive the internal Tribunal API (SEAM-02, intake side, D-06): mint a keyless Google-signed OIDC ID token for the Tribunal service URL, forward the acting superadmin's identity via headers, and call idempotent `ensure_org` / `ensure_project`. This is the seam Phase 16 builds the trigger on.

Purpose: Establishes the intake -> Tribunal transport with the correct OIDC audience (Pitfall 4), the D-05 acting-user attribution, and the identity mapping (space_id sent verbatim as the tenant). Run-trigger/status-poll/report-fetch methods are explicitly OUT OF SCOPE (Phase 16, D-06) — building them now would re-work against Phase 15's final report shape.

Output: New `backend/app/research/` package with `tribunal_client.py` (`_mint_id_token`, `ensure_org`, `ensure_project`); a non-secret `tribunal_service_url` Settings field; a unit test with the httpx + token calls mocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-auth-retirement-integration-seam/14-CONTEXT.md
@.planning/phases/14-auth-retirement-integration-seam/14-RESEARCH.md
@.planning/phases/14-auth-retirement-integration-seam/14-PATTERNS.md

<interfaces>
The httpx POST + timeout + raise_for_status shape to copy, from backend/app/mail/resend.py::send (lines 58-69):
  httpx.post(endpoint, headers={"Authorization": f"Bearer {tok}", ...}, json={...}, timeout=_TIMEOUT_S); resp.raise_for_status().
  A module const timeout (resend uses 15.0; the seam uses 30.0 per RESEARCH).

The acting-user source, from backend/app/auth/identity.py:
  Identity(uid, email, role, space_id) — frozen; produced by backend/app/auth/dependencies.py::get_current_identity.
  For a superadmin, role="superadmin" and space_id is None — the acting space_id is passed in by the caller (Phase 16), NOT read off the superadmin Identity.

The non-secret config pattern, from backend/app/core/config.py::Settings (storage_bucket / app_base_url, lines 69-90):
  Add tribunal_service_url: str | None = None (env TRIBUNAL_SERVICE_URL). It is NON-secret — a service URL, no Secret Manager reference. Read via get_settings().tribunal_service_url, never as a call-time os.environ secret.

OIDC minting (RESEARCH § Code Examples; keyless via ADC — google-auth arrives transitively via google-cloud-storage>=3,<4; do NOT add a separate pin):
  from google.auth.transport import requests as ga_requests
  from google.oauth2 import id_token as ga_id_token
  _TRANSPORT = ga_requests.Request()
  ga_id_token.fetch_id_token(_TRANSPORT, service_url)   # audience = service URL WITHOUT a path (Pitfall 4)

The wire contract (MUST match Plan 01's InternalCallerProvider header constants verbatim):
  Authorization: Bearer <id_token>
  X-Nestor-Tenant-Id: <space_id>       (space_id IS org.id — identity mapping)
  X-Acting-User-Id: <superadmin uid>   (the human, D-05)
  X-Acting-User-Email: <superadmin email>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the non-secret tribunal_service_url Settings field</name>
  <read_first>
    - backend/app/core/config.py (Settings — the storage_bucket / app_base_url non-secret field pattern, lines 69-90; the D-07 secrets-are-not-in-Settings rule)
  </read_first>
  <action>
    Add `tribunal_service_url: str | None = None` to `Settings` (env `TRIBUNAL_SERVICE_URL`) with a docstring comment mirroring the `storage_bucket` / `app_base_url` fields: it is NON-secret (the Cloud Run service URL of tribunal-api, e.g. `https://tribunal-api-xxxx.run.app`), read via `get_settings().tribunal_service_url` — NEVER a call-time `os.environ` secret and NEVER a Secret Manager reference. Note that it must be the service URL WITHOUT a path (it is used verbatim as the OIDC audience — Pitfall 4). Do not add any secret field — there is no secret in this seam (the OIDC token is keyless via ADC).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "import ast; s=open('app/core/config.py').read(); assert 'tribunal_service_url' in s; t=ast.parse(s); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `Settings` has a `tribunal_service_url: str | None = None` field bound to env `TRIBUNAL_SERVICE_URL`.
    - No secret field is added; the field's comment states it is non-secret and must be the URL without a path.
  </acceptance_criteria>
  <done>The Tribunal service URL is available as validated non-secret config for the client to read.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the tribunal_client (OIDC minting + ensure_org / ensure_project)</name>
  <read_first>
    - backend/app/mail/resend.py (send — httpx.post + timeout + raise_for_status; the transport shape to copy)
    - backend/app/ai/clients.py (call-time secret discipline — NOTE: there is NO secret here, so do not copy the os.environ pattern for the URL)
    - backend/app/core/config.py (get_settings / tribunal_service_url from Task 1)
    - backend/app/auth/identity.py (Identity fields uid/email — the acting-user source)
    - .planning/phases/14-auth-retirement-integration-seam/14-RESEARCH.md § Code Examples (minting + calling the seam) + Pitfall 4 (aud without path)
    - .planning/phases/14-auth-retirement-integration-seam/14-PATTERNS.md § backend/app/research/tribunal_client.py (header contract + config note)
  </read_first>
  <behavior>
    - Test: ensure_org mints a token via fetch_id_token(transport, service_url) and POSTs to {service_url}/api/orgs/ensure with headers Authorization: Bearer <tok>, X-Nestor-Tenant-Id == space_id, X-Acting-User-Id == acting uid, X-Acting-User-Email == acting email; a 200 response returns without raising.
    - Test: a non-2xx response raises httpx.HTTPStatusError (via raise_for_status).
    - Test: ensure_project POSTs to {service_url}/api/projects/ensure with the same headers and returns the JSON project_id from the response body.
    - Test: the audience passed to fetch_id_token is exactly the service URL with no path suffix.
    - Mock google.oauth2.id_token.fetch_id_token and httpx.post (no network, no ADC) so tests run without GCP credentials.
  </behavior>
  <action>
    Create `backend/app/research/__init__.py` (empty package marker) and `backend/app/research/tribunal_client.py`. Module-level: `_TRANSPORT = ga_requests.Request()`, `_TIMEOUT_S = 30.0`, header-name constants matching Plan 01 verbatim (`X-Nestor-Tenant-Id`, `X-Acting-User-Id`, `X-Acting-User-Email`).
    `def _mint_id_token(service_url: str) -> str`: return `ga_id_token.fetch_id_token(_TRANSPORT, service_url)` (keyless via ADC; audience = URL WITHOUT path — Pitfall 4).
    `def _headers(service_url, space_id, acting_user_id, acting_email) -> dict`: build the `Authorization: Bearer <_mint_id_token(service_url)>` + the three X- headers.
    `def ensure_org(*, service_url, space_id, acting_user_id, acting_email) -> None`: `httpx.post(f"{service_url}/api/orgs/ensure", headers=_headers(...), json={}, timeout=_TIMEOUT_S)`; `resp.raise_for_status()`.
    `def ensure_project(*, service_url, space_id, acting_user_id, acting_email) -> str`: POST to `f"{service_url}/api/projects/ensure"` the same way; `resp.raise_for_status()`; return `resp.json()["project_id"]`.
    Resolve `service_url` from `get_settings().tribunal_service_url` at the call site (Phase 16 will pass it in / read it); keep the functions parameterized on `service_url` so they stay testable. Blocking `httpx.post` (not async) mirrors resend.py — the FastAPI handlers run on the pg8000 threadpool. Do NOT add run-trigger / status-poll / report-fetch methods (Phase 16, D-06). Do NOT persist a project_id or add a DB column (D-06 — Phase 16 owns `research_runs`).
    Add `backend/tests/test_tribunal_client.py` covering the <behavior> cases with `fetch_id_token` + `httpx.post` mocked and skip-clean guards consistent with the sibling backend tests (`pytest.importorskip("app.research.tribunal_client")`).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "import ast; s=open('app/research/tribunal_client.py').read(); t=ast.parse(s); fns=[n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)]; assert 'ensure_org' in fns and 'ensure_project' in fns and '_mint_id_token' in fns, fns; assert 'fetch_id_token' in s and 'orgs/ensure' in s and 'projects/ensure' in s and 'X-Nestor-Tenant-Id' in s; print('ok')" &amp;&amp; test -f app/research/__init__.py &amp;&amp; test -f tests/test_tribunal_client.py</automated>
  </verify>
  <acceptance_criteria>
    - `tribunal_client.py` defines `_mint_id_token`, `ensure_org`, `ensure_project`; the package has an `__init__.py`.
    - `_mint_id_token` calls `google.oauth2.id_token.fetch_id_token` with the service URL (no path) as audience.
    - Both `ensure_*` functions send the three X- headers with the exact names from Plan 01 and call `raise_for_status()`.
    - No run-trigger/poll/report method exists; no DB column or project_id persistence is added.
    - `test_tribunal_client.py` passes with mocked token + httpx (no ADC / network needed).
  </acceptance_criteria>
  <done>The intake backend can mint an OIDC token and idempotently provision an org+project over the internal seam, with the acting superadmin attributed via headers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intake backend -> Tribunal API | The intake backend is the sole authenticated caller; it mints the credential and asserts the tenant + acting user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Spoofing | OIDC minting | mitigate | Keyless `fetch_id_token` via the attached SA's ADC — no SA JSON key exists (org policy); token audience pinned to the Tribunal URL without a path (Pitfall 4) |
| T-14-07 | Info Disclosure | service URL / token in logs | mitigate | Service URL is non-secret Settings config; the minted token is never logged; no secret enters Settings or the image (D-07) |
| T-14-08 | Tampering | acting-user headers | mitigate | acting-user id/email come from the verified superadmin Identity at the call site (Phase 16); the client forwards them into the D-05 header contract, never fabricates them |
| T-14-SC | Tampering | pip installs | mitigate | No new package — `google-auth` (transitive via google-cloud-storage) + `httpx` (existing direct dep) only. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- OIDC minting audience + header contract + raise-on-error verified by the mocked unit test (Task 2).
- The end-to-end negative/positive proof (real token accepted, unauthenticated/wrong-SA rejected, cross-tenant denied) is the D-07 operator checkpoint in Plan 04.
- The seam-level denial suite (missing/mismatched tenant, GUC-leak) is added in Plan 03 against Plan 01's endpoints + this client.
</verification>

<success_criteria>
- `ensure_org` / `ensure_project` mint the correct-audience OIDC token, send the D-05 acting-user headers + the space_id tenant header, and raise on non-2xx (SEAM-02 intake side).
- The Tribunal service URL is non-secret typed config; no secret is added to Settings or the image.
- No Phase-16 client method (trigger/poll/report) or `research_runs` column is introduced (D-06 boundary honored).
</success_criteria>

<output>
Create `.planning/phases/14-auth-retirement-integration-seam/14-02-SUMMARY.md` when done. Record the exact header names sent (must match Plan 01) and the `TRIBUNAL_SERVICE_URL` env var Plan 04 must set on the intake nestor-api service.
</output>
