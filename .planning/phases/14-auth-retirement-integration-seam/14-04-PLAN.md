---
phase: 14-auth-retirement-integration-seam
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - infra/main.tf
  - infra/variables.tf
  - infra/DEPLOY-RUNBOOK.md
  - tribunal/infrastructure/cloud-run/deploy-api.sh
  - tribunal/infrastructure/cloud-run/deploy-worker.sh
autonomous: false
requirements: [SEAM-01, SEAM-02]

user_setup:
  - service: gcp-cloud-run-iam
    why: "Dedicated Tribunal runtime SA + Tribunal-API invoker binding + retired-secret cleanup + the D-07 live proof are live gcloud changes (dev box has no Docker; Terraform is authored by-construction only — NEVER applied, CR-02)."
    env_vars:
      - name: GOOGLE_PROJECT
        source: "The intake 'Nestor Pulse' project id (operator exports it, per the Phase-13 runbook)"
    dashboard_config:
      - task: "Create the dedicated least-privilege tribunal-run SA + grant the invoker binding + seed TRIBUNAL_SERVICE_URL/INTAKE_RUNTIME_SA_EMAIL env, then run both Cloud Build test suites + the D-07 live proof"
        location: "Cloud Shell (gcloud) — runbook § Phase 14"

must_haves:
  truths:
    - "Tribunal services run as a dedicated least-privilege SA (tribunal-run@...), not the intake runtime SA (nestor-run@...)"
    - "Only the intake runtime SA (nestor-run@...) may invoke the tribunal-api service (run.invoker binding); the service stays --no-allow-unauthenticated"
    - "IDENTITY_PLATFORM_* env/secret references are absent from the Tribunal deploy config"
    - "tribunal-api carries the non-secret TRIBUNAL_SERVICE_URL + INTAKE_RUNTIME_SA_EMAIL env vars the InternalCallerProvider reads"
    - "The intake nestor-api carries the non-secret TRIBUNAL_SERVICE_URL env var the tribunal_client reads"
  artifacts:
    - path: "infra/main.tf"
      provides: "by-construction dedicated tribunal-run SA + least-priv bindings + tribunal-api invoker = nestor-run + seam env vars"
      contains: "tribunal_run"
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "operator-run § Phase 14 steps: SA create, invoker binding, secret cleanup, env wiring, two-suite CI gate, D-07 live proof + negative proofs"
      contains: "Phase 14"
  key_links:
    - from: "infra/main.tf"
      to: "google_service_account.tribunal_run"
      via: "tribunal services + migrate job point at the dedicated SA"
      pattern: "tribunal_run"
    - from: "infra/main.tf"
      to: "tribunal_api invoker"
      via: "run.invoker member = nestor-run intake SA only"
      pattern: "run.invoker"
---

<objective>
Close the D-04 defense-in-depth IAM layer and the WR-03/D-04b runtime-SA separation by-construction, then run the operator live session (D-07): give Tribunal its own least-privilege runtime SA, bind the tribunal-api invoker to ONLY the intake runtime SA, remove the retired `IDENTITY_PLATFORM_*` references, wire the seam env vars, run both Cloud Build denial suites, and prove one real server-to-server run + three negative proofs.

Purpose: The IAM invoker gate is theater until caller SA != callee SA (WR-03). This plan makes the gate meaningful AND runs the phase's live proof — absorbing the Phase-13 deferred queue-path proof (D-07). Terraform is authored by construction only and NEVER applied (CR-02 would rotate DB passwords); the live changes go through the gcloud runbook.

Output: `infra/main.tf` + `infra/variables.tf` updated (dedicated `tribunal-run` SA, least-priv bindings, invoker = nestor-run, retired secret/env removal, seam env vars); retargeted `deploy-api.sh` / `deploy-worker.sh` (SA -> tribunal-run); `infra/DEPLOY-RUNBOOK.md` § Phase 14; a completed operator proof (checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-auth-retirement-integration-seam/14-CONTEXT.md
@.planning/phases/14-auth-retirement-integration-seam/14-RESEARCH.md
@.planning/phases/14-auth-retirement-integration-seam/14-PATTERNS.md
@.planning/phases/14-auth-retirement-integration-seam/14-01-SUMMARY.md
@.planning/phases/14-auth-retirement-integration-seam/14-02-SUMMARY.md
@.planning/phases/14-auth-retirement-integration-seam/14-03-SUMMARY.md

<interfaces>
Current Tribunal IaC (all on the intake SA — the WR-03 finding to fix), from infra/main.tf:
  google_cloud_run_v2_service.tribunal_worker / .tribunal_api / google_cloud_run_v2_job.tribunal_migrate all set service_account = google_service_account.runtime.email (= nestor-run@...).
  google_service_account.runtime is the intake runtime SA (roles: cloudsql.client, cloudsql.instanceUser, identitytoolkit.admin, storage.objectAdmin on uploads, serviceAccountTokenCreator self, + resource-scoped secretAccessor on the Tribunal + intake secrets).
  google_cloud_run_v2_service_iam_member.tribunal_api_invoker is gated on var.allow_unauthenticated (allUsers) — must become a nestor-run run.invoker binding.

Current deploy scripts (SA = intake SA), from tribunal/infrastructure/cloud-run/deploy-*.sh:
  SA="nestor-run@${PROJECT}.iam.gserviceaccount.com"; --service-account="${SA}"; --no-allow-unauthenticated (already true).
  deploy-api.sh --set-env-vars="NESTOR_ENV=prod,NESTOR_TRIBUNAL_UNCAPPED=1"; --set-secrets includes DATABASE_URL, AUDIT_GCS_BUCKET, the three provider keys. Confirm no IDENTITY_PLATFORM_* crept back in.

The IaC discipline (MEMORY, CR-02 landmine): author infra/*.tf by construction; do the live change via the gcloud runbook. NEVER `terraform apply` (it would rotate tribunal_app_user / worker_user DB passwords — the ignore_changes[password] lifecycle guards state, but apply is still forbidden).

The Phase-13 runbook shape to mirror, from infra/DEPLOY-RUNBOOK.md § Phase 13 (Steps 13.a-13.i): secrets, roles, images via Cloud Build, migrate Job, deploy scripts, proof checkpoint. Phase 14 is a smaller delta on the already-deployed services.

The seam env vars to wire (from Plan 01 + Plan 02 SUMMARYs):
  tribunal-api: TRIBUNAL_SERVICE_URL (its own run.app URL, no path — captured from `gcloud run services describe`), INTAKE_RUNTIME_SA_EMAIL (nestor-run@<project>.iam.gserviceaccount.com). Both NON-secret.
  nestor-api (intake): TRIBUNAL_SERVICE_URL (the tribunal-api run.app URL, no path). NON-secret.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: By-construction IaC + retargeted deploy scripts (dedicated SA, invoker binding, retired-secret removal, seam env)</name>
  <files>infra/main.tf, infra/variables.tf, tribunal/infrastructure/cloud-run/deploy-api.sh, tribunal/infrastructure/cloud-run/deploy-worker.sh</files>
  <read_first>
    - infra/main.tf (the Phase-13 Tribunal block: tribunal_worker/api services, tribunal_migrate job, tribunal_api_invoker, google_service_account.runtime + its role bindings)
    - infra/variables.tf (var patterns for SA ids / service names / toggles)
    - tribunal/infrastructure/cloud-run/deploy-api.sh + deploy-worker.sh (SA line 32/40; env + secrets)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-REVIEW.md § WR-03 (the runtime-SA separation finding — least-priv grant list)
    - .planning/phases/14-auth-retirement-integration-seam/14-RESEARCH.md § Pattern 2 (dedicated SA + invoker) + Runtime State Inventory (secret cleanup scope A6)
    - .planning/phases/14-auth-retirement-integration-seam/14-01-SUMMARY.md + 14-02-SUMMARY.md (the seam env var names)
  </read_first>
  <action>
    In `infra/main.tf`, add a dedicated Tribunal runtime SA and repoint the Tribunal resources at it (WR-03/D-04b):
    - `google_service_account.tribunal_run` (account_id from a new `var.tribunal_runtime_sa_id`, default `tribunal-run`, display "Tribunal engine runtime (least-priv)").
    - Least-privilege bindings for tribunal-run ONLY: `roles/cloudsql.client` (the Tribunal services authenticate with a stored-password asyncpg DSN over the Cloud SQL unix socket, so they need cloudsql.client for the instance attach; they do NOT use IAM DB login, so `cloudsql.instanceUser` is NOT required — grant cloudsql.client only and add a comment stating the BUILT_IN-password path does not need instanceUser), resource-scoped `secretmanager.secretAccessor` on ONLY the six Tribunal secrets (`tribunal_gemini`, `tribunal_claude`, `tribunal_openai`, `tribunal_database_url`, `tribunal_database_url_worker`, `tribunal_audit_bucket`), and `storage.objectAdmin` on the `tribunal_audit` bucket ONLY. Do NOT grant identitytoolkit.admin, the intake superadmin DB-password secret, or the intake uploads bucket (least-priv — the compromised-worker exposure WR-03 flags).
    - Repoint `service_account = google_service_account.tribunal_run.email` on `google_cloud_run_v2_service.tribunal_worker`, `.tribunal_api`, and `google_cloud_run_v2_job.tribunal_migrate`. Change the six Tribunal `secretAccessor` IAM members + the audit-bucket objectAdmin member from `member = ...runtime.email` to `...tribunal_run.email` (the grants must follow the SA that now runs the services/job). Update the corresponding `depends_on` edges to reference the tribunal_run grants.
    - Replace `google_cloud_run_v2_service_iam_member.tribunal_api_invoker` (currently allUsers gated on allow_unauthenticated) with an UNCONDITIONAL `run.invoker` binding whose `member = "serviceAccount:${google_service_account.runtime.email}"` (the intake nestor-run SA is the ONLY allowed caller — D-04 outer gate). Keep the service `--no-allow-unauthenticated`.
    - Add the seam env vars: on `tribunal_api`, two plain (non-secret) env blocks — `TRIBUNAL_SERVICE_URL` (value from a new `var.tribunal_service_url`, default "") and `INTAKE_RUNTIME_SA_EMAIL` (value = `google_service_account.runtime.email`). On the intake `google_cloud_run_v2_service.api`, add a plain env block `TRIBUNAL_SERVICE_URL` (value from `var.tribunal_service_url`). Comment each with the IaC-DRIFT note (inert until the runbook sets it live).
    - Confirm no `IDENTITY_PLATFORM_*` env or secret reference remains in the Tribunal blocks (A6: remove only the Tribunal references; do NOT touch nestor-api's own IdP usage — `runtime_identitytoolkit_admin` stays for the intake API).
    Add the new vars to `infra/variables.tf` (`tribunal_runtime_sa_id`, `tribunal_service_url`) mirroring the existing var style.
    In `deploy-api.sh` + `deploy-worker.sh`, change `SA="nestor-run@..."` to `SA="tribunal-run@${PROJECT}.iam.gserviceaccount.com"`. In `deploy-api.sh`, append `TRIBUNAL_SERVICE_URL` + `INTAKE_RUNTIME_SA_EMAIL` to `--set-env-vars` (documented as populated from the describe-captured URL + the intake SA email). Confirm no `IDENTITY_PLATFORM_*` remains in either script.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -q "tribunal_run" infra/main.tf &amp;&amp; grep -q "run.invoker" infra/main.tf &amp;&amp; grep -q "TRIBUNAL_SERVICE_URL" infra/main.tf &amp;&amp; grep -q "INTAKE_RUNTIME_SA_EMAIL" infra/main.tf &amp;&amp; grep -q "tribunal-run@" tribunal/infrastructure/cloud-run/deploy-api.sh &amp;&amp; ! grep -q "IDENTITY_PLATFORM" tribunal/infrastructure/cloud-run/deploy-api.sh tribunal/infrastructure/cloud-run/deploy-worker.sh</automated>
  </verify>
  <acceptance_criteria>
    - `google_service_account.tribunal_run` exists; the two Tribunal services + the migrate job set `service_account = google_service_account.tribunal_run.email`.
    - tribunal-run's grants are least-privilege: cloudsql.client + the six Tribunal secrets' secretAccessor + the audit-bucket objectAdmin ONLY (no identitytoolkit.admin, no intake superadmin secret, no intake uploads bucket).
    - The tribunal-api invoker binding member is the intake `runtime` (nestor-run) SA, unconditional; the service stays --no-allow-unauthenticated.
    - `main.tf` (both tribunal-api and nestor-api) and both deploy scripts carry the seam env vars; no `IDENTITY_PLATFORM_*` remains in the Tribunal deploy config.
    - `infra/variables.tf` declares `tribunal_runtime_sa_id` + `tribunal_service_url`.
  </acceptance_criteria>
  <done>The by-construction IaC and deploy scripts give Tribunal a dedicated least-privilege SA, bind the API invoker to only the intake SA, wire the seam env vars, and carry no retired IdP references.</done>
</task>

<task type="auto">
  <name>Task 2: Write the § Phase 14 runbook (SA create, invoker binding, secret cleanup, env wiring, two-suite CI gate)</name>
  <files>infra/DEPLOY-RUNBOOK.md</files>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 13 (Steps 13.a-13.i — the operator-step shape to mirror: images via Cloud Build, deploy scripts, describe-capture, checkpoint)
    - infra/main.tf (Task 1 output — the resources the runbook applies live via gcloud)
    - tribunal/cloudbuild.test.yaml + tribunal/cloudbuild.test-critical.yaml + cloudbuild.test.yaml (the three suites the CI gate runs)
    - .planning/phases/14-auth-retirement-integration-seam/14-03-SUMMARY.md (which config runs each new test + the -k selectors)
    - .planning/phases/14-auth-retirement-integration-seam/14-RESEARCH.md § Runtime State Inventory (rebuild images; A6 secret-cleanup caution)
  </read_first>
  <action>
    Append a `## Phase 14 — Auth retirement + integration seam` section to `infra/DEPLOY-RUNBOOK.md` mirroring the § Phase 13 step style, with these ordered gcloud steps (each drift-honest — by-construction IaC is inert until run):
    - Step 14.a — Create the dedicated `tribunal-run` SA: `gcloud iam service-accounts create tribunal-run --project="$GOOGLE_PROJECT" --display-name="Tribunal engine runtime (least-priv)"`, then grant ONLY cloudsql.client (project-level), the six Tribunal secrets' resource-scoped secretAccessor (`gcloud secrets add-iam-policy-binding <secret> --member=serviceAccount:tribunal-run@... --role=roles/secretmanager.secretAccessor`), and the audit bucket objectAdmin. Explicitly note the grants NOT given (identitytoolkit.admin, intake superadmin secret, intake uploads bucket).
    - Step 14.b — Rebuild both Tribunal images via Cloud Build (the retirement + provider swap must be in the image; `firebase-admin` removed): `gcloud builds submit tribunal --config=tribunal/cloudbuild.api.yaml` and the worker config, capture the `$SHA`.
    - Step 14.c — Redeploy tribunal-worker then tribunal-api with the retargeted scripts (`IMAGE_TAG="$SHA" tribunal/infrastructure/cloud-run/deploy-worker.sh`, then deploy-api.sh) so both now run as `tribunal-run`. Capture the tribunal-api URL from `gcloud run services describe tribunal-api --format='value(status.url)'`.
    - Step 14.d — Set the seam env vars live: on tribunal-api `--update-env-vars TRIBUNAL_SERVICE_URL=<captured url>,INTAKE_RUNTIME_SA_EMAIL=nestor-run@$GOOGLE_PROJECT.iam.gserviceaccount.com`; on nestor-api `--update-env-vars TRIBUNAL_SERVICE_URL=<captured url>`. (The URL is the service URL WITHOUT a path — Pitfall 4.)
    - Step 14.e — Bind the invoker (D-04 outer gate): `gcloud run services add-invoker-policy-binding tribunal-api --member="serviceAccount:nestor-run@$GOOGLE_PROJECT.iam.gserviceaccount.com" --region="$REGION" --project="$GOOGLE_PROJECT"`; keep the service --no-allow-unauthenticated. Also remove any lingering allUsers invoker binding on tribunal-api if present.
    - Step 14.f — Retired-secret cleanup (A6, conservative): remove `IDENTITY_PLATFORM_*` env refs from the Tribunal service config (already absent from the scripts — verify with describe). Do NOT delete the Secret Manager entries themselves unless a "no other reader" grep across both deploy surfaces confirms the intake side does not share them; leave that deletion as a documented later cleanup if unproven.
    - Step 14.g — Run the two-suite CI gate: `gcloud builds submit backend --config=cloudbuild.test.yaml` (intake seam denial) and `gcloud builds submit tribunal --config=tribunal/cloudbuild.test.yaml` (tribunal.* RLS). Both must be green (D-08). Reference the `-k` selectors from 14-03-SUMMARY.
    - Add a `## Summary checklist` set of Phase-14 checkboxes mirroring the existing checklist.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -q "Phase 14" infra/DEPLOY-RUNBOOK.md &amp;&amp; grep -q "add-invoker-policy-binding tribunal-api" infra/DEPLOY-RUNBOOK.md &amp;&amp; grep -q "tribunal-run" infra/DEPLOY-RUNBOOK.md &amp;&amp; grep -q "TRIBUNAL_SERVICE_URL" infra/DEPLOY-RUNBOOK.md &amp;&amp; grep -q "cloudbuild.test" infra/DEPLOY-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `infra/DEPLOY-RUNBOOK.md` has a `## Phase 14` section with ordered steps 14.a-14.g covering SA create, image rebuild, redeploy-as-tribunal-run, seam env wiring, invoker binding, retired-secret cleanup, and the two-suite CI gate.
    - The invoker step binds ONLY the intake nestor-run SA and keeps --no-allow-unauthenticated.
    - The secret-cleanup step is conservative (no blind Secret Manager deletion without a no-other-reader check).
    - The CI-gate step references both Cloud Build configs and the 14-03 `-k` selectors.
  </acceptance_criteria>
  <done>The operator has a complete, ordered § Phase 14 runbook to apply the SA separation, invoker gate, seam env, and CI gate live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: D-07 operator live proof — real server-to-server run + three negative proofs</name>
  <what-built>
    The retirement + provider swap (Plan 01), the intake seam client (Plan 02), the two-suite denial gate (Plan 03), and the by-construction IaC + runbook (Tasks 1-2) are complete. The operator now runs the § Phase 14 runbook live and then executes the D-07 proof: this ABSORBS the Phase-13 deferred queue-path proof (strike it from Phase 16's backlog once green).
  </what-built>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md § Phase 14 (Steps 14.a-14.g authored in Task 2 — the live steps to run first)
    - backend/tests/test_tribunal_seam_denial.py (the -k selectors for the negative proofs, from 14-03)
    - tribunal/nestor_pulse_sdk/scripts/run_tribunal_smoke.py (the Phase-13 proof vehicle — --tenant-id drives distinct spaces; reuse for the positive run)
    - .planning/phases/13-tribunal-re-home-infra-baseline/13-HUMAN-UAT.md (the deferred queue-path proof this run absorbs, D-07)
  </read_first>
  <files>.planning/phases/14-auth-retirement-integration-seam/14-04-SUMMARY.md (operator records the proof numbers)</files>
  <action>
    Operator-run (no autonomous code). In Cloud Shell with `GOOGLE_PROJECT` / `REGION` exported, AFTER running runbook Steps 14.a-14.g:
    1. Positive proof (server-to-server, no UI, hand-built brief): drive the seam so it provisions org+project for a chosen space_id (org.id == space_id) and triggers ONE real research run end-to-end to `completed` green. Confirm the run's audit chain attributes it to the acting superadmin's id+email (D-05) and that `verify_chain` stays green (frozen canonical_json preserved).
    2. Negative — unauthenticated: call tribunal-api WITHOUT an OIDC bearer token; expect rejected at the Cloud Run IAM gate (401/403).
    3. Negative — wrong-SA: call tribunal-api with an OIDC token minted for a DIFFERENT service account (not nestor-run); expect rejected (IAM invoker gate blocks it; if it reaches the app, InternalCallerProvider returns 403). This proof is only constructible BECAUSE caller SA (nestor-run) != callee SA (tribunal-run) — WR-03 closed.
    4. Negative — cross-tenant: with a valid caller token, attempt to read space-B data using a space-A tenant context; expect denied (no foreign rows). Confirm via the seam denial suite `-k guc_leak` (14-03) or a live read.
    5. Confirm both Cloud Build test suites (intake `cloudbuild.test.yaml` + tribunal `cloudbuild.test.yaml`) are green (D-08 gate).
    Record the run id, the space_id used, the audit-chain acting-user attribution, `verify_chain` result, the three negative outcomes, and both build ids in 14-04-SUMMARY.md.
  </action>
  <verify>
    <human-check>The positive run is completed-green with correct D-05 acting-user attribution and verify_chain green; the three negative proofs (unauthenticated, wrong-SA, cross-tenant) all reject; both Cloud Build suites are green.</human-check>
  </verify>
  <done>One real server-to-server run completes green with correct acting-user attribution, all three negative proofs reject, and both CI suites are green — the D-07 gate (and the absorbed Phase-13 queue-path proof) passes.</done>
  <resume-signal>Type "approved" once the positive run is completed-green with correct acting-user attribution, all three negative proofs reject, and both CI suites are green — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any caller -> tribunal-api (Cloud Run IAM) | Outer gate: only nestor-run may invoke; --no-allow-unauthenticated |
| tribunal-run SA -> GCP resources | Callee least-privilege: only Tribunal secrets + audit bucket + cloudsql.client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-12 | Spoofing / Elevation | browser or third party invokes tribunal-api | mitigate | --no-allow-unauthenticated + run.invoker = nestor-run ONLY (Task 1 IaC / Task 2 runbook); proven by the unauthenticated negative proof (Task 3) |
| T-14-13 | Elevation | mis-set IAM binding silently opens tenants | mitigate | IAM is necessary not sufficient — InternalCallerProvider re-verifies (Plan 01); wrong-SA negative proof exercises both gates (Task 3) |
| T-14-14 | Elevation | compromised Tribunal worker reaches intake admin surfaces | mitigate | Dedicated tribunal-run least-priv SA — no identitytoolkit.admin, no intake superadmin DB secret, no intake uploads bucket (WR-03 fix, Task 1) |
| T-14-15 | Info Disclosure | secret cleanup deletes a shared IdP secret | accept | Conservative cleanup — remove only Tribunal references; delete Secret Manager entries only after a no-other-reader check (Task 2, A6) |
| T-14-SC | Tampering | image rebuild / installs | mitigate | Images rebuilt via Cloud Build from the verbatim-carried requirements (firebase-admin removed); no NEW package added this phase. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- IaC + deploy-script structure verified by grep (Tasks 1-2, automated).
- The runbook § Phase 14 completeness verified by grep (Task 2, automated).
- The live D-04 gate (unauthenticated + wrong-SA rejected), the SEAM-02 space-scoping (cross-tenant denied), the positive server-to-server run (completed-green + D-05 attribution + verify_chain green), and both Cloud Build suites green are the D-07 operator checkpoint (Task 3).
</verification>

<success_criteria>
- Tribunal runs as a dedicated least-privilege `tribunal-run` SA; only nestor-run may invoke tribunal-api; the service stays internal-only (SEAM-01 IAM layer, WR-03 closed).
- The seam env vars are wired on both services; no retired IDENTITY_PLATFORM_* references remain.
- The two-suite denial gate is green live (D-08).
- One real server-to-server run completes green with correct acting-user attribution, and the three negative proofs (unauthenticated, wrong-SA, cross-tenant) all reject (D-07) — absorbing the Phase-13 deferred queue-path proof.
</success_criteria>

<output>
Create `.planning/phases/14-auth-retirement-integration-seam/14-04-SUMMARY.md` when done. Record: the tribunal-run SA email + its exact grant list; the captured tribunal-api URL; the proof run id + space_id + audit-chain acting-user attribution + verify_chain result; the three negative-proof outcomes; both Cloud Build ids; and a note to strike the Phase-13 queue-path proof from Phase 16's backlog (D-07 absorbed it).
</output>
