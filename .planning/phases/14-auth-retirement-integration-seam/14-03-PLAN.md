---
phase: 14-auth-retirement-integration-seam
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - backend/tests/test_tribunal_seam_denial.py
  - tribunal/nestor_pulse_sdk/tests/test_seam_rls_denial.py
autonomous: true
requirements: [SEAM-02]

must_haves:
  truths:
    - "A seam call with a missing X-Nestor-Tenant-Id header is rejected before any unset-GUC query runs"
    - "A seam call whose OIDC caller email is not the intake SA is rejected (403)"
    - "An unauthenticated seam call (no bearer) is rejected (401)"
    - "A space-A tenant header can never return space-B Tribunal rows across the HTTP boundary (GUC-leak firewall)"
    - "Cross-tenant RLS denial holds on tribunal.* tables (native asyncpg harness)"
  artifacts:
    - path: "backend/tests/test_tribunal_seam_denial.py"
      provides: "seam-level denial suite (missing/mismatched tenant, wrong-SA, unauthenticated, GUC-leak) — intake pg8000 harness"
      contains: "def test_"
    - path: "tribunal/nestor_pulse_sdk/tests/test_seam_rls_denial.py"
      provides: "tribunal.* cross-tenant RLS denial through the InternalCallerProvider path — asyncpg harness"
      contains: "async def test_"
  key_links:
    - from: "backend/tests/test_tribunal_seam_denial.py"
      to: "InternalCallerProvider seam"
      via: "asserts EXACT status codes, never in (403,404)"
      pattern: "== 40"
    - from: "tribunal/nestor_pulse_sdk/tests/test_seam_rls_denial.py"
      to: "set_tenant_context"
      via: "two-tenant isolation on tribunal tables"
      pattern: "set_tenant_context"
---

<objective>
Extend the CI-gated cross-tenant denial suite to cover the Tribunal seam and `tribunal.*` tables (SEAM-02, D-08) — proving the GUC-name mismatch (`app.current_space_id` vs `app.tenant_id`) cannot leak across the HTTP boundary. Each layer is tested in its native harness: seam-level denial in the intake pg8000 suite, `tribunal.*` RLS denial in Tribunal's asyncpg suite. No driver mixing.

Purpose: Every new v1.1 surface is a fresh place the broken-RLS bug class can recur; from day one each read/write goes through the space-scoped session and is added to the CI-gated denial suite. This plan is the SEAM-02 test gate — both Cloud Build test configs must stay green.

Output: `backend/tests/test_tribunal_seam_denial.py` (seam denial, clone of `test_intake_cross_tenant.py`); `tribunal/nestor_pulse_sdk/tests/test_seam_rls_denial.py` (tribunal.* two-tenant RLS, extending `test_rls_isolation.py`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-auth-retirement-integration-seam/14-CONTEXT.md
@.planning/phases/14-auth-retirement-integration-seam/14-RESEARCH.md
@.planning/phases/14-auth-retirement-integration-seam/14-PATTERNS.md
@.planning/phases/14-auth-retirement-integration-seam/14-01-SUMMARY.md

<interfaces>
The intake denial pattern to clone (EXACT-status discipline, dependency_overrides, real-router-over-testcontainer), from backend/tests/test_intake_cross_tenant.py:
  - pytestmark = pytest.mark.integration; pytest.importorskip guards so the file collects on the dev box.
  - _build_app() mounts the REAL routers; app.dependency_overrides fabricates identity per case.
  - EXACT status assertions: assert resp.status_code == 404 (never `in (403, 404)`); assert foreign id/space not in resp.text.
  - Two-space seeding + _cleanup_spaces teardown.

The Tribunal two-tenant harness to extend, from tribunal/nestor_pulse_sdk/tests/test_rls_isolation.py:
  - pytestmark = pytest.mark.integration; skips when DATABASE_URL is unset (runs against live/CI Postgres, asyncpg).
  - isolated_two_tenants fixture: INSERT two org rows directly (org is NOT RLS-scoped); CASCADE-delete on teardown.
  - set_tenant_context(session, tenant) from nestor_pulse_sdk.db.rls; assert tenant_a NEVER sees tenant_b rows.

The seam surface under test (Plan 01): InternalCallerProvider + get_internal_claims; POST /api/orgs/ensure, POST /api/projects/ensure.
The missing-tenant-header rejection status code is recorded in 14-01-SUMMARY.md — use that EXACT code in the missing_tenant case.

The CI gates (D-08, both must stay green):
  - intake: cloudbuild.test.yaml runs `pytest backend/tests -m integration` against pgvector Postgres.
  - tribunal: tribunal/cloudbuild.test.yaml (full suite) + tribunal/cloudbuild.test-critical.yaml (phase-critical subset).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seam-level denial suite (intake pg8000 harness)</name>
  <read_first>
    - backend/tests/test_intake_cross_tenant.py (the full clone target — fixtures, _build_app, EXACT-status discipline, skip-clean guards)
    - backend/tests/conftest.py (the intake test harness fixtures: engine, set_space, two_spaces, superadmin_engine)
    - backend/app/research/tribunal_client.py (Plan 02 — the client whose header contract the test exercises)
    - tribunal/nestor_pulse_sdk/auth/internal_caller.py (Plan 01 — InternalCallerProvider + get_internal_claims: the provider under test)
    - .planning/phases/14-auth-retirement-integration-seam/14-RESEARCH.md § Pattern 4 (seam cases) + Pitfall 1 (GUC-leak firewall)
    - .planning/phases/14-auth-retirement-integration-seam/14-01-SUMMARY.md (the pinned missing-tenant-header status code)
  </read_first>
  <action>
    Create `backend/tests/test_tribunal_seam_denial.py` cloning the shape of `test_intake_cross_tenant.py`: `pytestmark = pytest.mark.integration`; `pytest.importorskip` guards (`app.research.tribunal_client`, `httpx`, `fastapi`, and the Tribunal `internal_caller` module) so the file COLLECTS on the dev box without erroring. Build a small FastAPI app that mounts the InternalCallerProvider path (get_internal_claims dependency + the two /ensure endpoints, or a minimal stand-in router that exercises get_internal_claims) and drive it with a `fastapi.testclient.TestClient`, mocking `google.oauth2.id_token.verify_oauth2_token` to fabricate the decoded claims per case (mirrors how the intake suite overrides `get_current_identity` — the OIDC verify is the one boundary that cannot run locally).
    Assert these cases with EXACT status codes (never `in (403, 404)`):
    - missing_tenant: valid caller token but NO `X-Nestor-Tenant-Id` header -> the EXACT status recorded in 14-01-SUMMARY (400 or 401); assert no space-B data is returned and the body leaks no foreign id.
    - wrong_sa: verify_oauth2_token returns an email != the intake SA -> EXACTLY 403.
    - unauthenticated: no Authorization bearer header -> EXACTLY 401.
    - guc_leak: a request carrying space-A's tenant header must never return space-B Tribunal rows — because the seam is HTTP-only with no shared DB session, prove that setting the space-A tenant header (and querying) never surfaces a space-B row (the Pitfall 1/2 firewall). Where a live tribunal.* row is needed, seed via the tribunal-side harness marker or assert at the provider/claims layer that the tenant used for the DB context is exactly the verified header value and nothing else.
    Copy the `_cleanup` teardown discipline. Keep every assertion EXACT and include the foreign-id-not-in-body check where a body is returned.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "import ast; s=open('tests/test_tribunal_seam_denial.py').read(); t=ast.parse(s); tests=[n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name.startswith('test_')]; assert any('missing_tenant' in n for n in tests) and any('wrong_sa' in n for n in tests) and any('unauth' in n for n in tests) and any('guc_leak' in n or 'leak' in n for n in tests), tests; assert 'pytest.mark.integration' in s and 'in (403, 404)' not in s; print(tests)"</automated>
  </verify>
  <acceptance_criteria>
    - `test_tribunal_seam_denial.py` exists with test cases covering missing-tenant, wrong-SA (403), unauthenticated (401), and GUC-leak.
    - Every status assertion is EXACT (`== <code>`); the string `in (403, 404)` never appears.
    - The file collects on the dev box (importorskip guards) and runs under `pytest -m integration` in Cloud Build.
    - The missing-tenant case asserts the EXACT status code recorded in 14-01-SUMMARY.
    - Where a response body is returned, the test asserts no foreign space/tenant id leaks into it.
  </acceptance_criteria>
  <done>The seam-level denial suite proves the InternalCallerProvider rejects missing/mismatched tenant, wrong-SA, and unauthenticated callers, and that a space-A header never surfaces space-B data.</done>
</task>

<task type="auto">
  <name>Task 2: tribunal.* cross-tenant RLS denial (asyncpg harness)</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/tests/test_rls_isolation.py (the two-tenant asyncpg harness to extend — isolated_two_tenants fixture, set_tenant_context, the headline zero-rows assertion)
    - tribunal/nestor_pulse_sdk/db/rls.py (set_tenant_context)
    - tribunal/nestor_pulse_sdk/db/models (Org / Project / Run tenant-scoped models)
    - tribunal/cloudbuild.test-critical.yaml (the phase-critical CI subset — note test_rls_isolation is EXCLUDED there because it connects as a superuser; a faithful RLS test needs a non-superuser DSN)
    - .planning/phases/14-auth-retirement-integration-seam/14-CONTEXT.md D-08 (each layer tested in its native harness, no driver mixing)
  </read_first>
  <action>
    Create `tribunal/nestor_pulse_sdk/tests/test_seam_rls_denial.py` extending the `test_rls_isolation.py` shape: `pytestmark = pytest.mark.integration`; skip cleanly when `DATABASE_URL` is unset; reuse the `isolated_two_tenants` two-org seeding pattern (INSERT two org rows directly; CASCADE-delete on teardown). Add a test that seeds one project (and optionally one run) per tenant under each tenant's `set_tenant_context`, then proves a session set to tenant_a NEVER sees tenant_b's `project` (and `run`) rows — the headline cross-tenant RLS assertion applied to the seam-provisioned tables (org.id == space_id). Also assert the no-tenant-context case denies (unset `app.tenant_id` returns zero rows or raises the GUC/uuid-cast error), mirroring `test_no_tenant_context_returns_empty`.
    IMPORTANT (RLS faithfulness): this test must run as a NON-superuser role for RLS to apply. The existing critical-subset harness (`cloudbuild.test-critical.yaml`) connects as the Postgres superuser and therefore EXCLUDES `test_rls_isolation.py`. Author this new test to be faithful: guard it to run only when a non-superuser `DATABASE_URL` is provided (skip-clean otherwise, e.g. detect and skip if `current_user` is a superuser, mirroring the exclusion note), so it is meaningful under the full-suite `tribunal/cloudbuild.test.yaml` (testcontainers) run. Record in the SUMMARY which CI config exercises it (full suite vs critical subset) and why.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; python -c "import ast; s=open('nestor_pulse_sdk/tests/test_seam_rls_denial.py').read(); t=ast.parse(s); tests=[n.name for n in ast.walk(t) if isinstance(n,ast.AsyncFunctionDef) and n.name.startswith('test_')]; assert tests, tests; assert 'set_tenant_context' in s and 'pytest.mark.integration' in s; print(tests)"</automated>
  </verify>
  <acceptance_criteria>
    - `test_seam_rls_denial.py` exists in the Tribunal suite (asyncpg harness, no pg8000 driver mixing).
    - It proves tenant_a never sees tenant_b's project/run rows and that an unset tenant context denies.
    - It is skip-clean without `DATABASE_URL` and guarded to be faithful (non-superuser) so RLS actually applies.
    - The SUMMARY records which Cloud Build config runs it (full-suite testcontainers) and the superuser-exclusion rationale.
  </acceptance_criteria>
  <done>Cross-tenant RLS denial on the seam-provisioned tribunal.* tables is proven in Tribunal's native asyncpg harness, faithful to a non-superuser role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intake -> Tribunal HTTP seam | The GUC-name mismatch firewall: no shared DB session; tenant comes only from the verified header |
| tribunal.* RLS | `app.tenant_id` GUC enforces per-tenant isolation at the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Info Disclosure | GUC-name mismatch across the boundary | mitigate | Seam GUC-leak test proves space-A header never returns space-B rows; HTTP-only seam, no shared session (Task 1) |
| T-14-10 | Info Disclosure / Elevation | cross-tenant read on tribunal.* | mitigate | asyncpg two-tenant RLS denial test on project/run tables + no-context denial (Task 2) |
| T-14-11 | Spoofing | wrong-SA / unauthenticated seam caller | mitigate | EXACT 403/401 assertions on the InternalCallerProvider path (Task 1) |
| T-14-SC | Tampering | pip installs | mitigate | No new package — pytest already present in both harnesses. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- Intake seam denial suite runs under `pytest backend/tests -m integration` via `cloudbuild.test.yaml` (Task 1).
- Tribunal RLS denial runs under `tribunal/cloudbuild.test.yaml` (full suite, testcontainers, non-superuser) (Task 2).
- Both Cloud Build configs green together form the SEAM-02 CI gate (D-08).
- These are authored by construction; the live gate run is invoked in the Plan 04 operator session (dev box has no Docker/Python).
</verification>

<success_criteria>
- Seam denial suite asserts EXACT status codes for missing-tenant, wrong-SA (403), unauthenticated (401), and the GUC-leak firewall (SEAM-02).
- tribunal.* two-tenant RLS denial proven in the native asyncpg harness, faithful to a non-superuser role.
- No driver mixing between harnesses (D-08); both Cloud Build test configs remain the CI gate.
</success_criteria>

<output>
Create `.planning/phases/14-auth-retirement-integration-seam/14-03-SUMMARY.md` when done. Record which Cloud Build config exercises each new test file and the exact `-k` selectors for the four seam cases (Plan 04's live run references them).
</output>
