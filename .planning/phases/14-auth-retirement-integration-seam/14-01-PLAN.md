---
phase: 14-auth-retirement-integration-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tribunal/nestor_pulse_sdk/auth/internal_caller.py
  - tribunal/nestor_pulse_sdk/auth/deps.py
  - tribunal/nestor_pulse_sdk/auth/__init__.py
  - tribunal/nestor_pulse_sdk/server.py
  - tribunal/nestor_pulse_sdk/orgs/provision.py
  - tribunal/nestor_pulse_sdk/orgs/api.py
  - tribunal/nestor_pulse_sdk/orgs/__init__.py
  - tribunal/nestor_pulse_sdk/auth/identity_platform.py
  - tribunal/nestor_pulse_sdk/account/__init__.py
  - tribunal/nestor_pulse_sdk/account/api.py
  - tribunal/nestor_pulse_sdk/demo/__init__.py
  - tribunal/nestor_pulse_sdk/demo/api.py
  - tribunal/nestor_pulse_sdk/demo/fixtures.py
  - tribunal/nestor_pulse_sdk/tests/test_internal_caller.py
  - tribunal/requirements.txt
autonomous: true
requirements: [SEAM-01, SEAM-02]

must_haves:
  truths:
    - "The Tribunal API installs InternalCallerProvider at startup (not IdentityPlatformProvider)"
    - "InternalCallerProvider verifies the Google-signed OIDC token (aud == Tribunal service URL, caller email == intake runtime SA) before trusting any tenant"
    - "The provider maps X-Nestor-Tenant-Id / X-Acting-User-Id / X-Acting-User-Email into the EXISTING AuthClaims fields (tenant_id / app_user_id / email) with no new or renamed field"
    - "POST /api/orgs/ensure and POST /api/projects/ensure provision org+project idempotently (org.id == space_id)"
    - "The retired surfaces (/api/me, /api/orgs/bootstrap, /api/auth/config, /app static, demo router) are absent from the app route table"
    - "firebase-admin is removed from tribunal/requirements.txt and no surviving-code module imports firebase_admin"
  artifacts:
    - path: "tribunal/nestor_pulse_sdk/auth/internal_caller.py"
      provides: "InternalCallerProvider (AuthProvider impl) + get_internal_claims header-reading dependency"
      contains: "class InternalCallerProvider"
    - path: "tribunal/nestor_pulse_sdk/orgs/provision.py"
      provides: "ensure_org / ensure_project idempotent get-or-create (firebase + app_user creation stripped)"
      contains: "async def ensure_org"
    - path: "tribunal/nestor_pulse_sdk/server.py"
      provides: "InternalCallerProvider install + dependency_overrides[get_current_user] + stripped router mounts"
      contains: "InternalCallerProvider"
  key_links:
    - from: "tribunal/nestor_pulse_sdk/server.py"
      to: "set_auth_provider"
      via: "startup install of InternalCallerProvider"
      pattern: "set_auth_provider\\(InternalCallerProvider"
    - from: "tribunal/nestor_pulse_sdk/auth/internal_caller.py"
      to: "google.oauth2.id_token.verify_oauth2_token"
      via: "OIDC verification (aud + caller email)"
      pattern: "verify_oauth2_token"
    - from: "tribunal/nestor_pulse_sdk/orgs/api.py"
      to: "ensure_org"
      via: "the /api/orgs/ensure endpoint calls the salvaged provisioner"
      pattern: "ensure_org"
---

<objective>
Retire Tribunal's standalone identity surface in the in-repo copy (`tribunal/`) and replace it with a single `InternalCallerProvider` installed at the existing `set_auth_provider()` swap point — turning the Tribunal API into a strictly-internal engine only the intake backend can drive (SEAM-01). Salvage the org/project provisioning internals into idempotent `ensure_org`/`ensure_project` reachable via two seam endpoints (SEAM-02 server side).

Purpose: This is the SEAM-01 keystone. The retirement is a provider SWAP (the D-10 abstraction working as designed), not surgery on the proven RLS boundary. `get_db_session` and `AuthClaims` stay untouched; only the provider and the router mounts change.

Output: New `internal_caller.py`; salvaged `provision.py` (`ensure_org`/`ensure_project`); new `/api/orgs/ensure` + `/api/projects/ensure` endpoints; stripped `server.py`; deleted `identity_platform.py`, `account/`, `demo/`, orgs bootstrap endpoint, `/api/auth/config`, `/app` static mount; `firebase-admin` removed from requirements.

Copy-only scope (D-01, HARD): Every deletion/edit here is in `tribunal/` ONLY. The original app at `C:\Users\ajimimo\Desktop\MOELD\Nestor\` is a frozen reference and is NEVER touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-auth-retirement-integration-seam/14-CONTEXT.md
@.planning/phases/14-auth-retirement-integration-seam/14-RESEARCH.md
@.planning/phases/14-auth-retirement-integration-seam/14-PATTERNS.md

<interfaces>
The frozen AuthClaims to reuse (D-05 — NEVER add/rename a field; the audit canonical_json is frozen), from tribunal/nestor_pulse_sdk/auth/provider.py:
  AuthClaims(app_user_id, tenant_id, email, raw_provider_user_id) — frozen dataclass.
  Map: X-Acting-User-Id -> app_user_id; X-Nestor-Tenant-Id -> tenant_id (== space_id); X-Acting-User-Email -> email; raw_provider_user_id = "intake-seam".
  AuthError(msg, status_code=401); AuthProvider ABC requires verify_id_token / lookup_user / sign_out.

The swap point + RLS boundary to leave UNTOUCHED, from tribunal/nestor_pulse_sdk/auth/deps.py:
  set_auth_provider(provider) at line 37 (single global bind point).
  get_current_user(request) extracts the bearer token and calls provider.verify_id_token(token).
  get_db_session(user=Depends(get_current_user)) does SET LOCAL app.tenant_id = user.tenant_id — DO NOT TOUCH.
  get_auth_provider() (deps.py:51-67) lazily falls back to IdentityPlatformProvider() — that fallback must be removed when identity_platform.py is deleted.

The proven dependency_overrides mechanism to mirror for header threading, from server.py:116-124:
  app.dependency_overrides[get_current_user] = dev_claims  (LOCAL_DEV_AUTH) — mirror this for get_internal_claims.

OIDC verify core (Google first-party — do NOT hand-roll JWKS): google.oauth2.id_token.verify_oauth2_token(token, google.auth.transport.requests.Request(), TRIBUNAL_SERVICE_URL). Audience = service URL WITHOUT a path (Pitfall 4). Then assert info["email"] == intake SA email and info["email_verified"].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the D-03 import-graph gate, then delete the retired identity surface</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/server.py (router mounts + /app static block + /api/auth/config inline route)
    - tribunal/nestor_pulse_sdk/auth/__init__.py (eagerly imports IdentityPlatformProvider — an importer to fix)
    - tribunal/nestor_pulse_sdk/auth/deps.py (get_auth_provider lazy fallback to IdentityPlatformProvider)
    - tribunal/nestor_pulse_sdk/auth/identity_platform.py (DELETE target; _ensure_initialized is imported by orgs/api.py)
    - tribunal/nestor_pulse_sdk/account/api.py + account/__init__.py (DELETE targets)
    - tribunal/nestor_pulse_sdk/demo/api.py + demo/__init__.py + demo/fixtures.py (DELETE targets)
    - tribunal/nestor_pulse_sdk/orgs/api.py (bootstrap endpoint DELETE; get_unscoped_token uses firebase_admin)
    - tribunal/requirements.txt (firebase-admin line)
    - .planning/phases/14-auth-retirement-integration-seam/14-PATTERNS.md ("No Analog Found" delete-inventory table)
  </read_first>
  <action>
    FIRST run the D-03 import-graph gate (MANDATORY before every deletion): grep the surviving SDK tree for importers of each module being deleted — `from nestor_pulse_sdk.account`, `from nestor_pulse_sdk.demo`, `from nestor_pulse_sdk.auth.identity_platform`, and `firebase_admin` / `firebase-admin` — excluding the modules being deleted themselves and their dedicated tests. Known importers that MUST be fixed in this task (not left dangling): (a) `auth/__init__.py` eagerly does `from nestor_pulse_sdk.auth.identity_platform import IdentityPlatformProvider` — remove that import and drop `IdentityPlatformProvider` from `__all__`; (b) `auth/deps.py::get_auth_provider` lazy-imports `IdentityPlatformProvider` as a fallback — replace that fallback with a hard failure (raise RuntimeError "no AuthProvider installed — call set_auth_provider()") so a missing install is loud, never a silent Firebase path; (c) `orgs/api.py` imports `ensure_org_for_user` + `identity_platform._ensure_initialized` — that whole bootstrap module is replaced in Task 3, so its firebase deletion is handled there. Anything `pipeline/` (or other kept engine code) imports STAYS — if the gate surfaces such an importer, STOP and report it (do not delete).

    THEN hard-delete (D-02/D-03, git history is the recovery path): `tribunal/nestor_pulse_sdk/auth/identity_platform.py`; `tribunal/nestor_pulse_sdk/account/` (both files); `tribunal/nestor_pulse_sdk/demo/` (all three files).

    In `server.py`, STRIP: the `account_router` import + `include_router`; the `if DEMO_MODE:` / `else:` structure collapsed to the non-demo body (drop the `demo_router` import + the `DEMO_MODE` branch); the inline `GET /api/auth/config` route; the `/app` StaticFiles mount + `_no_store_static` middleware + `/` RedirectResponse block; and the `orgs_router` bootstrap include (the new ensure router is added in Task 3). In `requirements.txt`, REMOVE the `firebase-admin==6.9.0` line.

    `auth/local_dev.py` fate (discretion): KEEP it — `dev_claims` still constructs a valid `AuthClaims`, imports nothing retired, and is used by `scripts/seed_local_dev.py`; the LOCAL_DEV_AUTH override block in server.py stays. Do NOT touch `pipeline/`, `runs/`, `audit/`, `citations/`, `uploads/`, `health/`, `projects/` beyond what Task 3 changes.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; test ! -e nestor_pulse_sdk/auth/identity_platform.py &amp;&amp; test ! -d nestor_pulse_sdk/account &amp;&amp; test ! -d nestor_pulse_sdk/demo &amp;&amp; ! grep -Eqn "account_router|demo_router|StaticFiles|api/auth/config" nestor_pulse_sdk/server.py &amp;&amp; ! grep -q "firebase-admin" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `identity_platform.py`, `account/`, and `demo/` do not exist under `tribunal/nestor_pulse_sdk/`.
    - No surviving non-test `.py` under `tribunal/nestor_pulse_sdk/` imports `firebase_admin`; `firebase-admin` is absent from `tribunal/requirements.txt`.
    - `server.py` references no `account_router`, `demo_router`, `StaticFiles`, `/api/auth/config`, or `/app` mount.
    - `auth/__init__.py` no longer imports or exports `IdentityPlatformProvider`.
    - `auth/deps.py::get_auth_provider` raises RuntimeError when no provider is installed (no IdentityPlatformProvider import).
    - The import-graph gate output is captured in the SUMMARY showing no surviving-code importer of a deleted module.
  </acceptance_criteria>
  <done>The retired identity/eval surface is hard-deleted, firebase-admin is gone from requirements, no surviving code imports a deleted module, and the import-graph gate ran and passed before deletion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement InternalCallerProvider + the header-reading dependency</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/auth/provider.py (AuthProvider ABC, frozen AuthClaims, AuthError — the contracts)
    - tribunal/nestor_pulse_sdk/auth/deps.py (get_current_user extracts the bearer token; set_auth_provider bind point)
    - tribunal/nestor_pulse_sdk/orgs/api.py (get_unscoped_token lines 78-86 — the bearer split-max-1 parse pattern to copy)
    - tribunal/nestor_pulse_sdk/auth/middleware.py (auth_exception_handler translates AuthError to HTTP; installed server.py:84)
    - .planning/phases/14-auth-retirement-integration-seam/14-RESEARCH.md § Pattern 1 + § Code Examples (verify core; Pitfall 4 aud-without-path; Pitfall 6 header threading)
  </read_first>
  <behavior>
    - Test: token whose decoded email == intake SA email + email_verified true, with the three headers present, yields AuthClaims(tenant_id=<space_id>, app_user_id=<acting id>, email=<acting email>, raw_provider_user_id="intake-seam").
    - Test: token whose decoded email != intake SA email raises AuthError(status_code=403).
    - Test: verify_oauth2_token raising ValueError (bad aud/sig/expiry) is translated to AuthError(status_code=401).
    - Test: request missing X-Nestor-Tenant-Id raises AuthError — pin the status (400 or 401) and keep it consistent with Plan 03's denial test.
    - Test: email_verified missing/false raises AuthError(status_code=403).
    - Mock google.oauth2.id_token.verify_oauth2_token (no network) so tests run in the keyless Cloud Build env.
  </behavior>
  <action>
    Create `tribunal/nestor_pulse_sdk/auth/internal_caller.py` with `class InternalCallerProvider(AuthProvider)`: `__init__(self, service_url, allowed_caller_email)` stores `self._aud = service_url` (Tribunal service URL WITHOUT a path — Pitfall 4), `self._caller = allowed_caller_email` (intake runtime SA email `nestor-run@<project>.iam.gserviceaccount.com`), and constructs `google.auth.transport.requests.Request()` once. `verify_id_token(self, token)` calls `google.oauth2.id_token.verify_oauth2_token(token, self._transport, self._aud)`; on any exception raise `AuthError("invalid internal caller token", status_code=401)`; then require `info.get("email") == self._caller and info.get("email_verified")` else `AuthError("caller is not the intake backend", status_code=403)`. This method validates the CALLER only. `lookup_user` and `sign_out` return `None`.

    Thread the tenant/acting-user headers via `async def get_internal_claims(request) -> AuthClaims` in the same module (Pitfall 6): parse the bearer token with the split-max-1 pattern from orgs/api.py::get_unscoped_token; call the installed provider's `verify_id_token(token)` (via get_auth_provider()) to validate the caller; read `X-Nestor-Tenant-Id` (required — missing raises the AuthError status you pinned in <behavior>), `X-Acting-User-Id`, `X-Acting-User-Email`; return `AuthClaims(tenant_id=<tenant header>, app_user_id=<acting id>, email=<acting email>, raw_provider_user_id="intake-seam")`. Reuse EXISTING AuthClaims fields ONLY (D-05). The three header-name constants live here and MUST match Plan 02's client verbatim: `X-Nestor-Tenant-Id`, `X-Acting-User-Id`, `X-Acting-User-Email`.

    Add `tribunal/nestor_pulse_sdk/tests/test_internal_caller.py` implementing the <behavior> cases with `verify_oauth2_token` mocked and the sibling tests' pytestmark/importorskip skip-clean guards.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; python -c "import ast; s=open('nestor_pulse_sdk/auth/internal_caller.py').read(); t=ast.parse(s); names=[n.name for n in ast.walk(t) if isinstance(n,(ast.ClassDef,ast.AsyncFunctionDef))]; assert 'InternalCallerProvider' in names and 'get_internal_claims' in names, names; assert 'verify_oauth2_token' in s and 'intake-seam' in s; print('ok')" &amp;&amp; test -f nestor_pulse_sdk/tests/test_internal_caller.py</automated>
  </verify>
  <acceptance_criteria>
    - `InternalCallerProvider` subclasses `AuthProvider` and implements all three abstract methods.
    - `verify_id_token` calls `google.oauth2.id_token.verify_oauth2_token` with audience = the service URL (no path), rejects a non-matching caller email with 403 and a bad token with 401.
    - `get_internal_claims` maps the three headers into the EXISTING AuthClaims fields and sets `raw_provider_user_id="intake-seam"`; it declares NO new AuthClaims field.
    - `test_internal_caller.py` exists; its behavior cases pass under Cloud Build (full-suite gate wired in Plan 03 / Plan 04).
    - A request with no `X-Nestor-Tenant-Id` raises AuthError before any tenant is trusted.
  </acceptance_criteria>
  <done>InternalCallerProvider verifies the intake backend's OIDC token and threads tenant + acting-user headers into the frozen AuthClaims shape; unit tests cover accept + wrong-SA/bad-token/missing-tenant denials.</done>
</task>

<task type="auto">
  <name>Task 3: Salvage ensure_org/ensure_project + the /ensure endpoints; install the provider</name>
  <read_first>
    - tribunal/nestor_pulse_sdk/orgs/provision.py (ensure_org_for_user — the get-or-create + flush-before-FORCED-child ordering to KEEP; the _firebase_set_claims call + app_user creation to STRIP)
    - tribunal/nestor_pulse_sdk/orgs/api.py (bootstrap endpoint to delete; the Project-id lookup lines 200-213 to salvage)
    - tribunal/nestor_pulse_sdk/projects/api.py (lines 29-34, 109-133 — the get_current_user + get_db_session router wiring to copy)
    - tribunal/nestor_pulse_sdk/orgs/__init__.py (re-exports to update)
    - tribunal/nestor_pulse_sdk/server.py (provider install point + router mounts; mirror the LOCAL_DEV_AUTH override for get_internal_claims)
    - tribunal/nestor_pulse_sdk/auth/internal_caller.py (Task 2 output)
    - tribunal/nestor_pulse_sdk/db/rls.py (set_tenant_context — the RLS helper the salvaged code calls)
  </read_first>
  <action>
    In `orgs/provision.py`, refactor `ensure_org_for_user` into two idempotent functions with the firebase + app_user machinery STRIPPED (Pitfall 2 — intake owns users; there is no app_user in the seam): `async def ensure_org(*, space_id, email, session) -> str` — `tenant_uuid = uuid.UUID(space_id)`; get-or-create `Org(id=tenant_uuid, name=..., slug=...)` (id == space_id, identity mapping); KEEP the `await session.flush()` then `await set_tenant_context(session, str(tenant_uuid))` ordering (Org is not RLS-scoped; children are FORCED). REMOVE `_firebase_set_claims` (module helper + call site) and the entire `User`/app_user get-or-create block. Derive org name/slug from `email` if present else a stable default (`f"Space {space_id[:8]}"`); the `_email_to_slug`/`_email_to_org_name` helpers may stay. Return the space_id.
    `async def ensure_project(*, space_id, session) -> str` — under the tenant context, `select(Project.id).where(Project.tenant_id == uuid.UUID(space_id)).limit(1)` (salvage from orgs/api.py:200-213); if none, create `Project(tenant_id=uuid.UUID(space_id), name="Research", status="active", owner_user_id=None)` (owner nullable — no app_user), flush, return `str(project_id)`. One project per space, idempotent, discoverable by tenant_id (D-06 discretion — do NOT persist a project_id intake-side; return it for Phase 16).

    In `orgs/api.py`, DELETE `bootstrap_org`, `get_unscoped_token`, `_get_unscoped_session`. Replace the router body with two endpoints copying projects/api.py dependency wiring (`user: AuthClaims = Depends(get_current_user)`, `session = Depends(get_db_session)` — the standard scoped session works because InternalCallerProvider supplies a verified tenant_id): `POST /api/orgs/ensure` calls `ensure_org(space_id=user.tenant_id, email=user.email, session=session)` and returns `{"tenant_id": <space_id>}`; `POST /api/projects/ensure` calls `ensure_org(...)` then `ensure_project(space_id=user.tenant_id, session=session)` and returns `{"tenant_id": <space_id>, "project_id": <project_id>}`. Update `orgs/__init__.py` re-exports to the new names.

    In `server.py`, install the provider at startup (mirror LOCAL_DEV_AUTH server.py:116-124): `set_auth_provider(InternalCallerProvider(service_url=os.environ["TRIBUNAL_SERVICE_URL"], allowed_caller_email=os.environ["INTAKE_RUNTIME_SA_EMAIL"]))` then `app.dependency_overrides[get_current_user] = get_internal_claims`. Keep the LOCAL_DEV_AUTH override as an ELSE branch (if LOCAL_DEV_AUTH use dev_claims, else install InternalCallerProvider + get_internal_claims). Add the new orgs `/ensure` router include (replacing the deleted bootstrap include). Both env vars (`TRIBUNAL_SERVICE_URL`, `INTAKE_RUNTIME_SA_EMAIL`) are NON-secret (a service URL + an SA email); record them in the SUMMARY as deploy-handoff env vars for Plan 04 to wire onto the tribunal-api service.
  </action>
  <verify>
    <automated>cd tribunal &amp;&amp; python -c "import ast; s=open('nestor_pulse_sdk/orgs/provision.py').read(); t=ast.parse(s); fns=[n.name for n in ast.walk(t) if isinstance(n,ast.AsyncFunctionDef)]; assert 'ensure_org' in fns and 'ensure_project' in fns, fns; assert '_firebase_set_claims' not in s; print('ok')" &amp;&amp; grep -q "/ensure" nestor_pulse_sdk/orgs/api.py &amp;&amp; grep -q "set_auth_provider(InternalCallerProvider" nestor_pulse_sdk/server.py &amp;&amp; grep -q "get_internal_claims" nestor_pulse_sdk/server.py</automated>
  </verify>
  <acceptance_criteria>
    - `ensure_org` and `ensure_project` exist in `orgs/provision.py`, are idempotent, set org.id = space_id, and contain no `_firebase_set_claims` and no app_user creation.
    - `POST /api/orgs/ensure` and `POST /api/projects/ensure` exist; `bootstrap_org` / `get_unscoped_token` are gone from `orgs/api.py`.
    - `server.py` installs `InternalCallerProvider` via `set_auth_provider(...)` and overrides `get_current_user` with `get_internal_claims`, keeping the LOCAL_DEV_AUTH branch intact.
    - `ensure_project` returns the `project_id`; no new intake-side column or persistence is added (D-06 boundary).
    - SUMMARY records `TRIBUNAL_SERVICE_URL` + `INTAKE_RUNTIME_SA_EMAIL` as non-secret env vars for Plan 04 to set on tribunal-api.
  </acceptance_criteria>
  <done>The seam endpoints provision org+project idempotently under the verified internal caller, and the InternalCallerProvider is installed at app startup with the header-reading dependency wired in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intake backend -> Tribunal API (HTTP seam) | The ONLY authenticated caller; untrusted-until-verified OIDC token + headers cross here |
| Tribunal API -> tribunal.* DB (RLS) | `get_db_session` sets `app.tenant_id` from the verified claims; unchanged Phase-13 boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Spoofing | `InternalCallerProvider.verify_id_token` | mitigate | `verify_oauth2_token` checks aud == Tribunal URL (no path) + email == intake SA + email_verified (Task 2); a forged/browser token fails |
| T-14-02 | Elevation | mis-set caller identity | mitigate | In-app OIDC re-verification is the inner gate (D-04); it does not trust IAM alone (IAM binding is Plan 04) |
| T-14-03 | Tampering / Info Disclosure | forged `X-Nestor-Tenant-Id` for another space | mitigate | tenant read only from the verified internal caller's header; RLS `app.tenant_id` enforces at DB; missing header rejected before any query (Task 2) |
| T-14-04 | Tampering (repudiation of legal record) | audit `canonical_json` field rename | mitigate | D-05 hard constraint — acting-user maps into EXISTING `app_user_id`/`email` fields only; no new/renamed AuthClaims field (Task 2 acceptance grep) |
| T-14-05 | Elevation | stale image importing a deleted module | mitigate | D-03 import-graph gate before deletion; get_auth_provider raises instead of silently loading IdentityPlatformProvider (Task 1) |
| T-14-SC | Tampering | npm/pip installs | mitigate | This plan installs NO new package (net-negative: removes firebase-admin). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- Both retired-surface deletions verified by absence (test -e / grep) — Task 1.
- InternalCallerProvider accept + wrong-SA/bad-token/missing-tenant denials pass in the Tribunal suite (via Cloud Build; the full-suite CI gate is wired in Plan 04) — Task 2.
- Seam endpoints + provider install verified by structure grep — Task 3.
- The Tribunal-side RLS denial test on `tribunal.*` tables is EXTENDED in Plan 03 (D-08 native harness).
- Live proof (real server-to-server run + negative proofs) is the D-07 operator checkpoint in Plan 04.
</verification>

<success_criteria>
- `InternalCallerProvider` installed at `set_auth_provider()`; `identity_platform.py`, `account/`, `demo/`, orgs bootstrap, `/api/auth/config`, `/app` mount deleted; `firebase-admin` removed (SEAM-01).
- `ensure_org`/`ensure_project` idempotent with `org.id == space_id`; `/api/orgs/ensure` + `/api/projects/ensure` reachable through the verified internal caller (SEAM-02 server side).
- No new/renamed `AuthClaims` field (audit hash-chain preserved).
- `get_db_session` and the RLS boundary are unmodified.
</success_criteria>

<output>
Create `.planning/phases/14-auth-retirement-integration-seam/14-01-SUMMARY.md` when done. Record: the import-graph gate output; the two non-secret env vars (`TRIBUNAL_SERVICE_URL`, `INTAKE_RUNTIME_SA_EMAIL`) Plan 04 must set on tribunal-api; the exact status code chosen for the missing-tenant-header rejection (Plan 03 must match it).
</output>
