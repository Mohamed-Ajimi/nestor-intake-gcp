---
phase: 18-human-report-upload-client-delivery
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - frontend/src/routes/intake.$id.report.tsx
  - frontend/src/routes/intake.index.tsx
  - frontend/src/routeTree.gen.ts
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/en/intake.json
autonomous: true
requirements: [REPORT-02]

must_haves:
  truths:
    - "A client visiting /intake/{id}/report on a delivered intake sees the report metadata + a download button"
    - "The route redirects to /intake if the intake status is not exactly 'delivered' (REPORT-02 invisibility)"
    - "The route requires an authenticated user (redirect to /auth/login otherwise)"
    - "The intake list shows a 'View report' CTA that opens the report page, only when status == 'delivered'"
    - "Downloading uses a backend-minted signed URL (attachment disposition); no inline PDF viewer"
    - "The page reserves layout space for the future Phase-19 Q&A chat but builds no chat UI"
  artifacts:
    - path: "frontend/src/routes/intake.$id.report.tsx"
      provides: "authenticated, delivered-only client report page (download-only)"
      contains: "getReport"
    - path: "frontend/src/routes/intake.index.tsx"
      provides: "delivered → View report CTA routed to the report page"
      contains: "report"
  key_links:
    - from: "intake.$id.report.tsx"
      to: "getReport(id) + signedDownloadUrl"
      via: "delivered-gated metadata load + signed-URL blob download"
      pattern: "getReport"
    - from: "intake.index.tsx rowCta"
      to: "/intake/$id/report"
      via: "delivered branch → target 'report' → openRow navigate"
      pattern: "report"
---

<objective>
Build the client-facing side of delivery (REPORT-02): a NEW authenticated `intake.$id.report.tsx`
route that renders ONLY when the intake is exactly `delivered` — showing report metadata (filename,
delivered date, size) and a download button backed by the existing signed-URL seam (no inline PDF
viewer, D-08) — plus a "View report" CTA on the client intake list that appears only for `delivered`
intakes and deep-links to the page. The page reserves visual space for the Phase-19 Q&A chat but
builds no chat UI.

Purpose: The client sees and downloads the final report in their own UI, and nothing research-related
is client-visible before delivery. This is the second of the D-09 two entry points (the delivery
email CTA — minted by 18-01 — is the first).
Output: one new route + the list CTA branch + report-page i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-human-report-upload-client-delivery/18-CONTEXT.md
@.planning/phases/18-human-report-upload-client-delivery/18-PATTERNS.md
@.planning/phases/18-human-report-upload-client-delivery/18-02-SUMMARY.md

<interfaces>
<!-- Contracts this plan consumes. -->

Client-route clone target — intake.$id.results.tsx (whole file): authReady() first-tick resolve
(:24-32); Route = createFileRoute("/intake/$id/results")({ beforeLoad: redirect if !user, component });
cancel-flag useEffect load (:76-124); minimal authenticated chrome (brand + email + logout);
StatusPill. CRITICAL DIVERGENCE: the results route gates at `validated_by_client` (a LOWER bar via
isValidatedOrLater). The report route MUST gate at EXACTLY `delivered` (Pitfall 2) — copy the
structure but change the gate to
`if (intakeRes.data.status !== "delivered") navigate({ to: "/intake" })`.

Seam (from 18-02): getIntake(id) → ApiResult<Intake>; getReport(id) → ApiResult<ReportView
{ filename, delivered_at, byte_size, mime_type, storage_path }>. storage.signedDownloadUrl({intakeId,
path, expiresIn}) → ApiResult<{url, expires_in}>. The download blob flow to reuse verbatim:
FinalReportBlock.onDownload (:112-138) — signedDownloadUrl → fetch(url) → blob → anchor click → revoke.

List route — intake.index.tsx: RowCta type (:62) `{ label, target: "fill" | "results" }`;
rowCta(status, t) (:67-72); openRow (:105-112) navigates by target. Add a `report` target + a
`status === "delivered"` branch, and route it in openRow to /intake/$id/report.

Route registration: frontend/src/routeTree.gen.ts is auto-generated by @tanstack/router-plugin from
the routes/ dir. Adding intake.$id.report.tsx regenerates it (the dev server / build does this). If
the generator is not run, register the route by hand mirroring the intake.$id.results.tsx entry.

i18n: intake.json `list.*` (nl:92) has ctaFill/ctaView/ctaResult — add `list.ctaReport`. Add a new
`reportPage.*` block (heading, deliveredOn, size, download, downloading, loadFailed, notAvailable,
backToOverview, brand, logout, chatComingSoon) in all three locales.

Design language (D-07): font-mono uppercase labels, border-ink, bg-paper/paperLight, font-serif
lowercase headings — as in intake.$id.results.tsx and FinalReportBlock's section styling.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New delivered-only client report route (download-only, chat space reserved)</name>
  <read_first>
    - frontend/src/routes/intake.$id.results.tsx (whole file — authReady, Route/beforeLoad, cancel-flag
      load, chrome, the status-gate to CHANGE to exactly delivered)
    - frontend/src/components/intake/FinalReportBlock.tsx (:112-138 onDownload signed-URL blob flow; :16-21 bytesLabel)
    - frontend/src/lib/api/intakes.ts (getIntake, getReport, ReportView from 18-02)
    - frontend/src/lib/api/storage.ts (signedDownloadUrl signature)
    - frontend/src/locales/nl/intake.json (the reportPage.* keys added in Task 2)
  </read_first>
  <action>
    Create `frontend/src/routes/intake.$id.report.tsx` cloning the AUTH + LOAD structure of
    `intake.$id.results.tsx`:
    - Copy `authReady()` verbatim.
    - `export const Route = createFileRoute("/intake/$id/report")({ beforeLoad: async () => { const
      user = await authReady(); if (!user) throw redirect({ to: "/auth/login" }); },
      component: UserIntakeReportPage })`.
    - In the component: a cancel-flag `useEffect` that (a) `getIntake(id)` — on failure set an error;
      (b) GATE EXACTLY: `if (intakeRes.data.status !== "delivered") { navigate({ to: "/intake" });
      return; }` (NOT a rank/>= comparison — Pitfall 2); (c) `getReport(id)` — on failure set error;
      store the ReportView in state (filename, delivered_at, byte_size, mime_type, storage_path) plus
      the client_name for the title.
    - Render (D-08 download-only, NO embedded viewer): the minimal authenticated chrome (brand +
      email + logout, mirror the results route), a header with the intake title + a `StatusPill`
      status="delivered", then a metadata card (font-mono labels): filename, delivered date (format
      `delivered_at` with a plain `toLocaleDateString` using the active i18n locale), size via a local
      `bytesLabel` helper (copy from FinalReportBlock :16-21), and a primary Download button whose
      onClick runs the FinalReportBlock onDownload blob flow (`signedDownloadUrl({ intakeId: id, path:
      report.storage_path, expiresIn: 300 })` → fetch → blob → anchor `download=report.filename` →
      revoke). Toast on failure with `reportPage.loadFailed`.
    - Reserve Phase-19 chat space (D-07 / deferred idea): below the card, render a bordered
      placeholder section with a `reportPage.chatComingSoon` label ONLY (font-mono, muted) — NO input,
      NO message list, NO chat logic. This is layout reservation, not a feature.
    - Loading + error states mirror the results route (skeleton/text + a back-to-overview link using
      `reportPage.backToOverview`).
    Use `useTranslation("intake")` and the `reportPage.*` keys.

    After creating the route, run the route generator so `routeTree.gen.ts` picks it up:
    `cd frontend && npx tsr generate` (or let the build regenerate). If the tool is unavailable,
    hand-edit `routeTree.gen.ts` to register `/intake/$id/report` mirroring the `/intake/$id/results`
    entry (import, route object, and the union types).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `intake.$id.report.tsx` exists with `createFileRoute("/intake/$id/report")` and a `beforeLoad`
      auth redirect
    - Source contains the exact gate `status !== "delivered"` (equality, no STATUS_RANK / >=)
    - Source calls `getReport(id)` and `signedDownloadUrl`; contains NO `<iframe`, `<embed`, or PDF
      viewer component
    - Source contains a `reportPage.chatComingSoon` placeholder and NO chat input/message-list markup
    - `routeTree.gen.ts` registers `/intake/$id/report`
    - `tsc --noEmit` passes
  </acceptance_criteria>
  <done>The client report route renders only for delivered intakes, shows metadata + a signed-URL download, reserves chat space, and requires auth.</done>
</task>

<task type="auto">
  <name>Task 2: List "View report" CTA (delivered only) + report-page i18n</name>
  <read_first>
    - frontend/src/routes/intake.index.tsx (:60-72 RowCta type + rowCta; :105-112 openRow)
    - frontend/src/locales/nl/intake.json (:92 list.* block; the finalReport.* / recipients.* blocks
      for style reference)
    - frontend/src/locales/fr/intake.json and en/intake.json (mirror targets)
  </read_first>
  <action>
    In `frontend/src/routes/intake.index.tsx`:
    - Widen `type RowCta = { label: string; target: "fill" | "results" | "report" }`.
    - In `rowCta(status, t)`, add BEFORE the fallback: `if (status === "delivered") return { label:
      t("list.ctaReport"), target: "report" };` (so a delivered intake shows "View report", not the
      generic result CTA — D-09).
    - In `openRow`, add `if (cta.target === "report") { navigate({ to: "/intake/$id/report", params:
      { id: row.id } }); return; }` before the existing results/fill branching.

    Add `list.ctaReport` (NL e.g. "Bekijk rapport") to the `list` block in all three locale files, and
    a new `reportPage` block in each with keys authored per locale (NL parity source, FR/EN mirror):
      `reportPage.heading` (NL "Jouw rapport"),
      `reportPage.deliveredOn` (NL "Bezorgd op {{date}}"),
      `reportPage.size` (NL "Grootte"),
      `reportPage.download` (NL "Download rapport"),
      `reportPage.downloading` (NL "Downloaden…"),
      `reportPage.loadFailed` (NL "Rapport laden mislukt"),
      `reportPage.notAvailable` (NL "Rapport nog niet beschikbaar"),
      `reportPage.backToOverview` (NL "Terug naar overzicht"),
      `reportPage.brand` (NL "Agenic"),
      `reportPage.logout` (NL "Uitloggen"),
      `reportPage.chatComingSoon` (NL "Vragen over dit rapport? Binnenkort beschikbaar.").
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `rowCta` returns `target: "report"` for `status === "delivered"`; `openRow` navigates to
      `/intake/$id/report` for that target
    - All three locale files carry `list.ctaReport` and a `reportPage` block with
      heading/download/deliveredOn/chatComingSoon/loadFailed
    - `tsc --noEmit` passes
  </acceptance_criteria>
  <done>The client list shows a delivered-only "View report" CTA into the report page; report-page copy exists in NL/FR/EN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser → API | The report read + signed-URL calls carry only the intake id + the server-issued storage_path |
| route gate → status | The page must be structurally invisible for any non-delivered status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Information Disclosure | Report page visible before delivery | mitigate | Front gate `status !== "delivered"` redirect + backend GET /report 404 (18-01) — defense in depth; list CTA only on delivered |
| T-18-12 | Information Disclosure | Signed URL over-long TTL | mitigate | `expiresIn: 300`; the storage seam clamps ≤ 900s server-side (Phase 9) |
| T-18-13 | Info Disclosure (scope creep) | Chat placeholder leaks findings early | mitigate | Placeholder is a static label only — no data fetch, no chat surface (Phase-19 deferred) |
| T-18-SC | Tampering | npm installs | mitigate | This plan installs NO package (existing router/i18n/date-fns/storage seam only) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes.
- `routeTree.gen.ts` includes `/intake/$id/report`.
- Manual/live UAT (18-04): a delivered intake shows the "View report" CTA; the page downloads the
  PDF; a non-delivered intake redirects away; a logged-out user is bounced to /auth/login.
- No frontend test framework exists (RESEARCH § Wave 0) — gating verified via live UAT in 18-04.
</verification>

<success_criteria>
- The client can reach the report page from the list CTA and the email CTA, but only once delivered.
- The page is download-only (no viewer) and reserves the Phase-19 chat area without building it.
- Any pre-delivery access (page or list CTA) is impossible.
</success_criteria>

<output>
Create `.planning/phases/18-human-report-upload-client-delivery/18-03-SUMMARY.md` when done.
</output>
