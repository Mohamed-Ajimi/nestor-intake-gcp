---
phase: 18-human-report-upload-client-delivery
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - frontend/src/lib/api/intakes.ts
  - frontend/src/lib/intake-phase.ts
  - frontend/src/components/intake/FinalReportBlock.tsx
  - frontend/src/routes/admin.pulse.intakes.$id.tsx
  - frontend/src/locales/nl/intake.json
  - frontend/src/locales/fr/intake.json
  - frontend/src/locales/en/intake.json
autonomous: true
requirements: [REPORT-01, REPORT-03]

must_haves:
  truths:
    - "During in_research the admin intake detail shows the FinalReportBlock (staged-upload UI)"
    - "Uploading a PDF stages it (holds the returned key locally) — status stays in_research, nothing client-visible"
    - "A separate Deliver action opens the RecipientPicker and, on confirm, calls deliverReport → status flips to delivered and the mail is sent"
    - "The file input accepts only .pdf (D-10)"
    - "After delivery the block shows Replace (with optional re-notify) and status stays delivered"
    - "The admin onChange no longer fakes the delivered status client-side — it reloads from the backend view"
  artifacts:
    - path: "frontend/src/lib/api/intakes.ts"
      provides: "deliverReport / replaceReport / getReport + ReportView type"
      contains: "deliverReport"
    - path: "frontend/src/lib/intake-phase.ts"
      provides: "phaseShowsFinalReport includes in_research; derivePhase fed real inputs"
      contains: "in_research"
    - path: "frontend/src/components/intake/FinalReportBlock.tsx"
      provides: "staged-upload + Deliver dialog + Replace + PDF-only, no auto-deliver"
      contains: "RecipientPicker"
  key_links:
    - from: "FinalReportBlock Deliver button"
      to: "deliverReport(intakeId, { storagePath, recipients })"
      via: "RecipientPicker onConfirm"
      pattern: "deliverReport"
    - from: "admin.pulse.intakes.$id.tsx FinalReportBlock onChange"
      to: "backend intake view reload"
      via: "reload the intake seam row (no client-side status fake)"
      pattern: "final_report_artifact_id"
---

<objective>
Awaken the dormant admin report-delivery UI: repair `FinalReportBlock.tsx` from a gated-off stub into
the staged-upload → explicit-Deliver → Replace flow (D-01/D-04/D-05/D-10), extend the frontend intake
API seam with `deliverReport`/`replaceReport`/`getReport`, wire `derivePhase` with the real
`final_report_artifact_id`/`results_link_sent_at` inputs, make `phaseShowsFinalReport` include
`in_research` so the block appears once a run is in research, and replace the admin route's
client-side status fake with a real backend reload.

Purpose: REPORT-01 (superadmin uploads + delivers) and REPORT-03 (delivery mail) on the admin side.
The upload path in the stub already works; the linking, staged/delivered state, Deliver dialog, and
PDF-only tightening are the work.
Output: repaired block + extended seam + phase-machine input wiring + admin wiring fix + i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-human-report-upload-client-delivery/18-CONTEXT.md
@.planning/phases/18-human-report-upload-client-delivery/18-PATTERNS.md
@.planning/phases/18-human-report-upload-client-delivery/18-01-SUMMARY.md

<interfaces>
<!-- Contracts this plan consumes / produces. -->

Backend endpoints (from 18-01): POST /intakes/{id}/deliver { storage_path, recipients } → IntakeView;
POST /intakes/{id}/report/replace { storage_path, recipients } → IntakeView;
GET /intakes/{id}/report → ReportView { filename, delivered_at, byte_size, mime_type, storage_path }.
Wire-format body fields are snake_case (storage_path), NOT camelCase.

Existing seam shapes to mirror (intakes.ts): apiFetch<T>(path, {method, body}); ApiResult<T> union;
submitIntake (:71-73) POST no body; sendIntakeMail(intakeId, type, recipients) (:124-133).
Intake type (:15-24) already carries final_report_artifact_id + results_link_sent_at.

Storage seam (storage.ts): uploadFile({intakeId, file, filename, category:"reports", contentType})
→ ApiResult<UploadedFileMeta{path, filename, size, mime_type}>; signedDownloadUrl({intakeId, path,
expiresIn}) → ApiResult<{url, expires_in}>.

RecipientPicker (RecipientPicker.tsx): props { open, onOpenChange, intakeId, type: IntakeMailType,
busy, onConfirm: (membershipIds: string[]) => void }. type="results" reuses the results copy family.
Loads members on open, preselects all, returns selected membership ids.

Phase machine (intake-phase.ts): derivePhase(intake, latestSkillRun, hasResearchArtifacts).
in_research branch (:65-77): final_report_artifact_id → awaiting_results_send; else in_research.
delivered branch (:79-81): results_link_sent_at ? completed : awaiting_results_send.
phaseShowsFinalReport (:112-119) currently: [awaiting_report_upload, awaiting_results_send,
completed, archived] — MISSING in_research, so the block never mounts during in_research today.

Admin route (admin.pulse.intakes.$id.tsx): derivePhase call (:310-327) already passes the real
intake fields; showFinalReport = phaseShowsFinalReport(currentPhase) (:963); FinalReportBlock mount
(:1457-1476) — the onChange (:1464-1473) fakes the delivered bump client-side and must be replaced.
The local Intake type carries final_report_artifact_id, results_link_sent_at, status.

i18n: frontend/src/locales/{nl,fr,en}/intake.json already have a `finalReport.*` block (nl:388) and
a `recipients.*` block (nl:340). Add delivery keys under `finalReport.*` (deliver, delivering,
delivered, deliverFailed, replaceConfirm, reNotify, staged, stagedHint) in all three locales.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the intake API seam + fix the phase machine inputs/visibility</name>
  <read_first>
    - frontend/src/lib/api/intakes.ts (whole file — the apiFetch pattern, Intake type, submitIntake
      and sendIntakeMail shapes to mirror)
    - frontend/src/lib/intake-phase.ts (whole file — the derivePhase branches + phaseShows* helpers)
    - .planning/phases/18-human-report-upload-client-delivery/18-01-SUMMARY.md (the exact backend
      response shapes)
  </read_first>
  <action>
    In `frontend/src/lib/api/intakes.ts`, add a `ReportView` type
    `{ filename: string | null; delivered_at: string | null; byte_size: number | null;
    mime_type: string | null; storage_path: string | null }` and three functions in the transition
    section, mirroring `submitIntake`/`sendIntakeMail`:
      `deliverReport(id, input: { storagePath: string; recipients: string[] })` →
        `apiFetch<Intake>(\`/intakes/${id}/deliver\`, { method: "POST",
        body: JSON.stringify({ storage_path: input.storagePath, recipients: input.recipients }) })`.
      `replaceReport(id, input: { storagePath: string; recipients: string[] })` →
        POST `/intakes/${id}/report/replace` with the SAME snake_case body (recipients may be []).
      `getReport(id)` → `apiFetch<ReportView>(\`/intakes/${id}/report\`, { method: "GET" })`.
    Keep the JSDoc convention of the surrounding functions (note D-06: recipients are membership ids;
    the wire body is snake_case).

    In `frontend/src/lib/intake-phase.ts`, add `"in_research"` to the `phaseShowsFinalReport` array
    (so the admin block mounts once a run is in research — the operator stages + delivers from
    `in_research`). Do NOT change `derivePhase` logic — the `in_research` branch already returns
    `awaiting_results_send` when `final_report_artifact_id` is set and `in_research` otherwise, which
    is correct. Update the STALE Phase-16 comment block (:65-73) to note Phase 18 now writes
    `final_report_artifact_id` (via the Deliver verb) and `results_link_sent_at` (delivered-mail
    timestamp), and that `in_research` is now a report-delivery-visible phase.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `intakes.ts` exports `deliverReport`, `replaceReport`, `getReport`, and `ReportView`; the wire
      bodies use `storage_path` (not `storagePath`)
    - `phaseShowsFinalReport("in_research") === true` (source: the array now contains "in_research")
    - `tsc --noEmit` passes
  </acceptance_criteria>
  <done>Seam extended with the three verbs + ReportView; phaseShowsFinalReport includes in_research; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Repair FinalReportBlock — staged upload, Deliver dialog, Replace, PDF-only</name>
  <read_first>
    - frontend/src/components/intake/FinalReportBlock.tsx (whole file — keep the upload/download/
      drag-drop/bytesLabel/sanitize helpers; the stubbed useEffect, maybeAutoDeliver, onPick,
      onRemove, and the accept list + STALE comments are the work)
    - frontend/src/components/intake/RecipientPicker.tsx (whole file — props + type="results")
    - frontend/src/lib/api/intakes.ts (deliverReport/replaceReport/getReport from Task 1)
    - frontend/src/lib/api/storage.ts (uploadFile + signedDownloadUrl signatures)
    - frontend/src/locales/nl/intake.json (the existing finalReport.* keys to extend)
  </read_first>
  <action>
    Rewrite the stubbed parts of `FinalReportBlock.tsx` while KEEPING `bytesLabel`,
    `sanitizeFilenameForStorage`, the drag-drop zone, `onDownload` (signed-URL blob flow), and the
    `storage.uploadFile({ category: "reports" })` call.

    State: add `stagedPath: string | null`, `stagedMeta: { filename: string; size: number } | null`,
    `delivering: boolean`, `deliverOpen: boolean`, `replaceOpen: boolean`. Derive `isDelivered` from
    `intakeStatus === "delivered"`.

    - Replace the `useEffect` (:54-59) stub: when `finalReportArtifactId` is set, fetch the report
      metadata via `getReport(intakeId)` and set `artifact` from the ReportView (map
      filename/byte_size/mime_type/storage_path; the block's local `Artifact.id` can be
      `finalReportArtifactId`). When it is null, `setArtifact(null)`.
    - DELETE `maybeAutoDeliver` (:61-66) entirely (contradicts D-01).
    - `onPick` (:68-94): after a successful `uploadFile`, STAGE — `setStagedPath(res.data.path)` and
      `setStagedMeta({ filename: res.data.filename, size: res.data.size })`; toast
      `finalReport.reportUploaded`; do NOT call `onChange` or deliver. Nothing is client-visible yet.
    - Tighten the file input `accept=".pdf,.docx,.md,.txt"` (:211) → `accept=".pdf"` (D-10).
    - Add a Deliver affordance: when `stagedPath` is set AND not yet delivered, show a primary
      "Deliver" button that opens `<RecipientPicker open={deliverOpen} onOpenChange={setDeliverOpen}
      intakeId={intakeId} type="results" busy={delivering} onConfirm={onDeliverConfirm} />`.
      `onDeliverConfirm(membershipIds)`: set delivering; `const res = await deliverReport(intakeId,
      { storagePath: stagedPath, recipients: membershipIds })`; on `!res.success` toast
      `finalReport.deliverFailed` and return; on success toast `finalReport.delivered`, clear staged
      state, and `await onChange(res.data.final_report_artifact_id)` (the backend view is the source
      of truth — see Task 3 for the admin reload). Wrap in try/finally to clear `delivering` and
      close the dialog.
    - Pre-delivery swap/remove: keep an onRemove that clears the staged file locally (before delivery
      the staged object may be swapped freely — D-06; do NOT call the backend delete, the operator
      simply re-uploads). Uploading again while a staged file exists replaces the staged key.
    - Post-delivery Replace (D-04/D-05): when `isDelivered`, show a "Replace" button. Replacing =
      pick a new PDF (re-uses `onPick` → stages), then a Replace dialog reusing `RecipientPicker`
      (type="results") whose `onConfirm` calls `replaceReport(intakeId, { storagePath: stagedPath,
      recipients: membershipIds })`; ALSO offer a silent-replace path (confirm the file swap without
      re-notify → `replaceReport(..., { recipients: [] })`). Use `finalReport.replaceConfirm` /
      `finalReport.reNotify` copy.
    - Update the STALE comments at :55-56, :82-84, :100-101, :147-149 (remove "Phase 7+, not wired
      this milestone" / "scope ceiling stops at decomposed" — Phase 18 IS this milestone).

    Keep `try/finally` + `toast.error(t(...))` + `busy` conventions. The block still self-gates via
    `phaseShowsFinalReport(phase)` (now true for in_research after Task 1).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source contains `accept=".pdf"` and NO `.docx`/`.md`/`.txt` on the report input
    - Source contains NO `maybeAutoDeliver`
    - Source contains a `RecipientPicker` with `type="results"` and an `onConfirm` calling
      `deliverReport`
    - Source contains a `replaceReport` call gated behind `isDelivered`
    - The stale "not wired this milestone" / "scope ceiling stops at decomposed" comments are gone
    - `tsc --noEmit` passes
  </acceptance_criteria>
  <done>FinalReportBlock stages uploads, delivers via an explicit RecipientPicker-driven act, replaces post-delivery with optional re-notify, accepts only PDF, and carries no auto-deliver.</done>
</task>

<task type="auto">
  <name>Task 3: Admin route wiring reload + i18n delivery keys</name>
  <read_first>
    - frontend/src/routes/admin.pulse.intakes.$id.tsx (:69-89 local Intake type; :310-327 derivePhase
      call; :955-963 showFinalReport; :1457-1476 the FinalReportBlock mount + the onChange status fake)
    - frontend/src/lib/api/intakes.ts (getIntake — the seam reload source)
    - frontend/src/locales/nl/intake.json (:388 finalReport.* block; :340 recipients.*)
    - frontend/src/locales/fr/intake.json and en/intake.json (mirror targets)
  </read_first>
  <action>
    In `admin.pulse.intakes.$id.tsx`, replace the FinalReportBlock `onChange` (:1464-1473) — which
    fakes `status: ... ? "delivered" : intake.status` client-side — with a real reload: call
    `getIntake(intake.id)` and, on success, merge the returned seam view fields
    (`final_report_artifact_id`, `results_link_sent_at`, `status`) onto the local `intake` state so
    the phase machine advances from the BACKEND's authoritative view (the Deliver verb owns the
    transition now — 18-01). Keep the `onChange(artifactId)` signature (the block passes the new
    artifact id, but the reload is the source of truth; the artifactId is advisory). Do NOT keep the
    `intake.client_results_token`-gated fake-delivered branch. `hasResultsToken` prop can stay wired
    to `!!intake.client_results_token` (unused by the repaired block, harmless) or be dropped if the
    block's prop is removed — keep the block's existing prop surface to minimize churn.

    Add the delivery i18n keys to the `finalReport` block in ALL THREE locale files
    (`frontend/src/locales/{nl,fr,en}/intake.json`), authored per locale (NL is the parity source;
    FR/EN preserve the shape):
      `finalReport.deliver` (button, e.g. NL "Bezorg aan klant"),
      `finalReport.delivering` (NL "Bezorgen…"),
      `finalReport.delivered` (toast, NL "Rapport bezorgd"),
      `finalReport.deliverFailed` (NL "Bezorgen mislukt: {{error}}"),
      `finalReport.staged` (NL "Klaargezet — nog niet bezorgd"),
      `finalReport.stagedHint` (NL "Controleer het bestand en klik Bezorg om de klant te mailen."),
      `finalReport.replaceConfirm` (NL "Rapport vervangen door dit bestand?"),
      `finalReport.reNotify` (NL "Klant opnieuw op de hoogte brengen"),
      `finalReport.silentReplace` (NL "Vervang zonder mail").
    Keep existing keys (reportUploaded, uploadFailed, label, intro, replaceReport, view, remove,
    etc.) intact.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && node -e "['nl','fr','en'].forEach(l=>{const j=require('./frontend/src/locales/'+l+'/intake.json');['deliver','delivering','delivered','deliverFailed','staged','replaceConfirm','reNotify'].forEach(k=>{if(!j.finalReport||!(k in j.finalReport))throw new Error(l+' missing finalReport.'+k)})});console.log('i18n ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The admin FinalReportBlock onChange calls `getIntake(intake.id)` and merges the returned
      status/final_report_artifact_id/results_link_sent_at (no `? "delivered" :` client-side literal)
    - All three locale files carry `finalReport.deliver/delivering/delivered/deliverFailed/staged/
      replaceConfirm/reNotify`
    - `tsc --noEmit` passes and the i18n presence check prints "i18n ok"
  </acceptance_criteria>
  <done>Admin route reloads the intake from the backend view after deliver/replace; delivery i18n keys exist in NL/FR/EN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | The Deliver/Replace calls send a staged storage_path + membership ids; the server re-validates both (18-01) |
| admin UI state → phase machine | The delivered state must come from the backend view, never a client-side fake |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Spoofing (status) | Admin route fakes `delivered` client-side | mitigate | Replace the onChange status-fake with a `getIntake` reload — the backend Deliver verb is the sole authority (18-01) |
| T-18-09 | Tampering | Non-PDF picked in the report input | mitigate | `accept=".pdf"` (UX) + server-side PDF-only at Deliver (18-01, authoritative) |
| T-18-10 | Info Disclosure | Staged file client-visible before Deliver | mitigate | Upload only STAGES (local state); nothing calls onChange/deliver until the explicit Deliver act (D-01) |
| T-18-SC | Tampering | npm installs | mitigate | This plan installs NO package (uses existing RecipientPicker/shadcn/sonner) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes for all three tasks.
- Manual/live UAT (runbook 18-04): the FinalReportBlock appears during in_research, staging holds the
  file without client visibility, Deliver flips status + sends mail, Replace works post-delivery.
- No frontend test framework exists in this repo (RESEARCH § Wave 0) — FE gating is verified via the
  live UAT session in 18-04.
</verification>

<success_criteria>
- The admin can stage a PDF, review it, and explicitly Deliver (status → delivered + mail) from the
  in_research phase.
- Replace works after delivery with optional re-notify.
- Only `.pdf` is offered in the input; the backend enforces it authoritatively.
- The delivered state is read from the backend, not faked in the client.
</success_criteria>

<output>
Create `.planning/phases/18-human-report-upload-client-delivery/18-02-SUMMARY.md` when done.
</output>
