---
phase: 18-human-report-upload-client-delivery
plan: 04
type: execute
wave: 4
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - infra/DEPLOY-RUNBOOK.md
autonomous: false
requirements: [REPORT-01, REPORT-02, REPORT-03]

must_haves:
  truths:
    - "infra/DEPLOY-RUNBOOK.md has a § Phase 18 section covering the nestor-api REBUILD + frontend deploy, with NO migrate Job and NO new secret/env"
    - "The runbook enumerates a live UAT: stage → deliver → client sees + downloads → mail arrives (NL/FR/EN)"
    - "The operator runs the Cloud Build backend suite against the fresh image before the live proof"
    - "The runbook records the deploy-gap failure triage (deliver 404 = stale image)"
  artifacts:
    - path: "infra/DEPLOY-RUNBOOK.md"
      provides: "§ Phase 18 operator runbook (backend rebuild + frontend deploy + live UAT)"
      contains: "Phase 18"
  key_links:
    - from: "§ Phase 18 runbook"
      to: "gcloud builds submit backend + frontend cloudbuild"
      via: "image REBUILD of nestor-api + frontend deploy (no migrate Job)"
      pattern: "gcloud builds submit"
---

<objective>
Extend `infra/DEPLOY-RUNBOOK.md` with a `§ Phase 18` section — the operator-executed live session that
ships the report-delivery surface to production: a `nestor-api` image REBUILD (new deliver/replace/
report verbs) + a frontend image deploy (new report route + repaired admin block), explicitly with
NO `nestor-migrate` Job run and NO new secret/env (RESEARCH: columns/enum pre-exist, reuse
`results_link_sent_at`, reuse `RESEND_API_KEY`/`APP_BASE_URL`). Then gate the live proof behind a
blocking checkpoint: the operator runs the Cloud Build backend suite against the fresh image, deploys
both images, and executes the stage → deliver → client-download → mail UAT.

Purpose: Nothing is real until the IMAGE is deployed (the recurring project deploy-gap). This phase's
Python + frontend both need rebuilds; a config-only flip ships neither. The live UAT closes REPORT-01/
02/03 end-to-end.
Output: the runbook section + an executed live session recorded in an 18-HUMAN-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-human-report-upload-client-delivery/18-CONTEXT.md
@.planning/phases/18-human-report-upload-client-delivery/18-RESEARCH.md
@.planning/phases/18-human-report-upload-client-delivery/18-01-SUMMARY.md
@.planning/phases/18-human-report-upload-client-delivery/18-02-SUMMARY.md
@.planning/phases/18-human-report-upload-client-delivery/18-03-SUMMARY.md

<interfaces>
<!-- Runbook idioms to mirror. -->

Shared exports (§ Phase 17 :1327-1333): GOOGLE_PROJECT (acct tools@dotto.be / tools@epicimpact.be),
REGION=europe-west1, INSTANCE_NAME=nestor-pg, INTAKE_SA=nestor-run@${GOOGLE_PROJECT}.iam...

Backend rebuild idiom (§ Phase 16 Step 16.a / § Phase 17 Step 17.b :1386-1394):
  export IMAGE="${REGION}-docker.pkg.dev/${GOOGLE_PROJECT}/nestor/backend:$(date +%Y%m%d-%H%M%S)"
  gcloud builds submit backend --tag "$IMAGE" --project="$GOOGLE_PROJECT"
  gcloud run services update nestor-api --region "$REGION" --project="$GOOGLE_PROJECT" --image "$IMAGE"

Cloud Build test suite (§ Phase 17 :1404-1405): from REPO ROOT (source `.`, never `backend`):
  gcloud builds submit . --config=cloudbuild.test.yaml --project="$GOOGLE_PROJECT"

Frontend deploy idiom (§ frontend :581-584): gcloud builds submit frontend --config=frontend/
cloudbuild.yaml --substitutions=_IMAGE=...,_API_BASE_URL=<live nestor-api origin>,_FB_API_KEY=...,
_FB_AUTH_DOMAIN=...,_FB_PROJECT_ID=... (`gcloud builds submit --tag` cannot inject build-args, so the
frontend uses its config + substitutions).

IaC-DRIFT reality (carry-over, read-first every phase): Terraform state never adopted;
`terraform apply` FORBIDDEN (rotates DB passwords, downs services). Every step is manual gcloud in
Cloud Shell; images built via Cloud Build (dev box has no Docker).

NO migration this phase (RESEARCH A1 / Runtime State Inventory): final_report_artifact_id,
results_link_sent_at, the `delivered` enum, and research_artifacts all pre-exist (migration 0001).
The `nestor-migrate` Job need NOT run. NO new secret/env: RESEND_API_KEY / APP_BASE_URL /
NESTOR_ADMIN_EMAIL already set (the delivery mail reuses the same guard — refuse-send if APP_BASE_URL
unset). The tribunal-api / tribunal-worker images are UNCHANGED this phase (no Tribunal rebuild).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the § Phase 18 runbook section</name>
  <read_first>
    - infra/DEPLOY-RUNBOOK.md (§ Phase 16 :1131-1289 and § Phase 17 :1292-1408 — the section
      structure, shared-exports block, the backend-rebuild + Cloud-Build-suite + failure-triage
      idioms, and the frontend deploy block around :581)
    - .planning/phases/18-human-report-upload-client-delivery/18-01-SUMMARY.md,
      18-02-SUMMARY.md, 18-03-SUMMARY.md (what shipped — the exact endpoint paths + routes for the
      failure-triage lines)
    - .planning/phases/18-human-report-upload-client-delivery/18-RESEARCH.md (§ Runtime State
      Inventory — the no-migration / no-secret confirmations)
  </read_first>
  <action>
    Append a `## Phase 18 — Human report upload + client delivery (nestor-api REBUILD + frontend
    deploy, NO migrate, NO new secret)` section to `infra/DEPLOY-RUNBOOK.md`, modeled on § Phase 17
    but SIMPLER (single backend deployable + frontend; no Tribunal rebuild, no migration). Include:
    - A read-first callout: the recurring deploy-gap ("a config-only env flip ships a STALE image") —
      Phase 18 adds new Python verbs to `nestor-api` AND new frontend routes, so BOTH need Cloud Build
      REBUILDs; state explicitly there is NO migration and NO new secret/env this phase (reuse
      `results_link_sent_at` + `RESEND_API_KEY`/`APP_BASE_URL`; the `delivered` enum pre-exists).
    - The IaC-DRIFT carry-over callout (terraform apply FORBIDDEN; Cloud Build only).
    - The shared-exports block (copy § Phase 17 :1327-1333).
    - Step 18.a — REBUILD + deploy the `nestor-api` image via Cloud Build (bakes the deliver/replace/
      report verbs). Use the § Phase 17 Step-17.b backend idiom verbatim (IMAGE tag with date; `gcloud
      builds submit backend --tag "$IMAGE"`; `gcloud run services update nestor-api --image "$IMAGE"`).
    - Step 18.b — Run the intake backend suite in Cloud Build against the fresh image BEFORE the live
      proof: `gcloud builds submit . --config=cloudbuild.test.yaml` (from repo root), calling out the
      new files `test_report_delivery.py` + the extended `test_intake_cross_tenant.py` deliver/report
      cases (authored by-construction, deferred to Cloud Build — dev box has no Python).
    - Step 18.c — Deploy the frontend image (new `/intake/$id/report` route + repaired
      FinalReportBlock) via the § frontend Cloud Build idiom (`gcloud builds submit frontend
      --config=frontend/cloudbuild.yaml --substitutions=...`), reusing the live `_API_BASE_URL` +
      Firebase substitutions from the prior frontend deploy. Explicitly: NO `nestor-migrate` Job run
      this phase (state why — no migration landed).
    - Step 18.d — Confirm the mail env is present (READ-ONLY): `gcloud run services describe nestor-api
      ... | grep -E 'APP_BASE_URL|NESTOR_ADMIN_EMAIL'` and note RESEND_API_KEY secret is bound (the
      delivery mail refuses-send if APP_BASE_URL is unset). NOTE the CLOSE-02 chore: rotate the Resend
      key post-UAT (do not rotate here).
    - Step 18.e — Live UAT (points to 18-HUMAN-UAT.md — see Task 2). Enumerate the steps:
      1. On an `in_research` smoke intake (a completed research run, or manually set to in_research),
         open the admin detail — the FinalReportBlock shows the staged-upload UI.
      2. Upload a real PDF (crafted in Claude Design from the Phase-17 bundle's report.md) → it STAGES;
         confirm status stays `in_research` and a CLIENT login shows NO report (REPORT-02).
      3. Click Deliver → RecipientPicker → confirm → status flips to `delivered`; the delivery email
         arrives at the recipient inbox (check NL/FR/EN per the recipient's locale); the CTA deep-links
         to `/intake/{id}/report`.
      4. Log in as a CLIENT member → the intake list shows "View report" → open the page → download
         and open the PDF (signed-URL attachment).
      5. Replace the report with a corrected PDF (status stays `delivered`); verify the client gets the
         newest file; test the silent-replace and the optional re-notify paths.
      6. Record the intake id, recipient(s), locales seen, and download success in 18-HUMAN-UAT.md.
    - A Failure triage block: `POST /intakes/{id}/deliver` **404** → Step-18.a rebuild skipped (stale
      image, recurring deploy-gap). A 500 `AttributeError`/`ModuleNotFound` on deliver → same. The
      `/intake/{id}/report` route 404s in the browser → Step-18.c frontend deploy skipped or
      routeTree.gen.ts not regenerated. The client sees the report before delivery → the front gate or
      the backend status-gate regressed (REPORT-02 — treat as blocking). Mail never arrives → check
      APP_BASE_URL/RESEND_API_KEY (Step 18.d) and the nestor-api logs for the refuse-send warning.
  </action>
  <verify>
    <automated>grep -q "Phase 18" infra/DEPLOY-RUNBOOK.md && grep -q "gcloud builds submit backend" infra/DEPLOY-RUNBOOK.md && grep -qi "no migrate\|NO migration\|no.*migrate Job" infra/DEPLOY-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `infra/DEPLOY-RUNBOOK.md` contains a `## Phase 18 —` heading with Steps 18.a–18.e
    - The section contains the backend `gcloud builds submit backend --tag` rebuild, the
      `cloudbuild.test.yaml` suite run, and the frontend `gcloud builds submit frontend` deploy
    - The section states explicitly there is NO migrate Job run and NO new secret/env this phase
    - A failure-triage block maps `deliver 404` → stale image and `report route 404` → frontend
      deploy/routeTree gap
  </acceptance_criteria>
  <done>The § Phase 18 runbook section is complete, mirrors the § Phase 17 idioms, and correctly omits the migrate Job + new secrets.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator live deploy + stage/deliver/download/mail UAT</name>
  <action>
    BLOCKING human-action checkpoint (dev box has no Python/Docker — deploy + real mail + browser
    download are operator-only). Execute `infra/DEPLOY-RUNBOOK.md § Phase 18` Steps 18.a–18.e in
    Cloud Shell: rebuild + deploy nestor-api, run the Cloud Build backend suite, deploy the frontend,
    confirm the mail env, then run the stage → deliver → client-download → mail → replace UAT. Record
    the outcome (PASS/FAIL per REPORT-01/02/03, intake id, recipient locales, download result,
    defects) in 18-HUMAN-UAT.md. See <how-to-verify> for the enumerated steps.
  </action>
  <what-built>
    18-01 backend verbs (deliver/replace/report), 18-02 repaired admin FinalReportBlock, 18-03 client
    report route + list CTA, and the § Phase 18 runbook (Task 1). All authored by-construction; the
    dev box has no Python/Docker, so the Cloud Build suite run + the live deploy + the mail/download
    UAT are operator-only steps (mirroring § Phase 16/17 live sessions).
  </what-built>
  <how-to-verify>
    Follow `infra/DEPLOY-RUNBOOK.md § Phase 18` end to end in Cloud Shell (acct with project access):
    1. Step 18.a — REBUILD + deploy `nestor-api`.
    2. Step 18.b — Run `gcloud builds submit . --config=cloudbuild.test.yaml` and confirm
       `test_report_delivery.py` + the new `test_intake_cross_tenant.py` deliver/report cases pass.
    3. Step 18.c — Deploy the frontend image.
    4. Step 18.d — Confirm APP_BASE_URL / NESTOR_ADMIN_EMAIL / RESEND_API_KEY are present.
    5. Step 18.e — Run the live UAT: stage a PDF (status stays in_research, client sees nothing) →
       Deliver (status → delivered, mail arrives, CTA → /intake/{id}/report) → client logs in, sees
       "View report", downloads and opens the PDF → Replace (silent + re-notify).
    Record everything in a new `.planning/phases/18-human-report-upload-client-delivery/18-HUMAN-UAT.md`
    (PASS/FAIL per REPORT-01/02/03, the intake id, recipient locales, download result, and any defects).
    If Anthropic credits or a completed research run are needed to reach `in_research`, an intake can be
    set to `in_research` directly for this UAT (the report flow does not require a real run to test).
  </how-to-verify>
  <resume-signal>Type "delivered" (with the UAT recorded) or describe any defects found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → GCP (Cloud Shell) | Deploy actions; image provenance is the Cloud Build build |
| deployed image → prod | Only the freshly-built image carries the new verbs/routes (deploy-gap) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Repudiation / Availability | Stale image shipped (config-only flip) | mitigate | Steps 18.a + 18.c are mandatory Cloud Build REBUILDs; failure-triage maps deliver/report 404 → stale image |
| T-18-15 | Information Disclosure | REPORT-02 regression reaches prod | mitigate | Step 18.e UAT explicitly checks a client sees nothing pre-delivery; treated as blocking if it fails |
| T-18-16 | Tampering | Wrong-file delivered to client | mitigate (by design) | Staging + explicit Deliver (D-01) — the operator reviews the staged PDF before Deliver in Step 18.e |
| T-18-SC | Tampering | pip/npm installs at deploy | mitigate | No new package this phase (RESEARCH: no install surface); Cloud Build uses the existing manifests |
</threat_model>

<verification>
- `grep` confirms the § Phase 18 section, the backend/frontend build commands, and the no-migrate note.
- The checkpoint is BLOCKING and human-action (live deploy + real mail + browser download cannot be
  automated on the dev box); it resumes only on operator sign-off with 18-HUMAN-UAT.md recorded.
</verification>

<success_criteria>
- The runbook section ships nestor-api + frontend via Cloud Build with no migrate Job / no new secret.
- The operator has proven REPORT-01/02/03 live: stage → deliver → client download → mail, plus replace.
</success_criteria>

<output>
Create `.planning/phases/18-human-report-upload-client-delivery/18-04-SUMMARY.md` when done, and
`.planning/phases/18-human-report-upload-client-delivery/18-HUMAN-UAT.md` from the live session.
</output>
